# Defaults are sensible — keep this file as a hook for project-specific overrides.
# Example: raise the allowed function complexity before clippy::cognitive_complexity fires.
# cognitive-complexity-threshold = 25
