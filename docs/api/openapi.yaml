openapi: 3.0.3
info:
  title: Agent Hub REST API
  version: 2.31.114
  description: |-
    REST API for **Agent Hub** — a full-stack platform for managing AI
    agent sessions, kanban boards, per-project wikis, heartbeats, and
    cron jobs.

    ### Authentication
    Most endpoints require one of:
    - A session cookie issued by `POST /api/auth/login`.
    - A user-owned API key minted via `POST /api/auth/keys`, sent as
      `Authorization: Bearer ahub_…` or `X-API-Key: ahub_…`.
    - The break-glass instance API key (`X-API-Key`), set via
      `~/.agent-hub/data/config.json` `apiKey`. Treated as Owner across
      every org — use only for automation / sub-agent spawn.

    ### Realtime
    Live updates (chat streaming, kanban / wiki events, cron output)
    are delivered over WebSocket on the same port as HTTP. This
    document covers the REST surface only.
  contact:
    name: Agent Hub
    url: https://github.com/Speakman-ai/agent-hub
  license:
    name: PolyForm Noncommercial License 1.0.0
    url: https://github.com/Speakman-ai/agent-hub/blob/main/docs/licensing.md
servers:
  - url: http://localhost:3051
    description: Local development server
  - url: https://hub.example.com
    description: Self-hosted production deployment (operator-defined)
security:
  - bearerAuth: []
  - apiKeyHeader: []
  - cookieAuth: []
tags:
  - name: Health
    description: Liveness and runtime info.
  - name: Auth
    description: Sign-in, API keys, per-user credential management.
  - name: Google
    description: Per-user Google Workspace proxy endpoints.
  - name: Projects
    description: Project CRUD and metadata.
  - name: Agents
    description: Agent configuration under a project.
  - name: Sessions
    description: Chat sessions and message history.
  - name: Board
    description: Per-project kanban boards (columns, cards, epics, comments).
  - name: Wiki
    description: Per-project wiki with FTS5 + semantic search.
  - name: Crons
    description: Project-scoped automated jobs.
  - name: Jobs
    description: Host-wide background job queue admin surface.
  - name: Admin
    description: Owner-only administrative endpoints.
components:
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
      bearerFormat: ahub_...
      description: "User-owned API key minted via `POST /api/auth/keys`. Send as `Authorization: Bearer ahub_...`."
    apiKeyHeader:
      type: apiKey
      in: header
      name: X-API-Key
      description: Either a per-user `ahub_*` key or the break-glass instance API key from `~/.agent-hub/data/config.json` (`apiKey`).
    cookieAuth:
      type: apiKey
      in: cookie
      name: agent_hub_session
      description: Session cookie issued by `POST /api/auth/login`.
    infraHealthIngestToken:
      type: http
      scheme: bearer
      description: Write-only `ahhealth_` AWS Health ingest token (also accepted via `X-AgentHub-Health-Token`). Identifies exactly one project; grants no read, query, or management access.
    logIngestToken:
      type: http
      scheme: bearer
      description: Write-only `ahlog_` log ingest token (also accepted via `X-AgentHub-Log-Token`). Identifies exactly one (project, source); grants no read/query/management access.
  schemas:
    Agent:
      type: object
      properties:
        id:
          type: string
        name:
          type: string
        engine:
          type: string
        model:
          type: string
        systemPrompt:
          type: string
        color:
          type: string
        avatar:
          type: string
        role:
          type: string
        reviewer:
          type: string
        canReview:
          type: boolean
        isDev:
          type: boolean
          description: Whether this agent accepts autonomously-dispatched kanban tickets. Default Dev roles (dev/lead) are always eligible and out-of-band roles (docs/reviewer) never are, regardless of this field; for other agents an explicit `false` opts out and `undefined` preserves pre-flag eligibility.
        active:
          type: boolean
        browserToolsEnabled:
          type: boolean
        browserViewportWidth:
          type: integer
        browserViewportHeight:
          type: integer
        browserPageLoadTimeoutMs:
          type: integer
        heartbeat:
          type: object
          properties:
            enabled:
              type: boolean
            interval:
              type: string
            prompt:
              type: string
          required:
            - enabled
            - interval
            - prompt
      required:
        - id
        - name
        - engine
      description: An agent. Many additional optional fields exist on the row (hooks, …) — only the stable, documented surface is enumerated here.
    AgentEnriched:
      allOf:
        - $ref: "#/components/schemas/Agent"
        - type: object
          properties:
            lastActivity:
              type: string
              nullable: true
            lastMessage:
              type: object
              nullable: true
              properties:
                role:
                  type: string
                content:
                  type: string
                created_at:
                  type: string
              required:
                - role
                - content
                - created_at
      description: Agent shape + the most-recent session timestamp and a truncated last message.
    AgentsBulkEngineResponse:
      type: object
      properties:
        updated:
          type: integer
        engine:
          type: string
        model:
          type: string
      required:
        - updated
        - engine
        - model
      description: Result of POST /api/agents/bulk-engine.
    AgentErrorResponse:
      type: object
      properties:
        error:
          type: string
        details:
          type: array
          items:
            type: object
            properties:
              path:
                type: array
                items:
                  anyOf:
                    - type: string
                    - type: number
              message:
                type: string
            required:
              - path
              - message
      required:
        - error
      description: Error envelope. `details` is populated for 400 schema-validation errors with one entry per failing Zod issue.
    Artifact:
      type: object
      properties:
        id:
          type: string
        sessionId:
          type: string
        filename:
          type: string
        contentType:
          type: string
        size:
          type: integer
        storageKind:
          type: string
          enum:
            - local
            - s3
        createdBy:
          type: string
          nullable: true
        createdAt:
          type: string
        url:
          type: string
          description: Relative URL to download/view the artifact bytes.
      required:
        - id
        - sessionId
        - filename
        - contentType
        - size
        - storageKind
        - createdBy
        - createdAt
        - url
      description: Metadata for one session artifact.
    ArtifactList:
      type: object
      properties:
        artifacts:
          type: array
          items:
            $ref: "#/components/schemas/Artifact"
      required:
        - artifacts
      description: All artifacts generated in a session, newest first.
    ArtifactErrorResponse:
      type: object
      properties:
        error:
          type: string
      required:
        - error
      description: Error envelope.
    ErrorResponse:
      type: object
      properties:
        error:
          type: string
          description: Human-readable error message.
        code:
          type: string
      required:
        - error
      description: Standard error envelope used across the API.
    ZodIssue:
      type: object
      properties:
        path:
          type: array
          items:
            anyOf:
              - type: string
              - type: number
        message:
          type: string
        code:
          type: string
      required:
        - path
        - message
      description: A single Zod validation issue. Returned on 400 responses caused by request-body shape errors.
    ZodErrorResponse:
      type: object
      properties:
        error:
          type: string
        issues:
          type: array
          items:
            $ref: "#/components/schemas/ZodIssue"
      required:
        - error
      description: A 400 response produced by Zod body/query validation. `issues` lists each field that failed.
    UserSummary:
      type: object
      properties:
        id:
          type: string
          nullable: true
        email:
          type: string
          nullable: true
        username:
          type: string
        needsEmailUpdate:
          type: boolean
        role:
          type: string
          enum:
            - Owner
            - Admin
            - User
        createdAt:
          anyOf:
            - type: string
            - type: number
            - nullable: true
        mfaEnabled:
          type: boolean
      required:
        - email
        - role
    TokenResponse:
      type: object
      properties:
        token:
          type: string
        expiresAt:
          type: string
        user:
          type: object
          properties:
            id:
              type: string
            email:
              type: string
              nullable: true
            needsEmailUpdate:
              type: boolean
            role:
              type: string
              enum:
                - Owner
                - Admin
                - User
          required:
            - email
            - role
      required:
        - token
        - expiresAt
        - user
    MfaRequiredResponse:
      type: object
      properties:
        mfaRequired:
          type: boolean
          enum:
            - true
        challengeId:
          type: string
        expiresAt:
          type: string
        user:
          type: object
          properties:
            id:
              type: string
            email:
              type: string
              nullable: true
            needsEmailUpdate:
              type: boolean
            role:
              type: string
              enum:
                - Owner
                - Admin
                - User
          required:
            - email
            - role
      required:
        - mfaRequired
        - challengeId
        - expiresAt
        - user
    AuthSetupBody:
      anyOf:
        - type: object
          properties:
            email:
              type: string
            username:
              type: string
            password:
              type: string
          required:
            - email
            - password
        - type: object
          properties:
            username:
              type: string
            email:
              type: string
            password:
              type: string
          required:
            - username
            - password
      description: "`email` is canonical. `username` is accepted for compatibility. One identifier is required."
    AuthLoginBody:
      anyOf:
        - type: object
          properties:
            email:
              type: string
            username:
              type: string
            password:
              type: string
          required:
            - email
            - password
        - type: object
          properties:
            username:
              type: string
            email:
              type: string
            password:
              type: string
          required:
            - username
            - password
      description: "`email` is canonical. `username` is accepted for legacy non-email account login. One identifier is required."
    AuthMfaCodeBody:
      type: object
      properties:
        code:
          type: string
      required:
        - code
    AuthMfaLoginBody:
      type: object
      properties:
        challengeId:
          type: string
        code:
          type: string
      required:
        - challengeId
        - code
    AuthCreateUserBody:
      anyOf:
        - type: object
          properties:
            email:
              type: string
            username:
              type: string
            password:
              type: string
            role:
              type: string
              enum:
                - Owner
                - Admin
                - User
            projectIds:
              type: array
              items:
                type: string
                minLength: 1
              description: Optional project ids to assign the new user to on creation. Each must be a non-empty string and becomes a project-member (visibility) assignment. Empty strings and unknown/inaccessible ids are rejected (not silently dropped).
          required:
            - email
            - password
        - type: object
          properties:
            username:
              type: string
            email:
              type: string
            password:
              type: string
            role:
              type: string
              enum:
                - Owner
                - Admin
                - User
            projectIds:
              type: array
              items:
                type: string
                minLength: 1
              description: Optional project ids to assign the new user to on creation. Each must be a non-empty string and becomes a project-member (visibility) assignment. Empty strings and unknown/inaccessible ids are rejected (not silently dropped).
          required:
            - username
            - password
      description: "`email` is canonical. `username` is accepted for compatibility. One identifier is required."
    AuthUpdateEmailBody:
      type: object
      properties:
        email:
          type: string
      required:
        - email
    AuthUpdateUserRoleBody:
      type: object
      properties:
        role:
          type: string
          enum:
            - Owner
            - Admin
            - User
      required:
        - role
    AuthPasswordResetBody:
      type: object
      properties:
        newPassword:
          type: string
      required:
        - newPassword
    AuthForgotPasswordBody:
      type: object
      properties:
        email:
          type: string
      required:
        - email
    AuthResetPasswordBody:
      type: object
      properties:
        token:
          type: string
        newPassword:
          type: string
      required:
        - token
        - newPassword
    CreateApiKeyBody:
      type: object
      properties:
        name:
          type: string
        expiresInDays:
          anyOf:
            - type: number
            - type: string
            - nullable: true
          description: 1–3650 days, or null/omitted to never expire. Strings are coerced to numbers.
      required:
        - name
    CreateInviteBody:
      type: object
      properties:
        role:
          type: string
          enum:
            - Admin
            - User
        email:
          type: string
          nullable: true
        ttlHours:
          type: number
        projectIds:
          type: array
          items:
            type: string
            minLength: 1
          description: Optional project ids to assign the invited user to on acceptance. Each must be a non-empty string. Requires the issuer to be an Owner (project-member assignment is an Owner-only ACL); each id is validated against the issuer’s visible project set. Empty strings and unknown/inaccessible ids are rejected (not silently dropped).
    AcceptInviteBody:
      anyOf:
        - type: object
          properties:
            email:
              type: string
            username:
              type: string
            password:
              type: string
          required:
            - email
            - password
        - type: object
          properties:
            username:
              type: string
            email:
              type: string
            password:
              type: string
          required:
            - username
            - password
      description: "`email` is canonical. `username` is accepted for compatibility. One identifier is required."
    UpdateClaudeAuthBody:
      type: object
      properties:
        anthropicApiKey:
          type: string
          nullable: true
        claudeCodeOAuthToken:
          type: string
          nullable: true
        claudeCodeOAuthExpiresAt:
          type: string
          nullable: true
      description: Per-user Claude credentials. Each field is whitelisted before reaching the DB — unknown keys are silently dropped.
    UpdateSingleKeyAuthBody:
      type: object
      properties:
        apiKey:
          type: string
          nullable: true
      description: Per-user single-key engine credentials (Cursor / Gemini / Codex).
    UpsertSkillCredentialBody:
      type: object
      properties:
        skill_id:
          type: string
        key_name:
          type: string
        agent_id:
          type: string
        value:
          type: string
      required:
        - skill_id
        - key_name
      description: "Upsert a per-user skill credential. `agent_id` is optional: omit it when setting a personal credential for a bundled default skill from the Account page."
    UpsertSkillOptionBody:
      type: object
      properties:
        skill_id:
          type: string
        option_name:
          type: string
        agent_id:
          type: string
        value:
          type: string
      required:
        - skill_id
        - option_name
        - value
      description: Select a per-user, non-secret skill option (owner-curated enum, e.g. dev/prod). `value` must be one of the option's declared choices.
    AgentEngineOverrideEntry:
      type: object
      properties:
        engine:
          type: string
          minLength: 1
        model:
          type: string
          minLength: 1
      required:
        - engine
      description: A per-agent engine override on the caller. The optional `model` further pins a specific CLI model id within that engine; when omitted the per-engine default (or hub fallback) is used.
    PutAgentEngineOverridesBody:
      type: object
      properties:
        agentEngineOverrides:
          type: object
          additionalProperties:
            $ref: "#/components/schemas/AgentEngineOverrideEntry"
      required:
        - agentEngineOverrides
    PutAgentModelOverridesBody:
      type: object
      properties:
        agentModelOverrides:
          type: object
          additionalProperties:
            type: string
      required:
        - agentModelOverrides
      description: 'Caller-scoped map of agentId → model id — the per-user "default model" picked from the agent / reviewer model dropdown. Pass `{ agentModelOverrides: {} }` to clear. Each model must be valid for some configured engine; pass an empty string to drop a single agent entry.'
    PutAgentModelOverrideEntryBody:
      type: object
      properties:
        model:
          type: string
          minLength: 1
      required:
        - model
      description: Set the caller's per-user default model for a single agent (path `:agentId`). Merges server-side, so it never disturbs other agents' picks or another tab's concurrent edit. Use DELETE on the same path to clear.
    SidebarCollapsedProjectsResponse:
      type: object
      properties:
        sidebarCollapsedProjects:
          type: array
          items:
            type: string
      required:
        - sidebarCollapsedProjects
      description: Project ids the caller has collapsed in the sidebar project list. Caller-scoped, so the same account sees the same collapsed projects on web, mobile, and Electron.
    PutSidebarCollapsedProjectBody:
      type: object
      properties:
        collapsed:
          type: boolean
      required:
        - collapsed
      description: Collapse (`true`) or expand (`false`) a single project in the caller's sidebar. Merges server-side so a toggle never clobbers another tab's concurrent edit.
    ClaudeLoginBody:
      type: object
      properties:
        method:
          type: string
        email:
          type: string
        sso:
          type: boolean
    ClaudeCallbackBody:
      type: object
      properties:
        code:
          type: string
          description: OAuth authorization code from Anthropic.
      required:
        - code
    ApiKeyOnlyBody:
      type: object
      properties:
        apiKey:
          type: string
          nullable: true
    ApiKeyRequiredBody:
      type: object
      properties:
        apiKey:
          type: string
          minLength: 1
      required:
        - apiKey
    OauthTokenBody:
      type: object
      properties:
        oauthToken:
          type: string
          nullable: true
    GithubConnectTokenBody:
      type: object
      properties:
        token:
          type: string
          minLength: 1
      required:
        - token
    BackgroundShell:
      type: object
      properties:
        id:
          type: string
        session_id:
          type: string
        project_id:
          type: string
        command:
          type: string
        label:
          type: string
          nullable: true
        cwd:
          type: string
          nullable: true
        pid:
          type: integer
          nullable: true
        pid_start_time:
          type: string
          nullable: true
        status:
          type: string
          enum:
            - running
            - exited
            - failed
            - stopped
            - timed_out
        exit_code:
          type: integer
          nullable: true
        log_path:
          type: string
          nullable: true
        watch:
          type: integer
          description: 1 while the watch loop should wake this shell’s session when it finishes.
        watch_resolved_at:
          type: string
          nullable: true
        timeout_ms:
          type: integer
          description: Wall-clock cap in milliseconds. The Hub stops the process group when it fires (default and max 30 minutes).
        created_at:
          type: string
        updated_at:
          type: string
      required:
        - id
        - session_id
        - project_id
        - command
        - label
        - cwd
        - pid
        - pid_start_time
        - status
        - exit_code
        - log_path
        - watch
        - watch_resolved_at
        - timeout_ms
        - created_at
        - updated_at
      description: A Hub-owned background shell — a long-running command that survives across chat turns.
    ReviewerThread:
      type: object
      properties:
        id:
          type: string
        run_id:
          type: string
        file_path:
          type: string
        line_start:
          type: integer
          nullable: true
        line_end:
          type: integer
          nullable: true
        body:
          type: string
        author:
          type: string
        created_at:
          type: integer
      required:
        - id
        - run_id
        - file_path
        - line_start
        - line_end
        - body
        - author
        - created_at
      description: One diff-anchored finding produced by the cold-eye reviewer agent during the review phase of a Finalize run.
    ReviewerThreadsResponse:
      type: object
      properties:
        run_id:
          type: string
        reviewer_verdict:
          type: string
          nullable: true
          enum:
            - approved
            - changes_requested
            - null
          description: Reviewer verdict for the run (`approved` / `changes_requested`); `null` while the review phase is still pending.
        threads:
          type: array
          items:
            $ref: "#/components/schemas/ReviewerThread"
      required:
        - run_id
        - reviewer_verdict
        - threads
      description: All reviewer threads for a finalize run, ordered by `file_path ASC, line_start ASC, created_at ASC` so the side-panel can group by file without re-sorting.
    FinalizeRun:
      type: object
      properties:
        id:
          type: string
        card_id:
          type: string
        session_id:
          type: string
          nullable: true
        project_id:
          type: string
        branch:
          type: string
        head_sha:
          type: string
        idempotency_key:
          type: string
        status:
          type: string
          enum:
            - queued
            - rebasing
            - reviewing
            - running
            - dispatching
            - pushing
            - ready_to_push
            - pushed
            - failed
            - timed_out
            - infra_error
            - cancelled
            - stalled_no_response
        phase:
          type: string
          nullable: true
          enum:
            - rebase
            - review
            - tasks
            - dispatching
            - push
            - null
        trigger_source:
          type: string
          enum:
            - ui_button
            - agent_block
        worktree_path:
          type: string
          nullable: true
        triggered_by_user_id:
          type: string
        author_name:
          type: string
        author_email:
          type: string
        reviewer_verdict:
          type: string
          nullable: true
          enum:
            - approved
            - changes_requested
            - null
        failure_reason:
          type: string
          nullable: true
        failed_step_index:
          type: integer
          nullable: true
        failed_step_name:
          type: string
          nullable: true
        failed_step_exit_code:
          type: integer
          nullable: true
        retry_of_run_id:
          type: string
          nullable: true
        active_seconds_consumed:
          type: integer
        started_at:
          type: integer
        ended_at:
          type: integer
          nullable: true
        pr_url:
          type: string
          nullable: true
      required:
        - id
        - card_id
        - session_id
        - project_id
        - branch
        - head_sha
        - idempotency_key
        - status
        - phase
        - trigger_source
        - worktree_path
        - triggered_by_user_id
        - author_name
        - author_email
        - reviewer_verdict
        - failure_reason
        - failed_step_index
        - failed_step_name
        - failed_step_exit_code
        - retry_of_run_id
        - active_seconds_consumed
        - started_at
        - ended_at
        - pr_url
      description: A single finalize_runs row.
    FinalizeRunStep:
      type: object
      properties:
        index:
          type: integer
        name:
          type: string
        state:
          type: string
          enum:
            - queued
            - running
            - passed
            - failed
            - skipped
        exitCode:
          type: integer
          nullable: true
        startedAt:
          type: integer
          nullable: true
        endedAt:
          type: integer
          nullable: true
      required:
        - index
        - name
        - state
        - exitCode
        - startedAt
        - endedAt
      description: One CI step row from a Finalize run.
    FinalizePhaseSummary:
      type: object
      properties:
        run_id:
          type: string
        status:
          type: string
        mode:
          type: string
          enum:
            - full
            - checks
            - review
        head_sha:
          type: string
        validated_head_sha:
          type: string
          nullable: true
        ended_at:
          type: integer
          nullable: true
      required:
        - run_id
        - status
        - mode
        - head_sha
        - validated_head_sha
        - ended_at
      description: Done-state for one Finalize phase (checks or review). Resolved from the latest run that exercised that phase — either a phase-scoped run or a combined `full` run.
    LatestFinalizeRunForSessionResponse:
      type: object
      properties:
        run:
          allOf:
            - $ref: "#/components/schemas/FinalizeRun"
            - nullable: true
              description: "`null` when the session has never triggered a Finalize run."
        steps:
          type: array
          items:
            $ref: "#/components/schemas/FinalizeRunStep"
          description: Persisted CI step states for the latest run; empty when no run.
        currentHeadSha:
          type: string
          nullable: true
          description: The session worktree's live `git rev-parse HEAD` at request time, or `null` when the worktree is missing or HEAD could not be resolved.
        stale:
          type: boolean
          description: True when the latest run's `head_sha` no longer matches the worktree's current HEAD — i.e. the agent committed new work after this run finished, so the run's results predate the current code. Clients (and `finalize.sh latest`) should surface this and trigger a fresh run rather than acting on the stale results. Fail-safe `false` when HEAD cannot be resolved.
        phases:
          type: object
          properties:
            checks:
              allOf:
                - $ref: "#/components/schemas/FinalizePhaseSummary"
                - nullable: true
            review:
              allOf:
                - $ref: "#/components/schemas/FinalizePhaseSummary"
                - nullable: true
          required:
            - checks
            - review
          description: Independent per-phase done-state powering the split "Run Tests" / "Reviewer" buttons. Each is `null` until that phase has run for the session.
      required:
        - run
        - steps
        - currentHeadSha
        - stale
        - phases
      description: Latest Finalize run for a given session.
    FinalizeErrorResponse:
      type: object
      properties:
        error:
          type: string
      required:
        - error
      description: Error envelope for finalize routes.
    StartFinalizeRunRequest:
      type: object
      properties:
        mode:
          type: string
          enum:
            - full
            - checks
            - review
          description: Which phases to run. `full` (default — the one Finalize button) = rebase + reviewer + checks. `checks` / `review` are legacy single-phase modes retained for back-compat and automation; the UI only sends `full`. Folded into the idempotency key so historical single-phase rows keep their own keys.
      description: Optional `mode` selects which phases run (defaults to `full`). The rest of the run is keyed off the card, its linked session, and the resolved HEAD SHA.
    StartFinalizeRunResponse:
      type: object
      properties:
        run_id:
          type: string
          nullable: true
          description: The `finalize_runs.id` for this run. `null` only when the run started but the row was not visible within the synchronous poll window (the 202 fallback).
        status:
          type: string
          enum:
            - queued
            - rebasing
            - reviewing
            - running
            - dispatching
            - pushing
            - ready_to_push
            - pushed
            - failed
            - timed_out
            - infra_error
            - cancelled
            - stalled_no_response
          description: Status of the run at response time — `queued` for fresh starts.
        reused:
          type: boolean
          description: True when the idempotency key matched an existing terminal row and that row was returned as-is.
      required:
        - run_id
        - status
        - reused
      description: A new or reused Finalize run.
    StartFinalizeRunPending:
      type: object
      properties:
        ok:
          type: boolean
          enum:
            - true
          description: Acknowledgement marker for the 202 fallback envelope.
        run_id:
          nullable: true
          description: Always `null` on the 202 path — the row was not visible within the synchronous poll window. Clients fall back to the WS event stream or `GET /api/sessions/:sessionId/finalize-runs/latest` to discover the id.
        status:
          type: string
          enum:
            - queued
          description: Always `queued` on the 202 path.
        message:
          type: string
          description: Human-readable note explaining the fallback.
      required:
        - ok
        - run_id
        - status
        - message
      description: Fallback envelope returned when the orchestrator fired but the row insert was not visible within ~300ms. Distinct shape from `StartFinalizeRunResponse` so OpenAPI clients can branch on it.
    StartFinalizeRunInFlight:
      type: object
      properties:
        error:
          type: string
          enum:
            - in_flight
        run_id:
          type: string
        status:
          type: string
          enum:
            - queued
            - rebasing
            - reviewing
            - running
            - dispatching
            - pushing
            - ready_to_push
            - pushed
            - failed
            - timed_out
            - infra_error
            - cancelled
            - stalled_no_response
        message:
          type: string
      required:
        - error
        - run_id
        - status
        - message
      description: Conflict envelope returned when a non-terminal Finalize run already exists for the same (project, branch, head_sha).
    CancelFinalizeRunRequest:
      type: object
      properties: {}
      description: Body is empty.
    PushFinalizeRunRequest:
      type: object
      properties:
        force:
          type: boolean
          description: Operator override — push even when gates have not passed.
      description: Optional push overrides.
    FinalizeStepOutputLine:
      type: object
      properties:
        stream:
          type: string
          enum:
            - stdout
            - stderr
        text:
          type: string
        created_at:
          type: string
      required:
        - stream
        - text
        - created_at
    FinalizeStepOutputResponse:
      type: object
      properties:
        run_id:
          type: string
        step_index:
          type: integer
        lines:
          type: array
          items:
            $ref: "#/components/schemas/FinalizeStepOutputLine"
        truncated:
          type: boolean
        total_lines:
          type: integer
      required:
        - run_id
        - step_index
        - lines
    CancelFinalizeRunResponse:
      type: object
      properties:
        ok:
          type: boolean
          enum:
            - true
        status:
          type: string
          enum:
            - cancelled
      required:
        - ok
        - status
      description: Cancellation confirmation.
    CancelFinalizeRunTerminal:
      type: object
      properties:
        error:
          type: string
          enum:
            - terminal
        status:
          type: string
          enum:
            - queued
            - rebasing
            - reviewing
            - running
            - dispatching
            - pushing
            - ready_to_push
            - pushed
            - failed
            - timed_out
            - infra_error
            - cancelled
            - stalled_no_response
        message:
          type: string
      required:
        - error
        - status
        - message
      description: Conflict envelope returned when the run is already in a terminal state.
    FinalizeMetricHistogramSummary:
      type: object
      properties:
        count:
          type: integer
        min:
          type: number
          nullable: true
        max:
          type: number
          nullable: true
        avg:
          type: number
          nullable: true
        p50:
          type: number
          nullable: true
        p95:
          type: number
          nullable: true
        p99:
          type: number
          nullable: true
      required:
        - count
        - min
        - max
        - avg
        - p50
        - p95
        - p99
      description: Standard summary for histogram-class metrics. Quantiles use linear interpolation (Type 7). `null` fields signal zero samples in the window.
    FinalizeMetricCounterAggregate:
      type: object
      properties:
        metric:
          type: string
          enum:
            - finalize_run_started
            - finalize_run_completed
            - finalize_run_active_seconds
            - finalize_run_wall_seconds
            - finalize_fix_dispatch_count
            - finalize_reviewer_verdict
            - finalize_step_result
            - finalize_stalled_no_response_count
            - merged_pr_provenance
            - finalize_github_parity
            - finalize_job_peak_memory_bytes
            - finalize_job_cpu_percent
        kind:
          type: string
          enum:
            - counter
        count:
          type: integer
        groups:
          type: array
          items:
            type: object
            properties:
              labels:
                type: object
                additionalProperties:
                  anyOf:
                    - type: string
                    - type: number
                    - type: boolean
                    - nullable: true
              count:
                type: integer
            required:
              - labels
              - count
      required:
        - metric
        - kind
        - count
        - groups
      description: "Counter aggregate: total `count` of rows in the window plus a breakdown per distinct label combination."
    FinalizeMetricHistogramAggregate:
      type: object
      properties:
        metric:
          type: string
          enum:
            - finalize_run_started
            - finalize_run_completed
            - finalize_run_active_seconds
            - finalize_run_wall_seconds
            - finalize_fix_dispatch_count
            - finalize_reviewer_verdict
            - finalize_step_result
            - finalize_stalled_no_response_count
            - merged_pr_provenance
            - finalize_github_parity
            - finalize_job_peak_memory_bytes
            - finalize_job_cpu_percent
        kind:
          type: string
          enum:
            - histogram
        summary:
          $ref: "#/components/schemas/FinalizeMetricHistogramSummary"
        groups:
          type: array
          items:
            type: object
            properties:
              labels:
                type: object
                additionalProperties:
                  anyOf:
                    - type: string
                    - type: number
                    - type: boolean
                    - nullable: true
              summary:
                $ref: "#/components/schemas/FinalizeMetricHistogramSummary"
            required:
              - labels
              - summary
      required:
        - metric
        - kind
        - summary
        - groups
      description: "Histogram aggregate: summary across all rows in the window plus a per-label-combination breakdown."
    FinalizeMetricsResponse:
      type: object
      properties:
        project_id:
          type: string
        range:
          type: object
          properties:
            from_ms:
              type: integer
            to_ms:
              type: integer
            from_iso:
              type: string
            to_iso:
              type: string
          required:
            - from_ms
            - to_ms
            - from_iso
            - to_iso
        sample_count:
          type: integer
          description: Number of metric event rows the read query touched.
        metrics:
          type: array
          items:
            anyOf:
              - $ref: "#/components/schemas/FinalizeMetricCounterAggregate"
              - $ref: "#/components/schemas/FinalizeMetricHistogramAggregate"
      required:
        - project_id
        - range
        - sample_count
        - metrics
      description: Aggregated adoption metrics for the requested time window. Counter and histogram metrics share the same `metrics` array; clients discriminate via the `kind` field.
    FinalizeJobResourcesResponse:
      type: object
      properties:
        project_id:
          type: string
        run_id:
          type: string
        jobs:
          type: array
          items:
            type: object
            properties:
              job_name:
                type: string
              matrix_key:
                type: string
              peak_mem_bytes:
                type: number
                nullable: true
              mem_total_bytes:
                type: number
                nullable: true
              peak_cpu_percent:
                type: number
                nullable: true
              observed_at:
                type: number
            required:
              - job_name
              - matrix_key
              - peak_mem_bytes
              - mem_total_bytes
              - peak_cpu_percent
              - observed_at
      required:
        - project_id
        - run_id
        - jobs
      description: Per-CI-job resource high-water marks (peak host memory + peak CPU) for one Finalize run, reported by the runner at job end. One entry per (job_name, matrix_key).
    FinalizeShipGateResponse:
      type: object
      properties:
        allowed:
          type: boolean
        code:
          type: string
        message:
          type: string
        run_id:
          type: string
          nullable: true
        failure_reason:
          type: string
          nullable: true
      required:
        - allowed
        - code
        - message
      description: Whether `gh pr create` is permitted for this session. Projects with `.agent-hub/ci.yaml` must ship via Finalize.
    PushFinalizeRunResponse:
      type: object
      properties:
        ok:
          type: boolean
          enum:
            - true
        pr_url:
          type: string
        status:
          type: string
          enum:
            - pushed
      required:
        - ok
        - pr_url
        - status
      description: Push + PR creation succeeded.
    KanbanCardLinkedSupportTicket:
      type: object
      properties:
        id:
          type: string
        project_id:
          type: string
        type:
          type: string
          enum:
            - bug
            - question
            - feature_request
            - incident
            - other
        severity:
          type: string
          enum:
            - critical
            - high
            - medium
            - low
        status:
          type: string
          enum:
            - new
            - investigating
            - converted
            - closed
            - duplicate
            - wont_do
        subject:
          type: string
        reporter:
          type: string
          nullable: true
        reporter_email:
          type: string
          nullable: true
          description: Reporter email for privileged callers; masked for non-privileged callers.
        reporter_email_masked:
          type: boolean
          description: True when reporter_email is present but masked for this response.
        converted_card_id:
          type: string
          nullable: true
        release_state:
          type: string
          nullable: true
          enum:
            - fixed_pending_release
            - released_to_prod
            - customer_notified
            - null
        fixed_at:
          type: string
          nullable: true
        released_to_prod_at:
          type: string
          nullable: true
        release_deployment_id:
          type: string
          nullable: true
        customer_notified_at:
          type: string
          nullable: true
        created_at:
          type: string
        updated_at:
          type: string
      required:
        - id
        - project_id
        - type
        - severity
        - status
        - subject
        - reporter
        - reporter_email
        - reporter_email_masked
        - converted_card_id
        - release_state
        - fixed_at
        - released_to_prod_at
        - release_deployment_id
        - customer_notified_at
        - created_at
        - updated_at
      description: Safe support-ticket metadata attached to a converted kanban card. The full ticket body and raw reporter contact stay on the support-ticket record.
    KanbanBoard:
      type: object
      properties:
        id:
          type: string
        project_id:
          type: string
        name:
          type: string
        card_seq:
          type: integer
          description: Monotonic per-board counter backing card short ids. Only ever incremented, so a deleted card never frees its number.
        card_prefix:
          type: string
          description: Persisted alphabetic prefix for human card ids, e.g. "AH". Frozen at board creation from the immutable project slug, so renaming a project never rewrites existing card ids. The full label is `${card_prefix}-${card.short_id}` ("AH-123"). Always present and non-null on the GET /board board payload (the server backfills/derives it).
        created_at:
          type: string
      required:
        - id
        - project_id
        - name
        - card_seq
        - card_prefix
        - created_at
      description: A project kanban board (one per project).
    KanbanColumn:
      type: object
      properties:
        id:
          type: string
        board_id:
          type: string
        name:
          type: string
        position:
          type: integer
        color:
          type: string
          nullable: true
        created_at:
          type: string
      required:
        - id
        - board_id
        - name
        - position
        - color
        - created_at
      description: A column on a kanban board (To Do, In Progress, …).
    KanbanAssignableUser:
      type: object
      properties:
        id:
          type: string
        username:
          type: string
      required:
        - id
        - username
      description: A user that can be assigned as a kanban lead user.
    KanbanCard:
      type: object
      properties:
        id:
          type: string
        column_id:
          type: string
        board_id:
          type: string
        title:
          type: string
        description:
          type: string
          nullable: true
        priority:
          type: string
          enum:
            - low
            - medium
            - high
            - urgent
        assignee:
          type: string
          nullable: true
        assigned_user_id:
          type: string
          nullable: true
        labels:
          type: string
          nullable: true
        session_id:
          type: string
          nullable: true
        github_issue_url:
          type: string
          nullable: true
        support_ticket_id:
          type: string
          nullable: true
          description: Durable id of the support ticket this card was converted from, when present. Used by release workflows instead of scraping card text.
        customer_report_id:
          type: string
          nullable: true
          description: Durable id of the source customer report. For support-ticket intake this matches support_ticket_id.
        linked_support_ticket:
          type: object
          nullable: true
          properties:
            id:
              type: string
            project_id:
              type: string
            type:
              type: string
              enum:
                - bug
                - question
                - feature_request
                - incident
                - other
            severity:
              type: string
              enum:
                - critical
                - high
                - medium
                - low
            status:
              type: string
              enum:
                - new
                - investigating
                - converted
                - closed
                - duplicate
                - wont_do
            subject:
              type: string
            reporter:
              type: string
              nullable: true
            reporter_email:
              type: string
              nullable: true
              description: Reporter email for privileged callers; masked for non-privileged callers.
            reporter_email_masked:
              type: boolean
              description: True when reporter_email is present but masked for this response.
            converted_card_id:
              type: string
              nullable: true
            release_state:
              type: string
              nullable: true
              enum:
                - fixed_pending_release
                - released_to_prod
                - customer_notified
                - null
            fixed_at:
              type: string
              nullable: true
            released_to_prod_at:
              type: string
              nullable: true
            release_deployment_id:
              type: string
              nullable: true
            customer_notified_at:
              type: string
              nullable: true
            created_at:
              type: string
            updated_at:
              type: string
          required:
            - id
            - project_id
            - type
            - severity
            - status
            - subject
            - reporter
            - reporter_email
            - reporter_email_masked
            - converted_card_id
            - release_state
            - fixed_at
            - released_to_prod_at
            - release_deployment_id
            - customer_notified_at
            - created_at
            - updated_at
          description: Safe linked support-ticket metadata for converted cards. reporter_email is role-gated/masked.
        source_type:
          type: string
          nullable: true
          enum:
            - manual
            - email
            - calendar
            - todo
            - log_issue
            - null
          description: "Capture provenance (spec CAPTURE-PROVENANCE): the origin this card was captured from. `todo` means it was promoted from a personal todo; `email`/`calendar` a direct dashboard capture; `manual`/null a card with no tracked origin."
        source_id:
          type: string
          nullable: true
          description: Opaque id of the capture origin (Gmail message id, Calendar event id, or todo id). NULL when untracked.
        source_meta:
          type: object
          nullable: true
          additionalProperties:
            nullable: true
          description: Parsed JSON deep-link blob preserving a pointer back to the capture origin so the dashboard can reopen it.
        pr_url:
          type: string
          nullable: true
        review_status:
          type: string
          nullable: true
          enum:
            - awaiting_review
            - reviewing
            - approved
            - changes_requested
            - null
        created_by:
          type: string
          nullable: true
        short_id:
          type: integer
          nullable: true
          description: Human-readable per-board sequence number (the "123" in "AH-123"). Assigned on insert; null only on legacy rows before backfill. Pair with the board `card_prefix` to build the display label.
        position:
          type: integer
        epic_id:
          type: string
          nullable: true
        phase_id:
          type: string
          nullable: true
        card_kind:
          type: string
          enum:
            - task
            - spike
          description: "`task` (default) or `spike` — spike cards resolve epic spec decisions via scoping sessions."
        documented:
          type: integer
        dispatched_by_autonomous:
          type: integer
        orphaned_at:
          type: string
          nullable: true
          description: Legacy marker written by older Hub versions when a card lost its working session. Restoring the session clears it.
        assign_model:
          type: string
          nullable: true
        assign_engine:
          type: string
          nullable: true
          description: Optional engine override pinned at assign / update time. One of "claude-code", "cursor-agent", "gemini-cli", or "codex-cli". Overrides the assignee agent's shared engine at session spawn.
        pr_base_branch:
          type: string
          nullable: true
        created_at:
          type: string
        updated_at:
          type: string
      required:
        - id
        - column_id
        - board_id
        - title
        - description
        - priority
        - assignee
        - labels
        - session_id
        - github_issue_url
        - pr_url
        - review_status
        - created_by
        - short_id
        - position
        - epic_id
        - documented
        - dispatched_by_autonomous
        - created_at
        - updated_at
      description: A kanban card (task / ticket).
    KanbanBlockerLink:
      type: object
      properties:
        id:
          type: string
        title:
          type: string
        column_id:
          type: string
        done:
          type: boolean
      required:
        - id
        - title
        - column_id
        - done
      description: A blocker / blocked-by relationship view. `done` is derived from the referenced card's column ("Done"-ish columns resolve the link).
    KanbanCardEnriched:
      allOf:
        - $ref: "#/components/schemas/KanbanCard"
        - type: object
          properties:
            blockers:
              type: array
              items:
                $ref: "#/components/schemas/KanbanBlockerLink"
            blocks:
              type: array
              items:
                $ref: "#/components/schemas/KanbanBlockerLink"
            finalize_run:
              allOf:
                - $ref: "#/components/schemas/FinalizeRun"
                - nullable: true
                  description: Latest Finalize Code Changes run for this card's `session_id`, or `null` when the card has no session or the session has never triggered Finalize. Folded into the board payload so the per-card status badge in the client can render without a separate GET per card.
          required:
            - blockers
            - blocks
            - finalize_run
      description: A card with its blocker graph and latest finalize run attached (used by GET /board).
    KanbanEpic:
      type: object
      properties:
        id:
          type: string
        board_id:
          type: string
        name:
          type: string
        description:
          type: string
          nullable: true
        state:
          type: string
          nullable: true
          enum:
            - not_started
            - in_progress
            - done
            - null
          description: Derived lifecycle from linked cards. `null` means the epic has no linked cards. `not_started` means linked cards exist but none have left To Do/backlog yet, `in_progress` means at least one linked card has started or completed but not every linked card is Done, and `done` means every linked card is in a Done-ish column.
        labels:
          type: string
          nullable: true
        assigned_user_id:
          type: string
          nullable: true
        color:
          type: string
        autonomous:
          type: integer
        autonomous_interval:
          type: integer
        autonomous_max_concurrent:
          type: integer
        autonomous_model:
          type: string
          nullable: true
        orchestration_budgets_json:
          type: string
          nullable: true
        pr_base_branch:
          type: string
          nullable: true
        position:
          type: integer
        created_at:
          type: string
        updated_at:
          type: string
      required:
        - id
        - board_id
        - name
        - description
        - state
        - color
        - autonomous
        - autonomous_interval
        - autonomous_max_concurrent
        - autonomous_model
        - position
        - created_at
        - updated_at
      description: A kanban feature. The wire shape keeps the existing epic field names for compatibility.
    KanbanCardTemplate:
      type: object
      properties:
        id:
          type: string
        name:
          type: string
        title:
          type: string
        description:
          type: string
        priority:
          type: string
          enum:
            - low
            - medium
            - high
            - urgent
        labels:
          type: string
        epicId:
          type: string
        updatedAt:
          type: string
      required:
        - id
        - name
        - title
        - description
        - priority
        - labels
        - epicId
        - updatedAt
      description: Reusable defaults for new kanban cards (title, description, priority, labels, epic).
    KanbanPhase:
      type: object
      properties:
        id:
          type: string
        epic_id:
          type: string
        board_id:
          type: string
        name:
          type: string
        description:
          type: string
          nullable: true
        position:
          type: integer
        autonomous:
          type: integer
        autonomous_interval:
          type: integer
        autonomous_max_concurrent:
          type: integer
        autonomous_model:
          type: string
          nullable: true
        autonomous_enabled_by:
          type: string
          nullable: true
        autonomous_send_it:
          type: integer
        autonomous_running:
          type: integer
        created_at:
          type: string
        updated_at:
          type: string
      required:
        - id
        - epic_id
        - board_id
        - name
        - description
        - position
        - autonomous
        - autonomous_interval
        - autonomous_max_concurrent
        - autonomous_model
        - created_at
        - updated_at
      description: A phase within an epic — groups related tickets for a feature run.
    KanbanEpicSpecItem:
      type: object
      properties:
        id:
          type: string
        epic_id:
          type: string
        board_id:
          type: string
        phase_id:
          type: string
          nullable: true
        tag:
          type: string
        title:
          type: string
        decision:
          type: string
          nullable: true
        status:
          type: string
          enum:
            - open
            - chosen
            - deferred
        position:
          type: integer
        spike_card_id:
          type: string
          nullable: true
        resolved_session_id:
          type: string
          nullable: true
        created_at:
          type: string
        updated_at:
          type: string
      required:
        - id
        - epic_id
        - board_id
        - phase_id
        - tag
        - title
        - decision
        - status
        - position
        - spike_card_id
        - resolved_session_id
        - created_at
        - updated_at
      description: An architecture decision for an epic. Open items have a linked spike ticket; spike sessions lock the decision.
    KanbanCardComment:
      type: object
      properties:
        id:
          type: string
        card_id:
          type: string
        author:
          type: string
        content:
          type: string
        created_at:
          type: string
      required:
        - id
        - card_id
        - author
        - content
        - created_at
      description: A comment posted to a kanban card.
    CardReplay:
      type: object
      properties:
        replayId:
          type: string
        durationMs:
          type: number
        eventCount:
          type: number
        createdAt:
          type: string
      required:
        - replayId
        - durationMs
        - eventCount
        - createdAt
      description: Pointer to the session replay attributed to a kanban card. The events are fetched from the per-replay playback endpoints under /api/replays/:id.
    BoardResponse:
      type: object
      properties:
        board:
          $ref: "#/components/schemas/KanbanBoard"
        columns:
          type: array
          items:
            $ref: "#/components/schemas/KanbanColumn"
        cards:
          type: array
          items:
            $ref: "#/components/schemas/KanbanCardEnriched"
        epics:
          type: array
          items:
            $ref: "#/components/schemas/KanbanEpic"
        phases:
          type: array
          items:
            $ref: "#/components/schemas/KanbanPhase"
          description: All phases on the board, ordered by position within their epic.
        specItems:
          type: array
          items:
            $ref: "#/components/schemas/KanbanEpicSpecItem"
          description: Architecture spec decisions for epics — resolved via spike tickets/sessions.
        counts:
          type: object
          additionalProperties:
            type: integer
          description: "Total card count per column, keyed by column id (`{ [columnId]: total }`). Always present. Lets a client decide whether a column has more cards than the page in `cards` and fetch the rest via GET /board/columns/:columnId/cards."
        cursors:
          type: object
          additionalProperties:
            type: string
            nullable: true
          description: "Per-column next-page cursor, keyed by column id (`{ [columnId]: nextCursor|null }`). Present on the bounded default and `?limit=N` responses; absent on the `?limit=all` full-board response. A non-null value is the opaque keyset cursor to pass as `cursor` to GET /board/columns/:columnId/cards for the next page; null means the first page is the last page. Lets a client seed per-column infinite scroll from this one request without reconstructing the opaque cursor."
        cardTemplates:
          type: array
          items:
            $ref: "#/components/schemas/KanbanCardTemplate"
          description: Reusable card templates for this board.
        availableLabels:
          type: array
          items:
            type: string
          description: Distinct card labels across the full board, even when `cards` is paginated. Used to render complete label filter facets without draining every card page first.
        assignableUsers:
          type: array
          items:
            $ref: "#/components/schemas/KanbanAssignableUser"
          description: Users in the request org who can be assigned as kanban lead users.
      required:
        - board
        - columns
        - cards
        - epics
        - counts
      description: "Board state: board metadata + columns + cards (with blocker graph) + epics + per-column counts. Bounded by default — `cards` carries only the first page per column (ordered by position, id) plus a `cursors` map, so the default response does not serialize the whole board. `?limit=N` sets the page size; `?limit=all` returns every card unpaged (no `cursors`)."
    PaginatedColumnCards:
      type: object
      properties:
        cards:
          type: array
          items:
            $ref: "#/components/schemas/KanbanCardEnriched"
        nextCursor:
          type: string
          nullable: true
          description: Opaque keyset cursor for the next page, or null on the final page. Echo it back as the `cursor` query param to fetch the following page.
        total:
          type: integer
          description: Total card count in this column.
      required:
        - cards
        - nextCursor
        - total
      description: One keyset-paginated page of a column's cards (with blocker graph attached).
    BoardErrorResponse:
      type: object
      properties:
        error:
          type: string
        details:
          type: array
          items:
            type: object
            properties:
              path:
                type: array
                items:
                  anyOf:
                    - type: string
                    - type: number
              message:
                type: string
            required:
              - path
              - message
        path:
          type: array
          items:
            type: string
      required:
        - error
      description: Error envelope. `details` is populated for 400 schema-validation errors; `path` is populated for the 409 cycle error from the blocker endpoint.
    Session:
      type: object
      properties:
        id:
          type: string
        agent_id:
          type: string
        name:
          type: string
        engine:
          type: string
        checkpoint_rewind_supported:
          type: boolean
          description: "True when the session engine supports Claude Code checkpoint file rewind (`POST /api/sessions/{sessionId}/rewind`). Today only `claude-code`. Other engines return HTTP 400 with `code: checkpoint_rewind_unsupported_engine` from that endpoint."
        can_isolated_mode:
          type: boolean
          description: True when Firecracker is registered on this host so the session may enter `isolated` (VM) mode. False for workflow (no-code) projects when the owning project is known. Clients hide the VM option when this is false.
        model:
          type: string
        use_worktree:
          type: integer
        ask_mode:
          type: integer
        react_loop_enabled:
          type: integer
          nullable: true
        session_mode:
          type: string
          nullable: true
          enum:
            - chat
            - isolated
            - design
            - scoping
            - skill-builder
            - consult
            - hub
            - null
          description: "Session mode picker dimension: `chat` (default), `isolated` (opt-in Firecracker VM; same ship surface as chat), `design`, `scoping`, `skill-builder`, or `consult`. NULL/absent on legacy rows → treated as `chat`. `isolated` forces a microVM when Firecracker is registered; `design` loads the design skill; `scoping` loads kanban planning with a live epic flowchart panel; `skill-builder` loads the skill-authoring coach; `consult` is Hub/project Q&A with no code ship or Finalize. Set via `PATCH /api/sessions/{sessionId}` or `PUT .../mode`."
        reasoning_effort:
          type: string
          nullable: true
          enum:
            - high
            - pro
            - null
          description: 'Codex reasoning ("thinking") preset: `high` (default) or `pro` (→ xhigh). NULL/absent on legacy rows and non-Codex sessions; treated as `high`.'
        worktree_path:
          type: string
          nullable: true
        worktree_branch:
          type: string
          nullable: true
        worktree_checkout_branch:
          type: string
          nullable: true
        code_changed_at:
          type: string
          nullable: true
        engine_session_id:
          type: string
          nullable: true
        cron_id:
          type: integer
          nullable: true
        deleted_at:
          type: string
          nullable: true
        created_at:
          type: string
        updated_at:
          type: string
        owner_user_id:
          type: string
          nullable: true
        orchestration_phase:
          type: string
          nullable: true
        orchestration_meta:
          type: string
          nullable: true
        max_turns:
          type: integer
        linked_design_id:
          type: string
          nullable: true
          description: Optional Design Studio design id linked to this session. When set, the web client renders the design's live canvas in a preview pane beside the chat. Set/cleared via `PUT /api/sessions/{sessionId}/linked-design`. Not a foreign key — a stale id (design since deleted) is tolerated and ignored at render time.
        linked_epic_id:
          type: string
          nullable: true
          description: Epic linked for scoping mode. When `session_mode` is `scoping`, the web client renders a live Epic → Phase → Ticket flowchart for this epic. Set/cleared via `PUT /api/sessions/{sessionId}/linked-epic`.
        state:
          type: string
          enum:
            - waiting_for_user_input
            - working
            - running_tests
            - reviewing
            - pending_checks
            - pending_push
            - pushed
            - merged
          description: Always-on lifecycle state of the session — exactly one value, surfaced as a single status icon in the clients. Resolved server-side at read time from active-task / Finalize-run / Done-column signals (see `server/session-state.ts`) and present on enriched session payloads (list, detail, `session_created`). The server also backfills the persisted `sessions.state` cache and emits a dedicated `session_state` WebSocket push at production signal boundaries (chat turn start/end, linked-card auto-close, kanban move to Done). Clients keep Finalize phases live from `finalize_run_phase_changed` and use `session_state` for persisted seed / terminal updates such as `merged`.
        agents:
          type: array
          items:
            type: object
            properties:
              participantId:
                type: string
              id:
                type: string
              name:
                type: string
              color:
                type: string
              engine:
                type: string
              model:
                type: string
                nullable: true
              position:
                type: integer
              role:
                type: string
                enum:
                  - executor
                  - advisor
              projectId:
                type: string
              projectName:
                type: string
            required:
              - participantId
              - id
              - name
              - color
              - engine
              - model
              - position
              - role
        advisor_count:
          type: integer
      required:
        - id
        - agent_id
        - name
        - engine
        - checkpoint_rewind_supported
        - can_isolated_mode
        - model
        - use_worktree
        - ask_mode
        - created_at
        - updated_at
      description: A chat session row. Booleans are stored as 0/1 SQLite ints for `use_worktree`, `ask_mode`, `react_loop_enabled`. `use_worktree` is always 1 for user-created sessions; the column is preserved for legacy rows and internal shared-checkout callers.
    Message:
      type: object
      properties:
        id:
          type: string
        session_id:
          type: string
        role:
          type: string
          enum:
            - user
            - assistant
            - system
        content:
          type: string
        engine:
          type: string
          nullable: true
        model:
          type: string
          nullable: true
        attachments:
          type: string
          nullable: true
        metadata:
          type: string
          nullable: true
        created_at:
          type: string
      required:
        - id
        - session_id
        - role
        - content
        - created_at
      description: A single message row inside a session.
    SessionMessagesList:
      type: object
      properties:
        messages:
          type: array
          items:
            $ref: "#/components/schemas/Message"
        truncated:
          type: boolean
          enum:
            - true
        omitted:
          type: integer
          minimum: 0
        total:
          type: integer
          minimum: 0
      required:
        - messages
        - truncated
        - omitted
        - total
      description: Truncated message list when the full transcript exceeds the JSON response budget. Oldest rows are omitted; `messages` is oldest-first within the returned slice.
    SessionSummary:
      type: object
      properties:
        session:
          type: object
          properties:
            id:
              type: string
            name:
              type: string
            engine:
              type: string
            model:
              type: string
            updatedAt:
              type: string
          required:
            - id
            - name
            - engine
            - model
            - updatedAt
        projectId:
          type: string
          nullable: true
        projectGithubRepo:
          type: string
          nullable: true
        linkedCard:
          type: object
          nullable: true
          properties:
            id:
              type: string
            title:
              type: string
            pr_url:
              type: string
              nullable: true
            review_status:
              type: string
              nullable: true
            columnName:
              type: string
              nullable: true
          required:
            - id
            - title
            - pr_url
            - review_status
            - columnName
        finalizePrUrl:
          type: string
          nullable: true
          description: PR URL recorded by the latest Finalize run for this session when the linked kanban card does not already carry a PR URL.
        sessionTitlePrUrl:
          type: string
          nullable: true
          description: Best-effort PR URL inferred from Resolve/Review-style session titles when neither the linked card nor the latest Finalize run supplies a PR URL. A github.com URL for GitHub-hosted projects, or the native client route `/projects/<projectId>/pulls/<number>` when the project is hosted on Agent Hub.
        runSnapshot:
          type: object
          additionalProperties:
            nullable: true
        skills:
          type: array
          items:
            type: object
            properties:
              id:
                type: string
              skillId:
                type: string
              status:
                type: string
              source:
                type: string
              injectedBytes:
                type: number
                nullable: true
              createdAt:
                type: string
            required:
              - id
              - skillId
              - status
              - source
              - injectedBytes
              - createdAt
      required:
        - session
        - projectId
        - projectGithubRepo
        - linkedCard
        - finalizePrUrl
        - sessionTitlePrUrl
        - runSnapshot
        - skills
      description: "Session summary metadata used by the chat header and orchestration panels: linked PR, run snapshot, and loaded skills."
    SessionErrorResponse:
      type: object
      properties:
        error:
          type: string
        code:
          type: string
          description: Optional machine-readable code (for example `checkpoint_rewind_unsupported_engine` from `POST .../rewind` when the session is not Claude Code).
        details:
          type: array
          items:
            type: object
            properties:
              path:
                type: array
                items:
                  anyOf:
                    - type: string
                    - type: number
              message:
                type: string
            required:
              - path
              - message
      required:
        - error
      description: Error envelope. `details` is populated for 400 schema-validation errors with one entry per failing Zod issue.
    SessionCredentialField:
      type: object
      properties:
        key:
          type: string
          minLength: 1
          maxLength: 64
          description: Stable field key, for example `username` or `password`.
        label:
          type: string
          minLength: 1
          maxLength: 80
        type:
          type: string
          enum:
            - text
            - username
            - password
      required:
        - key
        - label
        - type
    SessionCredentialStatusResponse:
      type: object
      properties:
        requestId:
          type: string
        service:
          type: string
        purpose:
          type: string
        fields:
          type: array
          items:
            $ref: "#/components/schemas/SessionCredentialField"
        status:
          type: string
          enum:
            - submitted
            - consumed
            - expired
        submittedAt:
          type: string
        consumedAt:
          type: string
          nullable: true
        expiresAt:
          type: string
        persisted:
          type: object
          properties:
            skillId:
              type: string
            stored:
              type: array
              items:
                type: string
            skipped:
              type: array
              items:
                type: object
                properties:
                  keyName:
                    type: string
                  reason:
                    type: string
                required:
                  - keyName
                  - reason
            error:
              type: string
          required:
            - skillId
            - stored
            - skipped
          description: "Present on the PUT (submit) response when the request declared a `persist` target: which credential keys were written to the owner's skill store, which were skipped, and any skill-level persist error."
      required:
        - requestId
        - service
        - purpose
        - fields
        - status
        - submittedAt
        - consumedAt
        - expiresAt
    ConsumeSessionCredentialResponse:
      type: object
      properties:
        requestId:
          type: string
        service:
          type: string
        purpose:
          type: string
        values:
          type: object
          additionalProperties:
            type: string
          description: Plaintext values. The same requestId can be consumed again until it expires (so an agent that loses the value can re-fetch it); the encrypted payload is erased when the request expires or is deleted.
      required:
        - requestId
        - service
        - purpose
        - values
    SessionChangeFile:
      type: object
      properties:
        path:
          type: string
          description: Repo-relative path (new path for renames).
        oldPath:
          type: string
          description: Previous path for renames/copies.
        status:
          type: string
          enum:
            - added
            - modified
            - deleted
            - renamed
            - copied
            - type-changed
        additions:
          type: integer
        deletions:
          type: integer
        binary:
          type: boolean
        untracked:
          type: boolean
          description: True for files git is not tracking yet (freshly created).
      required:
        - path
        - status
        - additions
        - deletions
        - binary
        - untracked
      description: One changed file in a session worktree.
    SessionChanges:
      type: object
      properties:
        baseBranch:
          type: string
          nullable: true
        baseSha:
          type: string
          nullable: true
          description: Merge-base SHA the diff is anchored to (null if unresolved).
        headSha:
          type: string
          nullable: true
        branch:
          type: string
          nullable: true
        dirty:
          type: boolean
          description: True when the worktree has uncommitted changes.
        files:
          type: array
          items:
            $ref: "#/components/schemas/SessionChangeFile"
        truncated:
          type: boolean
          description: True when the file list was capped at the server limit.
      required:
        - baseBranch
        - baseSha
        - headSha
        - branch
        - dirty
        - files
        - truncated
      description: Total session delta (committed + uncommitted + untracked) vs base.
    SessionFileDiff:
      type: object
      properties:
        path:
          type: string
        status:
          type: string
          enum:
            - added
            - modified
            - deleted
            - renamed
            - copied
            - type-changed
        binary:
          type: boolean
        unifiedDiff:
          type: string
          description: Unified diff body. Empty when binary or tooLarge.
        tooLarge:
          type: boolean
          description: True when the patch exceeded the byte cap and was withheld.
      required:
        - path
        - status
        - binary
        - unifiedDiff
        - tooLarge
      description: Unified diff for a single changed file.
    BugReportsErrorResponse:
      type: object
      properties:
        error:
          type: string
      required:
        - error
      description: Error envelope for bug-report intake.
    BugReportFormFields:
      type: object
      properties:
        title:
          type: string
          minLength: 1
          maxLength: 200
        severity:
          type: string
          enum:
            - low
            - medium
            - high
            - critical
          default: medium
        description:
          type: string
        sourceUrl:
          type: string
        userAgent:
          type: string
        appVersion:
          type: string
        clientType:
          type: string
          enum:
            - web
            - electron
            - mobile
        currentProjectId:
          type: string
        currentAgentId:
          type: string
        reporter_email:
          type: string
          format: email
          description: Optional reporter contact email. Stored as a protected support-ticket field and used later for customer fix notifications; not copied into the ticket body.
        replayRef:
          type: string
          description: Optional session-replay ref from POST /api/replays (`/uploads/replay-<id>.json`). Attributed to the ticket and surfaced to the AI investigation.
        replayMissReason:
          type: string
          enum:
            - recorder-not-initialized
            - recorder-inactive
            - buffer-too-small
            - no-full-snapshot
            - upload-failed
          description: Why no session replay attached, when `replayRef` is absent. Recorded in the ticket Reporter Context so a "didn't capture replay" report is self-diagnosing. Ignored if a valid `replayRef` is present.
        screenshotMissReason:
          type: string
          enum:
            - initial-capture-failed
            - retake-capture-failed
            - upload-rejected
          description: Why no screenshot attached, when the `screenshot` image part is absent or rejected. Recorded in the ticket Reporter Context so missing screenshots are diagnosable.
        screenshot:
          type: string
          format: binary
          description: Optional image part (PNG/JPEG/WebP/GIF, ≤8 MB). Validated by magic-byte signature, persisted under /uploads, and stored as the ticket `screenshot_ref` so it renders inline in the Customer Support queue. A bad/oversize/non-image part is dropped (the report still lands).
      required:
        - title
      description: Multipart form fields for the bug-report intake. Documentation-only; the handler reads multipart bytes directly because the body is `application/octet-stream` to express.
    CodeSearchResult:
      type: object
      properties:
        filePath:
          type: string
        chunkIdx:
          type: integer
        startLine:
          type: integer
        endLine:
          type: integer
        text:
          type: string
        score:
          type: number
        ftsScore:
          type: number
        semanticScore:
          type: number
        snippet:
          type: string
      required:
        - filePath
        - chunkIdx
        - startLine
        - endLine
        - text
        - score
      description: A single chunk-level code search hit with a citation range.
    CodeSearchResponse:
      type: object
      properties:
        mode:
          type: string
          enum:
            - hybrid
            - semantic
            - fts
        query:
          type: string
        geminiConfigured:
          type: boolean
        results:
          type: array
          items:
            $ref: "#/components/schemas/CodeSearchResult"
      required:
        - mode
        - geminiConfigured
        - results
      description: Code search envelope.
    CodeIndexResult:
      type: object
      properties:
        projectId:
          type: string
        root:
          type: string
        scanned:
          type: integer
        indexed:
          type: integer
        skipped:
          type: integer
        removed:
          type: integer
        chunks:
          type: integer
        errors:
          type: array
          items:
            type: object
            properties:
              file:
                type: string
              error:
                type: string
            required:
              - file
              - error
        geminiConfigured:
          type: boolean
        truncated:
          type: boolean
          description: True when the scan was capped by maxFiles; pruning was skipped.
      required:
        - projectId
        - root
        - scanned
        - indexed
        - skipped
        - removed
        - chunks
        - errors
        - geminiConfigured
        - truncated
      description: Per-run code indexing report.
    CodeIndexStatus:
      type: object
      properties:
        chunks:
          type: integer
        geminiConfigured:
          type: boolean
      required:
        - chunks
        - geminiConfigured
      description: Code index status for a project.
    CodeRagErrorResponse:
      type: object
      properties:
        error:
          type: string
      required:
        - error
      description: Error envelope.
    ConfigErrorResponse:
      type: object
      properties:
        error:
          type: string
        details:
          type: array
          items:
            type: object
            properties:
              path:
                type: array
                items:
                  anyOf:
                    - type: string
                    - type: number
              message:
                type: string
            required:
              - path
              - message
      required:
        - error
      description: Error envelope for config / github routes.
    AppConfig:
      type: object
      properties:
        claudeBin:
          type: string
        cursorBin:
          type: string
        geminiBin:
          type: string
        codexBin:
          type: string
        grokBin:
          type: string
        defaultModel:
          type: string
          deprecated: true
          description: Legacy compatibility field. The server no longer uses this host-wide value for model selection.
        defaultCwd:
          type: string
        port:
          type: integer
        publicUrl:
          type: string
        apiKey:
          type: string
        authRequired:
          type: boolean
        openaiApiKey:
          type: string
          description: Masked OpenAI API key used for host services. Empty when unset.
        openaiApiKeySet:
          type: boolean
          description: Whether the host OpenAI API key is configured.
        transcriptionProvider:
          type: string
          enum:
            - xai
            - openai
          description: Provider used by /api/transcribe for chat voice transcription. `xai` (default) uses the xAI Grok speech-to-text endpoint; `openai` uses Whisper. Selectable on the settings page.
        geminiApiKeySet:
          type: boolean
          description: Whether the host Gemini API key is configured (used for wiki RAG memory lookups).
        xaiApiKeySet:
          type: boolean
          description: Whether the host xAI (Grok) API key is configured (used for the default xAI transcription provider).
        personalOAuth:
          type: object
          properties:
            configured:
              type: boolean
            clientId:
              type: string
              nullable: true
          required:
            - configured
            - clientId
        anthropicApiKey:
          type: string
        anthropicApiKeySet:
          type: boolean
        codexDangerBypass:
          type: boolean
        emailLogoEnabled:
          type: boolean
          description: Whether deployment/release notification emails render the Agent Hub logo in the HTML header. Default true. Configurable via `emailLogoEnabled` in config.json, `PATCH /api/config`, or env `AGENT_HUB_EMAIL_LOGO_ENABLED`.
        codexProfile:
          type: string
          nullable: true
          description: Optional Codex CLI profile name forwarded as `--profile <name>` on every codex spawn (chat, room, design, slack one-shot). Null / empty = no flag. Configurable via `codexProfile` in config.json, `PATCH /api/config`, or env `CODEX_PROFILE`.
        _file:
          type: object
          properties:
            claudeBin:
              type: string
              nullable: true
            cursorBin:
              type: string
              nullable: true
            geminiBin:
              type: string
              nullable: true
            codexBin:
              type: string
              nullable: true
            grokBin:
              type: string
              nullable: true
          required:
            - claudeBin
            - cursorBin
            - geminiBin
            - codexBin
            - grokBin
        features:
          type: object
          properties:
            prEnv:
              type: boolean
          required:
            - prEnv
      required:
        - claudeBin
        - defaultCwd
        - port
        - publicUrl
        - apiKey
        - authRequired
        - openaiApiKey
        - openaiApiKeySet
        - transcriptionProvider
        - geminiApiKeySet
        - xaiApiKeySet
        - personalOAuth
        - anthropicApiKey
        - anthropicApiKeySet
        - _file
        - features
      additionalProperties:
        nullable: true
      description: Aggregated server configuration. Sensitive fields are masked (`••••••••`); `_file` reflects what is actually persisted to `config.json`.
    ModelsConfig:
      type: object
      properties:
        defaultModel:
          type: string
          deprecated: true
          description: Legacy compatibility field. New model resolution ignores this host-wide value.
      additionalProperties:
        nullable: true
      description: Per-engine model availability. The deprecated `defaultModel` field remains for legacy clients; new selection is per-user and per-agent.
    SpawnPath:
      type: object
      properties:
        path:
          type: string
          nullable: true
        source:
          type: string
        capturedAt:
          anyOf:
            - type: string
            - type: number
            - nullable: true
        entries:
          type: array
          items:
            type: string
        ok:
          type: boolean
      required:
        - path
        - source
        - entries
      description: Cached login-shell PATH used when spawning agent processes. `entries` is the colon-split form for convenience.
    PersonalOAuthStatus:
      type: object
      properties:
        configured:
          type: boolean
        clientId:
          type: string
          nullable: true
      required:
        - configured
        - clientId
      description: Personal GitHub OAuth App status (the OAuth client used for human-identity actions).
    GoogleOAuthStatus:
      type: object
      properties:
        configured:
          type: boolean
        clientId:
          type: string
          nullable: true
        redirectUri:
          type: string
          description: Canonical OAuth redirect URI the server sends to Google, derived from `publicUrl` (falling back to the request origin). The admin must register this exact value as an authorized redirect URI in the Google Cloud Console.
      required:
        - configured
        - clientId
        - redirectUri
      description: Server-global Google OAuth app status. `configured` is true when both client id and secret are stored; `clientId` is exposed for display, the secret never is.
    GithubAppInstallation:
      type: object
      properties:
        account:
          type: string
          description: GitHub account login (org or user) this installation serves.
        id:
          anyOf:
            - type: string
              pattern: ^(?!0+$)\d+$
            - type: integer
              minimum: 0
              exclusiveMinimum: true
          description: GitHub installation id — a positive integer.
      required:
        - id
      description: One GitHub App installation, keyed by the account it serves.
    GithubAppStatus:
      type: object
      properties:
        configured:
          type: boolean
          description: True when both an App id and a private key are stored (mint-ready).
        appId:
          anyOf:
            - type: string
            - type: number
            - nullable: true
          description: Numeric GitHub App id (display-safe; not a secret).
        installationId:
          anyOf:
            - type: string
            - type: number
            - nullable: true
          description: Default installation id used when no per-owner match is found.
        installations:
          type: array
          items:
            $ref: "#/components/schemas/GithubAppInstallation"
          description: Per-owner installation map so one App serving several orgs resolves right.
        hasPrivateKey:
          type: boolean
          description: Whether a PEM private key is stored. The key itself is never returned.
      required:
        - configured
        - appId
        - installationId
        - installations
        - hasPrivateKey
      description: Server-global GitHub App status — the mirror-push branch-protection bypass identity. The private key is write-only and never echoed; only `hasPrivateKey` is exposed.
    GithubStatus:
      type: object
      properties:
        authenticated:
          type: boolean
        user:
          type: string
        scopes:
          type: array
          items:
            type: string
        error:
          type: string
      required:
        - authenticated
      description: "`gh` CLI auth status."
    DetectRepoResponse:
      type: object
      properties:
        hasRemote:
          type: boolean
        owner:
          type: string
          nullable: true
        repo:
          type: string
          nullable: true
        url:
          type: string
        defaultBranch:
          type: string
      required:
        - hasRemote
      description: "Result of `git remote get-url origin` against `cwd`. `hasRemote: false` if the repo has no origin or git failed."
    TestConnectionResponse:
      type: object
      properties:
        ok:
          type: boolean
        repoInfo:
          type: object
          properties: {}
          additionalProperties:
            nullable: true
        error:
          type: string
      required:
        - ok
      description: "Result of `gh api repos/{owner}/{repo}` — `ok: true` with `repoInfo` on success; `ok: false` with `error` if `gh` rejects."
    ProjectExportEnvelope:
      type: object
      properties:
        version:
          anyOf:
            - type: number
              enum:
                - 3
            - type: integer
        type:
          type: string
        exportedAt:
          type: string
        project:
          type: object
          properties: {}
          additionalProperties:
            nullable: true
        crons:
          type: array
          items:
            type: object
            properties: {}
            additionalProperties:
              nullable: true
        rooms:
          type: array
          items:
            type: object
            properties: {}
            additionalProperties:
              nullable: true
        wiki:
          type: array
          items:
            type: object
            properties: {}
            additionalProperties:
              nullable: true
        kanban:
          type: object
          nullable: true
          properties: {}
          additionalProperties:
            nullable: true
      required:
        - version
      additionalProperties:
        nullable: true
      description: V3 project export envelope. Sub-objects (project, crons, rooms, …) are passed through to the import handler verbatim.
    ConfigExportEnvelope:
      type: object
      properties:
        version:
          anyOf:
            - type: number
              enum:
                - 1
            - type: number
              enum:
                - 2
            - type: integer
        exportedAt:
          type: string
        config:
          type: object
          properties: {}
          additionalProperties:
            nullable: true
        projects:
          type: array
          items:
            type: object
            properties: {}
            additionalProperties:
              nullable: true
        agents:
          type: array
          items:
            type: object
            properties: {}
            additionalProperties:
              nullable: true
        crons:
          type: array
          items:
            type: object
            properties: {}
            additionalProperties:
              nullable: true
        rooms:
          type: array
          items:
            type: object
            properties: {}
            additionalProperties:
              nullable: true
        slack:
          type: object
          properties:
            accounts:
              type: array
              items:
                type: object
                properties: {}
                additionalProperties:
                  nullable: true
          additionalProperties:
            nullable: true
      required:
        - version
      additionalProperties:
        nullable: true
      description: V1/V2 server-wide export envelope (config, projects/agents, crons, rooms, slack).
    ImportResult:
      type: object
      properties:
        message:
          type: string
        results:
          type: object
          additionalProperties:
            anyOf:
              - type: string
              - type: boolean
        project:
          type: object
          properties: {}
          additionalProperties:
            nullable: true
      required:
        - message
        - results
      description: Per-section result map — each entry is either `true`/`false` or a human-readable summary like `"3 new, 5 skipped"`.
    MaskedSmtpConfig:
      type: object
      properties:
        enabled:
          type: boolean
        host:
          type: string
        port:
          type: integer
        tlsMode:
          type: string
          enum:
            - none
            - starttls
            - ssl
        username:
          type: string
          nullable: true
        password:
          type: string
          nullable: true
          description: "`••••••••` when a password is stored, null otherwise."
        passwordSet:
          type: boolean
        from:
          type: string
        configured:
          type: boolean
      required:
        - enabled
        - host
        - port
        - tlsMode
        - username
        - password
        - passwordSet
        - from
        - configured
      description: SMTP settings with password masked.
    DbInstrumentationStats:
      type: object
      properties:
        enabled:
          type: boolean
          description: Whether statement timing is active.
        slowThresholdMs:
          type: number
          description: Calls at or above this wall-time (ms) count as slow.
        totalStatements:
          type: integer
          description: Distinct statement tags with at least one recorded call.
        totalCalls:
          type: integer
        totalSlowCalls:
          type: integer
        statements:
          type: array
          items:
            type: object
            properties:
              tag:
                type: string
                description: Statement key (never contains SQL text or params).
              count:
                type: integer
              totalMs:
                type: number
              maxMs:
                type: number
              slowCount:
                type: integer
            required:
              - tag
              - count
              - totalMs
              - maxMs
              - slowCount
          description: Per-statement aggregates, sorted by total wall time descending.
      required:
        - enabled
        - slowThresholdMs
        - totalStatements
        - totalCalls
        - totalSlowCalls
        - statements
      description: Aggregated prepared-statement wall-time timings.
    Cron:
      type: object
      properties:
        id:
          type: integer
        name:
          type: string
        schedule:
          type: string
        timezone:
          type: string
          nullable: true
        prompt:
          type: string
        cwd:
          type: string
        enabled:
          type: integer
        last_run:
          type: string
          nullable: true
        last_result:
          type: string
          nullable: true
        next_run_at:
          type: string
          nullable: true
        project_id:
          type: string
          nullable: true
        timeout_ms:
          type: integer
          nullable: true
        notify_on_run:
          type: integer
        model:
          type: string
          nullable: true
        skill_principal_agent_id:
          type: string
          nullable: true
        engine:
          type: string
          nullable: true
        owner_user_id:
          type: string
          nullable: true
        owner_username:
          type: string
          nullable: true
        shared:
          type: integer
        can_manage:
          type: boolean
        created_at:
          type: string
      required:
        - id
        - name
        - schedule
        - timezone
        - prompt
        - cwd
        - enabled
        - last_run
        - last_result
        - next_run_at
        - project_id
        - timeout_ms
        - notify_on_run
        - model
        - skill_principal_agent_id
        - engine
        - owner_user_id
        - shared
        - created_at
      description: A cron job row. Booleans (`enabled`, `notify_on_run`, `shared`) are stored as 0/1 SQLite ints. `timezone` is an optional IANA timezone used to interpret the cron expression; null falls back to the server scheduler timezone. `timeout_ms`, `model`, `project_id`, `skill_principal_agent_id`, `engine`, `owner_user_id` are nullable to mean "use default" / legacy system-owned. `engine` falls back to the skill principal agent's engine, then to `claude-code`; `owner_user_id` controls the spawn HOME for scheduled runs. `shared=1` makes the cron visible to the org while execution still uses the owner.
    CronLog:
      type: object
      properties:
        id:
          type: integer
        cron_id:
          type: integer
        timestamp:
          type: string
        status:
          type: string
          enum:
            - pending
            - running
            - success
            - error
        result:
          type: string
          nullable: true
        duration_ms:
          type: integer
          nullable: true
      required:
        - id
        - cron_id
        - timestamp
        - status
        - result
        - duration_ms
      description: A single cron run log row.
    CronThreadResponse:
      type: object
      properties:
        thread:
          nullable: true
        entries:
          type: array
          items:
            nullable: true
      required:
        - entries
      description: Cron thread (persistent output log). Both `thread` and `entries` come back null/empty when the cron has no project association or no run has produced a thread yet.
    CronRunAck:
      type: object
      properties:
        status:
          type: string
          enum:
            - running
      required:
        - status
      description: Acknowledgment from POST /api/crons/:id/run.
    CronDeleteAck:
      type: object
      properties:
        ok:
          type: boolean
          enum:
            - true
      required:
        - ok
      description: Delete acknowledgment.
    CronErrorResponse:
      type: object
      properties:
        error:
          type: string
        details:
          type: array
          items:
            type: object
            properties:
              path:
                type: array
                items:
                  anyOf:
                    - type: string
                    - type: number
              message:
                type: string
            required:
              - path
              - message
      required:
        - error
      description: Error envelope. `details` is populated for 400 schema-validation errors with one entry per failing Zod issue.
    DashboardErrorResponse:
      type: object
      properties:
        error:
          type: string
        activeOrgId:
          type: string
      required:
        - error
      description: Error envelope for dashboard route.
    DashboardResponse:
      type: object
      properties:
        orgId:
          type: string
        orgName:
          type: string
        isActive:
          type: boolean
        headline:
          type: object
          properties:
            projects:
              type: integer
            agents:
              type: integer
            sessions:
              type: integer
            activeSessions:
              type: integer
            openCards:
              type: integer
            openPRs:
              type: integer
            escalations:
              type: integer
          required:
            - projects
            - agents
            - sessions
            - activeSessions
            - openCards
            - openPRs
            - escalations
        kanban:
          type: object
          properties:
            totalBoards:
              type: integer
            totalCards:
              type: integer
            byColumn:
              type: array
              items:
                type: object
                properties:
                  columnName:
                    type: string
                  count:
                    type: integer
                required:
                  - columnName
                  - count
            byPriority:
              type: object
              properties:
                urgent:
                  type: integer
                high:
                  type: integer
                medium:
                  type: integer
                low:
                  type: integer
              required:
                - urgent
                - high
                - medium
                - low
          required:
            - totalBoards
            - totalCards
            - byColumn
            - byPriority
        activeSessions:
          type: array
          items:
            type: object
            properties:
              sessionId:
                type: string
              sessionName:
                type: string
              agentId:
                type: string
              agentName:
                type: string
              agentColor:
                type: string
                nullable: true
              engine:
                type: string
              model:
                type: string
                nullable: true
              prompt:
                type: string
              state:
                type: string
                enum:
                  - waiting_for_user_input
                  - working
                  - running_tests
                  - reviewing
                  - pending_checks
                  - pending_push
                  - pushed
                  - merged
              ownerUserId:
                type: string
                nullable: true
              ownerName:
                type: string
                nullable: true
              startedAt:
                type: string
                nullable: true
              lastActivityAt:
                type: string
            required:
              - sessionId
              - sessionName
              - agentId
              - agentName
              - agentColor
              - engine
              - model
              - prompt
              - state
              - ownerUserId
              - ownerName
              - startedAt
              - lastActivityAt
        openPRs:
          type: array
          items:
            type: object
            properties:
              key:
                type: string
              cardId:
                type: string
                nullable: true
              projectId:
                type: string
              projectName:
                type: string
              prUrl:
                type: string
              prNumber:
                type: integer
              title:
                type: string
              cardTitle:
                type: string
                nullable: true
              authorAgent:
                type: string
                nullable: true
              priority:
                type: string
                nullable: true
              updatedAt:
                type: integer
              mergeable:
                type: boolean
                nullable: true
              reviewDecision:
                type: string
                nullable: true
              reviewStatus:
                type: string
                nullable: true
            required:
              - key
              - cardId
              - projectId
              - projectName
              - prUrl
              - prNumber
              - title
              - cardTitle
              - authorAgent
              - priority
              - updatedAt
              - mergeable
              - reviewDecision
              - reviewStatus
        recentActivity:
          type: array
          items:
            type: object
            properties:
              type:
                type: string
                enum:
                  - card_created
                  - card_updated
                  - session_created
                  - escalation
                  - pr_created
              id:
                type: string
              title:
                type: string
              timestamp:
                type: string
              meta:
                type: object
                additionalProperties:
                  anyOf:
                    - type: string
                    - type: number
                    - nullable: true
            required:
              - type
              - id
              - title
              - timestamp
      required:
        - orgId
        - orgName
        - isActive
        - headline
        - kanban
        - activeSessions
        - openPRs
        - recentActivity
      description: Org-wide dashboard payload. `:id` may be an explicit org id or the alias `active`.
    ReleaseNotificationHistoryItem:
      type: object
      properties:
        id:
          type: string
        deployment_id:
          type: string
        release_item_id:
          type: string
          nullable: true
        support_ticket_id:
          type: string
          nullable: true
        notification_type:
          type: string
          enum:
            - ticket_release
            - release_digest
        recipient_type:
          type: string
          enum:
            - reporter
            - release_digest
        subject:
          type: string
        status:
          type: string
          enum:
            - pending
            - sending
            - sent
            - error
        attempts:
          type: integer
        sent_at:
          type: string
          nullable: true
        next_attempt_at:
          type: string
          nullable: true
        error_summary:
          type: string
          nullable: true
        can_retry:
          type: boolean
        created_at:
          type: string
        updated_at:
          type: string
      required:
        - id
        - deployment_id
        - release_item_id
        - support_ticket_id
        - notification_type
        - recipient_type
        - subject
        - status
        - attempts
        - sent_at
        - next_attempt_at
        - error_summary
        - can_retry
        - created_at
        - updated_at
    ReleaseNotificationRecipient:
      type: object
      properties:
        id:
          type: string
        deployment_id:
          type: string
        release_item_id:
          type: string
          nullable: true
        support_ticket_id:
          type: string
          nullable: true
        notification_type:
          type: string
          enum:
            - ticket_release
            - release_digest
        recipient_type:
          type: string
          enum:
            - reporter
            - release_digest
        recipient_email:
          type: string
        subject:
          type: string
        status:
          type: string
          enum:
            - pending
            - sending
            - sent
            - error
        attempts:
          type: integer
        sent_at:
          type: string
          nullable: true
        next_attempt_at:
          type: string
          nullable: true
        error_summary:
          type: string
          nullable: true
        created_at:
          type: string
        updated_at:
          type: string
      required:
        - id
        - deployment_id
        - release_item_id
        - support_ticket_id
        - notification_type
        - recipient_type
        - recipient_email
        - subject
        - status
        - attempts
        - sent_at
        - next_attempt_at
        - error_summary
        - created_at
        - updated_at
    DeploymentNotificationRecipientsResponse:
      type: object
      properties:
        recipients:
          type: array
          items:
            $ref: "#/components/schemas/ReleaseNotificationRecipient"
      required:
        - recipients
    Deployment:
      type: object
      properties:
        id:
          type: string
        project_id:
          type: string
        environment:
          type: string
        ref:
          type: string
        status:
          type: string
          enum:
            - pending
            - awaiting_approval
            - running
            - success
            - error
            - cancelled
        trigger:
          type: string
        triggered_by:
          type: string
          nullable: true
        source_deployment_id:
          type: string
          nullable: true
        runner_job_id:
          type: string
          nullable: true
        error:
          type: string
          nullable: true
        meta:
          nullable: true
        created_at:
          type: string
        started_at:
          type: string
          nullable: true
        completed_at:
          type: string
          nullable: true
        updated_at:
          type: string
      required:
        - id
        - project_id
        - environment
        - ref
        - status
        - trigger
        - triggered_by
        - source_deployment_id
        - runner_job_id
        - error
        - created_at
        - started_at
        - completed_at
        - updated_at
    DeploymentStep:
      type: object
      properties:
        id:
          type: string
        deployment_id:
          type: string
        name:
          type: string
        step_order:
          type: integer
        status:
          type: string
          enum:
            - pending
            - running
            - success
            - error
            - skipped
            - cancelled
        exit_code:
          type: integer
          nullable: true
        error:
          type: string
          nullable: true
        github_run_id:
          type: string
          nullable: true
        github_run_url:
          type: string
          nullable: true
        github_conclusion:
          type: string
          nullable: true
        started_at:
          type: string
          nullable: true
        completed_at:
          type: string
          nullable: true
        created_at:
          type: string
      required:
        - id
        - deployment_id
        - name
        - step_order
        - status
        - exit_code
        - error
        - github_run_id
        - github_run_url
        - github_conclusion
        - started_at
        - completed_at
        - created_at
    DeploymentApproval:
      type: object
      properties:
        id:
          type: string
        deployment_id:
          type: string
        approver_user_id:
          type: string
        approver_role:
          type: string
        decision:
          type: string
          enum:
            - approved
            - rejected
        note:
          type: string
          nullable: true
        created_at:
          type: string
      required:
        - id
        - deployment_id
        - approver_user_id
        - approver_role
        - decision
        - note
        - created_at
    DeploymentReleaseItem:
      type: object
      properties:
        id:
          type: string
        deployment_id:
          type: string
        card_id:
          type: string
        support_ticket_id:
          type: string
          nullable: true
        source:
          type: string
          enum:
            - derived
            - operator
        inclusion_status:
          type: string
          enum:
            - included
            - excluded
        operator_adjusted_by:
          type: string
          nullable: true
        operator_adjustment_note:
          type: string
          nullable: true
        operator_adjustment_meta:
          type: string
          nullable: true
        operator_adjusted_at:
          type: string
          nullable: true
        created_at:
          type: string
        updated_at:
          type: string
      required:
        - id
        - deployment_id
        - card_id
        - support_ticket_id
        - source
        - inclusion_status
        - operator_adjusted_by
        - operator_adjustment_note
        - operator_adjustment_meta
        - operator_adjusted_at
        - created_at
        - updated_at
    DeploymentReleaseItemReview:
      allOf:
        - $ref: "#/components/schemas/DeploymentReleaseItem"
        - type: object
          properties:
            card:
              type: object
              properties:
                id:
                  type: string
                title:
                  type: string
                shortId:
                  type: integer
                  nullable: true
                priority:
                  type: string
                  nullable: true
                columnName:
                  type: string
                  nullable: true
              required:
                - id
                - title
                - shortId
                - priority
                - columnName
            supportTicket:
              type: object
              nullable: true
              properties:
                id:
                  type: string
                subject:
                  type: string
                  nullable: true
                status:
                  type: string
                  nullable: true
                type:
                  type: string
                  nullable: true
                releaseState:
                  type: string
                  nullable: true
                  enum:
                    - fixed_pending_release
                    - released_to_prod
                    - customer_notified
                    - null
              required:
                - id
                - subject
                - status
                - type
                - releaseState
          required:
            - card
            - supportTicket
    DeploymentEnvironment:
      type: object
      properties:
        id:
          type: string
        project_id:
          type: string
        name:
          type: string
        current_ref:
          type: string
          nullable: true
        current_deployment_id:
          type: string
          nullable: true
        active_deployment_id:
          type: string
          nullable: true
        created_at:
          type: string
        updated_at:
          type: string
      required:
        - id
        - project_id
        - name
        - current_ref
        - current_deployment_id
        - active_deployment_id
        - created_at
        - updated_at
    DeployConfigStep:
      type: object
      properties:
        name:
          type: string
        run:
          type: string
      required:
        - name
        - run
    DeployConfigEnvironment:
      type: object
      properties:
        name:
          type: string
        approval:
          type: boolean
        runsOn:
          type: string
        timeoutMinutes:
          type: integer
        steps:
          type: array
          items:
            $ref: "#/components/schemas/DeployConfigStep"
        currentRef:
          type: string
          nullable: true
        currentDeploymentId:
          type: string
          nullable: true
        activeDeploymentId:
          type: string
          nullable: true
        activeDeployment:
          allOf:
            - $ref: "#/components/schemas/Deployment"
            - nullable: true
        currentDeployment:
          allOf:
            - $ref: "#/components/schemas/Deployment"
            - nullable: true
        lastDeployment:
          allOf:
            - $ref: "#/components/schemas/Deployment"
            - nullable: true
        rollbackTarget:
          allOf:
            - $ref: "#/components/schemas/Deployment"
            - nullable: true
      required:
        - name
        - approval
        - runsOn
        - timeoutMinutes
        - steps
        - currentRef
        - currentDeploymentId
        - activeDeploymentId
        - activeDeployment
        - currentDeployment
        - lastDeployment
        - rollbackTarget
    DeployConfigResponse:
      type: object
      properties:
        projectId:
          type: string
        configPath:
          type: string
        environments:
          type: array
          items:
            $ref: "#/components/schemas/DeployConfigEnvironment"
      required:
        - projectId
        - configPath
        - environments
    EnvironmentRuntimeConfig:
      type: object
      properties:
        id:
          type: string
        enabled:
          type: boolean
        meta:
          nullable: true
        createdAt:
          type: string
        updatedAt:
          type: string
      required:
        - id
        - enabled
        - createdAt
        - updatedAt
    ResolvedEnvironment:
      type: object
      properties:
        name:
          type: string
        active:
          type: boolean
        enabled:
          type: boolean
        deployable:
          type: boolean
        approval:
          type: boolean
          nullable: true
        runsOn:
          type: string
          nullable: true
        timeoutMinutes:
          type: integer
          nullable: true
        steps:
          type: array
          items:
            $ref: "#/components/schemas/DeployConfigStep"
        currentRef:
          type: string
          nullable: true
        currentDeploymentId:
          type: string
          nullable: true
        lastDeployment:
          allOf:
            - $ref: "#/components/schemas/Deployment"
            - nullable: true
        config:
          allOf:
            - $ref: "#/components/schemas/EnvironmentRuntimeConfig"
            - nullable: true
      required:
        - name
        - active
        - enabled
        - deployable
        - approval
        - runsOn
        - timeoutMinutes
        - steps
        - currentRef
        - currentDeploymentId
        - lastDeployment
        - config
    EnvironmentsReadResponse:
      type: object
      properties:
        projectId:
          type: string
        configPath:
          type: string
        environments:
          type: array
          items:
            $ref: "#/components/schemas/ResolvedEnvironment"
      required:
        - projectId
        - configPath
        - environments
    EnvironmentConfigDeleteResponse:
      type: object
      properties:
        removed:
          type: boolean
        projectId:
          type: string
        configPath:
          type: string
        environments:
          type: array
          items:
            $ref: "#/components/schemas/ResolvedEnvironment"
      required:
        - removed
        - projectId
        - configPath
        - environments
    DeployTrigger:
      type: object
      properties:
        id:
          type: string
        projectId:
          type: string
        environmentName:
          type: string
        event:
          type: string
          enum:
            - push
            - merge
        branchPattern:
          type: string
        enabled:
          type: boolean
        meta:
          nullable: true
        createdAt:
          type: string
        updatedAt:
          type: string
      required:
        - id
        - projectId
        - environmentName
        - event
        - branchPattern
        - enabled
        - createdAt
        - updatedAt
    DeployTriggerListResponse:
      type: object
      properties:
        projectId:
          type: string
        environmentName:
          type: string
        triggers:
          type: array
          items:
            $ref: "#/components/schemas/DeployTrigger"
      required:
        - projectId
        - environmentName
        - triggers
    DeployTriggerResponse:
      type: object
      properties:
        trigger:
          $ref: "#/components/schemas/DeployTrigger"
      required:
        - trigger
    DeployTriggerDeleteResponse:
      type: object
      properties:
        removed:
          type: boolean
      required:
        - removed
    DeploySchedule:
      type: object
      properties:
        id:
          type: string
        projectId:
          type: string
        environmentName:
          type: string
        ref:
          type: string
        cron:
          type: string
        timezone:
          type: string
          nullable: true
        ownerUserId:
          type: string
          nullable: true
        enabled:
          type: boolean
        meta:
          nullable: true
        createdAt:
          type: string
        updatedAt:
          type: string
      required:
        - id
        - projectId
        - environmentName
        - ref
        - cron
        - timezone
        - ownerUserId
        - enabled
        - createdAt
        - updatedAt
    DeployScheduleListResponse:
      type: object
      properties:
        projectId:
          type: string
        environmentName:
          type: string
        schedules:
          type: array
          items:
            $ref: "#/components/schemas/DeploySchedule"
      required:
        - projectId
        - environmentName
        - schedules
    DeployScheduleResponse:
      type: object
      properties:
        schedule:
          $ref: "#/components/schemas/DeploySchedule"
      required:
        - schedule
    DeployScheduleDeleteResponse:
      type: object
      properties:
        removed:
          type: boolean
      required:
        - removed
    ReleaseGateSelectionStatus:
      type: object
      properties:
        id:
          type: string
        state:
          type: string
          enum:
            - complete
            - pending
            - missing
      required:
        - id
        - state
    ReleaseGateProgress:
      type: object
      properties:
        sessions:
          type: array
          items:
            $ref: "#/components/schemas/ReleaseGateSelectionStatus"
        epics:
          type: array
          items:
            $ref: "#/components/schemas/ReleaseGateSelectionStatus"
        sessionsComplete:
          type: integer
        sessionsTotal:
          type: integer
        epicsComplete:
          type: integer
        epicsTotal:
          type: integer
        blocked:
          type: boolean
        satisfied:
          type: boolean
      required:
        - sessions
        - epics
        - sessionsComplete
        - sessionsTotal
        - epicsComplete
        - epicsTotal
        - blocked
        - satisfied
    DeployReleaseGate:
      type: object
      properties:
        id:
          type: string
        projectId:
          type: string
        environmentName:
          type: string
        ref:
          type: string
        sessionIds:
          type: array
          items:
            type: string
        epicIds:
          type: array
          items:
            type: string
        ownerUserId:
          type: string
          nullable: true
        status:
          type: string
          enum:
            - armed
            - fired
            - failed
        enabled:
          type: boolean
        firedDeploymentId:
          type: string
          nullable: true
        lastError:
          type: string
          nullable: true
        resolvedAt:
          type: string
          nullable: true
        progress:
          $ref: "#/components/schemas/ReleaseGateProgress"
        meta:
          nullable: true
        createdAt:
          type: string
        updatedAt:
          type: string
      required:
        - id
        - projectId
        - environmentName
        - ref
        - sessionIds
        - epicIds
        - ownerUserId
        - status
        - enabled
        - firedDeploymentId
        - lastError
        - resolvedAt
        - progress
        - createdAt
        - updatedAt
    DeployReleaseGateListResponse:
      type: object
      properties:
        projectId:
          type: string
        environmentName:
          type: string
        gates:
          type: array
          items:
            $ref: "#/components/schemas/DeployReleaseGate"
      required:
        - projectId
        - environmentName
        - gates
    DeployReleaseGateResponse:
      type: object
      properties:
        gate:
          $ref: "#/components/schemas/DeployReleaseGate"
      required:
        - gate
    DeployReleaseGateDeleteResponse:
      type: object
      properties:
        removed:
          type: boolean
      required:
        - removed
    ReleaseGateSessionCandidate:
      type: object
      properties:
        id:
          type: string
        label:
          type: string
      required:
        - id
        - label
    ReleaseGateCandidatesResponse:
      type: object
      properties:
        projectId:
          type: string
        sessions:
          type: array
          items:
            $ref: "#/components/schemas/ReleaseGateSessionCandidate"
      required:
        - projectId
        - sessions
    NotificationRouting:
      type: object
      properties:
        environmentName:
          type: string
        isProduction:
          type: boolean
        ticketReleaseEnabled:
          type: boolean
        releaseDigestEnabled:
          type: boolean
        isDefault:
          type: boolean
        updatedAt:
          type: string
          nullable: true
      required:
        - environmentName
        - isProduction
        - ticketReleaseEnabled
        - releaseDigestEnabled
        - isDefault
        - updatedAt
    NotificationRoutingResponse:
      type: object
      properties:
        projectId:
          type: string
        routing:
          $ref: "#/components/schemas/NotificationRouting"
      required:
        - projectId
        - routing
    DeploySetupWizardResponse:
      type: object
      properties:
        sessionId:
          type: string
        agentId:
          type: string
        session:
          type: object
          properties: {}
          additionalProperties:
            nullable: true
        configPath:
          type: string
      required:
        - sessionId
        - agentId
        - session
        - configPath
    DeploymentListResponse:
      type: object
      properties:
        deployments:
          type: array
          items:
            $ref: "#/components/schemas/Deployment"
        limit:
          type: integer
        offset:
          type: integer
      required:
        - deployments
        - limit
        - offset
    DeploymentDetailResponse:
      type: object
      properties:
        deployment:
          $ref: "#/components/schemas/Deployment"
        steps:
          type: array
          items:
            $ref: "#/components/schemas/DeploymentStep"
        approvals:
          type: array
          items:
            $ref: "#/components/schemas/DeploymentApproval"
        releaseItems:
          type: array
          items:
            $ref: "#/components/schemas/DeploymentReleaseItemReview"
        releaseNotifications:
          type: array
          items:
            $ref: "#/components/schemas/ReleaseNotificationHistoryItem"
        environment:
          allOf:
            - $ref: "#/components/schemas/DeploymentEnvironment"
            - nullable: true
        history:
          type: array
          items:
            $ref: "#/components/schemas/Deployment"
        logs:
          type: array
          items:
            type: object
            properties: {}
            additionalProperties:
              nullable: true
      required:
        - deployment
        - steps
        - approvals
        - releaseItems
        - releaseNotifications
        - environment
        - history
        - logs
    DeploymentReleaseItemListResponse:
      type: object
      properties:
        releaseItems:
          type: array
          items:
            $ref: "#/components/schemas/DeploymentReleaseItemReview"
      required:
        - releaseItems
    DeploymentReleaseItemResponse:
      type: object
      properties:
        releaseItem:
          $ref: "#/components/schemas/DeploymentReleaseItemReview"
        releaseItems:
          type: array
          items:
            $ref: "#/components/schemas/DeploymentReleaseItemReview"
      required:
        - releaseItem
        - releaseItems
    DeploymentReleaseDigestResponse:
      type: object
      properties:
        digestMarkdown:
          type: string
        settings:
          type: object
          properties:
            isDefault:
              type: boolean
            updatedAt:
              type: string
              nullable: true
          required:
            - isDefault
            - updatedAt
      required:
        - digestMarkdown
        - settings
    ReleaseNotificationRetryResponse:
      type: object
      properties:
        notification:
          $ref: "#/components/schemas/ReleaseNotificationHistoryItem"
        releaseNotifications:
          type: array
          items:
            $ref: "#/components/schemas/ReleaseNotificationHistoryItem"
      required:
        - notification
        - releaseNotifications
    DeploymentActionResponse:
      type: object
      properties:
        deployment:
          $ref: "#/components/schemas/Deployment"
        steps:
          type: array
          items:
            $ref: "#/components/schemas/DeploymentStep"
        approvals:
          type: array
          items:
            $ref: "#/components/schemas/DeploymentApproval"
      required:
        - deployment
        - steps
        - approvals
    DeploymentErrorResponse:
      type: object
      properties:
        error:
          type: string
      required:
        - error
    DevServerWizardErrorResponse:
      type: object
      properties:
        error:
          type: string
        message:
          type: string
      required:
        - error
      description: Error envelope for Dev Server wizard routes.
    DevServerStartCommandCandidate:
      type: object
      properties:
        command:
          type: string
        script:
          type: string
        raw:
          type: string
        recommended:
          type: boolean
      required:
        - command
        - script
        - raw
        - recommended
    DevServerPortGuess:
      type: object
      properties:
        internalPort:
          type: integer
        label:
          type: string
        source:
          type: string
      required:
        - internalPort
        - label
        - source
    DevServerSetupDraft:
      type: object
      properties:
        cwd:
          type: string
        packageManager:
          type: string
          nullable: true
          enum:
            - npm
            - pnpm
            - yarn
            - bun
            - null
        isMonorepo:
          type: boolean
        monorepoDirs:
          type: array
          items:
            type: string
        startCommandCandidates:
          type: array
          items:
            $ref: "#/components/schemas/DevServerStartCommandCandidate"
        frameworks:
          type: array
          items:
            type: string
        portGuesses:
          type: array
          items:
            $ref: "#/components/schemas/DevServerPortGuess"
        healthPathGuess:
          type: string
        existing:
          nullable: true
          description: Current `prEnv.devServer` config, or null.
        readme:
          type: object
          properties:
            path:
              type: string
              nullable: true
            excerpt:
              type: string
              nullable: true
          required:
            - path
            - excerpt
      required:
        - cwd
        - packageManager
        - isMonorepo
        - monorepoDirs
        - startCommandCandidates
        - frameworks
        - portGuesses
        - healthPathGuess
        - readme
      description: "Server-precomputed Dev Server scan: start-command candidates, package manager, monorepo layout, framework/port guesses, health-path default, existing config, and a README excerpt."
    DevServerSetupDraftResponse:
      type: object
      properties:
        projectId:
          type: string
        draft:
          $ref: "#/components/schemas/DevServerSetupDraft"
      required:
        - projectId
        - draft
      description: Dev Server detection result for a project.
    DevServerWizardStartResponse:
      type: object
      properties:
        sessionId:
          type: string
        agentId:
          type: string
        draft:
          $ref: "#/components/schemas/DevServerSetupDraft"
        session:
          nullable: true
          description: Raw `sessions` row for the spawned wizard session.
      required:
        - sessionId
        - agentId
        - draft
      description: Dev Server setup wizard session spawned successfully.
    DevServerSetupApplyRequest:
      type: object
      properties:
        devServer:
          type: object
          additionalProperties:
            nullable: true
          description: The authored `prEnv.devServer` block (startCommand, env, secretKeys, portMap, healthPath, readyTimeoutMs, cwd, aptPackages). Validated by parseDevServerConfig; the first `prEnv.devServer.<path>` issue is returned as a 400.
        secrets:
          type: object
          properties:
            env:
              type: string
              description: Dotenv `KEY=value` lines of secret values.
            mode:
              type: string
              enum:
                - merge
                - replace
            defaultKind:
              type: string
              enum:
                - plain
                - secret
          required:
            - env
          description: Freshly-typed secret values to store encrypted. The config references these by name via `devServer.secretKeys`; plaintext values never live in the config.
      required:
        - devServer
      description: Persist payload for the Dev Server setup wizard.
    DevServerSetupApplyResponse:
      type: object
      properties:
        ok:
          type: boolean
          enum:
            - true
        secretsImported:
          type: integer
      required:
        - ok
        - secretsImported
      description: Dev-server config persisted to the project record.
    DevServerWizardOkResponse:
      type: object
      properties:
        ok:
          type: boolean
          enum:
            - true
      required:
        - ok
    EscalationsErrorResponse:
      type: object
      properties:
        error:
          type: string
        details:
          type: array
          items:
            type: object
            properties:
              path:
                type: array
                items:
                  anyOf:
                    - type: string
                    - type: number
              message:
                type: string
            required:
              - path
              - message
      required:
        - error
      description: Error envelope for escalation routes.
    Escalation:
      type: object
      properties:
        id:
          type: string
        project_id:
          type: string
        type:
          type: string
          enum:
            - merge_conflict
            - ci_failure
            - review_needed
            - blocker
        title:
          type: string
        description:
          type: string
          nullable: true
        pr_number:
          type: integer
          nullable: true
        pr_url:
          type: string
          nullable: true
        card_id:
          type: string
          nullable: true
        source:
          type: string
          nullable: true
        acknowledged_at:
          type: string
          nullable: true
        created_at:
          type: string
      required:
        - id
        - project_id
        - type
        - title
        - description
        - pr_number
        - pr_url
        - card_id
        - source
        - acknowledged_at
        - created_at
      additionalProperties:
        nullable: true
      description: A single escalation row.
    FinalizeCiConfigErrorResponse:
      type: object
      properties:
        error:
          type: string
        message:
          type: string
        code:
          type: string
        path:
          type: string
          nullable: true
      required:
        - error
      description: Error envelope for server-stored Finalize CI config routes.
    FinalizeServerCiConfigView:
      type: object
      properties:
        scope:
          type: string
          enum:
            - project
            - personal
        ci_yaml_content:
          type: string
          description: The stored ci.yaml text.
        updated_by:
          type: string
          nullable: true
          description: User id of the last writer.
        updated_at:
          type: number
          description: Epoch ms of the last write.
      required:
        - scope
        - ci_yaml_content
        - updated_by
        - updated_at
      description: A server-stored ci.yaml config for one scope.
    FinalizeServerCiConfigGetResponse:
      type: object
      properties:
        project_id:
          type: string
        project:
          allOf:
            - $ref: "#/components/schemas/FinalizeServerCiConfigView"
            - nullable: true
              description: The project-scoped (shared) config, or null when unset.
        personal:
          allOf:
            - $ref: "#/components/schemas/FinalizeServerCiConfigView"
            - nullable: true
              description: The caller's personal override, or null when unset / no user.
      required:
        - project_id
        - project
        - personal
      description: Server-stored ci.yaml configs visible to the caller.
    FinalizeServerCiConfigPutRequest:
      type: object
      properties:
        ci_yaml_content:
          type: string
          description: Verbatim ci.yaml. Validated against the ci.yaml schema before it is stored.
        scope:
          type: string
          enum:
            - project
            - personal
          description: Which scope to write. Defaults to 'project' (shared). 'personal' stores an override keyed to the calling user.
      required:
        - ci_yaml_content
      description: Upsert a server-stored ci.yaml config.
    FinalizeServerCiConfigPutResponse:
      type: object
      properties:
        ok:
          type: boolean
          enum:
            - true
        project_id:
          type: string
        config:
          $ref: "#/components/schemas/FinalizeServerCiConfigView"
      required:
        - ok
        - project_id
        - config
      description: The stored config after upsert.
    FinalizeServerCiConfigDeleteResponse:
      type: object
      properties:
        ok:
          type: boolean
          enum:
            - true
        project_id:
          type: string
        scope:
          type: string
          enum:
            - project
            - personal
        deleted:
          type: boolean
          description: True when a row was actually removed.
      required:
        - ok
        - project_id
        - scope
        - deleted
      description: Delete outcome.
    ParityError:
      type: object
      properties:
        error:
          type: string
        message:
          type: string
      required:
        - error
    ParityJob:
      type: object
      properties:
        name:
          type: string
        state:
          type: string
          enum:
            - green
            - red
            - unknown
            - skipped
      required:
        - name
        - state
      description: One CI job/check, normalized to a name + parity state.
    ParityRecord:
      type: object
      properties:
        id:
          type: string
        project_id:
          type: string
        pr_number:
          type: integer
          nullable: true
        commit_sha:
          type: string
        run_id:
          type: string
          nullable: true
        finalize_verdict:
          type: string
          enum:
            - green
            - red
            - unknown
        finalize_jobs:
          type: array
          items:
            $ref: "#/components/schemas/ParityJob"
        github_verdict:
          type: string
          enum:
            - green
            - red
            - unknown
        github_jobs:
          type: array
          items:
            $ref: "#/components/schemas/ParityJob"
        divergence_class:
          type: string
          enum:
            - agree_green
            - agree_red
            - false_green
            - false_red
            - indeterminate
        note:
          type: string
          nullable: true
        observed_at:
          type: integer
      required:
        - id
        - project_id
        - pr_number
        - commit_sha
        - run_id
        - finalize_verdict
        - finalize_jobs
        - github_verdict
        - github_jobs
        - divergence_class
        - note
        - observed_at
      description: "One per-commit parity observation: the Finalize verdict + per-job states vs the GitHub Actions verdict + per-job states, with the derived divergence class."
    ParitySummary:
      type: object
      properties:
        total:
          type: integer
        agree_green:
          type: integer
        agree_red:
          type: integer
        false_green:
          type: integer
        false_red:
          type: integer
        indeterminate:
          type: integer
      required:
        - total
        - agree_green
        - agree_red
        - false_green
        - false_red
        - indeterminate
      description: Count of records per divergence class for the window.
    ParityListResponse:
      type: object
      properties:
        project_id:
          type: string
        range:
          type: object
          properties:
            from_ms:
              type: integer
            to_ms:
              type: integer
            from_iso:
              type: string
            to_iso:
              type: string
          required:
            - from_ms
            - to_ms
            - from_iso
            - to_iso
        summary:
          $ref: "#/components/schemas/ParitySummary"
        records:
          type: array
          items:
            $ref: "#/components/schemas/ParityRecord"
      required:
        - project_id
        - range
        - summary
        - records
    ParityRecordRequest:
      type: object
      properties:
        commit_sha:
          type: string
        pr_number:
          type: integer
          nullable: true
          minimum: 0
          exclusiveMinimum: true
        run_id:
          type: string
          nullable: true
        finalize_verdict:
          type: string
          enum:
            - green
            - red
            - unknown
        finalize_jobs:
          type: array
          items:
            $ref: "#/components/schemas/ParityJob"
        github_verdict:
          type: string
          enum:
            - green
            - red
            - unknown
        github_jobs:
          type: array
          items:
            $ref: "#/components/schemas/ParityJob"
        note:
          type: string
          nullable: true
      required:
        - commit_sha
        - finalize_verdict
        - github_verdict
      description: Record (or update, idempotent on commit) one parity observation.
    ParityRecordResponse:
      type: object
      properties:
        record:
          $ref: "#/components/schemas/ParityRecord"
      required:
        - record
    ParitySeedResponse:
      type: object
      properties:
        project_id:
          type: string
        seeded:
          type: integer
        records:
          type: array
          items:
            $ref: "#/components/schemas/ParityRecord"
      required:
        - project_id
        - seeded
        - records
    QuarantineError:
      type: object
      properties:
        error:
          type: string
        message:
          type: string
      required:
        - error
    QuarantineEntry:
      type: object
      properties:
        id:
          type: string
        job_id:
          type: string
        matrix_key:
          type: string
        owner:
          type: string
        reason:
          type: string
          nullable: true
        quarantined_at:
          type: integer
        expires_at:
          type: integer
        created_by:
          type: string
          nullable: true
        status:
          type: string
          enum:
            - active
            - overdue
        days_until_expiry:
          type: integer
      required:
        - id
        - job_id
        - matrix_key
        - owner
        - reason
        - quarantined_at
        - expires_at
        - created_by
        - status
        - days_until_expiry
      description: "One quarantine-lane entry: a flaky job instance excused from blocking the gate, with a named owner and an expiry. `overdue` means it is past expiry and no longer excuses."
    QuarantineListResponse:
      type: object
      properties:
        project_id:
          type: string
        now:
          type: integer
        max_days:
          type: integer
        default_days:
          type: integer
        active:
          type: array
          items:
            $ref: "#/components/schemas/QuarantineEntry"
        overdue:
          type: array
          items:
            $ref: "#/components/schemas/QuarantineEntry"
      required:
        - project_id
        - now
        - max_days
        - default_days
        - active
        - overdue
    QuarantineCreateRequest:
      type: object
      properties:
        job_id:
          type: string
          description: The ci.yaml job id to quarantine.
        matrix_key:
          type: string
          description: Matrix-shard discriminator; omit/empty for non-matrix jobs.
        owner:
          type: string
          description: Named owner accountable for resolving the flake.
        reason:
          type: string
          description: Why this instance is quarantined.
        days:
          type: number
          description: Quarantine duration in days; clamped to ≤30 (default 30).
        created_by:
          type: string
      required:
        - job_id
        - owner
    QuarantineCreateResponse:
      type: object
      properties:
        project_id:
          type: string
        entry:
          $ref: "#/components/schemas/QuarantineEntry"
      required:
        - project_id
        - entry
    FlakeInstance:
      type: object
      properties:
        job_id:
          type: string
        matrix_key:
          type: string
        runs:
          type: integer
        failed_runs:
          type: integer
        flaked_runs:
          type: integer
        flake_rate:
          type: number
        fail_rate:
          type: number
        last_seen:
          type: integer
        quarantined:
          type: boolean
      required:
        - job_id
        - matrix_key
        - runs
        - failed_runs
        - flaked_runs
        - flake_rate
        - fail_rate
        - last_seen
        - quarantined
      description: Per job/matrix instance flake statistics over the requested window. `flake_rate` = fraction of runs that flaked within the run OR ended failed.
    FlakeListResponse:
      type: object
      properties:
        project_id:
          type: string
        window_days:
          type: integer
        instances:
          type: array
          items:
            $ref: "#/components/schemas/FlakeInstance"
      required:
        - project_id
        - window_days
        - instances
    FinalizeWizardErrorResponse:
      type: object
      properties:
        error:
          type: string
        message:
          type: string
      required:
        - error
      description: Error envelope for finalize wizard routes.
    FinalizeSetupSubproject:
      type: object
      properties:
        path:
          type: string
          description: '`"."` for the root, else POSIX-relative.'
        manifest:
          type: string
        manager:
          type: string
          nullable: true
          enum:
            - npm
            - pnpm
            - yarn
            - pip
            - poetry
            - cargo
            - go
            - null
      required:
        - path
        - manifest
        - manager
      description: A detected sub-project (root + apps/* + packages/*).
    FinalizeNpmScriptHit:
      type: object
      properties:
        name:
          type: string
        body:
          type: string
        kind:
          type: string
          enum:
            - test
            - lint
            - typecheck
            - build
            - format
            - other
      required:
        - name
        - body
        - kind
      description: Top-level package.json script bucketed by name + body.
    FinalizeSetupDraft:
      type: object
      properties:
        existingCi:
          type: boolean
        existingCiContent:
          type: string
          nullable: true
        stack:
          type: string
          enum:
            - node
            - python
            - rust
            - go
            - mixed
            - unknown
        packageManager:
          type: string
          nullable: true
          enum:
            - npm
            - pnpm
            - yarn
            - pip
            - poetry
            - cargo
            - go
            - null
        isMonorepo:
          type: boolean
        subprojects:
          type: array
          items:
            $ref: "#/components/schemas/FinalizeSetupSubproject"
        githubWorkflows:
          type: array
          items:
            type: string
        makefileTargets:
          type: array
          items:
            type: string
        npmScripts:
          type: array
          items:
            $ref: "#/components/schemas/FinalizeNpmScriptHit"
        readme:
          type: object
          properties:
            readmePath:
              type: string
              nullable: true
            setupExcerpt:
              type: string
              nullable: true
            hasDockerHints:
              type: boolean
            envKeysFromReadme:
              type: array
              items:
                type: string
          required:
            - readmePath
            - setupExcerpt
            - hasDockerHints
            - envKeysFromReadme
        envVars:
          type: array
          items:
            type: object
            properties:
              key:
                type: string
              sources:
                type: array
                items:
                  type: string
              required:
                type: boolean
            required:
              - key
              - sources
              - required
        proposedCiYaml:
          type: string
      required:
        - existingCi
        - existingCiContent
        - stack
        - packageManager
        - isMonorepo
        - subprojects
        - githubWorkflows
        - makefileTargets
        - npmScripts
        - readme
        - envVars
        - proposedCiYaml
      description: Server-precomputed signal for the Finalize ci.yaml setup wizard. `proposedCiYaml` always parses against the v1 schema.
    FinalizeWizardResolvedTarget:
      type: object
      properties:
        sessionId:
          type: string
        branch:
          type: string
        worktreePath:
          type: string
      required:
        - sessionId
        - branch
        - worktreePath
      description: "Legacy shape retained for response compatibility. The setup wizard now always returns `target: null` because the spawned session owns its own worktree (`use_worktree=1`) and passes its own `session_id` to `setup-apply`."
    FinalizeWizardStartResponse:
      type: object
      properties:
        sessionId:
          type: string
        agentId:
          type: string
        draft:
          $ref: "#/components/schemas/FinalizeSetupDraft"
        session:
          nullable: true
          description: Raw `sessions` row for the spawned wizard session.
        target:
          allOf:
            - $ref: "#/components/schemas/FinalizeWizardResolvedTarget"
            - nullable: true
              description: Always `null`. The setup session owns its own worktree; there is no separate commit target to surface.
      required:
        - sessionId
        - agentId
        - draft
        - target
      description: Wizard session spawned successfully.
    FinalizeSetupApplyRequest:
      type: object
      properties:
        ci_yaml_content:
          type: string
          description: Verbatim `.agent-hub/ci.yaml` to commit. Validated against the v1 parser.
        session_id:
          type: string
          description: Target session id whose worktree receives the commit. The setup wizard passes its own session id. When omitted the server picks the most-recent project session that has a worktree, or auto-provisions a dedicated `[Finalize Config]` worktree on a fresh branch when none exists.
        secrets:
          type: object
          properties:
            env:
              type: string
              description: Dotenv blob (`KEY=value` lines).
            mode:
              type: string
              enum:
                - merge
                - replace
            defaultKind:
              type: string
              enum:
                - plain
                - secret
          required:
            - env
          description: Optional project secrets to persist (same shape as preview setup-apply). Merged into Finalize step runs.
        storage:
          type: string
          enum:
            - committed
            - server
          description: Where the config lives. Default 'committed' writes + commits `.agent-hub/ci.yaml`. 'server' stores it on the Agent Hub server (no file, no commit) — the response omits commit/branch fields.
        server_scope:
          type: string
          enum:
            - project
            - personal
          description: "Only for `storage: 'server'`. 'project' (default) is the shared config; 'personal' is an override keyed to the calling user."
      required:
        - ci_yaml_content
      description: Apply payload for the Finalize ci.yaml wizard.
    FinalizeSetupApplyServerResponse:
      type: object
      properties:
        ok:
          type: boolean
          enum:
            - true
        storage:
          type: string
          enum:
            - server
        server_scope:
          type: string
          enum:
            - project
            - personal
        secrets_imported:
          type: integer
      required:
        - ok
        - storage
        - server_scope
        - secrets_imported
      description: Config stored on the Agent Hub server (no commit).
    FinalizeSetupApplyResponse:
      type: object
      properties:
        ok:
          type: boolean
          enum:
            - true
        file:
          type: string
          description: Relative path of the committed file.
        commit_sha:
          type: string
        branch:
          type: string
        session_id:
          type: string
        secrets_imported:
          type: integer
          description: Count of secret keys imported from an optional `secrets` payload.
      required:
        - ok
        - file
        - commit_sha
        - branch
        - session_id
        - secrets_imported
      description: Commit landed on the worktree branch.
    FinalizeSetupApplyInvalidConfig:
      type: object
      properties:
        error:
          type: string
          enum:
            - ci_config_invalid
        code:
          type: string
        message:
          type: string
        path:
          type: string
          nullable: true
      required:
        - error
        - code
        - message
        - path
      description: YAML failed the v1 schema. `code` is the `CiConfigErrorCode` from `server/finalize/ci-config.ts`; `path` is the offending dotted field path when meaningful.
    FinalizeWizardCompleteResponse:
      type: object
      properties:
        ok:
          type: boolean
          enum:
            - true
      required:
        - ok
      description: Acknowledgement envelope.
    GoogleCalendarErrorResponse:
      type: object
      properties:
        error:
          type: string
        code:
          type: string
        requiredScopes:
          type: array
          items:
            type: string
      required:
        - error
    GoogleCalendarDateTime:
      type: object
      properties:
        date:
          type: string
          description: All-day date in YYYY-MM-DD form.
        dateTime:
          type: string
          description: RFC3339 timestamp. Offset is required unless timeZone is specified.
          example: 2026-06-30T09:00:00-07:00
        timeZone:
          type: string
          minLength: 1
      additionalProperties: false
    GoogleCalendarAttendee:
      type: object
      properties:
        email:
          type: string
          format: email
        displayName:
          type: string
        optional:
          type: boolean
        responseStatus:
          type: string
          enum:
            - needsAction
            - declined
            - tentative
            - accepted
      required:
        - email
      additionalProperties: false
    GoogleCalendarEventInput:
      type: object
      properties:
        summary:
          type: string
        description:
          type: string
        location:
          type: string
        start:
          $ref: "#/components/schemas/GoogleCalendarDateTime"
        end:
          $ref: "#/components/schemas/GoogleCalendarDateTime"
        attendees:
          type: array
          items:
            $ref: "#/components/schemas/GoogleCalendarAttendee"
        recurrence:
          type: array
          items:
            type: string
        colorId:
          type: string
        transparency:
          type: string
          enum:
            - opaque
            - transparent
        visibility:
          type: string
          enum:
            - default
            - public
            - private
            - confidential
        reminders:
          type: object
          properties:
            useDefault:
              type: boolean
            overrides:
              type: array
              items:
                type: object
                properties:
                  method:
                    type: string
                    enum:
                      - email
                      - popup
                  minutes:
                    type: integer
                    minimum: 0
                    maximum: 40320
                required:
                  - method
                  - minutes
                additionalProperties: false
          additionalProperties: false
      required:
        - start
        - end
      additionalProperties: false
    GoogleCalendarEventPatch:
      type: object
      properties:
        summary:
          type: string
        description:
          type: string
        location:
          type: string
        start:
          $ref: "#/components/schemas/GoogleCalendarDateTime"
        end:
          $ref: "#/components/schemas/GoogleCalendarDateTime"
        attendees:
          type: array
          items:
            $ref: "#/components/schemas/GoogleCalendarAttendee"
        recurrence:
          type: array
          items:
            type: string
        colorId:
          type: string
        transparency:
          type: string
          enum:
            - opaque
            - transparent
        visibility:
          type: string
          enum:
            - default
            - public
            - private
            - confidential
        reminders:
          type: object
          properties:
            useDefault:
              type: boolean
            overrides:
              type: array
              items:
                type: object
                properties:
                  method:
                    type: string
                    enum:
                      - email
                      - popup
                  minutes:
                    type: integer
                    minimum: 0
                    maximum: 40320
                required:
                  - method
                  - minutes
                additionalProperties: false
          additionalProperties: false
      additionalProperties: false
    GoogleCalendarEvent:
      type: object
      properties:
        id:
          type: string
          nullable: true
        status:
          type: string
          nullable: true
        htmlLink:
          type: string
          nullable: true
        summary:
          type: string
          nullable: true
        description:
          type: string
          nullable: true
        location:
          type: string
          nullable: true
        start:
          allOf:
            - $ref: "#/components/schemas/GoogleCalendarDateTime"
            - nullable: true
        end:
          allOf:
            - $ref: "#/components/schemas/GoogleCalendarDateTime"
            - nullable: true
        creator:
          type: object
          nullable: true
          properties:
            email:
              type: string
              nullable: true
            displayName:
              type: string
              nullable: true
          required:
            - email
            - displayName
        organizer:
          type: object
          nullable: true
          properties:
            email:
              type: string
              nullable: true
            displayName:
              type: string
              nullable: true
          required:
            - email
            - displayName
        attendees:
          type: array
          items:
            $ref: "#/components/schemas/GoogleCalendarAttendee"
        created:
          type: string
          nullable: true
        updated:
          type: string
          nullable: true
        etag:
          type: string
          nullable: true
        recurringEventId:
          type: string
          nullable: true
        hangoutLink:
          type: string
          nullable: true
      required:
        - id
        - status
        - htmlLink
        - summary
        - description
        - location
        - start
        - end
        - creator
        - organizer
        - created
        - updated
        - etag
        - recurringEventId
        - hangoutLink
    GoogleDriveErrorResponse:
      type: object
      properties:
        error:
          type: string
        code:
          type: string
        requiredScopes:
          type: array
          items:
            type: string
      required:
        - error
    GoogleDriveFile:
      type: object
      properties:
        id:
          type: string
          nullable: true
        name:
          type: string
          nullable: true
        mimeType:
          type: string
          nullable: true
        iconLink:
          type: string
          nullable: true
        webViewLink:
          type: string
          nullable: true
        modifiedTime:
          type: string
          nullable: true
        createdTime:
          type: string
          nullable: true
        size:
          type: string
          nullable: true
        owners:
          type: array
          nullable: true
          items:
            type: object
            properties:
              displayName:
                type: string
                nullable: true
              emailAddress:
                type: string
                nullable: true
            required:
              - displayName
              - emailAddress
        trashed:
          type: boolean
          nullable: true
      required:
        - id
        - name
        - mimeType
        - iconLink
        - webViewLink
        - modifiedTime
        - createdTime
        - size
        - owners
        - trashed
    GoogleGmailErrorResponse:
      type: object
      properties:
        error:
          type: string
        code:
          type: string
        requiredScopes:
          type: array
          items:
            type: string
      required:
        - error
    GoogleGmailThreadSummary:
      type: object
      properties:
        id:
          type: string
          nullable: true
        snippet:
          type: string
          nullable: true
        historyId:
          type: string
          nullable: true
      required:
        - id
        - snippet
        - historyId
    GoogleGmailMessage:
      type: object
      properties:
        id:
          type: string
          nullable: true
        threadId:
          type: string
          nullable: true
        labelIds:
          type: array
          items:
            type: string
        snippet:
          type: string
          nullable: true
        historyId:
          type: string
          nullable: true
        internalDate:
          type: string
          nullable: true
        sizeEstimate:
          type: number
          nullable: true
        from:
          type: string
          nullable: true
        to:
          type: string
          nullable: true
        cc:
          type: string
          nullable: true
        bcc:
          type: string
          nullable: true
        subject:
          type: string
          nullable: true
        date:
          type: string
          nullable: true
        messageIdHeader:
          type: string
          nullable: true
        bodyText:
          type: string
          nullable: true
        bodyHtml:
          type: string
          nullable: true
      required:
        - id
        - threadId
        - labelIds
        - snippet
        - historyId
        - internalDate
        - sizeEstimate
        - from
        - to
        - cc
        - bcc
        - subject
        - date
        - messageIdHeader
        - bodyText
        - bodyHtml
    GoogleGmailThread:
      type: object
      properties:
        id:
          type: string
          nullable: true
        historyId:
          type: string
          nullable: true
        messages:
          type: array
          items:
            $ref: "#/components/schemas/GoogleGmailMessage"
      required:
        - id
        - historyId
        - messages
    GoogleSheetsErrorResponse:
      type: object
      properties:
        error:
          type: string
        code:
          type: string
        requiredScopes:
          type: array
          items:
            type: string
      required:
        - error
    GoogleSheetsValueRange:
      type: object
      properties:
        range:
          type: string
          nullable: true
        majorDimension:
          type: string
          nullable: true
        values:
          type: array
          nullable: true
          items:
            type: array
            items:
              anyOf:
                - type: string
                - type: number
                - type: boolean
      required:
        - range
        - majorDimension
        - values
    GoogleSheetsTab:
      type: object
      properties:
        sheetId:
          type: number
          nullable: true
        title:
          type: string
          nullable: true
        index:
          type: number
          nullable: true
        sheetType:
          type: string
          nullable: true
        rowCount:
          type: number
          nullable: true
        columnCount:
          type: number
          nullable: true
      required:
        - sheetId
        - title
        - index
        - sheetType
        - rowCount
        - columnCount
    GoogleSheetsSpreadsheetMetadata:
      type: object
      properties:
        spreadsheetId:
          type: string
          nullable: true
        title:
          type: string
          nullable: true
        locale:
          type: string
          nullable: true
        timeZone:
          type: string
          nullable: true
        spreadsheetUrl:
          type: string
          nullable: true
        sheets:
          type: array
          items:
            $ref: "#/components/schemas/GoogleSheetsTab"
      required:
        - spreadsheetId
        - title
        - locale
        - timeZone
        - spreadsheetUrl
        - sheets
    HooksErrorResponse:
      type: object
      properties:
        error:
          type: string
        details:
          type: array
          items:
            type: object
            properties:
              path:
                type: array
                items:
                  anyOf:
                    - type: string
                    - type: number
              message:
                type: string
            required:
              - path
              - message
      required:
        - error
      description: Error envelope for hook routes.
    InfraAlertRoutingEntry:
      type: object
      properties:
        severity:
          type: string
          enum:
            - critical
            - warning
            - info
        channel:
          type: string
          enum:
            - in_app
            - push
            - email
        enabled:
          type: boolean
      required:
        - severity
        - channel
        - enabled
    InfraAlertRoutingResponse:
      type: object
      properties:
        projectId:
          type: string
        routing:
          type: array
          items:
            type: object
            properties:
              projectId:
                type: string
              severity:
                type: string
                enum:
                  - critical
                  - warning
                  - info
              channels:
                type: object
                properties:
                  in_app:
                    type: boolean
                  push:
                    type: boolean
                  email:
                    type: boolean
                required:
                  - in_app
                  - push
                  - email
              isDefault:
                type: boolean
              overrides:
                type: array
                items:
                  type: object
                  properties:
                    id:
                      type: string
                    project_id:
                      type: string
                    severity:
                      type: string
                      enum:
                        - critical
                        - warning
                        - info
                    channel:
                      type: string
                      enum:
                        - in_app
                        - push
                        - email
                    enabled:
                      type: integer
                    created_at:
                      type: integer
                    updated_at:
                      type: integer
                  required:
                    - id
                    - project_id
                    - severity
                    - channel
                    - enabled
                    - created_at
                    - updated_at
            required:
              - projectId
              - severity
              - channels
              - isDefault
              - overrides
      required:
        - projectId
        - routing
    InfraAlertRoutingError:
      type: object
      properties:
        error:
          type: string
      required:
        - error
    InfraAlertRuleCreateRequest:
      type: object
      properties:
        name:
          type: string
          minLength: 1
          maxLength: 200
          description: Operator-facing rule name, shown on the alert.
          example: ALB unhealthy hosts
        description:
          type: string
          nullable: true
          maxLength: 2000
        service:
          type: string
          minLength: 1
          maxLength: 64
          description: "Service token the rule applies to (`ec2`, `rds`, `elbv2`, …), matching `infra_resources.service`. Required: it decides which namespace the rule can apply to."
          example: elbv2
        accountId:
          type: string
          nullable: true
          maxLength: 64
          description: Narrow to one account. Null/absent matches every account in scope.
        region:
          type: string
          nullable: true
          maxLength: 32
          description: Narrow to one region. Null/absent matches every region in scope.
        resourceKey:
          type: string
          nullable: true
          maxLength: 512
          description: Pin to exactly one resource (`infra_resources.resource_key`). Null/absent matches every resource satisfying the rest of the selector, including ones discovered after the rule was written.
        tagFilter:
          type: object
          nullable: true
          additionalProperties:
            type: array
            items:
              type: string
              maxLength: 256
            minItems: 1
            maxItems: 50
          description: Tag predicate, e.g. `{"Environment":["prod"]}`. An empty object is stored as no filter.
        namespace:
          type: string
          minLength: 1
          maxLength: 255
          example: AWS/ApplicationELB
        metricName:
          type: string
          minLength: 1
          maxLength: 255
          example: UnHealthyHostCount
        stat:
          type: string
          minLength: 1
          maxLength: 64
          description: CloudWatch statistic (`Average`, `Maximum`, `Sum`, `p99`, …).
          example: Minimum
        periodS:
          type: integer
          minimum: 1
          maximum: 86400
          description: Period in seconds. CloudWatch only serves 60s within 15 days, 300s within 63 days and 3600s beyond, so a period finer than the data available returns nothing.
          example: 60
        threshold:
          type: number
        comparisonOperator:
          type: string
          enum:
            - GreaterThanOrEqualToThreshold
            - GreaterThanThreshold
            - LessThanThreshold
            - LessThanOrEqualToThreshold
          description: "Static-threshold operators only. The anomaly-detection operators `PutMetricAlarm` also accepts are rejected: we fit no model, so there is no threshold band for them to compare against."
        evaluationPeriods:
          type: integer
          minimum: 1
          maximum: 1440
          description: N — periods the metric is compared over.
        datapointsToAlarm:
          type: integer
          nullable: true
          minimum: 1
          maximum: 1440
          description: M of N. Null/absent means N (consecutive alarm), which is AWS’s own default. Must not exceed `evaluationPeriods` — such a rule could never reach ALARM.
        treatMissingData:
          type: string
          nullable: true
          enum:
            - missing
            - notBreaching
            - breaching
            - ignore
            - null
          description: Defaults to `missing`, matching CloudWatch.
        severity:
          type: string
          nullable: true
          enum:
            - critical
            - warning
            - info
            - null
          description: Routing severity. Defaults to `warning`. Not a CloudWatch concept — ours.
        enabled:
          type: boolean
          description: Defaults to true. Disabling stops evaluation while retaining the alert history a delete would cascade away.
      required:
        - name
        - service
        - namespace
        - metricName
        - stat
        - periodS
        - threshold
        - comparisonOperator
        - evaluationPeriods
    InfraAlertRuleUpdateRequest:
      type: object
      properties:
        name:
          type: string
          minLength: 1
          maxLength: 200
          description: Operator-facing rule name, shown on the alert.
          example: ALB unhealthy hosts
        description:
          type: string
          nullable: true
          maxLength: 2000
        service:
          type: string
          minLength: 1
          maxLength: 64
          description: "Service token the rule applies to (`ec2`, `rds`, `elbv2`, …), matching `infra_resources.service`. Required: it decides which namespace the rule can apply to."
          example: elbv2
        accountId:
          type: string
          nullable: true
          maxLength: 64
          description: Narrow to one account. Null/absent matches every account in scope.
        region:
          type: string
          nullable: true
          maxLength: 32
          description: Narrow to one region. Null/absent matches every region in scope.
        resourceKey:
          type: string
          nullable: true
          maxLength: 512
          description: Pin to exactly one resource (`infra_resources.resource_key`). Null/absent matches every resource satisfying the rest of the selector, including ones discovered after the rule was written.
        tagFilter:
          type: object
          nullable: true
          additionalProperties:
            type: array
            items:
              type: string
              maxLength: 256
            minItems: 1
            maxItems: 50
          description: Tag predicate, e.g. `{"Environment":["prod"]}`. An empty object is stored as no filter.
        namespace:
          type: string
          minLength: 1
          maxLength: 255
          example: AWS/ApplicationELB
        metricName:
          type: string
          minLength: 1
          maxLength: 255
          example: UnHealthyHostCount
        stat:
          type: string
          minLength: 1
          maxLength: 64
          description: CloudWatch statistic (`Average`, `Maximum`, `Sum`, `p99`, …).
          example: Minimum
        periodS:
          type: integer
          minimum: 1
          maximum: 86400
          description: Period in seconds. CloudWatch only serves 60s within 15 days, 300s within 63 days and 3600s beyond, so a period finer than the data available returns nothing.
          example: 60
        threshold:
          type: number
        comparisonOperator:
          type: string
          enum:
            - GreaterThanOrEqualToThreshold
            - GreaterThanThreshold
            - LessThanThreshold
            - LessThanOrEqualToThreshold
          description: "Static-threshold operators only. The anomaly-detection operators `PutMetricAlarm` also accepts are rejected: we fit no model, so there is no threshold band for them to compare against."
        evaluationPeriods:
          type: integer
          minimum: 1
          maximum: 1440
          description: N — periods the metric is compared over.
        datapointsToAlarm:
          type: integer
          nullable: true
          minimum: 1
          maximum: 1440
          description: M of N. Null/absent means N (consecutive alarm), which is AWS’s own default. Must not exceed `evaluationPeriods` — such a rule could never reach ALARM.
        treatMissingData:
          type: string
          nullable: true
          enum:
            - missing
            - notBreaching
            - breaching
            - ignore
            - null
          description: Defaults to `missing`, matching CloudWatch.
        severity:
          type: string
          nullable: true
          enum:
            - critical
            - warning
            - info
            - null
          description: Routing severity. Defaults to `warning`. Not a CloudWatch concept — ours.
        enabled:
          type: boolean
          description: Defaults to true. Disabling stops evaluation while retaining the alert history a delete would cascade away.
    InfraAlertRule:
      type: object
      properties:
        name:
          type: string
          minLength: 1
          maxLength: 200
          description: Operator-facing rule name, shown on the alert.
          example: ALB unhealthy hosts
        description:
          type: string
          nullable: true
          maxLength: 2000
        service:
          type: string
          minLength: 1
          maxLength: 64
          description: "Service token the rule applies to (`ec2`, `rds`, `elbv2`, …), matching `infra_resources.service`. Required: it decides which namespace the rule can apply to."
          example: elbv2
        accountId:
          type: string
          nullable: true
          maxLength: 64
          description: Narrow to one account. Null/absent matches every account in scope.
        region:
          type: string
          nullable: true
          maxLength: 32
          description: Narrow to one region. Null/absent matches every region in scope.
        resourceKey:
          type: string
          nullable: true
          maxLength: 512
          description: Pin to exactly one resource (`infra_resources.resource_key`). Null/absent matches every resource satisfying the rest of the selector, including ones discovered after the rule was written.
        tagFilter:
          type: object
          nullable: true
          additionalProperties:
            type: array
            items:
              type: string
              maxLength: 256
            minItems: 1
            maxItems: 50
          description: Tag predicate, e.g. `{"Environment":["prod"]}`. An empty object is stored as no filter.
        namespace:
          type: string
          minLength: 1
          maxLength: 255
          example: AWS/ApplicationELB
        metricName:
          type: string
          minLength: 1
          maxLength: 255
          example: UnHealthyHostCount
        stat:
          type: string
          minLength: 1
          maxLength: 64
          description: CloudWatch statistic (`Average`, `Maximum`, `Sum`, `p99`, …).
          example: Minimum
        periodS:
          type: integer
          minimum: 1
          maximum: 86400
          description: Period in seconds. CloudWatch only serves 60s within 15 days, 300s within 63 days and 3600s beyond, so a period finer than the data available returns nothing.
          example: 60
        threshold:
          type: number
        comparisonOperator:
          type: string
          enum:
            - GreaterThanOrEqualToThreshold
            - GreaterThanThreshold
            - LessThanThreshold
            - LessThanOrEqualToThreshold
          description: "Static-threshold operators only. The anomaly-detection operators `PutMetricAlarm` also accepts are rejected: we fit no model, so there is no threshold band for them to compare against."
        evaluationPeriods:
          type: integer
          minimum: 1
          maximum: 1440
          description: N — periods the metric is compared over.
        datapointsToAlarm:
          type: integer
          nullable: true
          minimum: 1
          maximum: 1440
          description: M of N. Null/absent means N (consecutive alarm), which is AWS’s own default. Must not exceed `evaluationPeriods` — such a rule could never reach ALARM.
        treatMissingData:
          type: string
          nullable: true
          enum:
            - missing
            - notBreaching
            - breaching
            - ignore
            - null
          description: Defaults to `missing`, matching CloudWatch.
        severity:
          type: string
          nullable: true
          enum:
            - critical
            - warning
            - info
            - null
          description: Routing severity. Defaults to `warning`. Not a CloudWatch concept — ours.
        enabled:
          type: boolean
        id:
          type: string
        projectId:
          type: string
        createdAt:
          type: integer
          description: Epoch ms.
        updatedAt:
          type: integer
          description: Epoch ms.
      required:
        - name
        - service
        - namespace
        - metricName
        - stat
        - periodS
        - threshold
        - comparisonOperator
        - evaluationPeriods
        - enabled
        - id
        - projectId
        - createdAt
        - updatedAt
      description: A threshold rule Agent Hub evaluates in its own poller. We never call `PutMetricAlarm`, so this rule does not exist in the monitored AWS account.
    InfraAlertRuleList:
      type: object
      properties:
        rules:
          type: array
          items:
            $ref: "#/components/schemas/InfraAlertRule"
      required:
        - rules
    InfraAlertTransition:
      type: object
      properties:
        fromState:
          type: string
          enum:
            - OK
            - ALARM
            - INSUFFICIENT_DATA
        toState:
          type: string
          enum:
            - OK
            - ALARM
            - INSUFFICIENT_DATA
        fromStatus:
          type: string
          enum:
            - open
            - resolved
            - ignored
        toStatus:
          type: string
          enum:
            - open
            - resolved
            - ignored
        reason:
          type: string
          nullable: true
          enum:
            - datapoints_breached
            - premature_alarm
            - within_threshold
            - all_datapoints_missing
            - missing_data_ignored
            - null
        actor:
          type: string
          description: Acting user id, or one of `system:evaluator`, `system:recurrence`, `system:recovery`.
        atMs:
          type: integer
      required:
        - fromState
        - toState
        - fromStatus
        - toStatus
        - reason
        - actor
        - atMs
      description: One recorded move. Both state and status are captured on every row, so the timeline reads as complete snapshots rather than half-updates.
    InfraAlert:
      type: object
      properties:
        id:
          type: string
        projectId:
          type: string
        ruleId:
          type: string
        resourceKey:
          type: string
        state:
          type: string
          enum:
            - OK
            - ALARM
            - INSUFFICIENT_DATA
          description: What the metric says. Owned by the evaluator.
        reason:
          type: string
          nullable: true
          enum:
            - datapoints_breached
            - premature_alarm
            - within_threshold
            - all_datapoints_missing
            - missing_data_ignored
            - null
        stateUpdatedAt:
          type: integer
          description: Observation timestamp the current state was decided from — the data’s clock, not the tick’s. An evaluation older than this updates the aggregates but cannot rewrite the state.
        status:
          type: string
          enum:
            - open
            - resolved
            - ignored
          description: What the operator decided. Recurrence reopens `resolved`; `ignored` stays muted through a full breach/recover round trip.
        statusUpdatedAt:
          type: integer
          nullable: true
        statusUpdatedBy:
          type: string
          nullable: true
        firstSeen:
          type: integer
          description: True minimum over every ALARM observation, including out-of-order ones.
        lastSeen:
          type: integer
          description: True maximum, same guarantee.
        occurrenceCount:
          type: integer
        lastValue:
          type: number
          nullable: true
        breachingDatapoints:
          type: integer
          nullable: true
        createdAt:
          type: integer
        updatedAt:
          type: integer
        transitions:
          type: array
          items:
            $ref: "#/components/schemas/InfraAlertTransition"
          description: Present on the detail route only.
      required:
        - id
        - projectId
        - ruleId
        - resourceKey
        - state
        - reason
        - stateUpdatedAt
        - status
        - statusUpdatedAt
        - statusUpdatedBy
        - firstSeen
        - lastSeen
        - occurrenceCount
        - lastValue
        - breachingDatapoints
        - createdAt
        - updatedAt
      description: One fired alert, keyed (rule, resource). Carries resource identifiers only — never credentials or account-identifying secrets.
    InfraAlertList:
      type: object
      properties:
        alerts:
          type: array
          items:
            $ref: "#/components/schemas/InfraAlert"
        nextCursor:
          type: string
          nullable: true
          description: Pass back as `cursor` for the next page. Null on the last page.
        total:
          type: integer
          description: Every alert matching the filters, ignoring the page bound. Use this for counts — `alerts.length` is a page size, so a badge derived from it silently reports the limit once a project exceeds it.
      required:
        - alerts
        - nextCursor
        - total
    InfraAlertStatusRequest:
      type: object
      properties:
        status:
          type: string
          enum:
            - open
            - resolved
            - ignored
          description: "`resolved` closes it out, `ignored` mutes it through recurrence, `open` reopens it."
      required:
        - status
    AwsHealthEventEnvelope:
      type: object
      properties:
        version:
          type: string
        id:
          type: string
          description: EventBridge message id. Unique per delivery, so it is NOT usable for dedupe across the backup Region.
        detail-type:
          type: string
          description: Must be `AWS Health Event` or `AWS Health Abuse Event`.
          example: AWS Health Event
        source:
          type: string
          enum:
            - aws.health
          description: Must be exactly `aws.health`. AWS documents that a rule pattern using a wildcard such as `aws.health*` never matches.
        account:
          type: string
          example: "123456789012"
        time:
          type: string
          example: 2026-01-27T01:43:21Z
        region:
          type: string
          description: The Region the notification was DELIVERED to, not necessarily the impacted one.
        resources:
          type: array
          items:
            type: string
        detail:
          type: object
          properties:
            eventArn:
              type: string
              description: ARN of the Health event. Stable across every update to the same incident, and half of the dedupe key.
              example: arn:aws:health:us-east-1::event/EC2/AWS_EC2_OPERATIONAL_ISSUE/AWS_EC2_OPERATIONAL_ISSUE_7f35c8ae
            communicationId:
              type: string
              description: Identifies one specific communication about the event. The other half of the dedupe key.
              example: 01b0993207d81a09dcd552ebd1e633e36cf1f09a-1
            service:
              type: string
            eventTypeCode:
              type: string
              example: AWS_EC2_OPERATIONAL_ISSUE
            eventTypeCategory:
              type: string
              description: "`issue`, `accountNotification`, `scheduledChange`, or `investigation`."
              example: issue
            eventScopeCode:
              type: string
              example: PUBLIC
            statusCode:
              type: string
              example: open
            eventRegion:
              type: string
              description: The Region actually impacted, which under the backup-Region fan-out often differs from the envelope `region`.
            startTime:
              type: string
              description: RFC-1123 string, not ISO-8601.
              example: Fri, 27 Jan 2023 06:02:51 GMT
            endTime:
              type: string
            lastUpdatedTime:
              type: string
            eventDescription:
              type: array
              items:
                type: object
                properties:
                  language:
                    type: string
                  latestDescription:
                    type: string
            affectedEntities:
              type: array
              items:
                type: object
                properties:
                  entityValue:
                    type: string
                  status:
                    type: string
                  lastUpdatedTime:
                    type: string
            affectedAccount:
              type: string
            backupEvent:
              type: string
              description: Delivered as the string `"true"`/`"false"`, not a boolean.
            page:
              type: string
            totalPages:
              type: string
          required:
            - eventArn
            - communicationId
            - eventTypeCode
            - eventTypeCategory
          additionalProperties:
            nullable: true
      required:
        - detail-type
        - source
        - account
        - region
        - detail
      additionalProperties:
        nullable: true
      description: One AWS Health event as delivered by Amazon EventBridge.
    InfraHealthIngestResult:
      type: object
      properties:
        accepted:
          type: integer
          description: Events written for the first time.
        deduped:
          type: integer
          description: "Deliveries suppressed as duplicates of an already-stored (eventArn, communicationId). A steady non-zero count is normal: EventBridge delivery is at-least-once and AWS additionally fans account-specific events out to a backup Region."
        rejected:
          type: integer
          description: Payload entries that were not usable Health events.
        overflow:
          type: integer
          description: Entries dropped beyond the 100-event cap.
        reasons:
          type: array
          items:
            type: string
          description: Present only when nothing was accepted, to make a misconfigured rule diagnosable.
      required:
        - accepted
        - deduped
        - rejected
        - overflow
    InfraHealthEvent:
      type: object
      properties:
        id:
          type: string
        projectId:
          type: string
        eventArn:
          type: string
        communicationId:
          type: string
        region:
          type: string
          description: Impacted Region, falling back to the delivery Region.
        deliveryRegion:
          type: string
        detailType:
          type: string
        service:
          type: string
          nullable: true
        eventTypeCode:
          type: string
        eventTypeCategory:
          type: string
        eventScopeCode:
          type: string
          nullable: true
        statusCode:
          type: string
          nullable: true
        severity:
          type: string
          enum:
            - critical
            - warning
            - info
          description: Severity this event routes under. `closed` events are downgraded to `info`.
        startTime:
          type: number
          nullable: true
          description: Unix ms; null if unparseable.
        endTime:
          type: number
          nullable: true
        lastUpdated:
          type: number
          nullable: true
        description:
          type: string
          nullable: true
        affectedEntities:
          type: array
          items:
            type: object
            additionalProperties:
              nullable: true
        affectedEntityCount:
          type: integer
        backupEvent:
          type: boolean
          description: True when delivered via the backup Region rather than the impacted one.
        page:
          type: integer
        totalPages:
          type: integer
        eventTime:
          type: number
          nullable: true
        receivedAt:
          type: number
      required:
        - id
        - projectId
        - eventArn
        - communicationId
        - region
        - deliveryRegion
        - detailType
        - service
        - eventTypeCode
        - eventTypeCategory
        - eventScopeCode
        - statusCode
        - severity
        - startTime
        - endTime
        - lastUpdated
        - description
        - affectedEntities
        - affectedEntityCount
        - backupEvent
        - page
        - totalPages
        - eventTime
        - receivedAt
    InfraHealthEventList:
      type: object
      properties:
        events:
          type: array
          items:
            $ref: "#/components/schemas/InfraHealthEvent"
        total:
          type: integer
        ingestConfigured:
          type: boolean
          description: False when no unrevoked ingest token exists, which distinguishes "no events yet" from "the EventBridge rule was never wired up".
      required:
        - events
        - total
        - ingestConfigured
    InfraHealthIngestTokenInfo:
      type: object
      nullable: true
      properties:
        projectId:
          type: string
        tokenPrefix:
          type: string
          description: Non-secret prefix, for identification only.
        createdAt:
          type: number
        rotatedAt:
          type: number
          nullable: true
        revokedAt:
          type: number
          nullable: true
        lastUsedAt:
          type: number
          nullable: true
          description: Last successful ingest, or null if the rule never delivered.
      required:
        - projectId
        - tokenPrefix
        - createdAt
        - rotatedAt
        - revokedAt
        - lastUsedAt
    InfraHealthIngestConfig:
      type: object
      properties:
        token:
          $ref: "#/components/schemas/InfraHealthIngestTokenInfo"
        ingestPath:
          type: string
          example: /api/infra/health/ingest
        eventPattern:
          type: object
          additionalProperties:
            type: array
            items:
              type: string
          description: The literal EventBridge rule pattern to paste into the monitored account. `source` must match `aws.health` exactly — AWS documents that a wildcard such as `aws.health*` never matches.
          example: &a1
            source:
              - aws.health
            detail-type:
              - AWS Health Event
              - AWS Health Abuse Event
      required:
        - token
        - ingestPath
        - eventPattern
    InfraHealthIngestTokenMint:
      type: object
      properties:
        token:
          type: string
          description: Plaintext `ahhealth_` token. Returned exactly once — it is stored only as a SHA-256 digest and cannot be read back.
        info:
          $ref: "#/components/schemas/InfraHealthIngestTokenInfo"
        ingestPath:
          type: string
        eventPattern:
          type: object
          additionalProperties:
            type: array
            items:
              type: string
          description: The literal EventBridge rule pattern to paste into the monitored account. `source` must match `aws.health` exactly — AWS documents that a wildcard such as `aws.health*` never matches.
          example: *a1
      required:
        - token
        - info
        - ingestPath
        - eventPattern
    InfraWizardErrorResponse:
      type: object
      properties:
        error:
          type: string
        message:
          type: string
      required:
        - error
      description: Error envelope for infrastructure wizard routes.
    InfraSetupProfileSummary:
      type: object
      properties:
        name:
          type: string
        type:
          type: string
          enum:
            - sso
            - static
            - role
        region:
          type: string
          description: The profile’s default AWS region.
        monitoringCapable:
          type: boolean
          description: Whether this profile can back unattended background collection, i.e. it is not interactive SSO.
      required:
        - name
        - type
        - region
        - monitoringCapable
      description: A configured project AWS profile, reduced to identity and type. Never carries access keys, secret keys, session tokens or the external ID.
    InfraSetupScopeSummary:
      type: object
      properties:
        profileName:
          type: string
        accountId:
          type: string
          nullable: true
          description: Filled in once `sts:GetCallerIdentity` has run for the profile; null until then.
        region:
          type: string
        service:
          type: string
        enabled:
          type: boolean
        hasTagFilter:
          type: boolean
        resourceCount:
          type: integer
          description: Non-terminated resources inventory holds for the triple. Zero on a fresh scope is expected — inventory sync runs hourly.
      required:
        - profileName
        - accountId
        - region
        - service
        - enabled
        - hasTagFilter
        - resourceCount
      description: One `infra_scopes` allowlist row.
    InfraSetupDraft:
      type: object
      properties:
        projectId:
          type: string
        infraEnabled:
          type: boolean
        profiles:
          type: array
          items:
            $ref: "#/components/schemas/InfraSetupProfileSummary"
        designatedMonitoringProfile:
          type: string
          nullable: true
          description: The stored `awsMonitoringProfile` designation, even when it no longer names a usable profile.
        monitoringProfile:
          type: string
          nullable: true
          description: The profile background collection would actually run as, or null when the collector would refuse.
        monitoringCapableProfiles:
          type: array
          items:
            type: string
        storageReady:
          type: boolean
          description: Whether `infra.db` is open. False means the scope and alert figures are unknown, not zero.
        scopes:
          type: array
          items:
            $ref: "#/components/schemas/InfraSetupScopeSummary"
        enabledScopeCount:
          type: integer
        alertRuleCount:
          type: integer
        enabledAlertRuleCount:
          type: integer
        blockers:
          type: array
          items:
            type: string
            enum:
              - infra-disabled
              - no-profiles
              - only-sso-profiles
              - no-monitoring-profile
              - storage-unavailable
              - no-scope
            description: "An unmet precondition. Causes and state co-occur: `only-sso-profiles` is why there is no designation, `no-monitoring-profile` is that there is none."
        notes:
          type: array
          items:
            type: string
      required:
        - projectId
        - infraEnabled
        - profiles
        - designatedMonitoringProfile
        - monitoringProfile
        - monitoringCapableProfiles
        - storageReady
        - scopes
        - enabledScopeCount
        - alertRuleCount
        - enabledAlertRuleCount
        - blockers
        - notes
      description: "Hub-local infrastructure monitoring readiness: configured AWS profiles and their types, the monitoring-profile designation, the collection allowlist, alert-rule counts, and the blockers that remain. Computed without calling AWS."
    InfraSetupDraftResponse:
      type: object
      properties:
        projectId:
          type: string
        draft:
          $ref: "#/components/schemas/InfraSetupDraft"
      required:
        - projectId
        - draft
      description: Infrastructure monitoring readiness for a project.
    InfraWizardStartResponse:
      type: object
      properties:
        sessionId:
          type: string
        agentId:
          type: string
        draft:
          $ref: "#/components/schemas/InfraSetupDraft"
        session:
          nullable: true
          description: Raw `sessions` row for the spawned wizard session.
      required:
        - sessionId
        - agentId
        - draft
      description: Infrastructure setup wizard session spawned successfully.
    InfraSetupApplyRequest:
      type: object
      properties:
        scopes:
          type: array
          items:
            type: object
            properties:
              profileName:
                type: string
                minLength: 1
                maxLength: 128
                description: A project AWS profile name. Collection needs one that is not interactive SSO.
              region:
                type: string
                minLength: 1
                maxLength: 128
                example: us-east-2
              service:
                type: string
                minLength: 1
                maxLength: 128
                description: Service key, lowercased on write.
                example: ec2
              tagFilter:
                type: object
                nullable: true
                additionalProperties:
                  type: array
                  items:
                    type: string
                  minItems: 1
                description: Tag key -> accepted values (ORed). Omit or null to match every resource.
              enabled:
                type: boolean
                description: Defaults to true. A disabled scope is a pause, not a delete.
            required:
              - profileName
              - region
              - service
          maxItems: 200
          description: The complete `infra_scopes` allowlist to store. Replaces the existing list; omitted triples are deleted.
        monthlyCeilingUsd:
          type: number
          nullable: true
          minimum: 0
          maximum: 1000000
          description: Monthly AWS API spend ceiling. `null` clears it. Omit to leave the current ceiling untouched. Required (here or already stored) when `infraEnabled` is true.
        infraEnabled:
          type: boolean
          description: Toggle the per-project Infrastructure module. Omit to leave the current flag untouched. Setting it to true requires an effective spend ceiling — see `monthlyCeilingUsd`.
      required:
        - scopes
      description: Infrastructure collection scope proposed by the wizard session.
    InfraSetupApplyResponse:
      type: object
      properties:
        ok:
          type: boolean
          enum:
            - true
        infraEnabled:
          type: boolean
        monthlyCeilingUsd:
          type: number
          nullable: true
        scopes:
          type: array
          items:
            nullable: true
          description: The stored allowlist, re-read.
        projection:
          nullable: true
          description: Projected monthly API cost for the enabled scopes.
      required:
        - ok
        - infraEnabled
        - monthlyCeilingUsd
        - scopes
      description: The allowlist as stored, not an echo of the request.
    InfraMonitoringStatus:
      type: object
      properties:
        profile:
          type: string
          nullable: true
          description: Profile the probe used. On a failure this is the profile the operator designated, so the UI can name it; null only when nothing was designated at all.
          example: monitoring
        region:
          type: string
          nullable: true
          description: Region the probe used, taken from the profile's own stanza. Null when resolution failed before a region was determined.
          example: us-east-2
        reachable:
          type: boolean
          description: True when credentials resolved in-process and CloudWatch answered a `DescribeAlarms` call in that region.
        code:
          type: string
          description: Machine-readable failure cause. `monitoring_profile_required` when no usable profile is designated; otherwise the AWS SDK error name (e.g. `AccessDeniedException`).
          example: monitoring_profile_required
        reason:
          type: string
          enum:
            - not_designated
            - interactive_sso
          description: "Present only with `code: monitoring_profile_required`. `not_designated` means no monitoring profile is set, or the designation names a profile that no longer exists. `interactive_sso` means the designated profile is an IAM Identity Center profile, whose token cache is keyed to a user HOME and expires unattended."
        error:
          type: string
          description: Operator-facing failure detail. Absent when `reachable` is true.
        checkedAt:
          type: number
          description: Epoch ms the probe ran. Nothing is cached server-side.
      required:
        - profile
        - region
        - reachable
        - checkedAt
      description: Whether this project can be monitored right now. Never carries credential material — profile name, region and failure text only.
    InfraServicePacks:
      type: object
      properties:
        packs:
          type: array
          items:
            type: object
            properties:
              service:
                type: string
                example: ec2
              label:
                type: string
                example: EC2
              metrics:
                type: array
                items:
                  type: object
                  properties:
                    namespace:
                      type: string
                      example: AWS/EC2
                    metricName:
                      type: string
                      example: StatusCheckFailed
                    dimensions:
                      type: array
                      items:
                        type: string
                      description: "The exact CloudWatch dimension-name set this series is keyed on. Exact, not a subset: `AWS/ECS` `CPUUtilization` at `ClusterName` and at `ClusterName` + `ServiceName` are two different numbers, so a pack may declare the same metric name twice and only the dimension set tells them apart."
                      example:
                        - ClusterName
                        - ServiceName
                    dimensionValues:
                      type: object
                      additionalProperties:
                        type: string
                      description: "Dimension **values** the series is additionally pinned to, present only where the dimension names alone do not identify it. `AWS/S3` is the case it exists for: `BucketSizeBytes` and `NumberOfObjects` are both keyed on `BucketName` + `StorageType`, but AWS documents `AllStorageTypes` as the object count’s only valid storage-type filter and does not list it among the byte total’s. A resource whose recorded dimension value contradicts a pin is skipped exactly as a dimension-name mismatch is."
                      example:
                        StorageType: AllStorageTypes
                    metricType:
                      type: string
                      enum:
                        - gauge
                        - counter
                        - flag
                        - balance
                        - latency
                      description: What the value is, which is what decides the statistic. A `flag` is a per-minute 0/1 check result and must be stored on `Maximum`; a `counter` accrues over the period and is stored on `Sum`. A `latency` is a distribution rather than a level, and is the only type a percentile statistic is legal on.
                    stat:
                      type: string
                      description: The statistic this series is collected and stored on. A CloudWatch percentile such as `p99` for a `latency` metric, otherwise a named statistic.
                      example: Maximum
                    validStatistics:
                      type: array
                      items:
                        type: string
                      description: Every statistic AWS documents as meaningful for this metric. `Sum` is absent from the EBS burst-balance metrics because AWS states it is not applicable to them. The literal token `pNN.NN` stands for any percentile, and is AWS’s own notation on the ALB `TargetResponseTime` entry.
                    minPeriodSeconds:
                      type: integer
                      description: The metric’s publication floor. Requesting a shorter period does not produce finer data, it produces a mostly-empty series that is billed in full.
                      example: 60
                    availability:
                      type: string
                      enum:
                        - either
                        - basic-only
                        - detailed-only
                      description: Which EC2 monitoring mode publishes the metric at all. `basic-only` metrics disappear when detailed monitoring is enabled, which is the opposite of what an operator paying for it expects.
                    appliesTo:
                      type: object
                      properties:
                        universal:
                          type: boolean
                        condition:
                          type: string
                          description: Which resources publish it, when not all of them do. Empty when `universal` is true.
                          example: Burstable performance (T-family) instances only.
                      required:
                        - universal
                        - condition
                      description: The inventory records a resource id, not an instance type, so this is rendered to the operator rather than applied as a collection filter.
                    requiresFeature:
                      type: string
                      nullable: true
                      description: Opt-in provider feature this metric needs, matched against the flags recorded per resource, or null when it is published unconditionally. A gated metric is never requested for a resource without the feature — `GetMetricData` bills per metric requested, so asking for a namespace the account does not publish is spend with no possible return.
                      example: containerInsights
                    description:
                      type: string
                      description: One operator-facing line about the metric.
                  required:
                    - namespace
                    - metricName
                    - dimensions
                    - metricType
                    - stat
                    - validStatistics
                    - minPeriodSeconds
                    - availability
                    - appliesTo
                    - requiresFeature
                    - description
              dimensions:
                type: array
                items:
                  type: object
                  properties:
                    name:
                      type: string
                      example: ImageId
                    detailedMonitoringOnly:
                      type: boolean
                      description: True when the dimension is populated only for instances with detailed monitoring enabled. Slicing by it on a default fleet returns nothing.
                    description:
                      type: string
                  required:
                    - name
                    - detailedMonitoringOnly
                    - description
              absentMetrics:
                type: array
                items:
                  type: object
                  properties:
                    label:
                      type: string
                      example: Memory utilization
                    reason:
                      type: string
                      description: Why the metric does not exist. A structural absence, not a failure.
                    remedy:
                      type: string
                      nullable: true
                      description: How to obtain it, when there is a way. Null when there is none.
                  required:
                    - label
                    - reason
                    - remedy
              features:
                type: array
                items:
                  type: object
                  properties:
                    key:
                      type: string
                      example: containerInsights
                    label:
                      type: string
                      example: Container Insights
                    whenOff:
                      type: string
                      description: What is unavailable while the feature is off, and how to turn it on.
                    costNote:
                      type: string
                      description: What enabling it costs. AWS bills this in the operator’s own account, not through Agent Hub — ECS Container Insights metrics are charged as CloudWatch custom metrics.
                    docsUrl:
                      type: string
                      description: AWS’s own page for the feature, so the cost claim is checkable.
                  required:
                    - key
                    - label
                    - whenOff
                    - costNote
                    - docsUrl
              defaultAlertRules:
                type: array
                items:
                  type: object
                  properties:
                    name:
                      type: string
                    description:
                      type: string
                    namespace:
                      type: string
                    metricName:
                      type: string
                    stat:
                      type: string
                    dimensions:
                      type: array
                      items:
                        type: string
                      description: "The dimension set of the series this rule evaluates. Not redundant with `metricName`: a pack may declare one metric at two levels, and a threshold that means something for a service means nothing for its cluster."
                    periodS:
                      type: integer
                    threshold:
                      type: number
                    comparisonOperator:
                      type: string
                      enum:
                        - GreaterThanOrEqualToThreshold
                        - GreaterThanThreshold
                        - LessThanThreshold
                        - LessThanOrEqualToThreshold
                    evaluationPeriods:
                      type: integer
                    datapointsToAlarm:
                      type: integer
                    treatMissingData:
                      type: string
                      enum:
                        - missing
                        - notBreaching
                        - breaching
                        - ignore
                    severity:
                      type: string
                      enum:
                        - critical
                        - warning
                        - info
                    rationale:
                      type: string
                      description: Where the numbers come from, cited so a reviewer can check them against AWS’s published guidance.
                  required:
                    - name
                    - description
                    - namespace
                    - metricName
                    - stat
                    - dimensions
                    - periodS
                    - threshold
                    - comparisonOperator
                    - evaluationPeriods
                    - datapointsToAlarm
                    - treatMissingData
                    - severity
                    - rationale
            required:
              - service
              - label
              - metrics
              - dimensions
              - absentMetrics
              - features
              - defaultAlertRules
      required:
        - packs
      description: The declared service packs. Static data — identical for every project, and carrying no account, credential or resource identifiers.
    InfraCostProjection:
      type: object
      properties:
        metricsRequestedPerMonth:
          type: number
        estimatedMonthlyCostUsd:
          type: number
        perScope:
          type: array
          items:
            type: object
            properties:
              id:
                type: string
              service:
                type: string
                example: ec2
              profileName:
                type: string
              region:
                type: string
                example: us-east-2
              resourceCount:
                type: number
              metricsPerResource:
                type: number
                description: Metrics in this service’s pack. Zero means the service is not collected at all.
              metricsRequestedPerMonth:
                type: number
              estimatedMonthlyCostUsd:
                type: number
                description: Priced at the scope’s own region. Most regions are $0.01 per 1,000 metrics requested; GovCloud is $0.013 and São Paulo $0.014.
              usdPer1000Metrics:
                type: number
                description: The `GetMetricData` rate this scope was priced at.
                example: 0.01
              regionPriceKnown:
                type: boolean
                description: False when the region is absent from the published price table and the **dearest** known rate was assumed instead — a guardrail must never knowingly under-report, so an unrecognised region is priced high, not at the list rate. Surfaced so an operator can see why an estimate is inflated (by up to 40%) rather than merely distrusting it.
              intervals:
                type: array
                items:
                  type: object
                  properties:
                    metricName:
                      type: string
                      example: CPUUtilization
                    namespace:
                      type: string
                      example: AWS/EC2
                    stat:
                      type: string
                      example: Average
                    minPeriodSeconds:
                      type: number
                      description: The rate AWS actually publishes this metric at. The poll interval can never go below it — requests for data that has not been published yet are billed and return nothing.
                      example: 300
                    pollIntervalSeconds:
                      type: number
                      description: "What the collector will request it at: the service tier, raised to `minPeriodSeconds`, raised to the collector tick, then multiplied if the project is `widened`."
                      example: 300
                    requestsPerMonth:
                      type: number
                  required:
                    - metricName
                    - namespace
                    - stat
                    - minPeriodSeconds
                    - pollIntervalSeconds
                    - requestsPerMonth
                  description: Resolved cadence for one metric in a service pack.
            required:
              - service
              - resourceCount
              - metricsPerResource
              - metricsRequestedPerMonth
              - estimatedMonthlyCostUsd
              - usdPer1000Metrics
              - regionPriceKnown
              - intervals
            description: What one scope contributes to the projected monthly bill.
      required:
        - metricsRequestedPerMonth
        - estimatedMonthlyCostUsd
        - perScope
      description: "Projected monthly `GetMetricData` spend for a set of scopes: resources × metrics × ticks per month. Estimates round *against* the wallet — a 31-day month, and no modelling of AWS’s ≤5-statistics-per-metric bundling — because a projection that under-reports produces the surprise bill this guardrail exists to prevent."
    InfraCostStatus:
      type: object
      properties:
        monthStartMs:
          type: number
          description: First epoch ms of the **UTC** calendar month the spend figures cover. UTC because AWS bills on UTC months; using the Hub’s local timezone would put the ceiling out of phase with the bill it guards.
        monthToDateUsd:
          type: number
          description: Summed `estimated_cost_usd` over this month’s collector ticks, including ticks that crashed — a spend audit that only counted completed ticks would under-report the expensive ones.
        extrapolatedMonthUsd:
          type: number
          description: Straight-line extrapolation of `monthToDateUsd` to month end. Answers “what is the current configuration on track for”, where `projection` answers “what will this configuration cost”; a scope added mid-month makes the two disagree, which is the signal.
        metricsRequested:
          type: number
        queriesIssued:
          type: number
        datapointsReturned:
          type: number
        throttles:
          type: number
        errors:
          type: number
        runs:
          type: number
          description: Collector ticks recorded this month.
        byKind:
          type: object
          properties:
            metrics:
              type: number
            cost_explorer:
              type: number
          required:
            - metrics
            - cost_explorer
          description: "`monthToDateUsd` split by which billed AWS API spent it. The total stays the ceiling’s input — both APIs draw on one budget — but the split is what an operator can act on: “$4 of your $5 went to Cost Explorer” leads somewhere that “$5” does not. A `kind` this build does not recognise still counts in the total and is simply absent here."
        futureDatedRuns:
          type: number
          description: Runs stamped beyond the end of this month, excluded from the totals above. Only reachable through host clock skew, since a run row is written with the wall clock at tick start. Reported rather than silently dropped — their spend is real but cannot be attributed to this month without double-counting it when that month arrives. Non-zero means the host clock needs looking at.
        monthlyCeilingUsd:
          type: number
          nullable: true
          description: Spend ceiling, or null for uncapped (the default). A ceiling of `0` is a real setting distinct from null and means “collect nothing”.
        degradation:
          type: string
          enum:
            - normal
            - widened
            - paused
          description: What the collector is doing about this project’s spend. `normal` polls at the configured tiers; `widened` multiplies every poll interval by 4 once month-to-date spend reaches the ceiling; `paused` stops issuing billed requests entirely once it reaches twice the ceiling.
        degradedAt:
          type: number
          nullable: true
          description: Epoch ms the level last changed; null while it has never left `normal`.
        costExplorerEnabled:
          type: boolean
          description: "Whether the operator opted this project into billed Cost Explorer polling. Off by default and never inferred: `GetCostAndUsage` charges $0.01 per paginated request with no free tier, so it takes an explicit yes."
        costExplorerSyncedAt:
          type: number
          nullable: true
          description: Epoch ms the last Cost Explorer sync **started**; null if one never has. Stamped before the first billed request rather than after the last, so a sweep that crashed partway still counts against the cadence guard.
        configured:
          type: boolean
          description: False when the project has no cost-config row and every setting above is a default — the UI needs this to distinguish “never opened” from “deliberately uncapped”.
        projection:
          $ref: "#/components/schemas/InfraCostProjection"
        recentRuns:
          type: array
          items:
            type: object
            properties:
              id:
                type: string
              accountId:
                type: string
                nullable: true
              region:
                type: string
                nullable: true
              startedAt:
                type: number
              kind:
                type: string
                enum:
                  - metrics
                  - cost_explorer
                description: Which billed AWS API this run spent on.
              finishedAt:
                type: number
                nullable: true
              durationMs:
                type: number
                nullable: true
              queriesIssued:
                type: number
              metricsRequested:
                type: number
              datapointsReturned:
                type: number
              pointsWritten:
                type: number
              throttles:
                type: number
              errors:
                type: number
              estimatedCostUsd:
                type: number
              status:
                type: string
                example: ok
              errorMessage:
                type: string
                nullable: true
            required:
              - id
              - accountId
              - region
              - startedAt
              - kind
              - finishedAt
              - durationMs
              - queriesIssued
              - metricsRequested
              - datapointsReturned
              - pointsWritten
              - throttles
              - errors
              - estimatedCostUsd
              - status
              - errorMessage
      required:
        - monthStartMs
        - monthToDateUsd
        - extrapolatedMonthUsd
        - metricsRequested
        - queriesIssued
        - datapointsReturned
        - throttles
        - errors
        - runs
        - byKind
        - futureDatedRuns
        - monthlyCeilingUsd
        - degradation
        - degradedAt
        - costExplorerEnabled
        - costExplorerSyncedAt
        - configured
        - projection
        - recentRuns
      description: Spend to date and projection for a project’s AWS API usage. Resource identifiers and dollar figures only — never credentials or anything else Admin-gated beyond the route’s own gate.
    InfraCostProjectionRequest:
      type: object
      properties:
        scopes:
          type: array
          items:
            type: object
            properties:
              service:
                type: string
                minLength: 1
                example: ec2
              resourceCount:
                type: integer
                minimum: 0
                maximum: 1000000
              id:
                type: string
              profileName:
                type: string
              region:
                type: string
                example: us-east-2
            required:
              - service
              - resourceCount
          maxItems: 500
        degradation:
          type: string
          enum:
            - normal
            - widened
          description: Price the projection as if the project were already degraded. Omit for the normal cadence.
      required:
        - scopes
      description: A proposed scope allowlist to price. `resourceCount` is what the operator expects the scope to match; the editor fills it from the inventory browser.
    InfraCostCeilingRequest:
      type: object
      properties:
        monthlyCeilingUsd:
          type: number
          nullable: true
          minimum: 0
          maximum: 1000000
          description: Ceiling in USD, or null to remove it. `0` means “collect nothing” and pauses the collector immediately.
          example: 25
      required:
        - monthlyCeilingUsd
      description: Set or clear a project’s monthly AWS API spend ceiling.
    InfraSpendTrend:
      type: object
      properties:
        enabled:
          type: boolean
          description: Whether billed Cost Explorer polling is on for this project. When false the arrays below are whatever was last cached before it was turned off — the cache is spend already paid for and is not discarded on disable.
        syncedAt:
          type: number
          nullable: true
          description: "Epoch ms the last sync started. Differs from `fetchedAt` when that sync failed: one says when we tried, the other when we last succeeded."
        windowStartDay:
          type: string
          description: Inclusive `YYYY-MM-DD` (UTC) lower bound of the window queried.
          example: 2026-07-08
        windowEndDay:
          type: string
          description: Exclusive `YYYY-MM-DD` (UTC) upper bound, matching Cost Explorer’s own half-open `TimePeriod`. This is **tomorrow**, so today’s partial spend is included.
          example: 2026-08-08
        days:
          type: array
          items:
            type: object
            properties:
              day:
                type: string
                example: 2026-08-06
              amountUsd:
                type: number
              estimated:
                type: boolean
                description: Cost Explorer has not finalized this day and the figure will move. The most recent day is always estimated.
            required:
              - day
              - amountUsd
              - estimated
          description: Daily totals, oldest first — already in plot order.
        topServices:
          type: array
          items:
            type: object
            properties:
              service:
                type: string
              amountUsd:
                type: number
            required:
              - service
              - amountUsd
          description: Most expensive services over the whole window, descending. Truncated to the requested `topServices`; `totalUsd` still includes the tail this omits.
        accounts:
          type: array
          items:
            type: object
            properties:
              linkedAccount:
                type: string
              amountUsd:
                type: number
            required:
              - linkedAccount
              - amountUsd
          description: Member accounts under a payer, descending. Empty for a standalone account, which Cost Explorer does not attribute.
        totalUsd:
          type: number
          description: Window total, summed independently of `topServices` so a truncated panel cannot understate the bill.
        unit:
          type: string
          nullable: true
          description: Currency Cost Explorer reported, carried verbatim. Almost always `USD`, but an account with a non-USD billing currency reports its own. Null when nothing is cached.
          example: USD
        fetchedAt:
          type: number
          nullable: true
          description: Epoch ms the cached rows were written; null when the cache is empty.
        lastRun:
          type: object
          nullable: true
          properties:
            startedAt:
              type: number
            finishedAt:
              type: number
              nullable: true
            status:
              type: string
              example: ok
            pages:
              type: number
              description: "Paginated requests issued, which is exactly what AWS billed: every page is its own $0.01 request."
            estimatedCostUsd:
              type: number
            errorMessage:
              type: string
              nullable: true
          required:
            - startedAt
            - finishedAt
            - status
            - pages
            - estimatedCostUsd
            - errorMessage
          description: The most recent Cost Explorer sync. Carries the failure reason so an operator who opted in and sees an empty chart can find out why without reading server logs — `DataUnavailableException` here usually means Cost Explorer was never enabled in the account, which no IAM grant can fix.
      required:
        - enabled
        - syncedAt
        - windowStartDay
        - windowEndDay
        - days
        - topServices
        - accounts
        - totalUsd
        - unit
        - fetchedAt
        - lastRun
      description: Cached daily AWS spend. Read entirely from local SQLite — this never calls AWS, which is the caching layer AWS’s own best-practices page asks for.
    InfraQuotaHeadroom:
      type: object
      properties:
        resourceKey:
          type: string
          description: Inventory key for the quota, joining it to its metric series.
        accountId:
          type: string
          description: AWS account the quota applies to.
        region:
          type: string
          description: Region the applied value was read from. A quota is per-region, so the same quota code can have different limits in different regions.
          example: us-east-1
        serviceCode:
          type: string
          description: Service Quotas ServiceCode, which is frequently not the marketing name — CloudWatch is `monitoring`, Location Service is `geo`.
          example: ec2
        quotaCode:
          type: string
          example: L-1216C47A
        quotaName:
          type: string
          example: Running On-Demand Standard instances
        limit:
          type: number
          nullable: true
          description: The *applied* quota, reflecting increases AWS granted this account rather than the published default. Null when AWS returned no applied value — documented as possible, and deliberately distinct from zero.
          example: 640
        unit:
          type: string
          nullable: true
          description: Unit as Service Quotas reports it, so headroom can be labelled without inferring one from the quota name.
          example: None
        adjustable:
          type: boolean
          description: Whether an increase can be requested at all. It changes what an operator can do about a full quota, so it travels beside the number saying it is full.
        usage:
          type: number
          nullable: true
          description: "Most recent AWS/Usage reading, or null when nothing has been collected or the newest point is older than `staleAfterMs`. Null is not zero: a stopped collector must not leave a reassuring number pinned to the panel."
          example: 512
        usageAtMs:
          type: number
          nullable: true
          description: Epoch milliseconds of that reading.
        metricName:
          type: string
          description: Which AWS/Usage metric measures this quota. Each quota names exactly one.
          example: ResourceCount
        utilizationPercent:
          type: number
          nullable: true
          description: "Usage as a percentage of the applied quota. Null when undefined — never zero, which would read as full headroom. Not clamped at 100: usage above an applied quota is real and observable after a quota decrease."
          example: 80
        headroom:
          type: number
          nullable: true
          description: "Absolute remaining capacity (`limit - usage`), floored at zero. The number the percentage cannot give: 82% of 40 leaves 7, 82% of 5,000 leaves 900."
          example: 128
        band:
          type: string
          enum:
            - critical
            - warning
            - ok
            - unknown
          description: Severity band. `unknown` is distinct from `ok` so an unmeasurable quota never counts toward "everything is fine".
      required:
        - resourceKey
        - accountId
        - region
        - serviceCode
        - quotaCode
        - quotaName
        - limit
        - unit
        - adjustable
        - usage
        - usageAtMs
        - metricName
        - utilizationPercent
        - headroom
        - band
      description: One service quota with its latest usage resolved into headroom.
    InfraQuotaHeadroomResponse:
      type: object
      properties:
        quotas:
          type: array
          items:
            $ref: "#/components/schemas/InfraQuotaHeadroom"
          description: Tightest quota first; unmeasurable ones last.
        summary:
          type: object
          properties:
            critical:
              type: integer
            warning:
              type: integer
            ok:
              type: integer
            unknown:
              type: integer
            total:
              type: integer
          required:
            - critical
            - warning
            - ok
            - unknown
            - total
          description: Counts per band, for the panel headline.
        thresholds:
          type: object
          properties:
            warning:
              type: number
              example: 80
            critical:
              type: number
              example: 100
          required:
            - warning
            - critical
          description: Band edges, sent so web and mobile cannot disagree with the server about where "warning" starts. The warning edge is the same 80 AWS's console walkthrough alarms on.
        expression:
          type: string
          description: The AWS metric-math expression this reproduces, so an operator diffing our percentage against the console can see what we claim to compute.
          example: m1/SERVICE_QUOTA(m1)*100
        staleAfterMs:
          type: number
          description: How old a usage reading may be and still be reported.
      required:
        - quotas
        - summary
        - thresholds
        - expression
        - staleAfterMs
      description: Service quota headroom. Read entirely from local SQLite; limits come from the hourly ListServiceQuotas sync and usage from the metric collector, so this never calls AWS.
    InfraSpendConfigRequest:
      type: object
      properties:
        enabled:
          type: boolean
          description: Turn billed Cost Explorer polling on or off for this project.
          example: true
      required:
        - enabled
      description: The explicit per-project opt-in that decision INFRA-COST requires, because Cost Explorer is unavoidably billable.
    InfraRetentionConfig:
      type: object
      properties:
        retentionDays:
          type: integer
          description: Age window before a metric point is reaped, in days.
          example: 30
        quotaBytes:
          type: integer
          description: Accounted footprint this project may hold before its oldest points are evicted.
          example: 8589934592
        configured:
          type: boolean
          description: False when this project has no override row and both values above are the code defaults.
        updatedAt:
          type: number
          nullable: true
          description: Epoch ms the override was last saved. Null when unconfigured.
        defaults:
          type: object
          properties:
            retentionDays:
              type: integer
            quotaBytes:
              type: integer
          required:
            - retentionDays
            - quotaBytes
          description: What an unconfigured project falls back to.
        bounds:
          type: object
          properties:
            minRetentionDays:
              type: integer
            maxRetentionDays:
              type: integer
            minQuotaBytes:
              type: integer
            maxQuotaBytes:
              type: integer
          required:
            - minRetentionDays
            - maxRetentionDays
            - minQuotaBytes
            - maxQuotaBytes
          description: Inclusive range each value is clamped to. Out-of-range input is clamped, not rejected.
        dbBytes:
          type: integer
          description: "On-disk size of the whole `infra.db` file, across every project. Deliberately not a per-project figure: that would need a full-table aggregate, which on a store of tens of millions of points would stall the server for a page load."
      required:
        - retentionDays
        - quotaBytes
        - configured
        - updatedAt
        - defaults
        - bounds
        - dbBytes
      description: A project’s resolved retention window and byte quota, with the bounds and defaults behind them.
    InfraRetentionConfigRequest:
      type: object
      properties:
        retentionDays:
          type: integer
          minimum: 0
          maximum: 100000
          description: New age window in days. Clamped to the documented bounds. Omit to leave as-is.
          example: 60
        quotaBytes:
          type: integer
          minimum: 0
          maximum: 1099511627776
          description: New byte quota. Clamped to the documented bounds. Omit to leave as-is.
          example: 8589934592
      description: Set a project’s metric retention overrides.
    InfraScope:
      type: object
      properties:
        profileName:
          type: string
          minLength: 1
          maxLength: 128
          description: Project AWS profile the scope collects through. Keyed on the profile rather than the account id because the profile is what the operator picks and what credential resolution needs.
          example: monitoring
        region:
          type: string
          minLength: 1
          maxLength: 128
          example: us-east-2
        service:
          type: string
          minLength: 1
          maxLength: 128
          description: "Service token, lowercased on save. Free text with no enum: the collectable list grows every release, and a token with no metric pack is accepted but reported back in `uncollectableServices` rather than rejected."
          example: ec2
        tagFilter:
          type: object
          nullable: true
          additionalProperties:
            type: array
            items:
              type: string
            minItems: 1
          description: Optional tag filter as tag key -> accepted values. Values within one key are ORed, keys are ANDed — EC2 filter semantics, with `*` and `?` wildcards. Omit or send null to match every resource the describe call returns.
          example: &a2
            Environment:
              - prod
              - staging
        enabled:
          type: boolean
        id:
          type: string
        projectId:
          type: string
        accountId:
          type: string
          nullable: true
          description: Identity behind the profile, filled in once `sts:GetCallerIdentity` has run. Null until then — a scope is never blocked on a live AWS call.
        createdAt:
          type: number
        updatedAt:
          type: number
        resourceCount:
          type: number
          description: Non-terminated resources inventory currently holds for this triple, within the collector’s own staleness bound. Zero on a new scope simply means the hourly inventory sync has not described the account yet — it does not mean the scope is free.
      required:
        - profileName
        - region
        - service
        - enabled
        - id
        - projectId
        - accountId
        - createdAt
        - updatedAt
        - resourceCount
      description: A stored scope with the population its projection is priced on.
    InfraScopesResponse:
      type: object
      properties:
        scopes:
          type: array
          items:
            $ref: "#/components/schemas/InfraScope"
        projection:
          $ref: "#/components/schemas/InfraCostProjection"
        collectableServices:
          type: array
          items:
            type: string
          description: Service tokens that have a metric pack, for the editor’s service picker.
        uncollectableServices:
          type: array
          items:
            type: string
          description: Services present in the allowlist that no metric pack covers. These are stored and priced at zero — the editor flags them so an inert scope does not read as a working one.
        monthlyCeilingUsd:
          type: number
          nullable: true
        degradation:
          type: string
          enum:
            - normal
            - widened
            - paused
          description: What the collector is doing about this project’s spend. `normal` polls at the configured tiers; `widened` multiplies every poll interval by 4 once month-to-date spend reaches the ceiling; `paused` stops issuing billed requests entirely once it reaches twice the ceiling.
        maxScopes:
          type: number
        configured:
          type: boolean
          description: False when the project has no scope rows at all — nothing is being polled.
      required:
        - scopes
        - projection
        - collectableServices
        - uncollectableServices
        - monthlyCeilingUsd
        - degradation
        - maxScopes
        - configured
      description: The allowlist, priced. The projection covers enabled scopes only, since a disabled scope issues no billed requests.
    InfraScopesReplaceRequest:
      type: object
      properties:
        scopes:
          type: array
          items:
            type: object
            properties:
              profileName:
                type: string
                minLength: 1
                maxLength: 128
                description: Project AWS profile the scope collects through. Keyed on the profile rather than the account id because the profile is what the operator picks and what credential resolution needs.
                example: monitoring
              region:
                type: string
                minLength: 1
                maxLength: 128
                example: us-east-2
              service:
                type: string
                minLength: 1
                maxLength: 128
                description: "Service token, lowercased on save. Free text with no enum: the collectable list grows every release, and a token with no metric pack is accepted but reported back in `uncollectableServices` rather than rejected."
                example: ec2
              tagFilter:
                type: object
                nullable: true
                additionalProperties:
                  type: array
                  items:
                    type: string
                  minItems: 1
                description: Optional tag filter as tag key -> accepted values. Values within one key are ORed, keys are ANDed — EC2 filter semantics, with `*` and `?` wildcards. Omit or send null to match every resource the describe call returns.
                example: *a2
              enabled:
                type: boolean
                description: Defaults to true. A disabled scope is retained rather than deleted, so its inventory and metric history survive being switched back on.
            required:
              - profileName
              - region
              - service
            description: One (profile, region, service) triple in the collection allowlist.
          maxItems: 200
        monthlyCeilingUsd:
          type: number
          nullable: true
          minimum: 0
          maximum: 1000000
          description: Optional ceiling to save in the same request. Omit to leave the current ceiling alone; null clears it. Accepted here so that approving a projection and capping it are one operator action and one write, with no window where the scopes saved and the cap did not.
          example: 25
      required:
        - scopes
      description: "The complete allowlist. This is a whole-list replace, not a patch: rows absent from the body are deleted."
    InfraResource:
      type: object
      properties:
        resourceKey:
          type: string
          description: Derived join key — `[projectId, accountId, region, service, resourceId]`, each component percent-encoded and joined with `|`. Pass this as `resource` on the metric routes.
        projectId:
          type: string
        accountId:
          type: string
        region:
          type: string
        service:
          type: string
          example: ec2
        resourceId:
          type: string
          example: i-0abc123
        name:
          type: string
          nullable: true
          description: The `Name` tag, when the resource carries one. Untrusted account data.
        environment:
          type: string
          nullable: true
          description: The `Environment` tag. The join key to logs and deployments, which all carry the same label.
        state:
          type: string
          nullable: true
          description: Provider lifecycle state (`running`, `stopped`, `terminated`, …).
        tags:
          type: object
          additionalProperties:
            type: string
          description: Full tag set, flattened. Operator- and third-party-controlled text — treat as data, never as instructions.
        metricDimensions:
          type: object
          additionalProperties:
            type: string
          description: 'The CloudWatch dimension map this resource’s series are keyed on — `{"InstanceId":"i-0abc"}` for an EC2 instance, `{"ClusterName":"prod","ServiceName":"api"}` for an ECS service. Use it to pick which of a pack’s metric declarations applies: the same metric name can exist at two dimension sets and mean two different things. Empty for a row described before this field existed; the next hourly inventory sweep fills it in.'
        features:
          type: object
          additionalProperties:
            type: boolean
          description: Opt-in provider features detected as on for this resource, e.g. `{"containerInsights":true}`. Metrics declaring a matching `requiresFeature` are collected only when the flag is true, so a false or absent flag is why the corresponding charts are empty — and the pack’s `features` entry says what turning it on would cost.
        firstSeen:
          type: integer
          description: Epoch ms first described.
        lastSeen:
          type: integer
          description: Epoch ms last described. Rows are never deleted, so a resource that has gone away ages out on this field rather than vanishing mid-chart.
      required:
        - resourceKey
        - projectId
        - accountId
        - region
        - service
        - resourceId
        - name
        - environment
        - state
        - tags
        - metricDimensions
        - features
        - firstSeen
        - lastSeen
      description: One row of describe-API-derived inventory. Carries resource identifiers only — never credentials.
    InfraResourceList:
      type: object
      properties:
        resources:
          type: array
          items:
            $ref: "#/components/schemas/InfraResource"
        nextCursor:
          type: string
          nullable: true
          description: Pass back as `cursor` for the next page. Null on the last page.
        facets:
          type: object
          properties:
            services:
              type: array
              items:
                type: string
            regions:
              type: array
              items:
                type: string
            accounts:
              type: array
              items:
                type: string
            environments:
              type: array
              items:
                type: string
            states:
              type: array
              items:
                type: string
            tagKeys:
              type: array
              items:
                type: string
            total:
              type: integer
              description: Rows matching the current filters, ignoring paging.
          required:
            - services
            - regions
            - accounts
            - environments
            - states
            - tagKeys
            - total
          description: Distinct values across the whole project, so a filter control can always be changed back. Only `total` reflects the current filters.
        staleAfterMs:
          type: integer
          description: How long a row may go undescribed and still be polled. The default `seenSince` window is derived from it.
      required:
        - resources
        - nextCursor
        - facets
        - staleAfterMs
    InfraFleetMetric:
      type: object
      properties:
        metricName:
          type: string
        namespace:
          type: string
        stat:
          type: string
        label:
          type: string
          description: Short dashboard label, e.g. `CPU`.
        unit:
          type: string
          enum:
            - percent
            - bytes
            - count
            - seconds
          description: How a human reads the value, which decides client formatting.
        description:
          type: string
          description: The pack's operator-facing line.
        latest:
          type: number
          nullable: true
          description: Newest bucket value, or null when the series reported nothing in the window. Null is a real answer — collected, nothing came back — and is not the same as zero.
        latestTsMs:
          type: integer
          nullable: true
        min:
          type: number
          nullable: true
        max:
          type: number
          nullable: true
        points:
          type: array
          items:
            type: object
            properties:
              tsMs:
                type: integer
              value:
                type: number
            required:
              - tsMs
              - value
          description: "Sparkline, oldest first. Empty buckets are absent rather than zero-filled: a gap is a resource that stopped reporting, and drawing it as zero would read as a healthy floor."
      required:
        - metricName
        - namespace
        - stat
        - label
        - unit
        - description
        - latest
        - latestTsMs
        - min
        - max
        - points
    InfraFleet:
      type: object
      properties:
        fromMs:
          type: integer
        toMs:
          type: integer
        bucketSeconds:
          type: integer
          description: Width of one sparkline bucket.
        services:
          type: array
          items:
            type: string
        resources:
          type: array
          items:
            type: object
            properties:
              resourceKey:
                type: string
              service:
                type: string
              resourceId:
                type: string
              name:
                type: string
                nullable: true
              region:
                type: string
              accountId:
                type: string
              environment:
                type: string
                nullable: true
              state:
                type: string
                nullable: true
              lastSeen:
                type: integer
              metricDimensions:
                type: object
                nullable: true
                additionalProperties:
                  nullable: true
                description: CloudWatch dimension map the resource’s series are keyed on. Carried so a card can open the full Metrics view without refetching the row.
              features:
                type: object
                nullable: true
                additionalProperties:
                  nullable: true
              metrics:
                type: array
                items:
                  $ref: "#/components/schemas/InfraFleetMetric"
            required:
              - resourceKey
              - service
              - resourceId
              - name
              - region
              - accountId
              - environment
              - state
              - lastSeen
              - metricDimensions
              - features
              - metrics
        truncated:
          type: boolean
          description: More resources matched than `limit`. Narrow, or use the Resources browser.
      required:
        - fromMs
        - toMs
        - bucketSeconds
        - services
        - resources
        - truncated
    InfraMetricSeries:
      type: object
      properties:
        namespace:
          type: string
          example: AWS/EC2
        metricName:
          type: string
          example: CPUUtilization
        stat:
          type: string
          example: Average
        periodSeconds:
          type: integer
          description: The period the series is stored at. Part of its identity, not a hint.
        dimensionsHash:
          type: string
        dimensionsJson:
          type: string
          nullable: true
        pointCount:
          type: integer
        firstTsMs:
          type: integer
        lastTsMs:
          type: integer
      required:
        - namespace
        - metricName
        - stat
        - periodSeconds
        - dimensionsHash
        - dimensionsJson
        - pointCount
        - firstTsMs
        - lastTsMs
      description: One chartable series. The catalog is built from what was actually collected, not from the service metric pack, so a metric the account never published is absent instead of offering an always-empty chart.
    InfraMetricSeriesList:
      type: object
      properties:
        resource:
          allOf:
            - $ref: "#/components/schemas/InfraResource"
            - nullable: true
        series:
          type: array
          items:
            $ref: "#/components/schemas/InfraMetricSeries"
      required:
        - resource
        - series
    InfraMetricRange:
      type: object
      properties:
        resource:
          allOf:
            - $ref: "#/components/schemas/InfraResource"
            - nullable: true
        series:
          allOf:
            - $ref: "#/components/schemas/InfraMetricSeries"
            - nullable: true
              description: The series the points came from. Null when nothing matching the filters is stored — the chart renders its empty state and the window echo is still authoritative.
        fromMs:
          type: integer
        toMs:
          type: integer
        periodSeconds:
          type: integer
          description: Bucket width the points are drawn at, resolved server-side from the window. Never finer than CloudWatch still serves for that window, never finer than the series is stored at, and always wide enough that the range fits in `maxBuckets`.
        aggregation:
          type: string
          enum:
            - min
            - max
            - sum
            - avg
          description: How source points were folded into a bucket, chosen from the series statistic. A `Maximum` series buckets by max, because averaging it erases the spike it was charted for.
        maxBuckets:
          type: integer
        truncated:
          type: boolean
          description: The window held more buckets than the cap and the newest were dropped. Should not occur for a server-resolved period; possible when the caller pins a finer `period`.
        points:
          type: array
          items:
            type: object
            properties:
              tsMs:
                type: integer
                description: Bucket’s left edge, epoch ms.
              value:
                type: number
              count:
                type: integer
                description: Source datapoints in the bucket.
            required:
              - tsMs
              - value
              - count
          description: Oldest first. Empty buckets are absent, not zero-filled — no observation is not a measurement of zero.
        alarmSegments:
          type: array
          items:
            type: object
            properties:
              alertId:
                type: string
              ruleId:
                type: string
              state:
                type: string
                enum:
                  - OK
                  - ALARM
                  - INSUFFICIENT_DATA
              startMs:
                type: integer
              endMs:
                type: integer
            required:
              - alertId
              - ruleId
              - state
              - startMs
              - endMs
          description: "Non-OK stretches to shade behind the chart, reconstructed from transition history and clipped to the window. OK stretches are omitted: they are the background."
        alerts:
          type: array
          items:
            type: object
            additionalProperties:
              nullable: true
          description: The alerts the segments belong to, so the overlay can be labelled.
      required:
        - resource
        - series
        - fromMs
        - toMs
        - periodSeconds
        - aggregation
        - maxBuckets
        - truncated
        - points
        - alarmSegments
        - alerts
      description: A bounded, bucketed metric range plus the alert timeline over the same window. Read by REST polling — there is no metric WebSocket (decision INFRA-UI).
    LogIngestBatch:
      type: object
      properties:
        resource:
          type: object
          additionalProperties:
            nullable: true
          description: Batch-level resource attributes merged under each record’s own resource.
        records:
          type: array
          items:
            type: object
            properties:
              timeUnixNano:
                anyOf:
                  - type: number
                  - type: string
                description: Event time, Unix nanoseconds. Falls back to `timeUnixMillis`, observed time, then ingest time.
              timeUnixMillis:
                type: number
                description: Convenience event time in Unix milliseconds.
              observedTimeUnixNano:
                anyOf:
                  - type: number
                  - type: string
              severityNumber:
                type: integer
                description: OTel severity number (1–24).
              severityText:
                type: string
              severity:
                type: string
                description: Free-text level (e.g. "error"); mapped to a severity number.
              body:
                nullable: true
                description: Log body — a string, or any JSON value (stored as JSON text).
              message:
                nullable: true
                description: Alias for `body`.
              attributes:
                type: object
                additionalProperties:
                  nullable: true
              resource:
                type: object
                additionalProperties:
                  nullable: true
              scope:
                type: object
                additionalProperties:
                  nullable: true
              traceId:
                type: string
              spanId:
                type: string
              service:
                type: string
                description: Convenience `service.name` facet override for this record.
              environment:
                type: string
                description: Convenience `deployment.environment` facet override.
            description: One log record in an Agent Hub JSON batch.
          maxItems: 1000
          description: Up to 1000 records; overflow is rejected and counted.
      required:
        - records
      description: A simple Agent Hub JSON log batch mapped to the canonical OTel LogRecord model.
    LogIngestResponse:
      type: object
      properties:
        accepted:
          type: integer
          description: Records persisted.
        rejected:
          type: integer
          description: Records rejected (batch overflow + oversize).
        redactions:
          type: integer
          description: Secret substrings/keys masked before persistence.
      required:
        - accepted
        - rejected
        - redactions
      description: Agent Hub batch ingest result.
    OtlpExportLogsResponse:
      type: object
      properties:
        partialSuccess:
          type: object
          properties:
            rejectedLogRecords:
              type: string
              description: Count of rejected records (int64 as string).
            errorMessage:
              type: string
          required:
            - rejectedLogRecords
          description: Present only when some records were rejected; absent/empty on full success.
      description: OTLP `ExportLogsServiceResponse` (JSON encoding).
    LogIngestError:
      type: object
      properties:
        error:
          type: string
      required:
        - error
      description: Ingest error envelope.
    LogIssueRelease:
      type: object
      properties:
        release:
          type: string
          nullable: true
        commitSha:
          type: string
          nullable: true
        firstSeen:
          type: integer
          description: Epoch nanoseconds (record time_unix_nano), not millis.
        lastSeen:
          type: integer
          description: Epoch nanoseconds (record time_unix_nano), not millis.
        eventCount:
          type: integer
      required:
        - release
        - commitSha
        - firstSeen
        - lastSeen
        - eventCount
    LogIssue:
      type: object
      properties:
        id:
          type: string
        projectId:
          type: string
        fingerprint:
          type: string
        title:
          type: string
        service:
          type: string
          nullable: true
        environment:
          type: string
          nullable: true
        exceptionType:
          type: string
          nullable: true
        messageTemplate:
          type: string
          nullable: true
        firstSeen:
          type: integer
          description: Epoch nanoseconds (record time_unix_nano), not millis.
        lastSeen:
          type: integer
          description: Epoch nanoseconds (record time_unix_nano), not millis.
        eventCount:
          type: integer
        status:
          type: string
          enum:
            - open
            - resolved
            - ignored
        statusUpdatedAt:
          type: integer
          nullable: true
        statusUpdatedBy:
          type: string
          nullable: true
        firstRecordId:
          type: integer
          nullable: true
        lastRecordId:
          type: integer
          nullable: true
        analyzeSessionId:
          type: string
          nullable: true
        fixCardId:
          type: string
          nullable: true
        fixSessionId:
          type: string
          nullable: true
        createdAt:
          type: integer
        updatedAt:
          type: integer
      required:
        - id
        - projectId
        - fingerprint
        - title
        - service
        - environment
        - exceptionType
        - messageTemplate
        - firstSeen
        - lastSeen
        - eventCount
        - status
        - statusUpdatedAt
        - statusUpdatedBy
        - firstRecordId
        - lastRecordId
        - analyzeSessionId
        - fixCardId
        - fixSessionId
        - createdAt
        - updatedAt
    LogIssueDetail:
      allOf:
        - $ref: "#/components/schemas/LogIssue"
        - type: object
          properties:
            releases:
              type: array
              items:
                $ref: "#/components/schemas/LogIssueRelease"
            samples:
              type: array
              items:
                type: object
                additionalProperties:
                  nullable: true
          required:
            - releases
            - samples
    LogIssueListResponse:
      type: object
      properties:
        issues:
          type: array
          items:
            $ref: "#/components/schemas/LogIssue"
        nextCursor:
          type: string
          nullable: true
      required:
        - issues
        - nextCursor
    LogIssueErrorResponse:
      type: object
      properties:
        error:
          type: string
      required:
        - error
    LogIssueActionRequest:
      type: object
      properties:
        startAnother:
          type: boolean
          default: false
    LogIssueBulkStatusRequest:
      type: object
      properties:
        issueIds:
          type: array
          items:
            type: string
            minLength: 1
            maxLength: 200
          minItems: 1
          maxItems: 200
        status:
          type: string
          enum:
            - open
            - resolved
            - ignored
      required:
        - issueIds
        - status
    LogIssueBulkStatusResponse:
      type: object
      properties:
        updated:
          type: array
          items:
            $ref: "#/components/schemas/LogIssue"
        notFound:
          type: array
          items:
            type: string
          description: Requested ids that do not belong to this project (or no longer exist).
      required:
        - updated
        - notFound
    LogIssueFixResponse:
      type: object
      properties:
        cardId:
          type: string
        sessionId:
          type: string
        agentId:
          type: string
        automation:
          type: string
          enum:
            - manual
            - review
            - push
            - merge
        reused:
          type: boolean
        issue:
          $ref: "#/components/schemas/LogIssue"
        card:
          type: object
          additionalProperties:
            nullable: true
      required:
        - cardId
        - sessionId
        - agentId
        - automation
        - reused
        - issue
        - card
    LogIssueAnalyzeResponse:
      type: object
      properties:
        sessionId:
          type: string
        agentId:
          type: string
        reused:
          type: boolean
        issue:
          $ref: "#/components/schemas/LogIssue"
      required:
        - sessionId
        - agentId
        - reused
        - issue
    LogStoreMetrics:
      type: object
      properties:
        queue:
          type: object
          properties:
            depth:
              type: integer
              description: Records pending in the write queue.
            depthLimit:
              type: integer
              description: Queue depth cap; a full queue applies 429 backpressure.
          required:
            - depth
            - depthLimit
          description: Hub-wide batch-writer queue state.
        counters:
          type: object
          properties:
            accepted:
              type: integer
            written:
              type: integer
            rejected:
              type: integer
            dropped:
              type: integer
            redacted:
              type: integer
            writeErrors:
              type: integer
            expiredDeleted:
              type: integer
            quotaDeleted:
              type: integer
            wsDrops:
              type: integer
              description: Live-tail subscribers force-dropped for reconnect recovery.
          required:
            - accepted
            - written
            - rejected
            - dropped
            - redacted
            - writeErrors
            - expiredDeleted
            - quotaDeleted
            - wsDrops
          description: Monotonic ingest/reaper counters since boot.
        latency:
          type: object
          properties:
            meanFlushMs:
              type: number
            flushCount:
              type: integer
          required:
            - meanFlushMs
            - flushCount
          description: Writer flush latency.
        storage:
          type: object
          properties:
            projectBytes:
              type: integer
              description: Normalized bytes stored for this project.
            dbBytes:
              type: integer
              description: On-disk size of logs.db.
            quotaBytes:
              type: integer
            retentionDays:
              type: integer
            retentionLagRecords:
              type: integer
              description: Records past retention not yet reaped for this project.
          required:
            - projectBytes
            - dbBytes
            - quotaBytes
            - retentionDays
            - retentionLagRecords
          description: Per-project storage + retention gauges.
      required:
        - queue
        - counters
        - latency
        - storage
      description: Log-store health metrics (decision LOG-SCOPE).
    LogMetricsErrorResponse:
      type: object
      properties:
        error:
          type: string
      required:
        - error
      description: Error envelope.
    LogRecord:
      type: object
      properties:
        id:
          type: integer
        projectId:
          type: string
        sourceId:
          type: string
        timeUnixNano:
          type: integer
        observedTimeUnixNano:
          type: integer
          nullable: true
        severityNumber:
          type: integer
        severityText:
          type: string
          nullable: true
        body:
          type: string
          nullable: true
        serviceName:
          type: string
          nullable: true
        environment:
          type: string
          nullable: true
        traceId:
          type: string
          nullable: true
        spanId:
          type: string
          nullable: true
        fingerprint:
          type: string
          nullable: true
        resourceJson:
          type: string
          nullable: true
        attributesJson:
          type: string
          nullable: true
        scopeJson:
          type: string
          nullable: true
        byteSize:
          type: integer
        ingestedAt:
          type: integer
      required:
        - id
        - projectId
        - sourceId
        - timeUnixNano
        - observedTimeUnixNano
        - severityNumber
        - severityText
        - body
        - serviceName
        - environment
        - traceId
        - spanId
        - fingerprint
        - resourceJson
        - attributesJson
        - scopeJson
        - byteSize
        - ingestedAt
    LogQueryResponse:
      type: object
      properties:
        records:
          type: array
          items:
            $ref: "#/components/schemas/LogRecord"
        nextCursor:
          type: integer
          nullable: true
        nextCursorTimeUnixNano:
          type: integer
          nullable: true
      required:
        - records
        - nextCursor
        - nextCursorTimeUnixNano
    LogQueryErrorResponse:
      type: object
      properties:
        error:
          type: string
      required:
        - error
    LogPurgeResponse:
      type: object
      properties:
        purged:
          type: integer
          minimum: 0
      required:
        - purged
    LogSource:
      type: object
      properties:
        id:
          type: string
        projectId:
          type: string
        name:
          type: string
        serviceName:
          type: string
          nullable: true
        environment:
          type: string
          nullable: true
        tokenPrefix:
          type: string
          nullable: true
        status:
          type: string
          enum:
            - active
            - revoked
        createdAt:
          type: integer
        rotatedAt:
          type: integer
          nullable: true
        revokedAt:
          type: integer
          nullable: true
        lastIngestAt:
          type: integer
          nullable: true
          description: Wall-clock ms of the most recent record ingested under this source, or null if it has never ingested.
      required:
        - id
        - projectId
        - name
        - serviceName
        - environment
        - tokenPrefix
        - status
        - createdAt
        - rotatedAt
        - revokedAt
        - lastIngestAt
      description: A project log source. Never carries token material.
    LogSourceWithToken:
      type: object
      properties:
        id:
          type: string
        projectId:
          type: string
        name:
          type: string
        serviceName:
          type: string
          nullable: true
        environment:
          type: string
          nullable: true
        tokenPrefix:
          type: string
          nullable: true
        status:
          type: string
          enum:
            - active
            - revoked
        createdAt:
          type: integer
        rotatedAt:
          type: integer
          nullable: true
        revokedAt:
          type: integer
          nullable: true
        lastIngestAt:
          type: integer
          nullable: true
          description: Wall-clock ms of the most recent record ingested under this source, or null if it has never ingested.
        token:
          type: string
          description: Plaintext `ahlog_` ingest token. Returned ONCE at create/rotate; only its hash is stored. Surface it to the user immediately — it cannot be retrieved later.
      required:
        - id
        - projectId
        - name
        - serviceName
        - environment
        - tokenPrefix
        - status
        - createdAt
        - rotatedAt
        - revokedAt
        - lastIngestAt
        - token
      description: A log source plus its freshly minted plaintext ingest token.
    LogSourceList:
      type: object
      properties:
        sources:
          type: array
          items:
            $ref: "#/components/schemas/LogSource"
      required:
        - sources
      description: Project sources.
    LogSourceAuditEntry:
      type: object
      properties:
        id:
          type: string
        projectId:
          type: string
        sourceId:
          type: string
          nullable: true
        action:
          type: string
          enum:
            - create
            - update
            - rotate
            - revoke
            - delete
        actorUserId:
          type: string
          nullable: true
        detail:
          type: string
          nullable: true
        createdAt:
          type: integer
      required:
        - id
        - projectId
        - sourceId
        - action
        - actorUserId
        - detail
        - createdAt
      description: A source/token lifecycle audit event.
    LogSourceAuditList:
      type: object
      properties:
        entries:
          type: array
          items:
            $ref: "#/components/schemas/LogSourceAuditEntry"
      required:
        - entries
      description: Lifecycle audit for a source.
    LogSourceErrorResponse:
      type: object
      properties:
        error:
          type: string
      required:
        - error
      description: Error envelope.
    LogsWizardErrorResponse:
      type: object
      properties:
        error:
          type: string
        message:
          type: string
      required:
        - error
      description: Error envelope for logs wizard routes.
    LogsEntryCandidate:
      type: object
      properties:
        path:
          type: string
        kind:
          type: string
          enum:
            - entrypoint
            - logger-config
            - bootstrap
      required:
        - path
        - kind
    LogsWizardSource:
      type: object
      properties:
        id:
          type: string
        projectId:
          type: string
        name:
          type: string
        serviceName:
          type: string
          nullable: true
        environment:
          type: string
          nullable: true
        tokenPrefix:
          type: string
          nullable: true
        status:
          type: string
          enum:
            - active
            - revoked
        createdAt:
          type: integer
        rotatedAt:
          type: integer
          nullable: true
        revokedAt:
          type: integer
          nullable: true
        lastIngestAt:
          type: integer
          nullable: true
      required:
        - id
        - projectId
        - name
        - serviceName
        - environment
        - tokenPrefix
        - status
        - createdAt
        - rotatedAt
        - revokedAt
        - lastIngestAt
      description: Existing log source metadata (never carries token material).
    LogsSetupDraft:
      type: object
      properties:
        ingestOrigin:
          type: string
        otlpEndpoint:
          type: string
        batchEndpoint:
          type: string
        stack:
          type: string
          enum:
            - node
            - python
            - go
            - ruby
            - java
            - mixed
            - unknown
        packageManager:
          type: string
          nullable: true
          enum:
            - npm
            - pnpm
            - yarn
            - bun
            - pip
            - poetry
            - go
            - bundler
            - maven
            - gradle
            - null
        loggingLibraries:
          type: array
          items:
            type: string
        hasOtelSdk:
          type: boolean
        hasOtelCollectorConfig:
          type: boolean
        collectorConfigPaths:
          type: array
          items:
            type: string
        entryCandidates:
          type: array
          items:
            $ref: "#/components/schemas/LogsEntryCandidate"
        recommendedApproach:
          type: string
          enum:
            - collector
            - otel-sdk
            - json-batch
        suggestedServiceName:
          type: string
          nullable: true
        envExampleKeys:
          type: array
          items:
            type: string
        readme:
          nullable: true
        notes:
          type: array
          items:
            type: string
        existingSources:
          type: array
          items:
            $ref: "#/components/schemas/LogsWizardSource"
      required:
        - ingestOrigin
        - otlpEndpoint
        - batchEndpoint
        - stack
        - packageManager
        - loggingLibraries
        - hasOtelSdk
        - hasOtelCollectorConfig
        - collectorConfigPaths
        - entryCandidates
        - recommendedApproach
        - suggestedServiceName
        - envExampleKeys
        - notes
        - existingSources
      description: "Server-precomputed logs-instrumentation scan: detected stack, logging libraries, existing OpenTelemetry setup, exporter target candidates, a recommended ingest approach, the Hub ingest endpoints, and the project’s existing log sources."
    LogsSetupDraftResponse:
      type: object
      properties:
        projectId:
          type: string
        draft:
          $ref: "#/components/schemas/LogsSetupDraft"
      required:
        - projectId
        - draft
      description: Logs-instrumentation detection result for a project.
    LogsWizardStartResponse:
      type: object
      properties:
        sessionId:
          type: string
        agentId:
          type: string
        draft:
          $ref: "#/components/schemas/LogsSetupDraft"
        session:
          nullable: true
          description: Raw `sessions` row for the spawned wizard session.
      required:
        - sessionId
        - agentId
        - draft
      description: Logs setup wizard session spawned successfully.
    MeDailySummaryErrorResponse:
      type: object
      properties:
        error:
          type: string
        code:
          type: string
      required:
        - error
      description: Error envelope for Hub Daily Summary routes.
    MeDailySummaryReport:
      type: object
      properties:
        date:
          type: string
        timeZone:
          type: string
        markdown:
          type: string
        engine:
          type: string
        model:
          type: string
        generatedAt:
          type: string
      required:
        - date
        - timeZone
        - markdown
        - engine
        - model
        - generatedAt
      description: The calling user's Daily Summary for one local calendar day. Cleared when the date is no longer today.
    MeDailySummaryResponse:
      type: object
      properties:
        date:
          type: string
        timeZone:
          type: string
        report:
          allOf:
            - $ref: "#/components/schemas/MeDailySummaryReport"
            - nullable: true
      required:
        - date
        - timeZone
        - report
      description: Today's Daily Summary for the caller, or report=null when none has been generated yet today.
    MeDailySummarySchedule:
      type: object
      properties:
        enabled:
          type: boolean
        timeZone:
          type: string
        times:
          type: array
          items:
            type: string
      required:
        - enabled
        - timeZone
        - times
      description: "Auto-refresh schedule for the caller's Hub Daily Summary: 1+ local `HH:MM` times of day (interpreted in `timeZone`) at which the report regenerates using the caller's own engine credentials."
    MeDailySummaryScheduleResponse:
      type: object
      properties:
        schedule:
          allOf:
            - $ref: "#/components/schemas/MeDailySummarySchedule"
            - nullable: true
      required:
        - schedule
      description: The stored schedule, or null when the summary is not scheduled.
    MeTodosErrorResponse:
      type: object
      properties:
        error:
          type: string
      required:
        - error
      description: Error envelope for personal-todo routes.
    UserTodo:
      type: object
      properties:
        id:
          type: string
        userId:
          type: string
        title:
          type: string
        notes:
          type: string
        status:
          type: string
          enum:
            - open
            - done
        priority:
          type: string
          enum:
            - urgent
            - high
            - medium
            - low
        doDate:
          type: string
          nullable: true
          description: Day the user plans to work the task (scheduling "do" date, not a deadline).
        doStartAt:
          type: string
          nullable: true
        doEndAt:
          type: string
          nullable: true
        dueAt:
          type: string
          nullable: true
          description: "Deprecated: retained for back-compat. Prefer doDate."
        position:
          type: number
        sourceType:
          type: string
          enum:
            - manual
            - email
            - calendar
        sourceId:
          type: string
          nullable: true
        sourceMeta:
          type: object
          nullable: true
          additionalProperties:
            nullable: true
        linkedType:
          type: string
          nullable: true
          enum:
            - card
            - epic
            - session
            - null
        linkedId:
          type: string
          nullable: true
        linkedProjectId:
          type: string
          nullable: true
        linkedCardId:
          type: string
          nullable: true
          description: "Deprecated: superseded by linkedType/linkedId. Kept in sync for a card link."
        createdAt:
          type: string
        updatedAt:
          type: string
      required:
        - id
        - userId
        - title
        - notes
        - status
        - priority
        - doDate
        - doStartAt
        - doEndAt
        - dueAt
        - position
        - sourceType
        - sourceId
        - sourceMeta
        - linkedType
        - linkedId
        - linkedProjectId
        - linkedCardId
        - createdAt
        - updatedAt
      description: A single cross-project personal todo owned by one Hub user. Independent of any project board.
    MeDashboardErrorResponse:
      type: object
      properties:
        error:
          type: string
      required:
        - error
      description: Error envelope for personal dashboard routes.
    DashboardWorkCard:
      type: object
      properties:
        id:
          type: string
        shortId:
          type: number
          nullable: true
        title:
          type: string
        priority:
          type: string
          enum:
            - urgent
            - high
            - medium
            - low
        columnId:
          type: string
        columnName:
          type: string
        isDone:
          type: boolean
        projectId:
          type: string
        projectName:
          type: string
        boardId:
          type: string
        epicId:
          type: string
          nullable: true
        prUrl:
          type: string
          nullable: true
        reviewStatus:
          type: string
          nullable: true
        sessionId:
          type: string
          nullable: true
        createdAt:
          type: string
        updatedAt:
          type: string
      required:
        - id
        - shortId
        - title
        - priority
        - columnId
        - columnName
        - isDone
        - projectId
        - projectName
        - boardId
        - epicId
        - prUrl
        - reviewStatus
        - sessionId
        - createdAt
        - updatedAt
      description: A kanban card assigned to the calling user on a board they can view.
    DashboardWork:
      type: object
      properties:
        cards:
          type: array
          items:
            $ref: "#/components/schemas/DashboardWorkCard"
        counts:
          type: object
          properties:
            total:
              type: number
            open:
              type: number
            byPriority:
              type: object
              properties:
                urgent:
                  type: number
                high:
                  type: number
                medium:
                  type: number
                low:
                  type: number
              required:
                - urgent
                - high
                - medium
                - low
          required:
            - total
            - open
            - byPriority
      required:
        - cards
        - counts
      description: The caller's assigned cards across every visible board, plus open-work counts.
    DashboardGoogle:
      type: object
      properties:
        configured:
          type: boolean
        connected:
          type: boolean
        email:
          type: string
          nullable: true
        reconnectRequired:
          type: boolean
        calendar:
          type: object
          properties:
            scopeGranted:
              type: boolean
            date:
              type: string
              nullable: true
            timeZone:
              type: string
              nullable: true
            events:
              type: array
              items:
                type: object
                properties:
                  id:
                    type: string
                    nullable: true
                  summary:
                    type: string
                    nullable: true
                  location:
                    type: string
                    nullable: true
                  allDay:
                    type: boolean
                  start:
                    type: string
                    nullable: true
                  end:
                    type: string
                    nullable: true
                  htmlLink:
                    type: string
                    nullable: true
                  hangoutLink:
                    type: string
                    nullable: true
                required:
                  - id
                  - summary
                  - location
                  - allDay
                  - start
                  - end
                  - htmlLink
                  - hangoutLink
            error:
              type: string
              nullable: true
          required:
            - scopeGranted
            - date
            - timeZone
            - events
            - error
        mail:
          type: object
          properties:
            scopeGranted:
              type: boolean
            unread:
              type: number
              nullable: true
            starred:
              type: number
              nullable: true
            important:
              type: number
              nullable: true
            messages:
              type: array
              items:
                type: object
                properties:
                  id:
                    type: string
                    nullable: true
                  threadId:
                    type: string
                    nullable: true
                  from:
                    type: string
                    nullable: true
                  subject:
                    type: string
                    nullable: true
                  snippet:
                    type: string
                    nullable: true
                  date:
                    type: string
                    nullable: true
                  internalDate:
                    type: string
                    nullable: true
                  unread:
                    type: boolean
                required:
                  - id
                  - threadId
                  - from
                  - subject
                  - snippet
                  - date
                  - internalDate
                  - unread
            error:
              type: string
              nullable: true
          required:
            - scopeGranted
            - unread
            - starred
            - important
            - messages
            - error
      required:
        - configured
        - connected
        - email
        - reconnectRequired
        - calendar
        - mail
      description: "The caller's Google slice (today's calendar + flagged-mail counts). Soft-degrades: unconfigured / unlinked / missing-scope all render as false blocks."
    MeDashboardResponse:
      type: object
      properties:
        generatedAt:
          type: string
        work:
          $ref: "#/components/schemas/DashboardWork"
        todos:
          type: object
          properties:
            open:
              type: array
              items:
                $ref: "#/components/schemas/UserTodo"
            openCount:
              type: number
          required:
            - open
            - openCount
        google:
          $ref: "#/components/schemas/DashboardGoogle"
      required:
        - generatedAt
        - work
        - todos
        - google
      description: Aggregated personal dashboard for the authenticated user.
    MeHubErrorResponse:
      type: object
      properties:
        error:
          type: string
      required:
        - error
      description: Error envelope for Hub session routes.
    HubAssistantAgent:
      type: object
      properties:
        id:
          type: string
        name:
          type: string
        role:
          type: string
        engine:
          type: string
        model:
          type: string
        projectId:
          type: string
      required:
        - id
        - name
        - engine
      additionalProperties:
        nullable: true
      description: The hidden Hub assistant agent (not on a project roster).
    MeHubSessionResponse:
      type: object
      properties:
        session:
          $ref: "#/components/schemas/Session"
        agent:
          $ref: "#/components/schemas/HubAssistantAgent"
      required:
        - session
        - agent
      description: The calling user's persistent Hub assistant session and its agent.
    MeHubModelResponse:
      type: object
      properties:
        engine:
          type: string
        model:
          type: string
      required:
        - engine
        - model
      description: Engine and model for Hub chats, Daily Summary, and other Hub-level generation.
    MemoryErrorResponse:
      type: object
      properties:
        error:
          type: string
        details:
          type: array
          items:
            type: object
            properties:
              path:
                type: array
                items:
                  anyOf:
                    - type: string
                    - type: number
              message:
                type: string
            required:
              - path
              - message
      required:
        - error
      description: Error envelope for memory routes.
    NoteProcessing:
      type: object
      properties:
        id:
          type: string
        project_id:
          type: string
        date:
          type: string
        excerpt:
          type: string
        target:
          type: string
          enum:
            - auto
            - wiki
            - memory
            - plan
        session_id:
          type: string
        status:
          anyOf:
            - type: string
              enum:
                - queued
                - running
                - success
                - error
            - type: string
        result:
          type: string
          nullable: true
        created_at:
          type: string
        updated_at:
          type: string
      required:
        - id
        - project_id
        - date
        - excerpt
        - target
        - session_id
        - status
      additionalProperties:
        nullable: true
      description: A single note-processing run row.
    MiscErrorResponse:
      type: object
      properties:
        error:
          type: string
      required:
        - error
      description: Error envelope for misc routes.
    HealthResponse:
      type: object
      properties:
        status:
          type: string
          enum:
            - ok
        version:
          type: string
        gitHash:
          type: string
        uptime:
          type: number
        projects:
          type: integer
        agents:
          type: integer
        authRequired:
          type: boolean
        apiKeyAuthEnabled:
          type: boolean
        jwtAuthEnabled:
          type: boolean
        logShipping:
          type: object
          properties:
            enabled:
              type: boolean
              description: True when AHLOG_TOKEN is configured and the server is shipping its own console logs to the Logs module. False means self log-shipping is disabled.
          required:
            - enabled
          description: Self log-shipping status (server/log-shipper.ts). Boolean only — the ingest endpoint and token are never exposed, since /api/health is unauthenticated.
      required:
        - status
        - version
        - gitHash
        - uptime
        - projects
        - agents
        - authRequired
        - apiKeyAuthEnabled
        - jwtAuthEnabled
        - logShipping
      description: Liveness + runtime info for the server.
    BrowseResponse:
      type: object
      properties:
        path:
          type: string
        parent:
          type: string
          nullable: true
        entries:
          type: array
          items:
            type: object
            properties:
              name:
                type: string
              type:
                type: string
                enum:
                  - directory
              path:
                type: string
            required:
              - name
              - type
              - path
      required:
        - path
        - parent
        - entries
      description: Directory listing under $HOME (the only allowed root). Hidden entries sorted last.
    DeviceTokenResponse:
      type: object
      properties:
        token:
          type: string
        platform:
          type: string
        enabledEvents:
          type: array
          nullable: true
          items:
            type: string
        supportedEvents:
          type: array
          items:
            type: string
      required:
        - token
        - platform
        - enabledEvents
        - supportedEvents
      description: Push-token preferences and supported event list.
    UsageResponse:
      type: object
      properties:
        totals:
          type: object
          properties:
            sessions:
              type: integer
            messages:
              type: integer
            tokens:
              type: integer
            cost_usd:
              type: number
          additionalProperties:
            nullable: true
        byAgent:
          type: array
          items:
            type: object
            properties:
              agent_id:
                type: string
              agent_name:
                type: string
              agent_color:
                type: string
            required:
              - agent_id
              - agent_name
              - agent_color
            additionalProperties:
              nullable: true
        byDay:
          type: array
          items:
            type: object
            properties:
              day:
                type: string
            required:
              - day
            additionalProperties:
              nullable: true
        recentSessions:
          type: array
          items:
            type: object
            properties:
              agent_id:
                type: string
              agent_name:
                type: string
              agent_color:
                type: string
            required:
              - agent_id
              - agent_name
              - agent_color
            additionalProperties:
              nullable: true
      required:
        - totals
        - byAgent
        - byDay
        - recentSessions
      description: Aggregated usage rollup. Cost de-dup of the Claude CLI cumulative `total_cost_usd` happens in `usage-aggregation.ts`.
    NotesErrorResponse:
      type: object
      properties:
        error:
          type: string
        details:
          type: array
          items:
            type: object
            properties:
              path:
                type: array
                items:
                  anyOf:
                    - type: string
                    - type: number
              message:
                type: string
            required:
              - path
              - message
      required:
        - error
      description: Error envelope for notes routes.
    Note:
      type: object
      properties:
        id:
          type: string
        project_id:
          type: string
        title:
          type: string
        content:
          type: string
          nullable: true
        created_at:
          type: string
        updated_at:
          type: string
      required:
        - id
        - project_id
        - title
        - content
        - created_at
        - updated_at
      description: A single project note row.
    PrAutoMergeRequest:
      type: object
      properties:
        prUrl:
          type: string
          description: Full GitHub PR URL (`https://github.com/owner/repo/pull/N`).
          example: https://github.com/acme/webapp/pull/42
        enabled:
          type: boolean
          description: true arms GitHub native auto-merge (merge once required checks pass and required reviews approve); false disarms it.
        mergeMethod:
          type: string
          enum:
            - squash
            - merge
            - rebase
          description: Merge strategy GitHub uses when auto-merge fires. Defaults to `squash`.
      required:
        - prUrl
        - enabled
      description: Arm or disarm GitHub native auto-merge on a PR, using the acting user’s connected GitHub credential.
    PrAutoMergeResponse:
      type: object
      properties:
        ok:
          type: boolean
          enum:
            - true
        enabled:
          type: boolean
        mergeMethod:
          type: string
          enum:
            - squash
            - merge
            - rebase
        pr:
          type: string
          description: PR number as a string.
      required:
        - ok
        - enabled
        - mergeMethod
        - pr
      description: Auto-merge toggle result.
    PullRequestSummary:
      type: object
      properties:
        number:
          type: number
        title:
          type: string
        state:
          type: string
        draft:
          type: boolean
        html_url:
          type: string
        user:
          type: string
          nullable: true
        head:
          type: string
          nullable: true
        base:
          type: string
          nullable: true
        created_at:
          type: string
          nullable: true
        updated_at:
          type: string
          nullable: true
        merged_at:
          type: string
          nullable: true
        closed_at:
          type: string
          nullable: true
      required:
        - number
        - title
        - state
      additionalProperties:
        nullable: true
      description: One row of the PR list. Additional fields (labels, review_decision, check_rollup, linked_card, linked_epic, …) vary by backend and are passed through.
    PullRequestListResponse:
      type: object
      properties:
        repo:
          type: string
          description: "`owner/repo` for GitHub-backed projects, or the project id for Agent Hub-hosted ones."
        state:
          type: string
          enum:
            - open
            - closed
            - all
        source:
          type: string
          enum:
            - agenthub
            - user-oauth
        page:
          type: number
          description: 1-based page that was served.
        limit:
          type: number
          description: Page size actually applied (max 100).
        hasMore:
          type: boolean
          description: 'Whether another page exists. Exact on both backends: Agent Hub-hosted projects over-fetch one row, GitHub-backed projects read `rel="next"` off GitHub’s `Link` header. Only falls back to a "the page came back full" guess if the upstream response carried no headers to read.'
        pulls:
          type: array
          items:
            $ref: "#/components/schemas/PullRequestSummary"
      required:
        - repo
        - state
        - source
        - page
        - limit
        - hasMore
        - pulls
      description: One page of pull requests, newest activity first.
    ProjectPreviewInstance:
      type: object
      properties:
        id:
          type: string
        sessionId:
          type: string
        agentId:
          type: string
          nullable: true
        sessionName:
          type: string
          nullable: true
        status:
          type: string
          enum:
            - starting
            - ready
            - failed
        port:
          type: number
          nullable: true
        url:
          type: string
          nullable: true
        startedAt:
          type: string
        lastActiveAt:
          type: string
      required:
        - id
        - sessionId
        - agentId
        - sessionName
        - status
        - port
        - url
        - startedAt
        - lastActiveAt
      description: Active worktree preview group for a chat session.
    PreviewSecretRow:
      type: object
      properties:
        id:
          type: string
        project_id:
          type: string
        key:
          type: string
        kind:
          type: string
          enum:
            - plain
            - secret
          description: "`plain` values are returned in clear on GET. `secret` values are masked on GET (return the `••••••••` sentinel) and only decrypted at spawn time inside `loadProjectEnvForSpawn`."
        value:
          type: string
          description: Decrypted value for `plain` kind; the `••••••••` MASK sentinel for `secret` kind. Never the plaintext of a secret-kind row.
        created_at:
          type: string
        updated_at:
          type: string
      required:
        - id
        - project_id
        - key
        - kind
        - value
        - created_at
        - updated_at
      description: One per-project secret row as returned by GET / PUT. `secret`-kind rows mask the value field; the only path that decrypts a secret is the spawn-time `loadProjectEnvForSpawn` (no caller-facing read).
    PreviewSecretsListResponse:
      type: object
      properties:
        secrets:
          type: array
          items:
            $ref: "#/components/schemas/PreviewSecretRow"
      required:
        - secrets
      description: Current secrets for the project, sorted by key. `secret`-kind values are masked.
    PreviewSecretsPutRequest:
      type: object
      properties:
        secrets:
          type: array
          items:
            type: object
            properties:
              key:
                type: string
                description: Env var name. Same validation rules as the URL `:key` param.
              value:
                type: string
                description: New plaintext value, OR the MASK sentinel (`••••••••`) for an existing `secret`-kind row to preserve its ciphertext without re-sending the plaintext.
              kind:
                type: string
                enum:
                  - plain
                  - secret
                description: Defaults to `secret` when omitted.
            required:
              - key
              - value
            description: One entry in a bulk-replace PUT body.
          description: Full replacement set. Keys not present in the array are removed. Send the MASK sentinel for an existing secret-kind row to preserve its current ciphertext.
      required:
        - secrets
      description: Body for PUT /secrets.
    PreviewSecretsImportRequest:
      type: object
      properties:
        env:
          type: string
          description: "`.env`-style blob. Supports `KEY=value`, quoted values, `export KEY=value`, comments and blank lines. Multi-line values and shell-expansion are NOT supported."
        mode:
          type: string
          enum:
            - replace
            - merge
          description: "`replace` (default) — wipe existing keys and install the parsed set. `merge` — keep existing keys, parsed entries win on conflict."
        defaultKind:
          type: string
          enum:
            - plain
            - secret
          description: Kind to assign to each parsed entry; defaults to `secret`.
      required:
        - env
      description: Body for POST /secrets/import.
    PreviewSecretsImportResponse:
      type: object
      properties:
        imported:
          type: integer
          description: Count of parsed entries that made it into the write.
        mode:
          type: string
          enum:
            - replace
            - merge
        secrets:
          type: array
          items:
            $ref: "#/components/schemas/PreviewSecretRow"
      required:
        - imported
        - mode
        - secrets
      description: Outcome of the import. `secrets` reflects the post-import state.
    PreviewSecretsErrorResponse:
      type: object
      properties:
        error:
          type: string
      required:
        - error
      description: Error envelope for preview-secrets routes.
    ProjectAwsSsoProfile:
      type: object
      properties:
        type:
          type: string
          enum:
            - sso
          description: Profile kind. Omitted legacy profiles are treated as SSO profiles.
        sso_account_id:
          type: string
          description: 12-digit AWS account id.
          example: "123456789012"
        sso_start_url:
          type: string
          description: IAM Identity Center start URL (`https://…`).
          example: https://d-1234567890.awsapps.com/start/
        sso_region:
          type: string
          description: AWS region code, e.g. `us-east-2`.
          example: us-east-2
        sso_role_name:
          type: string
          description: IAM Identity Center role to assume.
        region:
          type: string
          description: AWS region code, e.g. `us-east-2`.
          example: us-east-2
        output:
          type: string
          description: AWS CLI default output format; defaults to `json` on render.
      required:
        - sso_account_id
        - sso_start_url
        - sso_region
        - sso_role_name
        - region
      description: One AWS IAM Identity Center (SSO) profile stanza. Rendered into the per-spawn `AWS_CONFIG_FILE` as a `[profile <name>]` section. Not credentials — SSO tokens still live under the spawning user's `~/.aws/sso/cache`.
    ProjectAwsStaticProfile:
      type: object
      properties:
        type:
          type: string
          enum:
            - static
        aws_access_key_id:
          type: string
          description: AWS access key id for this project-scoped profile.
          example: AKIAIOSFODNN7EXAMPLE
        aws_secret_access_key:
          type: string
          description: AWS secret access key. Returned by the current admin API because project config is editable by Owners; callers must treat it as secret material.
        aws_session_token:
          type: string
          description: Optional temporary session token.
        region:
          type: string
          description: AWS region code, e.g. `us-east-2`.
          example: us-east-2
        output:
          type: string
          description: AWS CLI default output format; defaults to `json` on render.
      required:
        - type
        - aws_access_key_id
        - aws_secret_access_key
        - region
      description: One static-credential profile. Rendered into the project-scoped `AWS_SHARED_CREDENTIALS_FILE` as a `[name]` section and paired with a `[profile name]` config section for region/output.
    ProjectAwsRoleProfile:
      type: object
      properties:
        type:
          type: string
          enum:
            - role
        role_arn:
          type: string
          description: IAM role to assume.
          example: arn:aws:iam::123456789012:role/AgentHubMonitoring
        external_id:
          type: string
          description: External ID presented on AssumeRole and matched by the role's trust policy. Generated by the Hub and identical to `externalId` on the profiles response; any value sent here is discarded, because an operator-chosen external ID would not defend against the confused-deputy problem it exists for.
        source_profile:
          type: string
          description: Another profile in this project to chain from. Mutually exclusive with `credential_source`.
        credential_source:
          type: string
          enum:
            - Environment
            - Ec2InstanceMetadata
            - EcsContainer
          description: Where the base credentials come from when not chained. Omit to follow the Hub's detected runtime — see `ambientCredentialSource` on the profiles response.
        role_session_name:
          type: string
          description: Session name recorded in CloudTrail for the assumed role.
        region:
          type: string
          description: AWS region code, e.g. `us-east-2`.
          example: us-east-2
        output:
          type: string
          description: AWS CLI default output format; defaults to `json` on render.
      required:
        - type
        - role_arn
        - region
      description: One assume-role profile. Rendered into the project-scoped `AWS_CONFIG_FILE` as a `[profile <name>]` section with `role_arn` plus either `source_profile` or `credential_source`. Holds no secret material and needs no interactive login, so it is the profile kind unattended collection can use.
    ProjectAwsProfile:
      anyOf:
        - $ref: "#/components/schemas/ProjectAwsSsoProfile"
        - $ref: "#/components/schemas/ProjectAwsStaticProfile"
        - $ref: "#/components/schemas/ProjectAwsRoleProfile"
      description: "An AWS profile stanza: IAM Identity Center (SSO), static credentials, or an assumed role."
    ProjectAwsProfilesMap:
      type: object
      additionalProperties:
        $ref: "#/components/schemas/ProjectAwsProfile"
      description: Map of profile name → profile stanza.
    ProjectAwsProfilesResponse:
      type: object
      properties:
        profiles:
          $ref: "#/components/schemas/ProjectAwsProfilesMap"
        externalId:
          type: string
          description: "External ID this Hub presents when assuming any of the project's role profiles, and the value the customer puts in their role trust policy's `sts:ExternalId` condition. Hub-generated, unique per project, and read-only: AWS's confused-deputy guidance requires the assuming party to generate it, so a value the operator could choose would defeat its purpose. Minted on first read of this endpoint."
          example: agent-hub-8f14e45f-ceea-467a-9c1e-6b1e5f8a0d2c
        defaultProfile:
          type: string
          nullable: true
          description: Profile the operator explicitly designated as the project default, or null when none is designated.
        effectiveDefaultProfile:
          type: string
          nullable: true
          description: "Profile exported as `AWS_PROFILE` to spawns and the session Terminal: the designation when set, else the sole configured profile, else null. Without it, un-flagged `aws` commands fall back to a `[default]` section the project-scoped config file never has."
        monitoringProfile:
          type: string
          nullable: true
          description: Profile the operator designated for unattended background work (metric collection, alert evaluation), or null when none is designated. Never an SSO profile.
        effectiveMonitoringProfile:
          type: string
          nullable: true
          description: 'Profile background collection actually runs as: the designation when it still names a live non-SSO profile, else null. There is no sole-profile fallback — null means the Infrastructure module shows its "designate a monitoring profile" empty state.'
        ambientCredentialSource:
          type: string
          enum:
            - Environment
            - Ec2InstanceMetadata
            - EcsContainer
          description: The `credential_source` this Hub renders for role profiles that name no origin, detected from the server runtime (container credential endpoint → `EcsContainer`, ambient key env vars → `Environment`, else `Ec2InstanceMetadata`) and overridable with `AGENT_HUB_AWS_CREDENTIAL_SOURCE`.
      required:
        - profiles
        - externalId
        - defaultProfile
        - effectiveDefaultProfile
        - monitoringProfile
        - effectiveMonitoringProfile
        - ambientCredentialSource
      description: Current AWS profiles configured on the project.
    ProjectAwsProfilesRequest:
      type: object
      properties:
        profiles:
          anyOf:
            - $ref: "#/components/schemas/ProjectAwsProfilesMap"
            - type: array
              items:
                nullable: true
          description: "Replacement set of profiles. Accepts either a `{ name: stanza }` map or an array of `{ name, ...stanza }` objects. Pass `{}` to clear all profiles."
        defaultProfile:
          type: string
          nullable: true
          description: Profile to export as `AWS_PROFILE` for spawns and the session Terminal, so un-flagged `aws` commands resolve. Must name one of the profiles in this same request; pass null or omit to clear the designation.
        monitoringProfile:
          type: string
          nullable: true
          description: Profile unattended background work runs as. Must name a `static` or `role` profile in this same request — an SSO profile is rejected with 400 because its token cache is HOME-keyed and expires with nobody around to re-run `aws sso login`. Pass null or omit to clear the designation.
      required:
        - profiles
      description: Body for PUT /aws-profiles.
    ProjectAwsErrorResponse:
      type: object
      properties:
        error:
          type: string
      required:
        - error
      description: Error envelope for project-aws routes.
    ProjectAwsSsoStatusResponse:
      anyOf:
        - type: object
          properties:
            profile:
              type: string
            loggedIn:
              type: boolean
              enum:
                - true
            credentialType:
              type: string
              enum:
                - sso
            account:
              type: string
            arn:
              type: string
            userId:
              type: string
            homeSource:
              type: string
              enum:
                - caller
                - host
                - per-user
              description: "Which HOME's SSO token cache produced this identity: 'caller' (the requesting user's per-user HOME), 'host' (the shared break-glass/operator HOME), or 'per-user' (the caller's per-user HOME for a not-logged-in result)."
          required:
            - profile
            - loggedIn
            - homeSource
        - type: object
          properties:
            profile:
              type: string
            loggedIn:
              type: boolean
              enum:
                - false
            credentialType:
              type: string
              enum:
                - sso
            error:
              type: string
            needsLogin:
              type: boolean
            homeSource:
              type: string
              enum:
                - caller
                - host
                - per-user
              description: "Which HOME the caller should log into to authenticate this profile: 'per-user' (the requesting user's own HOME) or 'host' (the shared break-glass/operator HOME). Run POST /aws-sso/login under that HOME. (Not the last-probed HOME — both caller and host caches were checked and neither held a valid token.)"
          required:
            - profile
            - loggedIn
            - homeSource
        - type: object
          properties:
            profile:
              type: string
            loggedIn:
              type: boolean
              enum:
                - true
            credentialType:
              type: string
              enum:
                - static
                - role
            account:
              type: string
            arn:
              type: string
            userId:
              type: string
          required:
            - profile
            - loggedIn
            - credentialType
        - type: object
          properties:
            profile:
              type: string
            loggedIn:
              type: boolean
              enum:
                - false
            credentialType:
              type: string
              enum:
                - static
                - role
            error:
              type: string
            needsLogin:
              type: boolean
              enum:
                - false
          required:
            - profile
            - loggedIn
            - credentialType
      description: "`aws sts get-caller-identity` result for the named profile. SSO profiles probe the caller HOME first, then the shared host HOME as a fallback (never other users’ per-user HOMEs). Static profiles use the project-scoped credentials file and role profiles assume `role_arn` from the Hub’s ambient credentials; neither requires SSO login."
    ProjectAwsSsoLoginRequest:
      type: object
      properties:
        profile:
          type: string
          description: Configured profile name to log in.
      required:
        - profile
      description: Body for POST /aws-sso/login.
    ProjectAwsSsoLoginResponse:
      anyOf:
        - type: object
          properties:
            ok:
              type: boolean
              enum:
                - true
            loginId:
              type: string
              description: Server-local id for this login attempt — useful for log correlation.
            profile:
              type: string
            loginUrl:
              type: string
              description: IAM Identity Center device-authorization URL the user must open in a browser. Present on the standard happy path where the URL surfaces within ~30 seconds.
            completed:
              type: boolean
              description: True when `aws sso login --no-browser` finished entirely server-side (rare — usually the user completes the device flow in a browser before the CLI exits).
            output:
              type: string
              description: Tail of the CLI stdout/stderr (capped) for diagnostic purposes.
          required:
            - ok
            - loginId
            - profile
        - type: object
          properties:
            ok:
              type: boolean
              enum:
                - false
            loginId:
              type: string
            profile:
              type: string
            error:
              type: string
            output:
              type: string
          required:
            - ok
            - loginId
            - profile
            - error
      description: Outcome of the browser-less `aws sso login` spawn. Only one login proc runs per server at a time; if a second request arrives while a previous one is still running the previous proc is killed and a new one is spawned.
    ProjectEmailLogo:
      type: object
      properties:
        filename:
          type: string
        contentType:
          type: string
        size:
          type: integer
        updatedAt:
          type: string
      required:
        - filename
        - contentType
        - size
        - updatedAt
    ProjectEmailLogoResponse:
      type: object
      properties:
        emailLogo:
          allOf:
            - $ref: "#/components/schemas/ProjectEmailLogo"
            - nullable: true
      required:
        - emailLogo
    ProjectEmailLogoDeleteResponse:
      type: object
      properties:
        ok:
          type: boolean
        emailLogo:
          nullable: true
      required:
        - ok
        - emailLogo
    ProjectBrandingErrorResponse:
      type: object
      properties:
        error:
          type: string
      required:
        - error
    ReleaseEmailPreviewResponse:
      type: object
      properties:
        html:
          type: string
          description: Fully rendered branded email HTML for preview.
        subject:
          type: string
        usingProjectLogo:
          type: boolean
          description: True when the preview used the per-project logo override.
      required:
        - html
        - subject
        - usingProjectLogo
    ProjectOrderResponse:
      type: object
      properties:
        projectIds:
          type: array
          items:
            type: string
          description: Caller-visible project ids in their persisted order after the reorder commit.
      required:
        - projectIds
      description: Confirmation of the new project order. Mirrors the visibility-filtered slice the caller would see from GET /api/projects.
    ProjectOrderErrorResponse:
      type: object
      properties:
        error:
          type: string
      required:
        - error
      description: Error envelope returned when the reorder payload is malformed or references projects the caller cannot see.
    ProjectDetail:
      type: object
      properties:
        id:
          type: string
          description: Project slug — unique within the install.
        name:
          type: string
          description: Human-readable project name.
        cwd:
          type: string
          description: Absolute path to the project working directory on disk.
        githubRepo:
          type: string
          nullable: true
          description: GitHub repo in `owner/repo` form (e.g. `Speakman-ai/agent-hub`). Empty/null when the project has no GitHub remote.
      required:
        - id
        - name
      additionalProperties:
        nullable: true
      description: Project detail response. Includes the full Project shape stored in `projects.json`. Additional fields beyond the documented ones (agents, githubWorkflow, prEnv, …) flow through via passthrough — schematise as that legacy surface migrates onto the schema-first contract.
    ProjectDetailErrorResponse:
      type: object
      properties:
        error:
          type: string
      required:
        - error
      description: Error envelope returned when the project slug is unknown.
    ProjectAnalyzeResponse:
      type: object
      properties:
        analyzeId:
          type: string
          description: Opaque id used to correlate WebSocket analyze-progress / analyze-complete / analyze-error events.
      required:
        - analyzeId
      description: Analysis job accepted.
    ProjectAnalyzeErrorResponse:
      type: object
      properties:
        error:
          type: string
        code:
          type: string
        acceptedEngines:
          type: array
          items:
            type: string
            enum:
              - claude-code
              - cursor-agent
              - codex-cli
            description: Agent CLI engine to use for project analysis. Gemini is excluded because it is reserved for embeddings/RAG.
        acceptedModels:
          anyOf:
            - type: array
              items:
                type: string
            - type: object
              additionalProperties:
                type: array
                items:
                  type: string
        availability:
          type: object
          additionalProperties:
            type: object
            properties: {}
            additionalProperties:
              nullable: true
      required:
        - error
      description: Error envelope returned when the cwd, selected engine/model, or engine setup is invalid.
    ProjectOnboardErrorResponse:
      type: object
      properties:
        error:
          type: string
        message:
          type: string
      required:
        - error
      description: Error envelope returned when onboarding validation or setup fails.
    UserProjectSettings:
      type: object
      properties:
        projectId:
          type: string
          description: Project slug.
        defaultFinalizeAutomation:
          type: string
          nullable: true
          enum:
            - manual
            - review
            - push
            - merge
            - null
          description: The requesting user's default Finalize automation level for new ad-hoc sessions in this project, or null when they have set no preference (the session falls back to the global default `manual`).
      required:
        - projectId
        - defaultFinalizeAutomation
      description: Per-user, project-scoped settings for the authenticated caller. Scoped strictly to the requesting user.
    UserProjectSettingsErrorResponse:
      type: object
      properties:
        error:
          type: string
      required:
        - error
      description: "Error envelope: 404 when the project is missing or not visible to the caller; 400 when the body is malformed."
    SetupStatusResponse:
      type: object
      properties:
        firstRun:
          type: boolean
          description: True when no projects exist yet — the client may open the first-project flow after onboarding.
        authConfigured:
          type: boolean
          description: True once an Agent Hub Owner record exists (auth.json / users table).
        onboardingComplete:
          type: boolean
          description: True once the interactive SetupWizard has finished. Authoritative gate for resuming an interrupted first-run (e.g. password-manager kickout after Owner creation). Legacy installs without the flag treat authConfigured as complete.
        canCompleteOnboarding:
          type: boolean
          description: Whether THIS caller may finish instance onboarding (POST /api/setup/complete). Resolved server-side from the caller's CURRENT org-membership role (same gate the complete endpoint enforces) so a promotion/demotion after login cannot strand them. The client keys the Owner-only wizard ending off this rather than the login-cached role.
        hasAnyAiCredentials:
          type: boolean
          description: True when the requesting user has at least one working engine credential.
        engineAuth:
          type: object
          properties:
            claude-code:
              type: boolean
            cursor-agent:
              type: boolean
            codex-cli:
              type: boolean
          required:
            - claude-code
            - cursor-agent
            - codex-cli
          description: Per-engine authentication state for the requesting user.
        engines:
          type: object
          properties:
            claude-code:
              type: object
              properties:
                available:
                  type: boolean
                  description: Whether the engine binary responded to `<bin> --version` within the probe timeout.
                authenticated:
                  type: boolean
                  description: Whether the requesting user has working credentials for this engine (per-account; no host fallback).
                path:
                  type: string
                  description: Resolved path to the engine binary.
              required:
                - available
                - authenticated
                - path
              description: Availability + auth state for a single agent engine.
            cursor-agent:
              type: object
              properties:
                available:
                  type: boolean
                  description: Whether the engine binary responded to `<bin> --version` within the probe timeout.
                authenticated:
                  type: boolean
                  description: Whether the requesting user has working credentials for this engine (per-account; no host fallback).
                path:
                  type: string
                  description: Resolved path to the engine binary.
              required:
                - available
                - authenticated
                - path
              description: Availability + auth state for a single agent engine.
            codex-cli:
              type: object
              properties:
                available:
                  type: boolean
                  description: Whether the engine binary responded to `<bin> --version` within the probe timeout.
                authenticated:
                  type: boolean
                  description: Whether the requesting user has working credentials for this engine (per-account; no host fallback).
                path:
                  type: string
                  description: Resolved path to the engine binary.
              required:
                - available
                - authenticated
                - path
              description: Availability + auth state for a single agent engine.
            grok-cli:
              type: object
              properties:
                available:
                  type: boolean
                  description: Whether the engine binary responded to `<bin> --version` within the probe timeout.
                authenticated:
                  type: boolean
                  description: Whether the requesting user has working credentials for this engine (per-account; no host fallback).
                path:
                  type: string
                  description: Resolved path to the engine binary.
              required:
                - available
                - authenticated
                - path
              description: Availability + auth state for a single agent engine.
          required:
            - claude-code
            - cursor-agent
            - codex-cli
            - grok-cli
          description: Per-engine binary availability + auth + resolved path.
        dataDir:
          type: string
          description: Resolved Agent Hub data directory.
        projectsDir:
          type: string
          description: Resolved per-project data root.
      required:
        - firstRun
        - authConfigured
        - onboardingComplete
        - canCompleteOnboarding
        - hasAnyAiCredentials
        - engineAuth
        - engines
        - dataDir
        - projectsDir
      description: Boot-time setup + engine status. Hit on every app load to decide whether to show the setup wizard and which engines are usable.
    ProjectMember:
      type: object
      properties:
        userId:
          type: string
          description: Assigned user id.
        username:
          type: string
          description: Assigned user’s username.
        addedBy:
          type: string
          nullable: true
          description: User id of the Owner who made the assignment; null if that user was later deleted.
        createdAt:
          type: string
          description: When the assignment was made.
      required:
        - userId
        - username
        - addedBy
        - createdAt
      description: A user assigned to a project (a visibility-ACL row).
    ProjectMembersList:
      type: object
      properties:
        projectId:
          type: string
        ownerUserId:
          type: string
          nullable: true
          description: The project creator; always effectively a member (sees the project) even without an explicit row.
        visibility:
          type: string
          enum:
            - shared
            - private
          description: Project visibility. Shared projects with no members are org-visible; private projects with no members are owner-only.
        restricted:
          type: boolean
          description: True when the project has an active assignment ACL, even if it currently has zero assigned users. False means no explicit member ACL; combine with visibility to determine whether the project is org-visible (shared) or owner-only (private).
        members:
          type: array
          items:
            $ref: "#/components/schemas/ProjectMember"
      required:
        - projectId
        - ownerUserId
        - visibility
        - restricted
        - members
      description: The assignment ACL for a project.
    ProjectMemberAdded:
      type: object
      properties:
        projectId:
          type: string
        userId:
          type: string
        username:
          type: string
      required:
        - projectId
        - userId
        - username
      description: Confirmation that a user was assigned to a project.
    ProjectMemberRemoved:
      type: object
      properties:
        projectId:
          type: string
        userId:
          type: string
        removed:
          type: boolean
          enum:
            - true
      required:
        - projectId
        - userId
        - removed
      description: Confirmation that a user was unassigned from a project.
    ProjectMemberErrorResponse:
      type: object
      properties:
        error:
          type: string
      required:
        - error
      description: Error envelope for the project-member routes.
    ReleaseDigestRecipient:
      type: object
      properties:
        id:
          type: string
        projectId:
          type: string
        email:
          type: string
          format: email
        displayLabel:
          type: string
          nullable: true
        enabled:
          type: boolean
        createdBy:
          type: string
          nullable: true
        updatedBy:
          type: string
          nullable: true
        createdAt:
          type: string
        updatedAt:
          type: string
      required:
        - id
        - projectId
        - email
        - displayLabel
        - enabled
        - createdBy
        - updatedBy
        - createdAt
        - updatedAt
    ReleaseNotificationSettings:
      type: object
      properties:
        projectId:
          type: string
        releaseDigestPrompt:
          type: string
        defaultReleaseDigestPrompt:
          type: string
        isDefault:
          type: boolean
        promptMaxLength:
          type: integer
        factBoundedSystemTemplate:
          type: string
          description: Server-owned release digest generation boundary. The editable prompt is guidance only and cannot expand the allowed source facts.
        updatedBy:
          type: string
          nullable: true
        updatedAt:
          type: string
          nullable: true
        releaseDigestRecipients:
          type: array
          items:
            $ref: "#/components/schemas/ReleaseDigestRecipient"
          description: Admin-only release digest recipient list. Omitted for non-admin callers.
      required:
        - projectId
        - releaseDigestPrompt
        - defaultReleaseDigestPrompt
        - isDefault
        - promptMaxLength
        - factBoundedSystemTemplate
        - updatedBy
        - updatedAt
    ReleaseDigestRecipientsResponse:
      type: object
      properties:
        recipients:
          type: array
          items:
            $ref: "#/components/schemas/ReleaseDigestRecipient"
      required:
        - recipients
    ReleaseDigestRecipientDeleteResponse:
      type: object
      properties:
        ok:
          type: boolean
      required:
        - ok
    ReleaseNotificationSettingsErrorResponse:
      type: object
      properties:
        error:
          type: string
      required:
        - error
    UserFacingRelease:
      type: object
      properties:
        version:
          type: string
        tag:
          type: string
        publishedAt:
          type: string
          nullable: true
        url:
          type: string
          nullable: true
        summary:
          type: string
        sections:
          type: array
          items:
            type: object
            properties:
              title:
                type: string
              items:
                type: array
                items:
                  type: object
                  properties:
                    text:
                      type: string
                    hash:
                      type: string
                      nullable: true
                  required:
                    - text
                    - hash
            required:
              - title
              - items
      required:
        - version
        - tag
        - publishedAt
        - url
        - summary
        - sections
      description: Single release with user-facing changelog sections.
    ReleasesListResponse:
      type: object
      properties:
        repo:
          type: string
        source:
          type: string
          enum:
            - github
            - git
        currentVersion:
          type: string
        selected:
          allOf:
            - $ref: "#/components/schemas/UserFacingRelease"
            - nullable: true
        releases:
          type: array
          items:
            type: object
            properties:
              version:
                type: string
              tag:
                type: string
              publishedAt:
                type: string
                nullable: true
              url:
                type: string
                nullable: true
              summary:
                type: string
            required:
              - version
              - tag
              - publishedAt
              - url
              - summary
      required:
        - repo
        - source
        - currentVersion
        - selected
        - releases
      description: Release history for the Agent Hub repo.
    ReplayPlaylistErrorResponse:
      type: object
      properties:
        error:
          type: string
      required:
        - error
      description: Error envelope.
    ReplayPlaylist:
      type: object
      properties:
        id:
          type: string
        projectId:
          type: string
        name:
          type: string
        description:
          type: string
          nullable: true
        itemCount:
          type: integer
          minimum: 0
        extendedRetention:
          type: boolean
          description: Whether the whole playlist is flagged for extended retention.
        retainedUntil:
          type: string
          nullable: true
          description: Absolute SQLite-UTC instant the playlist (and its members) are retained until, or null when on the default window.
        retentionFlaggedAt:
          type: string
          nullable: true
        createdAt:
          type: string
        createdBy:
          type: string
          nullable: true
        updatedAt:
          type: string
      required:
        - id
        - projectId
        - name
        - description
        - itemCount
        - extendedRetention
        - retainedUntil
        - retentionFlaggedAt
        - createdAt
        - createdBy
        - updatedAt
      description: A named group of saved replay captures.
    ReplayPlaylistItem:
      type: object
      properties:
        replayId:
          type: string
        position:
          type: integer
          minimum: 0
        addedAt:
          type: string
        createdAt:
          type: string
        durationMs:
          type: integer
          minimum: 0
        eventCount:
          type: integer
          minimum: 0
        size:
          type: integer
          minimum: 0
        supportTicketId:
          type: string
          nullable: true
        cardId:
          type: string
          nullable: true
        retainedUntil:
          type: string
          nullable: true
        retentionFlaggedAt:
          type: string
          nullable: true
        eventsUrl:
          type: string
      required:
        - replayId
        - position
        - addedAt
        - createdAt
        - durationMs
        - eventCount
        - size
        - supportTicketId
        - cardId
        - retainedUntil
        - retentionFlaggedAt
        - eventsUrl
      description: A playlist membership joined to its capture metadata.
    ReplayPlaylistWithItems:
      type: object
      properties:
        id:
          type: string
        projectId:
          type: string
        name:
          type: string
        description:
          type: string
          nullable: true
        itemCount:
          type: integer
          minimum: 0
        extendedRetention:
          type: boolean
          description: Whether the whole playlist is flagged for extended retention.
        retainedUntil:
          type: string
          nullable: true
          description: Absolute SQLite-UTC instant the playlist (and its members) are retained until, or null when on the default window.
        retentionFlaggedAt:
          type: string
          nullable: true
        createdAt:
          type: string
        createdBy:
          type: string
          nullable: true
        updatedAt:
          type: string
        items:
          type: array
          items:
            $ref: "#/components/schemas/ReplayPlaylistItem"
      required:
        - id
        - projectId
        - name
        - description
        - itemCount
        - extendedRetention
        - retainedUntil
        - retentionFlaggedAt
        - createdAt
        - createdBy
        - updatedAt
        - items
      description: A playlist plus its ordered member captures.
    AddReplayPlaylistItemResponse:
      type: object
      properties:
        id:
          type: string
        projectId:
          type: string
        name:
          type: string
        description:
          type: string
          nullable: true
        itemCount:
          type: integer
          minimum: 0
        extendedRetention:
          type: boolean
          description: Whether the whole playlist is flagged for extended retention.
        retainedUntil:
          type: string
          nullable: true
          description: Absolute SQLite-UTC instant the playlist (and its members) are retained until, or null when on the default window.
        retentionFlaggedAt:
          type: string
          nullable: true
        createdAt:
          type: string
        createdBy:
          type: string
          nullable: true
        updatedAt:
          type: string
        added:
          type: boolean
          description: true when a new membership row was inserted; false when the capture was already a member (idempotent no-op).
        items:
          type: array
          items:
            $ref: "#/components/schemas/ReplayPlaylistItem"
      required:
        - id
        - projectId
        - name
        - description
        - itemCount
        - extendedRetention
        - retainedUntil
        - retentionFlaggedAt
        - createdAt
        - createdBy
        - updatedAt
        - added
        - items
      description: A playlist with its items plus whether the capture was newly added.
    CreateReplayPlaylistRequest:
      type: object
      properties:
        name:
          type: string
          description: Playlist name (1–200 chars, trimmed).
        description:
          type: string
          nullable: true
      required:
        - name
      description: Body for creating a playlist.
    UpdateReplayPlaylistRequest:
      type: object
      properties:
        name:
          type: string
          description: New name (1–200 chars).
        description:
          type: string
          nullable: true
      description: Partial update; omitted fields are unchanged.
    AddReplayPlaylistItemRequest:
      type: object
      properties:
        replayId:
          type: string
          description: Id of a saved capture attributed to this project.
      required:
        - replayId
      description: Body for adding a capture to a playlist.
    ReplayPlaylistRetentionRequest:
      type: object
      properties:
        extend:
          type: boolean
          description: true flags the playlist and every member capture for extended retention; false clears the playlist-level flag (members keep their own retained_until).
      required:
        - extend
      description: Body for flagging a playlist for extended retention.
    ReplaysDashboardErrorResponse:
      type: object
      properties:
        error:
          type: string
      required:
        - error
      description: Error envelope.
    ReplayListItem:
      type: object
      properties:
        id:
          type: string
        projectId:
          type: string
          nullable: true
        orphaned:
          type: boolean
          description: True when the capture is unattributed (project_id IS NULL).
        createdAt:
          type: string
        updatedAt:
          type: string
          description: Most-recent write (insert or append); falls back to createdAt.
        captureKind:
          type: string
          enum:
            - continuous
            - on-error
          description: "Derived capture tier: `continuous` (whole-session, interval/tail-flushed) vs `on-error` (record-on-error / manual bug-report)."
        live:
          type: boolean
          description: 'Best-effort "still streaming" signal: a continuous, unfinalized capture written to within the freshness window. Always false for on-error captures.'
        durationMs:
          type: integer
          minimum: 0
        eventCount:
          type: integer
          minimum: 0
        size:
          type: integer
          minimum: 0
          description: Compressed blob size (bytes).
        uncompressedSize:
          type: integer
          minimum: 0
        supportTicketId:
          type: string
          nullable: true
        cardId:
          type: string
          nullable: true
        pageUrl:
          type: string
          nullable: true
          description: Recorder-stamped page URL, if any.
        trigger:
          type: string
          nullable: true
        errorMessage:
          type: string
          nullable: true
        meta:
          type: object
          nullable: true
          additionalProperties:
            nullable: true
        eventsUrl:
          type: string
          description: Path to the paginated rrweb events endpoint.
        replayRef:
          type: string
          description: The /uploads/replay-<id>.json ref.
        ticket:
          type: object
          nullable: true
          properties:
            id:
              type: string
            subject:
              type: string
              nullable: true
            status:
              type: string
              nullable: true
          required:
            - id
            - subject
            - status
      required:
        - id
        - projectId
        - orphaned
        - createdAt
        - updatedAt
        - captureKind
        - live
        - durationMs
        - eventCount
        - size
        - uncompressedSize
        - supportTicketId
        - cardId
        - pageUrl
        - trigger
        - errorMessage
        - meta
        - eventsUrl
        - replayRef
        - ticket
      description: A session replay enriched with its linked support ticket (if any).
    ReplayListResponse:
      type: object
      properties:
        replays:
          type: array
          items:
            $ref: "#/components/schemas/ReplayListItem"
        total:
          type: integer
          minimum: 0
        limit:
          type: integer
          minimum: 0
          exclusiveMinimum: true
        offset:
          type: integer
          minimum: 0
        hasMore:
          type: boolean
        filter:
          type: string
          enum:
            - all
            - linked
            - unlinked
            - orphans
        kind:
          type: string
          enum:
            - all
            - continuous
            - on-error
          description: Active capture-kind facet. Default `all`.
        canViewOrphans:
          type: boolean
          description: Whether the caller may enumerate the global orphan (unattributed) set.
      required:
        - replays
        - total
        - limit
        - offset
        - hasMore
        - filter
        - kind
        - canViewOrphans
      description: A page of replays for the dashboard table.
    LinkReplayRequest:
      type: object
      properties:
        supportTicketId:
          type: string
          description: Id of a support ticket in the same project to attach to.
      required:
        - supportTicketId
      description: Body for attaching a replay to a support ticket.
    ReplayIngestErrorResponse:
      type: object
      properties:
        error:
          type: string
      required:
        - error
      description: Error envelope for replay ingest.
    ReplayIngestRequest:
      type: object
      properties:
        events:
          type: array
          items:
            type: object
            properties:
              type:
                type: number
                description: rrweb EventType.
              timestamp:
                type: number
                description: Event timestamp (ms epoch).
              data:
                nullable: true
            required:
              - type
              - timestamp
          minItems: 1
          description: Trailing rolling-buffer window of rrweb events.
        meta:
          type: object
          additionalProperties:
            nullable: true
          description: Optional context (trigger, url, ...).
      required:
        - events
      description: "A flushed session-replay buffer. May be sent as raw JSON or gzip-compressed bytes (`Content-Encoding: gzip` or a gzip-framed body)."
    SessionReplayMetadata:
      type: object
      properties:
        id:
          type: string
        projectId:
          type: string
          nullable: true
        createdAt:
          type: string
        durationMs:
          type: number
        eventCount:
          type: number
        size:
          type: number
          description: Compressed blob size in bytes.
        uncompressedSize:
          type: number
          description: Raw JSON length in bytes.
        supportTicketId:
          type: string
          nullable: true
        cardId:
          type: string
          nullable: true
        retainedUntil:
          type: string
          nullable: true
          description: "Extended-retention flag: absolute instant this capture is retained until (SQLite-UTC), or null when on the default window. When in the future the retention sweeper skips the row."
        retentionFlaggedAt:
          type: string
          nullable: true
          description: When the extended-retention flag was enabled (the 15-month clock starts here), or null.
        meta:
          type: object
          nullable: true
          additionalProperties:
            nullable: true
        eventsUrl:
          type: string
          description: Paginated events endpoint for this replay.
        defaultPageSize:
          type: number
      required:
        - id
        - projectId
        - createdAt
        - durationMs
        - eventCount
        - size
        - uncompressedSize
        - supportTicketId
        - cardId
        - retainedUntil
        - retentionFlaggedAt
        - meta
        - eventsUrl
        - defaultPageSize
      description: Session-replay metadata row.
    ReplayBatchIngestRequest:
      type: object
      properties:
        events:
          type: array
          items:
            type: object
            properties:
              type:
                type: number
                description: rrweb EventType.
              timestamp:
                type: number
                description: Event timestamp (ms epoch).
              data:
                nullable: true
            required:
              - type
              - timestamp
          minItems: 1
          description: One batch (chunk) of rrweb events.
        meta:
          type: object
          additionalProperties:
            nullable: true
          description: Optional context, honored on the first chunk only.
      required:
        - events
      description: "A chunk of a streamed replay. May be sent as raw JSON or gzip-compressed bytes (`Content-Encoding: gzip` or a gzip-framed body)."
    ResolvedReplayPolicy:
      type: object
      properties:
        sampleRate:
          type: number
          nullable: true
          description: Continuous-tier session sample rate in [0, 1], or null when the project has not set one (the recorder keeps its built-in default rather than treating unset as off).
        continuous:
          type: boolean
          description: Whether the continuous-capture tier is enabled for the project.
        segmented:
          type: boolean
          description: Whether the continuous tier streams view-scoped segments (append-only S3 objects + a SQLite manifest) instead of the monolithic blob. Only ever true when `continuous` is on. The recorder wires a segment flusher driven by a client-minted session/view manager when true. Because a segment append is O(1), the segmented path is not subject to the monolithic sub-minute flush-interval floor.
        maskAllEnforced:
          type: boolean
          description: When true the recorder must mask all text + inputs and the UI must not offer a relaxed masking mode. A strong default whenever continuous capture is on — true unless an Admin has explicitly opted the project out (project `replay.maskAllEnforced === false`).
        flushIntervalMs:
          type: number
          description: "Cadence (ms) the continuous recorder flushes at. Always present. Monolithic tier: the whole-blob flush interval, defaulting to 5 min and clamped to a >=60s floor (no sub-minute cadence on the O(n^2) monolithic append). Segmented tier: the per-segment duration bound the recorder rolls segments over on, defaulting to ~5s and floored only at 1s (append is O(1)). Capped at 1 hour either way."
        sessionSampleRate:
          type: number
          nullable: true
          description: "Datadog-style two-level sampling, level 1: fraction of sessions tracked in [0, 1], or null when unset (recorder keeps its built-in default)."
        sessionReplaySampleRate:
          type: number
          nullable: true
          description: "Two-level sampling, level 2: fraction OF the sampled sessions that also record a replay, in [0, 1], or null when unset. Nested under sessionSampleRate (a percentage of already-sampled sessions), not an independent gate."
        effectiveReplaySampleRate:
          type: number
          nullable: true
          description: "Precomputed effective replay probability: the product of the two nested rates (an unset level counts as 1). Null only when both nested rates are unset, so the recorder keeps its built-in default rather than reading null as off."
      required:
        - sampleRate
        - continuous
        - segmented
        - maskAllEnforced
        - flushIntervalMs
        - sessionSampleRate
        - sessionReplaySampleRate
        - effectiveReplaySampleRate
      description: Server-delivered per-project session-replay policy.
    ReplayTranscript:
      type: object
      properties:
        replayId:
          type: string
        transcript:
          type: string
          description: The rendered timeline, one event per line (`+MM:SS.s  kind  detail`). Redacted and bounded.
        stats:
          type: object
          properties:
            eventCount:
              type: number
            lineCount:
              type: number
            interactionCount:
              type: number
            errorCount:
              type: number
            networkFailureCount:
              type: number
            rageClickCount:
              type: number
            droppedEventCount:
              type: number
            durationMs:
              type: number
            truncated:
              type: boolean
            pageUrls:
              type: array
              items:
                type: string
            hasTelemetry:
              type: boolean
              description: False for captures recorded before browser console/network capture shipped — an empty error count then means "not recorded", not "no errors".
          required:
            - eventCount
            - lineCount
            - interactionCount
            - errorCount
            - networkFailureCount
            - rageClickCount
            - droppedEventCount
            - durationMs
            - truncated
            - pageUrls
            - hasTelemetry
          description: Counts derived while rendering the timeline.
        contextBlock:
          type: string
          description: "Prompt-ready block: safety preamble + trusted capture facts + the transcript inside BEGIN/END untrusted-data fences. Embed this verbatim when seeding an agent."
      required:
        - replayId
        - transcript
        - stats
        - contextBlock
      description: A human/agent-readable rendering of a stored rrweb capture.
    SessionSegmentManifest:
      type: object
      properties:
        sessionId:
          type: string
        storageLayout:
          type: string
          enum:
            - segmented
        projectId:
          type: string
          nullable: true
          description: Attribution shared by the session’s segments (null = anonymous).
        segmentCount:
          type: number
        durationMs:
          type: number
          description: Span from the earliest segment start to the latest segment end (ms).
        segments:
          type: array
          items:
            type: object
            properties:
              segmentId:
                type: string
              viewId:
                type: string
                description: Client-minted view id; segments never span views.
              indexInView:
                type: number
                description: 0-based position within the view.
              hasFullSnapshot:
                type: boolean
                description: True when the segment carries an rrweb full snapshot (type 2).
              startTs:
                type: number
                description: Earliest event timestamp in the segment (ms).
              endTs:
                type: number
                description: Latest event timestamp in the segment (ms).
              eventCount:
                type: number
              byteSize:
                type: number
                description: Gzipped object size in bytes.
              eventsUrl:
                type: string
                description: Per-segment events endpoint the player fetches to concat.
            required:
              - segmentId
              - viewId
              - indexInView
              - hasFullSnapshot
              - startTs
              - endTs
              - eventCount
              - byteSize
              - eventsUrl
            description: One playback-manifest segment pointer.
          description: Segments in playback order (chronological, then index within a view).
      required:
        - sessionId
        - storageLayout
        - projectId
        - segmentCount
        - durationMs
        - segments
      description: Segmented-capture playback manifest for a session.
    SegmentEvents:
      type: object
      properties:
        sessionId:
          type: string
        segmentId:
          type: string
        viewId:
          type: string
        indexInView:
          type: number
        hasFullSnapshot:
          type: boolean
        events:
          type: array
          items:
            type: object
            properties:
              type:
                type: number
              timestamp:
                type: number
              data:
                nullable: true
            required:
              - type
              - timestamp
          description: The decoded rrweb events for this one segment.
        eventCount:
          type: number
      required:
        - sessionId
        - segmentId
        - viewId
        - indexInView
        - hasFullSnapshot
        - events
        - eventCount
      description: One segment’s decoded rrweb events, for client-side concat.
    RumClientErrorResponse:
      type: object
      properties:
        error:
          type: string
      required:
        - error
      description: Error envelope for RUM ingest-client routes.
    RumClient:
      type: object
      properties:
        id:
          type: string
        projectId:
          type: string
        name:
          type: string
        prefix:
          type: string
          description: First 12 chars of the token (e.g. `rum_xxxx`).
        createdAt:
          type: string
        createdBy:
          type: string
          nullable: true
        lastUsedAt:
          type: string
          nullable: true
        revokedAt:
          type: string
          nullable: true
      required:
        - id
        - projectId
        - name
        - prefix
        - createdAt
        - createdBy
        - lastUsedAt
        - revokedAt
      description: Public metadata for a RUM ingest client. Never includes the token.
    MintedRumClient:
      allOf:
        - $ref: "#/components/schemas/RumClient"
        - type: object
          properties:
            token:
              type: string
              description: The plaintext `rum_`-prefixed token. Returned ONCE at mint; never retrievable again.
          required:
            - token
      description: A freshly minted RUM ingest client including its one-time token.
    RumClientListResponse:
      type: object
      properties:
        projectId:
          type: string
        clients:
          type: array
          items:
            $ref: "#/components/schemas/RumClient"
      required:
        - projectId
        - clients
      description: A project’s active (non-revoked) RUM ingest clients.
    RevokeRumClientResponse:
      type: object
      properties:
        revoked:
          type: boolean
          enum:
            - true
      required:
        - revoked
      description: RUM client revoked.
    RumSessionsErrorResponse:
      type: object
      properties:
        error:
          type: string
      required:
        - error
      description: Error envelope.
    RumSessionListItem:
      type: object
      properties:
        sessionId:
          type: string
          description: Client-minted session id.
        projectId:
          type: string
          nullable: true
        startedAt:
          type: integer
          nullable: true
          description: Earliest event timestamp across the session, epoch ms.
        endedAt:
          type: integer
          nullable: true
          description: Latest event timestamp across the session, epoch ms.
        timeSpent:
          type: integer
          minimum: 0
          description: Session duration, ms (endedAt - startedAt).
        viewCount:
          type: integer
          minimum: 0
        actionCount:
          type: integer
          minimum: 0
        errorCount:
          type: integer
          minimum: 0
        frustrationCount:
          type: integer
          minimum: 0
          description: Rolled-up rage/dead/error-click count.
        usrId:
          type: string
          nullable: true
        usrEmail:
          type: string
          nullable: true
        usrName:
          type: string
          nullable: true
        usrAttributes:
          type: object
          nullable: true
          additionalProperties:
            nullable: true
          description: Custom (non-standard) user attributes; null when none.
        deviceType:
          type: string
          nullable: true
        browser:
          type: string
          nullable: true
        os:
          type: string
          nullable: true
        geoCountry:
          type: string
          nullable: true
          description: ISO 3166-1 alpha-2 country from the ingest client IP.
        firstSeenAt:
          type: string
        updatedAt:
          type: string
      required:
        - sessionId
        - projectId
        - startedAt
        - endedAt
        - timeSpent
        - viewCount
        - actionCount
        - errorCount
        - frustrationCount
        - usrId
        - usrEmail
        - usrName
        - usrAttributes
        - deviceType
        - browser
        - os
        - geoCountry
        - firstSeenAt
        - updatedAt
      description: A session-grain rollup row for the RUM Session Explorer.
    RumSessionListResponse:
      type: object
      properties:
        sessions:
          type: array
          items:
            $ref: "#/components/schemas/RumSessionListItem"
        total:
          type: integer
          minimum: 0
        limit:
          type: integer
          minimum: 0
          exclusiveMinimum: true
        offset:
          type: integer
          minimum: 0
        hasMore:
          type: boolean
      required:
        - sessions
        - total
        - limit
        - offset
        - hasMore
      description: A page of RUM sessions for the Explorer table.
    RumWizardErrorResponse:
      type: object
      properties:
        error:
          type: string
        message:
          type: string
      required:
        - error
      description: Error envelope for RUM wizard routes.
    RumEntryCandidate:
      type: object
      properties:
        path:
          type: string
        kind:
          type: string
          enum:
            - root-layout
            - app-entry
            - html-entry
            - document
      required:
        - path
        - kind
    RumCspHit:
      type: object
      properties:
        path:
          type: string
        source:
          type: string
          enum:
            - meta
            - header
      required:
        - path
        - source
    RumInstrumentationPlan:
      type: object
      properties:
        alreadyInstrumented:
          type: boolean
        targetFile:
          type: string
          nullable: true
        injectionStyle:
          type: string
          nullable: true
          enum:
            - module-init
            - client-component
            - script-tag
            - null
        recommendedConnectSrc:
          type: string
        notes:
          type: array
          items:
            type: string
      required:
        - alreadyInstrumented
        - targetFile
        - injectionStyle
        - recommendedConnectSrc
        - notes
    RumSetupDraft:
      type: object
      properties:
        webRoot:
          type: string
          description: Directory relative to project.cwd where the browser app lives (`.` = repo root).
        framework:
          type: string
          enum:
            - next
            - nuxt
            - sveltekit
            - remix
            - astro
            - vue
            - angular
            - react
            - vanilla
            - unknown
        frameworkEvidence:
          type: array
          items:
            type: string
        packageManager:
          type: string
          nullable: true
          enum:
            - npm
            - pnpm
            - yarn
            - bun
            - null
        typescript:
          type: boolean
        entryCandidates:
          type: array
          items:
            $ref: "#/components/schemas/RumEntryCandidate"
        cspHits:
          type: array
          items:
            $ref: "#/components/schemas/RumCspHit"
        recorder:
          type: object
          properties:
            dependencyPresent:
              type: boolean
            initDetected:
              type: boolean
          required:
            - dependencyPresent
            - initDetected
        plan:
          $ref: "#/components/schemas/RumInstrumentationPlan"
        readme:
          nullable: true
      required:
        - webRoot
        - framework
        - frameworkEvidence
        - packageManager
        - typescript
        - entryCandidates
        - cspHits
        - recorder
        - plan
      description: "Server-precomputed RUM instrumentation scan: detected framework, injection-target candidates, existing CSP locations, recorder state, and a recommended instrumentation plan."
    RumSetupDraftResponse:
      type: object
      properties:
        projectId:
          type: string
        draft:
          $ref: "#/components/schemas/RumSetupDraft"
      required:
        - projectId
        - draft
      description: RUM instrumentation detection result for a project.
    RumWizardStartResponse:
      type: object
      properties:
        sessionId:
          type: string
        agentId:
          type: string
        draft:
          $ref: "#/components/schemas/RumSetupDraft"
        session:
          nullable: true
          description: Raw `sessions` row for the spawned wizard session.
      required:
        - sessionId
        - agentId
        - draft
      description: RUM recorder-injection wizard session spawned successfully.
    RumSetupApplyRequest:
      type: object
      properties:
        files:
          type: array
          items:
            type: string
          minItems: 1
          description: "Relative paths (inside the worktree) of the instrumentation files the wizard edited or created: the recorder target, any new client component, and each CSP file. Only these paths are staged + committed. Absolute paths and paths escaping the worktree are rejected."
        session_id:
          type: string
          description: Target session id whose worktree receives the commit. The wizard passes its own session id. When omitted the server picks the most-recent project session that has a worktree, or auto-provisions a dedicated `[RUM Config]` worktree on a fresh branch when none exists.
        message:
          type: string
          description: Optional commit message override. Defaults to a fixed RUM instrumentation title.
      required:
        - files
      description: Apply payload for the RUM instrumentation wizard.
    RumSetupApplyResponse:
      type: object
      properties:
        ok:
          type: boolean
          enum:
            - true
        files:
          type: array
          items:
            type: string
          description: Normalized relative paths that were committed.
        commit_sha:
          type: string
        branch:
          type: string
        session_id:
          type: string
      required:
        - ok
        - files
        - commit_sha
        - branch
        - session_id
      description: Instrumentation commit landed on the worktree branch.
    SecurityFinding:
      type: object
      properties:
        id:
          type: string
        project_id:
          type: string
        ecosystem:
          type: string
        package_name:
          type: string
        package_version:
          type: string
        advisory_id:
          type: string
        severity:
          type: string
          enum:
            - critical
            - high
            - medium
            - low
            - unknown
        summary:
          type: string
        fixed_version:
          type: string
          nullable: true
        advisory_url:
          type: string
        manifest_path:
          type: string
        status:
          type: string
          enum:
            - open
            - fixed
            - dismissed
        first_seen_at:
          type: number
        last_seen_at:
          type: number
        scan_ref:
          type: string
          nullable: true
      required:
        - id
        - project_id
        - ecosystem
        - package_name
        - package_version
        - advisory_id
        - severity
        - summary
        - fixed_version
        - advisory_url
        - manifest_path
        - status
        - first_seen_at
        - last_seen_at
        - scan_ref
    SkillEvalsBody:
      type: object
      properties:
        evals:
          type: array
          items:
            type: object
            properties:
              id:
                type: string
                description: Slug id (lowercase letters, digits, hyphens) — lets a single eval be re-run.
                example: happy-path
              prompt:
                type: string
                description: Realistic user prompt fed identically to the with-skill and baseline runs.
                example: How do I run only the server tests for the file I just edited?
              assertions:
                type: array
                items:
                  type: object
                  properties:
                    type:
                      type: string
                      enum:
                        - contains
                        - icontains
                        - not_contains
                        - regex
                      description: Check kind. `contains`/`not_contains` are case-sensitive substring; `icontains` is case-insensitive; `regex` is a JS regular-expression source matched against the output.
                    value:
                      type: string
                      example: cd server && npx vitest
                  required:
                    - type
                    - value
                  additionalProperties: false
                  description: One objective pass/fail check applied to a run output.
                description: Objective checks. Omit for a subjective eval that only gets a side-by-side diff (no auto pass/fail).
            required:
              - id
              - prompt
            additionalProperties: false
            description: 'A single eval: one prompt plus optional objective assertions. Unknown fields are rejected (a typo like "assertion" would otherwise silently drop the checks).'
        version:
          type: integer
          description: Optional, server-managed. The saved evals.json file carries `version` (currently 1); it is accepted here so the file can be round-tripped back verbatim, but clients normally send only `evals` and the server writes the canonical version.
      required:
        - evals
      description: The eval suite for a skill (2-3 prompts is typical; max 10).
    SkillEvalsList:
      type: object
      properties:
        evals:
          type: array
          items:
            type: object
            properties:
              id:
                type: string
                description: Slug id (lowercase letters, digits, hyphens) — lets a single eval be re-run.
                example: happy-path
              prompt:
                type: string
                description: Realistic user prompt fed identically to the with-skill and baseline runs.
                example: How do I run only the server tests for the file I just edited?
              assertions:
                type: array
                items:
                  type: object
                  properties:
                    type:
                      type: string
                      enum:
                        - contains
                        - icontains
                        - not_contains
                        - regex
                      description: Check kind. `contains`/`not_contains` are case-sensitive substring; `icontains` is case-insensitive; `regex` is a JS regular-expression source matched against the output.
                    value:
                      type: string
                      example: cd server && npx vitest
                  required:
                    - type
                    - value
                  additionalProperties: false
                  description: One objective pass/fail check applied to a run output.
                description: Objective checks. Omit for a subjective eval that only gets a side-by-side diff (no auto pass/fail).
            required:
              - id
              - prompt
            additionalProperties: false
            description: 'A single eval: one prompt plus optional objective assertions. Unknown fields are rejected (a typo like "assertion" would otherwise silently drop the checks).'
      required:
        - evals
      description: The skill eval suite.
    SkillEvalRunSummary:
      type: object
      properties:
        skillId:
          type: string
        total:
          type: number
        graded:
          type: number
          description: Count of evals with assertions (auto-graded).
        withSkillPassed:
          type: number
        baselinePassed:
          type: number
        improvedCount:
          type: number
          description: Objective evals that went baseline-fail → with-skill-pass.
        results:
          type: array
          items:
            type: object
            properties:
              evalId:
                type: string
              prompt:
                type: string
              graded:
                type: boolean
              withSkill:
                type: object
                properties:
                  output:
                    type: string
                  grade:
                    type: object
                    properties:
                      graded:
                        type: boolean
                      passed:
                        type: boolean
                      assertionResults:
                        type: array
                        items:
                          type: object
                          properties:
                            assertion:
                              type: object
                              properties:
                                type:
                                  type: string
                                  enum:
                                    - contains
                                    - icontains
                                    - not_contains
                                    - regex
                                  description: Check kind. `contains`/`not_contains` are case-sensitive substring; `icontains` is case-insensitive; `regex` is a JS regular-expression source matched against the output.
                                value:
                                  type: string
                                  example: cd server && npx vitest
                              required:
                                - type
                                - value
                              additionalProperties: false
                              description: One objective pass/fail check applied to a run output.
                            passed:
                              type: boolean
                            timedOut:
                              type: boolean
                              description: Set on a regex assertion aborted by the ReDoS time bound (treated as not matched).
                          required:
                            - assertion
                            - passed
                    required:
                      - graded
                      - passed
                      - assertionResults
                  error:
                    type: string
                required:
                  - output
                  - grade
              baseline:
                type: object
                properties:
                  output:
                    type: string
                  grade:
                    type: object
                    properties:
                      graded:
                        type: boolean
                      passed:
                        type: boolean
                      assertionResults:
                        type: array
                        items:
                          type: object
                          properties:
                            assertion:
                              type: object
                              properties:
                                type:
                                  type: string
                                  enum:
                                    - contains
                                    - icontains
                                    - not_contains
                                    - regex
                                  description: Check kind. `contains`/`not_contains` are case-sensitive substring; `icontains` is case-insensitive; `regex` is a JS regular-expression source matched against the output.
                                value:
                                  type: string
                                  example: cd server && npx vitest
                              required:
                                - type
                                - value
                              additionalProperties: false
                              description: One objective pass/fail check applied to a run output.
                            passed:
                              type: boolean
                            timedOut:
                              type: boolean
                              description: Set on a regex assertion aborted by the ReDoS time bound (treated as not matched).
                          required:
                            - assertion
                            - passed
                    required:
                      - graded
                      - passed
                      - assertionResults
                  error:
                    type: string
                required:
                  - output
                  - grade
              improved:
                type: boolean
                nullable: true
            required:
              - evalId
              - prompt
              - graded
              - withSkill
              - baseline
              - improved
        markdown:
          type: string
          description: Rendered side-by-side report the coach surfaces / saves.
        engine:
          type: string
        model:
          type: string
      required:
        - skillId
        - total
        - graded
        - withSkillPassed
        - baselinePassed
        - improvedCount
        - results
        - markdown
        - engine
        - model
      description: With-skill vs baseline run summary plus a Markdown report.
    SkillEvalRunBody:
      type: object
      properties:
        evals:
          type: array
          items:
            type: object
            properties:
              id:
                type: string
                description: Slug id (lowercase letters, digits, hyphens) — lets a single eval be re-run.
                example: happy-path
              prompt:
                type: string
                description: Realistic user prompt fed identically to the with-skill and baseline runs.
                example: How do I run only the server tests for the file I just edited?
              assertions:
                type: array
                items:
                  type: object
                  properties:
                    type:
                      type: string
                      enum:
                        - contains
                        - icontains
                        - not_contains
                        - regex
                      description: Check kind. `contains`/`not_contains` are case-sensitive substring; `icontains` is case-insensitive; `regex` is a JS regular-expression source matched against the output.
                    value:
                      type: string
                      example: cd server && npx vitest
                  required:
                    - type
                    - value
                  additionalProperties: false
                  description: One objective pass/fail check applied to a run output.
                description: Objective checks. Omit for a subjective eval that only gets a side-by-side diff (no auto pass/fail).
            required:
              - id
              - prompt
            additionalProperties: false
            description: 'A single eval: one prompt plus optional objective assertions. Unknown fields are rejected (a typo like "assertion" would otherwise silently drop the checks).'
          description: Inline eval suite to run instead of the saved evals.json — iterate on the suite without PUTting it first. Only skips saving the eval suite; the skill itself must already be saved (its SKILL.md is loaded from disk).
        evalIds:
          type: array
          items:
            type: string
          minItems: 1
          description: Restrict the run to these eval ids (single-prompt re-run). Must be non-empty when present — an empty array is a 400, not "run everything" (omit the field to run the whole suite). Every id must exist in the suite — a partial miss is a 400 listing the missing ids, never a silent run of the resolvable subset.
        engine:
          type: string
          enum:
            - claude-code
            - cursor-agent
            - codex-cli
          description: "Engine to run on. Must be an agent CLI — Gemini is excluded. An unknown value is rejected (400). When set, this is the engine the run uses: if it is unavailable the request 400s rather than silently falling back to another CLI (which would make results misleading). Omit to fall back across the agent CLIs."
        model:
          type: string
          description: Model override (non-empty).
        timeoutMs:
          type: integer
          description: Per-run timeout (ms). Must be an integer in [1000, 600000]; out of range is 400.
      additionalProperties: false
      description: Options for an eval run. All fields optional; unknown keys are rejected (400) so a typo cannot silently change the run.
    SkillEvalsError:
      type: object
      properties:
        error:
          type: string
        code:
          type: string
      required:
        - error
    CreateSkillBody:
      anyOf:
        - type: object
          properties:
            name:
              type: string
              description: "Skill slug + frontmatter name: lowercase letters, digits and hyphens. On PUT it must match the `:skillId` path segment (rename is not supported)."
              example: jira-triage
            description:
              type: string
              description: Trigger text — the single most important field. State WHAT the skill does and WHEN to use it.
              example: Triage Jira issues. TRIGGER when the user mentions Jira tickets or sprint triage.
            content:
              type: string
              description: Raw SKILL.md text (frontmatter + body). Its parsed frontmatter supplies any structured field omitted from the body; explicit fields win. Lets a minimal editor post a single textarea. The frontmatter MUST still yield a valid `name` (slug) and `description`. Frontmatter keys the server does not manage (anything other than name/description/category/version/credentials/keep-coding-instructions) are preserved verbatim, so fetching the raw file and PUTting it back is lossless.
            category:
              type: string
              enum:
                - general
                - integration
                - platform
                - workflow
                - automation
                - tooling
                - data
                - communication
              description: Skill category. Defaults to `general`.
            version:
              type: string
              description: Optional semver-ish version string.
            body:
              type: string
              description: Markdown body of SKILL.md (no YAML frontmatter block).
            keepCodingInstructions:
              type: boolean
              description: "Emit `keep-coding-instructions: true` in the frontmatter."
            credentials:
              type: array
              items:
                type: object
                properties:
                  name:
                    type: string
                    description: POSIX env var name the credential is injected as.
                    example: EXAMPLE_API_KEY
                  label:
                    type: string
                  description:
                    type: string
                  required:
                    type: boolean
                  type:
                    type: string
                    enum:
                      - string
                      - secret
                      - file
                      - json
                  docs_url:
                    type: string
                required:
                  - name
                description: A single declarative credential the skill needs at spawn time.
              description: Optional declarative credentials injected at spawn time.
          required:
            - name
            - description
          title: StructuredSkillWrite
          description: "Structured form: `name` and `description` supplied explicitly."
        - type: object
          properties:
            content:
              type: string
              description: Raw SKILL.md text (frontmatter + body). Its parsed frontmatter supplies any structured field omitted from the body; explicit fields win. Lets a minimal editor post a single textarea. The frontmatter MUST still yield a valid `name` (slug) and `description`. Frontmatter keys the server does not manage (anything other than name/description/category/version/credentials/keep-coding-instructions) are preserved verbatim, so fetching the raw file and PUTting it back is lossless.
            name:
              type: string
              description: "Skill slug + frontmatter name: lowercase letters, digits and hyphens. On PUT it must match the `:skillId` path segment (rename is not supported)."
              example: jira-triage
            description:
              type: string
              description: Trigger text — the single most important field. State WHAT the skill does and WHEN to use it.
              example: Triage Jira issues. TRIGGER when the user mentions Jira tickets or sprint triage.
            category:
              type: string
              enum:
                - general
                - integration
                - platform
                - workflow
                - automation
                - tooling
                - data
                - communication
              description: Skill category. Defaults to `general`.
            version:
              type: string
              description: Optional semver-ish version string.
            body:
              type: string
              description: Markdown body of SKILL.md (no YAML frontmatter block).
            keepCodingInstructions:
              type: boolean
              description: "Emit `keep-coding-instructions: true` in the frontmatter."
            credentials:
              type: array
              items:
                type: object
                properties:
                  name:
                    type: string
                    description: POSIX env var name the credential is injected as.
                    example: EXAMPLE_API_KEY
                  label:
                    type: string
                  description:
                    type: string
                  required:
                    type: boolean
                  type:
                    type: string
                    enum:
                      - string
                      - secret
                      - file
                      - json
                  docs_url:
                    type: string
                required:
                  - name
                description: A single declarative credential the skill needs at spawn time.
              description: Optional declarative credentials injected at spawn time.
          required:
            - content
          title: RawContentSkillWrite
          description: "Raw form: a `content` SKILL.md string whose frontmatter carries name/description."
      description: Create a project skill. Provide EITHER structured fields (`name` + `description` required) OR a raw `content` SKILL.md whose frontmatter yields a valid `name` + `description`. Either way the server validates the resolved frontmatter and returns 400 on a missing/invalid `name` or `description`.
    UpdateSkillBody:
      anyOf:
        - type: object
          properties:
            description:
              type: string
              description: Trigger text — the single most important field. State WHAT the skill does and WHEN to use it.
              example: Triage Jira issues. TRIGGER when the user mentions Jira tickets or sprint triage.
            name:
              type: string
              description: "Skill slug + frontmatter name: lowercase letters, digits and hyphens. On PUT it must match the `:skillId` path segment (rename is not supported)."
              example: jira-triage
            content:
              type: string
              description: Raw SKILL.md text (frontmatter + body). Its parsed frontmatter supplies any structured field omitted from the body; explicit fields win. Lets a minimal editor post a single textarea. The frontmatter MUST still yield a valid `name` (slug) and `description`. Frontmatter keys the server does not manage (anything other than name/description/category/version/credentials/keep-coding-instructions) are preserved verbatim, so fetching the raw file and PUTting it back is lossless.
            category:
              type: string
              enum:
                - general
                - integration
                - platform
                - workflow
                - automation
                - tooling
                - data
                - communication
              description: Skill category. Defaults to `general`.
            version:
              type: string
              description: Optional semver-ish version string.
            body:
              type: string
              description: Markdown body of SKILL.md (no YAML frontmatter block).
            keepCodingInstructions:
              type: boolean
              description: "Emit `keep-coding-instructions: true` in the frontmatter."
            credentials:
              type: array
              items:
                type: object
                properties:
                  name:
                    type: string
                    description: POSIX env var name the credential is injected as.
                    example: EXAMPLE_API_KEY
                  label:
                    type: string
                  description:
                    type: string
                  required:
                    type: boolean
                  type:
                    type: string
                    enum:
                      - string
                      - secret
                      - file
                      - json
                  docs_url:
                    type: string
                required:
                  - name
                description: A single declarative credential the skill needs at spawn time.
              description: Optional declarative credentials injected at spawn time.
          required:
            - description
          title: StructuredSkillUpdate
          description: "Structured form: `description` required; `name` optional (resolved from the path, must match when given)."
        - type: object
          properties:
            content:
              type: string
              description: Raw SKILL.md text (frontmatter + body). Its parsed frontmatter supplies any structured field omitted from the body; explicit fields win. Lets a minimal editor post a single textarea. The frontmatter MUST still yield a valid `name` (slug) and `description`. Frontmatter keys the server does not manage (anything other than name/description/category/version/credentials/keep-coding-instructions) are preserved verbatim, so fetching the raw file and PUTting it back is lossless.
            name:
              type: string
              description: "Skill slug + frontmatter name: lowercase letters, digits and hyphens. On PUT it must match the `:skillId` path segment (rename is not supported)."
              example: jira-triage
            description:
              type: string
              description: Trigger text — the single most important field. State WHAT the skill does and WHEN to use it.
              example: Triage Jira issues. TRIGGER when the user mentions Jira tickets or sprint triage.
            category:
              type: string
              enum:
                - general
                - integration
                - platform
                - workflow
                - automation
                - tooling
                - data
                - communication
              description: Skill category. Defaults to `general`.
            version:
              type: string
              description: Optional semver-ish version string.
            body:
              type: string
              description: Markdown body of SKILL.md (no YAML frontmatter block).
            keepCodingInstructions:
              type: boolean
              description: "Emit `keep-coding-instructions: true` in the frontmatter."
            credentials:
              type: array
              items:
                type: object
                properties:
                  name:
                    type: string
                    description: POSIX env var name the credential is injected as.
                    example: EXAMPLE_API_KEY
                  label:
                    type: string
                  description:
                    type: string
                  required:
                    type: boolean
                  type:
                    type: string
                    enum:
                      - string
                      - secret
                      - file
                      - json
                  docs_url:
                    type: string
                required:
                  - name
                description: A single declarative credential the skill needs at spawn time.
              description: Optional declarative credentials injected at spawn time.
          required:
            - content
          title: RawContentSkillWrite
          description: "Raw form: a `content` SKILL.md string whose frontmatter carries name/description."
      description: Update a project skill. Provide EITHER structured fields (`description` required; `name` optional, resolved from the path) OR a raw `content` SKILL.md. The resolved frontmatter is validated and returns 400 on a missing/invalid `description` (or a `name` that disagrees with the path).
    ProjectUpdateSkillBody:
      anyOf:
        - type: object
          properties:
            description:
              type: string
              description: Trigger text — the single most important field. State WHAT the skill does and WHEN to use it.
              example: Triage Jira issues. TRIGGER when the user mentions Jira tickets or sprint triage.
            name:
              type: string
              description: "Skill slug + frontmatter name: lowercase letters, digits and hyphens. On PUT it must match the `:skillId` path segment (rename is not supported)."
              example: jira-triage
            content:
              type: string
              description: Raw SKILL.md text (frontmatter + body). Its parsed frontmatter supplies any structured field omitted from the body; explicit fields win. Lets a minimal editor post a single textarea. The frontmatter MUST still yield a valid `name` (slug) and `description`. Frontmatter keys the server does not manage (anything other than name/description/category/version/credentials/keep-coding-instructions) are preserved verbatim, so fetching the raw file and PUTting it back is lossless.
            category:
              type: string
              enum:
                - general
                - integration
                - platform
                - workflow
                - automation
                - tooling
                - data
                - communication
              description: Skill category. Defaults to `general`.
            version:
              type: string
              description: Optional semver-ish version string.
            body:
              type: string
              description: Markdown body of SKILL.md (no YAML frontmatter block).
            keepCodingInstructions:
              type: boolean
              description: "Emit `keep-coding-instructions: true` in the frontmatter."
            credentials:
              type: array
              items:
                type: object
                properties:
                  name:
                    type: string
                    description: POSIX env var name the credential is injected as.
                    example: EXAMPLE_API_KEY
                  label:
                    type: string
                  description:
                    type: string
                  required:
                    type: boolean
                  type:
                    type: string
                    enum:
                      - string
                      - secret
                      - file
                      - json
                  docs_url:
                    type: string
                required:
                  - name
                description: A single declarative credential the skill needs at spawn time.
              description: Optional declarative credentials injected at spawn time.
            expectedCredentials:
              type: array
              items:
                type: object
                properties:
                  name:
                    type: string
                    description: POSIX env var name the credential is injected as.
                    example: EXAMPLE_API_KEY
                  label:
                    type: string
                  description:
                    type: string
                  required:
                    type: boolean
                  type:
                    type: string
                    enum:
                      - string
                      - secret
                      - file
                      - json
                  docs_url:
                    type: string
                required:
                  - name
                description: A single declarative credential the skill needs at spawn time.
              description: Optimistic concurrency precondition for project skill updates. The write returns 409 unless the current credential declaration exactly matches this value.
          required:
            - description
          title: StructuredSkillUpdate
          description: "Structured form: `description` required; `name` optional (resolved from the path, must match when given)."
        - type: object
          properties:
            content:
              type: string
              description: Raw SKILL.md text (frontmatter + body). Its parsed frontmatter supplies any structured field omitted from the body; explicit fields win. Lets a minimal editor post a single textarea. The frontmatter MUST still yield a valid `name` (slug) and `description`. Frontmatter keys the server does not manage (anything other than name/description/category/version/credentials/keep-coding-instructions) are preserved verbatim, so fetching the raw file and PUTting it back is lossless.
            name:
              type: string
              description: "Skill slug + frontmatter name: lowercase letters, digits and hyphens. On PUT it must match the `:skillId` path segment (rename is not supported)."
              example: jira-triage
            description:
              type: string
              description: Trigger text — the single most important field. State WHAT the skill does and WHEN to use it.
              example: Triage Jira issues. TRIGGER when the user mentions Jira tickets or sprint triage.
            category:
              type: string
              enum:
                - general
                - integration
                - platform
                - workflow
                - automation
                - tooling
                - data
                - communication
              description: Skill category. Defaults to `general`.
            version:
              type: string
              description: Optional semver-ish version string.
            body:
              type: string
              description: Markdown body of SKILL.md (no YAML frontmatter block).
            keepCodingInstructions:
              type: boolean
              description: "Emit `keep-coding-instructions: true` in the frontmatter."
            credentials:
              type: array
              items:
                type: object
                properties:
                  name:
                    type: string
                    description: POSIX env var name the credential is injected as.
                    example: EXAMPLE_API_KEY
                  label:
                    type: string
                  description:
                    type: string
                  required:
                    type: boolean
                  type:
                    type: string
                    enum:
                      - string
                      - secret
                      - file
                      - json
                  docs_url:
                    type: string
                required:
                  - name
                description: A single declarative credential the skill needs at spawn time.
              description: Optional declarative credentials injected at spawn time.
            expectedCredentials:
              type: array
              items:
                type: object
                properties:
                  name:
                    type: string
                    description: POSIX env var name the credential is injected as.
                    example: EXAMPLE_API_KEY
                  label:
                    type: string
                  description:
                    type: string
                  required:
                    type: boolean
                  type:
                    type: string
                    enum:
                      - string
                      - secret
                      - file
                      - json
                  docs_url:
                    type: string
                required:
                  - name
                description: A single declarative credential the skill needs at spawn time.
              description: Optimistic concurrency precondition for project skill updates. The write returns 409 unless the current credential declaration exactly matches this value.
          required:
            - content
          title: RawContentSkillWrite
          description: "Raw form: a `content` SKILL.md string whose frontmatter carries name/description."
      description: Update a project skill, optionally requiring its current credential declaration to match `expectedCredentials` before the write is applied.
    SkillWriteResult:
      type: object
      properties:
        id:
          type: string
        name:
          type: string
        description:
          type: string
        path:
          type: string
          description: Absolute path to the skill directory on disk.
      required:
        - id
        - name
        - description
        - path
      description: The created / updated project skill.
    SkillWriteError:
      type: object
      properties:
        error:
          type: string
      required:
        - error
      description: Error envelope.
    ProjectSkillListItem:
      type: object
      properties:
        id:
          type: string
        name:
          type: string
        description:
          type: string
        category:
          type: string
        credentials:
          type: array
          items:
            type: object
            properties:
              name:
                type: string
                description: POSIX env var name the credential is injected as.
                example: EXAMPLE_API_KEY
              label:
                type: string
              description:
                type: string
              required:
                type: boolean
              type:
                type: string
                enum:
                  - string
                  - secret
                  - file
                  - json
              docs_url:
                type: string
            required:
              - name
            description: A single declarative credential the skill needs at spawn time.
        source:
          type: string
          enum:
            - project
            - global
            - default
      required:
        - id
        - name
        - description
        - source
      description: A project-authored skill in `<dataDir>/project-skills/<projectId>`.
    GlobalSkillListItem:
      type: object
      properties:
        id:
          type: string
        name:
          type: string
        description:
          type: string
        path:
          type: string
        source:
          type: string
          enum:
            - global
            - default
      required:
        - id
        - name
        - description
        - path
        - source
      description: A built-in or user-authored global (shared) skill.
    GlobalSkillDetail:
      type: object
      properties:
        id:
          type: string
        name:
          type: string
        description:
          type: string
        content:
          type: string
          description: Raw SKILL.md text (frontmatter + body).
        path:
          type: string
        credentials:
          type: array
          items:
            type: object
            properties:
              name:
                type: string
                description: POSIX env var name the credential is injected as.
                example: EXAMPLE_API_KEY
              label:
                type: string
              description:
                type: string
              required:
                type: boolean
              type:
                type: string
                enum:
                  - string
                  - secret
                  - file
                  - json
              docs_url:
                type: string
            required:
              - name
            description: A single declarative credential the skill needs at spawn time.
        source:
          type: string
          enum:
            - global
      required:
        - id
        - name
        - description
        - content
        - path
        - credentials
        - source
      description: A single global skill with its raw SKILL.md content.
    GlobalSkillWriteResult:
      type: object
      properties:
        id:
          type: string
        name:
          type: string
        description:
          type: string
        path:
          type: string
          description: Absolute path to the skill directory on disk.
        source:
          type: string
          enum:
            - global
          description: Always `global` — lets clients pick the global delete path.
      required:
        - id
        - name
        - description
        - path
        - source
      description: The created / updated global (shared) skill.
    ProjectSkillDetail:
      type: object
      properties:
        id:
          type: string
        name:
          type: string
        description:
          type: string
        content:
          type: string
          description: Raw SKILL.md text (frontmatter + body).
        path:
          type: string
        credentials:
          type: array
          items:
            type: object
            properties:
              name:
                type: string
                description: POSIX env var name the credential is injected as.
                example: EXAMPLE_API_KEY
              label:
                type: string
              description:
                type: string
              required:
                type: boolean
              type:
                type: string
                enum:
                  - string
                  - secret
                  - file
                  - json
              docs_url:
                type: string
            required:
              - name
            description: A single declarative credential the skill needs at spawn time.
        source:
          type: string
          enum:
            - project
      required:
        - id
        - name
        - description
        - content
        - path
        - credentials
        - source
      description: A single project-authored skill with its raw SKILL.md content.
    SkillImprovementRecord:
      type: object
      properties:
        id:
          type: string
          description: Improvement record id (UUID).
        skillId:
          type: string
          description: Skill slug the lesson targets.
        skillName:
          type: string
          description: Display name of the skill (list responses only).
        source:
          type: string
          enum:
            - project
            - global
          description: Tier the skill resolved to when the lesson was captured.
        entry:
          type: string
          description: The suggested lesson text (max 1200 chars, newlines collapsed). Untrusted agent output — render as plain text.
        status:
          type: string
          enum:
            - pending
            - approved
            - rejected
        createdAt:
          type: string
          description: ISO timestamp the lesson was suggested.
        sessionId:
          type: string
          nullable: true
          description: Originating session id — provenance for reviewers.
        agentId:
          type: string
          nullable: true
          description: Agent that suggested the lesson.
        reviewedAt:
          type: string
          nullable: true
        rejectReason:
          type: string
          nullable: true
      required:
        - id
        - skillId
        - source
        - entry
        - status
        - createdAt
      description: One suggested skill lesson awaiting (or past) review. Approval appends a dated bullet to the skill's `## Learned Lessons` section.
    ReviewSkillImprovementResult:
      type: object
      properties:
        ok:
          type: boolean
          enum:
            - true
        improvement:
          $ref: "#/components/schemas/SkillImprovementRecord"
      required:
        - ok
        - improvement
      description: The reviewed record with its final status.
    SupportTicketVoteAggregate:
      type: object
      properties:
        score:
          type: integer
          description: SUM(value) across all votes.
        upvotes:
          type: integer
          minimum: 0
        downvotes:
          type: integer
          minimum: 0
        myVote:
          anyOf:
            - type: number
              enum:
                - 1
            - type: number
              enum:
                - -1
            - nullable: true
          description: Current vote for this voterKey, or null when they have not voted.
      required:
        - score
        - upvotes
        - downvotes
        - myVote
      description: Aggregate score for a feature-request ticket, plus the caller’s vote.
    SupportTicketReleaseNotification:
      type: object
      properties:
        id:
          type: string
        deployment_id:
          type: string
        release_item_id:
          type: string
          nullable: true
        support_ticket_id:
          type: string
          nullable: true
        notification_type:
          type: string
          enum:
            - ticket_release
            - release_digest
        recipient_type:
          type: string
          enum:
            - reporter
            - release_digest
        subject:
          type: string
        status:
          type: string
          enum:
            - pending
            - sending
            - sent
            - error
        attempts:
          type: integer
        sent_at:
          type: string
          nullable: true
        next_attempt_at:
          type: string
          nullable: true
        error_summary:
          type: string
          nullable: true
        can_retry:
          type: boolean
        created_at:
          type: string
        updated_at:
          type: string
      required:
        - id
        - deployment_id
        - release_item_id
        - support_ticket_id
        - notification_type
        - recipient_type
        - subject
        - status
        - attempts
        - sent_at
        - next_attempt_at
        - error_summary
        - can_retry
        - created_at
        - updated_at
    SupportTicketConvertedCard:
      type: object
      properties:
        id:
          type: string
        short_id:
          type: integer
          nullable: true
        title:
          type: string
        column_name:
          type: string
          nullable: true
      required:
        - id
        - short_id
        - title
        - column_name
      description: Board-facing identity of the kanban card a ticket was converted into or linked to, so clients can name and link to it instead of showing a bare card id.
    SupportTicketErrorResponse:
      type: object
      properties:
        error:
          type: string
      required:
        - error
      description: Error envelope for support-ticket routes.
    SupportTicket:
      type: object
      properties:
        id:
          type: string
        project_id:
          type: string
        type:
          type: string
          enum:
            - bug
            - question
            - feature_request
            - incident
            - other
        severity:
          type: string
          enum:
            - critical
            - high
            - medium
            - low
        status:
          type: string
          enum:
            - new
            - investigating
            - converted
            - closed
            - duplicate
            - wont_do
        subject:
          type: string
        body:
          type: string
        reporter:
          type: string
          nullable: true
        reporter_email:
          type: string
          nullable: true
          description: Protected reporter contact email. Returned in full to Admin/Owner/local callers; masked for lower-privilege responses.
        reporter_email_masked:
          type: boolean
          description: True when reporter_email is present but masked for this response. False for privileged full reads or tickets without an email.
        ai_summary:
          type: string
          nullable: true
        ai_investigation:
          type: string
          nullable: true
        ai_investigated_at:
          type: string
          nullable: true
        replay_ref:
          type: string
          nullable: true
        screenshot_ref:
          type: string
          nullable: true
          description: Server-relative ref to an attached screenshot, or null.
        converted_card_id:
          type: string
          nullable: true
        converted_card:
          allOf:
            - $ref: "#/components/schemas/SupportTicketConvertedCard"
            - nullable: true
              description: Resolved card identity for converted_card_id. Null when the ticket is unconverted or the card has since been deleted.
        wont_do_reason:
          type: string
          nullable: true
          description: Operator reason the ticket was marked 'wont_do', or null otherwise.
        release_state:
          type: string
          nullable: true
          enum:
            - fixed_pending_release
            - released_to_prod
            - customer_notified
            - null
          description: Derived release-facing state. fixed_pending_release means a linked card reached Done, released_to_prod means a production deployment included the card/ticket, and customer_notified means the reporter notification has been sent.
        fixed_at:
          type: string
          nullable: true
          description: Timestamp when a linked kanban card first reached Done, or null.
        released_to_prod_at:
          type: string
          nullable: true
          description: Timestamp when a production deployment released this ticket, or null.
        release_deployment_id:
          type: string
          nullable: true
          description: Deployment id that first released this ticket to production, or null.
        customer_notified_at:
          type: string
          nullable: true
          description: Timestamp when a reporter notification was sent, or null.
        read_at:
          type: string
          nullable: true
          description: Timestamp the ticket was first read, or null when unread.
        release_notifications:
          type: array
          items:
            $ref: "#/components/schemas/SupportTicketReleaseNotification"
          description: Release notification outbox history for this ticket.
        created_at:
          type: string
        updated_at:
          type: string
      required:
        - id
        - project_id
        - type
        - severity
        - status
        - subject
        - body
        - reporter
        - reporter_email
        - reporter_email_masked
        - ai_summary
        - ai_investigation
        - ai_investigated_at
        - replay_ref
        - screenshot_ref
        - converted_card_id
        - converted_card
        - wont_do_reason
        - release_state
        - fixed_at
        - released_to_prod_at
        - release_deployment_id
        - customer_notified_at
        - read_at
        - created_at
        - updated_at
      description: A single support ticket row.
    SupportTicketVotingTally:
      allOf:
        - $ref: "#/components/schemas/SupportTicketVoteAggregate"
        - type: object
          properties:
            comment_count:
              type: integer
              minimum: 0
              description: Count of non-hidden comments on this ticket.
          required:
            - comment_count
      description: Vote aggregate plus non-hidden comment_count for one feature-request ticket on the voting feed.
    SupportTicketVotingItem:
      allOf:
        - $ref: "#/components/schemas/SupportTicket"
        - type: object
          properties:
            voting:
              $ref: "#/components/schemas/SupportTicketVotingTally"
          required:
            - voting
      description: "Hub-facing voting item: a feature-request support ticket with vote tallies and comment_count. Returned to interactive Hub operators (JWT / local session). Severity, status, and release badges are unchanged from SupportTicket."
    SupportTicketVotingItemExternal:
      type: object
      properties:
        id:
          type: string
          description: Ticket id — required to cast subsequent vote/comment calls.
        type:
          type: string
          enum:
            - bug
            - question
            - feature_request
            - incident
            - other
        severity:
          type: string
          enum:
            - critical
            - high
            - medium
            - low
        status:
          type: string
          enum:
            - new
            - investigating
            - converted
            - closed
            - duplicate
            - wont_do
        subject:
          type: string
        body:
          type: string
        voting:
          $ref: "#/components/schemas/SupportTicketVotingTally"
      required:
        - id
        - type
        - severity
        - status
        - subject
        - body
        - voting
      description: "External-safe voting item returned to Survey-Tracker. Any API-key request qualifies — the global X-API-Key or a per-user `ahub_*` key — as opposed to an interactive Hub operator session. The shape is exactly the public contract: id + subject/body/type/severity/status + the voting tally (score/upvotes/downvotes/myVote/comment_count). No project_id or timestamps; every operator-only field is stripped by construction."
    RunSupportTicketInvestigationResponse:
      type: object
      properties:
        queued:
          type: boolean
          enum:
            - true
        engine:
          type: string
          enum:
            - claude-code
            - cursor-agent
            - codex-cli
            - grok-cli
        model:
          type: string
        ticket:
          $ref: "#/components/schemas/SupportTicket"
      required:
        - queued
        - engine
        - model
        - ticket
    ConvertSupportTicketResponse:
      type: object
      properties:
        card:
          $ref: "#/components/schemas/KanbanCard"
        ticket:
          allOf:
            - $ref: "#/components/schemas/SupportTicket"
            - description: The source ticket, now retained and flagged 'converted'.
        ticketId:
          type: string
          description: Id of the converted source ticket.
        converted:
          type: boolean
          enum:
            - true
          description: Always true — the source ticket is retained and flagged converted.
      required:
        - card
        - ticket
        - ticketId
        - converted
      description: The kanban card the ticket became, plus the retained source ticket (now converted).
    LinkSupportTicketToCardResponse:
      type: object
      properties:
        card:
          $ref: "#/components/schemas/KanbanCard"
        ticket:
          allOf:
            - $ref: "#/components/schemas/SupportTicket"
            - description: The source ticket, now retained and flagged 'converted', linked to the card.
        ticketId:
          type: string
          description: Id of the linked source ticket.
        linked:
          type: boolean
          enum:
            - true
          description: Always true — the ticket was linked to the existing card and flagged converted.
      required:
        - card
        - ticket
        - ticketId
        - linked
      description: The existing kanban card the ticket was linked to, plus the retained source ticket.
    SupportTicketComment:
      type: object
      properties:
        id:
          type: string
        support_ticket_id:
          type: string
        body:
          type: string
        display_name:
          type: string
          nullable: true
        source:
          type: string
          enum:
            - hub
            - external
          description: Where the comment was posted. Hub-auth responses always include this.
        hidden_at:
          type: string
          nullable: true
          description: Operator soft-delete timestamp. Present on Hub-auth responses; omitted from the external projection. Listed comments always have this null/absent.
        created_at:
          type: string
      required:
        - id
        - support_ticket_id
        - body
        - display_name
        - source
        - created_at
      description: Anonymous comment on a support ticket. No user id is stored. Operators hide via DELETE (sets hidden_at); lists skip hidden rows.
    SupportTicketOverviewErrorResponse:
      type: object
      properties:
        error:
          type: string
      required:
        - error
      description: Error envelope for the support-overview route.
    SupportTicketWithProject:
      allOf:
        - $ref: "#/components/schemas/SupportTicket"
        - type: object
          properties:
            project_name:
              type: string
              description: Display name of the ticket's project (falls back to its id).
          required:
            - project_name
      description: A support ticket enriched with its project name.
    SupportOverviewResponse:
      type: object
      properties:
        tickets:
          type: array
          items:
            $ref: "#/components/schemas/SupportTicketWithProject"
        projects:
          type: array
          items:
            type: object
            properties:
              id:
                type: string
              name:
                type: string
              count:
                type: integer
                minimum: 0
            required:
              - id
              - name
              - count
          description: Distinct projects that currently have support tickets, ordered by descending ticket count then name. Always the full unfiltered set so the client filter stays complete.
      required:
        - tickets
        - projects
      description: Cross-project support tickets plus project filter options.
    UploadResponse:
      type: object
      properties:
        id:
          type: string
        filename:
          type: string
        originalName:
          type: string
        contentType:
          type: string
        url:
          type: string
          description: Stable server-relative `/uploads/<filename>` ref.
      required:
        - id
        - filename
        - originalName
        - contentType
        - url
    UploadErrorResponse:
      type: object
      properties:
        error:
          type: string
      required:
        - error
    WikiPage:
      type: object
      properties:
        id:
          type: string
        project_id:
          type: string
        title:
          type: string
        slug:
          type: string
        content:
          type: string
        category:
          type: string
        updated_by:
          type: string
        created_at:
          type: string
        updated_at:
          type: string
      required:
        - id
        - project_id
        - title
        - slug
        - content
        - category
        - updated_by
        - created_at
        - updated_at
      description: A wiki page row (includes full content).
    WikiPageListItem:
      type: object
      properties:
        id:
          type: string
        project_id:
          type: string
        title:
          type: string
        slug:
          type: string
        category:
          type: string
        updated_by:
          type: string
        created_at:
          type: string
        updated_at:
          type: string
      required:
        - id
        - project_id
        - title
        - slug
        - category
        - updated_by
        - created_at
        - updated_at
      description: A wiki page list item (content omitted for payload size). Returned by `GET /wiki` bare and `?category=` paths.
    WikiSearchHit:
      allOf:
        - $ref: "#/components/schemas/WikiPageListItem"
        - type: object
          properties:
            snippet:
              type: string
            rank:
              type: number
      description: A wiki page returned from the legacy FTS5 search path (`GET /wiki?q=`). Adds the highlighted `snippet` and bm25-ish `rank`. Content is not included.
    WikiHybridSearchResult:
      type: object
      properties:
        id:
          type: string
        project_id:
          type: string
        title:
          type: string
        slug:
          type: string
        category:
          type: string
        updated_by:
          type: string
        created_at:
          type: string
        updated_at:
          type: string
        score:
          type: number
        ftsScore:
          type: number
        semanticScore:
          type: number
        matchedChunk:
          type: string
        snippet:
          type: string
      required:
        - id
        - project_id
        - title
        - slug
        - category
        - updated_by
        - created_at
        - updated_at
        - score
      description: A hit from the hybrid/semantic/fts wiki search. `score` is the blended ranking; `ftsScore` / `semanticScore` are the per-mode components; `matchedChunk` is the highest-scoring chunk on the page (semantic / hybrid only).
    WikiSearchResponse:
      type: object
      properties:
        mode:
          type: string
          enum:
            - hybrid
            - semantic
            - fts
        query:
          type: string
        geminiConfigured:
          type: boolean
        results:
          type: array
          items:
            $ref: "#/components/schemas/WikiHybridSearchResult"
      required:
        - mode
        - geminiConfigured
        - results
      description: Envelope returned by `GET /wiki/search`. `geminiConfigured` reports whether the server has a Gemini API key (semantic + hybrid modes fall back to FTS when false).
    WikiBackfillResult:
      type: object
      properties:
        projectId:
          type: string
        total:
          type: integer
        embedded:
          type: integer
        skipped:
          type: integer
        errors:
          type: array
          items:
            type: object
            properties:
              pageId:
                type: string
              error:
                type: string
            required:
              - pageId
              - error
      required:
        - projectId
        - total
        - embedded
        - skipped
        - errors
      description: Result of a project-wide wiki re-embedding pass. `total` is the count of pages scanned; `embedded` increments per successfully chunk-and-embed; `skipped` counts pages skipped because the embed client could not be invoked; `errors` lists per-page failures.
    WikiErrorResponse:
      type: object
      properties:
        error:
          type: string
        details:
          type: array
          items:
            type: object
            properties:
              path:
                type: array
                items:
                  anyOf:
                    - type: string
                    - type: number
              message:
                type: string
            required:
              - path
              - message
      required:
        - error
      description: Error envelope. `details` is populated for 400 schema-validation errors with one entry per failing Zod issue.
    WikiDocumentBackfillResult:
      type: object
      properties:
        skipped:
          type: boolean
        reason:
          type: string
        reused:
          type: boolean
        sessionId:
          type: string
        agentId:
          type: string
        queued:
          type: integer
      required:
        - skipped
      description: "Result of an on-demand wiki documentation backfill. `skipped: true` with `reason: none_undocumented` means the Done column has no remaining undocumented cards. Otherwise a docs-agent session was started (or reused) and `queued` is how many cards were handed to it."
    EpicPullsResponse:
      type: object
      properties:
        epicId:
          type: string
        featureBranch:
          type: string
          nullable: true
        source:
          type: string
          enum:
            - agenthub
            - github
        pulls:
          type: array
          items:
            $ref: "#/components/schemas/EpicPullSummary"
      required:
        - epicId
        - featureBranch
        - source
        - pulls
    EpicPullSummary:
      type: object
      properties:
        number:
          type: number
        title:
          type: string
        state:
          type: string
          description: "'open' or 'closed'."
        merged:
          type: boolean
        draft:
          type: boolean
        html_url:
          type: string
          nullable: true
        head:
          type: string
          nullable: true
        base:
          type: string
          nullable: true
        created_at:
          type: string
          nullable: true
        updated_at:
          type: string
          nullable: true
        merged_at:
          type: string
          nullable: true
        closed_at:
          type: string
          nullable: true
        relation:
          type: string
          enum:
            - targets
            - integration
          description: "`targets` = PR merges into the epic feature branch; `integration` = PR ships the feature branch (its head) onward."
      required:
        - number
        - title
        - state
        - merged
        - draft
        - html_url
        - head
        - base
        - created_at
        - updated_at
        - merged_at
        - closed_at
        - relation
    SessionDesignFile:
      type: object
      properties:
        path:
          type: string
          description: Forward-slash path relative to the worktree `design/` dir.
        size:
          type: number
          description: File size in bytes.
        mtime:
          type: string
          description: Last-modified time, ISO 8601.
      required:
        - path
        - size
        - mtime
    DesignImportResult:
      type: object
      properties:
        designId:
          type: string
        sessionId:
          type: string
          description: The design-mode session the design now lives in.
        agentId:
          type: string
        importedMessages:
          type: integer
          description: Count of design messages replayed into the session.
        reused:
          type: boolean
          description: True when the design was already imported and the existing session was returned.
        skipped:
          type: string
          enum:
            - already-imported
      required:
        - designId
        - sessionId
        - agentId
        - importedMessages
        - reused
    ForwardThreadEntryResponse:
      type: object
      properties:
        session:
          type: object
          additionalProperties:
            nullable: true
          description: The newly created session for the target agent (client-enriched shape).
        forwardedMessageId:
          type: string
          nullable: true
          description: ID of the pre-stored forwarded user message, or null when autoStart dispatched the turn instead.
      required:
        - session
        - forwardedMessageId
    ThreadsErrorResponse:
      type: object
      properties:
        error:
          type: string
      required:
        - error
      description: Error envelope.
    ForwardThreadEntryRequest:
      type: object
      properties:
        targetAgentId:
          type: string
          minLength: 1
          description: Agent to forward the thread entry to. May belong to a different project; the caller must be able to view the target agent’s project.
        prompt:
          type: string
          maxLength: 50000
          description: Extra instructions prepended to the forwarded entry (max 50k chars).
        autoStart:
          type: boolean
          description: When true, immediately dispatch the forwarded message to the target agent’s CLI (fire-and-forget).
      required:
        - targetAgentId
  parameters: {}
paths:
  /api/agents:
    get:
      tags:
        - Agents
      summary: List all agents (enriched with last-activity / last-message)
      responses:
        "200":
          description: Array of agents enriched with last-activity metadata.
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/AgentEnriched"
    post:
      tags:
        - Agents
      summary: Create a new agent under an existing project
      description: Returns 400 for missing/invalid id (must be alphanumeric + hyphens) or missing projectId, 404 if the project does not exist, 409 if the id collides with an existing agent.
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                id:
                  type: string
                  minLength: 1
                  pattern: ^[a-zA-Z0-9-]+$
                projectId:
                  type: string
                  minLength: 1
                name:
                  type: string
                engine:
                  type: string
                model:
                  type: string
                systemPrompt:
                  type: string
                color:
                  type: string
                avatar:
                  type: string
                role:
                  type: string
                isDev:
                  type: boolean
                  description: Accept autonomously-dispatched kanban tickets. Defaults to false in the UI (opt-in). Sending a value that contradicts a locked role (dev/lead → must be true; docs/reviewer → must be false) is rejected with 400.
                heartbeat:
                  type: object
                  properties:
                    enabled:
                      type: boolean
                    interval:
                      type: string
                    prompt:
                      type: string
                    model:
                      type: string
                    shared:
                      anyOf:
                        - type: boolean
                        - type: number
                          enum:
                            - 0
                        - type: number
                          enum:
                            - 1
                        - type: string
                          enum:
                            - "0"
                        - type: string
                          enum:
                            - "1"
                browserToolsEnabled:
                  type: boolean
                browserViewportWidth:
                  type: number
                  nullable: true
                browserViewportHeight:
                  type: number
                  nullable: true
                browserPageLoadTimeoutMs:
                  type: number
                  nullable: true
                allowedSkills:
                  type: array
                  nullable: true
                  items:
                    type: string
              required:
                - id
                - projectId
      responses:
        "201":
          description: Created agent.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AgentEnriched"
        "400":
          description: Validation failed.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AgentErrorResponse"
        "404":
          description: Project not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AgentErrorResponse"
        "409":
          description: Agent id already exists.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AgentErrorResponse"
  /api/agents/{agentId}:
    patch:
      tags:
        - Agents
      summary: Partially update an agent
      description: Omitted keys preserve the existing value. `browserViewportWidth` / `browserViewportHeight` / `browserPageLoadTimeoutMs` accept `null` to clear the override.
      parameters:
        - schema:
            type: string
            description: Agent ID.
          required: true
          description: Agent ID.
          name: agentId
          in: path
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                name:
                  type: string
                engine:
                  type: string
                model:
                  type: string
                systemPrompt:
                  type: string
                color:
                  type: string
                avatar:
                  type: string
                heartbeat:
                  type: object
                  properties:
                    enabled:
                      type: boolean
                    interval:
                      type: string
                    prompt:
                      type: string
                    model:
                      type: string
                    shared:
                      anyOf:
                        - type: boolean
                        - type: number
                          enum:
                            - 0
                        - type: number
                          enum:
                            - 1
                        - type: string
                          enum:
                            - "0"
                        - type: string
                          enum:
                            - "1"
                active:
                  type: boolean
                reviewer:
                  type: string
                role:
                  type: string
                canReview:
                  type: boolean
                isDev:
                  type: boolean
                  description: Accept autonomously-dispatched kanban tickets. Sending a value that contradicts a locked role (dev/lead → must be true; docs/reviewer → must be false) is rejected with 400; a matching value is a no-op.
                browserToolsEnabled:
                  type: boolean
                browserViewportWidth:
                  type: number
                  nullable: true
                browserViewportHeight:
                  type: number
                  nullable: true
                browserPageLoadTimeoutMs:
                  type: number
                  nullable: true
                allowedSkills:
                  type: array
                  nullable: true
                  items:
                    type: string
      responses:
        "200":
          description: Updated agent.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AgentEnriched"
        "400":
          description: Validation failed.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AgentErrorResponse"
        "404":
          description: Agent not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AgentErrorResponse"
    delete:
      tags:
        - Agents
      summary: Hard-delete an agent and its dependent rows
      description: Stops the in-memory heartbeat, transactionally clears every child store keyed by `agent_id` (sessions cascade messages, skill invocations, background tasks, queue entries, and checkpoints via FK ON DELETE CASCADE), drops the agent from `projects.json`, refreshes the project room, and best-effort removes the on-disk agent workspace. Returns 204 on success.
      parameters:
        - schema:
            type: string
            description: Agent ID.
          required: true
          description: Agent ID.
          name: agentId
          in: path
      responses:
        "204":
          description: Agent and dependent rows removed.
        "404":
          description: Agent not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AgentErrorResponse"
        "500":
          description: Internal cleanup failure.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AgentErrorResponse"
  /api/agents/bulk-engine:
    post:
      tags:
        - Agents
      summary: Bulk-set per-user engine + model overrides for every visible agent
      description: Requires authentication. Writes the caller's own `agentEngineOverrides` and `agentModelOverrides` for every agent in projects they can view — never the shared `agents` row. Falls back to the engine's default model when `model` is missing or not in the engine's allowlist.
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                engine:
                  type: string
                  minLength: 1
                model:
                  type: string
              required:
                - engine
      responses:
        "200":
          description: Bulk-engine result.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AgentsBulkEngineResponse"
        "400":
          description: Validation failed or unknown engine.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AgentErrorResponse"
        "401":
          description: Authentication required.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AgentErrorResponse"
  /api/agents/{agentId}/memory:
    put:
      tags:
        - Agents
      summary: Replace the agent memory file (MEMORY.md) contents
      parameters:
        - schema:
            type: string
            description: Agent ID.
          required: true
          description: Agent ID.
          name: agentId
          in: path
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                content:
                  type: string
              required:
                - content
      responses:
        "200":
          description: Acknowledgment.
          content:
            application/json:
              schema:
                type: object
                properties:
                  ok:
                    type: boolean
                    enum:
                      - true
                required:
                  - ok
        "400":
          description: Validation failed or workspace not configured.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AgentErrorResponse"
        "404":
          description: Agent not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AgentErrorResponse"
  /api/sessions/{sessionId}/artifacts:
    get:
      tags:
        - Artifacts
      summary: List a session’s generated artifacts
      description: Returns metadata for every document an agent generated in this session, newest first. The bytes are fetched separately via the per-artifact content endpoint.
      parameters:
        - schema:
            type: string
            description: Session ID.
          required: true
          description: Session ID.
          name: sessionId
          in: path
      responses:
        "200":
          description: Artifact list.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ArtifactList"
        "404":
          description: Session not found / not owned by caller.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ArtifactErrorResponse"
    post:
      tags:
        - Artifacts
      summary: Upload an artifact to a session
      description: "Stores a raw file body as a session artifact. Send the bytes as the request body with `Content-Type` set to the file’s MIME type and `x-filename` set to the display name. Agents can set `x-artifact-presentation: inline` for user-requested deliverables that should open automatically in the active session viewer. Used by agents (via the agent-hub artifact script) and the web UI. Rejects executables/native binaries and bodies over 100 MB."
      parameters:
        - schema:
            type: string
            description: Session ID.
          required: true
          description: Session ID.
          name: sessionId
          in: path
        - schema:
            type: string
            description: Artifact display filename.
          required: false
          description: Artifact display filename.
          name: x-filename
          in: header
        - schema:
            type: string
            description: Creating agent ID.
          required: false
          description: Creating agent ID.
          name: x-agent-id
          in: header
        - schema:
            type: string
            enum:
              - inline
            description: Requests automatic opening in the active session viewer after the upload completes.
          required: false
          description: Requests automatic opening in the active session viewer after the upload completes.
          name: x-artifact-presentation
          in: header
      requestBody:
        content:
          application/octet-stream:
            schema:
              type: string
              format: binary
              description: Raw file bytes.
      responses:
        "200":
          description: Created artifact metadata.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Artifact"
        "400":
          description: Empty body or rejected content type.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ArtifactErrorResponse"
        "404":
          description: Session not found / not owned by caller.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ArtifactErrorResponse"
        "413":
          description: File exceeds the 100 MB limit.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ArtifactErrorResponse"
        "500":
          description: Storage write failed.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ArtifactErrorResponse"
  /api/sessions/{sessionId}/artifacts/{artifactId}/content:
    get:
      tags:
        - Artifacts
      summary: Download or view an artifact’s bytes
      description: "Streams the artifact bytes with `X-Content-Type-Options: nosniff`. Inert types render inline by default; pass `?download=1` for an attachment disposition. Active/scriptable content (HTML, SVG, XML, JS) is always forced to download with a neutral `application/octet-stream` type so it cannot execute in the app origin. When the backend is S3 and `?redirect=1` is set (non-active content only), responds 302 to a short-lived presigned URL instead of proxying the bytes. The store is resolved from the artifact row’s recorded backend, so reads survive a later storage reconfiguration."
      parameters:
        - schema:
            type: string
            description: Session ID.
          required: true
          description: Session ID.
          name: sessionId
          in: path
        - schema:
            type: string
            description: Artifact ID.
          required: true
          description: Artifact ID.
          name: artifactId
          in: path
      responses:
        "200":
          description: Artifact bytes.
        "302":
          description: Redirect to a presigned download URL (S3 backend, redirect=1).
        "404":
          description: Session or artifact not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ArtifactErrorResponse"
        "500":
          description: Storage read failed.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ArtifactErrorResponse"
        "503":
          description: Artifact’s recorded storage backend is unavailable / unrecoverable.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ArtifactErrorResponse"
  /api/sessions/{sessionId}/artifacts/{artifactId}:
    delete:
      tags:
        - Artifacts
      summary: Delete an artifact
      description: Deletes the artifact bytes from object storage FIRST, then removes the metadata row. If the storage delete fails the request fails (502) and nothing is removed, so the bytes are never orphaned without a record to retry. Resolves the row’s recorded backend, so it works after a storage reconfiguration.
      parameters:
        - schema:
            type: string
            description: Session ID.
          required: true
          description: Session ID.
          name: sessionId
          in: path
        - schema:
            type: string
            description: Artifact ID.
          required: true
          description: Artifact ID.
          name: artifactId
          in: path
      responses:
        "200":
          description: Deleted.
          content:
            application/json:
              schema:
                type: object
                properties:
                  ok:
                    type: boolean
                required:
                  - ok
        "404":
          description: Session or artifact not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ArtifactErrorResponse"
        "502":
          description: Object-store deletion failed; nothing was removed (safe to retry).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ArtifactErrorResponse"
        "503":
          description: Artifact’s recorded storage backend is unavailable / unrecoverable.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ArtifactErrorResponse"
  /api/auth/status:
    get:
      tags:
        - Auth
      summary: Public auth status (used by the login screen).
      responses:
        "200":
          description: Whether auth is configured and which secrets are present.
          content:
            application/json:
              schema:
                type: object
                properties:
                  authConfigured:
                    type: boolean
                  email:
                    type: string
                    nullable: true
                  needsEmailUpdate:
                    type: boolean
                  role:
                    type: string
                    nullable: true
                    enum:
                      - Owner
                      - Admin
                      - User
                      - null
                  jwtConfigured:
                    type: boolean
                  apiKeyConfigured:
                    type: boolean
                  needsMigration:
                    type: boolean
                  activeOrgIsLocal:
                    type: boolean
                required:
                  - authConfigured
                  - email
                  - needsEmailUpdate
                  - role
                  - jwtConfigured
                  - apiKeyConfigured
                  - needsMigration
                  - activeOrgIsLocal
  /api/auth/setup:
    post:
      tags:
        - Auth
      summary: First-run setup — provisions the Owner user.
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AuthSetupBody"
      responses:
        "200":
          description: Setup succeeded; Owner token returned.
          content:
            application/json:
              schema:
                type: object
                properties:
                  ok:
                    type: boolean
                    enum:
                      - true
                  token:
                    type: string
                  expiresAt:
                    type: string
                  user:
                    type: object
                    properties:
                      id:
                        type: string
                      email:
                        type: string
                        nullable: true
                      needsEmailUpdate:
                        type: boolean
                      role:
                        type: string
                        enum:
                          - Owner
                          - Admin
                          - User
                    required:
                      - email
                      - role
                required:
                  - ok
                  - token
                  - expiresAt
                  - user
        "400":
          description: Invalid body or domain validation failure.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ZodErrorResponse"
        "409":
          description: Auth already configured.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /api/auth/login:
    post:
      tags:
        - Auth
      summary: Verify password and either issue a JWT or return an MFA challenge.
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AuthLoginBody"
      responses:
        "200":
          description: Login succeeded, or MFA is required before token issuance.
          content:
            application/json:
              schema:
                anyOf:
                  - $ref: "#/components/schemas/TokenResponse"
                  - $ref: "#/components/schemas/MfaRequiredResponse"
        "400":
          description: Invalid body shape.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ZodErrorResponse"
        "401":
          description: Wrong credentials.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "403":
          description: No membership in active org.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "409":
          description: Auth not configured.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "429":
          description: Rate-limited.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /api/auth/login/mfa:
    post:
      tags:
        - Auth
      summary: Complete a pending MFA login challenge and issue a JWT.
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AuthMfaLoginBody"
      responses:
        "200":
          description: MFA challenge completed; JWT returned.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TokenResponse"
        "400":
          description: Invalid body shape.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ZodErrorResponse"
        "401":
          description: Invalid or expired challenge/code.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "429":
          description: Rate-limited.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /api/auth/me:
    get:
      tags:
        - Auth
      summary: Identity of the currently authenticated caller.
      responses:
        "200":
          description: Caller summary.
          content:
            application/json:
              schema:
                type: object
                properties:
                  user:
                    type: object
                    nullable: true
                    properties:
                      id:
                        type: string
                      email:
                        type: string
                        nullable: true
                      needsEmailUpdate:
                        type: boolean
                      role:
                        type: string
                        nullable: true
                        enum:
                          - Owner
                          - Admin
                          - User
                          - null
                      mfaEnabled:
                        type: boolean
                    required:
                      - email
                      - role
                  authConfigured:
                    type: boolean
                  role:
                    type: string
                    nullable: true
                    enum:
                      - Owner
                      - Admin
                      - User
                      - null
                  orgId:
                    type: string
                    nullable: true
                required:
                  - user
                  - authConfigured
                  - role
                  - orgId
  /api/auth/me/email:
    put:
      tags:
        - Auth
      summary: Set the caller's canonical email login identifier.
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AuthUpdateEmailBody"
      responses:
        "200":
          description: Email updated; replacement JWT returned.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TokenResponse"
        "400":
          description: Invalid email body.
          content:
            application/json:
              schema:
                anyOf:
                  - $ref: "#/components/schemas/ZodErrorResponse"
                  - $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Not authenticated.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "404":
          description: User not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "409":
          description: Email already taken.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "500":
          description: Server auth not configured.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /api/auth/me/mfa/enrollment/start:
    post:
      tags:
        - Auth
      summary: Start app-based TOTP MFA enrollment for the current user.
      responses:
        "200":
          description: Pending TOTP secret and provisioning URI generated.
          content:
            application/json:
              schema:
                type: object
                properties:
                  ok:
                    type: boolean
                    enum:
                      - true
                  secret:
                    type: string
                  otpauthUri:
                    type: string
                  mfaEnabled:
                    type: boolean
                required:
                  - ok
                  - secret
                  - otpauthUri
                  - mfaEnabled
        "401":
          description: Not authenticated.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /api/auth/me/mfa/enrollment/confirm:
    post:
      tags:
        - Auth
      summary: Confirm the current TOTP code and enable MFA.
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AuthMfaCodeBody"
      responses:
        "200":
          description: MFA enabled; recovery codes returned once.
          content:
            application/json:
              schema:
                type: object
                properties:
                  ok:
                    type: boolean
                    enum:
                      - true
                  recoveryCodes:
                    type: array
                    items:
                      type: string
                  mfaEnabled:
                    type: boolean
                    enum:
                      - true
                required:
                  - ok
                  - recoveryCodes
                  - mfaEnabled
        "400":
          description: Invalid body or no pending enrollment.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Not authenticated.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "429":
          description: Rate-limited.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /api/auth/me/mfa/recovery-codes/regenerate:
    post:
      tags:
        - Auth
      summary: Regenerate single-use recovery codes for the current user.
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AuthMfaCodeBody"
      responses:
        "200":
          description: Replacement recovery codes returned once.
          content:
            application/json:
              schema:
                type: object
                properties:
                  ok:
                    type: boolean
                    enum:
                      - true
                  recoveryCodes:
                    type: array
                    items:
                      type: string
                required:
                  - ok
                  - recoveryCodes
        "400":
          description: Invalid body or MFA disabled.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Invalid MFA code.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "429":
          description: Rate-limited.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /api/auth/me/mfa/disable:
    post:
      tags:
        - Auth
      summary: Disable MFA for the current user after validating a current second factor.
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AuthMfaCodeBody"
      responses:
        "200":
          description: MFA disabled.
          content:
            application/json:
              schema:
                type: object
                properties:
                  ok:
                    type: boolean
                    enum:
                      - true
                  mfaEnabled:
                    type: boolean
                    enum:
                      - false
                required:
                  - ok
                  - mfaEnabled
        "400":
          description: Invalid body or MFA disabled.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Invalid MFA code.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "429":
          description: Rate-limited.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /api/auth/users/{id}/mfa/reset:
    post:
      tags:
        - Auth
      summary: Owner/Admin reset for a locked-out user MFA state.
      responses:
        "200":
          description: Target MFA state reset.
          content:
            application/json:
              schema:
                type: object
                properties:
                  ok:
                    type: boolean
                    enum:
                      - true
                  userId:
                    type: string
                  mfaEnabled:
                    type: boolean
                    enum:
                      - false
                  resetAt:
                    type: string
                    nullable: true
                  resetByUserId:
                    type: string
                    nullable: true
                required:
                  - ok
                  - userId
                  - mfaEnabled
                  - resetAt
                  - resetByUserId
        "403":
          description: Insufficient role.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "404":
          description: User not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /api/auth/me/claude-auth:
    get:
      tags:
        - Auth
      summary: Per-user Claude credentials (masked).
      responses:
        "200":
          description: Stored credentials with masking.
          content:
            application/json:
              schema:
                type: object
                additionalProperties:
                  nullable: true
        "401":
          description: 'Authenticated, but the caller has no per-user account row (legacy global apiKey / local-bundled bypass). Body carries `code: "no_user_identity"`; clients should render an empty state rather than treating it as an expired session. Any OTHER 401 from these routes is a genuinely dead session.'
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "404":
          description: User not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
    put:
      tags:
        - Auth
      summary: Update per-user Claude credentials.
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateClaudeAuthBody"
      responses:
        "200":
          description: Updated.
          content:
            application/json:
              schema:
                type: object
                additionalProperties:
                  nullable: true
        "400":
          description: Invalid body.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ZodErrorResponse"
        "401":
          description: 'Authenticated, but the caller has no per-user account row (legacy global apiKey / local-bundled bypass). Body carries `code: "no_user_identity"`; clients should render an empty state rather than treating it as an expired session. Any OTHER 401 from these routes is a genuinely dead session.'
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "404":
          description: User not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /api/auth/me/cursor-auth:
    get:
      tags:
        - Auth
      summary: Per-user cursor credentials (masked).
      responses:
        "200":
          description: Stored credentials with masking.
          content:
            application/json:
              schema:
                type: object
                properties:
                  engine:
                    type: string
                  apiKey:
                    type: string
                    nullable: true
                  updatedAt:
                    anyOf:
                      - type: string
                      - type: number
                      - nullable: true
                  hostConfigFallback:
                    type: object
                    properties:
                      apiKey:
                        type: boolean
                    required:
                      - apiKey
                required:
                  - engine
                  - apiKey
                  - hostConfigFallback
        "401":
          description: 'Authenticated, but the caller has no per-user account row (legacy global apiKey / local-bundled bypass). Body carries `code: "no_user_identity"`; clients should render an empty state rather than treating it as an expired session. Any OTHER 401 from these routes is a genuinely dead session.'
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "404":
          description: User not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
    put:
      tags:
        - Auth
      summary: Update per-user cursor credentials.
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateSingleKeyAuthBody"
      responses:
        "200":
          description: Updated.
          content:
            application/json:
              schema:
                type: object
                properties:
                  engine:
                    type: string
                  apiKey:
                    type: string
                    nullable: true
                  updatedAt:
                    anyOf:
                      - type: string
                      - type: number
                      - nullable: true
                  hostConfigFallback:
                    type: object
                    properties:
                      apiKey:
                        type: boolean
                    required:
                      - apiKey
                required:
                  - engine
                  - apiKey
                  - hostConfigFallback
        "400":
          description: Invalid body.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ZodErrorResponse"
        "401":
          description: 'Authenticated, but the caller has no per-user account row (legacy global apiKey / local-bundled bypass). Body carries `code: "no_user_identity"`; clients should render an empty state rather than treating it as an expired session. Any OTHER 401 from these routes is a genuinely dead session.'
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "404":
          description: User not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /api/auth/me/gemini-auth:
    get:
      tags:
        - Auth
      summary: Per-user gemini credentials (masked).
      responses:
        "200":
          description: Stored credentials with masking.
          content:
            application/json:
              schema:
                type: object
                properties:
                  engine:
                    type: string
                  apiKey:
                    type: string
                    nullable: true
                  updatedAt:
                    anyOf:
                      - type: string
                      - type: number
                      - nullable: true
                  hostConfigFallback:
                    type: object
                    properties:
                      apiKey:
                        type: boolean
                    required:
                      - apiKey
                required:
                  - engine
                  - apiKey
                  - hostConfigFallback
        "401":
          description: 'Authenticated, but the caller has no per-user account row (legacy global apiKey / local-bundled bypass). Body carries `code: "no_user_identity"`; clients should render an empty state rather than treating it as an expired session. Any OTHER 401 from these routes is a genuinely dead session.'
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "404":
          description: User not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
    put:
      tags:
        - Auth
      summary: Update per-user gemini credentials.
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateSingleKeyAuthBody"
      responses:
        "200":
          description: Updated.
          content:
            application/json:
              schema:
                type: object
                properties:
                  engine:
                    type: string
                  apiKey:
                    type: string
                    nullable: true
                  updatedAt:
                    anyOf:
                      - type: string
                      - type: number
                      - nullable: true
                  hostConfigFallback:
                    type: object
                    properties:
                      apiKey:
                        type: boolean
                    required:
                      - apiKey
                required:
                  - engine
                  - apiKey
                  - hostConfigFallback
        "400":
          description: Invalid body.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ZodErrorResponse"
        "401":
          description: 'Authenticated, but the caller has no per-user account row (legacy global apiKey / local-bundled bypass). Body carries `code: "no_user_identity"`; clients should render an empty state rather than treating it as an expired session. Any OTHER 401 from these routes is a genuinely dead session.'
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "404":
          description: User not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /api/auth/me/grok-auth:
    get:
      tags:
        - Auth
      summary: Per-user grok credentials (masked).
      responses:
        "200":
          description: Stored credentials with masking.
          content:
            application/json:
              schema:
                type: object
                properties:
                  engine:
                    type: string
                  apiKey:
                    type: string
                    nullable: true
                  updatedAt:
                    anyOf:
                      - type: string
                      - type: number
                      - nullable: true
                  hostConfigFallback:
                    type: object
                    properties:
                      apiKey:
                        type: boolean
                    required:
                      - apiKey
                required:
                  - engine
                  - apiKey
                  - hostConfigFallback
        "401":
          description: 'Authenticated, but the caller has no per-user account row (legacy global apiKey / local-bundled bypass). Body carries `code: "no_user_identity"`; clients should render an empty state rather than treating it as an expired session. Any OTHER 401 from these routes is a genuinely dead session.'
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "404":
          description: User not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
    put:
      tags:
        - Auth
      summary: Update per-user grok credentials.
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateSingleKeyAuthBody"
      responses:
        "200":
          description: Updated.
          content:
            application/json:
              schema:
                type: object
                properties:
                  engine:
                    type: string
                  apiKey:
                    type: string
                    nullable: true
                  updatedAt:
                    anyOf:
                      - type: string
                      - type: number
                      - nullable: true
                  hostConfigFallback:
                    type: object
                    properties:
                      apiKey:
                        type: boolean
                    required:
                      - apiKey
                required:
                  - engine
                  - apiKey
                  - hostConfigFallback
        "400":
          description: Invalid body.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ZodErrorResponse"
        "401":
          description: 'Authenticated, but the caller has no per-user account row (legacy global apiKey / local-bundled bypass). Body carries `code: "no_user_identity"`; clients should render an empty state rather than treating it as an expired session. Any OTHER 401 from these routes is a genuinely dead session.'
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "404":
          description: User not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /api/auth/me/codex-auth:
    get:
      tags:
        - Auth
      summary: Per-user Codex credentials (masked) + device-login (CODEX_HOME) status.
      responses:
        "200":
          description: Stored API key + per-user CODEX_HOME inspection.
          content:
            application/json:
              schema:
                type: object
                properties:
                  engine:
                    type: string
                  apiKey:
                    type: string
                    nullable: true
                  updatedAt:
                    anyOf:
                      - type: string
                      - type: number
                      - nullable: true
                  hostConfigFallback:
                    type: object
                    properties:
                      apiKey:
                        type: boolean
                    required:
                      - apiKey
                  deviceLogin:
                    type: object
                    properties:
                      uiStatus:
                        type: string
                        enum:
                          - missing
                          - pending
                          - authenticated
                      loginInProgress:
                        type: boolean
                      oauth:
                        type: object
                        properties:
                          loggedIn:
                            type: boolean
                          mode:
                            type: string
                            nullable: true
                          authJsonPath:
                            type: string
                            nullable: true
                        required:
                          - loggedIn
                          - mode
                          - authJsonPath
                      codexHomePath:
                        type: string
                        nullable: true
                    required:
                      - uiStatus
                      - loginInProgress
                      - oauth
                      - codexHomePath
                required:
                  - engine
                  - apiKey
                  - hostConfigFallback
                  - deviceLogin
        "401":
          description: 'Authenticated, but the caller has no per-user account row (legacy global apiKey / local-bundled bypass). Body carries `code: "no_user_identity"`; clients should render an empty state rather than treating it as an expired session. Any OTHER 401 from these routes is a genuinely dead session.'
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "404":
          description: User not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
    put:
      tags:
        - Auth
      summary: Update per-user Codex credentials.
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateSingleKeyAuthBody"
      responses:
        "200":
          description: Updated.
          content:
            application/json:
              schema:
                type: object
                properties:
                  engine:
                    type: string
                  apiKey:
                    type: string
                    nullable: true
                  updatedAt:
                    anyOf:
                      - type: string
                      - type: number
                      - nullable: true
                  hostConfigFallback:
                    type: object
                    properties:
                      apiKey:
                        type: boolean
                    required:
                      - apiKey
                required:
                  - engine
                  - apiKey
                  - hostConfigFallback
        "400":
          description: Invalid body.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ZodErrorResponse"
        "401":
          description: 'Authenticated, but the caller has no per-user account row (legacy global apiKey / local-bundled bypass). Body carries `code: "no_user_identity"`; clients should render an empty state rather than treating it as an expired session. Any OTHER 401 from these routes is a genuinely dead session.'
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "404":
          description: User not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /api/auth/me/agent-engine-overrides:
    get:
      tags:
        - Auth
      summary: Per-user per-agent engine (+ optional model) overrides.
      responses:
        "200":
          description: Caller-scoped map of agentId → { engine, model? }. Entries whose engine/model are no longer in `engineValidModels` are filtered out.
          content:
            application/json:
              schema:
                type: object
                properties:
                  agentEngineOverrides:
                    type: object
                    additionalProperties:
                      type: object
                      properties:
                        engine:
                          type: string
                        model:
                          type: string
                      required:
                        - engine
                required:
                  - agentEngineOverrides
        "401":
          description: Not authenticated.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "404":
          description: User row missing.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
    put:
      tags:
        - Auth
      summary: Replace the caller-scoped per-agent engine override map.
      description: "Pass `{ agentEngineOverrides: {} }` to clear all overrides. Each entry must name an engine present in `engineValidModels`; optional `model` must be a valid model for that engine."
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/PutAgentEngineOverridesBody"
      responses:
        "200":
          description: Updated map.
          content:
            application/json:
              schema:
                type: object
                properties:
                  agentEngineOverrides:
                    type: object
                    additionalProperties:
                      type: object
                      properties:
                        engine:
                          type: string
                        model:
                          type: string
                      required:
                        - engine
                required:
                  - agentEngineOverrides
        "400":
          description: Invalid payload.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ZodErrorResponse"
        "401":
          description: Not authenticated.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "404":
          description: User row missing.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /api/auth/me/agent-model-overrides:
    get:
      tags:
        - Auth
      summary: Per-user per-agent default-model picks.
      responses:
        "200":
          description: Caller-scoped map of agentId → model id (the per-user "default model" dropdown selection). Entries whose model is no longer valid for any engine are filtered out.
          content:
            application/json:
              schema:
                type: object
                properties:
                  agentModelOverrides:
                    type: object
                    additionalProperties:
                      type: string
                required:
                  - agentModelOverrides
        "401":
          description: Not authenticated.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "404":
          description: User row missing.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
    put:
      tags:
        - Auth
      summary: Replace the caller-scoped per-agent default-model map.
      description: "Pass `{ agentModelOverrides: {} }` to clear all picks. Each model id must be valid for at least one configured engine; an empty-string value drops that single agent entry. Only the caller’s own sessions are affected."
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/PutAgentModelOverridesBody"
      responses:
        "200":
          description: Updated map.
          content:
            application/json:
              schema:
                type: object
                properties:
                  agentModelOverrides:
                    type: object
                    additionalProperties:
                      type: string
                required:
                  - agentModelOverrides
        "400":
          description: Invalid payload.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ZodErrorResponse"
        "401":
          description: Not authenticated.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "404":
          description: User row missing.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /api/auth/me/agent-model-overrides/{agentId}:
    put:
      tags:
        - Auth
      summary: Set the caller's default model for one agent (merge).
      description: "Merges server-side: updates only this agent's pick, leaving every other agent (and any concurrent edit from another tab) untouched. Preferred over the whole-map PUT for single edits."
      parameters:
        - schema:
            type: string
            description: Agent id.
          required: true
          description: Agent id.
          name: agentId
          in: path
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/PutAgentModelOverrideEntryBody"
      responses:
        "200":
          description: Updated map.
          content:
            application/json:
              schema:
                type: object
                properties:
                  agentModelOverrides:
                    type: object
                    additionalProperties:
                      type: string
                required:
                  - agentModelOverrides
        "400":
          description: Invalid payload.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ZodErrorResponse"
        "401":
          description: Not authenticated.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "404":
          description: User row missing.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
    delete:
      tags:
        - Auth
      summary: Clear the caller's default model for one agent (merge).
      parameters:
        - schema:
            type: string
            description: Agent id.
          required: true
          description: Agent id.
          name: agentId
          in: path
      responses:
        "200":
          description: Updated map.
          content:
            application/json:
              schema:
                type: object
                properties:
                  agentModelOverrides:
                    type: object
                    additionalProperties:
                      type: string
                required:
                  - agentModelOverrides
        "400":
          description: Invalid payload.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Not authenticated.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "404":
          description: User row missing.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /api/auth/me/agent-engine-overrides/{agentId}:
    put:
      tags:
        - Auth
      summary: Set the caller's engine override for one agent (merge).
      description: "Merges server-side: updates only this agent's engine, leaving other agents untouched. Omitting `model` preserves any existing per-agent model the legacy combined override stored (dropped only if no longer valid for the chosen engine); an explicit `model` must be valid for the engine."
      parameters:
        - schema:
            type: string
            description: Agent id.
          required: true
          description: Agent id.
          name: agentId
          in: path
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AgentEngineOverrideEntry"
      responses:
        "200":
          description: Updated map.
          content:
            application/json:
              schema:
                type: object
                properties:
                  agentEngineOverrides:
                    type: object
                    additionalProperties:
                      type: object
                      properties:
                        engine:
                          type: string
                        model:
                          type: string
                      required:
                        - engine
                required:
                  - agentEngineOverrides
        "400":
          description: Invalid payload.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ZodErrorResponse"
        "401":
          description: Not authenticated.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "404":
          description: User row missing.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
    delete:
      tags:
        - Auth
      summary: Clear the caller's engine override for one agent (merge).
      parameters:
        - schema:
            type: string
            description: Agent id.
          required: true
          description: Agent id.
          name: agentId
          in: path
      responses:
        "200":
          description: Updated map.
          content:
            application/json:
              schema:
                type: object
                properties:
                  agentEngineOverrides:
                    type: object
                    additionalProperties:
                      type: object
                      properties:
                        engine:
                          type: string
                        model:
                          type: string
                      required:
                        - engine
                required:
                  - agentEngineOverrides
        "400":
          description: Invalid payload.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Not authenticated.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "404":
          description: User row missing.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /api/auth/me/sidebar-collapsed-projects:
    get:
      tags:
        - Auth
      summary: Projects the caller has collapsed in the sidebar.
      responses:
        "200":
          description: Caller-scoped list of collapsed project ids.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SidebarCollapsedProjectsResponse"
        "401":
          description: Not authenticated.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "404":
          description: User row missing.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /api/auth/me/sidebar-collapsed-projects/{projectId}:
    put:
      tags:
        - Auth
      summary: Collapse or expand one project in the caller’s sidebar (merge).
      description: "Merges server-side: flips only this project's entry, so a toggle in one tab never clobbers another tab's concurrent edit. Unknown project ids are accepted and stored verbatim; they are simply never rendered."
      parameters:
        - schema:
            type: string
            description: Project id.
          required: true
          description: Project id.
          name: projectId
          in: path
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/PutSidebarCollapsedProjectBody"
      responses:
        "200":
          description: Updated list.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SidebarCollapsedProjectsResponse"
        "400":
          description: Invalid payload.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ZodErrorResponse"
        "401":
          description: Not authenticated.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "404":
          description: User row missing.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /api/auth/me/skill-credentials:
    get:
      tags:
        - Auth
      summary: List masked per-user skill credentials.
      parameters:
        - schema:
            type: string
          required: false
          name: skillId
          in: query
      responses:
        "200":
          description: Credential list.
          content:
            application/json:
              schema:
                type: object
                properties:
                  credentials:
                    type: array
                    items:
                      type: object
                      additionalProperties:
                        nullable: true
                required:
                  - credentials
        "401":
          description: Not authenticated.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "500":
          description: Lookup failed.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
    put:
      tags:
        - Auth
      summary: Upsert a single per-user skill credential.
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpsertSkillCredentialBody"
      responses:
        "200":
          description: Created or cleared.
          content:
            application/json:
              schema:
                type: object
                additionalProperties:
                  nullable: true
        "400":
          description: Validation failure.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ZodErrorResponse"
        "401":
          description: Not authenticated.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "403":
          description: Caller is not a member of the org.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "404":
          description: Agent / workspace not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /api/auth/me/skill-credentials/audit:
    get:
      tags:
        - Auth
      summary: Audit trail for the caller's skill credentials, newest first.
      parameters:
        - schema:
            type: string
          required: false
          name: skillId
          in: query
        - schema:
            type: integer
            minimum: 1
            maximum: 500
          required: false
          name: limit
          in: query
      responses:
        "200":
          description: Audit rows.
          content:
            application/json:
              schema:
                type: object
                properties:
                  audit:
                    type: array
                    items:
                      type: object
                      properties:
                        id:
                          type: string
                        user_id:
                          type: string
                        skill_id:
                          type: string
                        key_name:
                          type: string
                        action:
                          type: string
                          enum:
                            - upsert
                            - delete
                        actor_user_id:
                          type: string
                        created_at:
                          type: string
                      required:
                        - id
                        - user_id
                        - skill_id
                        - key_name
                        - action
                        - actor_user_id
                        - created_at
                required:
                  - audit
        "401":
          description: Not authenticated.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "500":
          description: Lookup failed.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /api/auth/me/skill-credentials/{id}:
    delete:
      tags:
        - Auth
      summary: Delete a per-user skill credential by id.
      parameters:
        - schema:
            type: string
          required: true
          name: id
          in: path
      responses:
        "200":
          description: Deleted.
          content:
            application/json:
              schema:
                type: object
                properties:
                  ok:
                    type: boolean
                    enum:
                      - true
                required:
                  - ok
        "401":
          description: Not authenticated.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "404":
          description: Credential not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /api/auth/me/skill-options:
    get:
      tags:
        - Auth
      summary: List a skill's option schema plus the caller's current selections.
      parameters:
        - schema:
            type: string
          required: true
          name: skillId
          in: query
        - schema:
            type: string
          required: false
          name: agentId
          in: query
      responses:
        "200":
          description: Option specs with the caller's effective selection.
          content:
            application/json:
              schema:
                type: object
                properties:
                  options:
                    type: array
                    items:
                      type: object
                      properties:
                        name:
                          type: string
                        label:
                          type: string
                        description:
                          type: string
                        choices:
                          type: array
                          items:
                            type: object
                            properties:
                              value:
                                type: string
                              label:
                                type: string
                            required:
                              - value
                              - label
                        default:
                          type: string
                        required:
                          type: boolean
                        selected:
                          type: string
                      required:
                        - name
                        - label
                        - description
                        - choices
                        - default
                        - required
                        - selected
                required:
                  - options
        "400":
          description: Missing skillId or invalid option schema.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Not authenticated.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "500":
          description: Lookup failed.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
    put:
      tags:
        - Auth
      summary: Select a per-user skill option value.
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpsertSkillOptionBody"
      responses:
        "200":
          description: Selection stored.
          content:
            application/json:
              schema:
                type: object
                additionalProperties:
                  nullable: true
        "400":
          description: Validation failure (unknown option or value not a declared choice).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Not authenticated.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "403":
          description: Caller is not a member of the org.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "404":
          description: Agent / workspace not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /api/auth/me/skill-options/{skillId}/{optionName}:
    delete:
      tags:
        - Auth
      summary: Reset a per-user skill option to its default.
      parameters:
        - schema:
            type: string
          required: true
          name: skillId
          in: path
        - schema:
            type: string
          required: true
          name: optionName
          in: path
      responses:
        "200":
          description: Reset (row deleted or already absent).
          content:
            application/json:
              schema:
                type: object
                properties:
                  ok:
                    type: boolean
                required:
                  - ok
        "401":
          description: Not authenticated.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /api/auth/keys:
    get:
      tags:
        - Auth
      summary: List the caller's active API keys.
      responses:
        "200":
          description: Key metadata (no plaintext tokens).
          content:
            application/json:
              schema:
                type: object
                properties:
                  keys:
                    type: array
                    items:
                      type: object
                      properties:
                        id:
                          type: string
                        name:
                          type: string
                        prefix:
                          type: string
                        createdAt:
                          type: string
                        lastUsedAt:
                          type: string
                          nullable: true
                        expiresAt:
                          type: string
                          nullable: true
                      required:
                        - id
                        - name
                        - prefix
                        - createdAt
                        - lastUsedAt
                        - expiresAt
                required:
                  - keys
        "401":
          description: Not authenticated.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
    post:
      tags:
        - Auth
      summary: Create a new per-user API key.
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateApiKeyBody"
      responses:
        "201":
          description: Key created — plaintext token shown ONCE.
          content:
            application/json:
              schema:
                type: object
                properties:
                  id:
                    type: string
                  name:
                    type: string
                  token:
                    type: string
                  prefix:
                    type: string
                  createdAt:
                    type: string
                  expiresAt:
                    type: string
                    nullable: true
                required:
                  - id
                  - name
                  - token
                  - prefix
                  - createdAt
                  - expiresAt
        "400":
          description: Invalid body.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ZodErrorResponse"
        "401":
          description: Not authenticated.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "429":
          description: Key cap reached.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /api/auth/keys/{id}:
    delete:
      tags:
        - Auth
      summary: Revoke a per-user API key.
      parameters:
        - schema:
            type: string
          required: true
          name: id
          in: path
      responses:
        "200":
          description: Revoked.
          content:
            application/json:
              schema:
                type: object
                properties:
                  ok:
                    type: boolean
                    enum:
                      - true
                required:
                  - ok
        "401":
          description: Not authenticated.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "404":
          description: Key not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /api/auth/users:
    get:
      tags:
        - Auth
      summary: List members of the active org (Admin+).
      responses:
        "200":
          description: Member list.
          content:
            application/json:
              schema:
                type: object
                properties:
                  users:
                    type: array
                    items:
                      $ref: "#/components/schemas/UserSummary"
                required:
                  - users
        "403":
          description: Insufficient role.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
    post:
      tags:
        - Auth
      summary: Create a user + membership (Owner only).
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AuthCreateUserBody"
      responses:
        "201":
          description: Created.
          content:
            application/json:
              schema:
                type: object
                properties:
                  user:
                    $ref: "#/components/schemas/UserSummary"
                  assignedProjectIds:
                    type: array
                    items:
                      type: string
                    description: Project ids the new user was assigned to (echo of the request list).
                required:
                  - user
                  - assignedProjectIds
        "400":
          description: Invalid body (Zod validation, incl. empty project ids) or a domain failure such as an unknown/inaccessible project id.
          content:
            application/json:
              schema:
                anyOf:
                  - $ref: "#/components/schemas/ZodErrorResponse"
                  - $ref: "#/components/schemas/ErrorResponse"
        "403":
          description: Only Owner may create Owner.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "409":
          description: Email taken.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "500":
          description: Project assignment validation is unavailable (fail-closed).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /api/auth/users/{id}/role:
    put:
      tags:
        - Auth
      summary: Change a member's role in the active org (Admin+).
      parameters:
        - schema:
            type: string
          required: true
          name: id
          in: path
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AuthUpdateUserRoleBody"
      responses:
        "200":
          description: Updated.
          content:
            application/json:
              schema:
                type: object
                properties:
                  ok:
                    type: boolean
                    enum:
                      - true
                  userId:
                    type: string
                  orgId:
                    type: string
                  role:
                    type: string
                    enum:
                      - Owner
                      - Admin
                      - User
                required:
                  - ok
                  - userId
                  - orgId
                  - role
        "400":
          description: Invalid role or sole-Owner demotion.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ZodErrorResponse"
        "403":
          description: Insufficient role.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "404":
          description: User or membership missing.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /api/auth/users/{id}:
    delete:
      tags:
        - Auth
      summary: Remove a user from the active org (Owner only).
      parameters:
        - schema:
            type: string
          required: true
          name: id
          in: path
      responses:
        "200":
          description: Removed; cascade summary if the user was deleted entirely.
          content:
            application/json:
              schema:
                type: object
                properties:
                  ok:
                    type: boolean
                    enum:
                      - true
                  userId:
                    type: string
                  orgId:
                    type: string
                  userDeleted:
                    type: boolean
                  cascadedPrivateProjects:
                    type: array
                    items:
                      type: string
                  orphanedSharedProjects:
                    type: array
                    items:
                      type: string
                required:
                  - ok
                  - userId
                  - orgId
                  - userDeleted
                  - cascadedPrivateProjects
                  - orphanedSharedProjects
        "400":
          description: Sole-Owner protection.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "403":
          description: Insufficient role.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "404":
          description: User or membership missing.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /api/auth/forgot-password:
    post:
      tags:
        - Auth
      summary: Request a password reset for an email address.
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AuthForgotPasswordBody"
      responses:
        "200":
          description: Enumeration-safe receipt.
          content:
            application/json:
              schema:
                type: object
                properties:
                  ok:
                    type: boolean
                    enum:
                      - true
                required:
                  - ok
        "429":
          description: Rate-limited.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /api/auth/reset-password:
    post:
      tags:
        - Auth
      summary: Consume a password reset token and set a new password.
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AuthResetPasswordBody"
      responses:
        "200":
          description: Password updated.
          content:
            application/json:
              schema:
                type: object
                properties:
                  ok:
                    type: boolean
                    enum:
                      - true
                required:
                  - ok
        "400":
          description: Invalid body, expired token, or consumed token.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /api/auth/users/{id}/reset-token:
    post:
      tags:
        - Auth
      summary: Generate a one-time reset token for a user (Owner only).
      parameters:
        - schema:
            type: string
          required: true
          name: id
          in: path
      responses:
        "201":
          description: One-time plaintext token. Store only shown once.
          content:
            application/json:
              schema:
                type: object
                properties:
                  token:
                    type: string
                  url:
                    type: string
                  expiresAt:
                    type: string
                  userId:
                    type: string
                required:
                  - token
                  - url
                  - expiresAt
                  - userId
        "403":
          description: Owner role required.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "404":
          description: User not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /api/auth/users/{id}/password:
    post:
      tags:
        - Auth
      summary: Self-reset or Owner-reset a user password.
      parameters:
        - schema:
            type: string
          required: true
          name: id
          in: path
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AuthPasswordResetBody"
      responses:
        "200":
          description: Password updated.
          content:
            application/json:
              schema:
                type: object
                properties:
                  ok:
                    type: boolean
                    enum:
                      - true
                  userId:
                    type: string
                  passwordReset:
                    type: object
                    properties:
                      smtpConfigured:
                        type: boolean
                      fallbackAvailable:
                        type: boolean
                      fallback:
                        type: string
                        nullable: true
                        enum:
                          - owner_generated_reset_code
                          - null
                    required:
                      - smtpConfigured
                      - fallbackAvailable
                      - fallback
                required:
                  - ok
                  - userId
                  - passwordReset
        "400":
          description: Invalid body.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ZodErrorResponse"
        "403":
          description: Only self or Owner may reset.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "404":
          description: User not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /api/auth/invites:
    post:
      tags:
        - Auth
      summary: Issue an invite for the active org (Admin+).
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateInviteBody"
      responses:
        "201":
          description: Invite created.
          content:
            application/json:
              schema:
                type: object
                properties:
                  token:
                    type: string
                  url:
                    type: string
                  role:
                    type: string
                    enum:
                      - Admin
                      - User
                  email:
                    type: string
                    nullable: true
                  expiresAt:
                    type: string
                  createdAt:
                    type: string
                  projectIds:
                    type: array
                    items:
                      type: string
                    description: Projects the invited user will be assigned to on acceptance.
                  emailDelivery:
                    anyOf:
                      - type: object
                        properties:
                          attempted:
                            type: boolean
                            enum:
                              - false
                          sent:
                            type: boolean
                            enum:
                              - false
                          reason:
                            type: string
                        required:
                          - attempted
                          - sent
                          - reason
                      - type: object
                        properties:
                          attempted:
                            type: boolean
                            enum:
                              - true
                          sent:
                            type: boolean
                            enum:
                              - true
                        required:
                          - attempted
                          - sent
                      - type: object
                        properties:
                          attempted:
                            type: boolean
                            enum:
                              - true
                          sent:
                            type: boolean
                            enum:
                              - false
                          reason:
                            type: string
                        required:
                          - attempted
                          - sent
                          - reason
                required:
                  - token
                  - url
                  - role
                  - email
                  - expiresAt
                  - createdAt
                  - projectIds
                  - emailDelivery
        "400":
          description: "Invalid body (Zod validation, incl. empty project ids) or a domain failure: bad role, missing user session, or an unknown/inaccessible project id."
          content:
            application/json:
              schema:
                anyOf:
                  - $ref: "#/components/schemas/ZodErrorResponse"
                  - $ref: "#/components/schemas/ErrorResponse"
        "403":
          description: Insufficient role (incl. non-Owner attempting project pre-assignment).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "500":
          description: Project assignment validation is unavailable (fail-closed).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
    get:
      tags:
        - Auth
      summary: List active invites for the active org (Admin+).
      responses:
        "200":
          description: Invite list.
          content:
            application/json:
              schema:
                type: object
                properties:
                  invites:
                    type: array
                    items:
                      type: object
                      properties:
                        token:
                          type: string
                        orgId:
                          type: string
                        role:
                          type: string
                          enum:
                            - Admin
                            - User
                        email:
                          type: string
                          nullable: true
                        url:
                          type: string
                        expiresAt:
                          type: string
                        createdAt:
                          type: string
                      required:
                        - token
                        - orgId
                        - role
                        - email
                        - url
                        - expiresAt
                        - createdAt
                  emailDelivery:
                    type: object
                    properties:
                      smtpConfigured:
                        type: boolean
                    required:
                      - smtpConfigured
                required:
                  - invites
                  - emailDelivery
        "403":
          description: Insufficient role.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /api/auth/invites/{token}/email:
    post:
      tags:
        - Auth
      summary: Send or resend an invite email for an active invite (Admin+, same org only).
      parameters:
        - schema:
            type: string
          required: true
          name: token
          in: path
      responses:
        "200":
          description: Invite email sent.
          content:
            application/json:
              schema:
                type: object
                properties:
                  ok:
                    type: boolean
                    enum:
                      - true
                  invite:
                    type: object
                    properties:
                      token:
                        type: string
                      orgId:
                        type: string
                      role:
                        type: string
                        enum:
                          - Admin
                          - User
                      email:
                        type: string
                        nullable: true
                      url:
                        type: string
                      expiresAt:
                        type: string
                      createdAt:
                        type: string
                    required:
                      - token
                      - orgId
                      - role
                      - email
                      - url
                      - expiresAt
                      - createdAt
                  emailDelivery:
                    anyOf:
                      - type: object
                        properties:
                          attempted:
                            type: boolean
                            enum:
                              - false
                          sent:
                            type: boolean
                            enum:
                              - false
                          reason:
                            type: string
                        required:
                          - attempted
                          - sent
                          - reason
                      - type: object
                        properties:
                          attempted:
                            type: boolean
                            enum:
                              - true
                          sent:
                            type: boolean
                            enum:
                              - true
                        required:
                          - attempted
                          - sent
                      - type: object
                        properties:
                          attempted:
                            type: boolean
                            enum:
                              - true
                          sent:
                            type: boolean
                            enum:
                              - false
                          reason:
                            type: string
                        required:
                          - attempted
                          - sent
                          - reason
                required:
                  - ok
                  - invite
                  - emailDelivery
        "400":
          description: Invite has no email recipient or SMTP is unavailable.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "403":
          description: Cross-org or insufficient role.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "404":
          description: Invite not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "410":
          description: Invite expired or was already accepted.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "429":
          description: Rate limited.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "502":
          description: SMTP delivery failed.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /api/auth/invites/{token}:
    delete:
      tags:
        - Auth
      summary: Revoke an invite (Admin+, same org only).
      parameters:
        - schema:
            type: string
          required: true
          name: token
          in: path
      responses:
        "200":
          description: Revoked.
          content:
            application/json:
              schema:
                type: object
                properties:
                  ok:
                    type: boolean
                    enum:
                      - true
                  token:
                    type: string
                required:
                  - ok
                  - token
        "403":
          description: Cross-org or insufficient role.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "404":
          description: Invite not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
    get:
      tags:
        - Auth
      summary: Public landing metadata for an invite token.
      parameters:
        - schema:
            type: string
          required: true
          name: token
          in: path
      responses:
        "200":
          description: Invite preview.
          content:
            application/json:
              schema:
                type: object
                properties:
                  orgId:
                    type: string
                  orgName:
                    type: string
                  role:
                    type: string
                    enum:
                      - Admin
                      - User
                  email:
                    type: string
                    nullable: true
                  expiresAt:
                    type: string
                  accepted:
                    type: boolean
                required:
                  - orgId
                  - orgName
                  - role
                  - email
                  - expiresAt
                  - accepted
        "404":
          description: Invite not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "410":
          description: Invite expired.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /api/auth/invites/{token}/accept:
    post:
      tags:
        - Auth
      summary: Redeem an invite — creates user, membership, returns a JWT.
      parameters:
        - schema:
            type: string
          required: true
          name: token
          in: path
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AcceptInviteBody"
      responses:
        "201":
          description: User created and signed in.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TokenResponse"
        "400":
          description: Invalid body or domain failure.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ZodErrorResponse"
        "404":
          description: Invite not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "409":
          description: Email taken.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "410":
          description: Invite expired / already accepted / lost race.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "429":
          description: Rate-limited.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "500":
          description: Server auth not configured.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /api/auth/logout:
    post:
      tags:
        - Auth
      summary: Stateless logout receipt (client drops its JWT).
      responses:
        "200":
          description: Receipt.
          content:
            application/json:
              schema:
                type: object
                properties:
                  ok:
                    type: boolean
                    enum:
                      - true
                required:
                  - ok
  /api/sessions/{sessionId}/background-shells:
    get:
      summary: List background shells for a session
      tags:
        - Sessions
      parameters:
        - schema:
            type: string
            description: Chat session id.
          required: true
          description: Chat session id.
          name: sessionId
          in: path
      responses:
        "200":
          description: Background shells, newest first.
          content:
            application/json:
              schema:
                type: object
                properties:
                  shells:
                    type: array
                    items:
                      $ref: "#/components/schemas/BackgroundShell"
                required:
                  - shells
        "404":
          description: Session not found
        "503":
          description: Background shells unavailable on this server
    post:
      summary: Start a background shell in the session worktree
      tags:
        - Sessions
      parameters:
        - schema:
            type: string
            description: Chat session id.
          required: true
          description: Chat session id.
          name: sessionId
          in: path
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                command:
                  type: string
                  description: Shell command run via `sh -c`.
                label:
                  type: string
                  description: Optional human label surfaced in the UI.
                watch:
                  type: boolean
                  description: Wake the session automatically when this shell finishes. Defaults to true; pass false to start an unwatched shell.
                timeoutMs:
                  type: integer
                  description: Wall-clock cap in milliseconds. Defaults to 1800000 (30 minutes). Values above 30 minutes are clamped; omitted/invalid values use the default. There is no way to disable the cap.
              required:
                - command
      responses:
        "201":
          description: Background shell started.
          content:
            application/json:
              schema:
                type: object
                properties:
                  shell:
                    $ref: "#/components/schemas/BackgroundShell"
                required:
                  - shell
        "400":
          description: Missing command or no directory to run in
        "404":
          description: Session not found
        "503":
          description: Background shells unavailable on this server
  /api/sessions/{sessionId}/background-shells/watch/cancel:
    post:
      summary: Cancel the session watch loop and stop its watched shells
      tags:
        - Sessions
      parameters:
        - schema:
            type: string
            description: Chat session id.
          required: true
          description: Chat session id.
          name: sessionId
          in: path
      responses:
        "200":
          description: Watch disarmed; any still-running watched shells were stopped.
          content:
            application/json:
              schema:
                type: object
                properties:
                  stopped:
                    type: integer
                    description: How many shells were stopped.
                  shells:
                    type: array
                    items:
                      $ref: "#/components/schemas/BackgroundShell"
                required:
                  - stopped
                  - shells
        "404":
          description: Session not found
        "503":
          description: Background shells unavailable on this server
  /api/sessions/{sessionId}/background-shells/{shellId}:
    get:
      summary: Get one background shell
      tags:
        - Sessions
      parameters:
        - schema:
            type: string
            description: Chat session id.
          required: true
          description: Chat session id.
          name: sessionId
          in: path
        - schema:
            type: string
            description: Background shell id.
          required: true
          description: Background shell id.
          name: shellId
          in: path
      responses:
        "200":
          description: Background shell.
          content:
            application/json:
              schema:
                type: object
                properties:
                  shell:
                    $ref: "#/components/schemas/BackgroundShell"
                required:
                  - shell
        "404":
          description: Session or shell not found
        "503":
          description: Background shells unavailable on this server
  /api/sessions/{sessionId}/background-shells/{shellId}/logs:
    get:
      summary: Get a background shell log tail
      tags:
        - Sessions
      parameters:
        - schema:
            type: string
            description: Chat session id.
          required: true
          description: Chat session id.
          name: sessionId
          in: path
        - schema:
            type: string
            description: Background shell id.
          required: true
          description: Background shell id.
          name: shellId
          in: path
        - schema:
            type: string
            description: Max number of most-recent log lines to return.
          required: false
          description: Max number of most-recent log lines to return.
          name: limit
          in: query
      responses:
        "200":
          description: Log tail (in-memory; empty for shells from a prior Hub process).
          content:
            application/json:
              schema:
                type: object
                properties:
                  shell:
                    $ref: "#/components/schemas/BackgroundShell"
                  logs:
                    type: array
                    items:
                      type: string
                required:
                  - shell
                  - logs
        "404":
          description: Session or shell not found
        "503":
          description: Background shells unavailable on this server
  /api/sessions/{sessionId}/background-shells/{shellId}/stop:
    post:
      summary: Stop a background shell
      tags:
        - Sessions
      parameters:
        - schema:
            type: string
            description: Chat session id.
          required: true
          description: Chat session id.
          name: sessionId
          in: path
        - schema:
            type: string
            description: Background shell id.
          required: true
          description: Background shell id.
          name: shellId
          in: path
      responses:
        "200":
          description: Background shell stopped (or already terminal).
          content:
            application/json:
              schema:
                type: object
                properties:
                  shell:
                    allOf:
                      - $ref: "#/components/schemas/BackgroundShell"
                      - nullable: true
                required:
                  - shell
        "404":
          description: Session or shell not found
        "503":
          description: Background shells unavailable on this server
  /api/projects/{projectId}/finalize/{runId}/reviewer-threads:
    get:
      tags:
        - Finalize
      summary: List reviewer threads for a Finalize run
      description: Returns every diff-anchored reviewer thread tied to the run, plus the run-level verdict. Read-only at v0 — replies happen in the originating session, not on the threads themselves.
      parameters:
        - schema:
            type: string
            description: Project slug or id.
          required: true
          description: Project slug or id.
          name: projectId
          in: path
        - schema:
            type: string
            description: finalize_runs.id.
          required: true
          description: finalize_runs.id.
          name: runId
          in: path
      responses:
        "200":
          description: Reviewer threads for the run.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ReviewerThreadsResponse"
        "404":
          description: Project not found, or finalize run not found in this project.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/FinalizeErrorResponse"
  /api/sessions/{sessionId}/finalize-runs/latest:
    get:
      tags:
        - Finalize
      summary: Most-recent Finalize run for a session
      description: 'Convenience lookup so the session-view side-panel can resolve a run id from a session id. Returns `{ run: null }` when the session has never triggered a Finalize run (never 404 — "no runs yet" is the normal first-load state).'
      parameters:
        - schema:
            type: string
            description: sessions.id.
          required: true
          description: sessions.id.
          name: sessionId
          in: path
        - schema:
            type: string
            enum:
              - "0"
              - "1"
              - "true"
              - "false"
              - yes
              - no
              - on
              - off
            description: Pass `0`/`false`/`no`/`off` to skip the `git rev-parse HEAD` spawn that computes `stale`/`currentHeadSha`. The web/mobile live-checks views pass `0` because they only read `run`/`steps`/`phases`; skipping the spawn removes it from a hot-path poll so queued step rows render immediately during a busy run. Defaults to on, so the agent CLI (`finalize.sh latest`) still gets staleness without changing its call.
          required: false
          description: Pass `0`/`false`/`no`/`off` to skip the `git rev-parse HEAD` spawn that computes `stale`/`currentHeadSha`. The web/mobile live-checks views pass `0` because they only read `run`/`steps`/`phases`; skipping the spawn removes it from a hot-path poll so queued step rows render immediately during a busy run. Defaults to on, so the agent CLI (`finalize.sh latest`) still gets staleness without changing its call.
          name: includeStale
          in: query
      responses:
        "200":
          description: Latest finalize_runs row for the session, or `null`.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LatestFinalizeRunForSessionResponse"
  /api/projects/{projectId}/cards/{cardId}/finalize:
    post:
      tags:
        - Finalize
      summary: Start a Finalize Code Changes run for a card
      description: |-
        Kicks off the Finalize pipeline against the card's linked session.
        Idempotent on (project, branch, head_sha) per §4 — clicking the button
        twice against the same HEAD returns the same row id.

        Returns 200 with the row id when the run was created (`reused: false`) or
        when an idempotency match returned a TERMINAL row (`reused: true`).
        Returns 409 when an idempotency match is non-terminal (a run is in flight).

        Requires session ownership — non-owners receive 404 to mask existence.
      parameters:
        - schema:
            type: string
          required: true
          name: projectId
          in: path
        - schema:
            type: string
          required: true
          name: cardId
          in: path
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/StartFinalizeRunRequest"
      responses:
        "200":
          description: Run created or terminal row reused.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/StartFinalizeRunResponse"
        "202":
          description: "Run started but the row was not visible within the synchronous poll window — clients should fall back to the WS event stream. The 202 body uses a distinct envelope (`StartFinalizeRunPending`) from the 200 path so OpenAPI consumers can discriminate by `run_id: null`."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/StartFinalizeRunPending"
        "400":
          description: Missing session linkage, missing worktree, missing branch, or HEAD SHA could not be resolved.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/FinalizeErrorResponse"
        "404":
          description: Project or card not found, or caller does not own the session.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/FinalizeErrorResponse"
        "409":
          description: A non-terminal Finalize run already exists for this (branch, head_sha).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/StartFinalizeRunInFlight"
        "410":
          description: "The request body included a `jobs` filter. Single-job Finalize runs were removed — Finalize always runs the full pipeline. Resend without `jobs`. Error code: `jobs_unsupported`."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/FinalizeErrorResponse"
  /api/projects/{projectId}/sessions/{sessionId}/finalize:
    post:
      tags:
        - Finalize
      summary: Start Finalize for a session (auto-creates kanban card)
      description: |-
        Same pipeline as the card-scoped finalize trigger, but accepts a session id
        directly. When the session has no linked kanban card, one is created on the
        project board (In Progress column, titled from the session name) before the
        run starts.
      parameters:
        - schema:
            type: string
          required: true
          name: projectId
          in: path
        - schema:
            type: string
          required: true
          name: sessionId
          in: path
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/StartFinalizeRunRequest"
      responses:
        "200":
          description: Run created or terminal row reused. Includes `card_id` and `card_created`.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/StartFinalizeRunResponse"
                  - type: object
                    properties:
                      card_id:
                        type: string
                      card_created:
                        type: boolean
                    required:
                      - card_id
                      - card_created
                description: A new or reused Finalize run.
        "202":
          description: Run started but row not yet visible.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/StartFinalizeRunPending"
                  - type: object
                    properties:
                      card_id:
                        type: string
                      card_created:
                        type: boolean
                    required:
                      - card_id
                      - card_created
                description: Fallback envelope returned when the orchestrator fired but the row insert was not visible within ~300ms. Distinct shape from `StartFinalizeRunResponse` so OpenAPI clients can branch on it.
        "400":
          description: Missing worktree, branch, or HEAD SHA.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/FinalizeErrorResponse"
        "404":
          description: Project or session not found, or caller does not own the session.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/FinalizeErrorResponse"
        "409":
          description: A non-terminal Finalize run already exists for this (branch, head_sha).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/StartFinalizeRunInFlight"
        "410":
          description: "The request body included a `jobs` filter. Single-job Finalize runs were removed — Finalize always runs the full pipeline. Resend without `jobs`. Error code: `jobs_unsupported`."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/FinalizeErrorResponse"
  /api/projects/{projectId}/finalize/metrics:
    get:
      tags:
        - Finalize
      summary: Aggregated Finalize adoption metrics for a project
      description: |-
        Counters and histogram summaries over the requested time window.
        See wiki `finalize-code-changes-architecture-v0` §14 for the metric
        vocabulary. No dashboard at v0 — the endpoint plus ad-hoc SQL is the
        dogfood-window read surface.

        Range syntax: `<N><m|h|d>` for a relative window ending now (e.g. `24h`,
        `7d`), or `<isoFrom>..<isoTo>` for an explicit half-open `[from, to)`
        interval (the `..` separator avoids collision with ISO8601 colons).
        Defaults to the last 24 hours.

        Optional `metrics=...` filter accepts a comma-separated subset of metric
        names. Unknown names are silently dropped; every requested known name
        is present in the response, even when zero rows landed.
      parameters:
        - schema:
            type: string
          required: true
          name: projectId
          in: path
        - schema:
            type: string
            description: Time window. See description.
          required: false
          description: Time window. See description.
          name: range
          in: query
        - schema:
            type: string
            description: "Comma-separated subset: finalize_run_started, finalize_run_completed, finalize_run_active_seconds, finalize_run_wall_seconds, finalize_fix_dispatch_count, finalize_reviewer_verdict, finalize_step_result, finalize_stalled_no_response_count, merged_pr_provenance, finalize_github_parity, finalize_job_peak_memory_bytes, finalize_job_cpu_percent."
          required: false
          description: "Comma-separated subset: finalize_run_started, finalize_run_completed, finalize_run_active_seconds, finalize_run_wall_seconds, finalize_fix_dispatch_count, finalize_reviewer_verdict, finalize_step_result, finalize_stalled_no_response_count, merged_pr_provenance, finalize_github_parity, finalize_job_peak_memory_bytes, finalize_job_cpu_percent."
          name: metrics
          in: query
      responses:
        "200":
          description: Aggregated metrics for the window.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/FinalizeMetricsResponse"
        "400":
          description: Range or metrics filter could not be parsed.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/FinalizeErrorResponse"
        "404":
          description: Project not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/FinalizeErrorResponse"
  /api/projects/{projectId}/finalize/{runId}/job-resources:
    get:
      tags:
        - Finalize
      summary: Per-job resource usage for a Finalize run
      description: Peak host memory and peak CPU each CI job reached during the run. One job per host on the fleet, so host memory == job memory. Empty `jobs` when no runner reported a summary (e.g. Hub-local runs or older runs).
      parameters:
        - schema:
            type: string
          required: true
          name: projectId
          in: path
        - schema:
            type: string
          required: true
          name: runId
          in: path
      responses:
        "200":
          description: Per-job resource high-water marks.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/FinalizeJobResourcesResponse"
        "404":
          description: Project not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/FinalizeErrorResponse"
        "500":
          description: Resource rows could not be read.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/FinalizeErrorResponse"
  /api/sessions/{sessionId}/finalize-ship-gate:
    get:
      tags:
        - Finalize
      summary: Check whether direct PR creation is allowed
      description: Returns whether spawned agents may run `gh pr create` for this session. Blocked when Finalize is configured and the run has not completed successfully.
      parameters:
        - schema:
            type: string
          required: true
          name: sessionId
          in: path
      responses:
        "200":
          description: Ship-gate decision.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/FinalizeShipGateResponse"
        "404":
          description: Session not found or not readable.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/FinalizeErrorResponse"
  /api/projects/{projectId}/finalize/{runId}/push:
    post:
      tags:
        - Finalize
      summary: Push to GitHub after checks pass
      description: Runs git push + gh pr create for a `ready_to_push` finalize run. Requires an explicit operator click — Finalize itself does not push.
      parameters:
        - schema:
            type: string
          required: true
          name: projectId
          in: path
        - schema:
            type: string
          required: true
          name: runId
          in: path
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/PushFinalizeRunRequest"
      responses:
        "200":
          description: PR opened.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PushFinalizeRunResponse"
        "404":
          description: Project, run, or session not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/FinalizeErrorResponse"
        "409":
          description: Run is not ready_to_push (unless force=true), or HEAD moved since checks passed.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/FinalizeErrorResponse"
        "502":
          description: GitHub push or PR creation failed.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/FinalizeErrorResponse"
  /api/projects/{projectId}/finalize/{runId}/steps/{stepIndex}/output:
    get:
      tags:
        - Finalize
      summary: CI step output log
      description: Returns streamed stdout/stderr lines captured for one CI step. Project-level push and PR CI logs follow project visibility; session-owned Finalize logs also require session access.
      parameters:
        - schema:
            type: string
          required: true
          name: projectId
          in: path
        - schema:
            type: string
          required: true
          name: runId
          in: path
        - schema:
            type: integer
            minimum: 1
          required: true
          name: stepIndex
          in: path
      responses:
        "200":
          description: Step log lines.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/FinalizeStepOutputResponse"
        "404":
          description: Project, run, or readable session not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/FinalizeErrorResponse"
  /api/projects/{projectId}/sessions/{sessionId}/push-to-github:
    post:
      tags:
        - Finalize
      summary: Push session work to GitHub
      description: "Operator-initiated push for a session worktree. Uses the latest finalize run when ready; `force: true` bypasses gate checks."
      parameters:
        - schema:
            type: string
          required: true
          name: projectId
          in: path
        - schema:
            type: string
          required: true
          name: sessionId
          in: path
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/PushFinalizeRunRequest"
      responses:
        "200":
          description: PR opened.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PushFinalizeRunResponse"
        "404":
          description: Project or session not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/FinalizeErrorResponse"
        "409":
          description: Finalize checks have not passed (confirm with force=true).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/FinalizeErrorResponse"
        "502":
          description: GitHub push or PR creation failed.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/FinalizeErrorResponse"
  /api/projects/{projectId}/finalize/{runId}/cancel:
    post:
      tags:
        - Finalize
      summary: Cancel an in-flight Finalize run
      description: |-
        Flips the row to `cancelled` and broadcasts `finalize_run_phase_changed`
        and `finalize_run_completed`. Trips the orchestrator's in-process
        `CancelSignal` (via the run-abort registry) so the fix-dispatch loop and any
        in-flight reviewer turn stop instead of dispatching another fix, kills the
        originating session's agent turn, and broadcasts an `interrupted` event so
        the session falls idle and waits for user input. The UI subscribes to the
        broadcast pair.
      parameters:
        - schema:
            type: string
          required: true
          name: projectId
          in: path
        - schema:
            type: string
          required: true
          name: runId
          in: path
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CancelFinalizeRunRequest"
      responses:
        "200":
          description: Run was flipped to cancelled.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CancelFinalizeRunResponse"
        "404":
          description: Project not found, run not found, run belongs to a different project, or caller does not own the linked session.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/FinalizeErrorResponse"
        "409":
          description: Run is already terminal.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CancelFinalizeRunTerminal"
  /api/projects/{projectId}/board:
    get:
      tags:
        - Board
      summary: Get full board state
      description: "Returns the project board, columns, cards (with blocker graph attached), epics, and a `counts` map of total cards per column. Idempotently creates the board and the default columns on first call. Bounded by default: `cards` carries only the first page per column (keyset-ordered by position, id), with a `cursors` map to resume paging — so the default response never serializes the whole board. Pass `?limit=N` to set the page size, or `?limit=all` to opt into the full unpaged board."
      parameters:
        - schema:
            type: string
            description: Project ID (slug).
          required: true
          description: Project ID (slug).
          name: projectId
          in: path
        - schema:
            type: string
            description: Per-column page size. Omitted → the default page size (50). A number N (1–200; out-of-range clamped) → first N cards per column, with a `cursors` map. The literal `all` → the full unpaged board (no `cursors`); its heavy card read is served off the event loop via the async reader pool.
          required: false
          description: Per-column page size. Omitted → the default page size (50). A number N (1–200; out-of-range clamped) → first N cards per column, with a `cursors` map. The literal `all` → the full unpaged board (no `cursors`); its heavy card read is served off the event loop via the async reader pool.
          name: limit
          in: query
      responses:
        "200":
          description: Board state.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BoardResponse"
        "404":
          description: Project not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BoardErrorResponse"
  /api/projects/{projectId}/board/columns/{columnId}/cards:
    get:
      tags:
        - Board
      summary: List one keyset-paginated page of a column's cards
      description: Returns a page of the column's cards ordered by (position, id), enriched with the blocker graph and latest finalize run, plus an opaque `nextCursor` (null on the last page) and the column `total`. Keyset pagination resumes strictly after the supplied `cursor`, so it is stable under mid-scroll reordering.
      parameters:
        - schema:
            type: string
            description: Project ID (slug).
          required: true
          description: Project ID (slug).
          name: projectId
          in: path
        - schema:
            type: string
            description: Kanban column UUID.
          required: true
          description: Kanban column UUID.
          name: columnId
          in: path
        - schema:
            type: string
            description: Page size (default 50, clamped to 1–200).
          required: false
          description: Page size (default 50, clamped to 1–200).
          name: limit
          in: query
        - schema:
            type: string
            description: Opaque cursor from a prior response's `nextCursor`. Omit for the first page.
          required: false
          description: Opaque cursor from a prior response's `nextCursor`. Omit for the first page.
          name: cursor
          in: query
      responses:
        "200":
          description: One page of cards.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PaginatedColumnCards"
        "400":
          description: Invalid cursor.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BoardErrorResponse"
        "404":
          description: Project or column not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BoardErrorResponse"
  /api/projects/{projectId}/board/cards:
    get:
      tags:
        - Board
      summary: List cards on a project board
      parameters:
        - schema:
            type: string
            description: Project ID (slug).
          required: true
          description: Project ID (slug).
          name: projectId
          in: path
      responses:
        "200":
          description: Array of cards.
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/KanbanCard"
        "404":
          description: Project not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BoardErrorResponse"
    post:
      tags:
        - Board
      summary: Create a kanban card
      description: "Creates a card in the given column. When `sessionId` is omitted from the body, the server auto-links from `X-Agent-Hub-Session-Id` or a per-session spawn-creds API key (`spawn:<sessionId>`). Pass `sessionId: null` to opt out. Returns the existing card unchanged if (a) a card with the same case-insensitive title already exists on this board, or (b) `sessionId` is already linked to a card on this board."
      parameters:
        - schema:
            type: string
            description: Project ID (slug).
          required: true
          description: Project ID (slug).
          name: projectId
          in: path
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                title:
                  type: string
                  minLength: 1
                columnId:
                  type: string
                  minLength: 1
                description:
                  type: string
                  nullable: true
                priority:
                  type: string
                  enum:
                    - low
                    - medium
                    - high
                    - urgent
                assignee:
                  type: string
                  nullable: true
                assignedUserId:
                  type: string
                  nullable: true
                labels:
                  type: string
                  nullable: true
                sessionId:
                  type: string
                  nullable: true
                  description: "Session to link the card to. Defaults to the caller session (X-Agent-Hub-Session-Id header / spawn creds) when omitted. If that session already owns a card on this board, the create is deduplicated and the existing card is returned with an `X-Agent-Hub-Card-Deduplicated: session` response header. Pass `null` explicitly to opt out and force a new, unlinked card."
                githubIssueUrl:
                  type: string
                  nullable: true
                createdBy:
                  type: string
                  nullable: true
                epicId:
                  type: string
                  nullable: true
                phaseId:
                  type: string
                  nullable: true
                source:
                  type: object
                  properties:
                    sourceType:
                      type: string
                      enum:
                        - manual
                        - email
                        - calendar
                        - todo
                        - log_issue
                    sourceId:
                      type: string
                      nullable: true
                    sourceMeta:
                      type: object
                      nullable: true
                      additionalProperties:
                        nullable: true
                  required:
                    - sourceType
                  description: Optional capture provenance stamped on the new card (spec CAPTURE-PROVENANCE). Records the Gmail message / Calendar event / todo the card was captured from; `sourceMeta` preserves a deep link back to it.
              required:
                - title
                - columnId
      responses:
        "200":
          description: Card.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/KanbanCard"
        "400":
          description: Validation failed.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BoardErrorResponse"
        "404":
          description: Project or column not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BoardErrorResponse"
  /api/projects/{projectId}/board/cards/{cardId}/replay:
    get:
      tags:
        - Board
      summary: Get the session replay attributed to a card
      description: Returns a pointer to the rrweb session replay linked to the card (via session_replays.card_id, e.g. carried over when a bug support ticket was converted). The client uses `replayId` to drive the sandboxed rrweb-player playback surface against /api/replays/:id. 404 when the card has no replay.
      parameters:
        - schema:
            type: string
            description: Project ID (slug).
          required: true
          description: Project ID (slug).
          name: projectId
          in: path
        - schema:
            type: string
            description: Kanban card UUID.
          required: true
          description: Kanban card UUID.
          name: cardId
          in: path
      responses:
        "200":
          description: Replay pointer.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CardReplay"
        "404":
          description: Project, card, or replay not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BoardErrorResponse"
  /api/projects/{projectId}/board/cards/{cardId}:
    put:
      tags:
        - Board
      summary: Update a kanban card
      description: Partial update. Fields omitted from the body keep their current value. Nullable fields can be cleared by sending `null` explicitly.
      parameters:
        - schema:
            type: string
            description: Project ID (slug).
          required: true
          description: Project ID (slug).
          name: projectId
          in: path
        - schema:
            type: string
            description: Kanban card UUID.
          required: true
          description: Kanban card UUID.
          name: cardId
          in: path
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                title:
                  type: string
                  minLength: 1
                description:
                  type: string
                  nullable: true
                priority:
                  type: string
                  enum:
                    - low
                    - medium
                    - high
                    - urgent
                assignee:
                  type: string
                  nullable: true
                assignedUserId:
                  type: string
                  nullable: true
                labels:
                  type: string
                  nullable: true
                sessionId:
                  type: string
                  nullable: true
                githubIssueUrl:
                  type: string
                  nullable: true
                prUrl:
                  type: string
                  nullable: true
                epicId:
                  type: string
                  nullable: true
                phaseId:
                  type: string
                  nullable: true
                assignModel:
                  type: string
                  nullable: true
                assignEngine:
                  type: string
                  nullable: true
                  description: Optional engine override for the session spawn. One of "claude-code", "cursor-agent", "gemini-cli", or "codex-cli". When set, overrides the assignee agent's shared engine.
                prBaseBranch:
                  type: string
                  nullable: true
      responses:
        "200":
          description: Updated card.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/KanbanCard"
        "400":
          description: Validation failed.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BoardErrorResponse"
        "404":
          description: Card, project, or referenced epic not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BoardErrorResponse"
  /api/projects/{projectId}/board/cards/{cardId}/move:
    post:
      tags:
        - Board
      summary: Move a card to another column / position
      description: "Soft-enforced: blocker state does NOT gate the move. Clients should consult `blockers` on the GET /board response and warn before issuing the move."
      parameters:
        - schema:
            type: string
            description: Project ID (slug).
          required: true
          description: Project ID (slug).
          name: projectId
          in: path
        - schema:
            type: string
            description: Kanban card UUID.
          required: true
          description: Kanban card UUID.
          name: cardId
          in: path
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                columnId:
                  type: string
                  minLength: 1
                position:
                  type: integer
                force:
                  type: boolean
                  description: "Operator escape hatch: bypass the premature-Done guard. Moves into a Done column are rejected with 409 while the linked session is Finalize-gated and has not pushed yet — Done is written on merge by the platform."
              required:
                - columnId
      responses:
        "200":
          description: Updated card.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/KanbanCard"
        "400":
          description: Validation failed.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BoardErrorResponse"
        "404":
          description: Card not found, or target column not on this card's board.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BoardErrorResponse"
        "409":
          description: "Premature Done move rejected: the linked session is Finalize-gated and has not pushed yet. Done is written on merge; pass `force: true` to override."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BoardErrorResponse"
        "422":
          description: "Done-state contract violation: a [Spec]/[Partial]-titled card cannot move to Done without a comment listing the follow-up card IDs covering the unmet acceptance criteria. Pass `force: true` to override."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BoardErrorResponse"
  /api/projects/{projectId}/board/cards/{cardId}/assign:
    post:
      tags:
        - Board
      summary: Assign a card to an agent and spawn a session
      description: Picks the agent, mints a new session linked to the card, optionally overrides the model (must be valid for the agent's engine), moves the card to "In Progress", and dispatches the agent with a task-context prompt.
      parameters:
        - schema:
            type: string
            description: Project ID (slug).
          required: true
          description: Project ID (slug).
          name: projectId
          in: path
        - schema:
            type: string
            description: Kanban card UUID.
          required: true
          description: Kanban card UUID.
          name: cardId
          in: path
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                agentId:
                  type: string
                  minLength: 1
                model:
                  type: string
                  nullable: true
                engine:
                  type: string
                  nullable: true
                  description: Optional engine override. One of "claude-code", "cursor-agent", "gemini-cli", or "codex-cli". When set, the spawned session uses this engine instead of the assignee agent's shared engine. Validated against the server's engineValidModels — unknown engines yield 400.
                autoMerge:
                  type: boolean
                  nullable: true
                  description: Per-card auto-merge override. true → the spawned session runs at "Auto Merge" (build/review/test/push + GitHub auto-merge); false → "Build and Push" (no auto-merge). Persisted on the card. When omitted, falls back to the card's stored preference (e.g. carried over from a converted support ticket), then the project's githubWorkflow.autoMerge default.
                comment:
                  type: string
                  nullable: true
                  maxLength: 4000
                  description: Optional assignment note (max 4000 characters). Recorded as a comment on the card and prepended to the agent's task context so the assignee sees the instructions.
              required:
                - agentId
      responses:
        "200":
          description: Session id + updated card.
          content:
            application/json:
              schema:
                type: object
                properties:
                  sessionId:
                    type: string
                  card:
                    $ref: "#/components/schemas/KanbanCard"
                required:
                  - sessionId
                  - card
        "400":
          description: Validation failed or model invalid for engine.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BoardErrorResponse"
        "404":
          description: Card, agent, or project not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BoardErrorResponse"
  /api/projects/{projectId}/board/cards/{cardId}/comments:
    get:
      tags:
        - Board
      summary: List comments on a card
      parameters:
        - schema:
            type: string
            description: Project ID (slug).
          required: true
          description: Project ID (slug).
          name: projectId
          in: path
        - schema:
            type: string
            description: Kanban card UUID.
          required: true
          description: Kanban card UUID.
          name: cardId
          in: path
      responses:
        "200":
          description: Array of comments (oldest first).
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/KanbanCardComment"
    post:
      tags:
        - Board
      summary: Post a comment to a card
      parameters:
        - schema:
            type: string
            description: Project ID (slug).
          required: true
          description: Project ID (slug).
          name: projectId
          in: path
        - schema:
            type: string
            description: Kanban card UUID.
          required: true
          description: Kanban card UUID.
          name: cardId
          in: path
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                author:
                  type: string
                  minLength: 1
                content:
                  type: string
                  minLength: 1
                  description: "Comment text. Alias: `body`."
              required:
                - author
                - content
      responses:
        "200":
          description: Updated comment list.
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/KanbanCardComment"
        "400":
          description: Validation failed.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BoardErrorResponse"
        "404":
          description: Card not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BoardErrorResponse"
  /api/projects/{projectId}/board/cards/{cardId}/comments/{commentId}:
    delete:
      tags:
        - Board
      summary: Delete a card comment
      parameters:
        - schema:
            type: string
            description: Project ID (slug).
          required: true
          description: Project ID (slug).
          name: projectId
          in: path
        - schema:
            type: string
            description: Kanban card UUID.
          required: true
          description: Kanban card UUID.
          name: cardId
          in: path
        - schema:
            type: string
            description: Comment UUID.
          required: true
          description: Comment UUID.
          name: commentId
          in: path
      responses:
        "200":
          description: Acknowledgment.
          content:
            application/json:
              schema:
                type: object
                properties:
                  ok:
                    type: boolean
                    enum:
                      - true
                required:
                  - ok
  /api/projects/{projectId}/board/cards/{cardId}/blockers:
    post:
      tags:
        - Board
      summary: Declare that this card is blocked by another card
      description: Both cards must live on the same project board. Adding the edge is rejected when it would close a cycle (`409 cycle`, with `path` naming the cycle) or when the edge already exists (`409 duplicate`).
      parameters:
        - schema:
            type: string
            description: Project ID (slug).
          required: true
          description: Project ID (slug).
          name: projectId
          in: path
        - schema:
            type: string
            description: Kanban card UUID.
          required: true
          description: Kanban card UUID.
          name: cardId
          in: path
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                blockedByCardId:
                  type: string
                  minLength: 1
              required:
                - blockedByCardId
      responses:
        "201":
          description: New blocker link.
          content:
            application/json:
              schema:
                type: object
                properties:
                  id:
                    type: string
                  card_id:
                    type: string
                  blocked_by_card_id:
                    type: string
                required:
                  - id
                  - card_id
                  - blocked_by_card_id
        "400":
          description: Validation failed or self-block attempted.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BoardErrorResponse"
        "404":
          description: Card not found (either side).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BoardErrorResponse"
        "409":
          description: Duplicate edge or cycle.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BoardErrorResponse"
  /api/projects/{projectId}/board/cards/{cardId}/blockers/{blockedByCardId}:
    delete:
      tags:
        - Board
      summary: Remove a blocker edge
      parameters:
        - schema:
            type: string
            description: Project ID (slug).
          required: true
          description: Project ID (slug).
          name: projectId
          in: path
        - schema:
            type: string
            description: Kanban card UUID.
          required: true
          description: Kanban card UUID.
          name: cardId
          in: path
        - schema:
            type: string
            description: The blocker card UUID to remove.
          required: true
          description: The blocker card UUID to remove.
          name: blockedByCardId
          in: path
      responses:
        "204":
          description: Edge removed.
        "404":
          description: Blocker link not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BoardErrorResponse"
  /api/projects/{projectId}/board/epics:
    get:
      tags:
        - Board
      summary: List features on a project board
      parameters:
        - schema:
            type: string
            description: Project ID (slug).
          required: true
          description: Project ID (slug).
          name: projectId
          in: path
      responses:
        "200":
          description: Array of features.
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/KanbanEpic"
    post:
      tags:
        - Board
      summary: Create a feature
      parameters:
        - schema:
            type: string
            description: Project ID (slug).
          required: true
          description: Project ID (slug).
          name: projectId
          in: path
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                name:
                  type: string
                  minLength: 1
                description:
                  type: string
                  nullable: true
                labels:
                  type: string
                  nullable: true
                assignedUserId:
                  type: string
                  nullable: true
                color:
                  type: string
                prBaseBranch:
                  type: string
                  nullable: true
              required:
                - name
      responses:
        "200":
          description: New feature.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/KanbanEpic"
        "400":
          description: Validation failed.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BoardErrorResponse"
  /api/projects/{projectId}/board/epics/{epicId}/pulls:
    get:
      tags:
        - Board
      summary: List pull requests related to a feature's branch
      description: "Native (Agent Hub-hosted) PRs whose base branch merges into, or whose head branch ships, the epic's `pr_base_branch`. GitHub-repo projects return an empty list with `source: 'github'`."
      parameters:
        - schema:
            type: string
            description: Project ID (slug).
          required: true
          description: Project ID (slug).
          name: projectId
          in: path
        - schema:
            type: string
            description: Kanban epic UUID.
          required: true
          description: Kanban epic UUID.
          name: epicId
          in: path
      responses:
        "200":
          description: Related pull requests for the feature branch.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EpicPullsResponse"
        "404":
          description: Project or feature not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BoardErrorResponse"
  /api/projects/{projectId}/board/epics/{epicId}:
    put:
      tags:
        - Board
      summary: Update a feature
      description: Partial update. Setting `autonomous` to 1 on this epic clears the autonomous flag on any other autonomous epic on the same board (only one epic can be autonomous at a time).
      parameters:
        - schema:
            type: string
            description: Project ID (slug).
          required: true
          description: Project ID (slug).
          name: projectId
          in: path
        - schema:
            type: string
            description: Kanban epic UUID.
          required: true
          description: Kanban epic UUID.
          name: epicId
          in: path
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                name:
                  type: string
                  minLength: 1
                description:
                  type: string
                  nullable: true
                labels:
                  type: string
                  nullable: true
                assignedUserId:
                  type: string
                  nullable: true
                color:
                  type: string
                autonomous:
                  type: integer
                autonomousInterval:
                  type: integer
                autonomousMaxConcurrent:
                  type: integer
                autonomousModel:
                  type: string
                  nullable: true
                autonomousSendIt:
                  type: integer
                  enum:
                    - 0
                    - 1
                orchestrationBudgets:
                  type: object
                  description: Optional partial overrides merged onto the project orchestrationBudgets at run time.
                prBaseBranch:
                  type: string
                  nullable: true
      responses:
        "200":
          description: Updated feature.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/KanbanEpic"
        "400":
          description: Validation failed.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BoardErrorResponse"
        "404":
          description: Feature not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BoardErrorResponse"
    delete:
      tags:
        - Board
      summary: Delete a feature
      description: Unlinks every card that belonged to the feature, then removes the epic row.
      parameters:
        - schema:
            type: string
            description: Project ID (slug).
          required: true
          description: Project ID (slug).
          name: projectId
          in: path
        - schema:
            type: string
            description: Kanban epic UUID.
          required: true
          description: Kanban epic UUID.
          name: epicId
          in: path
      responses:
        "200":
          description: Acknowledgment.
          content:
            application/json:
              schema:
                type: object
                properties:
                  ok:
                    type: boolean
                    enum:
                      - true
                required:
                  - ok
  /api/projects/{projectId}/board/epics/{epicId}/assign-lead-to-cards:
    post:
      tags:
        - Board
      summary: Assign epic lead user to all epic cards
      description: Sets `assigned_user_id` on every card linked to the epic to the epic’s current lead user. Callable by the lead user, or in local bundled mode.
      parameters:
        - schema:
            type: string
            description: Project ID (slug).
          required: true
          description: Project ID (slug).
          name: projectId
          in: path
        - schema:
            type: string
            description: Kanban epic UUID.
          required: true
          description: Kanban epic UUID.
          name: epicId
          in: path
      responses:
        "200":
          description: Bulk assignment result.
          content:
            application/json:
              schema:
                type: object
                properties:
                  updatedCount:
                    type: integer
                    minimum: 0
                required:
                  - updatedCount
        "400":
          description: Epic has no lead user.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BoardErrorResponse"
        "403":
          description: Caller is not the epic lead user.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BoardErrorResponse"
        "404":
          description: Epic not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BoardErrorResponse"
  /api/projects/{projectId}/board/card-templates:
    get:
      tags:
        - Board
      summary: List card templates
      description: Reusable defaults for creating kanban cards on this board.
      parameters:
        - schema:
            type: string
            description: Project ID (slug).
          required: true
          description: Project ID (slug).
          name: projectId
          in: path
      responses:
        "200":
          description: Templates for the board.
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/KanbanCardTemplate"
    post:
      tags:
        - Board
      summary: Create a card template
      parameters:
        - schema:
            type: string
            description: Project ID (slug).
          required: true
          description: Project ID (slug).
          name: projectId
          in: path
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                name:
                  type: string
                  minLength: 1
                title:
                  type: string
                description:
                  type: string
                  nullable: true
                priority:
                  type: string
                  enum:
                    - low
                    - medium
                    - high
                    - urgent
                labels:
                  type: string
                  nullable: true
                epicId:
                  type: string
                  nullable: true
              required:
                - name
      responses:
        "200":
          description: Created template.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/KanbanCardTemplate"
        "400":
          description: Validation failed.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BoardErrorResponse"
  /api/projects/{projectId}/board/card-templates/{templateId}:
    put:
      tags:
        - Board
      summary: Update a card template
      parameters:
        - schema:
            type: string
            description: Project ID (slug).
          required: true
          description: Project ID (slug).
          name: projectId
          in: path
        - schema:
            type: string
            description: Kanban card template UUID.
          required: true
          description: Kanban card template UUID.
          name: templateId
          in: path
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                name:
                  type: string
                  minLength: 1
                title:
                  type: string
                description:
                  type: string
                  nullable: true
                priority:
                  type: string
                  enum:
                    - low
                    - medium
                    - high
                    - urgent
                labels:
                  type: string
                  nullable: true
                epicId:
                  type: string
                  nullable: true
      responses:
        "200":
          description: Updated template.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/KanbanCardTemplate"
        "400":
          description: Validation failed.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BoardErrorResponse"
        "404":
          description: Template not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BoardErrorResponse"
    delete:
      tags:
        - Board
      summary: Delete a card template
      parameters:
        - schema:
            type: string
            description: Project ID (slug).
          required: true
          description: Project ID (slug).
          name: projectId
          in: path
        - schema:
            type: string
            description: Kanban card template UUID.
          required: true
          description: Kanban card template UUID.
          name: templateId
          in: path
      responses:
        "200":
          description: Acknowledgment.
          content:
            application/json:
              schema:
                type: object
                properties:
                  ok:
                    type: boolean
                    enum:
                      - true
                required:
                  - ok
        "404":
          description: Template not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BoardErrorResponse"
  /api/projects/{projectId}/board/cards/{cardId}/epic:
    post:
      tags:
        - Board
      summary: Link or unlink a card to an epic
      description: 'Pass `{ epicId: "<id>" }` to link. Pass an empty body or `{ epicId: null }` to unlink.'
      parameters:
        - schema:
            type: string
            description: Project ID (slug).
          required: true
          description: Project ID (slug).
          name: projectId
          in: path
        - schema:
            type: string
            description: Kanban card UUID.
          required: true
          description: Kanban card UUID.
          name: cardId
          in: path
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                epicId:
                  type: string
                  nullable: true
      responses:
        "200":
          description: Updated card.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/KanbanCard"
        "400":
          description: Validation failed.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BoardErrorResponse"
        "404":
          description: Card or epic not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BoardErrorResponse"
  /api/projects/{projectId}/board/columns:
    post:
      tags:
        - Board
      summary: Create a column
      parameters:
        - schema:
            type: string
            description: Project ID (slug).
          required: true
          description: Project ID (slug).
          name: projectId
          in: path
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                name:
                  type: string
                  minLength: 1
                color:
                  type: string
                  nullable: true
              required:
                - name
      responses:
        "200":
          description: Updated columns list.
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/KanbanColumn"
        "400":
          description: Validation failed.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BoardErrorResponse"
        "404":
          description: Project not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BoardErrorResponse"
  /api/projects/{projectId}/board/scope-from-notes:
    post:
      tags:
        - Board
      summary: Open a scoping-mode session seeded with note content
      parameters:
        - schema:
            type: string
          required: true
          name: projectId
          in: path
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                content:
                  type: string
                  minLength: 1
                  description: The note content to scope — a whole note or a single heading-scoped block of markdown.
                title:
                  type: string
                  description: Human label for the session (e.g. the note title or heading text).
                agentId:
                  type: string
                  minLength: 1
                  description: Agent to run the scoping session. Defaults to the project lead.
              required:
                - content
      responses:
        "200":
          description: Scoping session created and seeded with the note content.
          content:
            application/json:
              schema:
                type: object
                properties:
                  sessionId:
                    type: string
                  agentId:
                    type: string
                required:
                  - sessionId
                  - agentId
        "400":
          description: No agent available or agent does not belong to this project.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BoardErrorResponse"
        "401":
          description: Authentication required.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BoardErrorResponse"
        "404":
          description: Project not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BoardErrorResponse"
  /api/projects/{projectId}/board/epics/{epicId}/scope:
    post:
      tags:
        - Board
      summary: Open a scoping-mode session pre-linked to an epic
      parameters:
        - schema:
            type: string
          required: true
          name: projectId
          in: path
        - schema:
            type: string
          required: true
          name: epicId
          in: path
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                agentId:
                  type: string
                  minLength: 1
                  description: Agent to run the scoping session. Defaults to the project lead.
      responses:
        "200":
          description: Scoping session created and linked to the epic.
          content:
            application/json:
              schema:
                type: object
                properties:
                  sessionId:
                    type: string
                  agentId:
                    type: string
                required:
                  - sessionId
                  - agentId
        "400":
          description: No agent available or agent does not belong to this project.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BoardErrorResponse"
        "401":
          description: Authentication required.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BoardErrorResponse"
        "404":
          description: Epic not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BoardErrorResponse"
  /api/projects/{projectId}/board/columns/{columnId}:
    put:
      tags:
        - Board
      summary: Update a column
      parameters:
        - schema:
            type: string
            description: Project ID (slug).
          required: true
          description: Project ID (slug).
          name: projectId
          in: path
        - schema:
            type: string
            description: Kanban column UUID.
          required: true
          description: Kanban column UUID.
          name: columnId
          in: path
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                name:
                  type: string
                position:
                  type: integer
                color:
                  type: string
                  nullable: true
      responses:
        "200":
          description: Acknowledgment.
          content:
            application/json:
              schema:
                type: object
                properties:
                  ok:
                    type: boolean
                    enum:
                      - true
                required:
                  - ok
        "400":
          description: Validation failed.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BoardErrorResponse"
        "404":
          description: Project not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BoardErrorResponse"
    delete:
      tags:
        - Board
      summary: Delete a column
      parameters:
        - schema:
            type: string
            description: Project ID (slug).
          required: true
          description: Project ID (slug).
          name: projectId
          in: path
        - schema:
            type: string
            description: Kanban column UUID.
          required: true
          description: Kanban column UUID.
          name: columnId
          in: path
      responses:
        "200":
          description: Acknowledgment.
          content:
            application/json:
              schema:
                type: object
                properties:
                  ok:
                    type: boolean
                    enum:
                      - true
                required:
                  - ok
  /api/projects/{projectId}/board/columns/reorder:
    post:
      tags:
        - Board
      summary: Reorder columns atomically
      parameters:
        - schema:
            type: string
            description: Project ID (slug).
          required: true
          description: Project ID (slug).
          name: projectId
          in: path
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                columnIds:
                  type: array
                  items:
                    type: string
                    minLength: 1
                  minItems: 1
                  description: All column ids for the board in the desired order. The server rewrites positions atomically to 0..N-1.
              required:
                - columnIds
      responses:
        "200":
          description: Updated columns list.
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/KanbanColumn"
        "400":
          description: Validation failed.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BoardErrorResponse"
        "404":
          description: Project not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BoardErrorResponse"
  /api/projects/{projectId}/board/spec-items/{specItemId}/decide-for-me:
    post:
      tags:
        - Board
      summary: Research and lock a spec decision via agent session
      parameters:
        - schema:
            type: string
          required: true
          name: projectId
          in: path
        - schema:
            type: string
          required: true
          name: specItemId
          in: path
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                agentId:
                  type: string
                  minLength: 1
                  description: Agent to run the research session. Defaults to the project lead.
      responses:
        "200":
          description: Scoping session started.
          content:
            application/json:
              schema:
                type: object
                properties:
                  sessionId:
                    type: string
                  agentId:
                    type: string
                  specItem:
                    $ref: "#/components/schemas/KanbanEpicSpecItem"
                required:
                  - sessionId
                  - agentId
                  - specItem
        "400":
          description: Spec item already locked or validation failed.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BoardErrorResponse"
        "404":
          description: Spec item not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BoardErrorResponse"
  /api/projects/{projectId}/board/phases:
    get:
      tags:
        - Board
      summary: List phases on the board
      parameters:
        - schema:
            type: string
            description: Project ID (slug).
          required: true
          description: Project ID (slug).
          name: projectId
          in: path
      responses:
        "200":
          description: All phases on the board, ordered by position within their epic.
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/KanbanPhase"
    post:
      tags:
        - Board
      summary: Create a phase under an epic
      parameters:
        - schema:
            type: string
            description: Project ID (slug).
          required: true
          description: Project ID (slug).
          name: projectId
          in: path
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                epicId:
                  type: string
                  minLength: 1
                name:
                  type: string
                  minLength: 1
                description:
                  type: string
                  nullable: true
                agentId:
                  type: string
                  minLength: 1
                  description: Optional agent whose effective default model seeds autonomousModel when autonomousModel is omitted.
                autonomousModel:
                  type: string
                  nullable: true
                  description: Optional phase autonomous model. Omit to seed from the selected agent and session owner when agentId is supplied; otherwise the phase model remains unset. Pass null to leave the phase model unset.
              required:
                - epicId
                - name
      responses:
        "200":
          description: The created phase.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/KanbanPhase"
        "404":
          description: Epic not found on this board.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BoardErrorResponse"
  /api/projects/{projectId}/board/phases/{phaseId}:
    put:
      tags:
        - Board
      summary: Update a phase (incl. autonomous settings)
      parameters:
        - schema:
            type: string
            description: Project ID (slug).
          required: true
          description: Project ID (slug).
          name: projectId
          in: path
        - schema:
            type: string
            description: Kanban phase UUID.
          required: true
          description: Kanban phase UUID.
          name: phaseId
          in: path
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                name:
                  type: string
                  minLength: 1
                description:
                  type: string
                  nullable: true
                autonomous:
                  type: integer
                autonomousInterval:
                  type: integer
                autonomousMaxConcurrent:
                  type: integer
                autonomousModel:
                  type: string
                  nullable: true
                autonomousSendIt:
                  type: integer
                  enum:
                    - 0
                    - 1
      responses:
        "200":
          description: The updated phase.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/KanbanPhase"
        "404":
          description: Phase not found on this project board.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BoardErrorResponse"
    delete:
      tags:
        - Board
      summary: Delete a phase (unlinks its cards)
      parameters:
        - schema:
            type: string
            description: Project ID (slug).
          required: true
          description: Project ID (slug).
          name: projectId
          in: path
        - schema:
            type: string
            description: Kanban phase UUID.
          required: true
          description: Kanban phase UUID.
          name: phaseId
          in: path
      responses:
        "200":
          description: Acknowledgment.
          content:
            application/json:
              schema:
                type: object
                properties:
                  ok:
                    type: boolean
                    enum:
                      - true
                required:
                  - ok
        "404":
          description: Phase not found on this project board.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BoardErrorResponse"
  /api/projects/{projectId}/board/phases/{phaseId}/run:
    post:
      tags:
        - Board
      summary: Start autonomous dispatch for a phase
      parameters:
        - schema:
            type: string
            description: Project ID (slug).
          required: true
          description: Project ID (slug).
          name: projectId
          in: path
        - schema:
            type: string
            description: Kanban phase UUID.
          required: true
          description: Kanban phase UUID.
          name: phaseId
          in: path
      responses:
        "200":
          description: The phase, now running.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/KanbanPhase"
        "400":
          description: Phase could not be started.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BoardErrorResponse"
  /api/projects/{projectId}/board/phases/{phaseId}/stop:
    post:
      tags:
        - Board
      summary: Stop autonomous dispatch for a phase
      parameters:
        - schema:
            type: string
            description: Project ID (slug).
          required: true
          description: Project ID (slug).
          name: projectId
          in: path
        - schema:
            type: string
            description: Kanban phase UUID.
          required: true
          description: Kanban phase UUID.
          name: phaseId
          in: path
      responses:
        "200":
          description: The phase, now stopped.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/KanbanPhase"
        "400":
          description: Phase could not be stopped.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BoardErrorResponse"
  /api/projects/{projectId}/board/phases/reorder:
    post:
      tags:
        - Board
      summary: Reorder an epic’s phases (explicit order or auto topological sort)
      parameters:
        - schema:
            type: string
            description: Project ID (slug).
          required: true
          description: Project ID (slug).
          name: projectId
          in: path
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                epicId:
                  type: string
                  minLength: 1
                phaseIds:
                  type: array
                  items:
                    type: string
                    minLength: 1
                  minItems: 1
                  description: All phase ids for the epic in the desired order. The server rewrites positions atomically to 0..N-1. Provide this OR sortByDependencies, not both.
                sortByDependencies:
                  type: boolean
                  description: When true, the server derives the order from the epic's card blocker graph (prerequisites first) and rewrites positions. Returns 409 `cycle` if the phase dependency graph has a loop. Provide this OR phaseIds, not both.
              required:
                - epicId
      responses:
        "200":
          description: The epic's phases in their new order.
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/KanbanPhase"
        "400":
          description: Validation failed.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BoardErrorResponse"
        "404":
          description: Project or epic not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BoardErrorResponse"
        "409":
          description: Phase dependency graph has a cycle (auto sort only).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BoardErrorResponse"
  /api/projects/{projectId}/board/spec-items:
    post:
      tags:
        - Board
      summary: Create an epic spec item (optionally already chosen)
      parameters:
        - schema:
            type: string
            description: Project ID (slug).
          required: true
          description: Project ID (slug).
          name: projectId
          in: path
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                epicId:
                  type: string
                  minLength: 1
                tag:
                  type: string
                  minLength: 1
                title:
                  type: string
                  minLength: 1
                decision:
                  type: string
                  nullable: true
                phaseId:
                  type: string
                  nullable: true
                status:
                  type: string
                  enum:
                    - open
                    - chosen
                    - deferred
                  description: Initial status. `chosen` requires a non-empty `decision`; defaults to `open`.
                createSpikeCard:
                  type: boolean
                  description: When true, also create a legacy spike kanban ticket linked to this spec item. Default false — use Decide for me or write the decision directly.
              required:
                - epicId
                - tag
                - title
      responses:
        "200":
          description: The created spec item.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/KanbanEpicSpecItem"
        "400":
          description: Validation failed (e.g. chosen without a decision, or a foreign phase).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BoardErrorResponse"
        "404":
          description: Epic not found on this board.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BoardErrorResponse"
  /api/projects/{projectId}/board/spec-items/{specItemId}:
    put:
      tags:
        - Board
      summary: Update an epic spec item
      parameters:
        - schema:
            type: string
            description: Project ID (slug).
          required: true
          description: Project ID (slug).
          name: projectId
          in: path
        - schema:
            type: string
            description: Epic spec item UUID.
          required: true
          description: Epic spec item UUID.
          name: specItemId
          in: path
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                tag:
                  type: string
                  minLength: 1
                title:
                  type: string
                  minLength: 1
                decision:
                  type: string
                  nullable: true
                status:
                  type: string
                  enum:
                    - open
                    - chosen
                    - deferred
                phaseId:
                  type: string
                  nullable: true
                position:
                  type: integer
                resolvedSessionId:
                  type: string
                  nullable: true
      responses:
        "200":
          description: The updated spec item.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/KanbanEpicSpecItem"
        "400":
          description: Validation failed (e.g. chosen without a decision, or a foreign phase).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BoardErrorResponse"
        "404":
          description: Spec item not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BoardErrorResponse"
    delete:
      tags:
        - Board
      summary: Delete an epic spec item
      parameters:
        - schema:
            type: string
            description: Project ID (slug).
          required: true
          description: Project ID (slug).
          name: projectId
          in: path
        - schema:
            type: string
            description: Epic spec item UUID.
          required: true
          description: Epic spec item UUID.
          name: specItemId
          in: path
      responses:
        "200":
          description: Acknowledgment.
          content:
            application/json:
              schema:
                type: object
                properties:
                  ok:
                    type: boolean
                    enum:
                      - true
                required:
                  - ok
        "404":
          description: Spec item not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BoardErrorResponse"
  /api/projects/{projectId}/board/spec-items/{specItemId}/spike:
    post:
      tags:
        - Board
      summary: Create (or return) the spike card for a spec item
      parameters:
        - schema:
            type: string
            description: Project ID (slug).
          required: true
          description: Project ID (slug).
          name: projectId
          in: path
        - schema:
            type: string
            description: Epic spec item UUID.
          required: true
          description: Epic spec item UUID.
          name: specItemId
          in: path
      responses:
        "200":
          description: The spec item and its linked spike card.
          content:
            application/json:
              schema:
                type: object
                properties:
                  specItem:
                    $ref: "#/components/schemas/KanbanEpicSpecItem"
                  spikeCard:
                    $ref: "#/components/schemas/KanbanCard"
                required:
                  - specItem
                  - spikeCard
        "404":
          description: Spec item not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BoardErrorResponse"
        "500":
          description: Failed to create the spike card.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BoardErrorResponse"
  /api/agents/{agentId}/sessions:
    get:
      tags:
        - Sessions
      summary: List sessions for an agent (owner-filtered)
      description: Returns only sessions the caller owns. Pre-Phase-4 NULL-owner rows fall through to org-owner ownership.
      parameters:
        - schema:
            type: string
            description: Agent ID.
          required: true
          description: Agent ID.
          name: agentId
          in: path
      responses:
        "200":
          description: Array of sessions.
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Session"
    post:
      tags:
        - Sessions
      summary: Create a new session for an agent
      description: Defaults the engine and model to the agent's configured engine/model. Sessions are always created with a per-session git worktree (the legacy shared-checkout option was removed when Agent Hub locked to worktree-only sessions).
      parameters:
        - schema:
            type: string
            description: Agent ID.
          required: true
          description: Agent ID.
          name: agentId
          in: path
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                name:
                  type: string
                engine:
                  type: string
                model:
                  type: string
                ask_mode:
                  type: boolean
                session_mode:
                  type: string
                  enum:
                    - chat
                    - isolated
                    - design
                    - scoping
                    - skill-builder
                    - consult
                    - hub
      responses:
        "200":
          description: New session.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Session"
        "400":
          description: Validation failed.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SessionErrorResponse"
    delete:
      tags:
        - Sessions
      summary: Bulk soft-delete (archive) every session for an agent
      parameters:
        - schema:
            type: string
            description: Agent ID.
          required: true
          description: Agent ID.
          name: agentId
          in: path
      responses:
        "200":
          description: Archive result. `ok` is false when any teardown failed; `archivedIds` is the authoritative removed set.
          content:
            application/json:
              schema:
                type: object
                properties:
                  ok:
                    type: boolean
                  archived:
                    type: integer
                  deleted:
                    type: integer
                  failed:
                    type: integer
                  archivedIds:
                    type: array
                    items:
                      type: string
                required:
                  - ok
                  - archived
                  - deleted
                  - failed
                  - archivedIds
        "500":
          description: Every candidate teardown failed; nothing was archived.
          content:
            application/json:
              schema:
                type: object
                properties:
                  ok:
                    type: boolean
                  archived:
                    type: integer
                  deleted:
                    type: integer
                  failed:
                    type: integer
                  archivedIds:
                    type: array
                    items:
                      type: string
                required:
                  - ok
                  - archived
                  - deleted
                  - failed
                  - archivedIds
  /api/agents/{agentId}/sessions/pushed:
    delete:
      tags:
        - Sessions
      summary: Bulk soft-delete (archive) every *pushed* session for an agent
      description: Archives only sessions whose resolved lifecycle state is `pushed` (Finalize pushed the branch but it has not merged yet). Sessions in any other state — working, waiting, in-flight Finalize phases, or merged — are left untouched, as are sessions with an active in-flight CLI process.
      parameters:
        - schema:
            type: string
            description: Agent ID.
          required: true
          description: Agent ID.
          name: agentId
          in: path
      responses:
        "200":
          description: Archive result. `ok` is false when any teardown failed; `archivedIds` is the authoritative removed set.
          content:
            application/json:
              schema:
                type: object
                properties:
                  ok:
                    type: boolean
                  archived:
                    type: integer
                  deleted:
                    type: integer
                  failed:
                    type: integer
                  archivedIds:
                    type: array
                    items:
                      type: string
                required:
                  - ok
                  - archived
                  - deleted
                  - failed
                  - archivedIds
        "500":
          description: Every candidate teardown failed; nothing was archived.
          content:
            application/json:
              schema:
                type: object
                properties:
                  ok:
                    type: boolean
                  archived:
                    type: integer
                  deleted:
                    type: integer
                  failed:
                    type: integer
                  archivedIds:
                    type: array
                    items:
                      type: string
                required:
                  - ok
                  - archived
                  - deleted
                  - failed
                  - archivedIds
  /api/agents/{agentId}/sessions/merged:
    delete:
      tags:
        - Sessions
      summary: Bulk soft-delete (archive) every *merged* session for an agent
      description: "Archives only sessions whose resolved lifecycle state is `merged` (the work landed on the default branch). Sessions in any other state — working, waiting, in-flight Finalize phases, or pushed-but-not-merged — are left untouched, as are sessions with an active in-flight CLI process. Companion to the pushed-only bulk clear: under Merge Automatically, shipped sessions settle in `merged`, not `pushed`."
      parameters:
        - schema:
            type: string
            description: Agent ID.
          required: true
          description: Agent ID.
          name: agentId
          in: path
      responses:
        "200":
          description: Archive result. `ok` is false when any teardown failed; `archivedIds` is the authoritative removed set.
          content:
            application/json:
              schema:
                type: object
                properties:
                  ok:
                    type: boolean
                  archived:
                    type: integer
                  deleted:
                    type: integer
                  failed:
                    type: integer
                  archivedIds:
                    type: array
                    items:
                      type: string
                required:
                  - ok
                  - archived
                  - deleted
                  - failed
                  - archivedIds
        "500":
          description: Every candidate teardown failed; nothing was archived.
          content:
            application/json:
              schema:
                type: object
                properties:
                  ok:
                    type: boolean
                  archived:
                    type: integer
                  deleted:
                    type: integer
                  failed:
                    type: integer
                  archivedIds:
                    type: array
                    items:
                      type: string
                required:
                  - ok
                  - archived
                  - deleted
                  - failed
                  - archivedIds
  /api/sessions/{sessionId}:
    get:
      tags:
        - Sessions
      summary: Get a single session by id
      parameters:
        - schema:
            type: string
            description: Session UUID.
          required: true
          description: Session UUID.
          name: sessionId
          in: path
      responses:
        "200":
          description: Session row.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Session"
        "404":
          description: Session not found (or hidden by ownership).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SessionErrorResponse"
    patch:
      tags:
        - Sessions
      summary: Update session fields
      description: Updates `name` and/or `max_turns` (multi-agent advisor cap). Other keys are ignored.
      parameters:
        - schema:
            type: string
            description: Session UUID.
          required: true
          description: Session UUID.
          name: sessionId
          in: path
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                name:
                  type: string
                max_turns:
                  type: integer
                  minimum: 0
                finalize_automation:
                  type: string
                  enum:
                    - manual
                    - review
                    - push
                    - merge
                session_mode:
                  type: string
                  enum:
                    - chat
                    - isolated
                    - design
                    - scoping
                    - skill-builder
                    - consult
                    - hub
                ask_mode:
                  type: boolean
      responses:
        "200":
          description: Updated session.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Session"
        "400":
          description: Validation failed.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SessionErrorResponse"
        "404":
          description: Session not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SessionErrorResponse"
        "503":
          description: Session environment transition support is unavailable.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SessionErrorResponse"
    delete:
      tags:
        - Sessions
      summary: Archive (soft-delete) a single session
      description: Kills any in-flight CLI process, closes the browser session best-effort, and marks the row with `deleted_at`. Recoverable for 24h via `POST /api/sessions/{sessionId}/restore`.
      parameters:
        - schema:
            type: string
            description: Session UUID.
          required: true
          description: Session UUID.
          name: sessionId
          in: path
      responses:
        "200":
          description: Archive ack.
          content:
            application/json:
              schema:
                type: object
                properties:
                  ok:
                    type: boolean
                    enum:
                      - true
                  archived:
                    type: boolean
                    enum:
                      - true
                required:
                  - ok
                  - archived
        "404":
          description: Session not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SessionErrorResponse"
  /api/sessions/{sessionId}/forward:
    post:
      tags:
        - Sessions
      summary: Forward a session’s conversation to another agent
      description: Creates a new session on the target agent seeded with the forwarded transcript as the initial user message. The target agent may live in a different project (cross-project forwarding); the caller must own the source session and be able to view the target agent’s project, otherwise 404. The new session inherits the source session’s owner.
      parameters:
        - schema:
            type: string
            description: Session UUID.
          required: true
          description: Session UUID.
          name: sessionId
          in: path
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                targetAgentId:
                  type: string
                  minLength: 1
                  description: Agent to forward the conversation to. May belong to a different project than the source session (cross-project forwarding); the caller must be able to view the target agent’s project.
                messageIds:
                  type: array
                  items:
                    type: string
                  description: "Specific message IDs to include (default: all, capped to the last 200)."
                prompt:
                  type: string
                  maxLength: 50000
                  description: Extra instructions prepended to the forwarded context (max 50k chars).
                autoStart:
                  type: boolean
                  description: When true, immediately send the forwarded message to the target agent’s CLI.
                model:
                  type: string
                  description: Override the model the new session runs. Must be valid for the target agent’s engine, otherwise 400. Defaults to the target agent’s own effective model.
              required:
                - targetAgentId
      responses:
        "201":
          description: New forwarded session plus the pre-stored forwarded message id (null when autoStart).
          content:
            application/json:
              schema:
                type: object
                properties:
                  session:
                    $ref: "#/components/schemas/Session"
                  forwardedMessageId:
                    type: string
                    nullable: true
                required:
                  - session
                  - forwardedMessageId
        "400":
          description: Validation failed, no messages to forward, or forwarded content too large.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SessionErrorResponse"
        "404":
          description: Source session not found, or target agent not found / not viewable.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SessionErrorResponse"
        "503":
          description: Auto-start requested but the chat handler is not initialized.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SessionErrorResponse"
  /api/sessions/{sessionId}/follow-up:
    post:
      tags:
        - Sessions
      summary: Start a follow-up session from this session
      description: Creates a new session on a fresh worktree branch, seeded with the source session’s Finalize summary (including the follow-up steps it flagged) rather than the whole transcript. Falls back to the tail of the conversation when the session never finalized. Defaults to the source session’s own agent; the new session inherits the source session’s owner.
      parameters:
        - schema:
            type: string
            description: Session UUID.
          required: true
          description: Session UUID.
          name: sessionId
          in: path
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                targetAgentId:
                  type: string
                  minLength: 1
                  description: Agent to run the follow-up. Defaults to the source session’s own agent. The caller must be able to view the target agent’s project.
                prompt:
                  type: string
                  maxLength: 50000
                  description: What the follow-up should do, prepended to the seeded context (max 50k chars).
                autoStart:
                  type: boolean
                  description: When true, immediately dispatch the seeded message to the target agent’s CLI.
      responses:
        "201":
          description: New follow-up session plus the pre-stored seeded message id (null when autoStart).
          content:
            application/json:
              schema:
                type: object
                properties:
                  session:
                    $ref: "#/components/schemas/Session"
                  seededMessageId:
                    type: string
                    nullable: true
                required:
                  - session
                  - seededMessageId
        "400":
          description: Validation failed (prompt not a string or over the length cap).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SessionErrorResponse"
        "404":
          description: Source session not found, or target agent not found / not viewable.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SessionErrorResponse"
        "503":
          description: Auto-start requested but the chat handler is not initialized.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SessionErrorResponse"
  /api/sessions/{sessionId}/messages:
    get:
      tags:
        - Sessions
      summary: List messages in a session (oldest first)
      description: Without `paginated`, returns the full transcript when it fits the JSON response budget, otherwise a truncated envelope with the newest rows; `limit` slices the newest N. With `paginated=1`, returns a DB-limited keyset page (newest page first, `limit` page size, default 40, max 200) as a plain oldest-first array; pass `before` (the oldest loaded message id) to fetch the next older page. Powers the client reverse-infinite-scroll loader.
      parameters:
        - schema:
            type: string
            description: Session UUID.
          required: true
          description: Session UUID.
          name: sessionId
          in: path
        - schema:
            type: string
            description: "Newest N messages (positive integer). Legacy: in-memory slice. Paginated: DB page size, clamped to 200 (default 40)."
          required: false
          description: "Newest N messages (positive integer). Legacy: in-memory slice. Paginated: DB page size, clamped to 200 (default 40)."
          name: limit
          in: query
        - schema:
            type: string
            description: Set to `1` (or `true`) to opt into DB-side keyset pagination (newest page first).
          required: false
          description: Set to `1` (or `true`) to opt into DB-side keyset pagination (newest page first).
          name: paginated
          in: query
        - schema:
            type: string
            description: The oldest already-loaded message id. Returns the page of messages immediately older than it. Implies pagination.
          required: false
          description: The oldest already-loaded message id. Returns the page of messages immediately older than it. Implies pagination.
          name: before
          in: query
      responses:
        "200":
          description: Message rows (oldest-first), or a truncated envelope for very large un-paginated transcripts.
          content:
            application/json:
              schema:
                anyOf:
                  - type: array
                    items:
                      $ref: "#/components/schemas/Message"
                  - $ref: "#/components/schemas/SessionMessagesList"
        "404":
          description: Session not found (or hidden by ownership).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SessionErrorResponse"
  /api/sessions/{sessionId}/credential-requests/{requestId}:
    get:
      tags:
        - Sessions
      summary: Get session credential request status
      description: Returns metadata and status for an ephemeral credential request. Plaintext credential values are never returned by this endpoint.
      parameters:
        - schema:
            type: string
            description: Session UUID.
          required: true
          description: Session UUID.
          name: sessionId
          in: path
        - schema:
            type: string
            description: Agent-supplied credential request id from the chat prompt block.
          required: true
          description: Agent-supplied credential request id from the chat prompt block.
          name: requestId
          in: path
      responses:
        "200":
          description: Credential request metadata and status.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SessionCredentialStatusResponse"
        "404":
          description: Session or credential request not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SessionErrorResponse"
    put:
      tags:
        - Sessions
      summary: Submit ephemeral session credentials
      description: Stores user-submitted credentials encrypted with a short TTL. The values are not added to chat history and are not returned by status reads.
      parameters:
        - schema:
            type: string
            description: Session UUID.
          required: true
          description: Session UUID.
          name: sessionId
          in: path
        - schema:
            type: string
            description: Agent-supplied credential request id from the chat prompt block.
          required: true
          description: Agent-supplied credential request id from the chat prompt block.
          name: requestId
          in: path
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                service:
                  type: string
                  minLength: 1
                  maxLength: 120
                  description: Human-facing service name, for example `Survey Tracker`.
                purpose:
                  type: string
                  minLength: 1
                  maxLength: 240
                  description: Why the agent is requesting these credentials.
                fields:
                  type: array
                  items:
                    $ref: "#/components/schemas/SessionCredentialField"
                  minItems: 1
                  maxItems: 6
                values:
                  type: object
                  additionalProperties:
                    type: string
                  description: Plaintext values keyed by field key. Stored encrypted until one-time consumption, never returned by status endpoints.
                ttlSeconds:
                  type: number
                  minimum: 0
                  exclusiveMinimum: true
                  maximum: 3600
                  description: Credential lifetime in seconds. Defaults to 15 minutes, capped at 1 hour.
                persist:
                  type: object
                  properties:
                    skillId:
                      type: string
                      minLength: 1
                      maxLength: 128
                      description: Skill id whose `credentials:` frontmatter declares the target keys.
                    map:
                      type: object
                      additionalProperties:
                        type: string
                      description: Map of request field key → the skill's declared credential key name. Only declared keys are stored.
                  required:
                    - skillId
                    - map
                  description: When present, the submitted values are ALSO written into the session owner's persistent per-user skill credential store for `skillId`, so future spawns inject them as env vars. The ephemeral session copy is still stored for same-session use.
              required:
                - service
                - purpose
                - fields
                - values
      responses:
        "200":
          description: Credential request accepted.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SessionCredentialStatusResponse"
        "400":
          description: Validation failed.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SessionErrorResponse"
        "404":
          description: Session not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SessionErrorResponse"
  /api/sessions/{sessionId}/credential-requests/{requestId}/consume:
    post:
      tags:
        - Sessions
      summary: Consume ephemeral session credentials
      description: Returns plaintext values for the owning session. The same requestId can be consumed repeatedly until it expires, so an agent that loses the value (for example inside a throwaway subprocess) can re-fetch it instead of asking the user to resubmit. An expired or missing request returns 404; the encrypted payload is erased on expiry or delete.
      parameters:
        - schema:
            type: string
            description: Session UUID.
          required: true
          description: Session UUID.
          name: sessionId
          in: path
        - schema:
            type: string
            description: Agent-supplied credential request id from the chat prompt block.
          required: true
          description: Agent-supplied credential request id from the chat prompt block.
          name: requestId
          in: path
      responses:
        "200":
          description: Plaintext values for the requested credentials.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ConsumeSessionCredentialResponse"
        "404":
          description: Session or available credential request not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SessionErrorResponse"
  /api/sessions/{sessionId}/summary:
    get:
      tags:
        - Sessions
      summary: Get session summary metadata
      description: "Returns the compact metadata used by the chat header: linked kanban-card PR, latest Finalize PR fallback, title-inferred PR fallback, run snapshot, and loaded skills."
      parameters:
        - schema:
            type: string
            description: Session UUID.
          required: true
          description: Session UUID.
          name: sessionId
          in: path
      responses:
        "200":
          description: Session summary metadata.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SessionSummary"
        "404":
          description: Session not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SessionErrorResponse"
  /api/sessions/{sessionId}/extract-skill:
    post:
      tags:
        - Sessions
      summary: Turn this session into a skill (Skill Builder Phase 4)
      description: Hands the session's transcript to the project's Skill Builder coach agent, which mines the repeated context/procedures and drafts a SKILL.md via the skills write API ("extract, don't invent"). Spawns a fresh non-worktree coach session and returns its id; the coach streams a draft for the user to review/edit and save.
      parameters:
        - schema:
            type: string
            description: Session UUID.
          required: true
          description: Session UUID.
          name: sessionId
          in: path
      responses:
        "201":
          description: Skill Builder coach session spawned.
          content:
            application/json:
              schema:
                type: object
                properties:
                  sessionId:
                    type: string
                    description: The new coach session id.
                  agentId:
                    type: string
                    description: The Skill Builder agent id.
                  session:
                    allOf:
                      - $ref: "#/components/schemas/Session"
                      - nullable: true
                required:
                  - sessionId
                  - agentId
                  - session
        "400":
          description: No messages, no resolvable project, or no Skill Builder coach agent.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SessionErrorResponse"
        "404":
          description: Session not found (or hidden by ownership).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SessionErrorResponse"
  /api/sessions/{sessionId}/linked-design:
    put:
      tags:
        - Sessions
      summary: Link (or unlink) a Design Studio design to a session
      description: "Sets `linked_design_id` so the web client renders the design's live canvas in a preview pane beside the chat. Pass `designId: null` to clear the link. A non-null id must reference a design in the caller's active org."
      parameters:
        - schema:
            type: string
            description: Session UUID.
          required: true
          description: Session UUID.
          name: sessionId
          in: path
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                designId:
                  type: string
                  nullable: true
                  minLength: 1
                  description: Design id to link, or null to clear the link.
              required:
                - designId
      responses:
        "200":
          description: Updated session.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Session"
        "400":
          description: Validation failed.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SessionErrorResponse"
        "404":
          description: Session or design not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SessionErrorResponse"
  /api/sessions/{sessionId}/worktree-branch:
    put:
      tags:
        - Sessions
      summary: Choose (or clear) the existing branch a session worktree checks out onto
      description: "Before provisioning, sets `worktree_checkout_branch` so the session worktree starts directly on the chosen existing remote branch. After provisioning, switches a clean and idle worktree onto the chosen existing non-default branch. Commits then land there and Finalize pushes or updates its PR. Pass `branch: null` only before provisioning to clear the choice and revert to the default fresh `agent-hub/<agent>/session-<id>` branch. Code changes, active turns, Finalize runs, dirty worktrees, and the repository default branch are rejected."
      parameters:
        - schema:
            type: string
            description: Session UUID.
          required: true
          description: Session UUID.
          name: sessionId
          in: path
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                branch:
                  type: string
                  nullable: true
                  minLength: 1
                  maxLength: 255
                  pattern: ^(?!-)(?!.*\.\.)[A-Za-z0-9._][A-Za-z0-9._/-]*$
                  description: Existing remote branch to position the session worktree on, or null to clear the choice before provisioning (revert to the default fresh session branch). After provisioning, only a clean and idle session may switch to an existing non-default branch.
              required:
                - branch
      responses:
        "200":
          description: Updated session.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Session"
        "400":
          description: Validation failed or session does not use a worktree.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SessionErrorResponse"
        "404":
          description: Session not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SessionErrorResponse"
        "409":
          description: The branch cannot be changed in the current session state.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SessionErrorResponse"
  /api/sessions/{sessionId}/linked-epic:
    put:
      tags:
        - Sessions
      summary: Link (or unlink) a kanban epic to a session for scoping mode
      description: "Sets `linked_epic_id` so a scoping-mode session renders the Epic → Phase → Ticket flowchart and loads the epic's spec context. Pass `epicId: null` to clear the link. A non-null id must reference an epic on the session's own project board."
      parameters:
        - schema:
            type: string
            description: Session UUID.
          required: true
          description: Session UUID.
          name: sessionId
          in: path
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                epicId:
                  type: string
                  nullable: true
                  description: Epic UUID to link, or null to unlink.
      responses:
        "200":
          description: Updated session.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Session"
        "404":
          description: Session or epic not found (incl. an epic on a different project).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SessionErrorResponse"
  /api/sessions/{sessionId}/agents:
    post:
      tags:
        - Sessions
      summary: Add a read-only advisor agent to a multi-agent session
      description: Session owner only. Adds a new participant instance, so the same agent (including the executor agent) may be added more than once. An optional model overrides that participant instance only. Advisor may belong to any project the caller can view. Reviewer-role agents are rejected.
      parameters:
        - schema:
            type: string
            description: Session UUID.
          required: true
          description: Session UUID.
          name: sessionId
          in: path
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                agentId:
                  type: string
                  minLength: 1
                model:
                  type: string
                  nullable: true
                  minLength: 1
              required:
                - agentId
      responses:
        "200":
          description: Updated session with roster.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Session"
        "400":
          description: Validation failed or model is invalid for the agent engine.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SessionErrorResponse"
        "403":
          description: Reviewer agents cannot join multi-agent sessions.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SessionErrorResponse"
        "404":
          description: Session or agent not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SessionErrorResponse"
  /api/sessions/{sessionId}/agents/{participantId}:
    delete:
      tags:
        - Sessions
      summary: Remove an advisor from a multi-agent session
      description: Session owner only. Removes one advisor participant instance.
      parameters:
        - schema:
            type: string
            description: Session UUID.
          required: true
          description: Session UUID.
          name: sessionId
          in: path
        - schema:
            type: string
            description: Advisor participant instance ID.
          required: true
          description: Advisor participant instance ID.
          name: participantId
          in: path
      responses:
        "200":
          description: Updated session with roster.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Session"
        "404":
          description: Session or participant not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SessionErrorResponse"
  /api/sessions/{sessionId}/agents/{participantId}/model:
    put:
      tags:
        - Sessions
      summary: Set the model for one advisor participant instance
      description: Session owner only. Pass null to restore the agent default for this participant instance.
      parameters:
        - schema:
            type: string
            description: Session UUID.
          required: true
          description: Session UUID.
          name: sessionId
          in: path
        - schema:
            type: string
            description: Advisor participant instance ID.
          required: true
          description: Advisor participant instance ID.
          name: participantId
          in: path
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                model:
                  type: string
                  nullable: true
                  minLength: 1
              required:
                - model
      responses:
        "200":
          description: Updated session with roster.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Session"
        "400":
          description: Model is invalid for the agent engine.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SessionErrorResponse"
        "404":
          description: Session or participant not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SessionErrorResponse"
  /api/sessions/{sessionId}/restore:
    post:
      tags:
        - Sessions
      summary: Restore an archived session
      parameters:
        - schema:
            type: string
            description: Session UUID.
          required: true
          description: Session UUID.
          name: sessionId
          in: path
      responses:
        "200":
          description: Restored session.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Session"
        "404":
          description: Session not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SessionErrorResponse"
        "409":
          description: Session is not archived.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SessionErrorResponse"
  /api/sessions/{sessionId}/engine:
    put:
      tags:
        - Sessions
      summary: Switch the engine for a session
      description: Resets the model to the new engine's default if the current model isn't valid for the chosen engine. Clears the `engine_session_id` so the next chat starts a fresh CLI side.
      parameters:
        - schema:
            type: string
            description: Session UUID.
          required: true
          description: Session UUID.
          name: sessionId
          in: path
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                engine:
                  type: string
                  enum:
                    - claude-code
                    - cursor-agent
                    - gemini-cli
                    - codex-cli
                    - grok-cli
              required:
                - engine
      responses:
        "200":
          description: Updated session.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Session"
        "400":
          description: Validation failed or invalid engine.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SessionErrorResponse"
        "404":
          description: Session not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SessionErrorResponse"
  /api/sessions/{sessionId}/model:
    put:
      tags:
        - Sessions
      summary: Switch the model for a session
      description: "Two-step validation: `model` must be in `config.allValidModels` (the cross-engine union), AND in the current session engine's `engineValidModels` list. Returns 400 otherwise."
      parameters:
        - schema:
            type: string
            description: Session UUID.
          required: true
          description: Session UUID.
          name: sessionId
          in: path
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                model:
                  type: string
                  minLength: 1
              required:
                - model
      responses:
        "200":
          description: Updated session.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Session"
        "400":
          description: Validation failed or model not valid for the current engine.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SessionErrorResponse"
        "404":
          description: Session not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SessionErrorResponse"
  /api/sessions/{sessionId}/reasoning-effort:
    put:
      tags:
        - Sessions
      summary: Set the Codex reasoning ("thinking") level for a session
      description: Codex (`codex-cli`) sessions only. `high` (default) runs at `model_reasoning_effort=high`; `pro` runs at `xhigh` (max thinking, same model). Ignored for non-Codex engines.
      parameters:
        - schema:
            type: string
            description: Session UUID.
          required: true
          description: Session UUID.
          name: sessionId
          in: path
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                effort:
                  type: string
                  enum:
                    - high
                    - pro
              required:
                - effort
      responses:
        "200":
          description: Updated session.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Session"
        "400":
          description: Validation failed.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SessionErrorResponse"
        "404":
          description: Session not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SessionErrorResponse"
  /api/sessions/{sessionId}/ask-mode:
    put:
      tags:
        - Sessions
      summary: Legacy Ask toggle alias for Consult mode
      parameters:
        - schema:
            type: string
            description: Session UUID.
          required: true
          description: Session UUID.
          name: sessionId
          in: path
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                enabled:
                  type: boolean
              required:
                - enabled
      responses:
        "200":
          description: Updated session.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Session"
        "400":
          description: Validation failed.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SessionErrorResponse"
        "404":
          description: Session not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SessionErrorResponse"
  /api/sessions/{sessionId}/mode:
    put:
      tags:
        - Sessions
      summary: Set the session mode (chat | design)
      description: Folds the standalone Design Studio into the chat-mode picker. `chat` (default) is a normal coding session; `design` loads the design skill and produces HTML/CSS/JS artifacts in the session worktree. Flipping back to `chat` keeps those artifacts in the same checkout. `design` requires a session with an isolated worktree — setting it on a worktree-less session is rejected with 400 (`design_mode_requires_worktree`), since artifacts must live in the worktree and must not pollute the shared project checkout.
      parameters:
        - schema:
            type: string
            description: Session UUID.
          required: true
          description: Session UUID.
          name: sessionId
          in: path
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                mode:
                  type: string
                  enum:
                    - chat
                    - isolated
                    - design
                    - scoping
                    - skill-builder
                    - consult
                    - hub
              required:
                - mode
      responses:
        "200":
          description: Updated session.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Session"
        "400":
          description: Validation failed.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SessionErrorResponse"
        "404":
          description: Session not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SessionErrorResponse"
        "503":
          description: Session environment transition support is unavailable.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SessionErrorResponse"
  /api/sessions/{sessionId}/design-files:
    get:
      tags:
        - Sessions
      summary: List design-mode artifacts produced in the session worktree
      description: Returns the regular files a `design`-mode session has written under its worktree `design/` dir. The web client renders these live in an iframe canvas; mobile/Electron (no in-app iframe) show this flat list plus an open-in-browser link to `/session-files/{sessionId}/design/<path>`. A worktree-less session (which can never enter design mode) and a session that wrote no artifacts both return an empty list.
      parameters:
        - schema:
            type: string
            description: Session UUID.
          required: true
          description: Session UUID.
          name: sessionId
          in: path
      responses:
        "200":
          description: Design artifact listing.
          content:
            application/json:
              schema:
                type: object
                properties:
                  files:
                    type: array
                    items:
                      $ref: "#/components/schemas/SessionDesignFile"
                required:
                  - files
        "404":
          description: Session not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SessionErrorResponse"
  /api/sessions/{sessionId}/react-loop:
    put:
      tags:
        - Sessions
      summary: Toggle the host-mediated ReAct loop on a session
      parameters:
        - schema:
            type: string
            description: Session UUID.
          required: true
          description: Session UUID.
          name: sessionId
          in: path
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                enabled:
                  type: boolean
              required:
                - enabled
      responses:
        "200":
          description: Updated session.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Session"
        "400":
          description: Validation failed.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SessionErrorResponse"
        "404":
          description: Session not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SessionErrorResponse"
  /api/sessions/{sessionId}/rewind:
    post:
      tags:
        - Sessions
      summary: Rewind a Claude Code session to a checkpoint UUID
      description: "Spawns `claude --resume <engine_session_id> --rewind-files <uuid>`. Supported only when `session.engine` is `claude-code`. Other engines: HTTP 400 with `code: checkpoint_rewind_unsupported_engine`."
      parameters:
        - schema:
            type: string
            description: Session UUID.
          required: true
          description: Session UUID.
          name: sessionId
          in: path
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                uuid:
                  type: string
                  minLength: 1
              required:
                - uuid
      responses:
        "200":
          description: Rewind kicked off.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                    enum:
                      - rewind_started
                  uuid:
                    type: string
                  sessionId:
                    type: string
                required:
                  - status
                  - uuid
                  - sessionId
        "400":
          description: Body validation failed, engine does not support rewind, or session never captured a Claude engine session id.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SessionErrorResponse"
        "404":
          description: Session or checkpoint not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SessionErrorResponse"
        "409":
          description: Session is actively running.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SessionErrorResponse"
  /api/sessions/{sessionId}/checkpoints/{uuid}:
    patch:
      tags:
        - Sessions
      summary: Rename a checkpoint label
      parameters:
        - schema:
            type: string
            description: Session UUID.
          required: true
          description: Session UUID.
          name: sessionId
          in: path
        - schema:
            type: string
            description: Checkpoint UUID.
          required: true
          description: Checkpoint UUID.
          name: uuid
          in: path
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                label:
                  type: string
              required:
                - label
      responses:
        "200":
          description: Updated checkpoint.
          content:
            application/json:
              schema:
                type: object
                properties:
                  session_id:
                    type: string
                  uuid:
                    type: string
                  label:
                    type: string
                required:
                  - session_id
                  - uuid
                  - label
        "400":
          description: Validation failed.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SessionErrorResponse"
        "404":
          description: Checkpoint not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SessionErrorResponse"
  /api/sessions/{sessionId}/preview/ticket:
    post:
      tags:
        - Sessions
      summary: Mint a single-use preview iframe ticket
      description: "Returns a short-lived single-use ticket the SPA appends to the preview iframe `src` as `?ticket=…`. Browsers cannot attach `Authorization: Bearer …` to an iframe top-level navigation, so this endpoint is the bridge between the SPA's JWT session and the preview proxy. The ticket is bound to (sessionId, caller) and ~60 s TTL; on first hit the proxy consumes the ticket and writes a path-scoped HttpOnly cookie so sub-resources (.js/.css/images) authenticate automatically."
      parameters:
        - schema:
            type: string
            description: Session UUID.
          required: true
          description: Session UUID.
          name: sessionId
          in: path
      responses:
        "200":
          description: Ticket minted.
          content:
            application/json:
              schema:
                type: object
                properties:
                  ticket:
                    type: string
                    description: Single-use opaque token (prefixed `ahpt_`). Append as `?ticket=…`.
                  ttlSeconds:
                    type: integer
                    description: Seconds the ticket remains valid before it must be re-minted.
                required:
                  - ticket
                  - ttlSeconds
        "404":
          description: Session not found or not owned by the caller.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SessionErrorResponse"
  /api/sessions/{sessionId}/preview/stop:
    post:
      tags:
        - Sessions
      summary: Stop worktree preview for a chat session
      description: Tears down compose/legacy preview runtimes for this session. Safe while boot is still in progress.
      parameters:
        - schema:
            type: string
            description: Session UUID.
          required: true
          description: Session UUID.
          name: sessionId
          in: path
      responses:
        "200":
          description: Preview teardown requested.
          content:
            application/json:
              schema:
                type: object
                properties:
                  ok:
                    type: boolean
                    enum:
                      - true
                  stopped:
                    type: boolean
                    enum:
                      - true
                required:
                  - ok
                  - stopped
        "404":
          description: Session not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SessionErrorResponse"
        "500":
          description: Unexpected server error.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SessionErrorResponse"
  /api/sessions/{sessionId}/workspace/ensure:
    post:
      tags:
        - Sessions
      summary: Provision session git worktree
      description: Clones or reuses the per-session worktree so preview and file edits use the same checkout. Chat messages submitted during setup are queued and run automatically when the workspace and environment are ready. Idempotent when `worktree_path` is already set.
      parameters:
        - schema:
            type: string
            description: Session UUID.
          required: true
          description: Session UUID.
          name: sessionId
          in: path
      responses:
        "200":
          description: Worktree ready (or skipped for workflow / wizard sessions).
          content:
            application/json:
              schema:
                type: object
                properties:
                  ok:
                    type: boolean
                    enum:
                      - true
                  skipped:
                    type: boolean
                  worktreePath:
                    type: string
                  session:
                    $ref: "#/components/schemas/Session"
                required:
                  - ok
                  - skipped
                  - worktreePath
                  - session
        "404":
          description: Session not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SessionErrorResponse"
        "500":
          description: Unexpected server error.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SessionErrorResponse"
        "503":
          description: Provisioning not wired on this server.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SessionErrorResponse"
  /api/sessions/{sessionId}/ship:
    post:
      tags:
        - Sessions
      summary: Ship session work (Create ticket & PR)
      description: "Operator-initiated publish for ad-hoc sessions: injects the `create-ticket-and-pr` skill, records a system callout in chat, and starts an agent turn without persisting a visible user slash-command message."
      parameters:
        - schema:
            type: string
            description: Session UUID.
          required: true
          description: Session UUID.
          name: sessionId
          in: path
      responses:
        "200":
          description: Ship workflow started.
          content:
            application/json:
              schema:
                type: object
                properties:
                  ok:
                    type: boolean
                    enum:
                      - true
                required:
                  - ok
        "400":
          description: Session has no worktree.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SessionErrorResponse"
        "403":
          description: Disabled in workflow mode.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SessionErrorResponse"
        "404":
          description: Session, agent, or skill not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SessionErrorResponse"
        "409":
          description: Session is streaming, ship is in progress, or is a resolve-PR session.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SessionErrorResponse"
        "500":
          description: Unexpected server error.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SessionErrorResponse"
  /api/sessions/{sessionId}/preview/start:
    post:
      tags:
        - Sessions
      summary: Start worktree preview for a chat session
      description: Boots the project preview runtime for this session (human-initiated only; agent `<agenthub:preview>` blocks are ignored). Progress and iframe URL are delivered over WebSocket as `agenthub_preview` events.
      parameters:
        - schema:
            type: string
            description: Session UUID.
          required: true
          description: Session UUID.
          name: sessionId
          in: path
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                route:
                  type: string
                  description: In-app route to load (must start with `/`). Defaults to first capture route or `/`.
                reason:
                  type: string
                mode:
                  type: string
                  enum:
                    - rebuild
                    - restart-server
                  description: Start path. `rebuild` (default) runs the configured `buildCommand` then `startCommand`; `restart-server` skips the build and recycles only the server process, reusing the existing build output.
              description: Optional preview task overrides.
      responses:
        "200":
          description: Preview boot kicked off.
          content:
            application/json:
              schema:
                type: object
                properties:
                  ok:
                    type: boolean
                    enum:
                      - true
                  started:
                    type: boolean
                    enum:
                      - true
                required:
                  - ok
                  - started
        "404":
          description: Session not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SessionErrorResponse"
        "409":
          description: Session workspace is not ready yet.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SessionErrorResponse"
        "500":
          description: Unexpected server error.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SessionErrorResponse"
  /api/sessions/{sessionId}/preview/state:
    get:
      tags:
        - Sessions
      summary: Get current preview state for a chat session
      description: "Returns the current `agenthub_preview` event for the session — the same wire shape delivered live over WebSocket and on the WS connect-snapshot — or `{ event: null }` when no compose preview is active. The client uses this to reconcile a preview pane that is stuck on `preview_starting` because a live `ready` event was dropped while the socket stayed open (no reconnect, so the connect-snapshot never replayed)."
      parameters:
        - schema:
            type: string
            description: Session UUID.
          required: true
          description: Session UUID.
          name: sessionId
          in: path
      responses:
        "200":
          description: Current preview event, or null when none is active.
          content:
            application/json:
              schema:
                type: object
                properties:
                  event:
                    type: object
                    nullable: true
                    properties:
                      type:
                        type: string
                        enum:
                          - agenthub_preview
                      kind:
                        type: string
                        enum:
                          - preview_starting
                          - preview
                          - preview_failed
                      sessionId:
                        type: string
                      previewId:
                        type: string
                      target:
                        type: string
                        enum:
                          - client
                      route:
                        type: string
                      previewUrl:
                        type: string
                      fullUrl:
                        type: string
                      port:
                        type: number
                      screenshotPath:
                        type: string
                        nullable: true
                      logTail:
                        type: array
                        items:
                          type: string
                      error:
                        type: string
                    required:
                      - type
                      - kind
                      - sessionId
                      - previewId
                      - target
                      - route
                      - screenshotPath
                      - logTail
                    description: The current preview snapshot event, or null when no preview is active for the session.
                required:
                  - event
        "404":
          description: Session not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SessionErrorResponse"
        "500":
          description: Unexpected server error.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SessionErrorResponse"
  /api/sessions/{sessionId}/changes:
    get:
      tags:
        - Sessions
      summary: List files changed in a session worktree
      description: Returns the total session delta — committed, uncommitted, and untracked files — diffed against the merge-base of the base branch and the worktree HEAD. Owner-filtered; a session the caller does not own returns 404. Sessions with no worktree return an empty file list.
      parameters:
        - schema:
            type: string
            description: Session UUID.
          required: true
          description: Session UUID.
          name: sessionId
          in: path
      responses:
        "200":
          description: Session change summary.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SessionChanges"
        "404":
          description: Session not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SessionErrorResponse"
        "500":
          description: Failed to compute session changes.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SessionErrorResponse"
  /api/sessions/{sessionId}/changes/diff:
    get:
      tags:
        - Sessions
      summary: Unified diff for a single changed file
      description: Returns the unified diff for one file in a session worktree. The `file` must be a repo-relative path that the server reports as changed for this session — absolute paths and `..` traversal are rejected, and paths outside the changed-file set return 404 (the endpoint is membership-gated so it can't be used as an arbitrary file-read primitive). Whether a file is tracked vs. untracked is determined server-side. Binary and oversized diffs return an empty body with `binary`/`tooLarge` set.
      parameters:
        - schema:
            type: string
            description: Session UUID.
          required: true
          description: Session UUID.
          name: sessionId
          in: path
        - schema:
            type: string
            description: Repo-relative path to diff (new path for renames).
          required: true
          description: Repo-relative path to diff (new path for renames).
          name: file
          in: query
      responses:
        "200":
          description: File diff.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SessionFileDiff"
        "400":
          description: Missing or invalid (absolute / out-of-worktree) file path.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SessionErrorResponse"
        "404":
          description: Session not found, has no worktree, or file is not a session change.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SessionErrorResponse"
        "500":
          description: Failed to compute file diff.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SessionErrorResponse"
  /api/bug-reports:
    options:
      tags:
        - Bug Reports
      summary: CORS preflight (returns 204)
      responses:
        "204":
          description: CORS preflight OK.
    post:
      tags:
        - Bug Reports
      summary: Public bug-report intake (multipart)
      description: Unauthenticated, rate-limited (10 / hour per IP). Body must be `multipart/form-data` with at minimum a `title` field. Lands a `bug` support ticket in the `agent-hub` Customer Support queue (severity-ordered, with a one-shot AI investigation); an operator promotes it to a kanban card via "Convert to card". An optional `screenshot` image part is persisted and shown inline on the ticket.
      requestBody:
        content:
          multipart/form-data:
            schema:
              $ref: "#/components/schemas/BugReportFormFields"
      responses:
        "201":
          description: Support ticket created.
          content:
            application/json:
              schema:
                type: object
                properties:
                  ticketId:
                    type: string
                  status:
                    type: string
                    enum:
                      - received
                required:
                  - ticketId
                  - status
                description: A `bug` support ticket has been created in the `agent-hub` Customer Support queue. Result is the new ticket id; the AI investigation runs asynchronously.
        "202":
          description: Obvious fake test report accepted and discarded.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                    enum:
                      - ignored
                  reason:
                    type: string
                    enum:
                      - discardable_test_report
                required:
                  - status
                  - reason
                description: The report was accepted but intentionally not queued because it matched the conservative discardable-test-report filter.
        "400":
          description: Validation failed (missing title, bad severity, malformed multipart).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BugReportsErrorResponse"
        "429":
          description: Per-IP rate limit exceeded.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BugReportsErrorResponse"
        "500":
          description: Intake project missing or handler threw.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BugReportsErrorResponse"
  /api/projects/{projectId}/ci-runs:
    get:
      tags:
        - Projects
      summary: List past CI runs for a project (Runners page history)
      description: Finalize runs and push-CI runs (trigger_source git_push) newest first, each with per-job results. Filter by trigger via ?trigger=.
      parameters:
        - schema:
            type: string
          required: true
          name: projectId
          in: path
        - schema:
            type: string
            enum:
              - all
              - ui_button
              - agent_block
              - git_push
              - pr_push
          required: false
          name: trigger
          in: query
        - schema:
            type: integer
            minimum: 1
            maximum: 100
          required: false
          name: limit
          in: query
      responses:
        "200":
          description: Run history.
          content:
            application/json:
              schema:
                type: object
                properties:
                  runs:
                    type: array
                    items:
                      type: object
                      properties:
                        id:
                          type: string
                        branch:
                          type: string
                        head_sha:
                          type: string
                        status:
                          type: string
                        mode:
                          type: string
                        trigger_source:
                          type: string
                        failure_reason:
                          type: string
                          nullable: true
                        started_at:
                          type: number
                        ended_at:
                          type: number
                          nullable: true
                        session_id:
                          type: string
                          nullable: true
                        session_title:
                          type: string
                          nullable: true
                        jobs:
                          type: array
                          items:
                            type: object
                            properties:
                              job_id:
                                type: string
                              matrix_key:
                                type: string
                              state:
                                type: string
                              exit_code:
                                type: number
                                nullable: true
                              started_at:
                                type: number
                                nullable: true
                              ended_at:
                                type: number
                                nullable: true
                            required:
                              - job_id
                              - matrix_key
                              - state
                              - exit_code
                              - started_at
                              - ended_at
                      required:
                        - id
                        - branch
                        - head_sha
                        - status
                        - mode
                        - trigger_source
                        - failure_reason
                        - started_at
                        - ended_at
                        - session_id
                        - session_title
                        - jobs
                required:
                  - runs
        "404":
          description: Unknown project.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                required:
                  - error
  /api/projects/{projectId}/ci-runs/stats:
    get:
      tags:
        - Projects
      summary: Runner card completion and failure-rate stats
      description: Aggregates completed Runner runs for the Runners page. Per-test stats are limited to jobs currently configured in ci.yaml; configured jobs are returned even before they have historical samples.
      parameters:
        - schema:
            type: string
          required: true
          name: projectId
          in: path
        - schema:
            type: string
            enum:
              - all
              - 24h
          required: false
          name: range
          in: query
      responses:
        "200":
          description: Runner stats.
          content:
            application/json:
              schema:
                type: object
                properties:
                  range:
                    type: string
                    enum:
                      - all
                      - 24h
                  overall:
                    type: object
                    properties:
                      average_seconds:
                        type: number
                        nullable: true
                      total_runs:
                        type: integer
                      failed_runs:
                        type: integer
                      failure_rate:
                        type: number
                        nullable: true
                      total_errors:
                        type: integer
                      infra_errors:
                        type: integer
                      infra_error_rate:
                        type: number
                        nullable: true
                    required:
                      - average_seconds
                      - total_runs
                      - failed_runs
                      - failure_rate
                      - total_errors
                      - infra_errors
                      - infra_error_rate
                  tests:
                    type: array
                    items:
                      type: object
                      properties:
                        average_seconds:
                          type: number
                          nullable: true
                        total_runs:
                          type: integer
                        failed_runs:
                          type: integer
                        failure_rate:
                          type: number
                          nullable: true
                        total_errors:
                          type: integer
                        infra_errors:
                          type: integer
                        infra_error_rate:
                          type: number
                          nullable: true
                        job_id:
                          type: string
                        matrix_key:
                          type: string
                        name:
                          type: string
                        configured:
                          type: boolean
                      required:
                        - average_seconds
                        - total_runs
                        - failed_runs
                        - failure_rate
                        - total_errors
                        - infra_errors
                        - infra_error_rate
                        - job_id
                        - matrix_key
                        - name
                        - configured
                  ci_config:
                    type: object
                    properties:
                      found:
                        type: boolean
                      version:
                        type: number
                        nullable: true
                      error:
                        type: string
                        nullable: true
                    required:
                      - found
                      - version
                      - error
                required:
                  - range
                  - overall
                  - tests
                  - ci_config
        "404":
          description: Unknown project.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                required:
                  - error
        "500":
          description: Stats could not be read.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                required:
                  - error
  /api/projects/{projectId}/ci-runs/{runId}:
    get:
      tags:
        - Projects
      summary: Single CI run with job and step rows
      parameters:
        - schema:
            type: string
          required: true
          name: projectId
          in: path
        - schema:
            type: string
          required: true
          name: runId
          in: path
      responses:
        "200":
          description: Run detail.
          content:
            application/json:
              schema:
                type: object
                properties:
                  run:
                    type: object
                    properties:
                      id:
                        type: string
                      branch:
                        type: string
                      head_sha:
                        type: string
                      status:
                        type: string
                      mode:
                        type: string
                      trigger_source:
                        type: string
                      failure_reason:
                        type: string
                        nullable: true
                      started_at:
                        type: number
                      ended_at:
                        type: number
                        nullable: true
                      session_id:
                        type: string
                        nullable: true
                      session_title:
                        type: string
                        nullable: true
                      jobs:
                        type: array
                        items:
                          type: object
                          properties:
                            job_id:
                              type: string
                            matrix_key:
                              type: string
                            state:
                              type: string
                            exit_code:
                              type: number
                              nullable: true
                            started_at:
                              type: number
                              nullable: true
                            ended_at:
                              type: number
                              nullable: true
                          required:
                            - job_id
                            - matrix_key
                            - state
                            - exit_code
                            - started_at
                            - ended_at
                    required:
                      - id
                      - branch
                      - head_sha
                      - status
                      - mode
                      - trigger_source
                      - failure_reason
                      - started_at
                      - ended_at
                      - session_id
                      - session_title
                      - jobs
                  steps:
                    type: array
                    items:
                      type: object
                      properties:
                        step_index:
                          type: number
                        name:
                          type: string
                        state:
                          type: string
                        exit_code:
                          type: number
                          nullable: true
                        started_at:
                          type: number
                          nullable: true
                        ended_at:
                          type: number
                          nullable: true
                        job_id:
                          type: string
                          nullable: true
                        matrix_key:
                          type: string
                          nullable: true
                      required:
                        - step_index
                        - name
                        - state
                        - exit_code
                        - started_at
                        - ended_at
                        - job_id
                        - matrix_key
                required:
                  - run
                  - steps
        "404":
          description: Unknown project or run.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                required:
                  - error
  /api/projects/{projectId}/ci-runs/{runId}/rerun:
    post:
      tags:
        - Projects
      summary: Re-run a finished CI run (all jobs, or one job)
      description: GitHub-style re-run for push / pr-ci runs against the SAME commit. Pass jobId to re-run a single job. Finalize runs are excluded (400) — re-run those via the Finalize button.
      parameters:
        - schema:
            type: string
          required: true
          name: projectId
          in: path
        - schema:
            type: string
          required: true
          name: runId
          in: path
      requestBody:
        required: false
        content:
          application/json:
            schema:
              type: object
              properties:
                jobId:
                  type: string
      responses:
        "202":
          description: Re-run queued.
          content:
            application/json:
              schema:
                type: object
                properties:
                  ok:
                    type: boolean
                required:
                  - ok
        "400":
          description: Not a re-runnable CI run.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                required:
                  - error
        "404":
          description: Unknown project or run.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                required:
                  - error
        "409":
          description: Run still in progress.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                required:
                  - error
  /api/projects/{projectId}/code-index:
    post:
      tags:
        - Code RAG
      summary: Index (or incrementally re-index) the project source tree
      description: "Walks the project checkout (`project.cwd`), embeds eligible source files into chunk-level vectors, and refreshes the FTS index. Idempotent: unchanged files (matched by content hash) are skipped and deleted files are pruned (pruning is skipped when the scan is capped by `maxFiles`, i.e. `truncated: true`). Invalid `maxFiles` (non-positive or > 20000) returns 400. Returns 503 when no Gemini embedding key is configured."
      parameters:
        - schema:
            type: string
            description: Project ID (slug).
          required: true
          description: Project ID (slug).
          name: projectId
          in: path
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                maxFiles:
                  type: integer
                  minimum: 0
                  exclusiveMinimum: true
                  maximum: 20000
      responses:
        "200":
          description: Indexing report.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CodeIndexResult"
        "400":
          description: Request body validation failed.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CodeRagErrorResponse"
        "404":
          description: Project not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CodeRagErrorResponse"
        "500":
          description: Indexing error.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CodeRagErrorResponse"
        "503":
          description: Gemini embedding key not configured.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CodeRagErrorResponse"
  /api/projects/{projectId}/code-index/status:
    get:
      tags:
        - Code RAG
      summary: Code index status
      description: Returns the number of indexed chunks for the project and whether Gemini is configured.
      parameters:
        - schema:
            type: string
            description: Project ID (slug).
          required: true
          description: Project ID (slug).
          name: projectId
          in: path
      responses:
        "200":
          description: Status.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CodeIndexStatus"
        "404":
          description: Project not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CodeRagErrorResponse"
  /api/projects/{projectId}/code-search:
    get:
      tags:
        - Code RAG
      summary: Search indexed project code (hybrid / semantic / fts)
      description: Defaults to hybrid mode. Falls back to pure FTS when the Gemini key is missing or the query embedding fails. Returns an empty `results` array for blank queries. Invalid `?limit=` (non-numeric) and unknown `?mode=` values return 400.
      parameters:
        - schema:
            type: string
            description: Project ID (slug).
          required: true
          description: Project ID (slug).
          name: projectId
          in: path
        - schema:
            type: string
          required: false
          name: q
          in: query
        - schema:
            type: string
            enum:
              - hybrid
              - semantic
              - fts
          required: false
          name: mode
          in: query
        - schema:
            type: integer
            minimum: 0
            exclusiveMinimum: true
          required: false
          name: limit
          in: query
      responses:
        "200":
          description: Search envelope.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CodeSearchResponse"
        "400":
          description: Query parameter validation failed.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CodeRagErrorResponse"
        "404":
          description: Project not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CodeRagErrorResponse"
        "500":
          description: Underlying search or embedding error.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CodeRagErrorResponse"
  /api/config:
    get:
      tags:
        - Config
      summary: Read aggregated server config (with masked secrets)
      responses:
        "200":
          description: Server config.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AppConfig"
    patch:
      tags:
        - Config
      summary: Update one or more config fields
      description: "Allowed keys: `claudeBin`, `cursorBin`, `geminiBin`, `codexBin`, `grokBin`, `defaultModel` (deprecated and ignored), `defaultCwd`, `port`, `apiKey`, `openaiApiKey`, `publicUrl`, `codexDangerBypass`, `codexProfile`. The deprecated `defaultModel` key is accepted but ignored with a deprecation warning. Model selection is per-user and per-agent; unknown keys are silently dropped. Returns the updated subset (with secrets masked)."
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                claudeBin:
                  type: string
                cursorBin:
                  type: string
                geminiBin:
                  type: string
                codexBin:
                  type: string
                grokBin:
                  type: string
                defaultModel:
                  type: string
                  deprecated: true
                  description: Accepted but ignored for compatibility. Configure model selection per user and agent instead.
                defaultCwd:
                  type: string
                port:
                  type: integer
                apiKey:
                  type: string
                  nullable: true
                openaiApiKey:
                  type: string
                  nullable: true
                  description: Host OpenAI API key for Whisper transcription and session titles. Empty string or null clears it.
                transcriptionProvider:
                  type: string
                  enum:
                    - xai
                    - openai
                  description: Voice-transcription provider for /api/transcribe. Must be `xai` or `openai`; any other value returns 400.
                publicUrl:
                  type: string
                codexDangerBypass:
                  type: boolean
                emailLogoEnabled:
                  type: boolean
                  description: Enable/disable the Agent Hub logo in deployment/release notification email headers. Default true.
                codexProfile:
                  type: string
                  nullable: true
                  description: Codex CLI profile name. Pass null / empty string to clear. Forwarded as `--profile <name>` on every codex spawn.
              additionalProperties:
                nullable: true
              description: Partial config update. Unknown keys are ignored. At least one allowed key must be present, otherwise the handler returns 400.
      responses:
        "200":
          description: Updated keys (masked where applicable).
          content:
            application/json:
              schema:
                type: object
                properties:
                  ok:
                    type: boolean
                  updated:
                    type: object
                    additionalProperties:
                      nullable: true
                  deprecations:
                    type: object
                    additionalProperties:
                      type: string
                required:
                  - ok
                  - updated
        "400":
          description: No valid config fields provided.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ConfigErrorResponse"
  /api/config/models:
    get:
      tags:
        - Config
      summary: List engines + per-engine authenticated models
      responses:
        "200":
          description: Model availability map.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ModelsConfig"
  /api/config/smtp:
    get:
      tags:
        - Config
      summary: Read masked SMTP email settings
      responses:
        "200":
          description: SMTP settings and password-reset fallback status.
          content:
            application/json:
              schema:
                type: object
                properties:
                  smtp:
                    $ref: "#/components/schemas/MaskedSmtpConfig"
                  passwordReset:
                    type: object
                    properties:
                      smtpConfigured:
                        type: boolean
                      fallbackAvailable:
                        type: boolean
                      fallback:
                        type: string
                        nullable: true
                        enum:
                          - owner_generated_reset_code
                          - null
                    required:
                      - smtpConfigured
                      - fallbackAvailable
                      - fallback
                required:
                  - smtp
                  - passwordReset
        "403":
          description: Admin role required.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ConfigErrorResponse"
    patch:
      tags:
        - Config
      summary: Update SMTP email settings
      description: Partial update that preserves the stored password when `password` is omitted or equal to the mask sentinel.
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                enabled:
                  type: boolean
                host:
                  type: string
                  nullable: true
                port:
                  anyOf:
                    - type: integer
                    - type: string
                tlsMode:
                  type: string
                  enum:
                    - none
                    - starttls
                    - ssl
                username:
                  type: string
                  nullable: true
                password:
                  type: string
                  nullable: true
                  description: Omit or pass `••••••••` to preserve the existing secret; pass empty string/null to clear.
                from:
                  type: string
                  nullable: true
              description: Partial SMTP config update.
      responses:
        "200":
          description: Updated masked SMTP settings.
          content:
            application/json:
              schema:
                type: object
                properties:
                  smtp:
                    $ref: "#/components/schemas/MaskedSmtpConfig"
                  passwordReset:
                    type: object
                    properties:
                      smtpConfigured:
                        type: boolean
                      fallbackAvailable:
                        type: boolean
                      fallback:
                        type: string
                        nullable: true
                        enum:
                          - owner_generated_reset_code
                          - null
                    required:
                      - smtpConfigured
                      - fallbackAvailable
                      - fallback
                  ok:
                    type: boolean
                    enum:
                      - true
                required:
                  - smtp
                  - passwordReset
                  - ok
        "400":
          description: Invalid SMTP settings.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ConfigErrorResponse"
        "403":
          description: Admin role required.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ConfigErrorResponse"
  /api/config/smtp/test:
    post:
      tags:
        - Config
      summary: Send a test email through the configured SMTP transport
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                to:
                  type: string
                  description: Optional recipient. Owners may send to any valid address; Admins can only send to their own email.
      responses:
        "200":
          description: Test email accepted by the SMTP transport.
          content:
            application/json:
              schema:
                type: object
                properties:
                  ok:
                    type: boolean
                    enum:
                      - true
                  to:
                    type: string
                required:
                  - ok
                  - to
        "400":
          description: SMTP is unconfigured, no usable recipient, or invalid recipient.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ConfigErrorResponse"
        "403":
          description: Supplied recipient is not allowed for this caller.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ConfigErrorResponse"
        "429":
          description: Rate-limited.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ConfigErrorResponse"
        "502":
          description: SMTP transport failed without exposing secrets.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ConfigErrorResponse"
  /api/config/spawn-path:
    get:
      tags:
        - Config
      summary: Read the cached login-shell PATH used when spawning agents
      responses:
        "200":
          description: Spawn-PATH snapshot.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SpawnPath"
  /api/config/refresh-spawn-path:
    post:
      tags:
        - Config
      summary: Re-run the login shell to pick up newly installed CLIs
      description: Idempotent — refreshes the cached PATH that `buildSpawnEnv` merges into every spawn.
      responses:
        "200":
          description: Refreshed spawn-PATH snapshot.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SpawnPath"
        "500":
          description: Login shell failed.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ConfigErrorResponse"
  /api/config/personal-oauth:
    get:
      tags:
        - Config
      summary: Read personal GitHub OAuth App configuration status
      responses:
        "200":
          description: Personal OAuth status.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PersonalOAuthStatus"
    put:
      tags:
        - Config
      summary: Set personal GitHub OAuth App credentials
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                clientId:
                  type: string
                  minLength: 1
                clientSecret:
                  type: string
                  minLength: 1
              required:
                - clientId
                - clientSecret
              description: GitHub OAuth App client credentials. Both fields are required.
      responses:
        "200":
          description: Saved.
          content:
            application/json:
              schema:
                type: object
                properties:
                  ok:
                    type: boolean
                  configured:
                    type: boolean
                    enum:
                      - true
                  clientId:
                    type: string
                required:
                  - ok
                  - configured
                  - clientId
        "400":
          description: clientId and clientSecret are both required.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ConfigErrorResponse"
    delete:
      tags:
        - Config
      summary: Clear personal GitHub OAuth App credentials
      responses:
        "200":
          description: Removed.
          content:
            application/json:
              schema:
                type: object
                properties:
                  ok:
                    type: boolean
                required:
                  - ok
  /api/config/google-oauth:
    get:
      tags:
        - Config
      summary: Read server-global Google OAuth app configuration status
      responses:
        "200":
          description: Google OAuth app status.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GoogleOAuthStatus"
        "401":
          description: Authentication required.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ConfigErrorResponse"
        "403":
          description: Requires the Admin role or higher.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ConfigErrorResponse"
    put:
      tags:
        - Config
      summary: Set server-global Google OAuth app credentials
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                clientId:
                  type: string
                  minLength: 1
                clientSecret:
                  type: string
                  minLength: 1
              required:
                - clientId
                - clientSecret
              description: Google OAuth app client credentials. Both fields are required.
      responses:
        "200":
          description: Saved.
          content:
            application/json:
              schema:
                type: object
                properties:
                  ok:
                    type: boolean
                  configured:
                    type: boolean
                    enum:
                      - true
                  clientId:
                    type: string
                required:
                  - ok
                  - configured
                  - clientId
        "400":
          description: clientId and clientSecret are both required.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ConfigErrorResponse"
        "401":
          description: Authentication required.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ConfigErrorResponse"
        "403":
          description: Requires the Admin role or higher.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ConfigErrorResponse"
    delete:
      tags:
        - Config
      summary: Clear server-global Google OAuth app credentials
      responses:
        "200":
          description: Removed.
          content:
            application/json:
              schema:
                type: object
                properties:
                  ok:
                    type: boolean
                required:
                  - ok
        "401":
          description: Authentication required.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ConfigErrorResponse"
        "403":
          description: Requires the Admin role or higher.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ConfigErrorResponse"
  /api/config/github-app:
    get:
      tags:
        - Config
      summary: Read server-global GitHub App configuration status (mirror-push bypass identity)
      responses:
        "200":
          description: GitHub App status. The private key is never returned.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GithubAppStatus"
        "401":
          description: Authentication required.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ConfigErrorResponse"
        "403":
          description: Requires the Admin role or higher.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ConfigErrorResponse"
    put:
      tags:
        - Config
      summary: Set server-global GitHub App credentials
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                appId:
                  anyOf:
                    - type: string
                      pattern: ^(?!0+$)\d+$
                    - type: integer
                      minimum: 0
                      exclusiveMinimum: true
                  description: GitHub App id (the `iss` of the signed App JWT) — a positive integer. Digits only, and not zero; a non-numeric or all-zero value is rejected with 400. Required.
                privateKey:
                  type: string
                  description: "PEM **RSA** private key. Optional on update: omit to keep the stored key (required on first configuration). A non-RSA or unparseable key is rejected with 400. Write-only — never returned on read."
                installationId:
                  anyOf:
                    - type: string
                      pattern: ^(?!0+$)\d+$
                    - type: integer
                      minimum: 0
                      exclusiveMinimum: true
                    - nullable: true
                  description: Default installation id — a positive integer. Cleared when absent or blank; a non-numeric or all-zero value is rejected with 400.
                installations:
                  type: array
                  items:
                    $ref: "#/components/schemas/GithubAppInstallation"
                  description: Per-owner installation map. Replaces the stored list; cleared when omitted.
              required:
                - appId
              description: Server-global GitHub App credentials for the mirror-push bypass identity. `appId` is required; `privateKey` is required only when none is already stored.
      responses:
        "200":
          description: Saved.
          content:
            application/json:
              schema:
                type: object
                properties:
                  ok:
                    type: boolean
                  configured:
                    type: boolean
                    enum:
                      - true
                  appId:
                    anyOf:
                      - type: string
                      - type: number
                  installationId:
                    anyOf:
                      - type: string
                      - type: number
                      - nullable: true
                  installations:
                    type: array
                    items:
                      $ref: "#/components/schemas/GithubAppInstallation"
                  hasPrivateKey:
                    type: boolean
                required:
                  - ok
                  - configured
                  - appId
                  - installationId
                  - installations
                  - hasPrivateKey
        "400":
          description: appId is required (and privateKey when none is stored).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ConfigErrorResponse"
        "401":
          description: Authentication required.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ConfigErrorResponse"
        "403":
          description: Requires the Admin role or higher.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ConfigErrorResponse"
    delete:
      tags:
        - Config
      summary: Clear server-global GitHub App credentials
      responses:
        "200":
          description: Removed.
          content:
            application/json:
              schema:
                type: object
                properties:
                  ok:
                    type: boolean
                required:
                  - ok
        "401":
          description: Authentication required.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ConfigErrorResponse"
        "403":
          description: Requires the Admin role or higher.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ConfigErrorResponse"
  /api/config/db-stats:
    get:
      tags:
        - Config
      summary: Read aggregated DB prepared-statement wall-time timings
      description: Per-statement timing aggregates for the async-DB Phase-1 instrumentation. Tags only — no SQL text or bound parameters are exposed. Empty when instrumentation is disabled (the default).
      responses:
        "200":
          description: Timing snapshot.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DbInstrumentationStats"
        "401":
          description: Authentication required.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ConfigErrorResponse"
        "403":
          description: Requires the Admin role or higher.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ConfigErrorResponse"
  /api/config/db-stats/reset:
    post:
      tags:
        - Config
      summary: Clear accumulated DB statement timing aggregates
      responses:
        "200":
          description: Cleared.
          content:
            application/json:
              schema:
                type: object
                properties:
                  ok:
                    type: boolean
                    enum:
                      - true
                required:
                  - ok
        "401":
          description: Authentication required.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ConfigErrorResponse"
        "403":
          description: Requires the Admin role or higher.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ConfigErrorResponse"
  /api/github/status:
    get:
      tags:
        - GitHub
      summary: Read `gh` CLI auth status
      responses:
        "200":
          description: GitHub status.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GithubStatus"
  /api/github/detect-repo:
    post:
      tags:
        - GitHub
      summary: Detect the GitHub remote in a working directory
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                cwd:
                  type: string
                  minLength: 1
              required:
                - cwd
              description: Working directory to inspect with `git remote get-url origin`.
      responses:
        "200":
          description: Remote detection result.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DetectRepoResponse"
        "400":
          description: cwd is required.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ConfigErrorResponse"
  /api/github/test-connection:
    post:
      tags:
        - GitHub
      summary: Probe `gh api repos/{owner}/{repo}` to verify connectivity
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                owner:
                  type: string
                  minLength: 1
                repo:
                  type: string
                  minLength: 1
              required:
                - owner
                - repo
              description: GitHub repo coords. Both fields must match `^[a-zA-Z0-9._-]+$`; the handler enforces the regex.
      responses:
        "200":
          description: Connection probe result.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TestConnectionResponse"
        "400":
          description: Missing or invalid owner/repo.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ConfigErrorResponse"
  /api/projects/{projectId}/export:
    get:
      tags:
        - Projects
      summary: Export a project (project + crons + rooms + wiki + kanban)
      description: Returns a V3 export envelope as a downloadable JSON attachment. Session-id linkages on kanban cards are stripped.
      parameters:
        - schema:
            type: string
            description: Project slug or id.
          required: true
          description: Project slug or id.
          name: projectId
          in: path
      responses:
        "200":
          description: Project export.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ProjectExportEnvelope"
        "404":
          description: Project not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ConfigErrorResponse"
        "500":
          description: Export failed.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ConfigErrorResponse"
  /api/projects/{projectId}/import:
    post:
      tags:
        - Projects
      summary: Merge a V3 project export into an existing project
      description: Per-section idempotent merge — duplicate names/slugs are skipped. Errors are tagged with the offending section (`crons`, `wiki`, `kanban`, etc.) for debuggability.
      parameters:
        - schema:
            type: string
            description: Project slug or id.
          required: true
          description: Project slug or id.
          name: projectId
          in: path
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ProjectExportEnvelope"
      responses:
        "200":
          description: Per-section merge results.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ImportResult"
        "400":
          description: Invalid envelope (wrong version/type).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ConfigErrorResponse"
        "404":
          description: Target project not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ConfigErrorResponse"
        "500":
          description: Import failed (response includes `section` when applicable).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ConfigErrorResponse"
  /api/projects/import:
    post:
      tags:
        - Projects
      summary: Create a new project from a V3 export
      description: Materialises a fresh project record (and its data dir) from the embedded `project` block, then runs the same merge logic as the per-project endpoint. Generates a unique slug if the exported id collides.
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ProjectExportEnvelope"
      responses:
        "201":
          description: Created.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ImportResult"
        "400":
          description: Invalid envelope or missing `project` block.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ConfigErrorResponse"
        "500":
          description: Import failed (response includes `section` when applicable).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ConfigErrorResponse"
  /api/config/export:
    get:
      tags:
        - Config
      summary: Export server-wide config (config + projects + crons + rooms + slack)
      description: Returns a V2 export envelope as a downloadable JSON attachment. Slack bot/app tokens are redacted.
      responses:
        "200":
          description: Config export.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ConfigExportEnvelope"
        "500":
          description: Export failed.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ConfigErrorResponse"
  /api/config/import:
    post:
      tags:
        - Config
      summary: Import a V1 or V2 server-wide config export
      description: Writes `config.json`, replaces `projects.json` (or upgrades a V1 `agents` array), merges crons + rooms by name, and writes `slack-config.json` only if real (non-redacted) tokens are present.
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ConfigExportEnvelope"
      responses:
        "200":
          description: Per-section import results.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ImportResult"
        "400":
          description: Invalid export format (expected version 1 or 2).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ConfigErrorResponse"
        "500":
          description: Import failed.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ConfigErrorResponse"
  /api/crons:
    get:
      tags:
        - Crons
      summary: List visible cron jobs
      description: Returns shared crons, crons owned by the caller, and all crons for org Owners. Private crons are hidden from other non-Owner users.
      responses:
        "200":
          description: Array of cron jobs.
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Cron"
    post:
      tags:
        - Crons
      summary: Create a new cron job
      description: "`schedule` must be a valid cron expression. The cron owner is the authenticated caller. `shared` defaults to false; when true the cron is visible to the org but still executes under the owner's credentials. `engine` (when provided) must be one of: claude-code, cursor-agent, codex-cli, gemini-cli, grok-cli — when omitted/null, the cron inherits its engine from the resolved skill principal agent at run time, falling back to `claude-code`. `model` is validated against `engineValidModels[engine]` for whichever engine the cron resolves to. `skill_principal_agent_id` (when provided) must be an agent in the cron's project."
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                name:
                  type: string
                  minLength: 1
                schedule:
                  type: string
                timezone:
                  type: string
                  nullable: true
                prompt:
                  type: string
                  minLength: 1
                cwd:
                  type: string
                enabled:
                  type: boolean
                project_id:
                  type: string
                  nullable: true
                timeout_ms:
                  anyOf:
                    - type: string
                      enum:
                        - ""
                    - nullable: true
                    - type: integer
                      minimum: 0
                      exclusiveMinimum: true
                notify_on_run:
                  anyOf:
                    - type: boolean
                    - type: number
                      enum:
                        - 0
                    - type: number
                      enum:
                        - 1
                    - type: string
                      enum:
                        - "0"
                    - type: string
                      enum:
                        - "1"
                    - type: string
                      enum:
                        - "true"
                    - type: string
                      enum:
                        - "false"
                shared:
                  anyOf:
                    - type: boolean
                    - type: number
                      enum:
                        - 0
                    - type: number
                      enum:
                        - 1
                    - type: string
                      enum:
                        - "0"
                    - type: string
                      enum:
                        - "1"
                model:
                  type: string
                  nullable: true
                skill_principal_agent_id:
                  type: string
                  nullable: true
                engine:
                  anyOf:
                    - type: string
                    - nullable: true
                    - type: string
                      enum:
                        - ""
              required:
                - name
                - schedule
                - prompt
      responses:
        "200":
          description: Created cron.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Cron"
        "400":
          description: Validation failed.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CronErrorResponse"
  /api/crons/{id}:
    put:
      tags:
        - Crons
      summary: Update a cron job
      description: Owner or cron owner only. Omitted fields preserve the existing value. `timeout_ms` / `notify_on_run` / `model` / `skill_principal_agent_id` / `engine` follow the present-key tristate (`undefined` = preserve, `null`/`""` = clear). `shared` toggles org-wide visibility without changing execution owner. When `engine` changes and an existing `model` is no longer valid for the new engine, the model is cleared (the user can resend a compatible value in the same PUT).
      parameters:
        - schema:
            type: string
            description: Cron job numeric id (URL string).
          required: true
          description: Cron job numeric id (URL string).
          name: id
          in: path
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                name:
                  type: string
                schedule:
                  type: string
                timezone:
                  type: string
                  nullable: true
                prompt:
                  type: string
                cwd:
                  type: string
                enabled:
                  type: boolean
                project_id:
                  type: string
                  nullable: true
                timeout_ms:
                  anyOf:
                    - type: string
                      enum:
                        - ""
                    - nullable: true
                    - type: integer
                      minimum: 0
                      exclusiveMinimum: true
                notify_on_run:
                  anyOf:
                    - type: boolean
                    - type: number
                      enum:
                        - 0
                    - type: number
                      enum:
                        - 1
                    - type: string
                      enum:
                        - "0"
                    - type: string
                      enum:
                        - "1"
                    - type: string
                      enum:
                        - "true"
                    - type: string
                      enum:
                        - "false"
                shared:
                  anyOf:
                    - type: boolean
                    - type: number
                      enum:
                        - 0
                    - type: number
                      enum:
                        - 1
                    - type: string
                      enum:
                        - "0"
                    - type: string
                      enum:
                        - "1"
                model:
                  type: string
                  nullable: true
                skill_principal_agent_id:
                  type: string
                  nullable: true
                engine:
                  anyOf:
                    - type: string
                    - nullable: true
                    - type: string
                      enum:
                        - ""
      responses:
        "200":
          description: Updated cron.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Cron"
        "400":
          description: Validation failed.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CronErrorResponse"
        "404":
          description: Cron not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CronErrorResponse"
    delete:
      tags:
        - Crons
      summary: Delete a cron job
      description: Disables the in-memory schedule and removes the row.
      parameters:
        - schema:
            type: string
            description: Cron job numeric id (URL string).
          required: true
          description: Cron job numeric id (URL string).
          name: id
          in: path
      responses:
        "200":
          description: Delete ack.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CronDeleteAck"
  /api/crons/{id}/logs:
    get:
      tags:
        - Crons
      summary: List recent run logs for a cron
      parameters:
        - schema:
            type: string
            description: Cron job numeric id (URL string).
          required: true
          description: Cron job numeric id (URL string).
          name: id
          in: path
        - schema:
            type: string
            description: Max rows (default 3, capped at 50).
          required: false
          description: Max rows (default 3, capped at 50).
          name: limit
          in: query
      responses:
        "200":
          description: Array of log rows.
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/CronLog"
  /api/crons/{id}/run:
    post:
      tags:
        - Crons
      summary: Trigger an immediate one-shot cron run
      description: Returns immediately; the job runs asynchronously.
      parameters:
        - schema:
            type: string
            description: Cron job numeric id (URL string).
          required: true
          description: Cron job numeric id (URL string).
          name: id
          in: path
      responses:
        "200":
          description: Run ack.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CronRunAck"
        "404":
          description: Cron not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CronErrorResponse"
  /api/crons/{id}/thread:
    get:
      tags:
        - Crons
      summary: Fetch the persistent output thread for a cron
      parameters:
        - schema:
            type: string
            description: Cron job numeric id (URL string).
          required: true
          description: Cron job numeric id (URL string).
          name: id
          in: path
      responses:
        "200":
          description: Thread + entries (both null/empty when no run has produced a thread yet).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CronThreadResponse"
        "404":
          description: Cron not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CronErrorResponse"
  /api/orgs/{id}/dashboard:
    get:
      tags:
        - Dashboard
      summary: Org-wide dashboard counters + recent activity
      description: Aggregates project/agent/session/kanban/PR counters for the **active** org. Use `:id = "active"` to ask for the current org without knowing its real id (used by remote-mode clients). Returns 409 if the requested org is not the active one.
      parameters:
        - schema:
            type: string
            description: Org id, or the literal string `active`.
          required: true
          description: Org id, or the literal string `active`.
          name: id
          in: path
      responses:
        "200":
          description: Dashboard payload.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DashboardResponse"
        "401":
          description: Authentication required.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DashboardErrorResponse"
        "403":
          description: Caller is not a member of this org.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DashboardErrorResponse"
        "404":
          description: Org not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DashboardErrorResponse"
        "409":
          description: Org is not the active one — switch first.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DashboardErrorResponse"
  /api/projects/{projectId}/deploy/setup-wizard:
    post:
      tags:
        - Deployments
      summary: Start an AI deploy.yaml setup workflow
      description: Admin+. Spawns a worktree-backed setup session loaded with the `deploy-setup` skill so an agent can author `.agent-hub/deploy.yaml` on a reviewable branch.
      parameters:
        - schema:
            type: string
          required: true
          name: projectId
          in: path
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties: {}
              additionalProperties:
                nullable: true
      responses:
        "201":
          description: Deploy setup wizard session created.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DeploySetupWizardResponse"
        "400":
          description: Project has no cwd or no agent to host the wizard.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DeploymentErrorResponse"
        "403":
          description: Admin role required.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DeploymentErrorResponse"
        "404":
          description: Project not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DeploymentErrorResponse"
  /api/projects/{projectId}/deploy/config:
    get:
      tags:
        - Deployments
      summary: Read deploy.yaml environments and live deployment state
      parameters:
        - schema:
            type: string
          required: true
          name: projectId
          in: path
      responses:
        "200":
          description: Deployment environments configured in deploy.yaml with live state.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DeployConfigResponse"
        "400":
          description: Invalid deploy.yaml.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DeploymentErrorResponse"
        "404":
          description: Project or deploy.yaml not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DeploymentErrorResponse"
  /api/projects/{projectId}/deploy/environments:
    get:
      tags:
        - Deployments
      summary: Read resolved deployment environments (deploy.yaml + operator config)
      description: Merges deploy.yaml-declared environments with per-environment operator runtime config, tagging each active/enabled/deployable. Also surfaces orphaned config rows whose environment was removed from deploy.yaml (active:false, deployable:false) so they are never silently dropped. A missing deploy.yaml returns those config rows with no declared environments rather than a 404.
      parameters:
        - schema:
            type: string
          required: true
          name: projectId
          in: path
      responses:
        "200":
          description: Resolved environment list.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvironmentsReadResponse"
        "400":
          description: Invalid deploy.yaml.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DeploymentErrorResponse"
        "404":
          description: Project not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DeploymentErrorResponse"
  /api/projects/{projectId}/deploy/environments/{environmentName}:
    patch:
      tags:
        - Deployments
      summary: Enable or disable an environment (operator runtime config)
      description: Admin+. Flips the per-environment operator pause switch without a commit. deploy.yaml stays the source of truth for which environments exist; a disabled environment is not deployable. Allowed on any environment declared in deploy.yaml or that already has a config row; an unknown environment name is 404.
      parameters:
        - schema:
            type: string
          required: true
          name: projectId
          in: path
        - schema:
            type: string
          required: true
          name: environmentName
          in: path
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                enabled:
                  type: boolean
              required:
                - enabled
      responses:
        "200":
          description: Updated resolved environment list.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvironmentsReadResponse"
        "400":
          description: Invalid body or deploy.yaml.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DeploymentErrorResponse"
        "403":
          description: Admin role required.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DeploymentErrorResponse"
        "404":
          description: Project or environment not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DeploymentErrorResponse"
    delete:
      tags:
        - Deployments
      summary: Remove an environment runtime config row
      description: "Admin+. Deletes the per-environment operator config row. For a still-declared environment this resets it to the enabled default; for an orphaned environment (removed from deploy.yaml) this cleans up the stale row. Idempotent: `removed` is false when there was no row."
      parameters:
        - schema:
            type: string
          required: true
          name: projectId
          in: path
        - schema:
            type: string
          required: true
          name: environmentName
          in: path
      responses:
        "200":
          description: Removal result plus the refreshed resolved environment list.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvironmentConfigDeleteResponse"
        "400":
          description: Invalid deploy.yaml.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DeploymentErrorResponse"
        "403":
          description: Admin role required.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DeploymentErrorResponse"
        "404":
          description: Project not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DeploymentErrorResponse"
  /api/projects/{projectId}/deploy/environments/{environmentName}/triggers:
    get:
      tags:
        - Deployments
      summary: List deploy triggers for an environment
      description: Operator-editable git-event triggers keyed by (project, environment). Each trigger fires a deployment for its environment when a matching push/merge updates a branch matching its pattern. Triggers whose environment was removed from deploy.yaml are retained and listed (they never fire).
      parameters:
        - schema:
            type: string
          required: true
          name: projectId
          in: path
        - schema:
            type: string
          required: true
          name: environmentName
          in: path
      responses:
        "200":
          description: Deploy triggers for the environment.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DeployTriggerListResponse"
        "404":
          description: Project not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DeploymentErrorResponse"
    post:
      tags:
        - Deployments
      summary: Create a deploy trigger for an environment
      description: Admin+. Creates a git-event deploy trigger without a commit. The environment must be declared in deploy.yaml or already have a runtime config row; an unknown environment name is 404. A duplicate (event, branchPattern) on the same environment is 409.
      parameters:
        - schema:
            type: string
          required: true
          name: projectId
          in: path
        - schema:
            type: string
          required: true
          name: environmentName
          in: path
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                event:
                  type: string
                  enum:
                    - push
                    - merge
                branchPattern:
                  type: string
                  minLength: 1
                  maxLength: 200
                enabled:
                  type: boolean
                meta:
                  nullable: true
              required:
                - event
                - branchPattern
      responses:
        "201":
          description: Created deploy trigger.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DeployTriggerResponse"
        "400":
          description: Invalid body or deploy.yaml.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DeploymentErrorResponse"
        "403":
          description: Admin role required.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DeploymentErrorResponse"
        "404":
          description: Project or environment not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DeploymentErrorResponse"
        "409":
          description: Duplicate trigger for this event and branch pattern.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DeploymentErrorResponse"
  /api/projects/{projectId}/deploy/environments/{environmentName}/triggers/{triggerId}:
    patch:
      tags:
        - Deployments
      summary: Update a deploy trigger
      description: "Admin+. Partial update: omitted fields keep their current value. A change that collides with another trigger (event, branchPattern) on the environment is 409."
      parameters:
        - schema:
            type: string
          required: true
          name: projectId
          in: path
        - schema:
            type: string
          required: true
          name: environmentName
          in: path
        - schema:
            type: string
          required: true
          name: triggerId
          in: path
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                event:
                  type: string
                  enum:
                    - push
                    - merge
                branchPattern:
                  type: string
                  minLength: 1
                  maxLength: 200
                enabled:
                  type: boolean
                meta:
                  nullable: true
      responses:
        "200":
          description: Updated deploy trigger.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DeployTriggerResponse"
        "400":
          description: Invalid body.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DeploymentErrorResponse"
        "403":
          description: Admin role required.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DeploymentErrorResponse"
        "404":
          description: Project or trigger not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DeploymentErrorResponse"
        "409":
          description: Duplicate trigger for this event and branch pattern.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DeploymentErrorResponse"
    delete:
      tags:
        - Deployments
      summary: Delete a deploy trigger
      description: Admin+. Removes the trigger row.
      parameters:
        - schema:
            type: string
          required: true
          name: projectId
          in: path
        - schema:
            type: string
          required: true
          name: environmentName
          in: path
        - schema:
            type: string
          required: true
          name: triggerId
          in: path
      responses:
        "200":
          description: Deletion result.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DeployTriggerDeleteResponse"
        "403":
          description: Admin role required.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DeploymentErrorResponse"
        "404":
          description: Project or trigger not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DeploymentErrorResponse"
  /api/projects/{projectId}/deploy/environments/{environmentName}/schedules:
    get:
      tags:
        - Deployments
      summary: List deploy schedules for an environment
      description: Operator-editable cron deploy schedules keyed by (project, environment). Each schedule fires a deployment for its environment when its node-cron expression ticks, running under the owner identity. Schedules whose environment was removed from deploy.yaml are retained and listed (they never fire).
      parameters:
        - schema:
            type: string
          required: true
          name: projectId
          in: path
        - schema:
            type: string
          required: true
          name: environmentName
          in: path
      responses:
        "200":
          description: Deploy schedules for the environment.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DeployScheduleListResponse"
        "404":
          description: Project not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DeploymentErrorResponse"
    post:
      tags:
        - Deployments
      summary: Create a deploy schedule for an environment
      description: Admin+. Creates a cron deploy schedule without a commit. The environment must be declared in deploy.yaml or already have a runtime config row; an unknown environment name is 404. A duplicate (ref, cron) on the same environment is 409. The schedule runs under the creating caller identity.
      parameters:
        - schema:
            type: string
          required: true
          name: projectId
          in: path
        - schema:
            type: string
          required: true
          name: environmentName
          in: path
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                ref:
                  type: string
                  minLength: 1
                  maxLength: 255
                cron:
                  type: string
                  minLength: 1
                  maxLength: 200
                timezone:
                  type: string
                  nullable: true
                  minLength: 1
                enabled:
                  type: boolean
                meta:
                  nullable: true
              required:
                - ref
                - cron
      responses:
        "201":
          description: Created deploy schedule.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DeployScheduleResponse"
        "400":
          description: Invalid body or deploy.yaml.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DeploymentErrorResponse"
        "403":
          description: Admin role required.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DeploymentErrorResponse"
        "404":
          description: Project or environment not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DeploymentErrorResponse"
        "409":
          description: Duplicate schedule for this ref and cron.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DeploymentErrorResponse"
  /api/projects/{projectId}/deploy/environments/{environmentName}/schedules/{scheduleId}:
    patch:
      tags:
        - Deployments
      summary: Update a deploy schedule
      description: "Admin+. Partial update: omitted fields keep their current value. The owner identity is fixed at create time and cannot be changed. A change that collides with another schedule (ref, cron) on the environment is 409."
      parameters:
        - schema:
            type: string
          required: true
          name: projectId
          in: path
        - schema:
            type: string
          required: true
          name: environmentName
          in: path
        - schema:
            type: string
          required: true
          name: scheduleId
          in: path
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                ref:
                  type: string
                  minLength: 1
                  maxLength: 255
                cron:
                  type: string
                  minLength: 1
                  maxLength: 200
                timezone:
                  type: string
                  nullable: true
                  minLength: 1
                enabled:
                  type: boolean
                meta:
                  nullable: true
      responses:
        "200":
          description: Updated deploy schedule.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DeployScheduleResponse"
        "400":
          description: Invalid body.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DeploymentErrorResponse"
        "403":
          description: Admin role required.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DeploymentErrorResponse"
        "404":
          description: Project or schedule not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DeploymentErrorResponse"
        "409":
          description: Duplicate schedule for this ref and cron.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DeploymentErrorResponse"
    delete:
      tags:
        - Deployments
      summary: Delete a deploy schedule
      description: Admin+. Removes the schedule row.
      parameters:
        - schema:
            type: string
          required: true
          name: projectId
          in: path
        - schema:
            type: string
          required: true
          name: environmentName
          in: path
        - schema:
            type: string
          required: true
          name: scheduleId
          in: path
      responses:
        "200":
          description: Deletion result.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DeployScheduleDeleteResponse"
        "403":
          description: Admin role required.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DeploymentErrorResponse"
        "404":
          description: Project or schedule not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DeploymentErrorResponse"
  /api/projects/{projectId}/deploy/release-gate-candidates:
    get:
      tags:
        - Deployments
      summary: List candidate sessions for a release gate
      description: Project-wide, server-validated list of in-flight sessions the operator may add to a release gate. Derived from non-terminal board cards but filtered to sessions that still exist and are not yet merged, so a purged or corrupt session id left on an old card is never offered.
      parameters:
        - schema:
            type: string
          required: true
          name: projectId
          in: path
      responses:
        "200":
          description: Candidate sessions for a release gate.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ReleaseGateCandidatesResponse"
        "404":
          description: Project not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DeploymentErrorResponse"
  /api/projects/{projectId}/deploy/environments/{environmentName}/release-gates:
    get:
      tags:
        - Deployments
      summary: List release gates for an environment
      description: Operator-editable one-shot release gates keyed by (project, environment). A gate fires a single deployment once every selected session is merged AND every selected epic is done, then is consumed (status → fired). Each gate carries live completion `progress`. Gates whose environment was removed from deploy.yaml are retained and listed (they never fire).
      parameters:
        - schema:
            type: string
          required: true
          name: projectId
          in: path
        - schema:
            type: string
          required: true
          name: environmentName
          in: path
      responses:
        "200":
          description: Release gates for the environment.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DeployReleaseGateListResponse"
        "404":
          description: Project not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DeploymentErrorResponse"
    post:
      tags:
        - Deployments
      summary: Create a release gate for an environment
      description: Admin+. Creates a one-shot release gate without a commit. The gate must watch at least one session or epic. The environment must be declared in deploy.yaml or already have a runtime config row; an unknown environment name is 404. The deployment fires under the creating caller identity, with ref defaulting to `main`.
      parameters:
        - schema:
            type: string
          required: true
          name: projectId
          in: path
        - schema:
            type: string
          required: true
          name: environmentName
          in: path
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                ref:
                  type: string
                  nullable: true
                  minLength: 1
                  maxLength: 255
                sessionIds:
                  type: array
                  items:
                    type: string
                    minLength: 1
                  maxItems: 100
                epicIds:
                  type: array
                  items:
                    type: string
                    minLength: 1
                  maxItems: 100
                enabled:
                  type: boolean
                meta:
                  nullable: true
      responses:
        "201":
          description: Created release gate.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DeployReleaseGateResponse"
        "400":
          description: Invalid body or deploy.yaml.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DeploymentErrorResponse"
        "403":
          description: Admin role required.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DeploymentErrorResponse"
        "404":
          description: Project or environment not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DeploymentErrorResponse"
  /api/projects/{projectId}/deploy/environments/{environmentName}/release-gates/{gateId}:
    patch:
      tags:
        - Deployments
      summary: Update a release gate
      description: "Admin+. Partial update of an armed gate: omitted fields keep their current value. The owner identity and lifecycle status are fixed. A fired/failed gate is terminal (retry by delete + recreate). The gate must still watch at least one session or epic."
      parameters:
        - schema:
            type: string
          required: true
          name: projectId
          in: path
        - schema:
            type: string
          required: true
          name: environmentName
          in: path
        - schema:
            type: string
          required: true
          name: gateId
          in: path
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                ref:
                  type: string
                  nullable: true
                  minLength: 1
                  maxLength: 255
                sessionIds:
                  type: array
                  items:
                    type: string
                    minLength: 1
                  maxItems: 100
                epicIds:
                  type: array
                  items:
                    type: string
                    minLength: 1
                  maxItems: 100
                enabled:
                  type: boolean
                meta:
                  nullable: true
      responses:
        "200":
          description: Updated release gate.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DeployReleaseGateResponse"
        "400":
          description: Invalid body.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DeploymentErrorResponse"
        "403":
          description: Admin role required.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DeploymentErrorResponse"
        "404":
          description: Project or gate not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DeploymentErrorResponse"
    delete:
      tags:
        - Deployments
      summary: Delete a release gate
      description: Admin+. Removes the gate row (the way to cancel a pending release).
      parameters:
        - schema:
            type: string
          required: true
          name: projectId
          in: path
        - schema:
            type: string
          required: true
          name: environmentName
          in: path
        - schema:
            type: string
          required: true
          name: gateId
          in: path
      responses:
        "200":
          description: Deletion result.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DeployReleaseGateDeleteResponse"
        "403":
          description: Admin role required.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DeploymentErrorResponse"
        "404":
          description: Project or gate not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DeploymentErrorResponse"
  /api/projects/{projectId}/deploy/environments/{environmentName}/notification-routing:
    get:
      tags:
        - Deployments
      summary: Get notification routing for an environment
      description: Resolves which release notification types (ticket_release, release_digest) fire when a deployment to this environment succeeds. When no operator override is saved, the resolved default is prod → reporter + digest, non-prod → nothing (isDefault=true).
      parameters:
        - schema:
            type: string
          required: true
          name: projectId
          in: path
        - schema:
            type: string
          required: true
          name: environmentName
          in: path
      responses:
        "200":
          description: Resolved notification routing for the environment.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/NotificationRoutingResponse"
        "404":
          description: Project not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DeploymentErrorResponse"
    put:
      tags:
        - Deployments
      summary: Update notification routing for an environment
      description: "Admin+. Saves which release notification types fire for this environment without a commit. Partial update: omitted fields keep their current value (a new row seeds from the env-name default). The environment must be declared in deploy.yaml or already have a runtime config row; an unknown environment name is 404."
      parameters:
        - schema:
            type: string
          required: true
          name: projectId
          in: path
        - schema:
            type: string
          required: true
          name: environmentName
          in: path
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                ticketReleaseEnabled:
                  type: boolean
                releaseDigestEnabled:
                  type: boolean
                meta:
                  nullable: true
      responses:
        "200":
          description: Resolved notification routing after the update.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/NotificationRoutingResponse"
        "400":
          description: Invalid body or deploy.yaml.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DeploymentErrorResponse"
        "403":
          description: Admin role required.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DeploymentErrorResponse"
        "404":
          description: Project or environment not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DeploymentErrorResponse"
  /api/projects/{projectId}/deployments:
    get:
      tags:
        - Deployments
      summary: List deployments
      parameters:
        - schema:
            type: string
          required: true
          name: projectId
          in: path
        - schema:
            type: string
            minLength: 1
          required: false
          name: environment
          in: query
        - schema:
            type: integer
            minimum: 1
            maximum: 200
          required: false
          name: limit
          in: query
        - schema:
            type: integer
            nullable: true
            minimum: 0
          required: false
          name: offset
          in: query
      responses:
        "200":
          description: Deployment history.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DeploymentListResponse"
        "400":
          description: Invalid query.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DeploymentErrorResponse"
        "404":
          description: Project not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DeploymentErrorResponse"
    post:
      tags:
        - Deployments
      summary: Trigger a deployment
      parameters:
        - schema:
            type: string
          required: true
          name: projectId
          in: path
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                environment:
                  type: string
                  minLength: 1
                ref:
                  type: string
                  minLength: 1
                sessionId:
                  type: string
                  minLength: 1
                meta:
                  nullable: true
              required:
                - environment
                - ref
      responses:
        "202":
          description: Deployment accepted.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DeploymentActionResponse"
        "400":
          description: Invalid body, deploy.yaml, or ref.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DeploymentErrorResponse"
        "404":
          description: Project or environment not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DeploymentErrorResponse"
        "409":
          description: Environment is already busy.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DeploymentErrorResponse"
  /api/projects/{projectId}/deployments/{deploymentId}/release-notifications/{notificationId}/retry:
    post:
      tags:
        - Deployments
      summary: Retry a failed release notification
      description: Admin+. Requeues the existing failed release notification outbox row. Sent rows are never duplicated.
      parameters:
        - schema:
            type: string
          required: true
          name: projectId
          in: path
        - schema:
            type: string
          required: true
          name: deploymentId
          in: path
        - schema:
            type: string
          required: true
          name: notificationId
          in: path
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties: {}
              additionalProperties:
                nullable: true
      responses:
        "200":
          description: Existing notification row requeued for delivery.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ReleaseNotificationRetryResponse"
        "403":
          description: Admin role required.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DeploymentErrorResponse"
        "404":
          description: Project, deployment, or release notification not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DeploymentErrorResponse"
        "409":
          description: Notification is already sent or not failed.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DeploymentErrorResponse"
  /api/projects/{projectId}/deployments/{deploymentId}:
    get:
      tags:
        - Deployments
      summary: Get deployment status
      parameters:
        - schema:
            type: string
          required: true
          name: projectId
          in: path
        - schema:
            type: string
          required: true
          name: deploymentId
          in: path
      responses:
        "200":
          description: Deployment detail.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DeploymentDetailResponse"
        "404":
          description: Project or deployment not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DeploymentErrorResponse"
  /api/projects/{projectId}/deployments/{deploymentId}/release-items:
    get:
      tags:
        - Deployments
      summary: List reviewed release items for a deployment
      parameters:
        - schema:
            type: string
          required: true
          name: projectId
          in: path
        - schema:
            type: string
          required: true
          name: deploymentId
          in: path
      responses:
        "200":
          description: Release items with card and support-ticket review context.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DeploymentReleaseItemListResponse"
        "404":
          description: Project or deployment not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DeploymentErrorResponse"
  /api/projects/{projectId}/deployments/{deploymentId}/notification-recipients:
    get:
      tags:
        - Deployments
      summary: List a deployment’s release notification recipients
      description: Admin+. Returns the deployment’s release notification outbox rows projected with their actual `recipient_email` so operators can audit exactly who was (or will be) notified, along with status, attempts, sent time, and a sanitized error summary. Unlike the safe `releaseNotifications` history embedded in the deployment detail response, this endpoint exposes recipient addresses and therefore requires Admin.
      parameters:
        - schema:
            type: string
          required: true
          name: projectId
          in: path
        - schema:
            type: string
          required: true
          name: deploymentId
          in: path
      responses:
        "200":
          description: Release notification recipients with delivery status.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DeploymentNotificationRecipientsResponse"
        "403":
          description: Admin role required.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DeploymentErrorResponse"
        "404":
          description: Project or deployment not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DeploymentErrorResponse"
  /api/projects/{projectId}/deployments/{deploymentId}/release-digest:
    post:
      tags:
        - Deployments
      summary: Generate a deployment release digest draft
      description: Admin+. Generates a customer-facing markdown release digest draft from included release items. The model prompt always wraps the stored project release digest prompt inside the fixed fact-bounded generation template.
      parameters:
        - schema:
            type: string
          required: true
          name: projectId
          in: path
        - schema:
            type: string
          required: true
          name: deploymentId
          in: path
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties: {}
              additionalProperties:
                nullable: true
      responses:
        "200":
          description: Generated release digest draft.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DeploymentReleaseDigestResponse"
        "403":
          description: Admin role required.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DeploymentErrorResponse"
        "404":
          description: Project or deployment not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DeploymentErrorResponse"
        "500":
          description: Digest generation failed.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DeploymentErrorResponse"
  /api/projects/{projectId}/deployments/{deploymentId}/release-items/{cardId}:
    put:
      tags:
        - Deployments
      summary: Include or exclude a linked card in a deployment release
      description: Admin+. Creates a release item for a missed card or updates an existing item, stamping operator audit fields with the caller, timestamp, and reason.
      parameters:
        - schema:
            type: string
          required: true
          name: projectId
          in: path
        - schema:
            type: string
          required: true
          name: deploymentId
          in: path
        - schema:
            type: string
          required: true
          name: cardId
          in: path
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                inclusionStatus:
                  type: string
                  enum:
                    - included
                    - excluded
                reason:
                  type: string
                  minLength: 1
                  maxLength: 2000
                supportTicketId:
                  type: string
                  nullable: true
                  minLength: 1
              required:
                - inclusionStatus
                - reason
      responses:
        "200":
          description: Adjusted release item and refreshed release item list.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DeploymentReleaseItemResponse"
        "400":
          description: Invalid request body or support-ticket link.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DeploymentErrorResponse"
        "403":
          description: Admin role required.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DeploymentErrorResponse"
        "404":
          description: Project, deployment, or card not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DeploymentErrorResponse"
        "409":
          description: Deployment release items are no longer reviewable.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DeploymentErrorResponse"
  /api/projects/{projectId}/deployments/{deploymentId}/cancel:
    post:
      tags:
        - Deployments
      summary: Cancel a deployment
      parameters:
        - schema:
            type: string
          required: true
          name: projectId
          in: path
        - schema:
            type: string
          required: true
          name: deploymentId
          in: path
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                reason:
                  type: string
                  maxLength: 2000
      responses:
        "202":
          description: Action accepted.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DeploymentActionResponse"
        "400":
          description: Invalid request or action is not applicable.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DeploymentErrorResponse"
        "403":
          description: Admin role required.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DeploymentErrorResponse"
        "404":
          description: Project or deployment not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DeploymentErrorResponse"
        "409":
          description: Deployment is no longer actionable.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DeploymentErrorResponse"
  /api/projects/{projectId}/deployments/{deploymentId}/approve:
    post:
      tags:
        - Deployments
      summary: Approve a gated deployment
      parameters:
        - schema:
            type: string
          required: true
          name: projectId
          in: path
        - schema:
            type: string
          required: true
          name: deploymentId
          in: path
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                note:
                  type: string
                  maxLength: 2000
      responses:
        "202":
          description: Action accepted.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DeploymentActionResponse"
        "400":
          description: Invalid request or action is not applicable.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DeploymentErrorResponse"
        "403":
          description: Admin role required.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DeploymentErrorResponse"
        "404":
          description: Project or deployment not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DeploymentErrorResponse"
        "409":
          description: Deployment is no longer awaiting approval.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DeploymentErrorResponse"
  /api/projects/{projectId}/deployments/{deploymentId}/rollback:
    post:
      tags:
        - Deployments
      summary: Rollback by redeploying a historical ref
      parameters:
        - schema:
            type: string
          required: true
          name: projectId
          in: path
        - schema:
            type: string
          required: true
          name: deploymentId
          in: path
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                sessionId:
                  type: string
                  minLength: 1
                meta:
                  nullable: true
      responses:
        "202":
          description: Action accepted.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DeploymentActionResponse"
        "400":
          description: Invalid request or action is not applicable.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DeploymentErrorResponse"
        "403":
          description: Admin role required.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DeploymentErrorResponse"
        "404":
          description: Project or deployment not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DeploymentErrorResponse"
        "409":
          description: Environment is already busy.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DeploymentErrorResponse"
  /api/designs/{id}/import:
    post:
      tags:
        - Designs
      summary: Migrate a standalone design into a design-mode session
      description: "Creates a `design`-mode session for an agent in one of the design’s linked projects, provisions its worktree, copies the design artifact dir into `<worktree>/design/`, and replays the design transcript as session messages. Non-destructive: the design row, files, and messages are preserved (standalone tables are dropped a release later, gated on parity). Idempotent — re-running returns the existing mapping with `reused: true`."
      parameters:
        - schema:
            type: string
            description: Design id.
          required: true
          description: Design id.
          name: id
          in: path
      responses:
        "200":
          description: Design was already imported; existing session returned.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DesignImportResult"
        "201":
          description: Design imported into a new session.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DesignImportResult"
        "404":
          description: Design not found.
        "409":
          description: 'Import could not proceed. `error: "design_import_failed"` (e.g. no eligible target agent in a linked project) is terminal; `error: "import_in_progress"` (`retryable: true`) means a concurrent import holds the lock — retry shortly.'
        "503":
          description: Worktree provisioning is not wired.
  /api/projects/{projectId}/dev-server/setup-draft:
    get:
      tags:
        - Dev Server
      summary: Scan a project for dev-server setup signal
      description: "Admin+. Reads the project working copy and returns the detection draft the Dev Server setup wizard needs: start-command candidates from `package.json` scripts, package manager, monorepo layout, framework/port guesses, a health-path default, the existing config, and a README excerpt. Read-only — no session spawn, no writes."
      parameters:
        - schema:
            type: string
            description: Project slug.
          required: true
          description: Project slug.
          name: projectId
          in: path
      responses:
        "200":
          description: Detection draft computed.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DevServerSetupDraftResponse"
        "400":
          description: Project has no cwd configured.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DevServerWizardErrorResponse"
        "404":
          description: Project not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DevServerWizardErrorResponse"
  /api/projects/{projectId}/dev-server/setup-wizard:
    post:
      tags:
        - Dev Server
      summary: Spawn the Dev Server setup wizard
      description: Admin+. Scans the project and spawns a worktree-backed `[Dev Server Setup]` chat session loaded with the `dev-server-setup` skill, the draft embedded in the kickoff prompt. The agent walks the user through authoring `prEnv.devServer` (start command, port map, env, secret references, health path, monorepo cwd) and calls setup-apply to persist it.
      parameters:
        - schema:
            type: string
            description: Project slug.
          required: true
          description: Project slug.
          name: projectId
          in: path
      responses:
        "201":
          description: Wizard session spawned.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DevServerWizardStartResponse"
        "400":
          description: Project has no cwd configured, or no agents to host the wizard.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DevServerWizardErrorResponse"
        "404":
          description: Project not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DevServerWizardErrorResponse"
        "500":
          description: Wizard agent could not be resolved.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DevServerWizardErrorResponse"
  /api/projects/{projectId}/dev-server/setup-apply:
    post:
      tags:
        - Dev Server
      summary: Persist the authored dev-server config
      description: Admin+. Validates and persists the authored `prEnv.devServer` config (and optional secret values) to the project record. Unlike preview/rum setup this touches no repo file — dev-server config lives in `projects.json` — so there is no git commit step. Secret values are stored encrypted; the config references them by name via `devServer.secretKeys`.
      parameters:
        - schema:
            type: string
            description: Project slug.
          required: true
          description: Project slug.
          name: projectId
          in: path
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/DevServerSetupApplyRequest"
      responses:
        "200":
          description: Config persisted.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DevServerSetupApplyResponse"
        "400":
          description: devServer missing/invalid, or a `prEnv.devServer.<path>` validation error.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DevServerWizardErrorResponse"
        "404":
          description: Project not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DevServerWizardErrorResponse"
  /api/projects/{projectId}/dev-server/wizard-complete:
    post:
      tags:
        - Dev Server
      summary: Signal Dev Server wizard completion
      description: User+. Broadcasts `dev_server_wizard_complete` so the Settings panel refetches the project record after the wizard persists config.
      parameters:
        - schema:
            type: string
            description: Project slug.
          required: true
          description: Project slug.
          name: projectId
          in: path
      responses:
        "200":
          description: Completion broadcast (idempotent).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DevServerWizardOkResponse"
  /api/escalations:
    get:
      tags:
        - Escalations
      summary: List active escalations across every project
      responses:
        "200":
          description: Active escalations.
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Escalation"
  /api/projects/{projectId}/escalations:
    get:
      tags:
        - Escalations
      summary: List escalations for a project
      parameters:
        - schema:
            type: string
            description: Project slug or id.
          required: true
          description: Project slug or id.
          name: projectId
          in: path
        - schema:
            type: string
            enum:
              - "true"
              - "false"
            description: When `true`, only unacknowledged rows.
          required: false
          description: When `true`, only unacknowledged rows.
          name: active
          in: query
      responses:
        "200":
          description: Escalation rows.
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Escalation"
        "404":
          description: Project not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EscalationsErrorResponse"
    post:
      tags:
        - Escalations
      summary: Create an escalation
      parameters:
        - schema:
            type: string
            description: Project slug or id.
          required: true
          description: Project slug or id.
          name: projectId
          in: path
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                type:
                  type: string
                  enum:
                    - merge_conflict
                    - ci_failure
                    - review_needed
                    - blocker
                title:
                  type: string
                  minLength: 1
                description:
                  type: string
                prNumber:
                  type: integer
                prUrl:
                  type: string
                cardId:
                  type: string
                source:
                  type: string
              required:
                - type
                - title
      responses:
        "201":
          description: Created escalation.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Escalation"
        "400":
          description: Invalid type or missing title.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EscalationsErrorResponse"
        "404":
          description: Project not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EscalationsErrorResponse"
  /api/escalations/{id}/acknowledge:
    post:
      tags:
        - Escalations
      summary: Mark an escalation acknowledged
      parameters:
        - schema:
            type: string
            description: Escalation id.
          required: true
          description: Escalation id.
          name: id
          in: path
      responses:
        "200":
          description: Acknowledged escalation.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Escalation"
        "404":
          description: Escalation not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EscalationsErrorResponse"
  /api/escalations/{id}:
    delete:
      tags:
        - Escalations
      summary: Dismiss / delete an escalation
      parameters:
        - schema:
            type: string
            description: Escalation id.
          required: true
          description: Escalation id.
          name: id
          in: path
      responses:
        "200":
          description: Dismissed.
          content:
            application/json:
              schema:
                type: object
                properties:
                  ok:
                    type: boolean
                required:
                  - ok
        "404":
          description: Escalation not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EscalationsErrorResponse"
  /api/projects/{projectId}/finalize/ci-config:
    get:
      tags:
        - Finalize
      summary: Get the server-stored Finalize CI config(s)
      description: Admin+. Returns the project-scoped server config and (when the caller has one) their personal override. These are the fallback used when a repo does not commit `.agent-hub/ci.yaml` — a committed file always takes precedence.
      parameters:
        - schema:
            type: string
          required: true
          name: projectId
          in: path
      responses:
        "200":
          description: Stored configs.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/FinalizeServerCiConfigGetResponse"
        "403":
          description: Caller lacks the Admin role.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/FinalizeCiConfigErrorResponse"
        "404":
          description: Project not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/FinalizeCiConfigErrorResponse"
    put:
      tags:
        - Finalize
      summary: Upsert a server-stored Finalize CI config
      description: Admin+. Validates the ci.yaml against the schema, then stores it on the Agent Hub server (not committed to the repo). Runs in the Finalize runner exactly like a committed config would.
      parameters:
        - schema:
            type: string
          required: true
          name: projectId
          in: path
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/FinalizeServerCiConfigPutRequest"
      responses:
        "200":
          description: Config stored.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/FinalizeServerCiConfigPutResponse"
        "400":
          description: Missing/invalid ci_yaml_content, bad scope, or ci_config_invalid.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/FinalizeCiConfigErrorResponse"
        "403":
          description: Caller lacks the Admin role.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/FinalizeCiConfigErrorResponse"
        "404":
          description: Project not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/FinalizeCiConfigErrorResponse"
        "500":
          description: Store write failed.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/FinalizeCiConfigErrorResponse"
    delete:
      tags:
        - Finalize
      summary: Delete a server-stored Finalize CI config
      description: Admin+. Removes one scope's server-stored ci.yaml (defaults to the project scope).
      parameters:
        - schema:
            type: string
          required: true
          name: projectId
          in: path
        - schema:
            type: string
            enum:
              - project
              - personal
            description: Scope to delete. Defaults to 'project'.
          required: false
          description: Scope to delete. Defaults to 'project'.
          name: scope
          in: query
      responses:
        "200":
          description: Delete processed.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/FinalizeServerCiConfigDeleteResponse"
        "400":
          description: Bad scope.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/FinalizeCiConfigErrorResponse"
        "403":
          description: Caller lacks the Admin role.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/FinalizeCiConfigErrorResponse"
        "404":
          description: Project not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/FinalizeCiConfigErrorResponse"
  /api/projects/{projectId}/finalize/parity:
    get:
      tags:
        - Finalize
      summary: List Finalize↔GitHub parity records with a divergence breakdown
      description: Returns per-commit parity observations for the window, newest first, plus a class summary. The summary always reflects the full window even when `class` filters the record list.
      parameters:
        - schema:
            type: string
          required: true
          name: projectId
          in: path
        - schema:
            type: string
            description: "`<N><m|h|d>` (e.g. `7d`, `24h`) or `<isoFrom>..<isoTo>`. Defaults to 24h."
          required: false
          description: "`<N><m|h|d>` (e.g. `7d`, `24h`) or `<isoFrom>..<isoTo>`. Defaults to 24h."
          name: range
          in: query
        - schema:
            type: string
            enum:
              - agree_green
              - agree_red
              - false_green
              - false_red
              - indeterminate
            description: Filter the record list to a single divergence class.
          required: false
          description: Filter the record list to a single divergence class.
          name: class
          in: query
      responses:
        "200":
          description: Parity records + summary for the window.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ParityListResponse"
        "400":
          description: Range or class filter could not be parsed.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ParityError"
        "404":
          description: Project not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ParityError"
    post:
      tags:
        - Finalize
      summary: Record one Finalize↔GitHub parity observation
      description: Idempotent on (project, commit_sha). Derives the divergence class, persists the record, emits the `finalize_github_parity` counter, and fires a false-green alert when Finalize is green but GitHub is red.
      parameters:
        - schema:
            type: string
          required: true
          name: projectId
          in: path
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ParityRecordRequest"
      responses:
        "201":
          description: The stored parity record.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ParityRecordResponse"
        "400":
          description: Missing commit_sha or invalid verdict / jobs / pr_number.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ParityError"
        "404":
          description: Project not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ParityError"
  /api/projects/{projectId}/finalize/parity/seed:
    post:
      tags:
        - Finalize
      summary: Seed the parity dataset with known false-greens (PR#1001)
      description: Idempotently seeds the project dataset with documented false-green observations, starting with PR webapp#1001 (commit 6ad87ec).
      parameters:
        - schema:
            type: string
          required: true
          name: projectId
          in: path
      responses:
        "201":
          description: The seeded parity records.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ParitySeedResponse"
        "404":
          description: Project not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ParityError"
  /api/projects/{projectId}/finalize/quarantine:
    get:
      tags:
        - Finalize
      summary: List flaky-test quarantine entries (active + overdue)
      description: Returns the project quarantine lane split into active entries (still excusing their instance) and overdue entries (past expiry, awaiting human renewal or removal).
      parameters:
        - schema:
            type: string
          required: true
          name: projectId
          in: path
      responses:
        "200":
          description: Quarantine entries for the project.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/QuarantineListResponse"
        "404":
          description: Project not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/QuarantineError"
        "500":
          description: Quarantine list could not be read.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/QuarantineError"
    post:
      tags:
        - Finalize
      summary: Quarantine a flaky job instance
      description: Adds (or updates, idempotent per instance) a quarantine entry. `days` is clamped to ≤30. A quarantined instance still runs but no longer blocks the push gate until it expires.
      parameters:
        - schema:
            type: string
          required: true
          name: projectId
          in: path
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/QuarantineCreateRequest"
      responses:
        "201":
          description: Quarantine entry created/updated.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/QuarantineCreateResponse"
        "400":
          description: Missing job_id/owner or invalid days.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/QuarantineError"
        "404":
          description: Project not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/QuarantineError"
        "500":
          description: Quarantine entry could not be written.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/QuarantineError"
  /api/projects/{projectId}/finalize/quarantine/{id}:
    delete:
      tags:
        - Finalize
      summary: Release a quarantine entry
      parameters:
        - schema:
            type: string
          required: true
          name: projectId
          in: path
        - schema:
            type: string
          required: true
          name: id
          in: path
      responses:
        "204":
          description: Quarantine entry released.
        "404":
          description: Project or quarantine entry not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/QuarantineError"
        "500":
          description: Quarantine entry could not be deleted.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/QuarantineError"
  /api/projects/{projectId}/finalize/flakes:
    get:
      tags:
        - Finalize
      summary: Per-instance flake statistics from recorded run history
      description: Computes a flake rate per job/matrix instance over the requested window (default 30 days), newest-evidence first, annotated with whether the instance is currently quarantined.
      parameters:
        - schema:
            type: string
          required: true
          name: projectId
          in: path
        - schema:
            type: string
            description: Look-back window in days; positive integer (default 30, max 365).
          required: false
          description: Look-back window in days; positive integer (default 30, max 365).
          name: windowDays
          in: query
      responses:
        "200":
          description: Flake statistics for the window.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/FlakeListResponse"
        "400":
          description: windowDays must be a positive integer.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/QuarantineError"
        "404":
          description: Project not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/QuarantineError"
        "500":
          description: Flake history could not be read.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/QuarantineError"
  /api/projects/{projectId}/finalize/environment-draft:
    get:
      tags:
        - Finalize
      summary: Scan project for Finalize ci.yaml setup signals
      description: Admin+. Returns the same draft shape embedded in the setup wizard kickoff (stack, workflows, env vars, proposed ci.yaml). Used by Settings → Finalize to show required secrets without spawning a wizard session.
      parameters:
        - schema:
            type: string
          required: true
          name: projectId
          in: path
      responses:
        "200":
          description: Draft scan result.
          content:
            application/json:
              schema:
                type: object
                properties:
                  draft:
                    $ref: "#/components/schemas/FinalizeSetupDraft"
                  projectId:
                    type: string
                required:
                  - draft
                  - projectId
        "400":
          description: Project has no cwd configured.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/FinalizeWizardErrorResponse"
        "404":
          description: Project not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/FinalizeWizardErrorResponse"
  /api/projects/{projectId}/finalize/setup-wizard:
    post:
      tags:
        - Finalize
      summary: Spawn the Finalize ci.yaml setup wizard
      description: |-
        Admin+. Scans the project (README, package manifests, .github/workflows,
        Makefile, package.json scripts) and spawns a **worktree-backed** chat
        session (`use_worktree=1`) loaded with the `finalize-setup` skill. The
        session provisions its own git clone on a fresh `agent-hub/…` branch so
        the agent can author `.agent-hub/ci.yaml`, verify the pipeline locally,
        push, and open a PR. The pre-built `proposedCiYaml` is embedded in the
        kickoff prompt.
      parameters:
        - schema:
            type: string
          required: true
          name: projectId
          in: path
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties: {}
              description: Body is empty.
      responses:
        "201":
          description: Wizard session spawned.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/FinalizeWizardStartResponse"
        "400":
          description: Project has no cwd configured, or no agents to host the wizard.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/FinalizeWizardErrorResponse"
        "404":
          description: Project not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/FinalizeWizardErrorResponse"
        "500":
          description: Wizard agent could not be resolved.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/FinalizeWizardErrorResponse"
  /api/projects/{projectId}/finalize/setup-apply:
    post:
      tags:
        - Finalize
      summary: Commit a wizard-generated ci.yaml to the worktree
      description: |-
        Admin+. Validates `ci_yaml_content` against the v1/v2 schema, writes
        `<worktree>/.agent-hub/ci.yaml`, and commits the file to the worktree
        branch. Target resolution order: (1) request body's `session_id` when
        supplied (the setup wizard passes its own id), (2) the most-recent
        project session that has a worktree, (3) auto-provision a dedicated
        `[Finalize Config]` session + worktree when none exists.
      parameters:
        - schema:
            type: string
          required: true
          name: projectId
          in: path
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/FinalizeSetupApplyRequest"
      responses:
        "200":
          description: "Committed to the worktree, or stored on the server (`storage: 'server'`)."
          content:
            application/json:
              schema:
                anyOf:
                  - $ref: "#/components/schemas/FinalizeSetupApplyResponse"
                  - $ref: "#/components/schemas/FinalizeSetupApplyServerResponse"
        "400":
          description: "Payload invalid: missing content, invalid v1 schema, bad server_scope, no worktree-bearing session found, or worktree path is unreadable."
          content:
            application/json:
              schema:
                anyOf:
                  - $ref: "#/components/schemas/FinalizeWizardErrorResponse"
                  - $ref: "#/components/schemas/FinalizeSetupApplyInvalidConfig"
        "404":
          description: Project not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/FinalizeWizardErrorResponse"
        "500":
          description: Write, commit, or server store failed.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/FinalizeWizardErrorResponse"
  /api/projects/{projectId}/finalize/wizard-complete:
    post:
      tags:
        - Finalize
      summary: Notify the UI that the Finalize wizard has finished
      description: User+. Broadcasts `finalize_wizard_complete` for the Settings panel so it can refresh state.
      parameters:
        - schema:
            type: string
          required: true
          name: projectId
          in: path
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties: {}
              description: Body is empty.
      responses:
        "200":
          description: Acknowledged.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/FinalizeWizardCompleteResponse"
        "404":
          description: Project not found (silent — broadcast is a no-op).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/FinalizeWizardErrorResponse"
  /api/config/gemini-auth:
    get:
      tags:
        - Auth
      summary: Gemini CLI auth status (host-config + masked preview).
      responses:
        "200":
          description: Current Gemini auth status.
          content:
            application/json:
              schema:
                type: object
                properties:
                  apiKey:
                    type: object
                    properties:
                      configured:
                        type: boolean
                      source:
                        type: string
                        nullable: true
                        enum:
                          - environment
                          - config
                          - null
                      masked:
                        type: string
                        nullable: true
                    required:
                      - configured
                      - source
                      - masked
                  activeMethod:
                    type: string
                    enum:
                      - api-key
                      - none
                  oauth:
                    type: object
                    properties:
                      loggedIn:
                        type: boolean
                        nullable: true
                    required:
                      - loggedIn
                required:
                  - apiKey
                  - activeMethod
                  - oauth
    delete:
      tags:
        - Auth
      summary: Clear the host-wide Gemini API key.
      responses:
        "200":
          description: Cleared.
          content:
            application/json:
              schema:
                type: object
                properties:
                  ok:
                    type: boolean
                    enum:
                      - true
                  output:
                    type: string
                required:
                  - ok
                  - output
        "500":
          description: Server error.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /api/config/gemini-auth/api-key:
    post:
      tags:
        - Auth
      summary: Set or clear the host-wide Gemini API key.
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ApiKeyOnlyBody"
      responses:
        "200":
          description: API key persisted to config.json.
          content:
            application/json:
              schema:
                type: object
                properties:
                  ok:
                    type: boolean
                    enum:
                      - true
                  configured:
                    type: boolean
                  masked:
                    type: string
                    nullable: true
                required:
                  - ok
                  - configured
                  - masked
        "400":
          description: Invalid body shape.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ZodErrorResponse"
  /api/config/gemini-auth/validate-key:
    post:
      tags:
        - Auth
      summary: Validate a candidate Gemini API key against the CLI.
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ApiKeyRequiredBody"
      responses:
        "200":
          description: Validation result.
          content:
            application/json:
              schema:
                type: object
                properties:
                  valid:
                    type: boolean
                  output:
                    type: string
                required:
                  - valid
                  - output
        "400":
          description: Invalid body shape or missing apiKey.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ZodErrorResponse"
  /api/projects/{projectId}/git-host:
    get:
      tags:
        - Projects
      summary: Git hosting status for a project
      description: "Reports whether the project uses Agent Hub-hosted git (gitHost: agenthub), the clone URL, default branch, import progress, and the GitHub mirror policy."
      parameters:
        - schema:
            type: string
          required: true
          name: projectId
          in: path
      responses:
        "200":
          description: Hosting status.
          content:
            application/json:
              schema:
                type: object
                properties:
                  enabled:
                    type: boolean
                  cloneUrl:
                    type: string
                    nullable: true
                  defaultBranch:
                    type: string
                    nullable: true
                  branchCount:
                    type: number
                  importState:
                    type: object
                    nullable: true
                    properties:
                      status:
                        type: string
                        enum:
                          - importing
                          - ready
                          - error
                      startedAt:
                        type: number
                      finishedAt:
                        type: number
                      error:
                        type: string
                      importedFrom:
                        type: string
                        enum:
                          - github
                          - cwd
                          - empty
                    required:
                      - status
                      - startedAt
                  mirror:
                    type: object
                    nullable: true
                    properties:
                      enabled:
                        type: boolean
                      refs:
                        type: string
                        enum:
                          - default-branch
                          - all
                      githubRepo:
                        type: string
                        nullable: true
                      repoUrl:
                        type: string
                        nullable: true
                    required:
                      - enabled
                      - refs
                      - githubRepo
                      - repoUrl
                required:
                  - enabled
                  - cloneUrl
                  - defaultBranch
                  - branchCount
                  - importState
                  - mirror
        "404":
          description: Unknown project.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                required:
                  - error
  /api/projects/{projectId}/git-host/enable:
    post:
      tags:
        - Projects
      summary: Enable Agent Hub git hosting for a project
      description: Creates the hosted bare repo (importing from GitHub via repoUrl, from the project cwd, or empty), rewrites the project cwd origin to the hosted repo, and flips gitHost to agenthub. The import runs in the background — poll GET /git-host for progress. Requires Admin.
      parameters:
        - schema:
            type: string
          required: true
          name: projectId
          in: path
      requestBody:
        required: false
        content:
          application/json:
            schema:
              type: object
              properties:
                importFrom:
                  type: string
                  enum:
                    - auto
                    - github
                    - cwd
                    - empty
      responses:
        "202":
          description: Import started (or already running).
          content:
            application/json:
              schema:
                type: object
                properties:
                  enabled:
                    type: boolean
                  cloneUrl:
                    type: string
                    nullable: true
                  defaultBranch:
                    type: string
                    nullable: true
                  branchCount:
                    type: number
                  importState:
                    type: object
                    nullable: true
                    properties:
                      status:
                        type: string
                        enum:
                          - importing
                          - ready
                          - error
                      startedAt:
                        type: number
                      finishedAt:
                        type: number
                      error:
                        type: string
                      importedFrom:
                        type: string
                        enum:
                          - github
                          - cwd
                          - empty
                    required:
                      - status
                      - startedAt
                  mirror:
                    type: object
                    nullable: true
                    properties:
                      enabled:
                        type: boolean
                      refs:
                        type: string
                        enum:
                          - default-branch
                          - all
                      githubRepo:
                        type: string
                        nullable: true
                      repoUrl:
                        type: string
                        nullable: true
                    required:
                      - enabled
                      - refs
                      - githubRepo
                      - repoUrl
                required:
                  - enabled
                  - cloneUrl
                  - defaultBranch
                  - branchCount
                  - importState
                  - mirror
        "400":
          description: Invalid importFrom.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                required:
                  - error
        "404":
          description: Unknown project.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                required:
                  - error
        "409":
          description: Hosting already enabled.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                required:
                  - error
  /api/projects/{projectId}/git-host/disable:
    post:
      tags:
        - Projects
      summary: Disable Agent Hub git hosting for a project
      description: Flips gitHost back to github and restores the project cwd origin to repoUrl when set. The hosted bare repo is retained on disk. Requires Admin.
      parameters:
        - schema:
            type: string
          required: true
          name: projectId
          in: path
      responses:
        "200":
          description: Hosting disabled.
          content:
            application/json:
              schema:
                type: object
                properties:
                  enabled:
                    type: boolean
                  cloneUrl:
                    type: string
                    nullable: true
                  defaultBranch:
                    type: string
                    nullable: true
                  branchCount:
                    type: number
                  importState:
                    type: object
                    nullable: true
                    properties:
                      status:
                        type: string
                        enum:
                          - importing
                          - ready
                          - error
                      startedAt:
                        type: number
                      finishedAt:
                        type: number
                      error:
                        type: string
                      importedFrom:
                        type: string
                        enum:
                          - github
                          - cwd
                          - empty
                    required:
                      - status
                      - startedAt
                  mirror:
                    type: object
                    nullable: true
                    properties:
                      enabled:
                        type: boolean
                      refs:
                        type: string
                        enum:
                          - default-branch
                          - all
                      githubRepo:
                        type: string
                        nullable: true
                      repoUrl:
                        type: string
                        nullable: true
                    required:
                      - enabled
                      - refs
                      - githubRepo
                      - repoUrl
                required:
                  - enabled
                  - cloneUrl
                  - defaultBranch
                  - branchCount
                  - importState
                  - mirror
        "404":
          description: Unknown project.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                required:
                  - error
        "409":
          description: Hosting not enabled.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                required:
                  - error
  /api/projects/{projectId}/git-host/branches:
    get:
      tags:
        - Projects
      summary: List branches of an Agent Hub-hosted repo
      description: "Branches of the hosted bare repo, newest-commit first, with ahead/behind counts relative to the default branch. Repository-page surface; 404 unless the project uses gitHost: agenthub."
      parameters:
        - schema:
            type: string
          required: true
          name: projectId
          in: path
      responses:
        "200":
          description: Branch list.
          content:
            application/json:
              schema:
                type: object
                properties:
                  defaultBranch:
                    type: string
                    nullable: true
                  branches:
                    type: array
                    items:
                      type: object
                      properties:
                        name:
                          type: string
                        sha:
                          type: string
                        subject:
                          type: string
                        author:
                          type: string
                        date:
                          type: string
                        isDefault:
                          type: boolean
                        ahead:
                          type: number
                          nullable: true
                        behind:
                          type: number
                          nullable: true
                      required:
                        - name
                        - sha
                        - subject
                        - author
                        - date
                        - isDefault
                        - ahead
                        - behind
                required:
                  - defaultBranch
                  - branches
        "404":
          description: Unknown project or not Hub-hosted.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                required:
                  - error
  /api/projects/{projectId}/git-host/branches/{branch}:
    delete:
      tags:
        - Projects
      summary: Delete a branch from the hosted repo
      description: Admin only. Refuses the default branch and branches backing an open native PR (409).
      parameters:
        - schema:
            type: string
          required: true
          name: projectId
          in: path
        - schema:
            type: string
          required: true
          name: branch
          in: path
      responses:
        "200":
          description: Deleted.
          content:
            application/json:
              schema:
                type: object
                properties:
                  ok:
                    type: boolean
                required:
                  - ok
        "400":
          description: Invalid branch name.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                required:
                  - error
        "404":
          description: Unknown project, not Hub-hosted, or unknown branch.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                required:
                  - error
        "409":
          description: Default branch or backs an open PR.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                required:
                  - error
  /api/projects/{projectId}/git-host/default-branch:
    post:
      tags:
        - Projects
      summary: Set the hosted repo's default branch (HEAD symref)
      description: Admin only. Moves HEAD to the given existing branch and re-syncs branch protection (the pre-receive push block follows the default branch).
      parameters:
        - schema:
            type: string
          required: true
          name: projectId
          in: path
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                branch:
                  type: string
              required:
                - branch
      responses:
        "200":
          description: Updated git-host status.
        "400":
          description: Invalid branch name.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                required:
                  - error
        "404":
          description: Unknown project, not Hub-hosted, or unknown branch.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                required:
                  - error
  /api/projects/{projectId}/git-host/recent-pushes:
    get:
      tags:
        - Projects
      summary: Recently pushed branches without an open pull request
      description: Feeds the "Compare & pull request"-style banner on the Pulls page. In-memory, ~2h window; excludes the default branch, Agent Hub-managed session branches, zero-diff branches, and branches already covered by an open native PR.
      parameters:
        - schema:
            type: string
          required: true
          name: projectId
          in: path
      responses:
        "200":
          description: Recent pushes, newest first.
          content:
            application/json:
              schema:
                type: object
                properties:
                  pushes:
                    type: array
                    items:
                      type: object
                      properties:
                        branch:
                          type: string
                        pushedAt:
                          type: number
                      required:
                        - branch
                        - pushedAt
                required:
                  - pushes
        "404":
          description: Unknown project or not Hub-hosted.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                required:
                  - error
  /api/projects/{projectId}/git-host/mirror:
    get:
      tags:
        - Projects
      summary: GitHub mirror sync status for a Hub-hosted repo
      description: "Reports the last-known relationship between the Hub default branch and GitHub (synced / ahead / behind / diverged), ahead/behind counts, last sync + last error, and the last reconcile action. `diverged: true` means the branches forked and could not be auto-reconciled — surface it. 404 unless gitHost: agenthub."
      parameters:
        - schema:
            type: string
          required: true
          name: projectId
          in: path
      responses:
        "200":
          description: Mirror status.
          content:
            application/json:
              schema:
                type: object
                properties:
                  enabled:
                    type: boolean
                  refs:
                    type: string
                    enum:
                      - default-branch
                      - all
                  state:
                    type: object
                    properties:
                      status:
                        type: string
                        enum:
                          - synced
                          - ahead
                          - behind
                          - diverged
                          - unknown
                      diverged:
                        type: boolean
                      hubSha:
                        type: string
                      githubSha:
                        type: string
                      aheadBy:
                        type: number
                      behindBy:
                        type: number
                      lastSyncAt:
                        type: string
                      lastError:
                        type: string
                      lastErrorAt:
                        type: string
                      lastPollAt:
                        type: string
                      lastReconcileAt:
                        type: string
                      lastReconcileAction:
                        type: string
                required:
                  - enabled
                  - refs
                  - state
        "404":
          description: Unknown project or not Hub-hosted.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                required:
                  - error
  /api/projects/{projectId}/git-host/mirror/reconcile:
    post:
      tags:
        - Projects
      summary: Reconcile the Hub default branch with GitHub on demand
      description: "Admin only. Fetches GitHub's default branch and brings the two into sync: fast-forwards the Hub when GitHub is ahead (e.g. a release-bot version bump), pushes when the Hub is ahead, or attempts a clean auto-merge when they diverged. If they cannot be merged automatically, returns status \"diverged\" and records it. Never force-pushes or rewrites history."
      parameters:
        - schema:
            type: string
          required: true
          name: projectId
          in: path
      responses:
        "200":
          description: Reconcile outcome + refreshed state.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                    enum:
                      - synced
                      - ahead
                      - behind
                      - diverged
                      - unknown
                  action:
                    type: string
                    enum:
                      - none
                      - pulled
                      - pushed
                      - merged
                      - diverged
                      - skipped
                      - error
                  hubSha:
                    type: string
                  githubSha:
                    type: string
                  aheadBy:
                    type: number
                  behindBy:
                    type: number
                  error:
                    type: string
                  state:
                    type: object
                    properties:
                      status:
                        type: string
                        enum:
                          - synced
                          - ahead
                          - behind
                          - diverged
                          - unknown
                      diverged:
                        type: boolean
                      hubSha:
                        type: string
                      githubSha:
                        type: string
                      aheadBy:
                        type: number
                      behindBy:
                        type: number
                      lastSyncAt:
                        type: string
                      lastError:
                        type: string
                      lastErrorAt:
                        type: string
                      lastPollAt:
                        type: string
                      lastReconcileAt:
                        type: string
                      lastReconcileAction:
                        type: string
                required:
                  - status
                  - action
                  - state
        "404":
          description: Unknown project or not Hub-hosted.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                required:
                  - error
  /api/projects/{projectId}/git-host/mirror/owners:
    get:
      tags:
        - Projects
      summary: GitHub accounts the caller can create a mirror repo under
      description: "Admin only. Returns the caller's GitHub login plus every org their stored token can see, for the create-repo owner picker. `connected: false` (with an empty list) means no GitHub token is stored for the caller — connect one in Settings → GitHub."
      parameters:
        - schema:
            type: string
          required: true
          name: projectId
          in: path
      responses:
        "200":
          description: Owner candidates.
          content:
            application/json:
              schema:
                type: object
                properties:
                  connected:
                    type: boolean
                    description: False when the caller has no GitHub token stored — owners is then empty.
                  owners:
                    type: array
                    items:
                      type: object
                      properties:
                        login:
                          type: string
                        type:
                          type: string
                          enum:
                            - user
                            - organization
                      required:
                        - login
                        - type
                required:
                  - connected
                  - owners
        "404":
          description: Unknown project or not Hub-hosted.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                required:
                  - error
        "502":
          description: GitHub API unreachable.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                required:
                  - error
  /api/projects/{projectId}/git-host/mirror/link:
    post:
      tags:
        - Projects
      summary: Link (or create) the GitHub repo a Hub-hosted project mirrors to
      description: "Admin only. Points the hosted repo at a GitHub mirror target: `mode: \"existing\"` verifies the repo and the caller's push access, `mode: \"create\"` creates an empty repo under the caller's account or one of their orgs first. Sets githubRepo + repoUrl, enables mirroring, and kicks off an initial push in the background."
      parameters:
        - schema:
            type: string
          required: true
          name: projectId
          in: path
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                mode:
                  type: string
                  enum:
                    - existing
                    - create
                  description: "`existing` links a repo that is already on GitHub; `create` creates it first, then links it."
                repo:
                  type: string
                  description: "Repository reference: `owner/repo`, a bare name (paired with `owner`), or a GitHub clone URL."
                owner:
                  type: string
                  description: Account or org that owns (or will own) the repo. Defaults to the caller GitHub login.
                private:
                  type: boolean
                  description: create mode only; defaults to true.
                refs:
                  type: string
                  enum:
                    - default-branch
                    - all
              required:
                - mode
                - repo
      responses:
        "200":
          description: Mirror linked.
          content:
            application/json:
              schema:
                type: object
                properties:
                  created:
                    type: boolean
                  githubRepo:
                    type: string
                  repoUrl:
                    type: string
                  status:
                    type: object
                    properties:
                      enabled:
                        type: boolean
                      cloneUrl:
                        type: string
                        nullable: true
                      defaultBranch:
                        type: string
                        nullable: true
                      branchCount:
                        type: number
                      importState:
                        type: object
                        nullable: true
                        properties:
                          status:
                            type: string
                            enum:
                              - importing
                              - ready
                              - error
                          startedAt:
                            type: number
                          finishedAt:
                            type: number
                          error:
                            type: string
                          importedFrom:
                            type: string
                            enum:
                              - github
                              - cwd
                              - empty
                        required:
                          - status
                          - startedAt
                      mirror:
                        type: object
                        nullable: true
                        properties:
                          enabled:
                            type: boolean
                          refs:
                            type: string
                            enum:
                              - default-branch
                              - all
                          githubRepo:
                            type: string
                            nullable: true
                          repoUrl:
                            type: string
                            nullable: true
                        required:
                          - enabled
                          - refs
                          - githubRepo
                          - repoUrl
                    required:
                      - enabled
                      - cloneUrl
                      - defaultBranch
                      - branchCount
                      - importState
                      - mirror
                required:
                  - created
                  - githubRepo
                  - repoUrl
                  - status
        "400":
          description: Invalid repo reference, or no GitHub token stored for the caller.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                required:
                  - error
        "403":
          description: GitHub refused the token.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                required:
                  - error
        "404":
          description: Unknown project, not Hub-hosted, or repo not visible to the caller.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                required:
                  - error
        "409":
          description: Repo already exists (create mode).
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                required:
                  - error
        "502":
          description: GitHub API unreachable.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                required:
                  - error
    delete:
      tags:
        - Projects
      summary: Unlink the GitHub mirror target from a Hub-hosted project
      description: Admin only. Clears githubRepo + repoUrl and disables mirroring. The GitHub repo itself is left untouched.
      parameters:
        - schema:
            type: string
          required: true
          name: projectId
          in: path
      responses:
        "200":
          description: Mirror unlinked.
          content:
            application/json:
              schema:
                type: object
                properties:
                  enabled:
                    type: boolean
                  cloneUrl:
                    type: string
                    nullable: true
                  defaultBranch:
                    type: string
                    nullable: true
                  branchCount:
                    type: number
                  importState:
                    type: object
                    nullable: true
                    properties:
                      status:
                        type: string
                        enum:
                          - importing
                          - ready
                          - error
                      startedAt:
                        type: number
                      finishedAt:
                        type: number
                      error:
                        type: string
                      importedFrom:
                        type: string
                        enum:
                          - github
                          - cwd
                          - empty
                    required:
                      - status
                      - startedAt
                  mirror:
                    type: object
                    nullable: true
                    properties:
                      enabled:
                        type: boolean
                      refs:
                        type: string
                        enum:
                          - default-branch
                          - all
                      githubRepo:
                        type: string
                        nullable: true
                      repoUrl:
                        type: string
                        nullable: true
                    required:
                      - enabled
                      - refs
                      - githubRepo
                      - repoUrl
                required:
                  - enabled
                  - cloneUrl
                  - defaultBranch
                  - branchCount
                  - importState
                  - mirror
        "404":
          description: Unknown project or not Hub-hosted.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                required:
                  - error
  /api/projects/{projectId}/git-host/readme:
    get:
      tags:
        - Projects
      summary: Root README of an Agent Hub-hosted repo branch
      description: "Returns the root-level README (path + raw markdown content) of the given branch, defaulting to the default branch. `readme` is null when the repo has no root README. Repository-page surface; 404 unless gitHost: agenthub. Content is capped at 512 KiB."
      parameters:
        - schema:
            type: string
          required: true
          name: projectId
          in: path
        - schema:
            type: string
            description: Defaults to the default branch.
          required: false
          description: Defaults to the default branch.
          name: branch
          in: query
      responses:
        "200":
          description: "README content, or { readme: null } when absent."
          content:
            application/json:
              schema:
                type: object
                properties:
                  readme:
                    type: object
                    nullable: true
                    properties:
                      branch:
                        type: string
                      path:
                        type: string
                      content:
                        type: string
                      truncated:
                        type: boolean
                      mediaToken:
                        type: string
                    required:
                      - branch
                      - path
                      - content
                      - truncated
                      - mediaToken
                required:
                  - readme
        "400":
          description: Invalid branch name.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                required:
                  - error
        "404":
          description: Unknown project or not Hub-hosted.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                required:
                  - error
  /api/projects/{projectId}/git-host/commits:
    get:
      tags:
        - Projects
      summary: Commit log of an Agent Hub-hosted repo branch
      parameters:
        - schema:
            type: string
          required: true
          name: projectId
          in: path
        - schema:
            type: string
            description: Defaults to the default branch.
          required: false
          description: Defaults to the default branch.
          name: branch
          in: query
        - schema:
            type: integer
            minimum: 1
            maximum: 200
          required: false
          name: limit
          in: query
      responses:
        "200":
          description: Commit log, newest first.
          content:
            application/json:
              schema:
                type: object
                properties:
                  branch:
                    type: string
                  commits:
                    type: array
                    items:
                      type: object
                      properties:
                        sha:
                          type: string
                        subject:
                          type: string
                        author:
                          type: string
                        date:
                          type: string
                      required:
                        - sha
                        - subject
                        - author
                        - date
                required:
                  - branch
                  - commits
        "400":
          description: Invalid branch name.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                required:
                  - error
        "404":
          description: Unknown project, not Hub-hosted, or unknown branch.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                required:
                  - error
  /api/projects/{projectId}/git-host/commits/{sha}:
    get:
      tags:
        - Projects
      summary: Single-commit detail (stat + patch) from an Agent Hub-hosted repo
      parameters:
        - schema:
            type: string
          required: true
          name: projectId
          in: path
        - schema:
            type: string
          required: true
          name: sha
          in: path
      responses:
        "200":
          description: Commit metadata, diffstat, and unified patch (capped at 1 MiB).
          content:
            application/json:
              schema:
                type: object
                properties:
                  sha:
                    type: string
                  subject:
                    type: string
                  body:
                    type: string
                  author:
                    type: string
                  date:
                    type: string
                  parents:
                    type: array
                    items:
                      type: string
                  stat:
                    type: string
                  patch:
                    type: string
                  patchTruncated:
                    type: boolean
                required:
                  - sha
                  - subject
                  - body
                  - author
                  - date
                  - parents
                  - stat
                  - patch
                  - patchTruncated
        "404":
          description: Unknown project/commit.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                required:
                  - error
  /api/auth/github/start:
    get:
      tags:
        - Auth
      summary: Mint a signed state token + return the GitHub authorize URL.
      parameters:
        - schema:
            type: string
          required: false
          name: returnTo
          in: query
      responses:
        "200":
          description: Authorize URL the client should redirect the browser to.
          content:
            application/json:
              schema:
                type: object
                properties:
                  authorizeUrl:
                    type: string
                required:
                  - authorizeUrl
        "401":
          description: Not authenticated.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "503":
          description: GitHub OAuth not configured on the server.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /api/auth/github/callback:
    get:
      tags:
        - Auth
      summary: GitHub OAuth callback — exchanges code for tokens (HTML response).
      parameters:
        - schema:
            type: string
          required: false
          name: code
          in: query
        - schema:
            type: string
          required: false
          name: state
          in: query
        - schema:
            type: string
          required: false
          name: error
          in: query
      responses:
        "200":
          description: Successful sign-in (HTML status page).
          content:
            text/html:
              schema:
                type: string
        "400":
          description: Missing or invalid parameters.
          content:
            text/html:
              schema:
                type: string
        "502":
          description: GitHub token exchange failed.
          content:
            text/html:
              schema:
                type: string
        "503":
          description: Server not ready or OAuth not configured.
          content:
            text/html:
              schema:
                type: string
  /api/auth/github/status:
    get:
      tags:
        - Auth
      summary: GitHub connection status for the calling user.
      responses:
        "200":
          description: Connection summary.
          content:
            application/json:
              schema:
                type: object
                properties:
                  connected:
                    type: boolean
                  login:
                    type: string
                    nullable: true
                  connectedAt:
                    type: string
                    nullable: true
                  tokenExpiresAt:
                    type: string
                    nullable: true
                  serverConfigured:
                    type: boolean
                required:
                  - connected
                  - serverConfigured
        "401":
          description: Not authenticated.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /api/auth/github:
    delete:
      tags:
        - Auth
      summary: Disconnect the calling user from GitHub.
      responses:
        "200":
          description: Disconnected.
          content:
            application/json:
              schema:
                type: object
                properties:
                  ok:
                    type: boolean
                    enum:
                      - true
                required:
                  - ok
        "401":
          description: Not authenticated.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /api/auth/github/connect-token:
    post:
      tags:
        - Auth
      summary: Connect via a GitHub Personal Access Token (PAT).
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/GithubConnectTokenBody"
      responses:
        "200":
          description: Token validated and stored.
          content:
            application/json:
              schema:
                type: object
                properties:
                  ok:
                    type: boolean
                    enum:
                      - true
                  login:
                    type: string
                required:
                  - ok
                  - login
        "400":
          description: Missing or invalid token.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ZodErrorResponse"
        "401":
          description: Not authenticated.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "502":
          description: GitHub API call failed.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /api/google/calendar/events:
    get:
      tags:
        - Google
      summary: List Google Calendar events for the calling user over a time range
      parameters:
        - schema:
            type: string
            minLength: 1
            description: Calendar identifier. Defaults to Google Calendar `primary`.
          required: false
          description: Calendar identifier. Defaults to Google Calendar `primary`.
          name: calendarId
          in: query
        - schema:
            type: string
            minLength: 1
            description: Lower bound for event end time, as an RFC3339 timestamp with offset.
            example: 2026-06-30T00:00:00Z
          required: true
          description: Lower bound for event end time, as an RFC3339 timestamp with offset.
          name: timeMin
          in: query
        - schema:
            type: string
            minLength: 1
            description: Upper bound for event start time, as an RFC3339 timestamp with offset.
            example: 2026-07-01T00:00:00Z
          required: true
          description: Upper bound for event start time, as an RFC3339 timestamp with offset.
          name: timeMax
          in: query
        - schema:
            type: string
          required: false
          name: timeZone
          in: query
        - schema:
            type: integer
            minimum: 1
            maximum: 2500
          required: false
          name: maxResults
          in: query
        - schema:
            type: string
          required: false
          name: pageToken
          in: query
        - schema:
            type: string
          required: false
          name: q
          in: query
      responses:
        "200":
          description: Calendar events in agenda order.
          content:
            application/json:
              schema:
                type: object
                properties:
                  calendarId:
                    type: string
                  timeMin:
                    type: string
                  timeMax:
                    type: string
                  timeZone:
                    type: string
                    nullable: true
                  nextPageToken:
                    type: string
                    nullable: true
                  nextSyncToken:
                    type: string
                    nullable: true
                  events:
                    type: array
                    items:
                      $ref: "#/components/schemas/GoogleCalendarEvent"
                required:
                  - calendarId
                  - timeMin
                  - timeMax
                  - timeZone
                  - nextPageToken
                  - nextSyncToken
                  - events
        "400":
          description: Invalid query.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GoogleCalendarErrorResponse"
        "401":
          description: Not authenticated or Google not connected.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GoogleCalendarErrorResponse"
        "403":
          description: Required Calendar scope has not been granted.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GoogleCalendarErrorResponse"
        "429":
          description: Google Calendar rate limit exceeded.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GoogleCalendarErrorResponse"
        "502":
          description: Google Calendar request failed.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GoogleCalendarErrorResponse"
        "503":
          description: Google OAuth is not configured.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GoogleCalendarErrorResponse"
    post:
      tags:
        - Google
      summary: Create a Google Calendar event for the calling user
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                calendarId:
                  type: string
                  minLength: 1
                sendUpdates:
                  type: string
                  enum:
                    - all
                    - externalOnly
                    - none
                event:
                  $ref: "#/components/schemas/GoogleCalendarEventInput"
              required:
                - event
              additionalProperties: false
      responses:
        "201":
          description: Created event.
          content:
            application/json:
              schema:
                type: object
                properties:
                  calendarId:
                    type: string
                  event:
                    $ref: "#/components/schemas/GoogleCalendarEvent"
                required:
                  - calendarId
                  - event
        "400":
          description: Invalid body.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GoogleCalendarErrorResponse"
        "401":
          description: Not authenticated or Google not connected.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GoogleCalendarErrorResponse"
        "403":
          description: Required Calendar scope has not been granted.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GoogleCalendarErrorResponse"
        "429":
          description: Google Calendar rate limit exceeded.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GoogleCalendarErrorResponse"
        "502":
          description: Google Calendar request failed.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GoogleCalendarErrorResponse"
        "503":
          description: Google OAuth is not configured.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GoogleCalendarErrorResponse"
  /api/google/calendar/events/{eventId}:
    patch:
      tags:
        - Google
      summary: Patch a Google Calendar event for the calling user
      parameters:
        - schema:
            type: string
            minLength: 1
          required: true
          name: eventId
          in: path
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                calendarId:
                  type: string
                  minLength: 1
                sendUpdates:
                  type: string
                  enum:
                    - all
                    - externalOnly
                    - none
                event:
                  $ref: "#/components/schemas/GoogleCalendarEventPatch"
              required:
                - event
              additionalProperties: false
      responses:
        "200":
          description: Updated event.
          content:
            application/json:
              schema:
                type: object
                properties:
                  calendarId:
                    type: string
                  event:
                    $ref: "#/components/schemas/GoogleCalendarEvent"
                required:
                  - calendarId
                  - event
        "400":
          description: Invalid body or event id.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GoogleCalendarErrorResponse"
        "401":
          description: Not authenticated or Google not connected.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GoogleCalendarErrorResponse"
        "403":
          description: Required Calendar scope has not been granted.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GoogleCalendarErrorResponse"
        "404":
          description: Calendar or event not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GoogleCalendarErrorResponse"
        "429":
          description: Google Calendar rate limit exceeded.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GoogleCalendarErrorResponse"
        "502":
          description: Google Calendar request failed.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GoogleCalendarErrorResponse"
        "503":
          description: Google OAuth is not configured.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GoogleCalendarErrorResponse"
  /api/google/drive/files:
    get:
      tags:
        - Google
      summary: List app-accessible Drive files (drive.file) for the calling user
      parameters:
        - schema:
            type: string
            description: Drive query string, e.g. `mimeType = "application/pdf"`.
            example: mimeType = 'application/vnd.google-apps.spreadsheet'
          required: false
          description: Drive query string, e.g. `mimeType = "application/pdf"`.
          name: q
          in: query
        - schema:
            type: integer
            minimum: 1
            maximum: 100
          required: false
          name: pageSize
          in: query
        - schema:
            type: string
          required: false
          name: pageToken
          in: query
        - schema:
            type: string
            description: Comma-separated sort keys, e.g. `modifiedTime desc`.
            example: modifiedTime desc
          required: false
          description: Comma-separated sort keys, e.g. `modifiedTime desc`.
          name: orderBy
          in: query
        - schema:
            type: string
            minLength: 1
            description: Restrict the search to a single shared drive. Omit to search My Drive plus every shared drive the user belongs to.
          required: false
          description: Restrict the search to a single shared drive. Omit to search My Drive plus every shared drive the user belongs to.
          name: driveId
          in: query
      responses:
        "200":
          description: Files this app created or that the user opened with it, across My Drive and shared drives.
          content:
            application/json:
              schema:
                type: object
                properties:
                  files:
                    type: array
                    items:
                      $ref: "#/components/schemas/GoogleDriveFile"
                  nextPageToken:
                    type: string
                    nullable: true
                  incompleteSearch:
                    type: boolean
                    nullable: true
                required:
                  - files
                  - nextPageToken
                  - incompleteSearch
        "400":
          description: Invalid query.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GoogleDriveErrorResponse"
        "401":
          description: Not authenticated or Google not connected.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GoogleDriveErrorResponse"
        "403":
          description: Required Drive scope (drive.file) has not been granted.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GoogleDriveErrorResponse"
        "429":
          description: Google Drive rate limit exceeded.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GoogleDriveErrorResponse"
        "502":
          description: Google Drive request failed.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GoogleDriveErrorResponse"
        "503":
          description: Google OAuth is not configured.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GoogleDriveErrorResponse"
    post:
      tags:
        - Google
      summary: Create a Drive or Google Docs file for the calling user and return its link
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                name:
                  type: string
                  minLength: 1
                  maxLength: 255
                  example: release-notes.md
                mimeType:
                  type: string
                  minLength: 1
                  maxLength: 255
                  example: text/markdown
                targetMimeType:
                  type: string
                  enum:
                    - application/vnd.google-apps.document
                  description: Optional Google Workspace MIME type to convert into. Currently supports Google Docs.
                  example: application/vnd.google-apps.document
                description:
                  type: string
                  maxLength: 1024
                folderId:
                  type: string
                  minLength: 1
                  description: Optional Drive folder id to create the file in. The app must have access to it.
                content:
                  type: string
                  maxLength: 5242880
                  description: UTF-8 text content to save. Provide exactly one of content or base64Content. Uploads are capped at 5 MiB.
                base64Content:
                  type: string
                  maxLength: 6990508
                  description: Base64-encoded file bytes. Provide exactly one of content or base64Content. Encoded length is capped at 6990508 characters for the 5 MiB upload limit.
              required:
                - name
              additionalProperties: false
      responses:
        "200":
          description: The created Drive file metadata, including webViewLink when Google returns it.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GoogleDriveFile"
        "400":
          description: Invalid file payload.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GoogleDriveErrorResponse"
        "401":
          description: Not authenticated or Google not connected.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GoogleDriveErrorResponse"
        "403":
          description: Required Drive scope (drive.file) has not been granted.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GoogleDriveErrorResponse"
        "404":
          description: Folder not found or not accessible to this app.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GoogleDriveErrorResponse"
        "413":
          description: File payload is too large.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GoogleDriveErrorResponse"
        "429":
          description: Google Drive rate limit exceeded.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GoogleDriveErrorResponse"
        "502":
          description: Google Drive request failed.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GoogleDriveErrorResponse"
        "503":
          description: Google OAuth is not configured.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GoogleDriveErrorResponse"
  /api/google/drive/files/{fileId}:
    get:
      tags:
        - Google
      summary: Read metadata for an app-accessible Drive file for the calling user
      parameters:
        - schema:
            type: string
            minLength: 1
          required: true
          name: fileId
          in: path
      responses:
        "200":
          description: The file metadata.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GoogleDriveFile"
        "400":
          description: Invalid file id.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GoogleDriveErrorResponse"
        "401":
          description: Not authenticated or Google not connected.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GoogleDriveErrorResponse"
        "403":
          description: Required Drive scope (drive.file) has not been granted.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GoogleDriveErrorResponse"
        "404":
          description: File not found or not accessible to this app.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GoogleDriveErrorResponse"
        "429":
          description: Google Drive rate limit exceeded.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GoogleDriveErrorResponse"
        "502":
          description: Google Drive request failed.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GoogleDriveErrorResponse"
        "503":
          description: Google OAuth is not configured.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GoogleDriveErrorResponse"
  /api/google/gmail/threads:
    get:
      tags:
        - Google
      summary: List Gmail threads for the calling user
      parameters:
        - schema:
            type: string
            description: Gmail search query, e.g. `from:alice is:unread`.
            example: is:unread
          required: false
          description: Gmail search query, e.g. `from:alice is:unread`.
          name: q
          in: query
        - schema:
            anyOf:
              - type: string
              - type: array
                items:
                  type: string
            description: Restrict to threads carrying all of these label ids.
          required: false
          description: Restrict to threads carrying all of these label ids.
          name: labelIds
          in: query
        - schema:
            type: integer
            minimum: 1
            maximum: 500
          required: false
          name: maxResults
          in: query
        - schema:
            type: string
          required: false
          name: pageToken
          in: query
        - schema:
            type: string
            enum:
              - "true"
              - "false"
          required: false
          name: includeSpamTrash
          in: query
      responses:
        "200":
          description: Matching thread summaries in reverse-chronological order.
          content:
            application/json:
              schema:
                type: object
                properties:
                  threads:
                    type: array
                    items:
                      $ref: "#/components/schemas/GoogleGmailThreadSummary"
                  nextPageToken:
                    type: string
                    nullable: true
                  resultSizeEstimate:
                    type: number
                    nullable: true
                required:
                  - threads
                  - nextPageToken
                  - resultSizeEstimate
        "400":
          description: Invalid query.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GoogleGmailErrorResponse"
        "401":
          description: Not authenticated or Google not connected.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GoogleGmailErrorResponse"
        "403":
          description: Required Gmail scope has not been granted.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GoogleGmailErrorResponse"
        "429":
          description: Gmail rate limit exceeded.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GoogleGmailErrorResponse"
        "502":
          description: Gmail request failed.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GoogleGmailErrorResponse"
        "503":
          description: Google OAuth is not configured.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GoogleGmailErrorResponse"
  /api/google/gmail/threads/{threadId}:
    get:
      tags:
        - Google
      summary: Read a Gmail thread (with shaped messages) for the calling user
      parameters:
        - schema:
            type: string
            minLength: 1
          required: true
          name: threadId
          in: path
        - schema:
            type: string
            enum:
              - full
              - metadata
              - minimal
          required: false
          name: format
          in: query
      responses:
        "200":
          description: The thread and its shaped messages.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GoogleGmailThread"
        "400":
          description: Invalid thread id or query.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GoogleGmailErrorResponse"
        "401":
          description: Not authenticated or Google not connected.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GoogleGmailErrorResponse"
        "403":
          description: Required Gmail scope has not been granted.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GoogleGmailErrorResponse"
        "404":
          description: Thread not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GoogleGmailErrorResponse"
        "429":
          description: Gmail rate limit exceeded.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GoogleGmailErrorResponse"
        "502":
          description: Gmail request failed.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GoogleGmailErrorResponse"
        "503":
          description: Google OAuth is not configured.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GoogleGmailErrorResponse"
  /api/google/gmail/messages:
    post:
      tags:
        - Google
      summary: Send a Gmail message as the calling user
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                to:
                  type: array
                  items:
                    type: string
                    format: email
                  minItems: 1
                  maxItems: 50
                cc:
                  type: array
                  items:
                    type: string
                    format: email
                  maxItems: 50
                bcc:
                  type: array
                  items:
                    type: string
                    format: email
                  maxItems: 50
                subject:
                  type: string
                  maxLength: 998
                text:
                  type: string
                html:
                  type: string
                threadId:
                  type: string
                  minLength: 1
                inReplyTo:
                  type: string
                  maxLength: 998
                references:
                  type: string
                  maxLength: 998
              required:
                - to
              additionalProperties: false
      responses:
        "201":
          description: The sent message.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GoogleGmailMessage"
        "400":
          description: Invalid body.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GoogleGmailErrorResponse"
        "401":
          description: Not authenticated or Google not connected.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GoogleGmailErrorResponse"
        "403":
          description: Required Gmail send scope has not been granted.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GoogleGmailErrorResponse"
        "429":
          description: Gmail rate limit exceeded.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GoogleGmailErrorResponse"
        "502":
          description: Gmail request failed.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GoogleGmailErrorResponse"
        "503":
          description: Google OAuth is not configured.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GoogleGmailErrorResponse"
  /api/google/gmail/messages/{messageId}/modify:
    post:
      tags:
        - Google
      summary: Add or remove labels on a Gmail message for the calling user
      parameters:
        - schema:
            type: string
            minLength: 1
          required: true
          name: messageId
          in: path
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                addLabelIds:
                  type: array
                  items:
                    type: string
                    minLength: 1
                removeLabelIds:
                  type: array
                  items:
                    type: string
                    minLength: 1
              additionalProperties: false
      responses:
        "200":
          description: The updated message with refreshed label ids.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GoogleGmailMessage"
        "400":
          description: Invalid body or message id.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GoogleGmailErrorResponse"
        "401":
          description: Not authenticated or Google not connected.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GoogleGmailErrorResponse"
        "403":
          description: Required Gmail modify scope has not been granted.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GoogleGmailErrorResponse"
        "404":
          description: Message not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GoogleGmailErrorResponse"
        "429":
          description: Gmail rate limit exceeded.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GoogleGmailErrorResponse"
        "502":
          description: Gmail request failed.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GoogleGmailErrorResponse"
        "503":
          description: Google OAuth is not configured.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GoogleGmailErrorResponse"
  /api/auth/google/callback:
    get:
      tags:
        - Auth
      summary: Google OAuth callback — exchanges code for tokens (HTML response).
      parameters:
        - schema:
            type: string
          required: false
          name: code
          in: query
        - schema:
            type: string
          required: false
          name: state
          in: query
        - schema:
            type: string
          required: false
          name: error
          in: query
      responses:
        "200":
          description: Successful connect (HTML status page).
          content:
            text/html:
              schema:
                type: string
        "400":
          description: Missing or invalid parameters / state.
          content:
            text/html:
              schema:
                type: string
        "502":
          description: Google token exchange failed.
          content:
            text/html:
              schema:
                type: string
        "503":
          description: Server not ready or OAuth not configured.
          content:
            text/html:
              schema:
                type: string
  /api/auth/google/status:
    get:
      tags:
        - Auth
      summary: Google connection status for the calling user (never returns tokens).
      responses:
        "200":
          description: Connection summary.
          content:
            application/json:
              schema:
                type: object
                properties:
                  connected:
                    type: boolean
                  email:
                    type: string
                    nullable: true
                  grantedScopes:
                    type: array
                    items:
                      type: string
                  connectedAt:
                    type: string
                    nullable: true
                  tokenExpiresAt:
                    type: string
                    nullable: true
                  serverConfigured:
                    type: boolean
                required:
                  - connected
                  - email
                  - grantedScopes
                  - connectedAt
                  - tokenExpiresAt
                  - serverConfigured
        "401":
          description: Not authenticated.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                required:
                  - error
  /api/auth/google/connect:
    delete:
      tags:
        - Auth
      summary: Disconnect the calling user from Google (revoke + clear).
      responses:
        "200":
          description: Disconnected.
          content:
            application/json:
              schema:
                type: object
                properties:
                  ok:
                    type: boolean
                    enum:
                      - true
                required:
                  - ok
        "401":
          description: Not authenticated.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                required:
                  - error
  /api/auth/google/start:
    get:
      tags:
        - Auth
      summary: Mint a signed state token + return the Google authorize URL.
      parameters:
        - schema:
            type: string
          required: false
          name: returnTo
          in: query
        - schema:
            type: string
            description: Optional space- or comma-separated extra OAuth scopes for incremental per-surface consent. Identity scopes (openid email profile) are always included.
          required: false
          description: Optional space- or comma-separated extra OAuth scopes for incremental per-surface consent. Identity scopes (openid email profile) are always included.
          name: scopes
          in: query
      responses:
        "200":
          description: Authorize URL the client should redirect the browser to.
          content:
            application/json:
              schema:
                type: object
                properties:
                  authorizeUrl:
                    type: string
                required:
                  - authorizeUrl
        "401":
          description: Not authenticated.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                required:
                  - error
        "503":
          description: Google OAuth not configured on the server.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  code:
                    type: string
                required:
                  - error
                  - code
  /api/google/sheets/{spreadsheetId}:
    get:
      tags:
        - Google
      summary: Read spreadsheet metadata (title + tabs) for the calling user
      parameters:
        - schema:
            type: string
            minLength: 1
          required: true
          name: spreadsheetId
          in: path
      responses:
        "200":
          description: Spreadsheet metadata with its sheet tabs.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GoogleSheetsSpreadsheetMetadata"
        "400":
          description: Invalid spreadsheet id.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GoogleSheetsErrorResponse"
        "401":
          description: Not authenticated or Google not connected.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GoogleSheetsErrorResponse"
        "403":
          description: Required Sheets scope has not been granted.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GoogleSheetsErrorResponse"
        "404":
          description: Spreadsheet not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GoogleSheetsErrorResponse"
        "429":
          description: Google Sheets rate limit exceeded.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GoogleSheetsErrorResponse"
        "502":
          description: Google Sheets request failed.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GoogleSheetsErrorResponse"
        "503":
          description: Google OAuth is not configured.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GoogleSheetsErrorResponse"
  /api/google/sheets/{spreadsheetId}/values:
    get:
      tags:
        - Google
      summary: Read a range of values from a spreadsheet for the calling user
      parameters:
        - schema:
            type: string
            minLength: 1
          required: true
          name: spreadsheetId
          in: path
        - schema:
            type: string
            minLength: 1
            description: A1 notation range to read, e.g. `Sheet1!A1:C10`.
            example: Sheet1!A1:C10
          required: true
          description: A1 notation range to read, e.g. `Sheet1!A1:C10`.
          name: range
          in: query
        - schema:
            type: string
            enum:
              - ROWS
              - COLUMNS
          required: false
          name: majorDimension
          in: query
        - schema:
            type: string
            enum:
              - FORMATTED_VALUE
              - UNFORMATTED_VALUE
              - FORMULA
          required: false
          name: valueRenderOption
          in: query
        - schema:
            type: string
            enum:
              - SERIAL_NUMBER
              - FORMATTED_STRING
          required: false
          name: dateTimeRenderOption
          in: query
      responses:
        "200":
          description: The values in the requested range.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GoogleSheetsValueRange"
        "400":
          description: Invalid range or spreadsheet id.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GoogleSheetsErrorResponse"
        "401":
          description: Not authenticated or Google not connected.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GoogleSheetsErrorResponse"
        "403":
          description: Required Sheets scope has not been granted.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GoogleSheetsErrorResponse"
        "404":
          description: Spreadsheet or range not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GoogleSheetsErrorResponse"
        "429":
          description: Google Sheets rate limit exceeded.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GoogleSheetsErrorResponse"
        "502":
          description: Google Sheets request failed.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GoogleSheetsErrorResponse"
        "503":
          description: Google OAuth is not configured.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GoogleSheetsErrorResponse"
    put:
      tags:
        - Google
      summary: Update a range of values in a spreadsheet for the calling user
      parameters:
        - schema:
            type: string
            minLength: 1
          required: true
          name: spreadsheetId
          in: path
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                range:
                  type: string
                  minLength: 1
                  example: Sheet1!A1:B2
                values:
                  type: array
                  items:
                    type: array
                    items:
                      anyOf:
                        - type: string
                        - type: number
                        - type: boolean
                        - nullable: true
                  minItems: 1
                  description: Row-major matrix of cell values to write.
                  example: &a3
                    - - Name
                      - Score
                    - - Alice
                      - 42
                valueInputOption:
                  type: string
                  enum:
                    - RAW
                    - USER_ENTERED
                majorDimension:
                  type: string
                  enum:
                    - ROWS
                    - COLUMNS
              required:
                - range
                - values
              additionalProperties: false
      responses:
        "200":
          description: Update summary including the affected range.
          content:
            application/json:
              schema:
                type: object
                properties:
                  spreadsheetId:
                    type: string
                    nullable: true
                  updatedRange:
                    type: string
                    nullable: true
                  updatedRows:
                    type: number
                    nullable: true
                  updatedColumns:
                    type: number
                    nullable: true
                  updatedCells:
                    type: number
                    nullable: true
                required:
                  - spreadsheetId
                  - updatedRange
                  - updatedRows
                  - updatedColumns
                  - updatedCells
        "400":
          description: Invalid body or spreadsheet id.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GoogleSheetsErrorResponse"
        "401":
          description: Not authenticated or Google not connected.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GoogleSheetsErrorResponse"
        "403":
          description: Required Sheets write scope has not been granted.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GoogleSheetsErrorResponse"
        "404":
          description: Spreadsheet or range not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GoogleSheetsErrorResponse"
        "429":
          description: Google Sheets rate limit exceeded.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GoogleSheetsErrorResponse"
        "502":
          description: Google Sheets request failed.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GoogleSheetsErrorResponse"
        "503":
          description: Google OAuth is not configured.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GoogleSheetsErrorResponse"
  /api/google/sheets/{spreadsheetId}/values/append:
    post:
      tags:
        - Google
      summary: Append rows of values to a spreadsheet for the calling user
      parameters:
        - schema:
            type: string
            minLength: 1
          required: true
          name: spreadsheetId
          in: path
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                range:
                  type: string
                  minLength: 1
                  example: Sheet1!A1
                values:
                  type: array
                  items:
                    type: array
                    items:
                      anyOf:
                        - type: string
                        - type: number
                        - type: boolean
                        - nullable: true
                  minItems: 1
                  description: Row-major matrix of cell values to write.
                  example: *a3
                valueInputOption:
                  type: string
                  enum:
                    - RAW
                    - USER_ENTERED
                insertDataOption:
                  type: string
                  enum:
                    - OVERWRITE
                    - INSERT_ROWS
                majorDimension:
                  type: string
                  enum:
                    - ROWS
                    - COLUMNS
              required:
                - range
                - values
              additionalProperties: false
      responses:
        "200":
          description: Append summary including the affected range.
          content:
            application/json:
              schema:
                type: object
                properties:
                  spreadsheetId:
                    type: string
                    nullable: true
                  tableRange:
                    type: string
                    nullable: true
                  updates:
                    type: object
                    nullable: true
                    properties:
                      spreadsheetId:
                        type: string
                        nullable: true
                      updatedRange:
                        type: string
                        nullable: true
                      updatedRows:
                        type: number
                        nullable: true
                      updatedColumns:
                        type: number
                        nullable: true
                      updatedCells:
                        type: number
                        nullable: true
                    required:
                      - spreadsheetId
                      - updatedRange
                      - updatedRows
                      - updatedColumns
                      - updatedCells
                required:
                  - spreadsheetId
                  - tableRange
                  - updates
        "400":
          description: Invalid body or spreadsheet id.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GoogleSheetsErrorResponse"
        "401":
          description: Not authenticated or Google not connected.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GoogleSheetsErrorResponse"
        "403":
          description: Required Sheets write scope has not been granted.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GoogleSheetsErrorResponse"
        "404":
          description: Spreadsheet not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GoogleSheetsErrorResponse"
        "429":
          description: Google Sheets rate limit exceeded.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GoogleSheetsErrorResponse"
        "502":
          description: Google Sheets request failed.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GoogleSheetsErrorResponse"
        "503":
          description: Google OAuth is not configured.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GoogleSheetsErrorResponse"
  /api/hooks/stop:
    post:
      tags:
        - Hooks
      summary: CLI session-stop hook
      description: Invoked by the Claude Code CLI when a session ends. The actual auto-commit / changes_ready logic runs from `chat.ts` proc.on("close"); this endpoint exists for the CLI contract and is informational only.
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                sessionId:
                  type: string
                  minLength: 1
              required:
                - sessionId
      responses:
        "200":
          description: Hook acknowledged.
          content:
            application/json:
              schema:
                type: object
                properties:
                  ok:
                    type: boolean
                  sessionId:
                    type: string
                required:
                  - ok
                  - sessionId
                description: Acknowledgment that the stop hook was received.
        "400":
          description: Missing sessionId.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HooksErrorResponse"
        "404":
          description: Session not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HooksErrorResponse"
  /api/projects/{projectId}/infra/alert-routing:
    get:
      tags:
        - Infrastructure
      summary: Resolve infrastructure alert delivery routing
      parameters:
        - schema:
            type: string
          required: true
          name: projectId
          in: path
      responses:
        "200":
          description: Resolved alert routing.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/InfraAlertRoutingResponse"
        "404":
          description: Project not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/InfraAlertRoutingError"
    put:
      tags:
        - Infrastructure
      summary: Set an infrastructure alert delivery override
      parameters:
        - schema:
            type: string
          required: true
          name: projectId
          in: path
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/InfraAlertRoutingEntry"
      responses:
        "200":
          description: Updated alert routing.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/InfraAlertRoutingResponse"
        "400":
          description: Invalid routing entry.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/InfraAlertRoutingError"
        "404":
          description: Project not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/InfraAlertRoutingError"
  /api/projects/{projectId}/infra/alert-routing/{severity}/{channel}:
    delete:
      tags:
        - Infrastructure
      summary: Remove an infrastructure alert delivery override
      parameters:
        - schema:
            type: string
          required: true
          name: projectId
          in: path
        - schema:
            type: string
            enum:
              - critical
              - warning
              - info
          required: true
          name: severity
          in: path
        - schema:
            type: string
            enum:
              - in_app
              - push
              - email
          required: true
          name: channel
          in: path
      responses:
        "200":
          description: Updated alert routing.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/InfraAlertRoutingResponse"
        "404":
          description: Project or routing override not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/InfraAlertRoutingError"
  /api/projects/{projectId}/infra/alert-rules:
    get:
      tags:
        - Projects
      summary: List infra alert rules
      description: |-
        Every threshold rule on the project, newest first. Unpaginated: rules are hand-authored plus a per-service default pack, so the population is tens per project.

        **Cost:** local SQLite only. No AWS call.
      parameters:
        - schema:
            type: string
            description: Project slug (e.g. `agent-hub`).
          required: true
          description: Project slug (e.g. `agent-hub`).
          name: projectId
          in: path
        - schema:
            type: string
            maxLength: 64
          required: false
          name: service
          in: query
        - schema:
            type: string
            enum:
              - "true"
              - "false"
          required: false
          name: enabled
          in: query
      responses:
        "200":
          description: Rule list. Empty array on a Hub whose infra store never opened.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/InfraAlertRuleList"
        "400":
          description: Malformed body or query.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                    description: Human-readable failure reason.
                required:
                  - error
        "404":
          description: Project not found, or the caller cannot see it.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                    description: Human-readable failure reason.
                required:
                  - error
    post:
      tags:
        - Projects
      summary: Create an infra alert rule
      description: |-
        Persists a rule evaluated by Agent Hub’s own poller (decision INFRA-ALERT). Nothing is written to the monitored AWS account: no `PutMetricAlarm`, no SNS topic, no IAM role, and nothing to clean up if the rule is later deleted.

        A rule that could never reach ALARM (`datapointsToAlarm` above `evaluationPeriods`) is rejected rather than repaired, so an operator is never handed an alarm that looks armed and is not.
      parameters:
        - schema:
            type: string
            description: Project slug (e.g. `agent-hub`).
          required: true
          description: Project slug (e.g. `agent-hub`).
          name: projectId
          in: path
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                name:
                  type: string
                  minLength: 1
                  maxLength: 200
                  description: Operator-facing rule name, shown on the alert.
                  example: ALB unhealthy hosts
                description:
                  type: string
                  nullable: true
                  maxLength: 2000
                service:
                  type: string
                  minLength: 1
                  maxLength: 64
                  description: "Service token the rule applies to (`ec2`, `rds`, `elbv2`, …), matching `infra_resources.service`. Required: it decides which namespace the rule can apply to."
                  example: elbv2
                accountId:
                  type: string
                  nullable: true
                  maxLength: 64
                  description: Narrow to one account. Null/absent matches every account in scope.
                region:
                  type: string
                  nullable: true
                  maxLength: 32
                  description: Narrow to one region. Null/absent matches every region in scope.
                resourceKey:
                  type: string
                  nullable: true
                  maxLength: 512
                  description: Pin to exactly one resource (`infra_resources.resource_key`). Null/absent matches every resource satisfying the rest of the selector, including ones discovered after the rule was written.
                tagFilter:
                  type: object
                  nullable: true
                  additionalProperties:
                    type: array
                    items:
                      type: string
                      maxLength: 256
                    minItems: 1
                    maxItems: 50
                  description: Tag predicate, e.g. `{"Environment":["prod"]}`. An empty object is stored as no filter.
                namespace:
                  type: string
                  minLength: 1
                  maxLength: 255
                  example: AWS/ApplicationELB
                metricName:
                  type: string
                  minLength: 1
                  maxLength: 255
                  example: UnHealthyHostCount
                stat:
                  type: string
                  minLength: 1
                  maxLength: 64
                  description: CloudWatch statistic (`Average`, `Maximum`, `Sum`, `p99`, …).
                  example: Minimum
                periodS:
                  type: integer
                  minimum: 1
                  maximum: 86400
                  description: Period in seconds. CloudWatch only serves 60s within 15 days, 300s within 63 days and 3600s beyond, so a period finer than the data available returns nothing.
                  example: 60
                threshold:
                  type: number
                comparisonOperator:
                  type: string
                  enum:
                    - GreaterThanOrEqualToThreshold
                    - GreaterThanThreshold
                    - LessThanThreshold
                    - LessThanOrEqualToThreshold
                  description: "Static-threshold operators only. The anomaly-detection operators `PutMetricAlarm` also accepts are rejected: we fit no model, so there is no threshold band for them to compare against."
                evaluationPeriods:
                  type: integer
                  minimum: 1
                  maximum: 1440
                  description: N — periods the metric is compared over.
                datapointsToAlarm:
                  type: integer
                  nullable: true
                  minimum: 1
                  maximum: 1440
                  description: M of N. Null/absent means N (consecutive alarm), which is AWS’s own default. Must not exceed `evaluationPeriods` — such a rule could never reach ALARM.
                treatMissingData:
                  type: string
                  nullable: true
                  enum:
                    - missing
                    - notBreaching
                    - breaching
                    - ignore
                    - null
                  description: Defaults to `missing`, matching CloudWatch.
                severity:
                  type: string
                  nullable: true
                  enum:
                    - critical
                    - warning
                    - info
                    - null
                  description: Routing severity. Defaults to `warning`. Not a CloudWatch concept — ours.
                enabled:
                  type: boolean
                  description: Defaults to true. Disabling stops evaluation while retaining the alert history a delete would cascade away.
              required:
                - name
                - service
                - namespace
                - metricName
                - stat
                - periodS
                - threshold
                - comparisonOperator
                - evaluationPeriods
      responses:
        "201":
          description: Rule created.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/InfraAlertRule"
        "400":
          description: Malformed body or query.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                    description: Human-readable failure reason.
                required:
                  - error
        "404":
          description: Project not found, or the caller cannot see it.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                    description: Human-readable failure reason.
                required:
                  - error
        "503":
          description: The infrastructure store is not open on this Hub.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                    description: Human-readable failure reason.
                required:
                  - error
  /api/projects/{projectId}/infra/alert-rules/{ruleId}:
    put:
      tags:
        - Projects
      summary: Update an infra alert rule
      description: |-
        Partial update — absent keys are left alone. The `datapointsToAlarm <= evaluationPeriods` invariant is checked against the merged rule, not the patch, so lowering `evaluationPeriods` alone cannot strand an earlier `datapointsToAlarm`.

        Existing alerts keep their state; the next evaluation applies the new threshold.
      parameters:
        - schema:
            type: string
            description: Project slug (e.g. `agent-hub`).
          required: true
          description: Project slug (e.g. `agent-hub`).
          name: projectId
          in: path
        - schema:
            type: string
            description: Alert rule id.
          required: true
          description: Alert rule id.
          name: ruleId
          in: path
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                name:
                  type: string
                  minLength: 1
                  maxLength: 200
                  description: Operator-facing rule name, shown on the alert.
                  example: ALB unhealthy hosts
                description:
                  type: string
                  nullable: true
                  maxLength: 2000
                service:
                  type: string
                  minLength: 1
                  maxLength: 64
                  description: "Service token the rule applies to (`ec2`, `rds`, `elbv2`, …), matching `infra_resources.service`. Required: it decides which namespace the rule can apply to."
                  example: elbv2
                accountId:
                  type: string
                  nullable: true
                  maxLength: 64
                  description: Narrow to one account. Null/absent matches every account in scope.
                region:
                  type: string
                  nullable: true
                  maxLength: 32
                  description: Narrow to one region. Null/absent matches every region in scope.
                resourceKey:
                  type: string
                  nullable: true
                  maxLength: 512
                  description: Pin to exactly one resource (`infra_resources.resource_key`). Null/absent matches every resource satisfying the rest of the selector, including ones discovered after the rule was written.
                tagFilter:
                  type: object
                  nullable: true
                  additionalProperties:
                    type: array
                    items:
                      type: string
                      maxLength: 256
                    minItems: 1
                    maxItems: 50
                  description: Tag predicate, e.g. `{"Environment":["prod"]}`. An empty object is stored as no filter.
                namespace:
                  type: string
                  minLength: 1
                  maxLength: 255
                  example: AWS/ApplicationELB
                metricName:
                  type: string
                  minLength: 1
                  maxLength: 255
                  example: UnHealthyHostCount
                stat:
                  type: string
                  minLength: 1
                  maxLength: 64
                  description: CloudWatch statistic (`Average`, `Maximum`, `Sum`, `p99`, …).
                  example: Minimum
                periodS:
                  type: integer
                  minimum: 1
                  maximum: 86400
                  description: Period in seconds. CloudWatch only serves 60s within 15 days, 300s within 63 days and 3600s beyond, so a period finer than the data available returns nothing.
                  example: 60
                threshold:
                  type: number
                comparisonOperator:
                  type: string
                  enum:
                    - GreaterThanOrEqualToThreshold
                    - GreaterThanThreshold
                    - LessThanThreshold
                    - LessThanOrEqualToThreshold
                  description: "Static-threshold operators only. The anomaly-detection operators `PutMetricAlarm` also accepts are rejected: we fit no model, so there is no threshold band for them to compare against."
                evaluationPeriods:
                  type: integer
                  minimum: 1
                  maximum: 1440
                  description: N — periods the metric is compared over.
                datapointsToAlarm:
                  type: integer
                  nullable: true
                  minimum: 1
                  maximum: 1440
                  description: M of N. Null/absent means N (consecutive alarm), which is AWS’s own default. Must not exceed `evaluationPeriods` — such a rule could never reach ALARM.
                treatMissingData:
                  type: string
                  nullable: true
                  enum:
                    - missing
                    - notBreaching
                    - breaching
                    - ignore
                    - null
                  description: Defaults to `missing`, matching CloudWatch.
                severity:
                  type: string
                  nullable: true
                  enum:
                    - critical
                    - warning
                    - info
                    - null
                  description: Routing severity. Defaults to `warning`. Not a CloudWatch concept — ours.
                enabled:
                  type: boolean
                  description: Defaults to true. Disabling stops evaluation while retaining the alert history a delete would cascade away.
      responses:
        "200":
          description: Updated rule.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/InfraAlertRule"
        "400":
          description: Malformed body or query.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                    description: Human-readable failure reason.
                required:
                  - error
        "404":
          description: Project or rule not found.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                    description: Human-readable failure reason.
                required:
                  - error
        "503":
          description: The infrastructure store is not open on this Hub.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                    description: Human-readable failure reason.
                required:
                  - error
    delete:
      tags:
        - Projects
      summary: Delete an infra alert rule
      description: |-
        Cascades to every alert the rule fired and their transition history — an alert without its rule has no threshold to be read against.

        To stop paging while keeping the incident record, `PUT` the rule with `enabled: false` instead.
      parameters:
        - schema:
            type: string
            description: Project slug (e.g. `agent-hub`).
          required: true
          description: Project slug (e.g. `agent-hub`).
          name: projectId
          in: path
        - schema:
            type: string
            description: Alert rule id.
          required: true
          description: Alert rule id.
          name: ruleId
          in: path
      responses:
        "204":
          description: Rule deleted.
        "404":
          description: Project or rule not found.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                    description: Human-readable failure reason.
                required:
                  - error
        "503":
          description: The infrastructure store is not open on this Hub.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                    description: Human-readable failure reason.
                required:
                  - error
  /api/projects/{projectId}/infra/alerts:
    get:
      tags:
        - Projects
      summary: List fired infra alerts
      description: |-
        Most-recently-seen first, keyset-paginated. Filter by lifecycle `status`, alarm `state`, rule or resource.

        A resource that has never breached has no row at all — the list is what needs attention, not an inventory with a health column.

        **Cost:** local SQLite only. No AWS call.
      parameters:
        - schema:
            type: string
            description: Project slug (e.g. `agent-hub`).
          required: true
          description: Project slug (e.g. `agent-hub`).
          name: projectId
          in: path
        - schema:
            type: string
            enum:
              - open
              - resolved
              - ignored
          required: false
          name: status
          in: query
        - schema:
            type: string
            enum:
              - OK
              - ALARM
              - INSUFFICIENT_DATA
          required: false
          name: state
          in: query
        - schema:
            type: string
            maxLength: 64
          required: false
          name: ruleId
          in: query
        - schema:
            type: string
            maxLength: 512
          required: false
          name: resourceKey
          in: query
        - schema:
            type: integer
            minimum: 1
            maximum: 200
          required: false
          name: limit
          in: query
        - schema:
            type: string
            minLength: 1
            maxLength: 600
          required: false
          name: cursor
          in: query
      responses:
        "200":
          description: Alert page.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/InfraAlertList"
        "400":
          description: Malformed body or query.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                    description: Human-readable failure reason.
                required:
                  - error
        "404":
          description: Project not found, or the caller cannot see it.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                    description: Human-readable failure reason.
                required:
                  - error
  /api/projects/{projectId}/infra/alerts/{alertId}:
    get:
      tags:
        - Projects
      summary: Get one infra alert with its transition history
      description: The alert plus its recorded transitions, newest first. History is trimmed to a bounded window per alert, so a flapping resource cannot grow it without limit; the aggregate on the alert row (occurrence count, true first and last seen) is the durable record.
      parameters:
        - schema:
            type: string
            description: Project slug (e.g. `agent-hub`).
          required: true
          description: Project slug (e.g. `agent-hub`).
          name: projectId
          in: path
        - schema:
            type: string
            description: Alert id.
          required: true
          description: Alert id.
          name: alertId
          in: path
      responses:
        "200":
          description: The alert.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/InfraAlert"
        "404":
          description: Project or alert not found.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                    description: Human-readable failure reason.
                required:
                  - error
        "503":
          description: The infrastructure store is not open on this Hub.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                    description: Human-readable failure reason.
                required:
                  - error
  /api/projects/{projectId}/infra/alerts/{alertId}/status:
    put:
      tags:
        - Projects
      summary: Resolve, ignore or reopen an infra alert
      description: |-
        One endpoint for all three moves, because they are one column.

        - `resolved` — closed out. A later breach **reopens** it automatically, attributed to `system:recurrence`.
        - `ignored` — muted, and it stays muted through recurrence *and* through recovery. This is the only way to stop a known-bad resource paging on every tick.
        - `open` — reopen a resolved or ignored alert.

        The move is recorded in the alert’s transition history with the acting user as actor, so human and evaluator moves read as one timeline.
      parameters:
        - schema:
            type: string
            description: Project slug (e.g. `agent-hub`).
          required: true
          description: Project slug (e.g. `agent-hub`).
          name: projectId
          in: path
        - schema:
            type: string
            description: Alert id.
          required: true
          description: Alert id.
          name: alertId
          in: path
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                status:
                  type: string
                  enum:
                    - open
                    - resolved
                    - ignored
                  description: "`resolved` closes it out, `ignored` mutes it through recurrence, `open` reopens it."
              required:
                - status
      responses:
        "200":
          description: Updated alert.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/InfraAlert"
        "400":
          description: Malformed body or query.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                    description: Human-readable failure reason.
                required:
                  - error
        "404":
          description: Project or alert not found.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                    description: Human-readable failure reason.
                required:
                  - error
        "503":
          description: The infrastructure store is not open on this Hub.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                    description: Human-readable failure reason.
                required:
                  - error
  /api/infra/health/ingest:
    post:
      tags:
        - Infrastructure
      summary: Ingest an AWS Health event from EventBridge
      security:
        - infraHealthIngestToken: []
      description: |-
        Write-only endpoint targeted by an **operator-created** EventBridge rule in the monitored AWS account, via an API destination using `API_KEY` or Bearer authorization.

        Agent Hub deliberately does **not** call the Health API `DescribeEvents`: it requires a Business Support+ / Enterprise / Unified Operations plan and returns `SubscriptionRequiredException` otherwise, while EventBridge delivery of the same events is free to every AWS customer. The Hub creates nothing in the monitored account — setup is entirely operator-performed.

        The rule pattern must match the source **exactly**:

        ```json
        { "source": ["aws.health"], "detail-type": ["AWS Health Event", "AWS Health Abuse Event"] }
        ```

        A wildcard such as `"aws.health*"` silently never matches.

        **Duplicates are expected.** EventBridge delivery is at-least-once and AWS fans account-specific events out to a backup Region on purpose, so ingestion dedupes on `eventArn` + `communicationId` (widened by the affected account and page number) and reports the suppressed count as `deduped`.

        The handler performs one bounded SQLite write and returns, because EventBridge times out an API-destination request that takes longer than 5 seconds.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              anyOf:
                - $ref: "#/components/schemas/AwsHealthEventEnvelope"
                - type: array
                  items:
                    $ref: "#/components/schemas/AwsHealthEventEnvelope"
                  maxItems: 100
              description: A single EventBridge envelope (what an API destination sends) or an array of up to 100 of them.
      responses:
        "200":
          description: |-
            Delivery processed. Inspect `accepted` / `deduped`.

            A payload that was not a Health event also returns 200, with `accepted: 0` and a `reasons` array. EventBridge does not retry a plain 4xx, so a 400 would not be re-delivered — it would simply count as a failed invocation, filling the dead-letter queue and firing `FailedInvocations` alarms for deliveries that are merely out of scope. A rule scoped slightly too broadly is a configuration nit, not an outage.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/InfraHealthIngestResult"
        "400":
          description: Malformed JSON body.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                required:
                  - error
        "401":
          description: Missing, invalid, or revoked ingest token.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                required:
                  - error
        "413":
          description: Request body exceeds the 1 MiB cap.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                required:
                  - error
        "429":
          description: Per-IP or per-project rate limit exceeded. Retry after the `Retry-After` delay.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                required:
                  - error
        "503":
          description: The infrastructure store is unavailable. Retryable — EventBridge redelivers for up to 24 hours.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                required:
                  - error
  /api/projects/{projectId}/infra/health-events:
    get:
      tags:
        - Projects
      summary: List ingested AWS Health events
      description: |-
        Reads the Health events an operator-created EventBridge rule has delivered to this project. **No AWS call is made** — this is a local read of ingested data, so it is free and works on any AWS support tier.

        By default each incident collapses to its newest communication, which is what the Infrastructure Overview timeline renders.
      parameters:
        - schema:
            type: string
            example: agent-hub
          required: true
          name: projectId
          in: path
        - schema:
            type: integer
            minimum: 1
            maximum: 200
            default: 50
            description: Maximum rows returned.
          required: false
          description: Maximum rows returned.
          name: limit
          in: query
        - schema:
            anyOf:
              - type: boolean
              - type: string
                enum:
                  - "true"
                  - "false"
            description: "Default `true`: collapse each incident to its newest communication, which is what the Overview timeline shows. Pass `false` to see every update AWS published about an event."
          required: false
          description: "Default `true`: collapse each incident to its newest communication, which is what the Overview timeline shows. Pass `false` to see every update AWS published about an event."
          name: latestOnly
          in: query
        - schema:
            type: string
            enum:
              - open
              - closed
              - upcoming
            description: Restrict to one lifecycle state.
          required: false
          description: Restrict to one lifecycle state.
          name: statusCode
          in: query
      responses:
        "200":
          description: Timeline rows, newest first. A project whose infrastructure store never opened returns an empty list rather than an error.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/InfraHealthEventList"
        "404":
          description: Project not found, or the caller cannot see it.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                required:
                  - error
  /api/projects/{projectId}/infra/health-ingest:
    get:
      tags:
        - Projects
      summary: Read AWS Health ingest configuration
      description: Returns the non-secret metadata for the project ingest token, the ingest path, and the exact EventBridge rule pattern to create in the monitored account. Never returns the token plaintext.
      parameters:
        - schema:
            type: string
            example: agent-hub
          required: true
          name: projectId
          in: path
      responses:
        "200":
          description: Ingest configuration. `token` is null when none has been minted.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/InfraHealthIngestConfig"
        "404":
          description: Project not found, or the caller cannot see it.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                required:
                  - error
        "503":
          description: The infrastructure store is not initialized.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                required:
                  - error
    post:
      tags:
        - Projects
      summary: Mint or rotate the AWS Health ingest token
      description: |-
        Creates the project ingest token, or replaces an existing one. Rotation takes effect immediately — there is no grace window, so update the EventBridge connection in the same sitting. Rotating also clears a revocation, so it doubles as re-enable.

        The plaintext is in this response body and nowhere else.
      parameters:
        - schema:
            type: string
            example: agent-hub
          required: true
          name: projectId
          in: path
      responses:
        "201":
          description: Token minted. Copy the plaintext now.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/InfraHealthIngestTokenMint"
        "404":
          description: Project not found, or the caller cannot see it.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                required:
                  - error
        "503":
          description: The infrastructure store is not initialized.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                required:
                  - error
    delete:
      tags:
        - Projects
      summary: Revoke the AWS Health ingest token
      description: Disables ingest without destroying the audit trail — the row is kept with `revokedAt` set, and subsequent deliveries are rejected with 401. Idempotent.
      parameters:
        - schema:
            type: string
            example: agent-hub
          required: true
          name: projectId
          in: path
      responses:
        "200":
          description: "`revoked` is false when the token was already revoked or never existed."
          content:
            application/json:
              schema:
                type: object
                properties:
                  revoked:
                    type: boolean
                  token:
                    $ref: "#/components/schemas/InfraHealthIngestTokenInfo"
                required:
                  - revoked
                  - token
        "404":
          description: Project not found, or the caller cannot see it.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                required:
                  - error
        "503":
          description: The infrastructure store is not initialized.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                required:
                  - error
  /api/projects/{projectId}/infra/setup-draft:
    get:
      tags:
        - Infrastructure
      summary: Report a project’s infrastructure monitoring readiness
      description: |-
        Admin+. Returns the Hub-local draft the infrastructure setup wizard starts from:
        configured AWS profiles with their types (`sso` / `static` / `role`), whether a
        monitoring profile is designated and still resolves, whether the module is enabled,
        the existing `infra_scopes` allowlist, alert-rule counts, and the `blockers[]` that
        stand between the project and unattended collection.

        **Calls AWS zero times.** The draft must work for a project whose only profiles are
        interactive SSO — the case that cannot monitor anything and the most common reason to
        open the wizard — so it never depends on credentials resolving. The live account probe
        happens inside the spawned wizard session under describe-only rules instead.

        Read-only: no session is spawned, no files are written, and no credential material
        (access key, secret key, session token, external ID) appears in the response.
      parameters:
        - schema:
            type: string
            description: Project slug.
          required: true
          description: Project slug.
          name: projectId
          in: path
      responses:
        "200":
          description: Readiness draft computed.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/InfraSetupDraftResponse"
        "404":
          description: Project not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/InfraWizardErrorResponse"
  /api/projects/{projectId}/infra/setup-wizard:
    post:
      tags:
        - Infrastructure
      summary: Spawn the guided infrastructure setup session
      description: |-
        Admin+. Creates an `[Infra Setup]` worktree-backed session, seeds it with the
        Hub-local readiness draft, and starts the agent turn fire-and-forget. Returns as
        soon as the session row exists — the walkthrough itself runs asynchronously over
        the chat WebSocket.

        The session probes the live AWS account **describe-only** (never `GetMetricData`,
        never paginated `ListMetrics`, never an SSO login) and ends by calling
        `setup-apply` with a proposed collection allowlist. It writes configuration, not
        repository files, so there is nothing to commit and no PR to open.

        The draft is embedded in the kickoff prompt inside explicit
        `-----BEGIN UNTRUSTED AWS PROBE-----` markers, and the same fence binds every
        string the agent later reads out of the account. AWS resource names, bucket names
        and tag values are third-party-controlled input; none of them are interpolated
        into the prompt’s authoritative text.
      parameters:
        - schema:
            type: string
            description: Project slug.
          required: true
          description: Project slug.
          name: projectId
          in: path
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties: {}
              description: No body fields; send an empty object.
      responses:
        "201":
          description: Wizard session spawned.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/InfraWizardStartResponse"
        "400":
          description: Project has no agents to host the wizard, or no `cwd` configured. The session is worktree-backed, so it needs a checkout to branch from; refusing here avoids a session that is created but can never start.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/InfraWizardErrorResponse"
        "404":
          description: Project not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/InfraWizardErrorResponse"
        "500":
          description: Wizard agent could not be resolved.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/InfraWizardErrorResponse"
  /api/projects/{projectId}/infra/setup-apply:
    post:
      tags:
        - Infrastructure
      summary: Persist the collection allowlist proposed by the wizard
      description: |-
        Admin+. Stores the `infra_scopes` allowlist the wizard session agreed with the
        operator, plus an optional monthly spend ceiling and the module toggle. This is
        configuration persistence — it writes `infra.db` and `projects.json`, never repo
        files.

        `scopes` is the **complete** list, not a patch: any (profile, region, service)
        triple absent from it is deleted. Surviving triples keep their id, creation time
        and resolved account id.

        Writes are ordered ceiling → allowlist → module flag, so a rejected allowlist
        still leaves a lowered cap applied and collection is only enabled once both the
        scope it would poll and the ceiling that stops it are in place.

        Setting `infraEnabled` to true is refused with 400 unless a ceiling is in effect —
        either sent in the same request or stored by an earlier apply. `GetMetricData` is
        billed per 1,000 metrics with no free tier, so enabling collection with no cap
        would leave the collector nothing to degrade against. The ceiling is never
        defaulted on the operator’s behalf.
      parameters:
        - schema:
            type: string
            description: Project slug.
          required: true
          description: Project slug.
          name: projectId
          in: path
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                scopes:
                  type: array
                  items:
                    type: object
                    properties:
                      profileName:
                        type: string
                        minLength: 1
                        maxLength: 128
                        description: A project AWS profile name. Collection needs one that is not interactive SSO.
                      region:
                        type: string
                        minLength: 1
                        maxLength: 128
                        example: us-east-2
                      service:
                        type: string
                        minLength: 1
                        maxLength: 128
                        description: Service key, lowercased on write.
                        example: ec2
                      tagFilter:
                        type: object
                        nullable: true
                        additionalProperties:
                          type: array
                          items:
                            type: string
                          minItems: 1
                        description: Tag key -> accepted values (ORed). Omit or null to match every resource.
                      enabled:
                        type: boolean
                        description: Defaults to true. A disabled scope is a pause, not a delete.
                    required:
                      - profileName
                      - region
                      - service
                  maxItems: 200
                  description: The complete `infra_scopes` allowlist to store. Replaces the existing list; omitted triples are deleted.
                monthlyCeilingUsd:
                  type: number
                  nullable: true
                  minimum: 0
                  maximum: 1000000
                  description: Monthly AWS API spend ceiling. `null` clears it. Omit to leave the current ceiling untouched. Required (here or already stored) when `infraEnabled` is true.
                infraEnabled:
                  type: boolean
                  description: Toggle the per-project Infrastructure module. Omit to leave the current flag untouched. Setting it to true requires an effective spend ceiling — see `monthlyCeilingUsd`.
              required:
                - scopes
              description: Infrastructure collection scope proposed by the wizard session.
      responses:
        "200":
          description: Allowlist stored.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/InfraSetupApplyResponse"
        "400":
          description: Request body failed validation, or a scope row was rejected.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/InfraWizardErrorResponse"
        "404":
          description: Project not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/InfraWizardErrorResponse"
        "503":
          description: Infrastructure store is unavailable.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/InfraWizardErrorResponse"
  /api/projects/{projectId}/infra/monitoring-status:
    get:
      tags:
        - Projects
      summary: Check whether a project can be monitored unattended
      description: |-
        Resolves the project's designated AWS monitoring profile in-process (no `aws` CLI spawn) and probes CloudWatch with `DescribeAlarms` limited to one record.

        A project that cannot be monitored still returns 200 with `reachable: false` — the Infrastructure module renders its empty state from this body, so callers branch on `code` / `reason` rather than on the status code.

        **Cost:** each call issues exactly one CloudWatch `DescribeAlarms` request against the target account, and nothing is cached server-side. `DescribeAlarms` is a `Requests`-usage-type operation, so it counts against the AWS Free Tier allowance of 1 million CloudWatch API requests per month and is billed per 1,000 requests beyond that allowance — it is not unconditionally free, it is only outside the three operations (`GetMetricData`, `GetInsightRuleReport`, `GetMetricWidgetImage`) that AWS charges from the first call. Call this when a view opens or on operator action; do not poll it on a tight timer.
      parameters:
        - schema:
            type: string
            description: Project slug (e.g. `agent-hub`).
          required: true
          description: Project slug (e.g. `agent-hub`).
          name: projectId
          in: path
      responses:
        "200":
          description: Probe ran. Inspect `reachable`.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/InfraMonitoringStatus"
        "404":
          description: Project not found, or the caller cannot see it.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                    description: Human-readable failure reason.
                required:
                  - error
  /api/projects/{projectId}/infra/metric-packs:
    get:
      tags:
        - Projects
      summary: The declared metric packs, with their caveats and recommended alert rules
      description: |-
        What the collector asks CloudWatch for per service, plus everything an operator needs to read an empty chart correctly: which statistic each metric is legal on, which monitoring mode publishes it, which resources publish it at all, and what is structurally absent (EC2 has no memory or disk-usage metric because the hypervisor cannot see inside the guest).

        `defaultAlertRules` are templates encoding AWS’s own published alarm recommendations. Nothing here is written to `infra_alert_rules` — they exist so the rule editor opens on a recommendation rather than a blank form.

        **Cost:** free. Static declarations; no database read and no AWS call.
      parameters:
        - schema:
            type: string
            description: Project slug (e.g. `agent-hub`).
          required: true
          description: Project slug (e.g. `agent-hub`).
          name: projectId
          in: path
      responses:
        "200":
          description: The pack catalog.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/InfraServicePacks"
        "404":
          description: Project not found, or the caller cannot see it.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                    description: Human-readable failure reason.
                required:
                  - error
  /api/projects/{projectId}/infra/cost:
    get:
      tags:
        - Projects
      summary: AWS API spend to date and projected monthly cost
      description: |-
        Reports what this project has spent on `GetMetricData` so far this UTC month (summed from the `infra_collect_runs` audit trail), the projected monthly cost of its current enabled scopes, the configured ceiling, and the collector’s current degradation level.

        **Cost:** free. Every figure is read from local SQLite — this endpoint issues no AWS calls at all.
      parameters:
        - schema:
            type: string
            description: Project slug (e.g. `agent-hub`).
          required: true
          description: Project slug (e.g. `agent-hub`).
          name: projectId
          in: path
      responses:
        "200":
          description: Spend and projection.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/InfraCostStatus"
        "404":
          description: Project not found, or the caller cannot see it.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                    description: Human-readable failure reason.
                required:
                  - error
  /api/projects/{projectId}/infra/cost/projection:
    post:
      tags:
        - Projects
      summary: Price a proposed scope allowlist before saving it
      description: |-
        Prices a **hypothetical** set of scopes without persisting anything. This is what backs decision INFRA-COST’s requirement that the scope editor show a projected monthly API cost *before* the operator saves — the number that changes behaviour is the one shown at decision time, not the one on next month’s bill.

        Pure arithmetic over the service metric packs. Issues no AWS calls, writes nothing, and is safe to call on every keystroke.
      parameters:
        - schema:
            type: string
            description: Project slug (e.g. `agent-hub`).
          required: true
          description: Project slug (e.g. `agent-hub`).
          name: projectId
          in: path
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                scopes:
                  type: array
                  items:
                    type: object
                    properties:
                      service:
                        type: string
                        minLength: 1
                        example: ec2
                      resourceCount:
                        type: integer
                        minimum: 0
                        maximum: 1000000
                      id:
                        type: string
                      profileName:
                        type: string
                      region:
                        type: string
                        example: us-east-2
                    required:
                      - service
                      - resourceCount
                  maxItems: 500
                degradation:
                  type: string
                  enum:
                    - normal
                    - widened
                  description: Price the projection as if the project were already degraded. Omit for the normal cadence.
              required:
                - scopes
              description: A proposed scope allowlist to price. `resourceCount` is what the operator expects the scope to match; the editor fills it from the inventory browser.
      responses:
        "200":
          description: Projected cost for the submitted scopes.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/InfraCostProjection"
        "400":
          description: Malformed body.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                    description: Human-readable failure reason.
                required:
                  - error
        "404":
          description: Project not found, or the caller cannot see it.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                    description: Human-readable failure reason.
                required:
                  - error
  /api/projects/{projectId}/infra/cost/config:
    put:
      tags:
        - Projects
      summary: Set the project’s monthly AWS API spend ceiling
      description: |-
        Sets (or clears, with `null`) the ceiling the collector degrades against.

        Saving a ceiling deliberately does **not** reset the stored degradation level: that level is a fact about spend which has already happened, and the next collector tick recomputes it. Clearing it here would let an operator un-pause a project by re-saving the same ceiling, and the following tick would pause it again after issuing one more round of billed requests.
      parameters:
        - schema:
            type: string
            description: Project slug (e.g. `agent-hub`).
          required: true
          description: Project slug (e.g. `agent-hub`).
          name: projectId
          in: path
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                monthlyCeilingUsd:
                  type: number
                  nullable: true
                  minimum: 0
                  maximum: 1000000
                  description: Ceiling in USD, or null to remove it. `0` means “collect nothing” and pauses the collector immediately.
                  example: 25
              required:
                - monthlyCeilingUsd
              description: Set or clear a project’s monthly AWS API spend ceiling.
      responses:
        "200":
          description: Ceiling saved; the full cost status is returned.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/InfraCostStatus"
        "400":
          description: Malformed body.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                    description: Human-readable failure reason.
                required:
                  - error
        "404":
          description: Project not found, or the caller cannot see it.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                    description: Human-readable failure reason.
                required:
                  - error
  /api/projects/{projectId}/infra/quotas:
    get:
      tags:
        - Projects
      summary: Service quota headroom from AWS/Usage metrics
      description: |-
        Returns each inventoried service quota with its applied limit, its latest usage reading, and the resulting utilization and absolute headroom.

        This catches the failure where nothing is down and you still cannot launch anything: no per-service alarm fires because no service is unhealthy, the account has simply run out of a quota.

        **Cost: free.** This endpoint never calls AWS. Limits come from the hourly `ListServiceQuotas` inventory sweep and usage from the metric collector; both are already stored locally.

        Utilization reproduces AWS's documented `m1/SERVICE_QUOTA(m1)*100`, substituting the applied value from `ListServiceQuotas` for `SERVICE_QUOTA(m1)`. Only quotas carrying a `UsageMetric` are inventoried at all — that field is absent for the large majority of quotas, and only around 17 AWS services publish usage metrics.
      parameters:
        - schema:
            type: string
            description: Project slug (e.g. `agent-hub`).
          required: true
          description: Project slug (e.g. `agent-hub`).
          name: projectId
          in: path
        - schema:
            type: integer
            description: How many minutes old the newest usage reading may be before a quota reports unknown usage instead. Defaults to 30, comfortably more than the 5-minute collector tick so an ordinary skipped tick does not blank the panel.
            example: 30
          required: false
          description: How many minutes old the newest usage reading may be before a quota reports unknown usage instead. Defaults to 30, comfortably more than the 5-minute collector tick so an ordinary skipped tick does not blank the panel.
          name: staleAfterMinutes
          in: query
      responses:
        "200":
          description: Quota headroom. An empty array when no quota scope is configured or nothing has been synced yet — not an error.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/InfraQuotaHeadroomResponse"
        "400":
          description: Malformed query params.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                    description: Human-readable failure reason.
                required:
                  - error
        "404":
          description: Project not found, or the caller cannot see it.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                    description: Human-readable failure reason.
                required:
                  - error
  /api/projects/{projectId}/infra/spend:
    get:
      tags:
        - Projects
      summary: Cached daily AWS spend trend and top services
      description: |-
        Returns the daily spend trend and top-N services from the `infra_cost_daily` cache that the three-times-daily Cost Explorer poller fills.

        **Cost: free.** This endpoint never calls AWS. `GetCostAndUsage` is billed $0.01 per paginated request with no free tier, so refreshing on demand would charge a cent every time an operator opened the page. AWS’s own best-practices guidance asks for precisely this split: *"we recommend architecting the application so that it has a caching layer… but doesn't trigger queries every time that an individual in your organization accesses it."*

        The cache is filled at most three times a day because *"AWS billing information is updated up to three times daily"* — polling faster produces no fresher data, only a larger bill.
      parameters:
        - schema:
            type: string
            description: Project slug (e.g. `agent-hub`).
          required: true
          description: Project slug (e.g. `agent-hub`).
          name: projectId
          in: path
        - schema:
            type: integer
            description: Days of history to return, ending today. Bounded at 400 because Cost Explorer itself serves at most 13 months plus the current one, so a wider ask could only ever return a window the cache never filled.
            example: 30
          required: false
          description: Days of history to return, ending today. Bounded at 400 because Cost Explorer itself serves at most 13 months plus the current one, so a wider ask could only ever return a window the cache never filled.
          name: days
          in: query
        - schema:
            type: integer
            description: How many services the top-N panel returns.
            example: 5
          required: false
          description: How many services the top-N panel returns.
          name: topServices
          in: query
      responses:
        "200":
          description: Cached spend trend. Empty arrays when nothing has been synced yet.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/InfraSpendTrend"
        "400":
          description: Malformed query params.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                    description: Human-readable failure reason.
                required:
                  - error
        "404":
          description: Project not found, or the caller cannot see it.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                    description: Human-readable failure reason.
                required:
                  - error
  /api/projects/{projectId}/infra/spend/config:
    put:
      tags:
        - Projects
      summary: Opt a project into (or out of) billed Cost Explorer polling
      description: |-
        Every other collector reads a signal that costs nothing until a scope row exists. `GetCostAndUsage` charges from the first request with no free tier at all, so this one takes a deliberate opt-in with a name.

        Enabling does **not** trigger an immediate sync: charging a cent inside the request that saved the checkbox would make the toggle itself cost money. The next scheduled sweep fills the cache.

        Disabling keeps the cached rows. They are spend already paid for, and discarding them would mean an operator toggling the feature off and on re-bought the same history.
      parameters:
        - schema:
            type: string
            description: Project slug (e.g. `agent-hub`).
          required: true
          description: Project slug (e.g. `agent-hub`).
          name: projectId
          in: path
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                enabled:
                  type: boolean
                  description: Turn billed Cost Explorer polling on or off for this project.
                  example: true
              required:
                - enabled
              description: The explicit per-project opt-in that decision INFRA-COST requires, because Cost Explorer is unavoidably billable.
      responses:
        "200":
          description: Opt-in saved; the full spend body is returned.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/InfraSpendTrend"
        "400":
          description: Malformed body.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                    description: Human-readable failure reason.
                required:
                  - error
        "404":
          description: Project not found, or the caller cannot see it.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                    description: Human-readable failure reason.
                required:
                  - error
        "503":
          description: The infrastructure store is unavailable.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                    description: Human-readable failure reason.
                required:
                  - error
  /api/projects/{projectId}/infra/retention:
    get:
      tags:
        - Projects
      summary: Resolved metric retention window and byte quota
      description: |-
        Reports the age window and per-project byte quota the retention reaper enforces against `infra_metric_points`, resolved with defaults for a project that has never configured them.

        **Cost:** free. Local SQLite metadata only — no AWS calls and no table scan.
      parameters:
        - schema:
            type: string
            description: Project slug (e.g. `agent-hub`).
          required: true
          description: Project slug (e.g. `agent-hub`).
          name: projectId
          in: path
      responses:
        "200":
          description: Resolved retention configuration.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/InfraRetentionConfig"
        "404":
          description: Project not found, or the caller cannot see it.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                    description: Human-readable failure reason.
                required:
                  - error
    put:
      tags:
        - Projects
      summary: Override the project’s metric retention window and byte quota
      description: |-
        Writes this project’s `infra_retention_config` row. Either field may be sent alone; the other keeps its current resolved value.

        Values outside the documented bounds are **clamped rather than rejected**, and the clamp is re-applied on read, so narrowing a bound later reinterprets an old row instead of stranding it. The response echoes what was actually stored.

        Shrinking the window or the quota takes effect on the next reaper tick and deletes points, which is not reversible — the data is not re-fetched from CloudWatch.
      parameters:
        - schema:
            type: string
            description: Project slug (e.g. `agent-hub`).
          required: true
          description: Project slug (e.g. `agent-hub`).
          name: projectId
          in: path
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                retentionDays:
                  type: integer
                  minimum: 0
                  maximum: 100000
                  description: New age window in days. Clamped to the documented bounds. Omit to leave as-is.
                  example: 60
                quotaBytes:
                  type: integer
                  minimum: 0
                  maximum: 1099511627776
                  description: New byte quota. Clamped to the documented bounds. Omit to leave as-is.
                  example: 8589934592
              description: Set a project’s metric retention overrides.
      responses:
        "200":
          description: Overrides saved; the resolved configuration is returned.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/InfraRetentionConfig"
        "400":
          description: Malformed body.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                    description: Human-readable failure reason.
                required:
                  - error
        "404":
          description: Project not found, or the caller cannot see it.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                    description: Human-readable failure reason.
                required:
                  - error
        "503":
          description: The infrastructure store is unavailable on this Hub.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                    description: Human-readable failure reason.
                required:
                  - error
  /api/projects/{projectId}/infra/scopes:
    get:
      tags:
        - Projects
      summary: Read the project’s collection allowlist and what it costs
      description: |-
        Returns every scope row for the project, enabled or not, alongside the projected monthly `GetMetricData` spend for the enabled ones.

        Collection is opt-in (decision INFRA-SCOPE): an empty list means **nothing is polled**, never "poll everything". Auto-discovering an account would produce a surprise bill and a throttling storm in someone else’s account, so every billed request the collector issues traces back to a row here.

        Issues no AWS calls — scopes, resource counts and pricing are all local.
      parameters:
        - schema:
            type: string
            description: Project slug (e.g. `agent-hub`).
          required: true
          description: Project slug (e.g. `agent-hub`).
          name: projectId
          in: path
      responses:
        "200":
          description: The allowlist and its projected monthly cost.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/InfraScopesResponse"
        "404":
          description: Project not found, or the caller cannot see it.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                    description: Human-readable failure reason.
                required:
                  - error
    put:
      tags:
        - Projects
      summary: Replace the project’s collection allowlist
      description: |-
        Replaces the whole allowlist in one transaction. Rows absent from the body are **deleted**, which stops collection but touches neither `infra_resources` nor `infra_metric_points` — history a scope produced outlives the scope, and re-adding the triple resumes its charts rather than starting from an empty axis.

        Whole-list rather than per-row on purpose: the editor prices the list before saving, and that price is a property of the whole list. Per-row saves would let an operator approve one estimate and commit a larger configuration a row at a time, each save individually consistent and the total never shown.

        A surviving `(profile, region, service)` triple keeps its `id`, `createdAt` and resolved `accountId`. Tag filters are validated with the same parser the collector uses, so a filter that saves is one the collector will accept.
      parameters:
        - schema:
            type: string
            description: Project slug (e.g. `agent-hub`).
          required: true
          description: Project slug (e.g. `agent-hub`).
          name: projectId
          in: path
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                scopes:
                  type: array
                  items:
                    type: object
                    properties:
                      profileName:
                        type: string
                        minLength: 1
                        maxLength: 128
                        description: Project AWS profile the scope collects through. Keyed on the profile rather than the account id because the profile is what the operator picks and what credential resolution needs.
                        example: monitoring
                      region:
                        type: string
                        minLength: 1
                        maxLength: 128
                        example: us-east-2
                      service:
                        type: string
                        minLength: 1
                        maxLength: 128
                        description: "Service token, lowercased on save. Free text with no enum: the collectable list grows every release, and a token with no metric pack is accepted but reported back in `uncollectableServices` rather than rejected."
                        example: ec2
                      tagFilter:
                        type: object
                        nullable: true
                        additionalProperties:
                          type: array
                          items:
                            type: string
                          minItems: 1
                        description: Optional tag filter as tag key -> accepted values. Values within one key are ORed, keys are ANDed — EC2 filter semantics, with `*` and `?` wildcards. Omit or send null to match every resource the describe call returns.
                        example: *a2
                      enabled:
                        type: boolean
                        description: Defaults to true. A disabled scope is retained rather than deleted, so its inventory and metric history survive being switched back on.
                    required:
                      - profileName
                      - region
                      - service
                    description: One (profile, region, service) triple in the collection allowlist.
                  maxItems: 200
                monthlyCeilingUsd:
                  type: number
                  nullable: true
                  minimum: 0
                  maximum: 1000000
                  description: Optional ceiling to save in the same request. Omit to leave the current ceiling alone; null clears it. Accepted here so that approving a projection and capping it are one operator action and one write, with no window where the scopes saved and the cap did not.
                  example: 25
              required:
                - scopes
              description: "The complete allowlist. This is a whole-list replace, not a patch: rows absent from the body are deleted."
      responses:
        "200":
          description: Allowlist saved; the stored scopes and their projection are returned.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/InfraScopesResponse"
        "400":
          description: Malformed body, a duplicate triple, or an unparseable tag filter.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                    description: Human-readable failure reason.
                required:
                  - error
        "404":
          description: Project not found, or the caller cannot see it.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                    description: Human-readable failure reason.
                required:
                  - error
        "503":
          description: The infrastructure store is unavailable on this Hub.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                    description: Human-readable failure reason.
                required:
                  - error
  /api/projects/{projectId}/infra/resources:
    get:
      tags:
        - Projects
      summary: Browse discovered infrastructure resources
      description: |-
        The inventory the hourly describe sweep built, filterable by service, region, account, environment, tag and lifecycle state, most-recently-seen first.

        Rows are never deleted (decision INFRA-SCOPE): a terminated instance keeps its history and ages out on `lastSeen`. The default `seenSince` window matches the collector’s staleness bound, so by default this lists what is actually being polled rather than everything ever described.

        **Cost:** local SQLite only. No AWS call.
      parameters:
        - schema:
            type: string
            description: Project slug (e.g. `agent-hub`).
          required: true
          description: Project slug (e.g. `agent-hub`).
          name: projectId
          in: path
        - schema:
            type: string
            maxLength: 64
          required: false
          name: service
          in: query
        - schema:
            type: string
            maxLength: 32
          required: false
          name: region
          in: query
        - schema:
            type: string
            maxLength: 64
          required: false
          name: accountId
          in: query
        - schema:
            type: string
            maxLength: 128
            description: Exact match. The sentinel `none` selects rows carrying no environment label.
          required: false
          description: Exact match. The sentinel `none` selects rows carrying no environment label.
          name: environment
          in: query
        - schema:
            type: string
            maxLength: 64
          required: false
          name: state
          in: query
        - schema:
            type: string
            maxLength: 256
            description: Case-insensitive substring over resource id and name.
          required: false
          description: Case-insensitive substring over resource id and name.
          name: search
          in: query
        - schema:
            type: string
            maxLength: 128
          required: false
          name: tagKey
          in: query
        - schema:
            type: string
            maxLength: 256
            description: Exact tag value. Ignored without `tagKey`.
          required: false
          description: Exact tag value. Ignored without `tagKey`.
          name: tagValue
          in: query
        - schema:
            type: integer
            nullable: true
            description: Epoch ms. Drop rows not described since then. Defaults to the collector’s own staleness bound, so the browser shows what is actually being polled; pass `0` to include everything ever seen.
          required: false
          description: Epoch ms. Drop rows not described since then. Defaults to the collector’s own staleness bound, so the browser shows what is actually being polled; pass `0` to include everything ever seen.
          name: seenSince
          in: query
        - schema:
            type: integer
            minimum: 1
            maximum: 500
          required: false
          name: limit
          in: query
        - schema:
            type: string
            minLength: 1
            maxLength: 600
          required: false
          name: cursor
          in: query
        - schema:
            anyOf:
              - type: boolean
              - type: string
                enum:
                  - "true"
                  - "false"
            description: "Default `false`: Service Quotas rows (`service=quota`, ids like `kms/L-0…`) are excluded. They describe a limit rather than something that runs, there are dozens per account, and the Overview quota panel already summarises them. Pass `true` to list them alongside real inventory. An explicit `service=quota` filter includes them regardless."
          required: false
          description: "Default `false`: Service Quotas rows (`service=quota`, ids like `kms/L-0…`) are excluded. They describe a limit rather than something that runs, there are dozens per account, and the Overview quota panel already summarises them. Pass `true` to list them alongside real inventory. An explicit `service=quota` filter includes them regardless."
          name: includeQuotas
          in: query
      responses:
        "200":
          description: Inventory page. Empty on a Hub whose infra store never opened.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/InfraResourceList"
        "400":
          description: Malformed query.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                    description: Human-readable failure reason.
                required:
                  - error
        "404":
          description: Project not found, or the caller cannot see it.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                    description: Human-readable failure reason.
                required:
                  - error
  /api/projects/{projectId}/infra/fleet:
    get:
      tags:
        - Projects
      summary: Compute fleet with headline metrics
      description: |-
        Every EC2 instance, ECS cluster/service and RDS instance in scope, each carrying the two or three headline series that belong on a dashboard, already reduced to a latest value and a sparkline.

        This is the batch counterpart to `/infra/metrics`, which serves one resource and one metric per request — filling a dashboard through that endpoint costs resources × metrics round trips. Here the SQL scan count is bounded by the headline catalog and does not grow with the number of resources on the page.

        Which series are headline is decided by the service packs (`server/infra/headline-metrics.ts`); a Container Insights series is omitted for a cluster that does not have the feature, because the collector never asked CloudWatch for it either.

        **Cost:** local SQLite only. No AWS call.
      parameters:
        - schema:
            type: string
            description: Project slug (e.g. `agent-hub`).
          required: true
          description: Project slug (e.g. `agent-hub`).
          name: projectId
          in: path
        - schema:
            type: string
            maxLength: 128
            description: Comma-separated service tokens. Defaults to every dashboard service (`ec2`, `ecs`, `rds`). Tokens outside that set contribute nothing.
          required: false
          description: Comma-separated service tokens. Defaults to every dashboard service (`ec2`, `ecs`, `rds`). Tokens outside that set contribute nothing.
          name: services
          in: query
        - schema:
            type: string
            maxLength: 32
          required: false
          name: region
          in: query
        - schema:
            type: string
            maxLength: 128
            description: Exact match. The sentinel `none` selects rows carrying no environment label.
          required: false
          description: Exact match. The sentinel `none` selects rows carrying no environment label.
          name: environment
          in: query
        - schema:
            type: integer
            minimum: 900000
            maximum: 604800000
            description: Lookback ending now. Defaults to 3h. The bucket width is derived from it so a sparkline holds roughly the same number of points at any window.
          required: false
          description: Lookback ending now. Defaults to 3h. The bucket width is derived from it so a sparkline holds roughly the same number of points at any window.
          name: windowMs
          in: query
        - schema:
            type: integer
            minimum: 1
            maximum: 200
            description: Resources returned. Defaults to 60. Beyond the ceiling a dashboard is a list, which is what the Resources browser is for.
          required: false
          description: Resources returned. Defaults to 60. Beyond the ceiling a dashboard is a list, which is what the Resources browser is for.
          name: limit
          in: query
      responses:
        "200":
          description: The fleet. Empty `resources` on a project with no scopes, and on a Hub whose infra store never opened.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/InfraFleet"
        "400":
          description: Malformed query.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                    description: Human-readable failure reason.
                required:
                  - error
        "404":
          description: Project not found, or the caller cannot see it.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                    description: Human-readable failure reason.
                required:
                  - error
  /api/projects/{projectId}/infra/metric-series:
    get:
      tags:
        - Projects
      summary: List the metric series stored for one resource
      description: |-
        Populates the chart’s metric picker. Every entry is a series that has real stored points behind it, so choosing one cannot produce an empty chart for want of collection.

        **Cost:** local SQLite only. No AWS call.
      parameters:
        - schema:
            type: string
            description: Project slug (e.g. `agent-hub`).
          required: true
          description: Project slug (e.g. `agent-hub`).
          name: projectId
          in: path
        - schema:
            type: string
            minLength: 1
            maxLength: 512
            description: Resource key to catalog.
          required: true
          description: Resource key to catalog.
          name: resource
          in: query
      responses:
        "200":
          description: The series catalog. `resource` is null when the key is unknown.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/InfraMetricSeriesList"
        "400":
          description: Malformed query.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                    description: Human-readable failure reason.
                required:
                  - error
        "404":
          description: Project not found, or the caller cannot see it.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                    description: Human-readable failure reason.
                required:
                  - error
  /api/projects/{projectId}/infra/metrics:
    get:
      tags:
        - Projects
      summary: Read a bounded metric range for one resource
      description: |-
        The chart read. `from` and `to` are **required** and the window is capped at 455 days — CloudWatch’s own longest retention tier, past which a wider range can only be empty on its old end. An unbounded range is rejected rather than served, because one series at 60s over a year is half a million rows.

        The bucket width is resolved server-side from the window using the collector’s own `resolvePeriod` semantics, then widened until the range fits the bucket cap. That is what stops a 90-day view from asking for 60s data that aged out of CloudWatch 75 days ago and rendering empty.

        Alert state over the same window comes back as `alarmSegments`, reconstructed from transition history — a chart’s job is to show *when* a resource went bad, which its current alert state cannot answer.

        Poll this on an interval; there is no metric WebSocket (decision INFRA-UI). **Cost:** local SQLite only. No AWS call.
      parameters:
        - schema:
            type: string
            description: Project slug (e.g. `agent-hub`).
          required: true
          description: Project slug (e.g. `agent-hub`).
          name: projectId
          in: path
        - schema:
            type: string
            minLength: 1
            maxLength: 512
            description: Resource key to chart.
          required: true
          description: Resource key to chart.
          name: resource
          in: query
        - schema:
            type: string
            minLength: 1
            maxLength: 255
            example: CPUUtilization
          required: true
          name: metric
          in: query
        - schema:
            type: integer
            description: Window start, epoch ms. Required.
          required: true
          description: Window start, epoch ms. Required.
          name: from
          in: query
        - schema:
            type: integer
            description: Window end, epoch ms. Required.
          required: true
          description: Window end, epoch ms. Required.
          name: to
          in: query
        - schema:
            type: string
            maxLength: 255
          required: false
          name: namespace
          in: query
        - schema:
            type: string
            maxLength: 64
          required: false
          name: stat
          in: query
        - schema:
            type: string
            maxLength: 64
          required: false
          name: dimensionsHash
          in: query
        - schema:
            type: integer
            minimum: 1
            maximum: 86400
            description: Pin the stored period tier to read from. Omit to take it from the series catalog — the server never guesses a tier the data is not stored at.
          required: false
          description: Pin the stored period tier to read from. Omit to take it from the series catalog — the server never guesses a tier the data is not stored at.
          name: period
          in: query
      responses:
        "200":
          description: The bucketed range. Empty `points` when nothing is stored for the series.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/InfraMetricRange"
        "400":
          description: Missing or unbounded window, or a malformed filter.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                    description: Human-readable failure reason.
                required:
                  - error
        "404":
          description: Project not found, or the caller cannot see it.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                    description: Human-readable failure reason.
                required:
                  - error
  /api/jobs:
    get:
      tags:
        - Jobs
      summary: List background jobs (Admin) — filter by status and/or type
      description: Newest-first listing of the host-wide job queue with per-status counts and the distinct job types present. Admin/Owner only.
      parameters:
        - schema:
            type: string
            enum:
              - queued
              - running
              - done
              - dead_letter
          required: false
          name: status
          in: query
        - schema:
            type: string
          required: false
          name: type
          in: query
        - schema:
            type: integer
            minimum: 1
            maximum: 200
          required: false
          name: limit
          in: query
        - schema:
            type: integer
            nullable: true
            minimum: 0
          required: false
          name: offset
          in: query
      responses:
        "200":
          description: Jobs, counts, and the distinct types present.
          content:
            application/json:
              schema:
                type: object
                properties:
                  jobs:
                    type: array
                    items:
                      type: object
                      properties:
                        id:
                          type: string
                        type:
                          type: string
                        payload:
                          type: string
                        status:
                          type: string
                          enum:
                            - queued
                            - running
                            - done
                            - dead_letter
                        priority:
                          type: integer
                        attempts:
                          type: integer
                        max_attempts:
                          type: integer
                        run_at:
                          type: number
                        claimed_by:
                          type: string
                          nullable: true
                        claimed_at:
                          type: number
                          nullable: true
                        lease_id:
                          type: string
                          nullable: true
                        last_error:
                          type: string
                          nullable: true
                        created_at:
                          type: number
                        updated_at:
                          type: number
                      required:
                        - id
                        - type
                        - payload
                        - status
                        - priority
                        - attempts
                        - max_attempts
                        - run_at
                        - claimed_by
                        - claimed_at
                        - lease_id
                        - last_error
                        - created_at
                        - updated_at
                  counts:
                    type: object
                    properties:
                      queued:
                        type: integer
                      running:
                        type: integer
                      done:
                        type: integer
                      dead_letter:
                        type: integer
                      total:
                        type: integer
                    required:
                      - queued
                      - running
                      - done
                      - dead_letter
                      - total
                  types:
                    type: array
                    items:
                      type: string
                  limit:
                    type: integer
                  offset:
                    type: integer
                required:
                  - jobs
                  - counts
                  - types
                  - limit
                  - offset
        "400":
          description: Invalid filter.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                required:
                  - error
        "403":
          description: Not an Admin/Owner.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                required:
                  - error
  /api/jobs/{id}/retry:
    post:
      tags:
        - Jobs
      summary: Requeue a dead-lettered job (Admin)
      description: Resets a dead_letter job back to queued with a fresh attempt budget so a worker retries it. Only dead_letter jobs are retryable (409 otherwise).
      parameters:
        - schema:
            type: string
          required: true
          name: id
          in: path
      responses:
        "200":
          description: Job requeued.
          content:
            application/json:
              schema:
                type: object
                properties:
                  job:
                    type: object
                    properties:
                      id:
                        type: string
                      type:
                        type: string
                      payload:
                        type: string
                      status:
                        type: string
                        enum:
                          - queued
                          - running
                          - done
                          - dead_letter
                      priority:
                        type: integer
                      attempts:
                        type: integer
                      max_attempts:
                        type: integer
                      run_at:
                        type: number
                      claimed_by:
                        type: string
                        nullable: true
                      claimed_at:
                        type: number
                        nullable: true
                      lease_id:
                        type: string
                        nullable: true
                      last_error:
                        type: string
                        nullable: true
                      created_at:
                        type: number
                      updated_at:
                        type: number
                    required:
                      - id
                      - type
                      - payload
                      - status
                      - priority
                      - attempts
                      - max_attempts
                      - run_at
                      - claimed_by
                      - claimed_at
                      - lease_id
                      - last_error
                      - created_at
                      - updated_at
                required:
                  - job
        "403":
          description: Not an Admin/Owner.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                required:
                  - error
        "404":
          description: Unknown job.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                required:
                  - error
        "409":
          description: Job is not in the dead_letter state.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                required:
                  - error
  /api/jobs/{id}:
    delete:
      tags:
        - Jobs
      summary: Delete a background job row (Admin)
      parameters:
        - schema:
            type: string
          required: true
          name: id
          in: path
      responses:
        "200":
          description: Job deleted.
          content:
            application/json:
              schema:
                type: object
                properties:
                  ok:
                    type: boolean
                required:
                  - ok
        "403":
          description: Not an Admin/Owner.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                required:
                  - error
        "404":
          description: Unknown job.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                required:
                  - error
  /api/otel/v1/logs:
    post:
      tags:
        - Log Ingest
      summary: OTLP/HTTP logs ingest
      description: "OpenTelemetry OTLP/HTTP logs endpoint. Accepts `application/json` and binary `application/x-protobuf` `ExportLogsServiceRequest` bodies, each optionally gzip-compressed (`Content-Encoding: gzip` or a raw gzip-framed body). Requests are capped at 1 MiB on the wire; batches at 1,000 records; single records at 256 KiB. Rejected records are reported via `partialSuccess` — the HTTP status stays 200 so the source app is never blocked. Fields (severity, timestamps, resource, attributes, scope, trace_id, span_id) are preserved and redacted before persistence."
      security: &a4
        - logIngestToken: []
      requestBody:
        content:
          application/json:
            schema:
              type: object
              additionalProperties:
                nullable: true
              description: "OTLP `ExportLogsServiceRequest` (`{ resourceLogs: [...] }`)."
          application/x-protobuf:
            schema:
              type: string
              format: binary
              description: Binary-Protobuf ExportLogsServiceRequest.
      responses:
        "200":
          description: Accepted (possibly partial). `partialSuccess` populated when some records were rejected.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OtlpExportLogsResponse"
        "400":
          description: Malformed JSON/Protobuf body.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LogIngestError"
            application/x-protobuf:
              schema:
                type: string
                format: binary
                description: Binary-Protobuf `google.rpc.Status` with code `INVALID_ARGUMENT`.
        "401":
          description: Missing, invalid, or revoked ingest token.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LogIngestError"
        "413":
          description: Request or decompressed body exceeds the size cap.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LogIngestError"
        "429":
          description: Per-source or per-IP rate limit exceeded.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LogIngestError"
        "503":
          description: Log store temporarily unavailable (backpressure).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LogIngestError"
  /api/logs/ingest:
    post:
      tags:
        - Log Ingest
      summary: Agent Hub JSON log batch ingest
      description: "Simple JSON batch endpoint mapped to the same canonical OTel LogRecord model. Body is `{ resource?, records: [...] }`, optionally gzip-compressed. Same 1 MiB request / 1,000-record batch / 256 KiB record caps, source-token auth, and pre-persistence redaction as the OTLP endpoint."
      security: *a4
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/LogIngestBatch"
      responses:
        "200":
          description: Ingest result (accepted / rejected / redactions).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LogIngestResponse"
        "400":
          description: Malformed JSON body or missing `records` array.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LogIngestError"
        "401":
          description: Missing, invalid, or revoked ingest token.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LogIngestError"
        "413":
          description: Request or decompressed body exceeds the size cap.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LogIngestError"
        "429":
          description: Per-source or per-IP rate limit exceeded.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LogIngestError"
        "503":
          description: Log store temporarily unavailable (backpressure).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LogIngestError"
  /api/projects/{projectId}/logs/issues:
    get:
      tags:
        - Logs
      summary: List grouped error issues
      description: Project-scoped, newest-activity-first, cursor-paginated. Filter by lifecycle status (open/resolved/ignored). A cursor from another project cannot reveal its issues.
      parameters:
        - schema:
            type: string
          required: true
          name: projectId
          in: path
        - schema:
            type: string
            enum:
              - open
              - resolved
              - ignored
          required: false
          name: status
          in: query
        - schema:
            type: integer
            minimum: 1
            maximum: 200
          required: false
          name: limit
          in: query
        - schema:
            type: string
            minLength: 1
            maxLength: 200
          required: false
          name: cursor
          in: query
      responses:
        "200":
          description: Issue page.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LogIssueListResponse"
        "400":
          description: Malformed query.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LogIssueErrorResponse"
        "403":
          description: Insufficient role.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LogIssueErrorResponse"
        "404":
          description: Project not found or not visible.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LogIssueErrorResponse"
  /api/projects/{projectId}/logs/issues/{issueId}/fix:
    post:
      tags:
        - Logs
      summary: Start or reuse a tracked Fix session for an error issue
      description: Creates one In Progress kanban card and one isolated worktree chat session per active issue. The session inherits the initiating user's project Finalize automation preference, falling back to manual/Build. The prompt includes the bounded, redacted issue context and requires a regression test. This action never uses board assignment defaults. Set startAnother=true only when the user explicitly wants a second workflow; it becomes the canonical linked workflow.
      parameters:
        - schema:
            type: string
          required: true
          name: projectId
          in: path
        - schema:
            type: string
          required: true
          name: issueId
          in: path
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/LogIssueActionRequest"
      responses:
        "200":
          description: The newly started or existing Fix workflow.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LogIssueFixResponse"
        "400":
          description: No eligible agent or malformed board configuration.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LogIssueErrorResponse"
        "403":
          description: Insufficient role.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LogIssueErrorResponse"
        "404":
          description: Project or issue not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LogIssueErrorResponse"
        "409":
          description: Another Fix workflow is being created for this issue.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LogIssueErrorResponse"
  /api/projects/{projectId}/logs/issues/{issueId}:
    get:
      tags:
        - Logs
      summary: Get an error issue with release facets and sample records
      parameters:
        - schema:
            type: string
          required: true
          name: projectId
          in: path
        - schema:
            type: string
          required: true
          name: issueId
          in: path
      responses:
        "200":
          description: Issue detail.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LogIssueDetail"
        "403":
          description: Insufficient role.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LogIssueErrorResponse"
        "404":
          description: Project or issue not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LogIssueErrorResponse"
  /api/projects/{projectId}/logs/issues/bulk-status:
    post:
      tags:
        - Logs
      summary: Set the lifecycle status of many error issues at once
      description: Applies one status (open/resolved/ignored) to every listed issue in a single transaction. Ids are deduplicated and project-scoped; ids that do not resolve in this project are reported in `notFound` rather than failing the batch.
      parameters:
        - schema:
            type: string
          required: true
          name: projectId
          in: path
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/LogIssueBulkStatusRequest"
      responses:
        "200":
          description: Updated issues plus any unresolved ids.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LogIssueBulkStatusResponse"
        "400":
          description: Malformed body (empty selection, unknown status, or over the id cap).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LogIssueErrorResponse"
        "403":
          description: Insufficient role.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LogIssueErrorResponse"
        "404":
          description: Project not found or not visible.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LogIssueErrorResponse"
  /api/projects/{projectId}/logs/issues/{issueId}/resolve:
    post:
      tags:
        - Logs
      summary: Resolve an error issue
      parameters:
        - schema:
            type: string
          required: true
          name: projectId
          in: path
        - schema:
            type: string
          required: true
          name: issueId
          in: path
      responses:
        "200":
          description: Updated issue.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LogIssue"
        "403":
          description: Insufficient role.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LogIssueErrorResponse"
        "404":
          description: Project or issue not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LogIssueErrorResponse"
  /api/projects/{projectId}/logs/issues/{issueId}/ignore:
    post:
      tags:
        - Logs
      summary: Ignore (mute) an error issue
      parameters:
        - schema:
            type: string
          required: true
          name: projectId
          in: path
        - schema:
            type: string
          required: true
          name: issueId
          in: path
      responses:
        "200":
          description: Updated issue.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LogIssue"
        "403":
          description: Insufficient role.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LogIssueErrorResponse"
        "404":
          description: Project or issue not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LogIssueErrorResponse"
  /api/projects/{projectId}/logs/issues/{issueId}/reopen:
    post:
      tags:
        - Logs
      summary: Reopen an error issue
      parameters:
        - schema:
            type: string
          required: true
          name: projectId
          in: path
        - schema:
            type: string
          required: true
          name: issueId
          in: path
      responses:
        "200":
          description: Updated issue.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LogIssue"
        "403":
          description: Insufficient role.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LogIssueErrorResponse"
        "404":
          description: Project or issue not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LogIssueErrorResponse"
  /api/projects/{projectId}/logs/issues/{issueId}/analyze:
    post:
      tags:
        - Logs
      summary: Start (or reopen) a read-only Analyze chat session for an error issue
      description: "Idempotently starts a normal chat session on the project default dev/lead agent, seeded with a bounded, redacted, fenced log context pack and a read-only investigation brief (finalize_automation=manual, isolated worktree when supported). While a live linked session exists, repeat calls return it (`reused: true`). Set startAnother=true only when the user explicitly wants another investigation; it becomes the canonical linked session. The linked session id is exposed as `analyzeSessionId` on issue detail."
      parameters:
        - schema:
            type: string
          required: true
          name: projectId
          in: path
        - schema:
            type: string
          required: true
          name: issueId
          in: path
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/LogIssueActionRequest"
      responses:
        "200":
          description: The linked Analyze session (newly started or reused).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LogIssueAnalyzeResponse"
        "400":
          description: No eligible agent for the project.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LogIssueErrorResponse"
        "403":
          description: Insufficient role.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LogIssueErrorResponse"
        "404":
          description: Project or issue not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LogIssueErrorResponse"
  /api/projects/{projectId}/logs/metrics:
    get:
      tags:
        - Log Sources
      summary: Log-store health metrics
      description: Returns batch-writer queue depth, ingest/reaper counters (accepted, written, rejected, dropped, redacted, write errors, reaper deletions, WebSocket drops), writer latency, and per-project storage + retention-lag gauges. Requires Admin and project access.
      parameters:
        - schema:
            type: string
            description: Project ID (slug).
          required: true
          description: Project ID (slug).
          name: projectId
          in: path
      responses:
        "200":
          description: Metrics.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LogStoreMetrics"
        "403":
          description: Insufficient role.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LogMetricsErrorResponse"
        "404":
          description: Project not found or not visible to the caller.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LogMetricsErrorResponse"
  /api/projects/{projectId}/logs:
    get:
      tags:
        - Logs
      summary: Query project logs
      description: Returns a bounded page ordered newest-first by event time, keyset-paginated on (timeUnixNano, id). Pass back both `nextCursor` and `nextCursorTimeUnixNano` for the next older page. Every filter and cursor is scoped to the path project; a cursor from another project cannot reveal records.
      parameters:
        - schema:
            type: string
          required: true
          name: projectId
          in: path
        - schema:
            type: integer
            nullable: true
            minimum: 0
          required: false
          name: startTimeUnixNano
          in: query
        - schema:
            type: integer
            nullable: true
            minimum: 0
          required: false
          name: endTimeUnixNano
          in: query
        - schema:
            type: string
            minLength: 1
            maxLength: 200
          required: false
          name: sourceId
          in: query
        - schema:
            type: string
            minLength: 1
            maxLength: 200
          required: false
          name: serviceName
          in: query
        - schema:
            type: string
            minLength: 1
            maxLength: 200
          required: false
          name: environment
          in: query
        - schema:
            type: integer
            nullable: true
            minimum: 0
            maximum: 24
          required: false
          name: minSeverityNumber
          in: query
        - schema:
            type: string
            minLength: 1
            maxLength: 500
          required: false
          name: text
          in: query
        - schema:
            type: string
            minLength: 1
            maxLength: 256
          required: false
          name: traceId
          in: query
        - schema:
            type: string
            minLength: 1
            maxLength: 256
          required: false
          name: fingerprint
          in: query
        - schema:
            type: integer
            minimum: 0
            exclusiveMinimum: true
          required: false
          name: cursor
          in: query
        - schema:
            type: integer
            nullable: true
            minimum: 0
          required: false
          name: cursorTimeUnixNano
          in: query
        - schema:
            type: integer
            minimum: 1
            maximum: 500
          required: false
          name: limit
          in: query
      responses:
        "200":
          description: Log page.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LogQueryResponse"
        "400":
          description: Malformed query parameters.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LogQueryErrorResponse"
        "403":
          description: Insufficient role.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LogQueryErrorResponse"
        "404":
          description: Project not found or not visible.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LogQueryErrorResponse"
    delete:
      tags:
        - Logs
      summary: Clear all project logs
      description: Purges every ingested log record (and its full-text index row) for the project in one transaction, returning the number removed. Destructive and Admin-gated. Grouped error Issues are a separate surface and are left intact.
      parameters:
        - schema:
            type: string
          required: true
          name: projectId
          in: path
      responses:
        "200":
          description: Records purged.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LogPurgeResponse"
        "403":
          description: Insufficient role.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LogQueryErrorResponse"
        "404":
          description: Project not found or not visible.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LogQueryErrorResponse"
  /api/projects/{projectId}/log-sources:
    get:
      tags:
        - Log Sources
      summary: List a project’s log sources
      description: Returns all log sources for the project, newest-first. Never includes token material — only the non-secret `tokenPrefix`. Requires Admin and project access.
      parameters:
        - schema:
            type: string
            description: Project ID (slug).
          required: true
          description: Project ID (slug).
          name: projectId
          in: path
      responses:
        "200":
          description: Sources.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LogSourceList"
        "403":
          description: Insufficient role.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LogSourceErrorResponse"
        "404":
          description: Project not found or not visible to the caller.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LogSourceErrorResponse"
    post:
      tags:
        - Log Sources
      summary: Create a log source and mint its ingest token
      description: Registers a named source and returns its `ahlog_` ingest token exactly once (only the hash is stored). Source names are unique per project (409 on collision). Requires Admin and project access.
      parameters:
        - schema:
            type: string
            description: Project ID (slug).
          required: true
          description: Project ID (slug).
          name: projectId
          in: path
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                name:
                  type: string
                  minLength: 1
                  maxLength: 100
                serviceName:
                  type: string
                  nullable: true
                  maxLength: 200
                environment:
                  type: string
                  nullable: true
                  maxLength: 200
              required:
                - name
      responses:
        "201":
          description: Source created; token revealed once.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LogSourceWithToken"
        "400":
          description: Body validation failed.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LogSourceErrorResponse"
        "403":
          description: Insufficient role.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LogSourceErrorResponse"
        "404":
          description: Project not found or not visible to the caller.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LogSourceErrorResponse"
        "409":
          description: A source with this name already exists in the project.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LogSourceErrorResponse"
  /api/projects/{projectId}/log-sources/{sourceId}:
    get:
      tags:
        - Log Sources
      summary: Get one log source
      parameters:
        - schema:
            type: string
            description: Project ID (slug).
          required: true
          description: Project ID (slug).
          name: projectId
          in: path
        - schema:
            type: string
            description: Log source ID.
          required: true
          description: Log source ID.
          name: sourceId
          in: path
      responses:
        "200":
          description: Source.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LogSource"
        "403":
          description: Insufficient role.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LogSourceErrorResponse"
        "404":
          description: Project or source not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LogSourceErrorResponse"
    patch:
      tags:
        - Log Sources
      summary: Update a log source’s metadata
      description: Updates name / serviceName / environment. Token material is untouched. Requires Admin and project access.
      parameters:
        - schema:
            type: string
            description: Project ID (slug).
          required: true
          description: Project ID (slug).
          name: projectId
          in: path
        - schema:
            type: string
            description: Log source ID.
          required: true
          description: Log source ID.
          name: sourceId
          in: path
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                name:
                  type: string
                  minLength: 1
                  maxLength: 100
                serviceName:
                  type: string
                  nullable: true
                  maxLength: 200
                environment:
                  type: string
                  nullable: true
                  maxLength: 200
      responses:
        "200":
          description: Updated source.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LogSource"
        "400":
          description: Body validation failed.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LogSourceErrorResponse"
        "403":
          description: Insufficient role.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LogSourceErrorResponse"
        "404":
          description: Project or source not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LogSourceErrorResponse"
        "409":
          description: A source with this name already exists in the project.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LogSourceErrorResponse"
    delete:
      tags:
        - Log Sources
      summary: Delete a log source and its token
      parameters:
        - schema:
            type: string
            description: Project ID (slug).
          required: true
          description: Project ID (slug).
          name: projectId
          in: path
        - schema:
            type: string
            description: Log source ID.
          required: true
          description: Log source ID.
          name: sourceId
          in: path
      responses:
        "204":
          description: Deleted.
        "403":
          description: Insufficient role.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LogSourceErrorResponse"
        "404":
          description: Project or source not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LogSourceErrorResponse"
  /api/projects/{projectId}/log-sources/{sourceId}/rotate:
    post:
      tags:
        - Log Sources
      summary: Rotate a log source’s ingest token
      description: Mints a fresh token (invalidating the previous one) and re-activates a revoked source. The new plaintext token is returned exactly once. Requires Admin and project access.
      parameters:
        - schema:
            type: string
            description: Project ID (slug).
          required: true
          description: Project ID (slug).
          name: projectId
          in: path
        - schema:
            type: string
            description: Log source ID.
          required: true
          description: Log source ID.
          name: sourceId
          in: path
      responses:
        "200":
          description: Rotated; new token revealed once.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LogSourceWithToken"
        "403":
          description: Insufficient role.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LogSourceErrorResponse"
        "404":
          description: Project or source not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LogSourceErrorResponse"
  /api/projects/{projectId}/log-sources/{sourceId}/revoke:
    post:
      tags:
        - Log Sources
      summary: Revoke a log source’s ingest token
      description: "Write-disables the source: its token can no longer ingest. The row is kept for audit. Idempotent. Requires Admin and project access."
      parameters:
        - schema:
            type: string
            description: Project ID (slug).
          required: true
          description: Project ID (slug).
          name: projectId
          in: path
        - schema:
            type: string
            description: Log source ID.
          required: true
          description: Log source ID.
          name: sourceId
          in: path
      responses:
        "200":
          description: Revoked source.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LogSource"
        "403":
          description: Insufficient role.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LogSourceErrorResponse"
        "404":
          description: Project or source not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LogSourceErrorResponse"
  /api/projects/{projectId}/log-sources/{sourceId}/audit:
    get:
      tags:
        - Log Sources
      summary: List a log source’s lifecycle audit
      description: Returns create/update/rotate/revoke/delete events for the source, newest-first, attributed to the acting user. Requires Admin and project access.
      parameters:
        - schema:
            type: string
            description: Project ID (slug).
          required: true
          description: Project ID (slug).
          name: projectId
          in: path
        - schema:
            type: string
            description: Log source ID.
          required: true
          description: Log source ID.
          name: sourceId
          in: path
      responses:
        "200":
          description: Audit entries.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LogSourceAuditList"
        "403":
          description: Insufficient role.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LogSourceErrorResponse"
        "404":
          description: Project or source not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LogSourceErrorResponse"
  /api/projects/{projectId}/logs/setup-draft:
    get:
      tags:
        - Logs
      summary: Scan a project for log-instrumentation signal
      description: "Admin+. Reads the project working copy and returns the draft the logs setup wizard needs: language stack, existing logging libraries and OpenTelemetry setup, exporter target candidates, a recommended ingest approach, the Hub ingest endpoints, and the project’s existing log sources (metadata only). Read-only — no session is spawned and no files are written."
      parameters:
        - schema:
            type: string
            description: Project slug.
          required: true
          description: Project slug.
          name: projectId
          in: path
      responses:
        "200":
          description: Detection draft computed.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LogsSetupDraftResponse"
        "400":
          description: Project has no cwd configured.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LogsWizardErrorResponse"
        "404":
          description: Project not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LogsWizardErrorResponse"
  /api/projects/{projectId}/logs/setup-wizard:
    post:
      tags:
        - Logs
      summary: Spawn the AI logs setup wizard
      description: |-
        Admin+. Scans the project and spawns a **worktree-backed** chat session
        (`use_worktree=1`) loaded with the `logs-setup` skill. The detection draft
        (stack, recommended approach, exporter target candidates, ingest endpoints,
        existing sources) is embedded in the kickoff prompt. The agent creates a
        log source (minting an `ahlog_` token via the Hub API), wires an
        OTLP/JSON-batch exporter into the app referencing the token as an env var,
        commits to its branch, and uses Finalize Code Changes to push and open a PR.
      parameters:
        - schema:
            type: string
            description: Project slug.
          required: true
          description: Project slug.
          name: projectId
          in: path
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties: {}
              description: No body fields; send an empty object.
      responses:
        "201":
          description: Wizard session spawned.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LogsWizardStartResponse"
        "400":
          description: Project has no cwd configured, or no agents to host the wizard.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LogsWizardErrorResponse"
        "404":
          description: Project not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LogsWizardErrorResponse"
        "500":
          description: Wizard agent could not be resolved.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LogsWizardErrorResponse"
  /api/me/daily-summary:
    get:
      tags:
        - Hub
      summary: Read today's Hub Daily Summary without generating
      description: "Returns the stored Daily Summary when it was generated on the caller's local calendar day (`tz`). A leftover report from a previous day is treated as empty (`report: null`). Never spawns a model."
      parameters:
        - schema:
            type: string
            description: IANA timezone used to resolve "today". Defaults to UTC.
          required: false
          description: IANA timezone used to resolve "today". Defaults to UTC.
          name: tz
          in: query
      responses:
        "200":
          description: Stored report for today, or report=null.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MeDailySummaryResponse"
        "401":
          description: Authentication required.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MeDailySummaryErrorResponse"
    post:
      tags:
        - Hub
      summary: Generate (or regenerate) today's Hub Daily Summary
      description: Gathers today / right-now / yesterday facts for the caller and runs an available, non-erroring engine. Replaces any existing report for the same local day. GET is never enough to spawn this.
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                tz:
                  type: string
                  description: IANA timezone used to resolve "today". Defaults to UTC.
      responses:
        "200":
          description: Newly generated report.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MeDailySummaryResponse"
        "400":
          description: Engine credentials missing for this account.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MeDailySummaryErrorResponse"
        "401":
          description: Authentication required.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MeDailySummaryErrorResponse"
        "503":
          description: No AI engines are configured or available.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MeDailySummaryErrorResponse"
  /api/me/daily-summary/schedule:
    get:
      tags:
        - Hub
      summary: Read the caller's Hub Daily Summary auto-refresh schedule
      description: "Returns the caller's stored schedule, or `schedule: null` when the summary is not scheduled."
      responses:
        "200":
          description: The stored schedule (or null).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MeDailySummaryScheduleResponse"
        "401":
          description: Authentication required.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MeDailySummaryErrorResponse"
    put:
      tags:
        - Hub
      summary: Set the caller's Hub Daily Summary auto-refresh schedule
      description: Replaces the schedule. Invalid `HH:MM` entries are dropped; an empty or all-invalid `times` array clears the schedule. The ticker regenerates the report at each configured local time using the caller's own engine credentials.
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                enabled:
                  type: boolean
                  description: When false the times are kept but auto-refresh is paused.
                timeZone:
                  type: string
                  description: IANA timezone the times are interpreted in. Defaults to UTC.
                times:
                  type: array
                  items:
                    type: string
                  description: 24h local times of day (`HH:MM`). Capped at 12.
      responses:
        "200":
          description: The normalized, stored schedule (or null when cleared).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MeDailySummaryScheduleResponse"
        "400":
          description: Malformed schedule body.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MeDailySummaryErrorResponse"
        "401":
          description: Authentication required.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MeDailySummaryErrorResponse"
  /api/me/todos:
    get:
      tags:
        - Personal Todos
      summary: List the calling user's personal todos
      parameters:
        - schema:
            type: string
            enum:
              - open
              - done
            description: Filter to only open or only done.
          required: false
          description: Filter to only open or only done.
          name: status
          in: query
      responses:
        "200":
          description: The caller's todos in per-user order.
          content:
            application/json:
              schema:
                type: object
                properties:
                  todos:
                    type: array
                    items:
                      $ref: "#/components/schemas/UserTodo"
                required:
                  - todos
        "400":
          description: Invalid status filter.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MeTodosErrorResponse"
        "401":
          description: Authentication required.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MeTodosErrorResponse"
    post:
      tags:
        - Personal Todos
      summary: Create a personal todo
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                title:
                  type: string
                  minLength: 1
                notes:
                  type: string
                priority:
                  type: string
                  enum:
                    - urgent
                    - high
                    - medium
                    - low
                doDate:
                  type: string
                  nullable: true
                  description: Day the user plans to work the task (scheduling "do" date, not a deadline).
                doStartAt:
                  type: string
                  nullable: true
                doEndAt:
                  type: string
                  nullable: true
                dueAt:
                  type: string
                  nullable: true
                sourceType:
                  type: string
                  enum:
                    - manual
                    - email
                    - calendar
                sourceId:
                  type: string
                  nullable: true
                sourceMeta:
                  type: object
                  additionalProperties:
                    nullable: true
                linkedType:
                  type: string
                  nullable: true
                  enum:
                    - card
                    - epic
                    - session
                    - null
                  description: Polymorphic link target type. Co-dependent with linkedId; an explicit null clears the link.
                linkedId:
                  type: string
                  nullable: true
                  description: Linked entity id. Required when linkedType is set.
                linkedProjectId:
                  type: string
                  nullable: true
                  description: Scopes a project-bound link target (card / epic). Ignored for a session link.
              required:
                - title
      responses:
        "201":
          description: Created todo.
          content:
            application/json:
              schema:
                type: object
                properties:
                  todo:
                    $ref: "#/components/schemas/UserTodo"
                required:
                  - todo
        "400":
          description: Validation failed.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MeTodosErrorResponse"
        "401":
          description: Authentication required.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MeTodosErrorResponse"
  /api/me/todos/{id}:
    put:
      tags:
        - Personal Todos
      summary: Update a personal todo (partial)
      parameters:
        - schema:
            type: string
            description: Todo id.
          required: true
          description: Todo id.
          name: id
          in: path
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                title:
                  type: string
                  minLength: 1
                notes:
                  type: string
                status:
                  type: string
                  enum:
                    - open
                    - done
                priority:
                  type: string
                  enum:
                    - urgent
                    - high
                    - medium
                    - low
                doDate:
                  type: string
                  nullable: true
                doStartAt:
                  type: string
                  nullable: true
                doEndAt:
                  type: string
                  nullable: true
                dueAt:
                  type: string
                  nullable: true
                linkedType:
                  type: string
                  nullable: true
                  enum:
                    - card
                    - epic
                    - session
                    - null
                  description: Polymorphic link target type. Co-dependent with linkedId; an explicit null clears the link.
                linkedId:
                  type: string
                  nullable: true
                  description: Linked entity id. Required when linkedType is set.
                linkedProjectId:
                  type: string
                  nullable: true
                  description: Scopes a project-bound link target (card / epic). Ignored for a session link.
      responses:
        "200":
          description: Updated todo.
          content:
            application/json:
              schema:
                type: object
                properties:
                  todo:
                    $ref: "#/components/schemas/UserTodo"
                required:
                  - todo
        "400":
          description: Validation failed.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MeTodosErrorResponse"
        "401":
          description: Authentication required.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MeTodosErrorResponse"
        "404":
          description: Todo not found (or not owned by caller).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MeTodosErrorResponse"
    delete:
      tags:
        - Personal Todos
      summary: Delete a personal todo
      parameters:
        - schema:
            type: string
            description: Todo id.
          required: true
          description: Todo id.
          name: id
          in: path
      responses:
        "200":
          description: Deleted.
          content:
            application/json:
              schema:
                type: object
                properties:
                  ok:
                    type: boolean
                required:
                  - ok
        "401":
          description: Authentication required.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MeTodosErrorResponse"
        "404":
          description: Todo not found (or not owned by caller).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MeTodosErrorResponse"
  /api/me/todos/{id}/promote:
    post:
      tags:
        - Personal Todos
      summary: Promote a personal todo to a project kanban card
      description: Creates a real kanban card in the target project (To Do by default), stamps card provenance back to the source todo, and links the todo to the created card.
      parameters:
        - schema:
            type: string
            description: Todo id.
          required: true
          description: Todo id.
          name: id
          in: path
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                projectId:
                  type: string
                  minLength: 1
                columnId:
                  type: string
                  minLength: 1
                epicId:
                  type: string
                  minLength: 1
                priority:
                  type: string
                  enum:
                    - urgent
                    - high
                    - medium
                    - low
                  description: Overrides the created card's priority. Omit to carry over the todo priority.
              required:
                - projectId
      responses:
        "200":
          description: Todo was already promoted; returning the existing linked card.
          content:
            application/json:
              schema:
                type: object
                properties:
                  todo:
                    $ref: "#/components/schemas/UserTodo"
                  card:
                    $ref: "#/components/schemas/KanbanCard"
                required:
                  - todo
                  - card
        "201":
          description: Created card and updated todo link.
          content:
            application/json:
              schema:
                type: object
                properties:
                  todo:
                    $ref: "#/components/schemas/UserTodo"
                  card:
                    $ref: "#/components/schemas/KanbanCard"
                required:
                  - todo
                  - card
        "400":
          description: Validation failed.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MeTodosErrorResponse"
        "401":
          description: Authentication required.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MeTodosErrorResponse"
        "404":
          description: Todo, project, column, or epic not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MeTodosErrorResponse"
        "409":
          description: Todo is already linked to a different or missing card.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MeTodosErrorResponse"
  /api/me/todos/linked:
    get:
      tags:
        - Personal Todos
      summary: The caller's todos linked to a given card / epic / session
      description: "Reverse side of the polymorphic link: returns the calling user's own todos that point at the target entity (bidirectional display). Todos are private, so this never surfaces another user's from-todo."
      parameters:
        - schema:
            type: string
            enum:
              - card
              - epic
              - session
            description: Target entity type.
          required: true
          description: Target entity type.
          name: targetType
          in: query
        - schema:
            type: string
            minLength: 1
            description: Target entity id.
          required: true
          description: Target entity id.
          name: targetId
          in: query
        - schema:
            type: string
            minLength: 1
            description: Project of a card / epic target. Ignored for a session.
          required: false
          description: Project of a card / epic target. Ignored for a session.
          name: projectId
          in: query
      responses:
        "200":
          description: The caller's todos linked to the target.
          content:
            application/json:
              schema:
                type: object
                properties:
                  todos:
                    type: array
                    items:
                      $ref: "#/components/schemas/UserTodo"
                required:
                  - todos
        "400":
          description: Validation failed.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MeTodosErrorResponse"
        "401":
          description: Authentication required.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MeTodosErrorResponse"
        "404":
          description: Target not found or not visible to the caller.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MeTodosErrorResponse"
  /api/me/todos/{id}/link:
    post:
      tags:
        - Personal Todos
      summary: Link a personal todo to an existing card / epic / session
      description: Associates the todo with an existing entity without creating anything. The caller must be able to see the target (project visibility for a card / epic, session ownership for a session), else 404.
      parameters:
        - schema:
            type: string
            description: Todo id.
          required: true
          description: Todo id.
          name: id
          in: path
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                targetType:
                  type: string
                  enum:
                    - card
                    - epic
                    - session
                  description: "Existing entity to link to: a kanban card, an epic, or a session."
                targetId:
                  type: string
                  minLength: 1
                projectId:
                  type: string
                  minLength: 1
                  description: Required for a card / epic target (scopes and RBAC-gates the project). Ignored for a session.
              required:
                - targetType
                - targetId
      responses:
        "200":
          description: Linked todo.
          content:
            application/json:
              schema:
                type: object
                properties:
                  todo:
                    $ref: "#/components/schemas/UserTodo"
                required:
                  - todo
        "400":
          description: Validation failed.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MeTodosErrorResponse"
        "401":
          description: Authentication required.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MeTodosErrorResponse"
        "404":
          description: Todo or target not found (or not visible to the caller).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MeTodosErrorResponse"
    delete:
      tags:
        - Personal Todos
      summary: Clear a personal todo's link
      parameters:
        - schema:
            type: string
            description: Todo id.
          required: true
          description: Todo id.
          name: id
          in: path
      responses:
        "200":
          description: Unlinked todo.
          content:
            application/json:
              schema:
                type: object
                properties:
                  todo:
                    $ref: "#/components/schemas/UserTodo"
                required:
                  - todo
        "401":
          description: Authentication required.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MeTodosErrorResponse"
        "404":
          description: Todo not found (or not owned by caller).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MeTodosErrorResponse"
  /api/me/todos/reorder:
    post:
      tags:
        - Personal Todos
      summary: Reassign the per-user order of the caller's todos
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                orderedIds:
                  type: array
                  items:
                    type: string
                  description: Ids in the desired order. Ids the caller does not own are ignored.
              required:
                - orderedIds
      responses:
        "200":
          description: The reordered todo list.
          content:
            application/json:
              schema:
                type: object
                properties:
                  todos:
                    type: array
                    items:
                      $ref: "#/components/schemas/UserTodo"
                required:
                  - todos
        "400":
          description: Validation failed.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MeTodosErrorResponse"
        "401":
          description: Authentication required.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MeTodosErrorResponse"
  /api/me/dashboard:
    get:
      tags:
        - Personal Todos
      summary: Aggregate the calling user's cross-project dashboard
      description: "Server-side per-uid fan-out across every board the caller can view: assigned cards, open todos, today's Google calendar, and flagged-mail counts. Cached per user; pass ?fresh=1 to bypass."
      parameters:
        - schema:
            type: string
            description: Set to 1/true to bypass the per-uid cache.
          required: false
          description: Set to 1/true to bypass the per-uid cache.
          name: fresh
          in: query
        - schema:
            type: string
            description: "YYYY-MM-DD day for the calendar read (default: today, UTC)."
          required: false
          description: "YYYY-MM-DD day for the calendar read (default: today, UTC)."
          name: date
          in: query
        - schema:
            type: string
            description: IANA time zone passed to Google for calendar display.
          required: false
          description: IANA time zone passed to Google for calendar display.
          name: tz
          in: query
      responses:
        "200":
          description: The aggregated dashboard payload.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MeDashboardResponse"
        "401":
          description: Authentication required.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MeDashboardErrorResponse"
  /api/me/work:
    get:
      tags:
        - Personal Todos
      summary: List the calling user's assigned cards across visible boards
      description: "The 'My Work' slice: kanban cards whose assigned_user_id is the caller, from every project they can view (RBAC-filtered). Read-only, always fresh."
      responses:
        "200":
          description: Assigned cards + open-work counts.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DashboardWork"
        "401":
          description: Authentication required.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MeDashboardErrorResponse"
  /api/me/hub-session:
    get:
      tags:
        - Hub
      summary: Get or create the calling user's Hub assistant session
      description: Idempotent. Returns the existing Hub session for this user, or creates one (session_mode=hub, no worktree) on the hidden Hub assistant agent. Spawn credentials follow the session owner. The Hub project is omitted from GET /api/projects.
      responses:
        "200":
          description: Existing Hub session.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MeHubSessionResponse"
        "201":
          description: Newly created Hub session.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MeHubSessionResponse"
        "401":
          description: Authentication required.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MeHubErrorResponse"
    post:
      tags:
        - Hub
      summary: Get or create the calling user's Hub assistant session
      description: Idempotent. Returns the existing Hub session for this user, or creates one (session_mode=hub, no worktree) on the hidden Hub assistant agent. Spawn credentials follow the session owner. The Hub project is omitted from GET /api/projects.
      responses:
        "200":
          description: Existing Hub session.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MeHubSessionResponse"
        "201":
          description: Newly created Hub session.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MeHubSessionResponse"
        "401":
          description: Authentication required.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MeHubErrorResponse"
  /api/me/hub-model:
    get:
      tags:
        - Hub
      summary: Read the caller's Hub engine and model
      description: Resolved from the caller's per-user Hub assistant override, else the hidden Hub agent defaults. Daily Summary and other Hub generation use this pick.
      responses:
        "200":
          description: Resolved Hub engine and model.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MeHubModelResponse"
        "401":
          description: Authentication required.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MeHubErrorResponse"
    put:
      tags:
        - Hub
      summary: Set the caller's Hub engine and model
      description: Persists a per-user override on the hidden Hub assistant and updates the caller's live Hub session. Daily Summary generation uses the same pick.
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                engine:
                  type: string
                model:
                  type: string
              required:
                - engine
                - model
      responses:
        "200":
          description: Saved Hub engine and model.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MeHubModelResponse"
        "400":
          description: Invalid engine or model.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MeHubErrorResponse"
        "401":
          description: Authentication required.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MeHubErrorResponse"
  /api/me/hub-session/clear:
    post:
      tags:
        - Hub
      summary: Clear Hub assistant chat history
      description: Archives the current Hub chat and creates a fresh empty session with the caller's Hub engine/model. Message history is recoverable from the archived session for 24h.
      responses:
        "200":
          description: New empty Hub session.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MeHubSessionResponse"
        "401":
          description: Authentication required.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MeHubErrorResponse"
  /api/memory/reconcile:
    post:
      tags:
        - Memory
      summary: Manually trigger the wiki ↔ memory reconciliation sweep
      responses:
        "200":
          description: Sweep started (runs async).
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                    enum:
                      - running
                required:
                  - status
  /api/projects/{projectId}/notes/{date}/process:
    post:
      tags:
        - Memory
      summary: Hand a daily note to the project's docs agent for processing
      description: Spawns a session for the project's docs agent with a target-specific prompt. The session id is included in the response so callers can stream the result.
      parameters:
        - schema:
            type: string
          required: true
          name: projectId
          in: path
        - schema:
            type: string
            pattern: ^\d{4}-\d{2}-\d{2}$
          required: true
          name: date
          in: path
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                target:
                  type: string
                  enum:
                    - auto
                    - wiki
                    - memory
                    - plan
                  description: Where to write the extracted knowledge. Defaults to `auto`.
                excerpt:
                  type: string
                  description: Override the note content with an explicit excerpt (used when the user has already trimmed the note).
      responses:
        "201":
          description: Processing row created.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/NoteProcessing"
        "400":
          description: Invalid date or target, or empty note.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MemoryErrorResponse"
        "404":
          description: Project / note / docs agent not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MemoryErrorResponse"
        "500":
          description: Failed to read note from disk.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MemoryErrorResponse"
  /api/projects/{projectId}/notes/processings:
    get:
      tags:
        - Memory
      summary: List recent note-processing runs for a project
      parameters:
        - schema:
            type: string
            description: Project slug or id.
          required: true
          description: Project slug or id.
          name: projectId
          in: path
        - schema:
            type: string
            description: Max rows (default 50, cap 200).
          required: false
          description: Max rows (default 50, cap 200).
          name: limit
          in: query
      responses:
        "200":
          description: Processing rows.
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/NoteProcessing"
        "404":
          description: Project not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MemoryErrorResponse"
  /api/projects/{projectId}/notes/{date}/processings:
    get:
      tags:
        - Memory
      summary: List processing runs for a specific daily note
      parameters:
        - schema:
            type: string
          required: true
          name: projectId
          in: path
        - schema:
            type: string
            pattern: ^\d{4}-\d{2}-\d{2}$
          required: true
          name: date
          in: path
      responses:
        "200":
          description: Processing rows.
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/NoteProcessing"
        "400":
          description: Invalid date format.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MemoryErrorResponse"
        "404":
          description: Project not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MemoryErrorResponse"
  /api/projects/{projectId}/notes/processings/{processingId}:
    get:
      tags:
        - Memory
      summary: Read a single note-processing run
      parameters:
        - schema:
            type: string
          required: true
          name: projectId
          in: path
        - schema:
            type: string
          required: true
          name: processingId
          in: path
      responses:
        "200":
          description: Processing row.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/NoteProcessing"
        "404":
          description: Processing not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MemoryErrorResponse"
  /api/health:
    get:
      tags:
        - Health
      summary: Liveness + runtime info
      responses:
        "200":
          description: Health payload.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HealthResponse"
  /api/server-logs:
    get:
      tags:
        - Health
      summary: Tail of the in-memory server log buffer
      responses:
        "200":
          description: Recent log entries.
          content:
            application/json:
              schema:
                type: array
                items:
                  type: object
                  properties:
                    ts:
                      type: string
                    level:
                      type: string
                    msg:
                      type: string
                  additionalProperties:
                    nullable: true
  /api/browse:
    get:
      tags:
        - Misc
      summary: Directory browser scoped to $HOME
      description: Lists immediate subdirectories of `path` (default $HOME). Refuses any path outside $HOME with 403; refuses non-directories with 400.
      parameters:
        - schema:
            type: string
            description: Directory under $HOME. `~` and `~/foo` expand.
          required: false
          description: Directory under $HOME. `~` and `~/foo` expand.
          name: path
          in: query
      responses:
        "200":
          description: Directory listing.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrowseResponse"
        "400":
          description: Path is not a directory.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MiscErrorResponse"
        "403":
          description: Path escapes $HOME.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MiscErrorResponse"
        "404":
          description: Path does not exist.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MiscErrorResponse"
  /api/devices:
    post:
      tags:
        - Devices
      summary: Register an APNs / FCM push token
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                token:
                  type: string
                  minLength: 1
                platform:
                  type: string
              required:
                - token
      responses:
        "200":
          description: Registered.
          content:
            application/json:
              schema:
                type: object
                properties:
                  ok:
                    type: boolean
                required:
                  - ok
        "400":
          description: token is required.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MiscErrorResponse"
  /api/devices/{token}:
    delete:
      tags:
        - Devices
      summary: Remove a push token
      parameters:
        - schema:
            type: string
          required: true
          name: token
          in: path
      responses:
        "200":
          description: Removed.
          content:
            application/json:
              schema:
                type: object
                properties:
                  ok:
                    type: boolean
                required:
                  - ok
    get:
      tags:
        - Devices
      summary: Read a push token's preferences + supported events
      parameters:
        - schema:
            type: string
          required: true
          name: token
          in: path
      responses:
        "200":
          description: Token row.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DeviceTokenResponse"
        "404":
          description: Token not registered.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MiscErrorResponse"
  /api/devices/{token}/preferences:
    put:
      tags:
        - Devices
      summary: Update per-event push preferences
      description: '`enabledEvents: null` clears the preference (legacy "all events" behaviour). Unknown event names are stripped server-side.'
      parameters:
        - schema:
            type: string
          required: true
          name: token
          in: path
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                enabledEvents:
                  type: array
                  nullable: true
                  items:
                    type: string
                    enum:
                      - awaiting_feedback
                      - ready_to_push
                      - pushed
                      - support_ticket_created
                      - thread_message
                      - review_assigned_to_you
                      - pr_merged
                      - infra_alert
      responses:
        "200":
          description: Updated.
          content:
            application/json:
              schema:
                type: object
                properties:
                  ok:
                    type: boolean
                  enabledEvents:
                    type: array
                    nullable: true
                    items:
                      type: string
                required:
                  - ok
                  - enabledEvents
        "400":
          description: enabledEvents must be an array or null.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MiscErrorResponse"
        "404":
          description: Token not registered.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MiscErrorResponse"
  /api/usage:
    get:
      tags:
        - Usage
      summary: Aggregated usage stats (totals, per-agent, per-day, recent sessions)
      responses:
        "200":
          description: Usage payload.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UsageResponse"
        "500":
          description: Aggregation query failed.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MiscErrorResponse"
  /api/projects/{projectId}/notes:
    get:
      tags:
        - Notes
      summary: List notes (or FTS search) for a project
      parameters:
        - schema:
            type: string
            description: Project slug or id.
          required: true
          description: Project slug or id.
          name: projectId
          in: path
        - schema:
            type: string
            description: FTS search query.
          required: false
          description: FTS search query.
          name: q
          in: query
        - schema:
            type: string
            description: Max results when searching (default 20).
          required: false
          description: Max results when searching (default 20).
          name: limit
          in: query
      responses:
        "200":
          description: Note rows.
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Note"
        "404":
          description: Project not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/NotesErrorResponse"
    post:
      tags:
        - Notes
      summary: Create a note
      parameters:
        - schema:
            type: string
            description: Project slug or id.
          required: true
          description: Project slug or id.
          name: projectId
          in: path
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                title:
                  type: string
                  minLength: 1
                content:
                  type: string
              required:
                - title
      responses:
        "201":
          description: Created note.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Note"
        "400":
          description: Title is required.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/NotesErrorResponse"
        "404":
          description: Project not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/NotesErrorResponse"
        "409":
          description: Slug collision or constraint violation.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/NotesErrorResponse"
  /api/projects/{projectId}/notes/{noteId}:
    get:
      tags:
        - Notes
      summary: Read a single note
      parameters:
        - schema:
            type: string
          required: true
          name: projectId
          in: path
        - schema:
            type: string
          required: true
          name: noteId
          in: path
      responses:
        "200":
          description: Note row.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Note"
        "404":
          description: Note not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/NotesErrorResponse"
    put:
      tags:
        - Notes
      summary: Update a note
      parameters:
        - schema:
            type: string
          required: true
          name: projectId
          in: path
        - schema:
            type: string
          required: true
          name: noteId
          in: path
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                title:
                  type: string
                content:
                  type: string
      responses:
        "200":
          description: Updated note.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Note"
        "404":
          description: Note not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/NotesErrorResponse"
        "500":
          description: Update failed.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/NotesErrorResponse"
    delete:
      tags:
        - Notes
      summary: Delete a note
      parameters:
        - schema:
            type: string
          required: true
          name: projectId
          in: path
        - schema:
            type: string
          required: true
          name: noteId
          in: path
      responses:
        "200":
          description: Deleted.
          content:
            application/json:
              schema:
                type: object
                properties:
                  ok:
                    type: boolean
                required:
                  - ok
        "404":
          description: Note not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/NotesErrorResponse"
  /api/auth/me/claude-auth/browser:
    get:
      tags:
        - Auth
      summary: Per-user Claude Code browser-login status.
      responses:
        "200":
          description: Caller-scoped Claude Code OAuth state.
          content:
            application/json:
              schema:
                type: object
                properties:
                  uiStatus:
                    type: string
                  binary:
                    type: object
                    properties:
                      present:
                        type: boolean
                      path:
                        type: string
                    required:
                      - present
                      - path
                  oauth:
                    type: object
                    properties:
                      loggedIn:
                        type: boolean
                        nullable: true
                    required:
                      - loggedIn
                  loginInProgress:
                    type: boolean
                  activeMethod:
                    type: string
                    enum:
                      - oauth
                      - none
                  statusError:
                    type: string
                    nullable: true
                required:
                  - uiStatus
                  - binary
                  - oauth
                  - loginInProgress
                  - activeMethod
                  - statusError
        "401":
          description: Not authenticated.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
    delete:
      tags:
        - Auth
      summary: Sign the per-user Claude Code cache out.
      responses:
        "200":
          description: Cache cleared.
          content:
            application/json:
              schema:
                type: object
                properties:
                  ok:
                    type: boolean
                    enum:
                      - true
                  output:
                    type: string
                required:
                  - ok
                  - output
        "401":
          description: Not authenticated.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /api/auth/me/claude-auth/browser/login:
    post:
      tags:
        - Auth
      summary: Start a per-user Claude Code browser login.
      responses:
        "200":
          description: Login URL emitted by Claude Code or login result.
          content:
            application/json:
              schema:
                type: object
                properties:
                  ok:
                    type: boolean
                  loginId:
                    type: string
                  loginUrl:
                    type: string
                  completed:
                    type: boolean
                  output:
                    type: string
                required:
                  - ok
        "400":
          description: Claude Code binary missing.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Not authenticated.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /api/auth/me/claude-auth/browser/cancel-login:
    post:
      tags:
        - Auth
      summary: Cancel an in-progress per-user Claude Code login.
      responses:
        "200":
          description: Cancellation receipt.
          content:
            application/json:
              schema:
                type: object
                properties:
                  ok:
                    type: boolean
                    enum:
                      - true
                  output:
                    type: string
                required:
                  - ok
                  - output
        "401":
          description: Not authenticated.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /api/auth/me/cursor-auth/browser:
    get:
      tags:
        - Auth
      summary: Per-user Cursor "Sign in with browser" status.
      responses:
        "200":
          description: Caller-scoped Cursor OAuth state.
          content:
            application/json:
              schema:
                type: object
                properties:
                  uiStatus:
                    type: string
                  binary:
                    type: object
                    properties:
                      present:
                        type: boolean
                      path:
                        type: string
                    required:
                      - present
                      - path
                  oauth:
                    type: object
                    properties:
                      loggedIn:
                        type: boolean
                        nullable: true
                      email:
                        type: string
                        nullable: true
                    required:
                      - loggedIn
                  loginInProgress:
                    type: boolean
                  activeMethod:
                    type: string
                    enum:
                      - oauth
                      - none
                  statusError:
                    type: string
                    nullable: true
                required:
                  - uiStatus
                  - binary
                  - oauth
                  - loginInProgress
                  - activeMethod
                  - statusError
        "401":
          description: Not authenticated.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
    delete:
      tags:
        - Auth
      summary: Sign the per-user Cursor cache out (wipe ~/.cursor).
      responses:
        "200":
          description: Cache cleared.
          content:
            application/json:
              schema:
                type: object
                properties:
                  ok:
                    type: boolean
                    enum:
                      - true
                  output:
                    type: string
                required:
                  - ok
                  - output
        "401":
          description: Not authenticated.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /api/auth/me/cursor-auth/browser/login:
    post:
      tags:
        - Auth
      summary: Start a per-user Cursor OAuth login.
      responses:
        "200":
          description: Login URL emitted by cursor-agent or login result.
          content:
            application/json:
              schema:
                type: object
                properties:
                  ok:
                    type: boolean
                  loginId:
                    type: string
                  loginUrl:
                    type: string
                  completed:
                    type: boolean
                  output:
                    type: string
                required:
                  - ok
        "400":
          description: Cursor binary missing.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Not authenticated.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /api/auth/me/cursor-auth/browser/cancel-login:
    post:
      tags:
        - Auth
      summary: Cancel an in-progress per-user Cursor login.
      responses:
        "200":
          description: Cancellation receipt.
          content:
            application/json:
              schema:
                type: object
                properties:
                  ok:
                    type: boolean
                    enum:
                      - true
                  output:
                    type: string
                required:
                  - ok
                  - output
        "401":
          description: Not authenticated.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /api/auth/me/codex-auth/browser:
    get:
      tags:
        - Auth
      summary: Per-user Codex "Sign in with ChatGPT" status.
      responses:
        "200":
          description: Caller-scoped Codex OAuth state.
          content:
            application/json:
              schema:
                type: object
                properties:
                  uiStatus:
                    type: string
                  binary:
                    type: object
                    properties:
                      present:
                        type: boolean
                      path:
                        type: string
                    required:
                      - present
                      - path
                  oauth:
                    type: object
                    properties:
                      loggedIn:
                        type: boolean
                        nullable: true
                      mode:
                        type: string
                        nullable: true
                      authJsonPath:
                        type: string
                        nullable: true
                    required:
                      - loggedIn
                  loginInProgress:
                    type: boolean
                  activeMethod:
                    type: string
                    enum:
                      - oauth
                      - none
                  statusError:
                    type: string
                    nullable: true
                required:
                  - uiStatus
                  - binary
                  - oauth
                  - loginInProgress
                  - activeMethod
                  - statusError
        "401":
          description: Not authenticated.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
    delete:
      tags:
        - Auth
      summary: Sign the per-user Codex cache out (wipe ~/.codex).
      responses:
        "200":
          description: Cache cleared.
          content:
            application/json:
              schema:
                type: object
                properties:
                  ok:
                    type: boolean
                    enum:
                      - true
                  output:
                    type: string
                required:
                  - ok
                  - output
        "401":
          description: Not authenticated.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /api/auth/me/codex-auth/browser/device-login:
    post:
      tags:
        - Auth
      summary: Start a per-user Codex device-auth (ChatGPT) login.
      responses:
        "200":
          description: Device code emitted or login outcome.
          content:
            application/json:
              schema:
                type: object
                properties:
                  ok:
                    type: boolean
                  loginId:
                    type: string
                  deviceAuthUrl:
                    type: string
                  userCode:
                    type: string
                  output:
                    type: string
                required:
                  - ok
        "400":
          description: Codex binary missing.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Not authenticated.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /api/auth/me/codex-auth/browser/cancel-login:
    post:
      tags:
        - Auth
      summary: Cancel an in-progress per-user Codex device login.
      responses:
        "200":
          description: Cancellation receipt.
          content:
            application/json:
              schema:
                type: object
                properties:
                  ok:
                    type: boolean
                    enum:
                      - true
                  output:
                    type: string
                required:
                  - ok
                  - output
        "401":
          description: Not authenticated.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /api/auth/me/codex-auth/login:
    post:
      tags:
        - Auth
      summary: Start a per-user Codex ChatGPT device-auth login (CODEX_HOME isolation).
      responses:
        "200":
          description: Device code emitted, or login completed/failed.
          content:
            application/json:
              schema:
                type: object
                properties:
                  ok:
                    type: boolean
                  loginId:
                    type: string
                  deviceAuthUrl:
                    type: string
                  userCode:
                    type: string
                  output:
                    type: string
                required:
                  - ok
        "400":
          description: Codex binary missing.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Not authenticated.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /api/auth/me/codex-auth/login/cancel:
    post:
      tags:
        - Auth
      summary: Cancel the active per-user Codex device-auth login.
      responses:
        "200":
          description: Cancellation receipt.
          content:
            application/json:
              schema:
                type: object
                properties:
                  ok:
                    type: boolean
                    enum:
                      - true
                  output:
                    type: string
                required:
                  - ok
                  - output
        "401":
          description: Not authenticated.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /api/auth/me/grok-auth/browser:
    get:
      tags:
        - Auth
      summary: Per-user Grok "Sign in with browser" status.
      responses:
        "200":
          description: Caller-scoped Grok OAuth state.
          content:
            application/json:
              schema:
                type: object
                properties:
                  uiStatus:
                    type: string
                  binary:
                    type: object
                    properties:
                      present:
                        type: boolean
                      path:
                        type: string
                    required:
                      - present
                      - path
                  oauth:
                    type: object
                    properties:
                      loggedIn:
                        type: boolean
                        nullable: true
                      mode:
                        type: string
                        nullable: true
                      authJsonPath:
                        type: string
                        nullable: true
                    required:
                      - loggedIn
                  loginInProgress:
                    type: boolean
                  activeMethod:
                    type: string
                    enum:
                      - oauth
                      - none
                  statusError:
                    type: string
                    nullable: true
                required:
                  - uiStatus
                  - binary
                  - oauth
                  - loginInProgress
                  - activeMethod
                  - statusError
        "401":
          description: Not authenticated.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
    delete:
      tags:
        - Auth
      summary: Sign the per-user Grok cache out (wipe ~/.grok).
      responses:
        "200":
          description: Cache cleared.
          content:
            application/json:
              schema:
                type: object
                properties:
                  ok:
                    type: boolean
                    enum:
                      - true
                  output:
                    type: string
                required:
                  - ok
                  - output
        "401":
          description: Not authenticated.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /api/auth/me/grok-auth/browser/device-login:
    post:
      tags:
        - Auth
      summary: Start a per-user Grok device-auth (xAI) login.
      responses:
        "200":
          description: Device code emitted or login outcome.
          content:
            application/json:
              schema:
                type: object
                properties:
                  ok:
                    type: boolean
                  loginId:
                    type: string
                  deviceAuthUrl:
                    type: string
                  userCode:
                    type: string
                  output:
                    type: string
                required:
                  - ok
        "400":
          description: Grok binary missing.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Not authenticated.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /api/auth/me/grok-auth/browser/cancel-login:
    post:
      tags:
        - Auth
      summary: Cancel an in-progress per-user Grok device login.
      responses:
        "200":
          description: Cancellation receipt.
          content:
            application/json:
              schema:
                type: object
                properties:
                  ok:
                    type: boolean
                    enum:
                      - true
                  output:
                    type: string
                required:
                  - ok
                  - output
        "401":
          description: Not authenticated.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /api/pr/auto-merge:
    post:
      tags:
        - PR Actions
      summary: Arm or disarm GitHub native auto-merge on a PR
      description: Enables or disables GitHub’s native auto-merge on a GitHub-hosted PR via the acting user’s OAuth token (GraphQL). Agent Hub-hosted (native) PRs are rejected with 400 — their auto-merge is controlled by the session Finalize automation level.
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/PrAutoMergeRequest"
      responses:
        "200":
          description: Auto-merge armed or disarmed.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PrAutoMergeResponse"
        "400":
          description: Missing/invalid `enabled`, invalid merge method, invalid PR URL, or a native (Agent Hub-hosted) PR.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                required:
                  - error
        "412":
          description: 'Caller has not connected a GitHub account (`code: "github_not_connected"`). Not a 401: the caller is authenticated, so clients must surface this rather than treating it as an expired session.'
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                required:
                  - error
        "422":
          description: GitHub rejected the mutation (e.g. repo has "Allow auto-merge" disabled, or the PR is already mergeable with nothing to wait on).
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                required:
                  - error
  /api/projects/{projectId}/pulls:
    get:
      tags:
        - PR Actions
      summary: List a project’s pull requests (paginated)
      description: Returns one page of pull requests for the project, ordered by most recent activity. Agent Hub-hosted projects are served from the native PR table; GitHub-backed projects are proxied through the caller’s connected GitHub account. Page through with `page`; `hasMore` says whether to offer a next page.
      parameters:
        - schema:
            type: string
          required: true
          name: projectId
          in: path
        - schema:
            type: string
            enum:
              - open
              - closed
              - all
            description: Which PRs to include. Defaults to `open`; unknown values fall back to `open`.
          required: false
          description: Which PRs to include. Defaults to `open`; unknown values fall back to `open`.
          name: state
          in: query
        - schema:
            type: integer
            minimum: 1
            maximum: 100
            description: Page size. Defaults to 30, clamped to 100.
          required: false
          description: Page size. Defaults to 30, clamped to 100.
          name: limit
          in: query
        - schema:
            type: integer
            minimum: 1
            description: 1-based page number. Defaults to 1; invalid values fall back to 1.
          required: false
          description: 1-based page number. Defaults to 1; invalid values fall back to 1.
          name: page
          in: query
      responses:
        "200":
          description: One page of pull requests.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PullRequestListResponse"
        "400":
          description: Project has no `githubRepo` configured (GitHub-backed path only).
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  hint:
                    type: string
                required:
                  - error
        "404":
          description: Project not found.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  hint:
                    type: string
                required:
                  - error
        "412":
          description: 'Caller has not connected a GitHub account (`code: "github_not_connected"`).'
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  hint:
                    type: string
                required:
                  - error
        "502":
          description: GitHub rejected or failed the list request.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  hint:
                    type: string
                required:
                  - error
    post:
      tags:
        - Projects
      summary: Create (or reuse) a native pull request on an Agent Hub-hosted project
      description: Creates a DB-backed Hub PR for a branch already pushed to the hosted repo. Idempotent per open head branch. 400 for projects not hosted on Agent Hub — use gh pr create there.
      parameters:
        - schema:
            type: string
          required: true
          name: projectId
          in: path
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                headBranch:
                  type: string
                baseBranch:
                  type: string
                  description: Defaults to the default branch.
                title:
                  type: string
                  minLength: 1
                body:
                  type: string
                sessionId:
                  type: string
                  nullable: true
                  description: Acting session id used to attribute the PR author to the session owner when the caller has no per-user identity (e.g. the global break-glass apiKey). Falls back to the X-Agent-Hub-Session-Id header / spawn-creds key.
              required:
                - headBranch
                - title
      responses:
        "201":
          description: PR created (or reused when one was already open for the branch).
          content:
            application/json:
              schema:
                type: object
                properties:
                  prUrl:
                    type: string
                  number:
                    type: number
                  created:
                    type: boolean
                required:
                  - prUrl
                  - number
                  - created
        "400":
          description: Not Hub-hosted, invalid branch, or missing fields.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                required:
                  - error
        "404":
          description: Unknown project or head branch not pushed.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                required:
                  - error
  /api/projects/{projectId}/previews:
    get:
      summary: List active worktree previews for a project
      tags:
        - Preview
      parameters:
        - schema:
            type: string
            description: Project slug.
          required: true
          description: Project slug.
          name: projectId
          in: path
      responses:
        "200":
          description: Active preview groups (starting, ready, or failed).
          content:
            application/json:
              schema:
                type: object
                properties:
                  projectId:
                    type: string
                  previews:
                    type: array
                    items:
                      $ref: "#/components/schemas/ProjectPreviewInstance"
                required:
                  - projectId
                  - previews
        "404":
          description: Project not found
  /api/projects/{projectId}/previews/purge:
    post:
      summary: Stop all active worktree previews for a project
      tags:
        - Preview
      parameters:
        - schema:
            type: string
            description: Project slug.
          required: true
          description: Project slug.
          name: projectId
          in: path
      responses:
        "200":
          description: Purge result
          content:
            application/json:
              schema:
                type: object
                properties:
                  ok:
                    type: boolean
                    enum:
                      - true
                  stopped:
                    type: integer
                  failed:
                    type: array
                    items:
                      type: object
                      properties:
                        id:
                          type: string
                        error:
                          type: string
                      required:
                        - id
                        - error
                required:
                  - ok
                  - stopped
                  - failed
        "404":
          description: Project not found
  /api/projects/{projectId}/previews/{previewId}/stop:
    post:
      summary: Stop one worktree preview group
      tags:
        - Preview
      parameters:
        - schema:
            type: string
            description: Project slug.
          required: true
          description: Project slug.
          name: projectId
          in: path
        - schema:
            type: string
            description: Worktree preview group id.
          required: true
          description: Worktree preview group id.
          name: previewId
          in: path
      responses:
        "200":
          description: Preview stopped
          content:
            application/json:
              schema:
                type: object
                properties:
                  ok:
                    type: boolean
                    enum:
                      - true
                  stopped:
                    type: boolean
                required:
                  - ok
                  - stopped
        "404":
          description: Project or preview not found
  /api/projects/{projectId}/secrets:
    get:
      tags:
        - Projects
      summary: List per-project secrets
      description: Returns the project's full secret list, sorted by key. `secret`-kind values are masked with the `••••••••` sentinel — this endpoint never returns secret plaintexts. Admin+ role is required (PUT/DELETE/import require Owner).
      parameters:
        - schema:
            type: string
            description: Project slug (e.g. `agent-hub`).
          required: true
          description: Project slug (e.g. `agent-hub`).
          name: projectId
          in: path
      responses:
        "200":
          description: Current secret list (may be empty).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PreviewSecretsListResponse"
        "404":
          description: Project not found, or the caller cannot see it.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PreviewSecretsErrorResponse"
    put:
      tags:
        - Projects
      summary: Bulk-replace per-project secrets
      description: Wholesale replace the project's secret set. Keys absent from the request body are removed; secret-kind entries can carry the MASK sentinel to preserve the existing ciphertext without re-sending the plaintext. `updated_at` is bumped only on rows whose value actually changes.
      parameters:
        - schema:
            type: string
            description: Project slug (e.g. `agent-hub`).
          required: true
          description: Project slug (e.g. `agent-hub`).
          name: projectId
          in: path
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/PreviewSecretsPutRequest"
      responses:
        "200":
          description: Replacement applied. Returns the post-write secret list.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PreviewSecretsListResponse"
        "400":
          description: Validation failed — bad key, reserved-namespace collision, oversize value, duplicate keys in the batch, or MASK sentinel sent for a key that does not yet exist.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PreviewSecretsErrorResponse"
        "404":
          description: Project not found, or the caller cannot see it.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PreviewSecretsErrorResponse"
  /api/projects/{projectId}/secrets/{key}:
    delete:
      tags:
        - Projects
      summary: Delete a single per-project secret
      description: Removes one (project, key) entry without disturbing the rest of the set. Returns 204 on success and appends a `delete` row to the project secret audit log. 404 if the key does not exist — the caller may treat this as idempotent.
      parameters:
        - schema:
            type: string
            description: Project slug (e.g. `agent-hub`).
          required: true
          description: Project slug (e.g. `agent-hub`).
          name: projectId
          in: path
        - schema:
            type: string
            description: Env var name. Must match `/^[A-Za-z_][A-Za-z0-9_]*$/` and cannot be in the reserved namespace (`AGENT_HUB_*`, `NODE_*`, `PATH`, `HOME`).
            example: STRIPE_KEY
          required: true
          description: Env var name. Must match `/^[A-Za-z_][A-Za-z0-9_]*$/` and cannot be in the reserved namespace (`AGENT_HUB_*`, `NODE_*`, `PATH`, `HOME`).
          name: key
          in: path
      responses:
        "204":
          description: Deleted. No response body.
        "400":
          description: Key failed validation (bad identifier or reserved namespace).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PreviewSecretsErrorResponse"
        "404":
          description: Project not found, the caller cannot see it, or the key has no row.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PreviewSecretsErrorResponse"
  /api/projects/{projectId}/secrets/import:
    post:
      tags:
        - Projects
      summary: Import secrets from a `.env`-style blob
      description: Parses the supplied env blob and writes the result either as a wholesale replace (default) or a merge. Reserved keys (`AGENT_HUB_*`, `NODE_*`, `PATH`, `HOME`) are dropped at parse time. Multi-line values are not supported — encode newlines (`\n` escape, base64) before importing.
      parameters:
        - schema:
            type: string
            description: Project slug (e.g. `agent-hub`).
          required: true
          description: Project slug (e.g. `agent-hub`).
          name: projectId
          in: path
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/PreviewSecretsImportRequest"
      responses:
        "200":
          description: Import succeeded. Returns the parsed count and the post-import state.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PreviewSecretsImportResponse"
        "400":
          description: Body missing/invalid, or a parsed key failed validation.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PreviewSecretsErrorResponse"
        "404":
          description: Project not found, or the caller cannot see it.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PreviewSecretsErrorResponse"
  /api/projects/{projectId}/aws-profiles:
    get:
      tags:
        - Projects
      summary: List AWS profiles configured on a project
      description: Returns the project's AWS profile map. SSO token caches are never returned. Static profile access keys are part of this owner-managed project config and must be treated as secret material.
      parameters:
        - schema:
            type: string
            description: Project slug (e.g. `agent-hub`).
          required: true
          description: Project slug (e.g. `agent-hub`).
          name: projectId
          in: path
      responses:
        "200":
          description: Profile map (may be empty).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ProjectAwsProfilesResponse"
        "404":
          description: Project not found, or the caller cannot see it.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ProjectAwsErrorResponse"
    put:
      tags:
        - Projects
      summary: Replace the AWS profiles configured on a project
      description: Wholesale replace the project's AWS profile map. Pass an empty map to clear all profiles. After a successful save the server regenerates the on-disk `AWS_CONFIG_FILE` and `AWS_SHARED_CREDENTIALS_FILE` templates that future spawns reference.
      parameters:
        - schema:
            type: string
            description: Project slug (e.g. `agent-hub`).
          required: true
          description: Project slug (e.g. `agent-hub`).
          name: projectId
          in: path
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ProjectAwsProfilesRequest"
      responses:
        "200":
          description: Profiles saved. Returns the normalized map.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ProjectAwsProfilesResponse"
        "400":
          description: Validation failed — bad profile name, invalid account id / region / URL, duplicate keys, or a `monitoringProfile` naming an SSO profile.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ProjectAwsErrorResponse"
        "404":
          description: Project not found, or the caller cannot see it.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ProjectAwsErrorResponse"
  /api/projects/{projectId}/aws-sso/status:
    get:
      tags:
        - Projects
      summary: Check whether an AWS profile can authenticate
      description: Runs `aws sts get-caller-identity --profile <name>` against the project's rendered AWS config and credentials files. SSO profiles report whether cached SSO tokens can authenticate. Static profiles report whether the stored project credentials can authenticate.
      parameters:
        - schema:
            type: string
            description: Project slug (e.g. `agent-hub`).
          required: true
          description: Project slug (e.g. `agent-hub`).
          name: projectId
          in: path
        - schema:
            type: string
            description: Configured profile name.
          required: true
          description: Configured profile name.
          name: profile
          in: query
      responses:
        "200":
          description: Identity check ran. Inspect `loggedIn` to know whether the token is valid — a 200 is also returned for the "not logged in" case so callers can branch on the body without try/catch.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ProjectAwsSsoStatusResponse"
        "400":
          description: Project has no AWS profiles configured, or the profile name is unknown.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ProjectAwsErrorResponse"
        "404":
          description: Project not found, or the caller cannot see it.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ProjectAwsErrorResponse"
  /api/projects/{projectId}/aws-sso/login:
    post:
      tags:
        - Projects
      summary: Start a browser-less AWS SSO device-authorization flow
      description: |-
        Spawns `aws sso login --profile <name> --no-browser` server-side, watches its output for the IAM Identity Center device URL, and returns the URL to the caller. The user then opens that URL in their own browser to complete the device flow; once they do, the cached SSO token at `~/.aws/sso/cache` becomes valid and subsequent `GET /aws-sso/status` calls report `loggedIn: true`.

        Only one login proc runs per server at a time: a second request kills the previous proc before spawning a new one. The 30-second URL-extraction timeout also kills the proc so no orphaned `aws` processes accumulate.
      parameters:
        - schema:
            type: string
            description: Project slug (e.g. `agent-hub`).
          required: true
          description: Project slug (e.g. `agent-hub`).
          name: projectId
          in: path
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ProjectAwsSsoLoginRequest"
      responses:
        "200":
          description: "Login proc spawned. On the happy path the response carries `loginUrl`; on rare immediate completion it carries `completed: true`."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ProjectAwsSsoLoginResponse"
        "400":
          description: Project has no AWS profiles configured, the profile name is unknown, or the profile is not an SSO profile (static credentials or an assumed role).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ProjectAwsErrorResponse"
        "404":
          description: Project not found, or the caller cannot see it.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ProjectAwsErrorResponse"
        "500":
          description: Login proc failed — exited non-zero, errored on spawn, or timed out without surfacing a device URL.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ProjectAwsSsoLoginResponse"
  /api/projects/{projectId}/email-logo:
    get:
      tags:
        - Projects
      summary: Read the project email logo metadata
      description: Returns the project's per-project release/deployment email logo override metadata, or null when the project uses the global Agent Hub logo.
      parameters:
        - schema:
            type: string
            description: Project slug.
          required: true
          description: Project slug.
          name: projectId
          in: path
      responses:
        "200":
          description: Project email logo metadata.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ProjectEmailLogoResponse"
        "404":
          description: Project not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ProjectBrandingErrorResponse"
    put:
      tags:
        - Projects
      summary: Upload a project email logo
      description: Admin+ only. Stores a per-project override for the branded release/deployment email logo. Replaces any prior logo. The global `emailLogoEnabled` switch still disables branding entirely.
      parameters:
        - schema:
            type: string
            description: Project slug.
          required: true
          description: Project slug.
          name: projectId
          in: path
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                dataUrl:
                  type: string
                  minLength: 1
                  description: "Base64 `data:` URL of the image. Allowed types: image/png, image/jpeg, image/gif, image/webp. Max 2MB."
              required:
                - dataUrl
      responses:
        "200":
          description: Stored project email logo metadata.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ProjectEmailLogoResponse"
        "400":
          description: Malformed data URL, unsupported type, or too large.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ProjectBrandingErrorResponse"
        "403":
          description: Caller is below Admin role.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ProjectBrandingErrorResponse"
        "404":
          description: Project not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ProjectBrandingErrorResponse"
    delete:
      tags:
        - Projects
      summary: Remove the project email logo
      description: Admin+ only. Clears the per-project override so the project falls back to the global Agent Hub email logo.
      parameters:
        - schema:
            type: string
            description: Project slug.
          required: true
          description: Project slug.
          name: projectId
          in: path
      responses:
        "200":
          description: Override removed.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ProjectEmailLogoDeleteResponse"
        "403":
          description: Caller is below Admin role.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ProjectBrandingErrorResponse"
        "404":
          description: Project not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ProjectBrandingErrorResponse"
  /api/projects/{projectId}/email-logo/raw:
    get:
      tags:
        - Projects
      summary: Fetch the project email logo image bytes
      description: Streams the stored project email logo image (for settings preview). 404 when the project has no override.
      parameters:
        - schema:
            type: string
            description: Project slug.
          required: true
          description: Project slug.
          name: projectId
          in: path
      responses:
        "200":
          description: Image bytes.
          content:
            image/*:
              schema:
                type: string
                format: binary
        "404":
          description: No logo set or file missing.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ProjectBrandingErrorResponse"
  /api/projects/{projectId}/release-email-preview:
    get:
      tags:
        - Projects
      summary: Render a branded release/deployment email preview
      description: Returns the exact branded email shell a release/deployment notification uses, with the project logo (or global default, or wordmark fallback) inlined and a representative digest body, so an admin can preview logo + messaging before a real deployment ships.
      parameters:
        - schema:
            type: string
            description: Project slug.
          required: true
          description: Project slug.
          name: projectId
          in: path
      responses:
        "200":
          description: Rendered email preview.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ReleaseEmailPreviewResponse"
        "404":
          description: Project not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ProjectBrandingErrorResponse"
  /api/projects/{projectId}/stats:
    get:
      tags:
        - Projects
      summary: Per-project product metrics over time
      description: Daily / weekly / monthly time series for PRs merged, support tickets resolved, tickets made, tickets completed, epics completed, and model usage. Completion metrics use the completed_at / resolved_at timestamps maintained by DB triggers.
      parameters:
        - schema:
            type: string
          required: true
          name: projectId
          in: path
        - schema:
            type: string
            enum:
              - day
              - week
              - month
          required: false
          name: granularity
          in: query
        - schema:
            type: integer
            nullable: true
          required: false
          name: buckets
          in: query
      responses:
        "200":
          description: Project stats time series.
          content:
            application/json:
              schema:
                type: object
                properties:
                  granularity:
                    type: string
                    enum:
                      - day
                      - week
                      - month
                  buckets:
                    type: array
                    items:
                      type: object
                      properties:
                        start:
                          type: string
                        label:
                          type: string
                      required:
                        - start
                        - label
                  series:
                    type: object
                    properties:
                      prs_merged:
                        type: array
                        items:
                          type: integer
                      support_tickets_resolved:
                        type: array
                        items:
                          type: integer
                      tickets_made:
                        type: array
                        items:
                          type: integer
                      tickets_completed:
                        type: array
                        items:
                          type: integer
                      epics_completed:
                        type: array
                        items:
                          type: integer
                    required:
                      - prs_merged
                      - support_tickets_resolved
                      - tickets_made
                      - tickets_completed
                      - epics_completed
                  totals:
                    type: object
                    properties:
                      prs_merged:
                        type: integer
                      support_tickets_resolved:
                        type: integer
                      tickets_made:
                        type: integer
                      tickets_completed:
                        type: integer
                      epics_completed:
                        type: integer
                    required:
                      - prs_merged
                      - support_tickets_resolved
                      - tickets_made
                      - tickets_completed
                      - epics_completed
                  model_usage:
                    type: array
                    items:
                      type: object
                      properties:
                        model:
                          type: string
                        count:
                          type: integer
                      required:
                        - model
                        - count
                  top_model:
                    type: string
                    nullable: true
                required:
                  - granularity
                  - buckets
                  - series
                  - totals
                  - model_usage
                  - top_model
        "404":
          description: Unknown project.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                required:
                  - error
        "500":
          description: Stats could not be read.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                required:
                  - error
  /api/projects/order:
    put:
      tags:
        - Projects
      summary: Reorder the caller-visible project list
      description: Reorders `projects.json` so the caller-visible slice matches the supplied id list. Projects the caller cannot see (e.g. private projects owned by other users) keep their absolute slot — only the visible subset is permuted. Broadcasts `projects_updated` over the WebSocket on success so other open clients refresh their sidebars.
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                projectIds:
                  type: array
                  items:
                    type: string
                    minLength: 1
                  description: Permutation of the caller-visible project ids. Must cover the same set GET /api/projects returns for this user — no missing ids, no extras, no duplicates.
              required:
                - projectIds
              description: Body for PUT /api/projects/order — the new project order.
      responses:
        "200":
          description: Reorder accepted; returns the new caller-visible order.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ProjectOrderResponse"
        "400":
          description: Validation failed — body shape was wrong, an id was unknown or inaccessible, an id was missing, or duplicates were supplied.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ProjectOrderErrorResponse"
  /api/projects/{projectId}:
    get:
      tags:
        - Projects
      summary: Get a single project by slug
      description: "Returns the full project record from `projects.json`. The response is a superset of the `Project` row shape — extra fields flow through via `passthrough()` while the schema-first migration is in progress. Visibility-filtered: private projects only resolve for their owner (and the local-bypass / Owner-via-admin paths); other callers see a 404."
      parameters:
        - schema:
            type: string
            description: Project slug (e.g. `agent-hub`).
          required: true
          description: Project slug (e.g. `agent-hub`).
          name: projectId
          in: path
      responses:
        "200":
          description: Project detail.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ProjectDetail"
        "404":
          description: No project with this slug, or the caller cannot see it.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ProjectDetailErrorResponse"
  /api/projects/analyze:
    post:
      tags:
        - Projects
      summary: Analyze a local or cloned project directory
      description: Starts an asynchronous project-analysis run for the Open Project wizard. The response returns an `analyzeId`; progress and completion arrive over WebSocket events. Callers may select the engine/model from `/api/config/models` before starting analysis.
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                cwd:
                  type: string
                  minLength: 1
                  description: Directory to analyze. `~` is expanded against the server process HOME.
                engine:
                  type: string
                  enum:
                    - claude-code
                    - cursor-agent
                    - codex-cli
                  description: Optional selected analysis engine. When supplied, analysis uses this engine only and returns a targeted setup error if it cannot run.
                model:
                  type: string
                  minLength: 1
                  description: Optional selected model. Must belong to the selected engine, or to one of the supported project-analysis engines when engine is omitted.
              required:
                - cwd
              description: Body for POST /api/projects/analyze.
      responses:
        "200":
          description: Analysis was accepted and will stream progress over WebSocket.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ProjectAnalyzeResponse"
        "400":
          description: Invalid cwd, selected engine/model, or no usable selected engine.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ProjectAnalyzeErrorResponse"
        "500":
          description: Unexpected engine-selection failure.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ProjectAnalyzeErrorResponse"
  /api/projects/onboard:
    post:
      tags:
        - Projects
      summary: Create a project from reviewed analysis output
      description: Creates a project from the Open Project wizard review step. Persists the selected dev agent roster, context files, detected commands, optional GitHub link, and reviewed starter wiki pages.
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                project:
                  type: object
                  properties:
                    id:
                      type: string
                      minLength: 1
                      description: New project slug.
                    name:
                      type: string
                      description: Human-readable project name.
                    cwd:
                      type: string
                      description: Project working directory.
                    color:
                      type: string
                      description: Sidebar accent color.
                    githubRepo:
                      type: object
                      properties:
                        owner:
                          type: string
                        repo:
                          type: string
                      required:
                        - owner
                        - repo
                      description: GitHub repository selected during onboarding.
                    preCommitCommands:
                      nullable: true
                    sessionStartupCommands:
                      nullable: true
                    checkHealCommands:
                      nullable: true
                    checkHealMaxRounds:
                      nullable: true
                  required:
                    - id
                  additionalProperties:
                    nullable: true
                agents:
                  type: array
                  items:
                    type: object
                    properties:
                      id:
                        type: string
                      name:
                        type: string
                      role:
                        type: string
                      engine:
                        type: string
                      systemPrompt:
                        type: string
                      color:
                        type: string
                      identity:
                        type: string
                    required:
                      - id
                    additionalProperties:
                      nullable: true
                  description: Reviewed dev agent roster generated by analysis.
                contextFiles:
                  type: object
                  additionalProperties:
                    type: string
                  description: Reviewed workspace context files written into the project data directory.
                commands:
                  type: object
                  nullable: true
                  properties:
                    install:
                      type: string
                      nullable: true
                    build:
                      type: string
                      nullable: true
                    test:
                      type: string
                      nullable: true
                    lint:
                      type: string
                      nullable: true
                  description: Detected project commands.
                wikiPages:
                  type: array
                  items:
                    type: object
                    properties:
                      title:
                        type: string
                        minLength: 1
                        maxLength: 160
                        description: Starter wiki page title reviewed by the user in the Open Project wizard.
                      content:
                        type: string
                        description: Markdown body for the starter wiki page.
                      category:
                        type: string
                        enum:
                          - general
                          - api-docs
                          - architecture
                          - conventions
                          - test-patterns
                          - troubleshooting
                          - onboarding
                        description: Wiki category for the starter page.
                    required:
                      - title
                    description: Generated wiki page draft from project analysis, optionally edited by the user before onboarding.
                  description: Reviewed starter wiki pages generated during project analysis. Empty or omitted means no wiki pages are seeded.
              required:
                - project
              description: Body for POST /api/projects/onboard.
      responses:
        "201":
          description: Project created.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ProjectDetail"
        "400":
          description: Invalid project id, duplicate-free roster validation failure, or invalid options.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ProjectOnboardErrorResponse"
        "409":
          description: A project with the requested id already exists.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ProjectOnboardErrorResponse"
        "500":
          description: Project setup failed after validation.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ProjectOnboardErrorResponse"
  /api/projects/{projectId}/user-settings:
    get:
      tags:
        - Projects
      summary: Read the caller's per-project settings
      description: Returns the authenticated caller's per-project settings — currently their default Finalize automation level for new ad-hoc sessions. Scoped to the requesting user; never exposes another user's preference.
      parameters:
        - schema:
            type: string
            description: Project slug (e.g. `agent-hub`).
          required: true
          description: Project slug (e.g. `agent-hub`).
          name: projectId
          in: path
      responses:
        "200":
          description: Per-user project settings.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UserProjectSettings"
        "404":
          description: No project with this slug, or the caller cannot see it.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UserProjectSettingsErrorResponse"
    put:
      tags:
        - Projects
      summary: Update the caller's per-project settings
      description: Upserts the authenticated caller's per-project settings. Send `defaultFinalizeAutomation` as a known level to set it, `null` to clear it, or omit the key to leave it unchanged. Returns the resulting settings.
      parameters:
        - schema:
            type: string
            description: Project slug (e.g. `agent-hub`).
          required: true
          description: Project slug (e.g. `agent-hub`).
          name: projectId
          in: path
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                defaultFinalizeAutomation:
                  type: string
                  nullable: true
                  enum:
                    - manual
                    - review
                    - push
                    - merge
                    - null
                  description: The default Finalize automation level to store for the requesting user in this project. A known level sets it; `null` clears it; omitting the key leaves the existing value unchanged.
              description: Body for PUT /api/projects/{projectId}/user-settings.
      responses:
        "200":
          description: Updated per-user project settings.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UserProjectSettings"
        "400":
          description: Malformed body.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UserProjectSettingsErrorResponse"
        "404":
          description: No project with this slug, or the caller cannot see it.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UserProjectSettingsErrorResponse"
  /api/setup/status:
    get:
      tags:
        - Projects
      summary: Boot-time setup + engine availability status
      description: Reports first-run / auth-configured / onboarding-complete state, the requesting user’s per-engine credential status, and each engine binary’s availability and resolved path. Probes `<bin> --version` in parallel with a 5s timeout per engine. Drives the client’s setup-wizard gating on app load.
      responses:
        "200":
          description: Setup + engine status.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SetupStatusResponse"
  /api/setup/complete:
    post:
      tags:
        - Projects
      summary: Mark interactive first-run SetupWizard complete
      description: "Persists `onboardingComplete: true` in config.json so a later reload does not re-open the SetupWizard. Called when the client finishes the wizard (Open Project). Distinct from Owner creation via POST /api/auth/setup. Requires the Owner role — the flag is global instance state, so a non-Owner must not be able to mark another operator’s interrupted first-run as finished."
      responses:
        "200":
          description: Onboarding marked complete.
          content:
            application/json:
              schema:
                type: object
                properties:
                  ok:
                    type: boolean
                    enum:
                      - true
                  onboardingComplete:
                    type: boolean
                    enum:
                      - true
                required:
                  - ok
                  - onboardingComplete
                description: Confirmation that onboarding is complete.
        "403":
          description: Caller is authenticated but not an Owner.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                required:
                  - error
        "500":
          description: Failed to persist the flag.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                required:
                  - error
  /api/projects/{projectId}/members:
    get:
      tags:
        - Projects
      summary: List the users assigned to a project
      description: "Returns the project’s visibility ACL. Owner-only (org Owner role, or the local-bundle / global-apiKey break-glass). `restricted: false` means no assignment ACL: shared projects are org-visible and private projects are owner-only. `restricted: true` means assignment-gated even if no users are currently assigned."
      parameters:
        - schema:
            type: string
          required: true
          name: projectId
          in: path
      responses:
        "200":
          description: The project member list.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ProjectMembersList"
        "403":
          description: Caller is not an Owner.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ProjectMemberErrorResponse"
        "404":
          description: Project not found (or not visible to the caller).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ProjectMemberErrorResponse"
    post:
      tags:
        - Projects
      summary: Assign a user to a project
      description: Adds a user to the project’s visibility ACL so they can see and enter it. Owner-only. Assigning the first member flips the project from org-visible to members-only. Idempotent — re-assigning an existing member is a no-op. Broadcasts `projects_updated` over the WebSocket.
      parameters:
        - schema:
            type: string
          required: true
          name: projectId
          in: path
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                userId:
                  type: string
                  description: Id of the org user to assign.
              required:
                - userId
      responses:
        "200":
          description: User was already assigned; no new row was created.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ProjectMemberAdded"
        "201":
          description: User assigned.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ProjectMemberAdded"
        "400":
          description: Missing userId, or the user is not a member of this org.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ProjectMemberErrorResponse"
        "403":
          description: Caller is not an Owner.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ProjectMemberErrorResponse"
        "404":
          description: Project or user not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ProjectMemberErrorResponse"
  /api/projects/{projectId}/members/{userId}:
    delete:
      tags:
        - Projects
      summary: Unassign a user from a project
      description: Removes a user from the project’s visibility ACL. Owner-only. Removing the last member keeps the assignment ACL active, so a restricted shared project does not reopen to the whole org. Broadcasts `projects_updated`.
      parameters:
        - schema:
            type: string
          required: true
          name: projectId
          in: path
        - schema:
            type: string
          required: true
          name: userId
          in: path
      responses:
        "200":
          description: User unassigned.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ProjectMemberRemoved"
        "403":
          description: Caller is not an Owner.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ProjectMemberErrorResponse"
        "404":
          description: Project not found, or the user was not a member.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ProjectMemberErrorResponse"
  /api/projects/provision/suggest:
    post:
      tags:
        - Projects
      summary: AI-suggest a project name from a description
      description: Fills a blank project name using the requesting user's connected Claude account. Stack and app type are chosen later by the first build session.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                description:
                  type: string
                  minLength: 1
                appType:
                  type: string
                stack:
                  type: string
                model:
                  type: string
                  description: Claude model id override.
              required:
                - description
      responses:
        "200":
          description: Suggested values (null when the model produced none for a field).
          content:
            application/json:
              schema:
                type: object
                properties:
                  name:
                    type: string
                    nullable: true
                  appType:
                    type: string
                    nullable: true
                  stack:
                    type: string
                    nullable: true
                required:
                  - name
                  - appType
                  - stack
        "400":
          description: Missing description or no connected Claude account.
        "502":
          description: Model invocation failed.
  /api/projects/{projectId}/pulls/branch-changes:
    post:
      tags:
        - Projects
      summary: Preview branch changes for a native pull request
      description: Returns file-level changes for a branch pushed to an Agent Hub-hosted repo before creating a native PR. Base defaults to the hosted repo default branch.
      parameters:
        - schema:
            type: string
          required: true
          name: projectId
          in: path
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                headBranch:
                  type: string
                baseBranch:
                  type: string
              required:
                - headBranch
      responses:
        "200":
          description: Branch file changes.
          content:
            application/json:
              schema:
                type: object
                properties:
                  headBranch:
                    type: string
                  baseBranch:
                    type: string
                  stats:
                    type: object
                    properties:
                      changedFiles:
                        type: number
                      additions:
                        type: number
                      deletions:
                        type: number
                    required:
                      - changedFiles
                      - additions
                      - deletions
                  files:
                    type: array
                    items:
                      type: object
                      properties:
                        filename:
                          type: string
                        status:
                          type: string
                          enum:
                            - added
                            - removed
                            - modified
                            - renamed
                        additions:
                          type: number
                        deletions:
                          type: number
                      required:
                        - filename
                        - status
                        - additions
                        - deletions
                  truncated:
                    type: boolean
                required:
                  - headBranch
                  - baseBranch
                  - stats
                  - files
                  - truncated
        "400":
          description: Invalid branch or not Hub-hosted.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                required:
                  - error
        "404":
          description: Unknown project or branch.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                required:
                  - error
        "502":
          description: Git diff failed.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                required:
                  - error
  /api/projects/{projectId}/pulls/{number}:
    patch:
      tags:
        - Projects
      summary: Edit the title/body of an open native pull request
      description: Agent Hub-hosted projects only. Closed/merged PRs are immutable (409). At least one of title/body must be supplied.
      parameters:
        - schema:
            type: string
          required: true
          name: projectId
          in: path
        - schema:
            type: string
          required: true
          name: number
          in: path
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                title:
                  type: string
                  minLength: 1
                body:
                  type: string
      responses:
        "200":
          description: Updated PR summary (same shape as the list rows).
          content:
            application/json:
              schema:
                type: object
                properties:
                  pr:
                    type: object
                    additionalProperties:
                      nullable: true
                required:
                  - pr
        "400":
          description: Invalid input or not Hub-hosted.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                required:
                  - error
        "404":
          description: Unknown project or PR.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                required:
                  - error
        "409":
          description: PR is not open.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                required:
                  - error
  /api/projects/{projectId}/pulls/{number}/reopen:
    post:
      tags:
        - Projects
      summary: Reopen a closed native pull request
      description: Closed → open. Merged PRs are immutable (409). Agent Hub-hosted projects only.
      parameters:
        - schema:
            type: string
          required: true
          name: projectId
          in: path
        - schema:
            type: string
          required: true
          name: number
          in: path
      responses:
        "200":
          description: Reopened PR summary.
          content:
            application/json:
              schema:
                type: object
                properties:
                  pr:
                    type: object
                    additionalProperties:
                      nullable: true
                required:
                  - pr
        "400":
          description: Not Hub-hosted.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                required:
                  - error
        "404":
          description: Unknown project/PR.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                required:
                  - error
        "409":
          description: PR is merged or already open.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                required:
                  - error
  /api/projects/{projectId}/pulls/{number}/auto-merge:
    post:
      tags:
        - Projects
      summary: Arm or disarm auto-merge on a native pull request
      description: "Arms (or disarms) auto-merge on an open Agent Hub-hosted PR. An armed PR merges once its head checks pass and it is otherwise mergeable; if it is already green and mergeable, it merges immediately (response `merged: true`). Same behaviour as `git push -o automerge`. Agent Hub-hosted projects only; merged/closed PRs are rejected (409)."
      parameters:
        - schema:
            type: string
          required: true
          name: projectId
          in: path
        - schema:
            type: string
          required: true
          name: number
          in: path
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                enabled:
                  type: boolean
              required:
                - enabled
      responses:
        "200":
          description: The updated PR summary plus whether an immediate merge fired.
          content:
            application/json:
              schema:
                type: object
                properties:
                  pr:
                    type: object
                    additionalProperties:
                      nullable: true
                  merged:
                    type: boolean
                required:
                  - pr
                  - merged
        "400":
          description: Not Hub-hosted or invalid body.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                required:
                  - error
        "404":
          description: Unknown project/PR.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                required:
                  - error
        "409":
          description: PR is merged or closed.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                required:
                  - error
  /api/projects/{projectId}/pulls/{number}/revert:
    post:
      tags:
        - Projects
      summary: Revert a merged native pull request
      description: Commits the inverse of the PR's merge commit on its base branch and pushes the moved branch to the GitHub mirror, so the change is undone on both. History is not rewritten — a revert commit is added, matching `git revert` and GitHub's Revert button. Merged PRs only, once per PR.
      parameters:
        - schema:
            type: string
          required: true
          name: projectId
          in: path
        - schema:
            type: string
          required: true
          name: number
          in: path
      responses:
        "200":
          description: The revert commit sha and the updated PR summary.
          content:
            application/json:
              schema:
                type: object
                properties:
                  revertSha:
                    type: string
                  pr:
                    type: object
                    additionalProperties:
                      nullable: true
                required:
                  - revertSha
                  - pr
        "400":
          description: Not Hub-hosted.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                required:
                  - error
        "404":
          description: Unknown project/PR, or the merge commit is gone from the repo.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                required:
                  - error
        "409":
          description: PR is not merged, was already reverted, is no longer on the base branch, or the revert conflicts with later commits.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                required:
                  - error
        "503":
          description: The base branch kept moving; retry.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                required:
                  - error
  /api/projects/{projectId}/pulls/{number}/preview/start:
    post:
      tags:
        - Projects
      summary: Launch a live preview for a native pull request
      description: Boots a worktree preview for the session that owns the PR's head branch and returns once the start was accepted (the preview then transitions loading → ready/failed asynchronously; poll the preview state route or the `agenthub_preview` WebSocket channel). Agent Hub-hosted PRs only. 409 when no live session worktree backs the PR's head branch.
      parameters:
        - schema:
            type: string
          required: true
          name: projectId
          in: path
        - schema:
            type: string
          required: true
          name: number
          in: path
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                route:
                  type: string
                reason:
                  type: string
      responses:
        "200":
          description: Preview start accepted.
          content:
            application/json:
              schema:
                type: object
                properties:
                  ok:
                    type: boolean
                    enum:
                      - true
                  started:
                    type: boolean
                    enum:
                      - true
                  sessionId:
                    type: string
                required:
                  - ok
                  - started
                  - sessionId
        "400":
          description: Not Hub-hosted or invalid PR number.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                required:
                  - error
        "404":
          description: Unknown project/PR/session/agent.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                required:
                  - error
        "409":
          description: The PR is not open (previews only start for open PRs), no live session worktree backs the PR, or the worktree is not ready.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                required:
                  - error
        "501":
          description: Preview routing is not available on this deployment.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                required:
                  - error
  /api/projects/{projectId}/pulls/{number}/preview/stop:
    post:
      tags:
        - Projects
      summary: Tear down a native pull request preview
      description: "Stops the worktree preview for the session behind the PR's head branch (SIGTERM to the process group, port release). Idempotent — returns `stopped: 0` when nothing was running. Also fired automatically when the PR merges."
      parameters:
        - schema:
            type: string
          required: true
          name: projectId
          in: path
        - schema:
            type: string
          required: true
          name: number
          in: path
      responses:
        "200":
          description: Preview stop processed.
          content:
            application/json:
              schema:
                type: object
                properties:
                  ok:
                    type: boolean
                    enum:
                      - true
                  stopped:
                    type: number
                  sessionId:
                    type: string
                required:
                  - ok
                  - stopped
                  - sessionId
        "400":
          description: Not Hub-hosted or invalid PR number.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                required:
                  - error
        "404":
          description: Unknown project/PR.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                required:
                  - error
        "409":
          description: No live session worktree backs the PR.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                required:
                  - error
  /api/projects/{projectId}/pulls/{number}/preview/state:
    get:
      tags:
        - Projects
      summary: Read the current preview state for a native pull request
      description: "Returns the `agenthub_preview` snapshot event for the session behind the PR's head branch (status loading/ready/failed, url, port, log tail), or `preview: null` when no preview is active or no session backs the branch."
      parameters:
        - schema:
            type: string
          required: true
          name: projectId
          in: path
        - schema:
            type: string
          required: true
          name: number
          in: path
      responses:
        "200":
          description: Preview snapshot (or null).
          content:
            application/json:
              schema:
                type: object
                properties:
                  sessionId:
                    type: string
                    nullable: true
                  preview:
                    type: object
                    nullable: true
                    additionalProperties:
                      nullable: true
                required:
                  - sessionId
                  - preview
        "400":
          description: Not Hub-hosted or invalid PR number.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                required:
                  - error
        "404":
          description: Unknown project/PR.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                required:
                  - error
  /api/projects/{projectId}/pulls/{number}/request-review:
    post:
      tags:
        - Projects
      summary: Request a review on a native pull request (human flag and/or agent)
      description: "With requested=true: kind='human' flips the human-review flag only; kind='agent' dispatches the project Reviewer agent only, leaving the flag untouched; kind='both' (the default when omitted) does both, preserving legacy behavior. requested=false always clears the human-review flag and never dispatches, regardless of kind."
      parameters:
        - schema:
            type: string
          required: true
          name: projectId
          in: path
        - schema:
            type: string
          required: true
          name: number
          in: path
      requestBody:
        required: false
        content:
          application/json:
            schema:
              type: object
              properties:
                requested:
                  type: boolean
                kind:
                  type: string
                  enum:
                    - agent
                    - human
                    - both
      responses:
        "200":
          description: Updated PR summary. agent_review_dispatched is true only when a Reviewer agent session was actually dispatched (present only for agent/both requests).
          content:
            application/json:
              schema:
                type: object
                properties:
                  pr:
                    type: object
                    additionalProperties:
                      nullable: true
                  agent_review_dispatched:
                    type: boolean
                  agent_review_reason:
                    type: string
                    description: Why an agent dispatch did not occur, e.g. 'already_in_flight', 'no_reviewer', 'engine_unavailable'.
                required:
                  - pr
        "400":
          description: Not Hub-hosted, or kind is not one of 'agent' | 'human' | 'both'.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                required:
                  - error
        "404":
          description: Unknown project/PR.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                required:
                  - error
        "409":
          description: PR is not open.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                required:
                  - error
  /api/projects/{projectId}/pulls/{number}/reviews:
    post:
      tags:
        - Projects
      summary: Submit a human review on a native pull request
      description: "state: approved | changes_requested | commented. A verdict (approve/request changes) clears any outstanding review-request flag. Reviews render in the PR detail and feed the Autofix context."
      parameters:
        - schema:
            type: string
          required: true
          name: projectId
          in: path
        - schema:
            type: string
          required: true
          name: number
          in: path
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                state:
                  type: string
                  enum:
                    - approved
                    - changes_requested
                    - commented
                body:
                  type: string
                reviewer:
                  type: string
                  description: Reviewer-name override (agent reviews).
                session_id:
                  type: string
                  nullable: true
                  description: Acting session id (also accepted via the X-Agent-Hub-Session-Id header). A verdict clears the in-flight agent-review claim only when this matches the session that owns it.
              required:
                - state
      responses:
        "201":
          description: The recorded review (GitHub review-object shape).
          content:
            application/json:
              schema:
                type: object
                properties:
                  review:
                    type: object
                    additionalProperties:
                      nullable: true
                required:
                  - review
        "400":
          description: Invalid state or not Hub-hosted.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                required:
                  - error
        "404":
          description: Unknown project/PR.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                required:
                  - error
        "409":
          description: PR is not open.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                required:
                  - error
  /api/projects/{projectId}/pulls/{number}/reviews/{reviewId}/dismiss:
    post:
      tags:
        - Projects
      summary: Dismiss a submitted review on a native pull request
      description: 'GitHub-style "Dismiss review": the review row is kept for history but its verdict stops counting toward the review decision and renders collapsed with the dismissal note. Only approved / changes_requested reviews can be dismissed (a comment review has no verdict). A reason is required. Allowed on closed and merged PRs.'
      parameters:
        - schema:
            type: string
          required: true
          name: projectId
          in: path
        - schema:
            type: string
          required: true
          name: number
          in: path
        - schema:
            type: string
          required: true
          name: reviewId
          in: path
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                reason:
                  type: string
                  minLength: 1
                  description: Why the review is being dismissed.
              required:
                - reason
      responses:
        "200":
          description: The dismissed review (GitHub review-object shape).
          content:
            application/json:
              schema:
                type: object
                properties:
                  review:
                    type: object
                    additionalProperties:
                      nullable: true
                required:
                  - review
        "400":
          description: Missing reason, comment review, or not Hub-hosted.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                required:
                  - error
        "404":
          description: Unknown project/PR/review.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                required:
                  - error
        "409":
          description: Review is already dismissed.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                required:
                  - error
  /api/projects/{projectId}/pulls/{number}/comments:
    post:
      tags:
        - Projects
      summary: Add an inline (per-line) diff comment to a native pull request
      description: Anchored to a file + line in the PR diff. side 'new' = post-image line number (additions/context), 'old' = pre-image (deletions). Rendered inside the Files-changed diff and included in the Autofix context.
      parameters:
        - schema:
            type: string
          required: true
          name: projectId
          in: path
        - schema:
            type: string
          required: true
          name: number
          in: path
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                filePath:
                  type: string
                  minLength: 1
                line:
                  type: integer
                  minimum: 1
                side:
                  type: string
                  enum:
                    - old
                    - new
                body:
                  type: string
                  minLength: 1
              required:
                - filePath
                - line
                - body
      responses:
        "201":
          description: The recorded comment.
          content:
            application/json:
              schema:
                type: object
                properties:
                  comment:
                    type: object
                    additionalProperties:
                      nullable: true
                required:
                  - comment
        "400":
          description: Invalid input or not Hub-hosted.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                required:
                  - error
        "404":
          description: Unknown project/PR.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                required:
                  - error
        "409":
          description: PR is not open.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                required:
                  - error
  /api/projects/{projectId}/pulls/{number}/comment-threads/resolve:
    post:
      tags:
        - Projects
      summary: Resolve or unresolve an inline comment thread on a native pull request
      description: A thread is the set of inline comments sharing an anchor (filePath + line + side); resolution is stored against that anchor, so a later reply joins an already-resolved thread. Resolved threads render collapsed in the Files-changed diff. Allowed on closed and merged PRs.
      parameters:
        - schema:
            type: string
          required: true
          name: projectId
          in: path
        - schema:
            type: string
          required: true
          name: number
          in: path
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                filePath:
                  type: string
                  minLength: 1
                line:
                  type: integer
                  minimum: 1
                side:
                  type: string
                  enum:
                    - old
                    - new
                resolved:
                  type: boolean
              required:
                - filePath
                - line
                - resolved
      responses:
        "200":
          description: The thread anchor and its new resolution state.
          content:
            application/json:
              schema:
                type: object
                properties:
                  thread:
                    type: object
                    additionalProperties:
                      nullable: true
                required:
                  - thread
        "400":
          description: Invalid input or not Hub-hosted.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                required:
                  - error
        "404":
          description: Unknown project/PR, or no comment at that anchor.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                required:
                  - error
  /api/projects/{projectId}/pulls/{number}/comments/{commentId}:
    delete:
      tags:
        - Projects
      summary: Delete an inline diff comment from a native pull request
      parameters:
        - schema:
            type: string
          required: true
          name: projectId
          in: path
        - schema:
            type: string
          required: true
          name: number
          in: path
        - schema:
            type: string
          required: true
          name: commentId
          in: path
      responses:
        "200":
          description: Deleted.
          content:
            application/json:
              schema:
                type: object
                properties:
                  ok:
                    type: boolean
                required:
                  - ok
        "400":
          description: Not Hub-hosted.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                required:
                  - error
        "404":
          description: Unknown project/PR/comment.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                required:
                  - error
  /api/projects/{projectId}/pulls/generate-description:
    post:
      tags:
        - Projects
      summary: Generate a PR title and description from a branch diff (AI)
      description: Runs the configured model over the branch diff + commit subjects and returns a suggested title and body. Used by the optional "Generate" button on the PR creation form — fields stay editable.
      parameters:
        - schema:
            type: string
          required: true
          name: projectId
          in: path
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                headBranch:
                  type: string
                baseBranch:
                  type: string
              required:
                - headBranch
      responses:
        "200":
          description: Suggested PR text.
          content:
            application/json:
              schema:
                type: object
                properties:
                  title:
                    type: string
                  body:
                    type: string
                required:
                  - title
                  - body
        "400":
          description: Invalid branch or not Hub-hosted.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                required:
                  - error
        "404":
          description: Unknown project or branch.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                required:
                  - error
        "502":
          description: Model invocation failed.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                required:
                  - error
  /api/projects/{projectId}/release-notification-settings:
    get:
      tags:
        - Deployments
      summary: Read release notification settings
      description: Returns per-project release notification settings, including the editable release digest guidance prompt and the fixed fact-bounded generation template. Admin+ callers also receive release digest recipients; non-admin callers never receive recipient data.
      parameters:
        - schema:
            type: string
            description: Project slug.
          required: true
          description: Project slug.
          name: projectId
          in: path
      responses:
        "200":
          description: Release notification settings.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ReleaseNotificationSettings"
        "404":
          description: Project not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ReleaseNotificationSettingsErrorResponse"
    put:
      tags:
        - Deployments
      summary: Update release notification settings
      description: Admin+ only. Stores the operator guidance prompt used by release digest generation. The prompt is always applied inside the server-owned fact-bounded template.
      parameters:
        - schema:
            type: string
            description: Project slug.
          required: true
          description: Project slug.
          name: projectId
          in: path
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                releaseDigestPrompt:
                  type: string
                  minLength: 1
                  maxLength: 4000
              required:
                - releaseDigestPrompt
      responses:
        "200":
          description: Updated release notification settings.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ReleaseNotificationSettings"
        "400":
          description: Malformed or invalid prompt.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ReleaseNotificationSettingsErrorResponse"
        "403":
          description: Caller is below Admin role.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ReleaseNotificationSettingsErrorResponse"
        "404":
          description: Project not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ReleaseNotificationSettingsErrorResponse"
  /api/projects/{projectId}/release-notification-settings/recipients:
    get:
      tags:
        - Deployments
      summary: List release digest recipients
      description: Admin+ only. Lists project-scoped release digest recipients, including disabled rows for audit and re-enable workflows.
      parameters:
        - schema:
            type: string
            description: Project slug.
          required: true
          description: Project slug.
          name: projectId
          in: path
      responses:
        "200":
          description: Release digest recipients.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ReleaseDigestRecipientsResponse"
        "403":
          description: Caller is below Admin role.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ReleaseNotificationSettingsErrorResponse"
        "404":
          description: Project not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ReleaseNotificationSettingsErrorResponse"
    post:
      tags:
        - Deployments
      summary: Add a release digest recipient
      description: Admin+ only. Adds a project-scoped release digest recipient. Email uniqueness is enforced case-insensitively per project.
      parameters:
        - schema:
            type: string
            description: Project slug.
          required: true
          description: Project slug.
          name: projectId
          in: path
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                email:
                  type: string
                  maxLength: 254
                  format: email
                displayLabel:
                  type: string
                  nullable: true
                  maxLength: 120
                enabled:
                  type: boolean
              required:
                - email
      responses:
        "201":
          description: Created release digest recipient.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ReleaseDigestRecipient"
        "400":
          description: Malformed or invalid recipient.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ReleaseNotificationSettingsErrorResponse"
        "403":
          description: Caller is below Admin role.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ReleaseNotificationSettingsErrorResponse"
        "404":
          description: Project not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ReleaseNotificationSettingsErrorResponse"
        "409":
          description: Recipient email already exists for this project.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ReleaseNotificationSettingsErrorResponse"
  /api/projects/{projectId}/release-notification-settings/recipients/{recipientId}:
    patch:
      tags:
        - Deployments
      summary: Update a release digest recipient
      description: Admin+ only. Updates a recipient display label and enabled state. Email addresses are immutable; remove and add a recipient to change the address.
      parameters:
        - schema:
            type: string
            description: Project slug.
          required: true
          description: Project slug.
          name: projectId
          in: path
        - schema:
            type: string
            description: Release digest recipient id.
          required: true
          description: Release digest recipient id.
          name: recipientId
          in: path
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                displayLabel:
                  type: string
                  nullable: true
                  maxLength: 120
                enabled:
                  type: boolean
      responses:
        "200":
          description: Updated release digest recipient.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ReleaseDigestRecipient"
        "400":
          description: Malformed or invalid patch.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ReleaseNotificationSettingsErrorResponse"
        "403":
          description: Caller is below Admin role.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ReleaseNotificationSettingsErrorResponse"
        "404":
          description: Project or recipient not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ReleaseNotificationSettingsErrorResponse"
    delete:
      tags:
        - Deployments
      summary: Remove a release digest recipient
      description: Admin+ only. Removes a recipient from the project release digest list.
      parameters:
        - schema:
            type: string
            description: Project slug.
          required: true
          description: Project slug.
          name: projectId
          in: path
        - schema:
            type: string
            description: Release digest recipient id.
          required: true
          description: Release digest recipient id.
          name: recipientId
          in: path
      responses:
        "200":
          description: Recipient removed.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ReleaseDigestRecipientDeleteResponse"
        "403":
          description: Caller is below Admin role.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ReleaseNotificationSettingsErrorResponse"
        "404":
          description: Project or recipient not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ReleaseNotificationSettingsErrorResponse"
  /api/projects/{projectId}/release-notification-settings/reset:
    post:
      tags:
        - Deployments
      summary: Reset release notification settings
      description: Admin+ only. Clears the stored release digest prompt so the project uses the safe server default.
      parameters:
        - schema:
            type: string
            description: Project slug.
          required: true
          description: Project slug.
          name: projectId
          in: path
      responses:
        "200":
          description: Settings reset to the default prompt.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ReleaseNotificationSettings"
        "403":
          description: Caller is below Admin role.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ReleaseNotificationSettingsErrorResponse"
        "404":
          description: Project not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ReleaseNotificationSettingsErrorResponse"
  /api/releases:
    get:
      tags:
        - Health
      summary: List Agent Hub releases with user-facing summaries
      description: Fetches GitHub releases for the configured repo (default Speakman-ai/agent-hub), falling back to local git tags when the API is unavailable.
      parameters:
        - schema:
            type: string
            description: Prefer this version in `selected`.
          required: false
          description: Prefer this version in `selected`.
          name: version
          in: query
        - schema:
            type: integer
            minimum: 1
            maximum: 100
          required: false
          name: limit
          in: query
        - schema:
            type: string
            enum:
              - "1"
              - "true"
            description: Bypass the in-memory cache.
          required: false
          description: Bypass the in-memory cache.
          name: refresh
          in: query
      responses:
        "200":
          description: Release list.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ReleasesListResponse"
        "500":
          description: Upstream or parsing failure.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                required:
                  - error
  /api/releases/{version}:
    get:
      tags:
        - Health
      summary: Get one release with full user-facing changelog
      parameters:
        - schema:
            type: string
            description: Semver or tag (e.g. 1.30.0 or v1.30.0).
          required: true
          description: Semver or tag (e.g. 1.30.0 or v1.30.0).
          name: version
          in: path
      responses:
        "200":
          description: Release detail.
          content:
            application/json:
              schema:
                type: object
                properties:
                  repo:
                    type: string
                  release:
                    $ref: "#/components/schemas/UserFacingRelease"
                required:
                  - repo
                  - release
        "404":
          description: Unknown version.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                required:
                  - error
        "500":
          description: Upstream or parsing failure.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                required:
                  - error
  /api/projects/{projectId}/replay-playlists:
    get:
      tags:
        - Replays
      summary: List replay playlists for a project
      parameters:
        - schema:
            type: string
          required: true
          name: projectId
          in: path
      responses:
        "200":
          description: The project’s playlists (with member counts).
          content:
            application/json:
              schema:
                type: object
                properties:
                  playlists:
                    type: array
                    items:
                      $ref: "#/components/schemas/ReplayPlaylist"
                required:
                  - playlists
        "404":
          description: Project not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ReplayPlaylistErrorResponse"
    post:
      tags:
        - Replays
      summary: Create a replay playlist
      parameters:
        - schema:
            type: string
          required: true
          name: projectId
          in: path
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateReplayPlaylistRequest"
      responses:
        "201":
          description: The created playlist.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ReplayPlaylist"
        "400":
          description: Invalid name.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ReplayPlaylistErrorResponse"
        "404":
          description: Project not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ReplayPlaylistErrorResponse"
  /api/projects/{projectId}/replay-playlists/{playlistId}:
    get:
      tags:
        - Replays
      summary: Get one playlist with its member captures
      parameters:
        - schema:
            type: string
          required: true
          name: projectId
          in: path
        - schema:
            type: string
          required: true
          name: playlistId
          in: path
      responses:
        "200":
          description: The playlist and its items.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ReplayPlaylistWithItems"
        "404":
          description: Project or playlist not found (cross-project playlists are masked as 404).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ReplayPlaylistErrorResponse"
    patch:
      tags:
        - Replays
      summary: Rename or update a playlist
      parameters:
        - schema:
            type: string
          required: true
          name: projectId
          in: path
        - schema:
            type: string
          required: true
          name: playlistId
          in: path
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateReplayPlaylistRequest"
      responses:
        "200":
          description: The updated playlist.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ReplayPlaylist"
        "400":
          description: Invalid name.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ReplayPlaylistErrorResponse"
        "404":
          description: Project or playlist not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ReplayPlaylistErrorResponse"
    delete:
      tags:
        - Replays
      summary: Delete a playlist
      description: Removes the playlist and its membership rows (cascade). Member captures are not deleted or un-flagged.
      parameters:
        - schema:
            type: string
          required: true
          name: projectId
          in: path
        - schema:
            type: string
          required: true
          name: playlistId
          in: path
      responses:
        "204":
          description: Deleted.
        "404":
          description: Project or playlist not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ReplayPlaylistErrorResponse"
  /api/projects/{projectId}/replay-playlists/{playlistId}/items:
    post:
      tags:
        - Replays
      summary: Add a saved capture to a playlist
      description: Adds a project-owned capture to the playlist (idempotent). If the playlist is flagged for extended retention, the new member inherits the flag. A capture that is unattributed or owned by another project is masked as 404.
      parameters:
        - schema:
            type: string
          required: true
          name: projectId
          in: path
        - schema:
            type: string
          required: true
          name: playlistId
          in: path
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AddReplayPlaylistItemRequest"
      responses:
        "200":
          description: Capture already present (idempotent no-op, added=false).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AddReplayPlaylistItemResponse"
        "201":
          description: Capture added (added=true).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AddReplayPlaylistItemResponse"
        "400":
          description: Missing replayId.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ReplayPlaylistErrorResponse"
        "404":
          description: Project, playlist, or replay not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ReplayPlaylistErrorResponse"
  /api/projects/{projectId}/replay-playlists/{playlistId}/items/{replayId}:
    delete:
      tags:
        - Replays
      summary: Remove a capture from a playlist
      parameters:
        - schema:
            type: string
          required: true
          name: projectId
          in: path
        - schema:
            type: string
          required: true
          name: playlistId
          in: path
        - schema:
            type: string
          required: true
          name: replayId
          in: path
      responses:
        "204":
          description: Removed.
        "404":
          description: Project, playlist, or playlist item not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ReplayPlaylistErrorResponse"
  /api/projects/{projectId}/replay-playlists/{playlistId}/retention:
    post:
      tags:
        - Replays
      summary: Flag or unflag a playlist for extended retention
      description: extend=true stamps an absolute retained_until (enable-time + the tenant’s extension window) on the playlist and every member capture’s session_replays row; extend=false clears only the playlist-level flag (members keep their retained_until).
      parameters:
        - schema:
            type: string
          required: true
          name: projectId
          in: path
        - schema:
            type: string
          required: true
          name: playlistId
          in: path
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ReplayPlaylistRetentionRequest"
      responses:
        "200":
          description: The updated playlist.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ReplayPlaylist"
        "400":
          description: "Body must be { extend: boolean }."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ReplayPlaylistErrorResponse"
        "404":
          description: Project or playlist not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ReplayPlaylistErrorResponse"
  /api/projects/{projectId}/replays:
    get:
      tags:
        - Replays
      summary: List session replays for the Replays Explorer dashboard
      description: Paginated, filterable table of a project’s session replays, each row enriched with its linked support ticket. `filter=orphans` lists global unattributed captures and is restricted to privileged callers.
      parameters:
        - schema:
            type: string
          required: true
          name: projectId
          in: path
        - schema:
            type: string
            enum:
              - all
              - linked
              - unlinked
              - orphans
            description: Default `all`.
          required: false
          description: Default `all`.
          name: filter
          in: query
        - schema:
            type: string
            enum:
              - all
              - continuous
              - on-error
            description: Capture-kind facet (continuous vs on-error). Default `all`.
          required: false
          description: Capture-kind facet (continuous vs on-error). Default `all`.
          name: kind
          in: query
        - schema:
            type: integer
            minimum: 0
            exclusiveMinimum: true
            maximum: 200
          required: false
          name: limit
          in: query
        - schema:
            type: integer
            nullable: true
            minimum: 0
          required: false
          name: offset
          in: query
      responses:
        "200":
          description: A page of replays.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ReplayListResponse"
        "400":
          description: Invalid filter or kind.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ReplaysDashboardErrorResponse"
        "403":
          description: Not authorized to view unattributed replays.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ReplaysDashboardErrorResponse"
        "404":
          description: Project not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ReplaysDashboardErrorResponse"
  /api/projects/{projectId}/replays/{id}/link:
    post:
      tags:
        - Replays
      summary: Attach a replay to one of the project’s support tickets
      description: "Links a replay to a support ticket using the first-write attribution guard: the replay is attached only if it is unattributed or already owned by this project. Linking to the same ticket is idempotent. A replay already linked to a different ticket — or a ticket already linked to a different replay — is a 409 (unlink first). A capture owned by ANOTHER project is masked as 404 (no cross-project existence probe)."
      parameters:
        - schema:
            type: string
          required: true
          name: projectId
          in: path
        - schema:
            type: string
          required: true
          name: id
          in: path
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/LinkReplayRequest"
      responses:
        "200":
          description: Linked. Returns the updated replay row and ticket.
          content:
            application/json:
              schema:
                type: object
                properties:
                  replay:
                    $ref: "#/components/schemas/ReplayListItem"
                  ticket:
                    type: object
                    additionalProperties:
                      nullable: true
                required:
                  - replay
                  - ticket
        "400":
          description: Missing supportTicketId.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ReplaysDashboardErrorResponse"
        "404":
          description: Project, replay, or ticket not found. A replay owned by another project is masked as 404 (not 409) to avoid a cross-project existence probe.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ReplaysDashboardErrorResponse"
        "409":
          description: Replay is already linked to a different ticket, the ticket is already linked to a different replay, or the replay could not be attributed to this project.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ReplaysDashboardErrorResponse"
    delete:
      tags:
        - Replays
      summary: Detach a replay from its support ticket
      description: Clears the support-ticket link on a replay (and the ticket’s replay_ref). The replay stays attributed to the project.
      parameters:
        - schema:
            type: string
          required: true
          name: projectId
          in: path
        - schema:
            type: string
          required: true
          name: id
          in: path
      responses:
        "200":
          description: Unlinked. Returns the updated replay row.
          content:
            application/json:
              schema:
                type: object
                properties:
                  replay:
                    $ref: "#/components/schemas/ReplayListItem"
                required:
                  - replay
        "404":
          description: Project or replay not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ReplaysDashboardErrorResponse"
  /api/replays:
    options:
      tags:
        - Bug Reports
      summary: CORS preflight (returns 204)
      responses:
        "204":
          description: CORS preflight OK.
    post:
      tags:
        - Bug Reports
      summary: Public session-replay ingest (JSON or gzip)
      description: "Body carries a non-empty `events` array of rrweb events. May be sent as raw `application/json` or gzip-compressed bytes (a gzip-framed `application/octet-stream` body, or `Content-Encoding: gzip`) — compression is recommended since rrweb JSON is large and the decompressed payload is bounded at 16 MB. Persists the buffer and returns its `/uploads/replay-<id>.json` ref, usable as a support ticket `replayRef`. Auth is optional: with no `X-RUM-Token` the request is anonymous, rate-limited 30 / hour per IP, and the row is left unattributed. With a valid per-project `X-RUM-Token` (minted via `POST /api/projects/{projectId}/rum/clients`) the capture is attributed to that project and rate-limited 600 / hour per project instead; an invalid token is rejected 401."
      parameters:
        - schema:
            type: string
            description: Optional per-project RUM ingest token. When present it must be valid.
          required: false
          description: Optional per-project RUM ingest token. When present it must be valid.
          name: x-rum-token
          in: header
      requestBody:
        description: Either a raw JSON `ReplayIngestRequest` body, or its gzip-compressed bytes sent as `application/octet-stream` (a gzip-framed body the server inflates transparently).
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ReplayIngestRequest"
          application/octet-stream:
            schema:
              type: string
              format: binary
              description: gzip-compressed request JSON (gzip-framed bytes).
      responses:
        "201":
          description: Stored.
          content:
            application/json:
              schema:
                type: object
                properties:
                  replayId:
                    type: string
                  replayRef:
                    type: string
                    description: Local `/uploads/replay-<id>.json` ref.
                  projectId:
                    type: string
                    nullable: true
                    description: Project the capture was attributed to via a valid `X-RUM-Token`, or null for anonymous ingest.
                  size:
                    type: number
                    description: Compressed (gzip) blob size in bytes.
                  eventCount:
                    type: number
                    description: Number of rrweb events stored.
                  durationMs:
                    type: number
                    description: Span between first and last event timestamp (ms).
                required:
                  - replayId
                  - replayRef
                  - projectId
                  - size
                  - eventCount
                  - durationMs
                description: The persisted replay id, its uploads ref, and blob stats.
        "400":
          description: Validation failed (empty events, bad event shape, non-object meta), or a malformed JSON / gzip body.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ReplayIngestErrorResponse"
        "401":
          description: An X-RUM-Token header was supplied but is invalid or revoked.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ReplayIngestErrorResponse"
        "413":
          description: Request body (or its decompressed size) exceeds the 16 MB limit.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ReplayIngestErrorResponse"
        "429":
          description: Per-IP (anonymous) or per-project (token) rate limit exceeded.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ReplayIngestErrorResponse"
        "500":
          description: Handler threw while persisting the replay.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ReplayIngestErrorResponse"
  /api/replays/{id}/events:
    options:
      tags:
        - Bug Reports
      summary: CORS preflight for chunked replay ingest (returns 204)
      parameters:
        - schema:
            type: string
            description: Replay id (uuid).
          required: true
          description: Replay id (uuid).
          name: id
          in: path
      responses:
        "204":
          description: CORS preflight OK.
    post:
      tags:
        - Bug Reports
      summary: Public chunked session-replay ingest
      description: "Rate-limited (600 / hour per IP for anonymous ingest). Appends one batch of rrweb events to the replay `id`. The first chunk creates the replay and must include a full snapshot (type 2); later chunks append incremental events. The body is `{ events, meta? }` as raw JSON or gzip-compressed (≤2 MB on the wire, ≤16 MB decompressed). A capture is capped at 20,000 total events. Auth is optional and mirrors the one-shot path: with no `X-RUM-Token` the stream is anonymous and the row left unattributed; with a valid per-project token (minted via `POST /api/projects/{projectId}/rum/clients`) the creating chunk attributes the whole stream to that project and the request is rate-limited 6000 / hour per project instead. Every chunk of a stream should carry the same token. A chunk whose project disagrees with an already-attributed capture (anonymous or foreign token) is rejected 403. Once a replay is triage-linked to a support ticket or card it is finalized and rejects all further chunks (409)."
      parameters:
        - schema:
            type: string
            description: Replay id (uuid).
          required: true
          description: Replay id (uuid).
          name: id
          in: path
        - schema:
            type: string
            description: Optional per-project RUM ingest token. When present it must be valid, and must match the project of any already-attributed capture.
          required: false
          description: Optional per-project RUM ingest token. When present it must be valid, and must match the project of any already-attributed capture.
          name: x-rum-token
          in: header
      requestBody:
        description: Either a raw JSON `ReplayBatchIngestRequest` body, or its gzip-compressed bytes sent as `application/octet-stream` (a gzip-framed body the server inflates transparently).
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ReplayBatchIngestRequest"
          application/octet-stream:
            schema:
              type: string
              format: binary
              description: gzip-compressed request JSON (gzip-framed bytes).
      responses:
        "200":
          description: Chunk appended.
          content:
            application/json:
              schema:
                type: object
                properties:
                  replayId:
                    type: string
                  created:
                    type: boolean
                    description: True when this chunk created the replay.
                  projectId:
                    type: string
                    nullable: true
                    description: Project the capture is attributed to (from a verified X-RUM-Token), or null when anonymous.
                  eventCount:
                    type: number
                    description: Total events stored after this chunk.
                  size:
                    type: number
                    description: Compressed (gzip) blob size in bytes.
                  durationMs:
                    type: number
                    description: Span across all stored events (ms).
                required:
                  - replayId
                  - created
                  - projectId
                  - eventCount
                  - size
                  - durationMs
                description: Running totals for the replay after appending this chunk.
        "201":
          description: Replay created.
          content:
            application/json:
              schema:
                type: object
                properties:
                  replayId:
                    type: string
                  created:
                    type: boolean
                    description: True when this chunk created the replay.
                  projectId:
                    type: string
                    nullable: true
                    description: Project the capture is attributed to (from a verified X-RUM-Token), or null when anonymous.
                  eventCount:
                    type: number
                    description: Total events stored after this chunk.
                  size:
                    type: number
                    description: Compressed (gzip) blob size in bytes.
                  durationMs:
                    type: number
                    description: Span across all stored events (ms).
                required:
                  - replayId
                  - created
                  - projectId
                  - eventCount
                  - size
                  - durationMs
                description: Running totals for the replay after appending this chunk.
        "400":
          description: Bad id, undecodable body, or validation failure.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ReplayIngestErrorResponse"
        "401":
          description: An X-RUM-Token header was supplied but is invalid or revoked.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ReplayIngestErrorResponse"
        "403":
          description: The chunk’s project (anonymous or a different token) disagrees with the capture’s existing attribution.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ReplayIngestErrorResponse"
        "409":
          description: Replay is finalized (triage-linked to a support ticket or card) and cannot accept more events.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ReplayIngestErrorResponse"
        "413":
          description: Decompressed payload or total event count exceeds its cap.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ReplayIngestErrorResponse"
        "429":
          description: Per-IP (anonymous) or per-project (token) rate limit exceeded.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ReplayIngestErrorResponse"
        "503":
          description: The replay’s storage backend could not be resolved.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ReplayIngestErrorResponse"
    get:
      tags:
        - Bug Reports
      summary: Paginated session-replay events (authenticated)
      description: Returns one page of the gunzipped rrweb event array, sliced by `offset`/`limit` (defaults applied + capped server-side) so large captures never load in one request. The page carries `total`/`hasMore` for walking. Same per-replay authorization as the metadata endpoint — unauthorized access is masked as 404. This endpoint serves `monolithic` (single-blob) captures only; a `segmented` capture (per the `storage_layout` discriminator) has no monolithic blob to paginate and is rejected 409 — read it via the session segments playback API instead.
      parameters:
        - schema:
            type: string
            description: Replay id (uuid).
          required: true
          description: Replay id (uuid).
          name: id
          in: path
        - schema:
            type: number
            nullable: true
            description: Start index (default 0).
          required: false
          description: Start index (default 0).
          name: offset
          in: query
        - schema:
            type: number
            nullable: true
            description: Page size (default 500, max 5000).
          required: false
          description: Page size (default 500, max 5000).
          name: limit
          in: query
      responses:
        "200":
          description: A page of events.
          content:
            application/json:
              schema:
                type: object
                properties:
                  replayId:
                    type: string
                  events:
                    type: array
                    items:
                      type: object
                      properties:
                        type:
                          type: number
                        timestamp:
                          type: number
                        data:
                          nullable: true
                      required:
                        - type
                        - timestamp
                    description: One page of rrweb events.
                  total:
                    type: number
                    description: Total events in the capture.
                  offset:
                    type: number
                    description: Applied (clamped) offset.
                  limit:
                    type: number
                    description: Applied (clamped) page size.
                  hasMore:
                    type: boolean
                    description: True when more events follow this page.
                required:
                  - replayId
                  - events
                  - total
                  - offset
                  - limit
                  - hasMore
                description: A paginated window of a replay’s events.
        "404":
          description: No replay with that id, or the caller is not authorized to read it.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ReplayIngestErrorResponse"
        "409":
          description: The replay is `segmented`; read it via `GET /api/replays/sessions/{sessionId}/segments`.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ReplayIngestErrorResponse"
        "500":
          description: Failed to read the stored blob.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ReplayIngestErrorResponse"
        "503":
          description: The replay’s storage backend could not be resolved.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ReplayIngestErrorResponse"
  /api/replays/sessions/{sessionId}/views/{viewId}/segments/{index}:
    options:
      tags:
        - Bug Reports
      summary: CORS preflight for view-scoped segment ingest (returns 204)
      parameters:
        - schema:
            type: string
            description: Client-minted session id (RUM sessionization).
          required: true
          description: Client-minted session id (RUM sessionization).
          name: sessionId
          in: path
        - schema:
            type: string
            description: Client-minted view id (per route/navigation).
          required: true
          description: Client-minted view id (per route/navigation).
          name: viewId
          in: path
        - schema:
            type: string
            description: index_in_view — 0 is the view-opening segment (must carry a snapshot).
          required: true
          description: index_in_view — 0 is the view-opening segment (must carry a snapshot).
          name: index
          in: path
      responses:
        "204":
          description: CORS preflight OK.
    post:
      tags:
        - Bug Reports
      summary: Public view-scoped segment ingest (Datadog segment write path)
      description: "Appends ONE view-scoped replay segment — a single gzipped object per `(sessionId, viewId, index_in_view)` slot, an O(1) append indexed by the `rum_segments` manifest. The view-opening segment (index 0) must carry a full snapshot (type 2); later indices append incremental events. The body is `{ events, meta? }` as raw JSON or gzip-compressed (≤16 MB decompressed), capped at 10,000 events per segment. Auth is optional and mirrors the chunked path: with no `X-RUM-Token` the segment is anonymous and rate-limited per IP; with a valid per-project token it is attributed to that project and rate-limited per project instead. Re-writing an already-stored `(session, view, index)` slot is rejected 409."
      parameters:
        - schema:
            type: string
            description: Client-minted session id (RUM sessionization).
          required: true
          description: Client-minted session id (RUM sessionization).
          name: sessionId
          in: path
        - schema:
            type: string
            description: Client-minted view id (per route/navigation).
          required: true
          description: Client-minted view id (per route/navigation).
          name: viewId
          in: path
        - schema:
            type: string
            description: index_in_view — 0 is the view-opening segment (must carry a snapshot).
          required: true
          description: index_in_view — 0 is the view-opening segment (must carry a snapshot).
          name: index
          in: path
        - schema:
            type: string
            description: Optional per-project RUM ingest token. When present it must be valid.
          required: false
          description: Optional per-project RUM ingest token. When present it must be valid.
          name: x-rum-token
          in: header
      requestBody:
        description: Either a raw JSON `ReplayBatchIngestRequest` body, or its gzip-compressed bytes sent as `application/octet-stream` (a gzip-framed body the server inflates transparently).
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ReplayBatchIngestRequest"
          application/octet-stream:
            schema:
              type: string
              format: binary
              description: gzip-compressed request JSON (gzip-framed bytes).
      responses:
        "201":
          description: Segment stored.
          content:
            application/json:
              schema:
                type: object
                properties:
                  segmentId:
                    type: string
                    description: Server-assigned segment id.
                  sessionId:
                    type: string
                  viewId:
                    type: string
                  indexInView:
                    type: number
                    description: 0-based position within the view.
                  hasFullSnapshot:
                    type: boolean
                    description: True when this segment carries an rrweb full snapshot (type 2).
                  projectId:
                    type: string
                    nullable: true
                    description: Project the segment is attributed to (from a verified X-RUM-Token), or null.
                  eventCount:
                    type: number
                    description: Number of rrweb events stored in the segment.
                  byteSize:
                    type: number
                    description: Compressed (gzip) object size in bytes.
                  startTs:
                    type: number
                    description: Earliest event timestamp in the segment (ms epoch).
                  endTs:
                    type: number
                    description: Latest event timestamp in the segment (ms epoch).
                required:
                  - segmentId
                  - sessionId
                  - viewId
                  - indexInView
                  - hasFullSnapshot
                  - projectId
                  - eventCount
                  - byteSize
                  - startTs
                  - endTs
                description: The persisted segment’s manifest row summary.
        "400":
          description: Bad session/view id or index, undecodable body, validation failure, or a view-opening segment (index 0) with no full snapshot.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ReplayIngestErrorResponse"
        "401":
          description: An X-RUM-Token header was supplied but is invalid or revoked.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ReplayIngestErrorResponse"
        "409":
          description: The `(session, view, index)` slot has already been written.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ReplayIngestErrorResponse"
        "429":
          description: Per-IP (anonymous) or per-project (token) rate limit exceeded.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ReplayIngestErrorResponse"
        "500":
          description: Handler threw while persisting the segment.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ReplayIngestErrorResponse"
        "503":
          description: The segment storage backend could not be resolved.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ReplayIngestErrorResponse"
  /api/replays/config:
    options:
      tags:
        - Bug Reports
      summary: CORS preflight (returns 204)
      responses:
        "204":
          description: CORS preflight OK.
    get:
      tags:
        - Bug Reports
      summary: Per-project replay policy (public)
      description: Returns the server-delivered replay policy (sample rate, continuous opt-in, mask-all enforcement) for the project resolved from a valid `X-RUM-Token` header, else the `projectId` query param. No/unknown project resolves to the default policy (sampleRate null, continuous off) so it is not an existence oracle. Public and CORS `*` — the policy carries no secrets.
      parameters:
        - schema:
            type: string
            description: Project slug to resolve the policy for (when no RUM token).
          required: false
          description: Project slug to resolve the policy for (when no RUM token).
          name: projectId
          in: query
      responses:
        "200":
          description: The resolved policy.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ResolvedReplayPolicy"
  /api/replays/{id}:
    get:
      tags:
        - Bug Reports
      summary: Session-replay metadata (authenticated)
      description: "Returns the `session_replays` metadata row (duration, sizes, ticket/card links, events URL). Authenticated, and authorized per-replay: a replay linked to a project is readable only by callers who can view that project; an unattributed replay is readable only by a privileged caller. Unauthorized access is masked as 404."
      parameters:
        - schema:
            type: string
            description: Replay id (uuid).
          required: true
          description: Replay id (uuid).
          name: id
          in: path
      responses:
        "200":
          description: Metadata.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SessionReplayMetadata"
        "404":
          description: No replay with that id, or the caller is not authorized to read it.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ReplayIngestErrorResponse"
  /api/replays/{id}/retention:
    post:
      tags:
        - Bug Reports
      summary: Flag / unflag a capture for extended retention (authenticated)
      description: "Two-tier retention: keep an individual session past the default window (up to 15 months). `{ extend: true }` stamps an absolute `retainedUntil` = now + the tenant’s extension window (project `replay.extendedRetentionMonths`, clamped [1,15] months, default 15) — the clock starts at enable time, not capture — and the retention sweeper skips the row until that instant passes. `{ extend: false }` clears the flag so the row rejoins the default sweep. Same per-replay authorization as the metadata endpoint; unauthorized access is masked as 404. Returns the updated metadata row."
      parameters:
        - schema:
            type: string
            description: Replay id (uuid).
          required: true
          description: Replay id (uuid).
          name: id
          in: path
      requestBody:
        description: Whether to extend (flag) or clear the capture’s extended retention.
        content:
          application/json:
            schema:
              type: object
              properties:
                extend:
                  type: boolean
                  description: true to flag for extended retention; false to clear.
              required:
                - extend
              description: Extended-retention flag toggle.
      responses:
        "200":
          description: Updated metadata.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SessionReplayMetadata"
        "400":
          description: "Body is not { extend: boolean }."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ReplayIngestErrorResponse"
        "404":
          description: No replay with that id, or the caller is not authorized to manage it.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ReplayIngestErrorResponse"
  /api/replays/{id}/transcript:
    get:
      tags:
        - Bug Reports
      summary: Agent-readable session-replay transcript (authenticated)
      description: Renders a stored capture as a readable timeline — page loads, clicks (with rapid-repeat/rage bursts collapsed), typed-value shapes, scrolls, viewport changes, coalesced DOM mutation bursts, plus console errors and network outcomes when the capture carries browser telemetry. Element targets are resolved against the capture’s own node mirror, so a click reads `button#submit.primary "Place order"` rather than `node 147`. All replay-derived text is redacted through the shared secret pipeline. Works for both `monolithic` and `segmented` captures. Same per-replay authorization as the events endpoint — unauthorized access is masked as 404.
      parameters:
        - schema:
            type: string
            description: Replay id (uuid).
          required: true
          description: Replay id (uuid).
          name: id
          in: path
        - schema:
            type: number
            nullable: true
            description: Byte budget for the rendered timeline (default 24576). Middle-elided to fit.
          required: false
          description: Byte budget for the rendered timeline (default 24576). Middle-elided to fit.
          name: maxBytes
          in: query
      responses:
        "200":
          description: The transcript.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ReplayTranscript"
        "404":
          description: No replay with that id, or the caller is not authorized to read it.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ReplayIngestErrorResponse"
        "500":
          description: Failed to read or render the capture.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ReplayIngestErrorResponse"
        "503":
          description: The replay’s storage backend could not be resolved.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ReplayIngestErrorResponse"
  /api/replays/sessions/{sessionId}/segments:
    get:
      tags:
        - Bug Reports
      summary: Segmented-capture playback manifest (authenticated)
      description: Lists a session’s replay segments in playback order (chronological by segment start, then by index within a view) so the player can fetch and concatenate them. Each entry carries a per-segment `eventsUrl`. Authenticated and authorized per-session on the segments’ shared `project_id` (same rule as the monolithic replay read); a session with no segments, or one the caller cannot view, is masked as 404.
      parameters:
        - schema:
            type: string
            description: Client-minted RUM session id.
          required: true
          description: Client-minted RUM session id.
          name: sessionId
          in: path
      responses:
        "200":
          description: The playback manifest.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SessionSegmentManifest"
        "404":
          description: No segments for that session id, or the caller is not authorized to read them.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ReplayIngestErrorResponse"
  /api/replays/sessions/{sessionId}/segments/{segmentId}/events:
    get:
      tags:
        - Bug Reports
      summary: One replay segment’s decoded events (authenticated)
      description: Returns the decoded rrweb events for a single segment, which the player concatenates client-side. The segment must belong to the path `sessionId` and is authorized on its own `project_id`; not-found, wrong-session, and unauthorized all collapse to 404.
      parameters:
        - schema:
            type: string
            description: Client-minted RUM session id.
          required: true
          description: Client-minted RUM session id.
          name: sessionId
          in: path
        - schema:
            type: string
            description: Segment id from the manifest.
          required: true
          description: Segment id from the manifest.
          name: segmentId
          in: path
      responses:
        "200":
          description: The segment’s events.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SegmentEvents"
        "404":
          description: No such segment, it does not belong to the session, or the caller is not authorized.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ReplayIngestErrorResponse"
        "500":
          description: Failed to read the segment object.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ReplayIngestErrorResponse"
        "503":
          description: The segment’s storage backend could not be resolved.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ReplayIngestErrorResponse"
  /api/projects/{projectId}/rum/clients:
    post:
      tags:
        - RUM
      summary: Mint a per-project RUM ingest token
      description: Admin+. Mints a new `rum_`-prefixed ingest credential for the project. The plaintext token is returned ONCE in the `token` field and is never retrievable again. Vendor sites send it as the `X-RUM-Token` header on `POST /api/replays` to attribute a capture to this project.
      parameters:
        - schema:
            type: string
            description: Project slug.
          required: true
          description: Project slug.
          name: projectId
          in: path
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                name:
                  type: string
                  minLength: 1
                  maxLength: 100
                  description: Human label for the ingest client.
              required:
                - name
      responses:
        "201":
          description: Token minted.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MintedRumClient"
        "400":
          description: Invalid name.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RumClientErrorResponse"
        "404":
          description: Project not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RumClientErrorResponse"
    get:
      tags:
        - RUM
      summary: List a project’s RUM ingest clients
      description: Admin+. Lists active (non-revoked) ingest clients for the project. Returns metadata only — never the token or its hash.
      parameters:
        - schema:
            type: string
            description: Project slug.
          required: true
          description: Project slug.
          name: projectId
          in: path
      responses:
        "200":
          description: Active clients.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RumClientListResponse"
        "404":
          description: Project not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RumClientErrorResponse"
  /api/projects/{projectId}/rum/clients/{clientId}:
    delete:
      tags:
        - RUM
      summary: Revoke a RUM ingest client
      description: Admin+. Revokes (soft-deletes) an ingest client. Uploads bearing that token are rejected afterward. Scoped to the project — a clientId from another project resolves to 404.
      parameters:
        - schema:
            type: string
            description: Project slug.
          required: true
          description: Project slug.
          name: projectId
          in: path
        - schema:
            type: string
            description: RUM client id.
          required: true
          description: RUM client id.
          name: clientId
          in: path
      responses:
        "200":
          description: Revoked.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RevokeRumClientResponse"
        "404":
          description: Project or client not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RumClientErrorResponse"
  /api/projects/{projectId}/rum/sessions:
    get:
      tags:
        - Replays
      summary: List RUM sessions for the Session Explorer dashboard
      description: Paginated, filterable table of a project’s RUM sessions (the `rum_sessions` session-grain rollup). All facets are optional and AND together; each maps to an indexed column. Text facets are exact-match; count/duration facets are range bounds; the started-at window is inclusive.
      parameters:
        - schema:
            type: string
          required: true
          name: projectId
          in: path
        - schema:
            type: string
            description: Exact user email facet.
          required: false
          description: Exact user email facet.
          name: usrEmail
          in: query
        - schema:
            type: string
            description: Exact user name facet.
          required: false
          description: Exact user name facet.
          name: usrName
          in: query
        - schema:
            type: string
            description: Exact user id facet.
          required: false
          description: Exact user id facet.
          name: usrId
          in: query
        - schema:
            type: string
            description: Exact device class facet.
          required: false
          description: Exact device class facet.
          name: deviceType
          in: query
        - schema:
            type: string
            description: Exact browser family facet.
          required: false
          description: Exact browser family facet.
          name: browser
          in: query
        - schema:
            type: string
            description: Exact OS family facet.
          required: false
          description: Exact OS family facet.
          name: os
          in: query
        - schema:
            type: string
            description: Exact country (alpha-2) facet.
          required: false
          description: Exact country (alpha-2) facet.
          name: geoCountry
          in: query
        - schema:
            type: integer
            nullable: true
            minimum: 0
            description: Minimum session duration, ms (time_spent >=).
          required: false
          description: Minimum session duration, ms (time_spent >=).
          name: durationMinMs
          in: query
        - schema:
            type: integer
            nullable: true
            minimum: 0
            description: Maximum session duration, ms (time_spent <=).
          required: false
          description: Maximum session duration, ms (time_spent <=).
          name: durationMaxMs
          in: query
        - schema:
            type: integer
            nullable: true
            minimum: 0
          required: false
          name: viewCountMin
          in: query
        - schema:
            type: integer
            nullable: true
            minimum: 0
          required: false
          name: actionCountMin
          in: query
        - schema:
            type: integer
            nullable: true
            minimum: 0
          required: false
          name: errorCountMin
          in: query
        - schema:
            type: integer
            nullable: true
            minimum: 0
          required: false
          name: frustrationCountMin
          in: query
        - schema:
            type: integer
            nullable: true
            description: Inclusive started-at lower bound, epoch ms.
          required: false
          description: Inclusive started-at lower bound, epoch ms.
          name: from
          in: query
        - schema:
            type: integer
            nullable: true
            description: Inclusive started-at upper bound, epoch ms.
          required: false
          description: Inclusive started-at upper bound, epoch ms.
          name: to
          in: query
        - schema:
            type: integer
            minimum: 0
            exclusiveMinimum: true
            maximum: 200
          required: false
          name: limit
          in: query
        - schema:
            type: integer
            nullable: true
            minimum: 0
          required: false
          name: offset
          in: query
      responses:
        "200":
          description: A page of RUM sessions.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RumSessionListResponse"
        "404":
          description: Project not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RumSessionsErrorResponse"
  /api/projects/{projectId}/rum/setup-draft:
    get:
      tags:
        - RUM
      summary: Scan a project for RUM instrumentation signal
      description: "Admin+. Reads the project working copy and returns the detection draft the RUM recorder-injection wizard needs: frontend framework, injection-target candidates, existing Content-Security-Policy locations, recorder state, and a recommended plan. Read-only — no session is spawned and no files are written."
      parameters:
        - schema:
            type: string
            description: Project slug.
          required: true
          description: Project slug.
          name: projectId
          in: path
      responses:
        "200":
          description: Detection draft computed.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RumSetupDraftResponse"
        "400":
          description: Project has no cwd configured.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RumWizardErrorResponse"
        "404":
          description: Project not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RumWizardErrorResponse"
  /api/projects/{projectId}/rum/setup-apply:
    post:
      tags:
        - RUM
      summary: Commit wizard instrumentation edits to the worktree
      description: |-
        Admin+. Stages and commits the instrumentation files the recorder-
        injection wizard edited (`files[]`) into the session worktree branch
        with `git commit -o` (only the listed paths — unrelated pre-staged
        work is left intact), then lets the existing Finalize Code Changes /
        pulls flow push and open the PR. Target resolution order: (1) request
        body's `session_id` when supplied (the wizard passes its own id), (2)
        the most-recent project session that has a worktree, (3) auto-provision
        a dedicated `[RUM Config]` session + worktree when none exists.
      parameters:
        - schema:
            type: string
            description: Project slug.
          required: true
          description: Project slug.
          name: projectId
          in: path
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/RumSetupApplyRequest"
      responses:
        "200":
          description: Committed.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RumSetupApplyResponse"
        "400":
          description: "Payload invalid: empty/invalid files list, a listed path missing from the worktree or not a regular file, nothing to commit, or no worktree-bearing session found."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RumWizardErrorResponse"
        "404":
          description: Project not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RumWizardErrorResponse"
        "500":
          description: Stage or commit failed.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RumWizardErrorResponse"
  /api/projects/{projectId}/rum/setup-wizard:
    post:
      tags:
        - RUM
      summary: Spawn the RUM recorder-injection wizard
      description: |-
        Admin+. Scans the project and spawns a **worktree-backed** chat session
        (`use_worktree=1`) loaded with the `rum-setup` skill. The detection draft
        (framework, target file, injection style, CSP hits, recommended
        `connect-src`) is embedded in the kickoff prompt. The agent injects the
        rrweb recorder init into `draft.plan.targetFile` using
        `draft.plan.injectionStyle`, extends any `draft.cspHits` with the ingest
        origin, commits to its branch, and uses Finalize Code Changes to push and
        open a PR.
      parameters:
        - schema:
            type: string
            description: Project slug.
          required: true
          description: Project slug.
          name: projectId
          in: path
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                maskAllText:
                  type: boolean
                  description: Masking policy for the injected recorder, chosen per target app at setup time. `false` (default) masks password + PII fields only and records other inputs/text verbatim — the right default for instrumenting third-party apps. `true` masks ALL text and inputs (only structure/layout/timing recorded). Baked into the recorder init the wizard writes; it is NOT Agent Hub's own self-recording setting.
              description: Optional masking policy for the injected recorder.
      responses:
        "201":
          description: Wizard session spawned.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RumWizardStartResponse"
        "400":
          description: Project has no cwd configured, or no agents to host the wizard.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RumWizardErrorResponse"
        "404":
          description: Project not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RumWizardErrorResponse"
        "500":
          description: Wizard agent could not be resolved.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RumWizardErrorResponse"
  /api/projects/{projectId}/security-audit/findings:
    get:
      tags:
        - Projects
      summary: List dependency vulnerability findings for a project
      description: Returns persisted dependency-audit findings (optionally filtered by status) plus open-finding counts by severity. Findings come from the most recent scans; statuses are open / fixed / dismissed.
      parameters:
        - schema:
            type: string
          required: true
          name: projectId
          in: path
        - schema:
            type: string
            enum:
              - open
              - fixed
              - dismissed
          required: false
          name: status
          in: query
      responses:
        "200":
          description: Findings + severity counts.
          content:
            application/json:
              schema:
                type: object
                properties:
                  findings:
                    type: array
                    items:
                      $ref: "#/components/schemas/SecurityFinding"
                  openCounts:
                    type: object
                    properties:
                      critical:
                        type: number
                      high:
                        type: number
                      medium:
                        type: number
                      low:
                        type: number
                      unknown:
                        type: number
                    required:
                      - critical
                      - high
                      - medium
                      - low
                      - unknown
                required:
                  - findings
                  - openCounts
        "400":
          description: Invalid status filter.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                required:
                  - error
        "404":
          description: Unknown project.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                required:
                  - error
  /api/projects/{projectId}/security-audit/scan:
    post:
      tags:
        - Projects
      summary: Run a dependency security scan now
      description: "Scans the Hub-hosted repo (default branch unless `ref` is given): parses npm lockfiles (`package-lock.json`, `npm-shrinkwrap.json`) and Python/PyPI lockfiles (`requirements.txt` plus common `*requirements*.txt` variants, `poetry.lock`, `Pipfile.lock`), queries the OSV advisory database, persists findings with de-dupe, and opens a kanban card for genuinely new findings. Persistence is restricted to the default-branch tip — scanning any other `ref` is a read-only dry run (`dryRun: true`, nothing written). Requires gitHost: agenthub and the Admin role."
      parameters:
        - schema:
            type: string
          required: true
          name: projectId
          in: path
      requestBody:
        required: false
        content:
          application/json:
            schema:
              type: object
              properties:
                ref:
                  type: string
                  minLength: 1
                  description: Branch/commit. Defaults to default branch.
                generateCard:
                  type: boolean
                  description: Open a card for new findings (default true).
                autoPr:
                  type: boolean
                  description: "Dispatch an agent session to resolve fixable findings after the scan, even when the project has not set securityAutoPr.enabled. An explicit Autofix click is its own opt-in. Requires gitHost: agenthub; ignored otherwise."
              additionalProperties: false
      responses:
        "200":
          description: Scan summary.
          content:
            application/json:
              schema:
                type: object
                properties:
                  ref:
                    type: string
                  dryRun:
                    type: boolean
                    description: "True when the scanned ref is not the default-branch tip: the scan ran for visibility but persisted nothing (no findings, no sweep, no card). new/updated/fixed/suppressed/cardId are all empty in this case."
                  scannedManifests:
                    type: array
                    items:
                      type: string
                  failedManifests:
                    type: array
                    items:
                      type: string
                    description: Lockfiles matched but unreadable/unparsable this scan (findings preserved).
                  truncated:
                    type: boolean
                    description: True when more lockfiles existed than the per-scan cap; overflow not scanned.
                  dependencyCount:
                    type: number
                  vulnerableFindings:
                    type: number
                    description: Vulnerable dependency occurrences detected this scan (persisted or not).
                  newFindings:
                    type: number
                  reopened:
                    type: number
                    description: Previously-fixed findings that reappeared this scan (regressions).
                  updated:
                    type: number
                  fixed:
                    type: number
                  suppressed:
                    type: number
                  cardId:
                    type: string
                    nullable: true
                  fixSession:
                    type: object
                    nullable: true
                    properties:
                      sessionId:
                        type: string
                      agentId:
                        type: string
                      findingCount:
                        type: number
                      reused:
                        type: boolean
                        description: True when an already-running security-fix session was reused instead of starting a new one (idempotency guard).
                    required:
                      - sessionId
                      - agentId
                      - findingCount
                      - reused
                    description: "Set when Autofix was requested (securityAutoPr.enabled or autoPr:true) on a Hub-hosted repo and the persisted scan found open findings: the agent session dispatched to resolve them. null when autofix was not requested, this was a dry run, there were no open findings, or dispatch failed (see fixSessionError)."
                  fixSessionError:
                    type: string
                    nullable: true
                    description: Set when Autofix wanted to dispatch (open findings exist) but could not — currently only "no eligible agent on the roster". Distinguishes a configuration problem from a legitimate no-op (no open findings), where it is null.
                required:
                  - ref
                  - dryRun
                  - scannedManifests
                  - failedManifests
                  - truncated
                  - dependencyCount
                  - vulnerableFindings
                  - newFindings
                  - reopened
                  - updated
                  - fixed
                  - suppressed
                  - cardId
                  - fixSession
                  - fixSessionError
        "400":
          description: Invalid request body, or the requested ref does not resolve.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                required:
                  - error
        "403":
          description: Caller lacks the Admin role.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                required:
                  - error
        "404":
          description: Unknown project.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                required:
                  - error
        "409":
          description: Project is not Hub-hosted or repo is empty.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                required:
                  - error
        "500":
          description: Scan failed.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                required:
                  - error
  /api/projects/{projectId}/security-audit/findings/{id}/fix:
    post:
      tags:
        - Projects
      summary: Dispatch an agent session to resolve a finding
      description: "Dispatches an agent session to resolve the project's open dependency findings (not just the clicked row — the session fixes them all in one branch → one PR). The agent bumps each package to its fixed version, re-resolves the lockfile with the real package manager, and runs the tests; Finalize opens the PR at session end. Works for any ecosystem (npm + pip). Idempotent: if a security-fix session is already running for the project, the existing one is returned (200, reused:true) instead of starting a duplicate. Requires gitHost: agenthub, the Admin role, an open finding, and an eligible agent on the roster."
      parameters:
        - schema:
            type: string
          required: true
          name: projectId
          in: path
        - schema:
            type: string
          required: true
          name: id
          in: path
      responses:
        "200":
          description: An already-running fix session was reused (reused:true).
          content:
            application/json:
              schema:
                type: object
                properties:
                  sessionId:
                    type: string
                    nullable: true
                  agentId:
                    type: string
                    nullable: true
                  findingCount:
                    type: number
                  reused:
                    type: boolean
                    description: True when an already-running security-fix session was reused (200) instead of a new one being started (201).
                  session:
                    type: object
                    nullable: true
                    additionalProperties:
                      nullable: true
                required:
                  - sessionId
                  - agentId
                  - findingCount
                  - reused
                  - session
        "201":
          description: A new session was dispatched to resolve the findings.
          content:
            application/json:
              schema:
                type: object
                properties:
                  sessionId:
                    type: string
                    nullable: true
                  agentId:
                    type: string
                    nullable: true
                  findingCount:
                    type: number
                  reused:
                    type: boolean
                    description: True when an already-running security-fix session was reused (200) instead of a new one being started (201).
                  session:
                    type: object
                    nullable: true
                    additionalProperties:
                      nullable: true
                required:
                  - sessionId
                  - agentId
                  - findingCount
                  - reused
                  - session
        "403":
          description: Caller lacks the Admin role.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                required:
                  - error
        "404":
          description: Unknown project or finding.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                required:
                  - error
        "409":
          description: Project is not Hub-hosted, the finding is not open, or no eligible agent is available.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                required:
                  - error
  /api/projects/{projectId}/security-audit/fix:
    post:
      tags:
        - Projects
      summary: Dispatch an agent session to resolve all findings, optionally by severity
      description: "Dispatches an agent session to resolve every open finding for the project, optionally narrowed to a severity threshold (`high` = critical AND high; omit to include all). The agent bumps + re-resolves lockfiles + runs tests; Finalize opens the PR. Idempotent: an already-running fix session is reused (200, reused:true) rather than duplicated. Requires gitHost: agenthub and the Admin role. Returns 201 with a newly-dispatched session, or 200 when a session was reused or no finding matched the threshold (null session)."
      parameters:
        - schema:
            type: string
          required: true
          name: projectId
          in: path
      requestBody:
        required: false
        content:
          application/json:
            schema:
              type: object
              properties:
                minSeverity:
                  type: string
                  enum:
                    - critical
                    - high
                    - medium
                    - low
                  description: "Only fix findings at or above this severity (threshold, not exact): `high` fixes critical AND high. Omit to fix every fixable finding regardless of severity."
              additionalProperties: false
      responses:
        "200":
          description: An already-running session was reused (reused:true), or no finding matched the threshold (null session).
          content:
            application/json:
              schema:
                type: object
                properties:
                  sessionId:
                    type: string
                    nullable: true
                  agentId:
                    type: string
                    nullable: true
                  findingCount:
                    type: number
                  reused:
                    type: boolean
                    description: True when an already-running security-fix session was reused (200) instead of a new one being started (201).
                  session:
                    type: object
                    nullable: true
                    additionalProperties:
                      nullable: true
                required:
                  - sessionId
                  - agentId
                  - findingCount
                  - reused
                  - session
        "201":
          description: A new session was dispatched to resolve the findings.
          content:
            application/json:
              schema:
                type: object
                properties:
                  sessionId:
                    type: string
                    nullable: true
                  agentId:
                    type: string
                    nullable: true
                  findingCount:
                    type: number
                  reused:
                    type: boolean
                    description: True when an already-running security-fix session was reused (200) instead of a new one being started (201).
                  session:
                    type: object
                    nullable: true
                    additionalProperties:
                      nullable: true
                required:
                  - sessionId
                  - agentId
                  - findingCount
                  - reused
                  - session
        "400":
          description: Invalid request body (e.g. an unknown key or bad minSeverity).
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                required:
                  - error
        "403":
          description: Caller lacks the Admin role.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                required:
                  - error
        "404":
          description: Unknown project.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                required:
                  - error
        "409":
          description: Project is not Hub-hosted, or no eligible agent is available.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                required:
                  - error
  /api/projects/{projectId}/security-audit/findings/{id}/dismiss:
    post:
      tags:
        - Projects
      summary: Dismiss (and optionally suppress) a dependency finding
      description: Marks a finding dismissed. When `suppress` is true (default) a suppression is recorded so the advisory stays dismissed on future re-scans and is excluded from card generation. Requires the Admin role.
      parameters:
        - schema:
            type: string
          required: true
          name: projectId
          in: path
        - schema:
            type: string
          required: true
          name: id
          in: path
      requestBody:
        required: false
        content:
          application/json:
            schema:
              type: object
              properties:
                reason:
                  type: string
                  maxLength: 500
                suppress:
                  type: boolean
              additionalProperties: false
      responses:
        "200":
          description: Updated finding.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SecurityFinding"
        "400":
          description: Invalid request body (e.g. an unknown key).
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                required:
                  - error
        "403":
          description: Caller lacks the Admin role.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                required:
                  - error
        "404":
          description: Unknown project or finding.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                required:
                  - error
  /api/projects/{projectId}/skills/{skillId}/evals:
    get:
      tags:
        - Skills
      summary: Read a skill eval suite
      description: "Returns the eval suite at `<dataDir>/project-skills/<projectId>/<skillId>/evals/evals.json`. An absent file yields `{ evals: [] }`. A present-but-invalid file returns 422."
      parameters:
        - schema:
            type: string
            description: Project ID (slug).
          required: true
          description: Project ID (slug).
          name: projectId
          in: path
        - schema:
            type: string
            description: Project skill slug (folder id).
          required: true
          description: Project skill slug (folder id).
          name: skillId
          in: path
      responses:
        "200":
          description: The eval suite.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SkillEvalsList"
        "400":
          description: No project skill store / invalid skill id.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SkillEvalsError"
        "404":
          description: Project or skill not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SkillEvalsError"
        "422":
          description: evals.json present but invalid.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SkillEvalsError"
    put:
      tags:
        - Skills
      summary: Write a skill eval suite
      description: Validates and writes the eval suite to `evals/evals.json`. Eval ids must be unique slugs; each prompt is required; assertions are optional objective checks (`regex` patterns are compiled at write time).
      parameters:
        - schema:
            type: string
            description: Project ID (slug).
          required: true
          description: Project ID (slug).
          name: projectId
          in: path
        - schema:
            type: string
            description: Project skill slug (folder id).
          required: true
          description: Project skill slug (folder id).
          name: skillId
          in: path
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/SkillEvalsBody"
      responses:
        "200":
          description: The written eval suite.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SkillEvalsList"
        "400":
          description: Validation failed.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SkillEvalsError"
        "401":
          description: Authentication required.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SkillEvalsError"
        "403":
          description: Requires the Admin role or higher (project mutation).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SkillEvalsError"
        "404":
          description: Project or skill not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SkillEvalsError"
        "500":
          description: Filesystem write error.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SkillEvalsError"
  /api/projects/{projectId}/skills/{skillId}/evals/run:
    post:
      tags:
        - Skills
      summary: Run a skill eval suite (with-skill vs baseline)
      description: Runs each eval prompt twice — once with the skill's SKILL.md injected as the system prompt (with-skill) and once without (baseline) — grades both against the eval assertions, and returns a structured summary plus a rendered Markdown report. Uses the saved evals.json unless an inline `evals` array is supplied. Gemini is excluded from the engine fallback chain.
      parameters:
        - schema:
            type: string
            description: Project ID (slug).
          required: true
          description: Project ID (slug).
          name: projectId
          in: path
        - schema:
            type: string
            description: Project skill slug (folder id).
          required: true
          description: Project skill slug (folder id).
          name: skillId
          in: path
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/SkillEvalRunBody"
      responses:
        "200":
          description: Run summary + report.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SkillEvalRunSummary"
        "400":
          description: No evals defined / body validation failed / unknown evalIds / requested engine unavailable.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SkillEvalsError"
        "401":
          description: Authentication required.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SkillEvalsError"
        "403":
          description: Requires the Admin role or higher (spawns agent CLIs).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SkillEvalsError"
        "404":
          description: Project, skill, or SKILL.md not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SkillEvalsError"
        "422":
          description: Saved evals.json invalid.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SkillEvalsError"
        "500":
          description: Spawn / runtime error.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SkillEvalsError"
  /api/projects/{projectId}/skills:
    get:
      tags:
        - Skills
      summary: List project skills library
      description: Returns project-authored skills only (`<dataDir>/project-skills/<projectId>`). Built-in and shared global skills are listed via `GET /api/global-skills`.
      parameters:
        - schema:
            type: string
            description: Project ID (slug).
          required: true
          description: Project ID (slug).
          name: projectId
          in: path
      responses:
        "200":
          description: Project-authored skills.
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/ProjectSkillListItem"
        "404":
          description: Project not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SkillWriteError"
        "500":
          description: Filesystem read error.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SkillWriteError"
    post:
      tags:
        - Skills
      summary: Create a project skill
      description: Validates the author payload, composes a canonical `SKILL.md` (YAML frontmatter + Markdown body), and writes it to `<dataDir>/project-skills/<projectId>/<slug>/SKILL.md`. Accepts EITHER structured fields (`name` + `description`) OR a raw `content` SKILL.md whose frontmatter carries them. Rejects a slug that shadows a bundled default skill (409) or an already-existing project skill (409 — use PUT). Invalid frontmatter (bad slug, missing description, disallowed category, malformed credentials) returns 400.
      parameters:
        - schema:
            type: string
            description: Project ID (slug).
          required: true
          description: Project ID (slug).
          name: projectId
          in: path
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateSkillBody"
      responses:
        "201":
          description: Skill created.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SkillWriteResult"
        "400":
          description: Request body validation failed.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SkillWriteError"
        "404":
          description: Project not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SkillWriteError"
        "409":
          description: Slug collides with a bundled default or an existing project skill.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SkillWriteError"
        "500":
          description: Filesystem write error.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SkillWriteError"
  /api/global-skills:
    get:
      tags:
        - Skills
      summary: List global skills catalog
      description: "Returns the shared skills catalog: user-authored global skills (`<dataDir>/skills`) plus bundled built-in defaults. Project-authored skills are listed per project via `GET /api/projects/{projectId}/skills`."
      responses:
        "200":
          description: Global skills catalog (shared + built-in).
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/GlobalSkillListItem"
        "500":
          description: Filesystem read error.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SkillWriteError"
    post:
      tags:
        - Skills
      summary: Create a global (shared) skill
      description: Validates the author payload, composes a canonical `SKILL.md`, and writes it to `<dataDir>/skills/<slug>/SKILL.md`. Accepts EITHER structured fields (`name` + `description`) OR a raw `content` SKILL.md. Rejects a slug that shadows a bundled default skill (409) or an existing global skill (409 — use PUT). Invalid frontmatter returns 400.
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateSkillBody"
      responses:
        "201":
          description: Skill created.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GlobalSkillWriteResult"
        "400":
          description: Request body validation failed.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SkillWriteError"
        "409":
          description: Slug collides with a bundled default or an existing global skill.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SkillWriteError"
        "500":
          description: Filesystem write error.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SkillWriteError"
        "503":
          description: Global skills directory unavailable (server data dir not configured).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SkillWriteError"
  /api/global-skills/{skillId}:
    get:
      tags:
        - Skills
      summary: Get a global skill
      description: Returns a single global skill's raw SKILL.md content (for editing).
      parameters:
        - schema:
            type: string
            description: Global skill slug (folder id).
          required: true
          description: Global skill slug (folder id).
          name: skillId
          in: path
      responses:
        "200":
          description: Global skill.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GlobalSkillDetail"
        "400":
          description: Invalid skill id or malformed credentials frontmatter.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SkillWriteError"
        "404":
          description: Global skill not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SkillWriteError"
        "500":
          description: Filesystem read error.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SkillWriteError"
        "503":
          description: Global skills directory unavailable (server data dir not configured).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SkillWriteError"
    put:
      tags:
        - Skills
      summary: Update a global (shared) skill
      description: Rewrites an existing global skill's `SKILL.md`. A body `name` (or `content` frontmatter name) must equal the `:skillId` path segment — rename is not supported. Rejects bundled-default ids (409) and unknown global skills (404). Invalid frontmatter returns 400.
      parameters:
        - schema:
            type: string
            description: Global skill slug (folder id).
          required: true
          description: Global skill slug (folder id).
          name: skillId
          in: path
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateSkillBody"
      responses:
        "200":
          description: Skill updated.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GlobalSkillWriteResult"
        "400":
          description: Request body validation failed.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SkillWriteError"
        "404":
          description: Global skill not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SkillWriteError"
        "409":
          description: Slug is a bundled default skill.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SkillWriteError"
        "500":
          description: Filesystem write error.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SkillWriteError"
        "503":
          description: Global skills directory unavailable (server data dir not configured).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SkillWriteError"
    delete:
      tags:
        - Skills
      summary: Delete a global (shared) skill
      description: Removes a global skill directory from `<dataDir>/skills`.
      parameters:
        - schema:
            type: string
            description: Global skill slug (folder id).
          required: true
          description: Global skill slug (folder id).
          name: skillId
          in: path
      responses:
        "200":
          description: Deleted.
          content:
            application/json:
              schema:
                type: object
                properties:
                  ok:
                    type: boolean
                required:
                  - ok
        "400":
          description: Invalid skill id.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SkillWriteError"
        "500":
          description: Filesystem error.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SkillWriteError"
        "503":
          description: Global skills directory unavailable (server data dir not configured).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SkillWriteError"
  /api/projects/{projectId}/skills/{skillId}:
    get:
      tags:
        - Skills
      summary: Read a project skill
      description: Returns a single project-authored skill's raw `SKILL.md` (frontmatter + body) plus its credential schema. Project-owned read — does NOT require an agent and does NOT fall back to bundled defaults, so the editor can load a project skill even for an agentless project. Resolves directory (`<slug>/SKILL.md`) or flat (`<slug>.md`) form.
      parameters:
        - schema:
            type: string
            description: Project ID (slug).
          required: true
          description: Project ID (slug).
          name: projectId
          in: path
        - schema:
            type: string
            description: Project skill slug (folder id).
          required: true
          description: Project skill slug (folder id).
          name: skillId
          in: path
      responses:
        "200":
          description: The project skill with its raw SKILL.md content.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ProjectSkillDetail"
        "400":
          description: Invalid skill id or malformed credentials frontmatter.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SkillWriteError"
        "404":
          description: Project or project skill not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SkillWriteError"
        "500":
          description: Filesystem read error.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SkillWriteError"
    put:
      tags:
        - Skills
      summary: Update a project skill
      description: Rewrites an existing project skill's `SKILL.md` (frontmatter + body). Accepts EITHER structured fields (`description` required; `name` optional, resolved from the path) OR a raw `content` SKILL.md. A body `name` (or `content` frontmatter name) must equal the `:skillId` path segment — rename is not supported. `expectedCredentials` enables an optimistic concurrency check and returns 409 when authentication changed after the caller loaded the skill. Rejects bundled-default ids (409) and unknown project skills (404). Invalid frontmatter returns 400.
      parameters:
        - schema:
            type: string
            description: Project ID (slug).
          required: true
          description: Project ID (slug).
          name: projectId
          in: path
        - schema:
            type: string
            description: Project skill slug (folder id).
          required: true
          description: Project skill slug (folder id).
          name: skillId
          in: path
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ProjectUpdateSkillBody"
      responses:
        "200":
          description: Skill updated.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SkillWriteResult"
        "400":
          description: Request body validation failed.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SkillWriteError"
        "404":
          description: Project or project skill not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SkillWriteError"
        "409":
          description: Slug is a bundled default skill or the expected credential declaration no longer matches.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SkillWriteError"
        "500":
          description: Filesystem write error.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SkillWriteError"
  /api/projects/{projectId}/skill-improvements:
    get:
      tags:
        - Skills
      summary: List skill improvement suggestions
      description: Returns agent-suggested skill lessons across the project + global skill tiers (bundled defaults are read-only and never listed). Filter with `status` (default `pending`). Sorted oldest-first.
      parameters:
        - schema:
            type: string
            description: Project ID (slug).
          required: true
          description: Project ID (slug).
          name: projectId
          in: path
        - schema:
            type: string
            enum:
              - pending
              - approved
              - rejected
              - all
            description: Filter by review status. Defaults to `pending`.
          required: false
          description: Filter by review status. Defaults to `pending`.
          name: status
          in: query
      responses:
        "200":
          description: Improvement records matching the filter.
          content:
            application/json:
              schema:
                type: object
                properties:
                  improvements:
                    type: array
                    items:
                      $ref: "#/components/schemas/SkillImprovementRecord"
                required:
                  - improvements
        "400":
          description: Invalid status filter.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SkillWriteError"
        "404":
          description: Project not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SkillWriteError"
        "500":
          description: Filesystem read error.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SkillWriteError"
  /api/projects/{projectId}/skills/{skillId}/improvements/{improvementId}/approve:
    post:
      tags:
        - Skills
      summary: Approve a skill improvement (Admin+)
      description: Promotes a pending suggestion into the skill's `SKILL.md` as a dated bullet under `## Learned Lessons`, and marks the record `approved`. Runs under the per-skill file lock. Requires Admin or Owner. 409 when the record was already reviewed or the skill is a bundled default.
      parameters:
        - schema:
            type: string
            description: Project ID (slug).
          required: true
          description: Project ID (slug).
          name: projectId
          in: path
        - schema:
            type: string
            description: Skill slug the improvement belongs to.
          required: true
          description: Skill slug the improvement belongs to.
          name: skillId
          in: path
        - schema:
            type: string
            description: Improvement record id (UUID).
          required: true
          description: Improvement record id (UUID).
          name: improvementId
          in: path
      responses:
        "200":
          description: Approved and promoted.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ReviewSkillImprovementResult"
        "400":
          description: Invalid skill or improvement id.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SkillWriteError"
        "403":
          description: Caller is below Admin.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SkillWriteError"
        "404":
          description: Project, skill, or improvement not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SkillWriteError"
        "409":
          description: Already reviewed, or bundled default skill.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SkillWriteError"
        "500":
          description: Filesystem write error.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SkillWriteError"
  /api/projects/{projectId}/skills/{skillId}/improvements/{improvementId}/reject:
    post:
      tags:
        - Skills
      summary: Reject a skill improvement (Admin+)
      description: Marks a pending suggestion `rejected` without touching SKILL.md. An optional `reason` is kept on the record for audit. Requires Admin or Owner.
      parameters:
        - schema:
            type: string
            description: Project ID (slug).
          required: true
          description: Project ID (slug).
          name: projectId
          in: path
        - schema:
            type: string
            description: Skill slug the improvement belongs to.
          required: true
          description: Skill slug the improvement belongs to.
          name: skillId
          in: path
        - schema:
            type: string
            description: Improvement record id (UUID).
          required: true
          description: Improvement record id (UUID).
          name: improvementId
          in: path
      requestBody:
        required: false
        content:
          application/json:
            schema:
              type: object
              properties:
                reason:
                  type: string
                  description: Optional reviewer note kept for audit.
      responses:
        "200":
          description: Rejected.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ReviewSkillImprovementResult"
        "400":
          description: Invalid skill or improvement id.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SkillWriteError"
        "403":
          description: Caller is below Admin.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SkillWriteError"
        "404":
          description: Project, skill, or improvement not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SkillWriteError"
        "409":
          description: Already reviewed, or bundled default skill.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SkillWriteError"
        "500":
          description: Filesystem write error.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SkillWriteError"
  /api/projects/{projectId}/default-skills:
    get:
      tags:
        - Skills
      summary: List the project default-on skills
      description: The skill ids the project owner marked "on by default for every session". Each is auto-loaded (its SKILL.md injected) into every session of the project.
      parameters:
        - schema:
            type: string
            description: Project ID (slug).
          required: true
          description: Project ID (slug).
          name: projectId
          in: path
      responses:
        "200":
          description: Default skill ids.
          content:
            application/json:
              schema:
                type: object
                properties:
                  skillIds:
                    type: array
                    items:
                      type: string
                    description: Skill ids auto-loaded into every session of the project.
                required:
                  - skillIds
        "404":
          description: Project not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SkillWriteError"
    post:
      tags:
        - Skills
      summary: Mark a skill on-by-default for the project (Admin+)
      parameters:
        - schema:
            type: string
            description: Project ID (slug).
          required: true
          description: Project ID (slug).
          name: projectId
          in: path
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                skillId:
                  type: string
                  description: Skill id to enable by default.
              required:
                - skillId
      responses:
        "200":
          description: Added.
          content:
            application/json:
              schema:
                type: object
                properties:
                  skillIds:
                    type: array
                    items:
                      type: string
                    description: Skill ids auto-loaded into every session of the project.
                required:
                  - skillIds
        "400":
          description: skillId is required.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SkillWriteError"
        "403":
          description: Caller is below Admin.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SkillWriteError"
        "404":
          description: Project not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SkillWriteError"
        "500":
          description: Write error.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SkillWriteError"
  /api/projects/{projectId}/default-skills/{skillId}:
    delete:
      tags:
        - Skills
      summary: Remove a skill from the project default-on list (Admin+)
      parameters:
        - schema:
            type: string
            description: Project ID (slug).
          required: true
          description: Project ID (slug).
          name: projectId
          in: path
        - schema:
            type: string
          required: true
          name: skillId
          in: path
      responses:
        "200":
          description: Removed (or already absent).
          content:
            application/json:
              schema:
                type: object
                properties:
                  skillIds:
                    type: array
                    items:
                      type: string
                    description: Skill ids auto-loaded into every session of the project.
                required:
                  - skillIds
        "403":
          description: Caller is below Admin.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SkillWriteError"
        "404":
          description: Project not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SkillWriteError"
  /api/projects/{projectId}/support-tickets:
    get:
      tags:
        - Support
      summary: List a project’s support tickets, ordered by severity
      description: Defaults to the open states (new, investigating) — terminal tickets (converted/closed/duplicate/wont_do) are hidden until requested. Pass a comma-separated `status` list (e.g. `converted,closed`) and/or a single `type` to filter.
      parameters:
        - schema:
            type: string
            description: Project slug or id.
          required: true
          description: Project slug or id.
          name: projectId
          in: path
        - schema:
            type: string
            description: Comma-separated lifecycle states to include (new | investigating | converted | closed | duplicate | wont_do). Omit to default to the open states.
          required: false
          description: Comma-separated lifecycle states to include (new | investigating | converted | closed | duplicate | wont_do). Omit to default to the open states.
          name: status
          in: query
        - schema:
            type: string
            enum:
              - bug
              - question
              - feature_request
              - incident
              - other
            description: Filter to a single request type (e.g. bug, feature_request).
          required: false
          description: Filter to a single request type (e.g. bug, feature_request).
          name: type
          in: query
      responses:
        "200":
          description: Tickets ordered by severity (critical → low) then newest.
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/SupportTicket"
        "400":
          description: Invalid status or type filter.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SupportTicketErrorResponse"
        "404":
          description: Project not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SupportTicketErrorResponse"
    post:
      tags:
        - Support
      summary: Create a support ticket
      parameters:
        - schema:
            type: string
            description: Project slug or id.
          required: true
          description: Project slug or id.
          name: projectId
          in: path
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                body:
                  type: string
                  minLength: 1
                type:
                  type: string
                  enum:
                    - bug
                    - question
                    - feature_request
                    - incident
                    - other
                severity:
                  type: string
                  enum:
                    - critical
                    - high
                    - medium
                    - low
                subject:
                  type: string
                reporter:
                  type: string
                reporter_email:
                  type: string
                  format: email
                  description: Optional reporter contact email. Stored as a protected support-ticket field for release notifications; omitted/blank remains allowed for anonymous-compatible flows.
                replayRef:
                  type: string
                screenshot:
                  type: string
                  description: Optional screenshot as a base64 data URL (data:image/png|jpeg|webp|gif;base64,...). Persisted server-side and exposed via screenshot_ref.
              required:
                - body
      responses:
        "201":
          description: Created ticket.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SupportTicket"
        "400":
          description: Invalid type/severity or missing body.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SupportTicketErrorResponse"
        "404":
          description: Project not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SupportTicketErrorResponse"
  /api/projects/{projectId}/support-tickets/unread-count:
    get:
      tags:
        - Support
      summary: Count a project’s unread open support tickets
      description: Returns the number of open tickets (new or investigating) with read_at NULL. Drives the Support sidebar badge.
      parameters:
        - schema:
            type: string
            description: Project slug or id.
          required: true
          description: Project slug or id.
          name: projectId
          in: path
      responses:
        "200":
          description: Unread ticket count.
          content:
            application/json:
              schema:
                type: object
                properties:
                  count:
                    type: integer
                    minimum: 0
                required:
                  - count
        "404":
          description: Project not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SupportTicketErrorResponse"
  /api/projects/{projectId}/support-tickets/voting:
    get:
      tags:
        - Support
      summary: List feature-request tickets ranked by vote score
      description: |-
        Returns `type=feature_request` tickets for the project (every lifecycle status), joined with vote aggregates `{score, upvotes, downvotes, myVote}` and `comment_count` (hidden comments excluded). Sorted by score DESC, then created_at DESC. Pass `voterKey` to populate `voting.myVote` for that identity; omit it and myVote is null.

        Response shape depends on the caller: interactive Hub operators (JWT/cookie session, or the local bundle) receive the full `SupportTicketVotingItem` (all ticket fields plus `voting`); external API-key callers — the global X-API-Key or a per-user `ahub_*` key (Survey Tracker) — receive the allowlisted `SupportTicketVotingItemExternal`, which strips every operator-only field.
      parameters:
        - schema:
            type: string
            description: Project slug or id.
          required: true
          description: Project slug or id.
          name: projectId
          in: path
        - schema:
            type: string
            minLength: 1
            maxLength: 256
            pattern: ^[\x21-\x7e]+$
            description: Opaque per-voter token (printable ASCII, no whitespace, 1-256 chars). One vote per (ticket, voterKey). Prefer SHA-256(server salt + lowercased email) when the caller knows the voter email; otherwise a stable device/session token. Never send a raw email.
          required: false
          description: Opaque per-voter token (printable ASCII, no whitespace, 1-256 chars). One vote per (ticket, voterKey). Prefer SHA-256(server salt + lowercased email) when the caller knows the voter email; otherwise a stable device/session token. Never send a raw email.
          name: voterKey
          in: query
      responses:
        "200":
          description: Feature-request tickets ordered by score (highest first), then newest. Hub callers get SupportTicketVotingItem; external callers get SupportTicketVotingItemExternal.
          content:
            application/json:
              schema:
                anyOf:
                  - type: array
                    items:
                      $ref: "#/components/schemas/SupportTicketVotingItem"
                  - type: array
                    items:
                      $ref: "#/components/schemas/SupportTicketVotingItemExternal"
        "400":
          description: voterKey is not a string, or exceeds 256 characters.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SupportTicketErrorResponse"
        "404":
          description: Project not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SupportTicketErrorResponse"
  /api/projects/{projectId}/support-tickets/{id}:
    get:
      tags:
        - Support
      summary: Fetch a single support ticket
      parameters:
        - schema:
            type: string
            description: Project slug or id.
          required: true
          description: Project slug or id.
          name: projectId
          in: path
        - schema:
            type: string
            description: Support ticket id.
          required: true
          description: Support ticket id.
          name: id
          in: path
      responses:
        "200":
          description: The ticket.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SupportTicket"
        "404":
          description: Project or ticket not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SupportTicketErrorResponse"
    patch:
      tags:
        - Support
      summary: Update a support ticket (status, type, severity, AI investigation, replay ref)
      parameters:
        - schema:
            type: string
            description: Project slug or id.
          required: true
          description: Project slug or id.
          name: projectId
          in: path
        - schema:
            type: string
            description: Support ticket id.
          required: true
          description: Support ticket id.
          name: id
          in: path
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                status:
                  type: string
                  enum:
                    - new
                    - investigating
                    - converted
                    - closed
                    - duplicate
                    - wont_do
                type:
                  type: string
                  enum:
                    - bug
                    - question
                    - feature_request
                    - incident
                    - other
                  description: Reclassify the ticket request type (bug, question, feature_request, incident, other).
                severity:
                  type: string
                  enum:
                    - critical
                    - high
                    - medium
                    - low
                  description: Re-rate the ticket severity (critical, high, medium, low). Drives queue ordering and the priority a converted card inherits.
                wontDoReason:
                  type: string
                  nullable: true
                  description: Reason the ticket is being marked 'wont_do'. Required (non-empty) when status is 'wont_do'; cleared on any other status transition.
                aiSummary:
                  type: string
                  nullable: true
                aiInvestigation:
                  type: string
                  nullable: true
                replayRef:
                  type: string
                  nullable: true
                screenshot:
                  type: string
                  nullable: true
                  description: Attach a screenshot (base64 data URL) or clear it with null. Persisted server-side; exposed via screenshot_ref.
              description: "Partial update: status/type/severity change and/or AI/replay/screenshot fields."
      responses:
        "200":
          description: Updated ticket.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SupportTicket"
        "400":
          description: Invalid status, type, or severity.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SupportTicketErrorResponse"
        "404":
          description: Project or ticket not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SupportTicketErrorResponse"
    delete:
      tags:
        - Support
      summary: Delete a support ticket
      parameters:
        - schema:
            type: string
            description: Project slug or id.
          required: true
          description: Project slug or id.
          name: projectId
          in: path
        - schema:
            type: string
            description: Support ticket id.
          required: true
          description: Support ticket id.
          name: id
          in: path
      responses:
        "200":
          description: Deleted.
          content:
            application/json:
              schema:
                type: object
                properties:
                  ok:
                    type: boolean
                required:
                  - ok
        "404":
          description: Project or ticket not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SupportTicketErrorResponse"
  /api/projects/{projectId}/support-tickets/read-all:
    post:
      tags:
        - Support
      summary: Mark all of a project’s tickets read
      description: Stamps read_at on every unread ticket in the project and clears the sidebar badge.
      parameters:
        - schema:
            type: string
            description: Project slug or id.
          required: true
          description: Project slug or id.
          name: projectId
          in: path
      responses:
        "200":
          description: Tickets marked read.
          content:
            application/json:
              schema:
                type: object
                properties:
                  marked:
                    type: integer
                    minimum: 0
                  unreadCount:
                    type: integer
                    minimum: 0
                required:
                  - marked
                  - unreadCount
        "404":
          description: Project not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SupportTicketErrorResponse"
  /api/projects/{projectId}/support-tickets/{id}/investigate:
    post:
      tags:
        - Support
      summary: Queue a support ticket AI investigation
      description: Queues a one-shot investigation using the project main dev agent and the authenticated caller’s default model. The updated ticket is broadcast over WebSocket when the run completes.
      parameters:
        - schema:
            type: string
            description: Project slug or id.
          required: true
          description: Project slug or id.
          name: projectId
          in: path
        - schema:
            type: string
            description: Support ticket id.
          required: true
          description: Support ticket id.
          name: id
          in: path
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties: {}
              description: No selection is required. The project main dev agent and the caller’s default model are used.
      responses:
        "202":
          description: Investigation queued.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RunSupportTicketInvestigationResponse"
        "400":
          description: Invalid engine/model or unavailable engine credentials.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SupportTicketErrorResponse"
        "404":
          description: Project or ticket not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SupportTicketErrorResponse"
  /api/projects/{projectId}/support-tickets/{id}/read:
    post:
      tags:
        - Support
      summary: Mark a support ticket read
      parameters:
        - schema:
            type: string
            description: Project slug or id.
          required: true
          description: Project slug or id.
          name: projectId
          in: path
        - schema:
            type: string
            description: Support ticket id.
          required: true
          description: Support ticket id.
          name: id
          in: path
      responses:
        "200":
          description: The updated ticket.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SupportTicket"
        "404":
          description: Project or ticket not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SupportTicketErrorResponse"
  /api/projects/{projectId}/support-tickets/{id}/unread:
    post:
      tags:
        - Support
      summary: Mark a support ticket unread
      parameters:
        - schema:
            type: string
            description: Project slug or id.
          required: true
          description: Project slug or id.
          name: projectId
          in: path
        - schema:
            type: string
            description: Support ticket id.
          required: true
          description: Support ticket id.
          name: id
          in: path
      responses:
        "200":
          description: The updated ticket.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SupportTicket"
        "404":
          description: Project or ticket not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SupportTicketErrorResponse"
  /api/projects/{projectId}/support-tickets/{id}/convert:
    post:
      tags:
        - Support
      summary: Convert a support ticket into a To Do kanban card
      description: Creates a To Do card from the ticket (title/description, severity→priority, support,<type> labels), then flags the source ticket `converted` and marks it read (it is retained, not deleted). Re-converting an already-converted ticket 409s rather than creating a duplicate card. An optional body carries an auto-merge preference and a note onto the new card.
      parameters:
        - schema:
            type: string
            description: Project slug or id.
          required: true
          description: Project slug or id.
          name: projectId
          in: path
        - schema:
            type: string
            description: Support ticket id.
          required: true
          description: Support ticket id.
          name: id
          in: path
      requestBody:
        required: false
        content:
          application/json:
            schema:
              type: object
              properties:
                autoMerge:
                  type: boolean
                  nullable: true
                  description: Per-card auto-merge preference to stamp on the new card. true → the card (and any session later assigned to it) defaults to "Auto Merge"; false → "Build and Push". Carried over to the board so the assign UI pre-populates the checkbox. Omit to leave the card with no explicit preference (project default).
                comment:
                  type: string
                  nullable: true
                  maxLength: 4000
                  description: Optional note (max 4000 characters) recorded as a comment on the new card (e.g. context for whoever picks it up).
              description: Optional auto-merge preference and note to attach to the new card.
      responses:
        "201":
          description: Ticket converted to a new card; the source ticket is retained as converted.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ConvertSupportTicketResponse"
        "404":
          description: Project or ticket not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SupportTicketErrorResponse"
        "409":
          description: Support ticket already converted.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SupportTicketErrorResponse"
        "500":
          description: Board has no columns to place the card in.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SupportTicketErrorResponse"
  /api/projects/{projectId}/support-tickets/{id}/link-card:
    post:
      tags:
        - Support
      summary: Link a support ticket to an existing kanban card
      description: "Ties the ticket to an existing card instead of creating a new one: stamps the ticket back-link on the card, records a comment preserving the ticket context + screenshot, then flags the source ticket `converted` (retained, not deleted) and marks it read. The target card must be on this project board and not already linked to a different ticket. Re-linking an already-converted ticket 409s."
      parameters:
        - schema:
            type: string
            description: Project slug or id.
          required: true
          description: Project slug or id.
          name: projectId
          in: path
        - schema:
            type: string
            description: Support ticket id.
          required: true
          description: Support ticket id.
          name: id
          in: path
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                cardId:
                  type: string
                  minLength: 1
                  description: Id of an existing kanban card on this project board to link the ticket to. Must live on the project board and not already be linked to another ticket.
                comment:
                  type: string
                  nullable: true
                  maxLength: 4000
                  description: Optional note (max 4000 characters) recorded as a comment on the linked card, above an auto-generated footer that links back to the source ticket.
              required:
                - cardId
              description: Existing card to link the ticket to, plus an optional note recorded on that card.
      responses:
        "200":
          description: Ticket linked to the existing card; the source ticket is retained as converted.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LinkSupportTicketToCardResponse"
        "400":
          description: Invalid request body.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SupportTicketErrorResponse"
        "404":
          description: Project, ticket, or target card not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SupportTicketErrorResponse"
        "409":
          description: Ticket already converted, or the card is already linked to another ticket.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SupportTicketErrorResponse"
  /api/projects/{projectId}/support-tickets/{id}/vote:
    put:
      tags:
        - Support
      summary: Cast, change, or retract a vote on a feature-request ticket
      description: "One vote per (ticket, voterKey). Sending the opposite value flips the row; `value: null` retracts. The ticket must exist and have type `feature_request`. Broadcasts `support_ticket_vote_updated` with `{ ticketId, projectId, score, upvotes, downvotes }` (no voter identity)."
      parameters:
        - schema:
            type: string
            description: Project slug or id.
          required: true
          description: Project slug or id.
          name: projectId
          in: path
        - schema:
            type: string
            description: Support ticket id.
          required: true
          description: Support ticket id.
          name: id
          in: path
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                voterKey:
                  type: string
                  minLength: 1
                  maxLength: 256
                  pattern: ^[\x21-\x7e]+$
                  description: Opaque per-voter token (printable ASCII, no whitespace, 1-256 chars). One vote per (ticket, voterKey). Prefer SHA-256(server salt + lowercased email) when the caller knows the voter email; otherwise a stable device/session token. Never send a raw email.
                value:
                  anyOf:
                    - type: number
                      enum:
                        - 1
                    - type: number
                      enum:
                        - -1
                    - nullable: true
                  description: 1 to upvote, -1 to downvote, null to retract the existing vote.
              required:
                - voterKey
                - value
              description: "Cast, change, or retract a vote on a feature-request ticket. UNIQUE(ticket, voter_key) makes the write race-safe: the same key upserts in place; null deletes the row."
      responses:
        "200":
          description: Updated aggregate for this ticket, including the caller’s vote.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SupportTicketVoteAggregate"
        "400":
          description: Invalid body, or the ticket is not a feature_request (voting is only allowed on feature requests).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SupportTicketErrorResponse"
        "404":
          description: Project or ticket not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SupportTicketErrorResponse"
  /api/projects/{projectId}/support-tickets/{id}/comments:
    get:
      tags:
        - Support
      summary: List anonymous comments on a support ticket
      description: Non-hidden comments, oldest-first. Hub-auth responses include `source` and `hidden_at`. The external projection omits `hidden_at` and never returns hidden rows. Broadcasts are not emitted on list.
      parameters:
        - schema:
            type: string
            description: Project slug or id.
          required: true
          description: Project slug or id.
          name: projectId
          in: path
        - schema:
            type: string
            description: Support ticket id.
          required: true
          description: Support ticket id.
          name: id
          in: path
      responses:
        "200":
          description: Comments in created_at order (oldest first).
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/SupportTicketComment"
        "404":
          description: Project or ticket not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SupportTicketErrorResponse"
    post:
      tags:
        - Support
      summary: Append an anonymous comment
      description: Body is `{ body, displayName? }`. `source` defaults from the caller. Broadcasts `support_ticket_comment_created` with `{ ticketId, projectId, comment }`.
      parameters:
        - schema:
            type: string
            description: Project slug or id.
          required: true
          description: Project slug or id.
          name: projectId
          in: path
        - schema:
            type: string
            description: Support ticket id.
          required: true
          description: Support ticket id.
          name: id
          in: path
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                body:
                  type: string
                  minLength: 1
                  maxLength: 4000
                  description: Comment body (max 4000 characters).
                displayName:
                  type: string
                  maxLength: 80
                  description: Optional free-text display name. Not a user id; omitted comments are anonymous.
              required:
                - body
              description: Append an anonymous comment. `source` is derived from the caller (Hub UI → hub, API-key-only → external) and is not accepted in the body.
      responses:
        "201":
          description: Created comment row.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SupportTicketComment"
        "400":
          description: Invalid body (empty, or longer than 4000 characters).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SupportTicketErrorResponse"
        "404":
          description: Project or ticket not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SupportTicketErrorResponse"
  /api/projects/{projectId}/support-tickets/{id}/comments/{commentId}:
    delete:
      tags:
        - Support
      summary: Hide a comment (operator soft-delete)
      description: Hub-auth only. Sets `hidden_at` so subsequent lists skip the row. External (API-key-only) callers receive 403. Broadcasts `support_ticket_comment_deleted` with `{ ticketId, projectId, commentId }`.
      parameters:
        - schema:
            type: string
            description: Project slug or id.
          required: true
          description: Project slug or id.
          name: projectId
          in: path
        - schema:
            type: string
            description: Support ticket id.
          required: true
          description: Support ticket id.
          name: id
          in: path
        - schema:
            type: string
            description: Comment id.
          required: true
          description: Comment id.
          name: commentId
          in: path
      responses:
        "200":
          description: Hidden.
          content:
            application/json:
              schema:
                type: object
                properties:
                  ok:
                    type: boolean
                required:
                  - ok
        "403":
          description: Caller is not a Hub operator (API-key-only / external).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SupportTicketErrorResponse"
        "404":
          description: Project, ticket, or comment not found, or already hidden.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SupportTicketErrorResponse"
  /api/support-tickets:
    get:
      tags:
        - Support
      summary: List support tickets across all projects, ordered by severity
      description: Aggregates every project’s support tickets into one severity-ordered list (critical → low, then newest). Optional `status`, `projectId`, and `unread` query filters compose and are applied server-side.
      parameters:
        - schema:
            type: string
            description: "Filter to one or more lifecycle states, comma-separated (e.g. `new,investigating`). Absent → every status. Each token must be one of: new, investigating, converted, closed, duplicate, wont_do."
            example: new,investigating
          required: false
          description: "Filter to one or more lifecycle states, comma-separated (e.g. `new,investigating`). Absent → every status. Each token must be one of: new, investigating, converted, closed, duplicate, wont_do."
          name: status
          in: query
        - schema:
            type: string
            description: Scope to a single project (404 if unknown).
          required: false
          description: Scope to a single project (404 if unknown).
          name: projectId
          in: query
        - schema:
            type: string
            enum:
              - "0"
              - "1"
              - "true"
              - "false"
            description: When `true`/`1`, keep only unread tickets (read_at IS NULL) — the dashboard "needs triage" view.
          required: false
          description: When `true`/`1`, keep only unread tickets (read_at IS NULL) — the dashboard "needs triage" view.
          name: unread
          in: query
      responses:
        "200":
          description: Tickets (each with project_name) ordered by severity, plus project filter options.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SupportOverviewResponse"
        "400":
          description: Invalid status or unread filter value.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SupportTicketOverviewErrorResponse"
        "404":
          description: Project not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SupportTicketOverviewErrorResponse"
  /api/threads/{threadId}/entries/{entryId}/forward:
    post:
      tags:
        - Threads
      summary: Forward a single thread entry to an agent
      description: Creates a new session for the target agent seeded with the given thread entry as the initial user message. The new session is owned by the caller.
      parameters:
        - schema:
            type: string
            description: Thread UUID.
          required: true
          description: Thread UUID.
          name: threadId
          in: path
        - schema:
            type: string
            description: Thread entry UUID to forward.
          required: true
          description: Thread entry UUID to forward.
          name: entryId
          in: path
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ForwardThreadEntryRequest"
      responses:
        "201":
          description: Forwarded session created.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ForwardThreadEntryResponse"
        "400":
          description: Validation failed.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ThreadsErrorResponse"
        "404":
          description: Thread, entry, or target agent not found / not visible.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ThreadsErrorResponse"
        "503":
          description: Auto-start requested but chat handler unavailable.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ThreadsErrorResponse"
  /api/upload:
    post:
      tags:
        - Uploads
      summary: Upload a data-URL attachment
      description: Validates and stores a base64 data URL. AWS deployments use the configured artifact S3 bucket; local installs use the uploads directory. The returned `/uploads` ref is backend-independent.
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                dataUrl:
                  type: string
                  minLength: 1
                filename:
                  type: string
                  minLength: 1
              required:
                - dataUrl
                - filename
      responses:
        "200":
          description: Attachment stored.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UploadResponse"
        "400":
          description: Invalid or unsafe attachment.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UploadErrorResponse"
        "500":
          description: Storage write failed.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UploadErrorResponse"
  /api/upload/file:
    post:
      tags:
        - Uploads
      summary: Upload a raw binary attachment
      description: Stores a raw attachment body up to 100 MB. AWS deployments use the configured artifact S3 bucket; local installs use the uploads directory.
      parameters:
        - schema:
            type: string
          required: false
          name: x-filename
          in: header
        - schema:
            type: string
          required: false
          name: content-type
          in: header
      requestBody:
        content:
          application/octet-stream:
            schema:
              type: string
              format: binary
              description: Raw attachment bytes.
      responses:
        "200":
          description: Attachment stored.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UploadResponse"
        "400":
          description: Empty, invalid, or unsafe attachment.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UploadErrorResponse"
        "413":
          description: Attachment exceeds 100 MB.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UploadErrorResponse"
        "500":
          description: Storage write failed.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UploadErrorResponse"
  /api/projects/{projectId}/wiki:
    get:
      tags:
        - Wiki
      summary: List wiki pages (optionally filtered or FTS-searched)
      description: When `?q=` is set, returns FTS5 hits (`WikiSearchHit[]` — list-item fields + `snippet` + `rank`, no `content`). When `?category=` is set, returns pages in that category (`WikiPageListItem[]`, no `content`). Otherwise returns every page in the project (`WikiPageListItem[]`, no `content`). `limit` only applies to the `?q=` path; the bare and category responses are unpaginated. Invalid `?limit=` values (non-numeric) and other schema violations return 400.
      parameters:
        - schema:
            type: string
            description: Project ID (slug).
          required: true
          description: Project ID (slug).
          name: projectId
          in: path
        - schema:
            type: string
          required: false
          name: q
          in: query
        - schema:
            type: string
          required: false
          name: category
          in: query
        - schema:
            type: integer
            minimum: 0
            exclusiveMinimum: true
          required: false
          name: limit
          in: query
      responses:
        "200":
          description: Wiki pages (shape depends on the query params — see description).
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/WikiPageListItem"
        "400":
          description: Query parameter validation failed.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/WikiErrorResponse"
        "404":
          description: Project not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/WikiErrorResponse"
    post:
      tags:
        - Wiki
      summary: Create a wiki page
      description: Slug is derived from `title` via lowercase + non-alphanumeric squash. Returns 409 when the resulting slug collides with an existing page on the same project.
      parameters:
        - schema:
            type: string
            description: Project ID (slug).
          required: true
          description: Project ID (slug).
          name: projectId
          in: path
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                title:
                  type: string
                  minLength: 1
                content:
                  type: string
                category:
                  type: string
                updatedBy:
                  type: string
              required:
                - title
      responses:
        "201":
          description: New page.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/WikiPage"
        "400":
          description: Validation failed.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/WikiErrorResponse"
        "404":
          description: Project not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/WikiErrorResponse"
        "409":
          description: Slug collision.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/WikiErrorResponse"
  /api/projects/{projectId}/wiki/search:
    get:
      tags:
        - Wiki
      summary: Search wiki pages (hybrid / semantic / fts)
      description: Defaults to hybrid mode. Falls back to pure FTS when the Gemini API key is missing or the query embedding fails. Returns an empty `results` array for blank queries so the caller can branch on configuration without a separate health probe. Invalid `?limit=` values (non-numeric) and unknown `?mode=` values return 400.
      parameters:
        - schema:
            type: string
            description: Project ID (slug).
          required: true
          description: Project ID (slug).
          name: projectId
          in: path
        - schema:
            type: string
          required: false
          name: q
          in: query
        - schema:
            type: string
            enum:
              - hybrid
              - semantic
              - fts
          required: false
          name: mode
          in: query
        - schema:
            type: integer
            minimum: 0
            exclusiveMinimum: true
          required: false
          name: limit
          in: query
      responses:
        "200":
          description: Search envelope.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/WikiSearchResponse"
        "400":
          description: Query parameter validation failed.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/WikiErrorResponse"
        "404":
          description: Project not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/WikiErrorResponse"
        "500":
          description: Underlying search or embedding error.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/WikiErrorResponse"
  /api/projects/{projectId}/wiki/reembed:
    post:
      tags:
        - Wiki
      summary: Re-embed every page in the project
      description: "Idempotent: re-runs the chunk + embed pipeline for every page in the project. Useful after provisioning the Gemini API key for the first time or after changing the embedding model."
      parameters:
        - schema:
            type: string
            description: Project ID (slug).
          required: true
          description: Project ID (slug).
          name: projectId
          in: path
      responses:
        "200":
          description: Backfill report.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/WikiBackfillResult"
        "404":
          description: Project not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/WikiErrorResponse"
        "500":
          description: Backfill failed.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/WikiErrorResponse"
        "503":
          description: Gemini API key not configured.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/WikiErrorResponse"
  /api/projects/{projectId}/wiki/document-backfill:
    post:
      tags:
        - Wiki
      summary: Start an on-demand wiki documentation backfill
      description: Spawns the project docs agent to review the oldest undocumented Done cards. This is not a scheduled drain; call it when an operator asks to backfill. New work is documented automatically when a PR merges (if a docs agent exists and the linked card is still undocumented). `documented = 1` means reviewed for the wiki, not that the card has its own article.
      parameters:
        - schema:
            type: string
            description: Project ID (slug).
          required: true
          description: Project ID (slug).
          name: projectId
          in: path
      requestBody:
        required: false
        content:
          application/json:
            schema:
              type: object
              properties:
                limit:
                  type: integer
                  minimum: 1
                  maximum: 25
                  description: Max undocumented Done cards to hand the docs agent this run (default 10, cap 25). Oldest first. The agent writes at most one page then stops; remaining cards in the batch are skip-marked when trivial.
      responses:
        "200":
          description: Existing backfill session reused, or nothing left to review.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/WikiDocumentBackfillResult"
        "201":
          description: Docs session started.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/WikiDocumentBackfillResult"
        "400":
          description: Validation failed (invalid limit).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/WikiErrorResponse"
        "404":
          description: Project or docs agent not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/WikiErrorResponse"
        "409":
          description: Backfill skipped for another reason.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/WikiErrorResponse"
  /api/projects/{projectId}/wiki/categories:
    get:
      tags:
        - Wiki
      summary: List known wiki categories
      description: Returns the in-code list of categories (`general`, `api-docs`, `architecture`, `conventions`, `test-patterns`, `troubleshooting`, `onboarding`). The persistence layer accepts any string today; this list is purely a UI hint.
      parameters:
        - schema:
            type: string
            description: Project ID (slug).
          required: true
          description: Project ID (slug).
          name: projectId
          in: path
      responses:
        "200":
          description: Known categories.
          content:
            application/json:
              schema:
                type: array
                items:
                  type: string
                  enum:
                    - general
                    - api-docs
                    - architecture
                    - conventions
                    - test-patterns
                    - troubleshooting
                    - onboarding
  /api/projects/{projectId}/wiki/{slug}:
    get:
      tags:
        - Wiki
      summary: Get a single wiki page by slug
      parameters:
        - schema:
            type: string
            description: Project ID (slug).
          required: true
          description: Project ID (slug).
          name: projectId
          in: path
        - schema:
            type: string
            description: Wiki page slug (URL-safe).
          required: true
          description: Wiki page slug (URL-safe).
          name: slug
          in: path
      responses:
        "200":
          description: Page row.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/WikiPage"
        "404":
          description: Page not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/WikiErrorResponse"
    put:
      tags:
        - Wiki
      summary: Update a wiki page
      description: Partial update. Omitted fields keep their current value. Renaming `title` may change the slug; collisions return 409.
      parameters:
        - schema:
            type: string
            description: Project ID (slug).
          required: true
          description: Project ID (slug).
          name: projectId
          in: path
        - schema:
            type: string
            description: Wiki page slug (URL-safe).
          required: true
          description: Wiki page slug (URL-safe).
          name: slug
          in: path
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                title:
                  type: string
                  minLength: 1
                content:
                  type: string
                category:
                  type: string
                updatedBy:
                  type: string
      responses:
        "200":
          description: Updated page.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/WikiPage"
        "400":
          description: Validation failed.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/WikiErrorResponse"
        "404":
          description: Page not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/WikiErrorResponse"
        "409":
          description: Slug collision after rename.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/WikiErrorResponse"
    delete:
      tags:
        - Wiki
      summary: Delete a wiki page
      parameters:
        - schema:
            type: string
            description: Project ID (slug).
          required: true
          description: Project ID (slug).
          name: projectId
          in: path
        - schema:
            type: string
            description: Wiki page slug (URL-safe).
          required: true
          description: Wiki page slug (URL-safe).
          name: slug
          in: path
      responses:
        "200":
          description: Acknowledgment.
          content:
            application/json:
              schema:
                type: object
                properties:
                  ok:
                    type: boolean
                    enum:
                      - true
                required:
                  - ok
        "404":
          description: Page not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/WikiErrorResponse"
