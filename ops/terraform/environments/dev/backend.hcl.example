# S3 backend for the `dev` environment (personal dev sandbox).
# Copy to backend.hcl (a gitignored local overlay) and fill in the
# account-specific bucket. The live file is never committed — CI passes the
# same values inline from GitHub Variables (TF_STATE_BUCKET /
# TF_STATE_LOCK_TABLE / AWS_REGION).
#
#   cp environments/dev/backend.hcl.example environments/dev/backend.hcl
#   AWS_PROFILE=<profile> ./scripts/tf-init.sh dev

bucket  = "agent-hub-tfstate-<DEV_ACCOUNT_ID>"
key     = "dev/terraform.tfstate"
region  = "us-east-2"
encrypt = true

dynamodb_table = "agent-hub-tfstate-lock"
